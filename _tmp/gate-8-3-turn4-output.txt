Gate 8.3 turn 4 is closed. The admitted idempotency read is now bound evidence, not a free-floating answer.

## Fix

`WorthQueryAdmittedIdempotencyRead` now carries the `WorthQueryApplicationIdempotencyBinding` it was read for, and no longer derives `Clone`. `resolve_recovery_handle` denies with the new `ForeignIdempotencyRead` when the read’s binding is not the handle’s — distinct from `IdempotencyMismatch` on the handle axis. Tests: foreign-read denial + matching twin.

## Ledger

Gate 8.3 marked **CLOSED** in `milestone-9.16-runtime-phase-8-closure-ledger.md`, including R8.63 (first gate to update that ledger at its own closure). Q8.3 left as dated **PARTIAL**.

## Verification (named targets)

| Target | Result |
|---|---|
| `cargo test -p bank-server --test ordinary_mutations` | **49 passed** |
| `cargo test -p worth-query --test installed_operating_world` | **313 passed** |
| `cargo test -p worth-query --test public_declarative_journeys` | **37 passed** |
| `cargo test -p worth-query --test runtime_public_journeys` | **22 passed** |
| `cargo test -p worth-query-certification --test compile_certification` | **14 passed** |
| `cargo test -p worth-query-execution --lib` | **549 passed** |
| `boundary-check` / `agent-context check` | **exit 0** |
| `check_workspace_rust_line_caps.sh dirty` | **PASS** |
| `RUSTFLAGS=-Dwarnings` check on execution + bank-server | **warning-clean** |

Gate 8.4 not started.
