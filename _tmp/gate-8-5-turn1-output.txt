**Gate 8.5 is closeable.** Fresh redo intent and linear lineage landed; the type/policy centre holds on the signatures, not only the tests.

### R8.45 answers (by signature)

1. **Invalidation lives on the linear lane**, in `WorthQueryLinearLineageChain::evaluate_divergence`, consulted by `admit_redo`. `WorthQueryRedoIntent` only stores `bound_linear_head` — no live-head validity method.
2. **A 9.18 rebasing lane can reuse `WorthQueryRedoIntent` unchanged.** Real API: `derive(proved, bound_linear_head)`. Divergence policy is not on the type. `WorthQueryAftermathParentCausalityEdge` is parent→child only (no empty branch placeholder).

### What landed

- `redo_intent` / `redo_admission` / `redo_denial` / `redo_progression` / `linear_lineage`
- Bank `admit_redo_*` / `progress_redo_*`; lineage append on original/undo/redo commits
- Eight distinct causes through production paths (not enum dedup); divergence proved for ordinary intervening **and** intervening redo (unit also covers intervening undo)
- Replay residue check on ordinary redo modules
- R8.64: `redo_through_undo_handle_rail_and_aftermath`
- Closure ledger updated (R8.63)

### Standing verification

| Check | Result |
|---|---|
| bank `ordinary_mutations` | **70** |
| consumers | **313 / 37 / 22** |
| `compile_certification` | **14** |
| execution `--lib` × 5 | **578 × 5** |
| `-Dwarnings` check | clean |
| boundary-check / agent-context | exit 0 |

**Residual:** no disbursement clock-expiry twin for `Stale` after proved undo (exact 8.4 A10 shape); `Stale`/`NewlyUnauthorized` are covered by production `map_recovery_denial` on the bank path and foreign/terminal admits. Gate 8.6 not started.

Details: `_tmp/gate-8-5-boundary-and-plan.md`, `_tmp/gate-8-5-turn-1-status.md`.Those background checks all finished green: bank `ordinary_mutations` **70**, execution `--lib` **578**, `compile_certification` **14**, and warning-clean builds with `agent-context` exit 0. They match what was already recorded for Gate 8.5 turn 1.
