Phase 8 gap-closure is done. All four recorded gaps are **CLOSED**; zero deliberate carries remain.

### Closed
| Item | What landed |
|---|---|
| **PB1** | Platform slot renamed `Currency` → `Unit` (`ApplicationFieldUnit`, `NoApplicationUnit`, `ApplicationUnitRef`, `worth_query_unit!`). Bank finance `trait Currency` / `Money` / `UsdCurrency` kept. |
| **PB2** | `ApplicationCapabilityMagnitudeDimension` + `magnitude` / `magnitude_value`. Bank money `.amount()` kept. |
| **PB4** | All ordinary-branch sites route through `application_branch`; residue test forbids `BranchId("main")` elsewhere. |
| **Q8.3** | Four successor postures now carry `ExternalEffectPostureEvidence` (same treatment as Compensation/Reconciliation). R8.22 proved on unrepresentability. |
| **§11 row 11** | Handle non-leak kept; session/queue evidenced as unreachable — recovery handle owns neither, and `bank-user-node` does not exist. |

### Standing verification
| Target | Result |
|---|---|
| `ordinary_mutations` | **81** (was 80; +row-11 boundary test) |
| `installed_operating_world` | **313** |
| `public_declarative_journeys` | **37** |
| `runtime_public_journeys` | **22** |
| `compile_certification` | **14** |
| `worth-query-execution --lib` × 5 | **579 × 5** (was 578; +PB4 residue) |
| warnings / boundary-check / agent-context / line-cap | clean |

Ledgers updated: `milestone-9.16-runtime-phase-8-closure-ledger.md` and bank `front-door-closure-ledger.md`.Those are stale notifications from the verification pass mid-slice.

Early failures (bank compile, certification via wrong manifest, `--lib` import warning) were fixed in-session. Final standing set is green: **81 / 313 / 37 / 22 / 14**, and `--lib` **579 × 5**.
