**Gate 8.3 is not closed.** A vertical slice landed and Gate 8.1/8.2 stayed green; the exit-proof matrix is still incomplete.

### Landed
- **C1 / R8.62** — `WorthQueryApplicationCommitReceipt` now carries installed operation, admitted principal scope, and admitted idempotency binding, derived from admission (no public ctor, no defaults).
- **Linear handle** — move-only `WorthQueryRecoveryHandle` (no `Clone`/`Copy`), secret-ordinal opaque identity, managed-run-family registry (enumerate / force-terminate).
- **Six transitions** as separate functions; compensate keys `Compensation`; reconcile keys `RuntimeWithExternalOwner`.
- **Fresh authority** checks all eleven axes with distinct denial kinds; inspect requires disclosure; current-policy denial after mint proved.
- **R8.33/34/35/12** — inspect-twice at 0/0/0; wire/support/posture cannot readmit; `StoreCapabilityRequired` + `DegradedRecoveryReport`.
- **R8.64** — `phase8_cross_gate` lost-response scenario through the real rail + aftermath, with account-revision oracle.
- **Q8.3** — Compensation/Reconciliation require `ExternalEffectPostureEvidence` (residual: earlier ladder postures still constructible inside `external_effect`).

### Still open (blocks gate closure)
- Per-axis drift attacks with positive twins  
- Already-completed resolve twin through admitted graph read  
- Expiry M2/M3 adversarial proof (`evaluate_expiry` exists, untested)  
- Compile-fail for cloned handle / duplicate transition  
- Foreign principal / runtime / branch-equal-ordinal scenarios  
- Leak detection across all four terminal paths  

### Verification run
- Gate 8.1 aftermath: pass  
- Gate 8.2 external_effect + bank rail + undeclared: pass  
- `phase8_cross_gate`: 4 pass  
- `boundary-check`, `agent-context`: pass  
- Dirty line-cap: all ≤ 400  

Plan: `_tmp/gate-8-3-boundary-and-plan.md`. Ledger updated honestly under Gate 8.3 as **OPEN**.That verification batch exited 1 mainly because `bash` wasn’t available for the line-cap script and one cargo filter matched no tests. The important follow-ups were re-run separately afterward: Gate 8.1/8.2 suites, `phase8_cross_gate`, `boundary-check`, and `agent-context` all passed; dirty Rust files were within the 400-line cap.
