Gate 8.6 turn 3 is done; **Phase 8 is CLOSED**.

### What closed the hole
Gate 8.2 marked O2 proved on O1 evidence. Turn 3 adds a real mutation-free path:

- **Bank `RetransmitDeathNotice`** — emit-only (no domain field writes), death-notice rail, NotifyDeath-style aftermath
- **Query substrate** — empty provisional programs and empty invariant state loads are lawful for emit-only commits (still deny empty load against a non-empty plan)
- **E2e** `mutation_free_external_effect::mutation_free_external_effect_co_commits_outbox_recovers_lost_response_once` via real `bank-external-rail`: outbox co-commits, status unchanged, lost-response → `AlreadyCommitted`, rail attempts == 1

### Ledger
- **Q8.11 CLOSED** (Gate 8.2 origin)
- R8.25 / R8.55 evidence and courtroom **row 14** name the new test
- Row 11 left as the Bank Phase 5 honest gap

### Count note
`changed_record_count() == 0` is unreachable: first commit always includes Query scaffolding. Mutation-free asserts **count == 2** (idempotency + outbox) with unchanged domain status.

### Standing verification
| Target | Result |
|---|---|
| `ordinary_mutations` | **80** |
| `installed_operating_world` | **313** |
| `public_declarative_journeys` | **37** |
| `runtime_public_journeys` | **22** |
| `compile_certification` | **14** |
| `--lib` × 5 | **578 × 5** |
| warnings / boundary-check / agent-context / dirty line-cap | clean / exit 0 / PASS |

Sole deliberate carry remains **Q8.3** (Runtime Hardening Track → Phase 9). Status: `_tmp/gate-8-6-turn-3-status.md`.Bank-domain inventory tests finished clean (`estate_aftermath_contract`, `estate_schema_inventory`, `schema_inventory`, `estate_capability_installation`), and the new retransmit / O2 files are under the 400-line cap (270 / 283). That matches the Phase 8 close already recorded.
