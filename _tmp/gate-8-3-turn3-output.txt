Gate 8.3 turn 3 is complete. Exit matrix is filled; awaiting closeout audit.

## What changed

1. **400-line cap** — Split `admitted_operation` (accessors → `access.rs`) and `compare_and_commit` (receipt / outcome modules). All dirty Rust files ≤ 400.

2. **Gate 8.1 corrective (Q8.6)** — Retired orphaned `query_effect_lifecycle_authority()` (no successor; recovery uses `WorthQueryRecoveryEffectAuthority`). Re-blessed `ordinary_facade_cannot_import_replay_capability.stderr` after confirming **E0432** for the right reason (CompensationCapability suggestion gone).

3. **R8.32 resolve binding** — `resolve_admitted_application_idempotency` now returns privately-minted `WorthQueryAdmittedIdempotencyRead`. `resolve_recovery_handle` demands that proof; callers cannot supply a bare taxonomy value.

4. **Adversarial suite** — 11 per-axis drift+twin tests; T4b (`RuntimeWithExternalOwner`+`RecordedInverse`); grant-expiry → `CurrentPolicyDenied` ≠ `ForeignPrincipal`; clock-advanced `Expired` terminal; four-terminal leak paths; R8.33 mint/lookup/provider/inspect exact **0/0/0**.

5. **Warning-clean** build on touched packages.

## Verification (named targets)

| Target | Result |
|---|---|
| `cargo test -p worth-query-execution --lib binding_axis` | **11 passed** |
| `cargo test -p worth-query-execution --lib recovery_registry_enumerates` | **1 passed** |
| `cargo test -p bank-server --test ordinary_mutations` | **49 passed** (was 42) |
| `cargo test -p bank-server --test ordinary_mutations phase8_` | **13+ phase8 tests** |
| `cargo test -p worth-query --test installed_operating_world` | **313 passed** |
| `cargo test -p worth-query --test public_declarative_journeys` | **37 passed** |
| `cargo test -p worth-query --test runtime_public_journeys` | **22 passed** |
| `cargo test -p worth-query-certification --test compile_certification` | **14 passed** |
| `cargo test -p worth-query-certification --test application_aftermath_compile_fail` | **passed** |
| `boundary-check` / `agent-context check` | **exit 0** |
| Dirty line-cap (PowerShell count) | **all ≤ 400** |
| `cargo check -p worth-query-execution -p worth-query` / `bank-server` | **warning-clean** |

Ledger updated: Gate 8.3 R8.28–R8.33 + exit proof marked **PROVED**; Q8.6 **CLOSED**.The deferred enforcement check finished cleanly: all dirty Rust files are ≤ 400 lines, and both `agent-context check` and `boundary-check` exited 0.
