cursor-agent.cmd : Connection lost, reconnecting to https://agentn.global.api5.cursor.sh (attempt 1)...
At line:2 char:1
+ & "$env:LOCALAPPDATA\cursor-agent\cursor-agent.cmd" --model cursor-gr ...
+ ~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~
    + CategoryInfo          : NotSpecified: (Connection lost... (attempt 1)...:String) [], RemoteException
    + FullyQualifiedErrorId : NativeCommandError
 
Retry attempt 1...
## Stage 4 — Gate 8.7 report

### Boundary reviewed
Safe-retry admitted a transition the runtime never performed. `dispatch_external_effect` had one post-commit call site; the binding carried correlation identity only (could not form a dispatch request); Bank had no `safe_retry` consumer. Aftermath stays Signal/Bridge-free; re-dispatch is a host-port operation on the installed transport.

### Slice built
**R8.66–R8.71 / Q8.13** — safe-retry re-dispatch through production admission.

### Material changes
| Area | Change |
|---|---|
| Binding (R8.68) | `WorthQueryRecoveryHandleBinding` carries `dispatch_outbox`; `correlation()` preserved |
| Runtime (R8.67) | `redispatch_admitted_external_effect` beside post-commit dispatch; sole second caller of `dispatch_external_effect` |
| Proof (R8.66) | `WorthQueryAdmittedExternalRedispatch` privately minted; `safe_retry` requires it |
| Bank (Q8.13) | `safe_retry_commit_recovery` — authority → redispatch → transition |
| Durability (R8.71) | `WorthQueryDispatchOutboxDurabilityPosture::StoreCapabilityRequired` on admission |
| Rail (R8.70) | Idempotent re-dispatch of ledgered correlations + `admission_count` inquiry |
| Tests | §11 rows 16–18, denials (rail untouched), R8.4 twin, compile-fail + pass twin |

### Cutover
No competing path: safe-retry cannot run without a real redispatch proof; original receipt dispatch cannot be forged into that proof.

### Verification (by target)

| Target | Result |
|---|---|
| `cargo test -p bank-server --test ordinary_mutations` | **ok** (87 passed) |
| `phase8_safe_retry` (6 scenarios) | **ok** |
| `cargo test -p worth-query-certification --test compile_certification` | **ok** |
| `cargo test -p worth-query-certification --test application_aftermath_compile_fail` | **ok** |
| `cargo test -p worth-query-execution --lib` ×5 | **ok** each (579 passed / run) |
| `boundary-check --root .` | **ok** |
| `agent-context check` | **ok** |
| `check_workspace_rust_line_caps.sh dirty` | **PASS** |
| `RUSTFLAGS=-Dwarnings cargo check` (touched crates) | **ok** |
| `installed_operating_world` | **ok** |
| `public_declarative_journeys` | **ok** |
| `runtime_public_journeys` | **ok** |
| `cargo test --workspace --no-fail-fast` (worth-query) | **ok** |
| `cargo test --workspace --no-fail-fast` (bank-world) | **ok** (after clearing leaked `RUSTFLAGS`; first attempt failed only because `-Dwarnings` poisoned the env) |
| `r8_11_no_signal_decision_classifies_aftermath` | **ok** |

### Remaining
- **Q8.14** (crash-recovery outbox drain) — deferred to Store; typed as `StoreCapabilityRequired`, not built.
- Ledger rows R8.66–R8.71 / Q8.13 are implementation-closed; **I did not edit `_docs/`** (brief forbids it) — an auditor or follow-on must mark the ledger PROVED/CLOSED.
- Pre-existing warning: unused `undo` field in `phase8_redo_support.rs` (untouched).Gate 8.7 verification is complete. Focused and workspace suites are green, including the bank-world workspace re-run after clearing a leaked `RUSTFLAGS=-Dwarnings` that had falsely failed the first attempt.
