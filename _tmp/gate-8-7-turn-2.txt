## Gate 8.7 Turn 2 — closed

Corrective edits for F1–F4 were already in the tree; confirmed against the brief and re-ran the full standing set.

### Fixes
- **F1:** `redispatch_admitted_external_effect` takes handle + authority; `require_fresh_effect_authority` first; outbox from `handle.binding()`. Compile-fail: `redispatch_requires_handle_and_authority`.
- **F2:** `lost_response_after_commit_safe_retry_emits_nothing_twice` — `CommitThenLoseResponse` → `Unresolved` / `admission_count==1` → retry still 1 / `Completed`.
- **F3:** `admitted_external_redispatch_constructor_is_private` (struct literal + `mint`).
- **F4:** Foreign denial pinned to `CapabilityGrantMissing` (grant missing before binding-axis `ForeignPrincipal`).
- Dropped `_private` on the redispatch proof. Kept outbox equality in `safe_retry` — still blocks cross-handle proof swap.

### Verification
| Target | Result |
|---|---|
| `ordinary_mutations` | **88 passed** (was 87; **+1** lost-response retry) |
| `application_aftermath_compile_fail` | ok |
| `compile_certification` | ok (14) |
| `worth-query-execution --lib` ×5 | **579** each |
| boundary-check / agent-context / line-caps dirty | ok / ok / PASS |
| `RUSTFLAGS=-Dwarnings` check | ok |
| journeys (313 / 37 / 22) | ok |
| both workspace `--no-fail-fast` | ok |

### Remaining
Q8.14 still deferred (`StoreCapabilityRequired`). No `_docs/` edits. Pre-existing unused `undo` warning in untouched `phase8_redo_support.rs`.Both workspace suites finished green: `worth-query` and `worth-query-bank-world` `--no-fail-fast` each exited 0.
