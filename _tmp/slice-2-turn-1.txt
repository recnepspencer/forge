cursor-agent.cmd : Connection lost, reconnecting to https://agentn.global.api5.cursor.sh (attempt 1)...
At line:3 char:1
+ & "$env:LOCALAPPDATA\cursor-agent\cursor-agent.cmd" --model cursor-gr ...
+ ~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~
    + CategoryInfo          : NotSpecified: (Connection lost... (attempt 1)...:String) [], RemoteException
    + FullyQualifiedErrorId : NativeCommandError
 
Retry attempt 1...
## Slice 2 complete — Q8.20 closed

Recovery effect/inspect authority now carries a `worth-proof` `AssumptionBasis` of the runtime's `WorthQueryRuntimeAuthorityIdentity`. Slot ordinal comparison is gone. Cross-runtime slot-1 substitution is denied; consumers cannot mint the authority.

### 1. What changed

- Added `worth-proof` to `worth-query-execution` (edge allowed; `boundary-check` did not deny it).
- `WorthQueryRecoveryEffectAuthority` / `WorthQueryRecoveryInspectAuthority` wrap `AssumptionBasis<WorthQueryRuntimeAuthorityIdentity>`; `pub(crate) mint`; dropped `_private: ()`.
- Registry stamped with runtime identity at install (`for_runtime`); slots remain instance-local membership only (`next_slot: 1` kept, documented).
- `ensure_for` compares basis to `handle.runtime_authority()` — no slot check.
- Adversarial unit test:## Slice 2 complete: recovery authority is runtime-instance-bound via `worth-proof`

### 1. What changed

- Added `worth-proof` to `worth-query-execution`.
- `WorthQueryRecoveryEffectAuthority` / `WorthQueryRecoveryInspectAuthority` now carry `AssumptionBasis<WorthQueryRuntimeAuthorityIdentity>` (owner-private `pub(crate)` mint). Dropped slot ordinal + `_private: ()`.
- `ensure_for` compares that basis to the handle’s registry runtime identity — bare slot compare removed (R8.0).
- Registry is stamped at install with `for_runtime(authority_identity)`; slots remain local membership only (`next_slot: 1` kept, documented as non-authority).
- Admit paths mint from `self.runtime.authority_identity()` and deny foreign-handle registries before mint.
- Adversarial test: two slot-1 handles on different registries; A’s authority admits A, denies B.
- Compile-fail: consumers cannot struct-literal or call `mint` (privacy errors).

### 2. `worth-proof` surfaces used / not used

| Used | Deliberately not used |
|---|---|
| `AssumptionBasis` — value-carrying runtime identity | `AuthorityWitness` / `Proof` as identity (ZST trap: no A≠B; `from_authority_marker` is public and discards its arg) |
| Owner-specific wrapper (README **Owner-Specific Runtime Types**) | Pleasant recipe lane / `TransitionOutcome` (later slices) |

### 3. What a caller can still supply

Authority object (runtime-minted) + handle/target identity via existing transition params. Callers cannot mint these authorities, forge the basis into them, or open the door with a bare `AuthorityMarker`.

### 4. §1a rungs

| Guarantee | Rung | Why not higher |
|---|---|---|
| Consumer cannot construct authority | **2** | `pub(crate)` mint + private fields; compile-fail |
| Cross-runtime authority at colliding slot 1 denies | **3** | Runtime basis compare |
| Slot ordinal is not authority | **1** (deleted door) | Parameter/field removed |

**Rung 1 brand considered and rejected:** a per-instance type parameter would need generative brands for dynamically constructed runtimes. Query creates many runtime values at process lifetime; branding them as distinct types is not feasible without ceremony the platform does not use. Rung 3 on a process-unique `WorthQueryRuntimeAuthorityIdentity` basis is the honest rung here.

### Verification (exit codes)

| Check | Exit |
|---|---|
| `cargo fmt --all --check` (worth-query) | 0 |
| `cargo fmt --all --check` (bank-world) | 0 |
| `application_aftermath_compile_fail` | 0 |
| `compile_certification` | 0 |
| `worth-query-execution --lib` ×5 | 0,0,0,0,0 |
| `bank-server --test ordinary_mutations` | 0 |
| `installed_operating_world` / `public_declarative_journeys` / `runtime_public_journeys` | 0 |
| `RUSTFLAGS=-Dwarnings cargo check` both workspaces | 0 |
| `agent-context check` | 0 |
| `boundary-check` | **1** — pre-existing `BC5004` (`.claude/settings.json` hooks) + `BC6001` (legacy `forge-` in `worth-proof` docs). **Did not deny** `worth-query-execution → worth-proof`. |

### Out of scope (noticed, not fixed)

`recovery_target`/`aftermath` params (slice 3); handle typestate/freshness (4); linearity (5); undo/redo; lineage; transport; retention.Formatting finished cleanly: both workspaces pass `cargo fmt --all --check`. No further action from that run.
