# 2026-04-25

- Forge signal Milestone B Phase 1 implementation: added resource-node contract vocabulary and runtime-owned resource declaration lowering. Kept the domain boundary narrow: no host-owned async state, no completion admission yet, no god files.
- Important law reinforced in code and tests: resource request identity is distinct from signal node identity, lifecycle truth is distinct from output continuity, and facade summaries/proofs have private fields for compile-time enforcement.
- Next Milestone B implementation batch added runtime request admission: declared resource nodes can admit runtime-issued pending requests, in-flight records are hot-keyed by request id, repeated admission supersedes the prior active request for that node, and sealed compile-fail fixtures protect lifecycle transition/admission reports.
- QA loop on Milestone B foundation found and fixed weak precedent: removed caller-supplied generation from request intent, made declaration/supersession transitions explicit in reports, made resource branch restore bump/rekey in-flight handles by restore epoch, required full-handle in-flight lookup instead of request-id-only lookup, and corrected lifecycle transition performance counts.
- Next Milestone B slice implemented scalar completion envelope validation/admission: raw host completions now carry request/generation/branch-epoch/attempt plus payload contract digest, runtime mints sealed validated/admitted/denied completion proofs, stale/superseded/unknown/malformed/partial completions are typed denials with retained counters, and completion admission intentionally does not mutate committed lifecycle before the later transactional apply phase.
- Phase 7A continued in order with runtime-owned cancellation: active pending requests can be cancelled by handle, cancellation records a sealed `CancelledResourceRequest` and `ResourceCancellationReport`, removes the request from the active frontier while retaining it for late completion denial, and late success after cancellation is classified as `CompletionDenialClass::Cancelled`.
- QA on Phase 7A tightened two weak spots: completion admission now validates generation/branch epoch/attempt before lifecycle status so stale identity dominates cancelled/superseded classification, and cancellation denial now returns sealed `DeniedResourceCancellation` evidence through the report instead of a plain string error.
- Phase 7B implemented runtime-owned resource timeouts: timeout policies schedule node-owned temporal wakes on request admission, timeout admission requires the matching `ReadyTemporalWake` proof plus request handle identity, successful timeout transitions to `TimedOut`, retires the consumed wake, denies late completions as `CompletionDenialClass::TimedOut`, and exposes sealed timeout proof/report/denial types with compile-fail privacy fixtures.
- Phase 7C implemented explicit supersession lineage: request admission now records a sealed `ResourceSupersessionRecord` tying previous and replacing handles, retained in-flight requests expose `superseded_by`, telemetry counts supersession records/lineage width, and supersession retires the prior timeout wake before it can become stale timeout truth.
- Forge Query runtime API Batch 3A implemented runtime-owned live delivery evidence: `write` now routes affected live handles through active subscription maintenance delta/window/work-packet/batch emission, `drain_patches` returns sealed query delivery evidence alongside compatibility live patches, grouped live declarations ask the backend for grouped baseline members before live lowering, and runtime/program traces retain query-delivery patch artifacts. Verified runtime tests, trybuild phase boundaries, full `cargo test -p forge-query`, and `git diff --check` with only existing LF/CRLF warnings.
- QA on Forge Query runtime API Batch 3A found and fixed scaling/domain-boundary traps: live delivery routing now lives in `runtime/delivery.rs`, uses a collection-keyed live subscription index instead of scanning every view per write, suppresses projected-aspect-irrelevant updates, discards raw compatibility live patches at the runtime facade, and unregisters stale index entries when a live view name is redeclared against a different collection. Reverified runtime tests, trybuild phase boundaries, `cargo check -p forge-query --tests`, full `cargo test -p forge-query`, and `git diff --check` with only existing LF/CRLF warnings.
- Forge Query runtime API Batch 4 started the computed-handle foundation: `ForgeQueryDerivedView` now records upstream live/computed handle dependencies and produced aspects, computed declarations reject missing live/computed dependencies and cycles before registration, nested computed routing runs in topological order with aspect-scoped propagation, computed patch/handle/materialization runtime code moved into `runtime/computed.rs`, and a compile-fail fixture prevents forged computed handles. Verified runtime tests, phase-boundary trybuild, `cargo check -p forge-query --tests`, full `cargo test -p forge-query`, and targeted `git diff --check` with only LF/CRLF warnings.
- Forge Query runtime API Batch 4B added the computed dependency index and inspection evidence: derived view declarations are indexed by upstream live/computed handles, redeclaration replaces stale index membership, writes expose `considered_computed_view_count` as bounded invalidation evidence, and `inspect_derived_view` reports computed authority/dependencies/aspects/materialization through a sealed facade artifact. Verified runtime tests, phase-boundary trybuild, `cargo check -p forge-query --tests`, full `cargo test -p forge-query`, and targeted `git diff --check` with only LF/CRLF warnings.
- Forge Query runtime API Batch 5A began declaration-owned effects: added effect declarations/handles/triggers/conditions, query-local effect delivery routing after live/computed patches, effect delivery counters on write receipts, sealed inspection evidence, support-profile admission for the Effect family, and tests for live/computed triggers plus expression suppression/failure.
- QA on Forge Query Batch 5A tightened effect semantics and module shape: triggers are now handle-authored instead of raw public view names, effect delivery cannot target authoritative truth without the later intent boundary, computed-trigger effects ignore stale undrained patches by receipt identity, live triggers scope changed aspects to the trigger view collection, expression failure uses typed posture instead of descriptor substring heuristics, the unsupported `meaningful_change` promise was removed, and the effect runtime was split into declaration/delivery/inspection/registry/routing modules. Verified runtime tests, phase-boundary trybuild, `cargo check -p forge-query --tests`, full `cargo test -p forge-query`, and targeted `git diff --check` with only LF/CRLF warnings.
- Forge Query runtime API Batch 5B made meaningful-change suppression explicit and proof-bearing: effect declarations now carry `ForgeQueryEffectSuppressionPolicy`, `.with_meaningful_change_suppression()` records semantic-delta suppression intent, routing counts meaningful suppressions separately from generic condition suppression, deliveries and inspection evidence expose the admitted suppression policy, and write receipts expose meaningful suppression counts. Verified focused meaningful-change regression, runtime tests, phase-boundary trybuild, `cargo check -p forge-query --tests`, full `cargo test -p forge-query`, and targeted `git diff --check` with only LF/CRLF warnings.
- Forge Store 13.3 Phase 5B advanced certification coverage: support-trust rows now carry family kind, completed evidence bundles require all first-ship support families, bounded certification batch scope, exact counter/receipt-reuse evidence, and private bundle construction. Verified focused trust tests, trybuild phase boundaries, subscription-support tests, full `cargo test -p forge-store`, and targeted `git diff --check`.

- Forge Store 13.3 Phase 6A implemented generic/domain certification handoff: certified support trust now feeds a generic certification report, first-ship domain scenario rows carry certified semantic support or explicit advanced-family debt, domain batch plans/counters enforce scenario width/index/receipt/allocation bounds, and Roadmap 2 physical database readiness remains explicit handoff debt. Verified focused trust tests, trybuild phase boundaries, subscription-support tests, full cargo test -p forge-store, and targeted git diff --check.
- Forge Signal Milestone B Phase 8 first slice added a completion proof chain: admitted completions can be staged into a sealed effect and committed into a sealed artifact, commit alone mutates request lifecycle to fulfilled, duplicate completions after commit deny as retired, and commit consumes any live timeout wake. Added runtime/API exports, a denied-completion compile-fail staging fixture, focused resource runtime tests, and verified full `cargo test -p forge-signal`.
- QA on that Phase 8 slice found and fixed two proof-strength gaps: staging now revalidates that an admitted completion still targets an active pending request before issuing a staged effect, and staged/committed completion artifacts are no longer `Copy`. Added regressions for cancelled-after-admission staging denial plus compile-fail fixtures proving staged and committed artifact fields remain private.
- Continued Phase 8 QA tightened move-only proof discipline further: staged and committed completion artifacts, plus their reports, no longer implement `Clone`, and trybuild now proves external callers cannot clone those proof tokens.
- Forge Store 13.3 Phase 6B broadened certified support trust from exact-only to posture-aware operational certification: generic certification can now carry exact and degraded certified postures, first-ship domain rows materialize certified geometry/web-data/AI-degraded support instead of abstract debt, scenario checks enforce family/role/strength/class alignment, and degraded certification is explicitly rejected when spent as exact. Verified focused trust tests, trybuild phase boundaries, subscription-support tests, full `cargo test -p forge-store`, and targeted `git diff --check`.
- QA on Forge Store 13.3 Phase 6B found and fixed a domain-certification overclaim gap: bundles no longer accept explicit debt rows for first-ship scenarios that must be certified now, advanced debt scenarios cannot be promoted as certified semantic support, and debt rows now carry blocked trust posture plus concrete debt reason/future owner. Reverified focused trust tests, compile-fail phase boundaries, subscription-support tests, full `cargo test -p forge-store`, and targeted `git diff --check`.
- Forge Store 13.3 Phase 7A added the named `Subscription-Support Accuracy And Certification Test` suite foundation: required row ids now cover exact/degraded/rebuild/replication/migration/import/stale/policy/drift/certification/domain/handoff/overclaim/global-scan lanes, suite rows and outputs are machine-checkable, Phase 5/6 evidence bundles/generic/domain/handoff artifacts are consumed directly, and private-field trybuild coverage blocks synthetic suite row construction. Verified focused trust tests, subscription-support tests, phase-boundary trybuild, full `cargo test -p forge-store`, and targeted `git diff --check`.
- Forge Signal Milestone B Phase 8 next slice added rollback-safe completion transaction staging for denied outcomes: retained denied completions can stage into move-only `StagedDeniedResourceCompletionEffect`, admitted and denied staged effects can roll back into sealed rollback artifacts without mutating request/denial state, rollback/staging performance envelopes and telemetry are explicit, and trybuild proves denied staged tokens cannot be forged, cloned, or committed through the admitted path. Verified `cargo check -p forge-signal --tests`, focused rollback/runtime tests, resource API trybuild, full `cargo test -p forge-signal`, and targeted `git diff --check`.
- Forge Store 13.3 Phase 7B hardened the named suite from row-name coverage into artifact-bound evidence: suite rows now derive from the Phase 5 evidence bundle, generic certification digest, domain scenario row digests, and Roadmap 2 handoff digest; arbitrary public row construction is blocked; tampered row evidence rejects against the supplied phase artifacts; and compile-fail coverage proves callers cannot synthesize row evidence with `SubscriptionSupportAccuracyCertificationRow::new`. Verified focused trust tests, phase-boundary trybuild, subscription-support tests, and full `cargo test -p forge-store`.
- Forge Signal Milestone B Phase 8 continued with transaction-owned resource completion observation: `SignalTransaction` can now stage/commit admitted resource completions, commit classifies resource lifecycle changes through the existing observation scratch, rollback suppresses completion-driven observer delivery, and resource plus temporal wake state are restored by explicit rollback packets. Added runtime tests for delivered vs rollback-suppressed completion observations and a trybuild guard preventing denied staged effects from transaction commit. Verified `cargo check -p forge-signal --tests`, focused resource/observation tests, resource API trybuild, full `cargo test -p forge-signal`, and targeted `git diff --check`.
