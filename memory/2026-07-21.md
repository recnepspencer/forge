# 2026-07-21

- Implemented Worth UI 3.9.1 phases 1-3: adjudicated migration inventories, installed snapshot/recording operations with separately registered executors, reference-only application binding plans, and one attempt-scoped Query operating-world gateway.
- Query runtime construction requires the installed operation and executor registration sets to match exactly; semantic/executor drift is rejected before a workspace or UI authority exists.
- The recording workflow carries canonical `f32` bits through Query's `U64` workflow value and performs its only mutation in the terminal stage; an invalid second stage was certified to leave no partial backend write.
- File- and Rust-authored `.wui` bindings retain distinct provenance but resolve the same installed reference and Query binding identity.
- QMOD-001 remains explicitly `implemented-with-bounded-compatibility`: the old snapshot projection adapter stays only until the Phase 7 downstream frame-ingress cutover.
- Implemented Worth UI 3.9.1 phases 4-6: Query now owns the mint-once consumer contract and complete execute/publish/consume/settle progression; UI-only requirements stay adjacent and the local Query support receipt/posture authorities are deleted.
- Exact settled projections are stored once in heavyweight slots behind a separately rebuildable index. UI measurement facts, refinement counters, typed source generation/order, and atomic in-generation refresh derive only after settlement; denial returns the candidate and preserves the predecessor.
- Replacement equivalence now requires exact current installed Query authority plus a real active Query binding/settlement or exact live resource. Missing, foreign, stale, or unequal authority fails closed to a narrow rebind; UI presentation drift remains separately visible.
- Skeptical QA found and fixed three substantive issues: missing authority originally compared equal, the settled UI fact initially exposed only a field count instead of the measurement value, and the settlement lookup index was initially inseparable from proof storage.
- The post-phase 4-6 QA found deeper succession defects: equal regional replacement could substitute candidate settlement for the exact active predecessor, transferred source ordering could regress, vacant projection slots accumulated forever, replacement evidence reduced exact managed-live authority to a boolean, and installation generation could turn stale after lowering without blocking publication. These now preserve the exact predecessor, keep monotonic source coordinates, reuse vacant slots, carry typed exact evidence, and revalidate the single shared installed Query authority at the final succession boundary.
- Public certification now proves real `.wui` snapshot replacement, exact settlement retention across regional and complete succession, partial Query settlement preservation, and stale-generation denial before application publication. The generic Query Consumer Kit support fixture was split into application/source and measurement-workspace fixtures during structural QA.
- Skeptical QA of 3.9.1 phases 7-9 found and fixed several cross-phase seams: modern settled Query facts were not narrowed through the real mixed-source catalog, same-binding replacement left stale generation authority on plan links, virtualized frames cloned retained Query `Arc` evidence on every frame, native Query values disappeared from modern settled inspection, and the canonical public lifecycle registered Query without authoring the binding it later executed. Plan links are now stable slot locators with generation capability attached only at the active-session edge; steady frames carry compact copy-only evidence; budgeted inspection retains native fact access; and the real file/Rust application journey authors the same Query binding.
- Phase 7-9 closeout evidence: 861 runtime tests, 26 query-binding tests, 48 public application contracts, 23 consolidated compile contracts in two Cargo sessions, strict Clippy for the three affected packages, Worth UI-scoped 400-line cap, Road 1 boundary check, agent-context check, and formatting all pass. The repository-wide line-cap command remains red only because many non-Worth-UI platform and Worth Query files exceed the global cap; all existing non-allowlisted Rust files under `workspaces/worth-ui` pass.
- Milestone 3.9.1 closed after Phase 10 and a full skeptical QA loop. The final full-workspace gate exposed an incomplete seam: ordinary product/runtime facades still promoted managed-live compatibility and Query inspection/status types. Compatibility is now reachable only through `worth-ui-query-binding::compatibility::managed_live`; Query inspection is consumed directly at that binding boundary; UI diagnostics expose carried binding observations rather than a local Query status taxonomy.
- Final 3.9.1 evidence is source-bound and green: all-target/all-feature workspace tests, strict workspace Clippy, 115 topology contracts, 51 real application contracts, 14 compile-fail plus 9 compile-pass targets in two Cargo sessions, boundary check, agent-context check, topology/timing validators, and every Worth UI Rust file at or below 400 lines. Exact-source timing medians are 0.462 s targeted warm, 8.070 s fast lane, 16.935 s application contracts, 1.790 s warm compile contracts, and 80.738 s isolated cold compile contracts. The only retained predecessor seam is explicitly bounded to Query 9.14 Phases 17/19/23/24.
- Closed seven hostile C.5 follow-up findings: per-open aggregate append breadth
  admission, real C.6 frame-load/residency mediation, an independently packaged
  offline observer, explicit reconstructive manifest-capacity migration, linear
  offline page membership lookup, independently declared performance contracts,
  and machine-readable courtroom/mutation evidence.
- The broad record journey run caught two regressions during correction: geometry
  preflight had moved after an allocation, and an older policy-drift test still
  treated fanout migration as ordinary append. Both were corrected.
- Closeout is green: 61 physical-record journeys, 44 offline-verifier owner tests,
  24 runner tests, strict Clippy for the affected packages, the isolated observer
  build/dependency check, Road 1 boundary-check, and agent-context check.
- A later hostile pass replaced the fake expected-to-actual mutation ledger with
  14 executable production mutants, made candidate residency own frames across
  the real Store write, changed Courtroom B to six killed-process cut points,
  sealed Courtroom A behind independent writer/reopener/offline observations,
  and joined five invalid worlds to each fixed Courtroom C scale world. The final
  record journey is 60 tests after redundant claims were removed; all 60 pass in
  parallel, all 14 mutants die causally, all-target/all-feature Clippy is clean,
  and Road 1 boundary/context checks pass. The global line-cap script remains red
  only on pre-existing files outside this change; every changed Rust file passes.
- A subsequent intent QA hardened mutation evidence so the observed runtime
  predicate must be parsed from the failing process and match the catalogued
  predicate; it no longer self-certifies the expected label. It also removed
  locator readmission from the exclusive writer facade, made Courtroom B prove
  the exact artifact role of completed short-write prefixes, and mechanically
  barred legacy `PhysicalReference` from the C.5 durable record path. The runner
  has 28 green tests, representative direct/child/scale/current-truth/C.6 mutants
  report matching causal predicates, targeted journey tests pass, strict Clippy
  is warning-free, formatting/diff checks pass, and mutation sandboxes clean up.
- A read-only C.3/C.4/C.5 boundary audit found that the physical byte path is
  coherent but the joined lifecycle/outcome model still has gaps: the shared
  lifecycle stops at `MediaOwned` instead of progressing to record serving,
  C.5 initialization can create the record-family scaffold before entropy
  failure is returned as a reusable denial, serving shutdown returns only the
  C.4 media outcome and drops record-path terminal posture, retained record
  observations can mix acquisition-time root generation with current counters,
  and C.4 runtime admission conservatively maps several pre-effect backend
  denials to terminal inspection because backend variants do not carry enough
  effect-fate information. No production edits were made in this audit.
- Corrected all six joined-boundary findings. Record bootstrap now resolves
  entropy and format preconditions before its first filesystem effect; the
  shared lifecycle has a generation-advancing `RecordServing` phase; serving
  close/abort return record health, residue, released-owner disposition, and
  terminal counters alongside media shutdown; record observations expose a
  coherent, explicitly acquisition-scoped snapshot; pre-effect backend denials
  alone return reusable C.3 authority while post-ownership denials are terminal
  failures carrying their original reason and lease-release outcome; and the
  public Store facade describes C.4/C.5 while no longer exporting legacy
  `PhysicalReference`.
- Closeout after these repairs: 62 physical-record journeys, 16 physical-media
  journeys, the runtime authority UI suite, 28 runner tests, strict all-target /
  all-feature Clippy for Store and the physical backend, Road 1 boundary-check,
  and agent-context check all pass. The global workspace line-cap remains red
  on unrelated pre-existing files; every file touched by this repair is under
  400 lines. A nondeterministic Courtroom A exact I/O count was replaced by the
  stable physical durability relations it was actually intended to prove.
- Repaired four later cross-boundary findings without counter-derived authority:
  C.4 admission now carries direct effect and lease-release fate; C.5 separates
  current-call effect from persistent-world safety and never advances allocation
  after failed publication; authoritative read/scan/planning damage revokes one
  shared serving-health owner; and the C.6 candidate port carries exact artifact
  coordinates and may retain frame ownership across writes. A final process
  journey exposed terminal counters captured before directory-handle drops, so
  media-owner admission was isolated and now disposes every capability and lease
  before snapshotting failure evidence. Closeout: 170 backend tests, 16 physical
  media journeys, 64 physical record journeys, strict Clippy, formatting, scoped
  source caps, boundary-check, and agent-context all pass.
- Closed the exploitability audit: manifest fanout is structurally and
  defensively constrained to at least two; mutation-lock admission rejects
  multi-link handles and preserves an outside sentinel; Windows device and
  verbatim roots are denied; locator readmission damage revokes shared serving
  health; raw backend qualification is owner-feature gated and mechanically
  limited to Store by source/manifest gates; durable decoders receive bounded
  arbitrary and checksum-valid semantic-corruption pressure; and CI now runs a
  RustSec audit. The C.5/C.6 candidate seam was also corrected so residency
  retains bytes while Store alone performs each physical write. Closeout: 65
  record journeys, 17 media journeys, 129 format tests, 30 runner gates, strict
  all-feature Clippy, YAML validation, scoped line caps, boundary-check, and
  agent-context all pass. The repository-wide line-cap remains red only on
  unrelated pre-existing files.
