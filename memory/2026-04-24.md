# 2026-04-24

- Built the first real Forge Query 9.3 Phase 3 bridge-parity slice: added a dedicated `subscription/bridge_parity/` module with sealed `QuerySubscriptionManualBridgeWitness`, `BridgeWitnessAssemblyPosture`, parity width/receipt/counter types, typed parity failures/errors, `QuerySubscriptionBridgeParityComparison`, `QuerySubscriptionBridgeParityExplanation`, and the public `build_query_subscription_manual_bridge_witness(...)` plus `explain_query_subscription_bridge_parity(...)` facade surfaces.
- Parity now compares authoritative declaration, lowering, activation, and witness evidence without semantic rediscovery, distinguishes grouped/inspector/bounded shared-bridge families through `FamilyDistinctBridgeShared`, and denies typed on declaration/bridge/basis/signal source drift.
- Added focused runtime coverage in `crates/forge-query/src/subscription/tests/bridge_parity.rs` and a new compile-fail privacy fixture `subscription_bridge_parity_explanation_constructor_private.rs` with its accepted `.stderr` baseline.
- Verified `cargo fmt -p forge-query`, `cargo test -p forge-query bridge_parity -- --nocapture`, `cargo test -p forge-query --test phase_boundaries_compile_fail -- --nocapture`, full `cargo test -p forge-query`, and targeted `git diff --check` with only the existing LF/CRLF warnings on `crates/forge-query/src/facade.rs` and `crates/forge-query/src/subscription/mod.rs`.
- Hostile QA on the new 9.3 bridge-parity work found two proof-chain holes worth fixing before Phase 4 builds on them: parity explanation had been re-broadcasting semantic labels from the witness instead of deriving them from declaration/lowering authority, and it failed to validate witness `activation_digest` against the activation under comparison.
- Corrected bridge parity so explanation labels now come from canonical declaration/lowering semantics, parity rejects semantic label drift inside the witness, and parity adds a typed `ActivationMismatch` denial when witness and activation no longer share the same runtime activation identity.
- Current verification of the QA patch is blocked by an unrelated workspace compile failure in `crates/forge-signal/src/logic/transaction/runtime/state/temporal.rs` calling missing `GraphObserver::materialize_trace_summary`; local `cargo fmt` and targeted `git diff --check` for the touched files are clean.
- A deeper QA pass over the full diagnostic and bridge-parity code found two more non-obvious honesty bugs: denied diagnostic traces were able to mislabel admitted admission evidence under later failure stages, and diagnostic bundle basis labels were coming from bridge lowering request kinds instead of canonical declaration basis posture.
- Fixed diagnostic tracing so support-stage denials without a support report still emit an explicit denied `SupportReporting` stage, and later denials preserve an admitted `RuntimeBackedAdmission` stage instead of restamping admission evidence under the wrong stage.
- Fixed diagnostic semantic labels so admitted and denied bundles now carry canonical declaration basis posture strings, not lowering request-kind aliases; added regressions for support-failure trace shape and historical-basis label preservation.
- Verified `cargo fmt -p forge-query`, `cargo test -p forge-query diagnostic_bundles -- --nocapture`, `cargo test -p forge-query bridge_parity -- --nocapture`, `cargo test -p forge-query --test phase_boundaries_compile_fail -- --nocapture`, and targeted `git diff --check`.
- Built the first real Forge Query 9.3 Phase 4 runtime-certification slice: added `subscription/runtime_certification/` with family coverage matrices and rows, variation sets, `CertifiedFamilyCoverageHandle`, runtime certification scope assembly, certification coverage receipts, and shipped `certify_query_subscription_runtime_family(...)` bundle output wired through the crate facade.
- Added focused runtime coverage proving admitted-plus-hostile family certification closure, denial without hostile coverage, rejection of matrix-scan debt in coverage handles, and cross-family scope rejection; also added compile-fail privacy fixtures for runtime certification scope and bundle construction.
- Verification uncovered a real but non-product bug in the new trybuild lane: the runtime-certification constructor-privacy fixtures were correct, but their accepted `.stderr` baselines were missing. Added those baselines and reverified `cargo fmt -p forge-query`, focused `cargo test -p forge-query runtime_certification -- --nocapture`, full `cargo test -p forge-query --test phase_boundaries_compile_fail -- --nocapture`, and full `cargo test -p forge-query` with the new Phase 4 surfaces included.
- Ran a spec-completeness pass on the Phase 4 batch and found two meaningful honesty gaps before calling it done: runtime certification was accepting declaration-only support reports as long as digests aligned, and `CertifiedFamilyCoverageHandle` was not preserving its actual coverage-resolution posture, so certification receipts were laundering matrix-scan-debt into indexed coverage.
- Fixed Phase 4 so family coverage rows and runtime certification scopes now reject non-runtime support subjects and non-`RuntimeBackedCertified` support postures, while coverage handles now retain `CoverageResolutionPosture` and certification receipts/counters reflect indexed versus matrix-scan-debt behavior honestly.
- Added regressions proving declaration-only support cannot enter runtime family certification and that matrix-scan-debt posture survives into certification receipts/counters. Reverified `cargo fmt -p forge-query`, focused `cargo test -p forge-query runtime_certification -- --nocapture`, `cargo test -p forge-query --test phase_boundaries_compile_fail -- --nocapture`, and full `cargo test -p forge-query`.
- Ran a much stricter aerospace-grade QA pass over the new `forge-signal` temporal substrate work and found two real honesty bugs: previous-value capability proofs could survive branch restore if the restored branch recreated the same ready wake proof, and the compile-fail API boundary for previous-value proof structs had degraded into an unresolved-import failure instead of private-field enforcement.
- Hardened the temporal runtime so previous-value access now carries a restore-scoped capability epoch that is bumped on active branch restore, causing stale pre-restore capabilities to fail even if branch id and wake proof line up again after rewind.
- Re-exposed `SignalBranchId` through the non-test facade so external compile-fail fixtures can target the real privacy boundary, updated the previous-value UI fixtures/baselines, and added a regression proving restore cannot resurrect stale previous-value access.
- Verified `cargo fmt --package forge-signal`, `cargo test -p forge-signal temporal_runtime`, `cargo test -p forge-signal temporal_api`, and full `cargo test -p forge-signal` all green after the QA corrections.
- Completed Forge Query milestone 9.3 Phase 5 closeout: added a new `crates/forge-query/src/harness/milestone_nine_three_certification/` module with a named `Query Subscription Bridge Parity And Diagnostic Sufficiency Test` adapter, concrete canonical and rejection rows, required output digests, and compile-fail target coverage wired into the certification registry.
- The 9.3 harness composes the shipped support-report, bridge-parity, diagnostic-bundle, lifecycle-certification, and runtime-certification surfaces end to end, including continuation and preview-discard lanes, grouped/inspector family distinction checks, hostile support/runtime-certification denials, and a posture-sensitive coverage row for performance honesty.
- Added the missing 9.3 trybuild fixtures and accepted `.stderr` baselines for durable support overclaim, mismatched declaration/signal parity forging, denied-bundle hostile-coverage forging, and uncertified runtime-certification bundle forging.
- Verification passed with `cargo fmt -p forge-query`, `cargo test -p forge-query milestone_nine_three -- --nocapture`, `cargo test -p forge-query --test phase_boundaries_compile_fail -- --nocapture`, and full `cargo test -p forge-query`.
- Started Milestone 10 with the first honest store-backed slice instead of stretching 9.3: policy-aware historical planning now admits a narrow `StoreBackedRetainedSnapshot` basis with `StoreHistoricalRetainedBounded` posture, while store-backed diff remains explicit deferred debt.
- Query-context historical execution now records basis authority family and preserves runtime/store parity for retained historical reads by deriving historical result identity from canonical query meaning rather than route-local plan digests; this fixed a real route-shaped parity bug caught by the new store-backed retained regression.
- The policy execution seam support surface now marks `StoreBackedExecution` as `LimitedAdmission` rather than fully blocked, and the handoff report now names the remaining Milestone 10 work honestly: broader historical restore, diff parity, and wider runtime/store parity over policy-aware plans.
- Historical/diff certification now includes a canonical `runtime-vs-store-historical-parity` row instead of the old `store-backed-historical-deferred-debt` rejection row, and query-basis compile-fail baselines were updated for the new sealed `basis_authority_family` field.
- Verified `cargo fmt -p forge-query`, `cargo check -p forge-query --tests`, `cargo test -p forge-query store_backed_retained -- --nocapture`, `cargo test -p forge-query historical_diff -- --nocapture`, `cargo test -p forge-query --test phase_boundaries_compile_fail -- --nocapture`, and full `cargo test -p forge-query`. `git diff --check` stayed clean aside from the existing LF/CRLF warnings on touched query files.
- Ran a stricter hostile QA pass on the Milestone 10 retained-store slice and found two real overclaim bugs: query-context historical binding was willing to admit store-backed replay/reconstruction lanes simply because the preflight authority was `Store`, and the policy-execution support surface was naming a generic `StoreBackedExecution` lane even though only retained historical parity had actually been proven.
- Fixed the query-context boundary so store-backed historical binding now fails closed unless the admitted path and resolved materialization are both retained-snapshot, with new regressions proving store-backed replay and reconstruction remain explicit deferred debt.
- Tightened the support surface so it now names `StoreBackedRetainedHistoricalExecution` explicitly instead of a generic store-backed execution lane, preserving honest support vocabulary for later Milestone 10 batches.
- Reverified with `cargo fmt -p forge-query`, focused `cargo test -p forge-query query_context -- --nocapture`, focused `cargo test -p forge-query policy_execution_seam -- --nocapture`, full `cargo test -p forge-query`, and targeted `git diff --check` with only the existing LF/CRLF warnings.
- Reverted the premature Milestone 10 direct runtime/store parity batch after deciding store work is not ready yet; removed the planner/execution/harness changes from that last batch and returned focus to milestone 9.3.
- Ran another harsh 9.3 QA pass and fixed two proof-chain holes: hostile runtime-certification rows now validate denied diagnostic bundles against the same declaration/bridge/admission/support chain, and admitted/denied diagnostic bundle assembly now validates trace stage sources against every supplied artifact instead of trusting terminal stage alone.
- Ran a stricter test-quality QA on milestone 9.3 and tightened the certification harness so the assertions are less gameable: admitted lanes now require real proof digests instead of placeholder `"none"` values, semantic signatures now include more of the actual proof chain instead of a shallow subset, the grouped/inspector distinction test now compares against the correct shared-bridge family rows, and the `support-matrix-scale-honesty` row now isolates coverage posture instead of also perturbing admission identity.
- `cargo fmt -p forge-query` and targeted `git diff --check` for the milestone 9.3 harness passed. Executable Rust verification remains blocked by the unrelated existing `forge-signal` break: missing `clock_basis()` call sites in `transaction_evaluation.rs`, `transaction_keyed.rs`, and borrow errors in `runtime_execution.rs`.
