Worked the planner-owned routing test slowness in `worth-kernel` by tracing current public-proof and diagnostics paths from the workload-composition seam backward.

Added gated performance tracing in `crates/worth-kernel/src/workload_composition/performance_trace.rs` and instrumented the expensive current builders around conflict inventory, ordinary consumer cutover, selected route assembly, public proof assembly, and derived diagnostics.

Main speedup came from caching stable current surfaces at the production seam:
- `current_conflict_batch_admission_inventory`
- `current_worth_workload_ordinary_consumer_cutover`
- `current_replay_undo_inventory_report`
- `current_replay_undo_transaction_route_packet`
- `current_worth_touched_graph_conflict_selected_route_packet`
- `current_topology_query_backed_consumer_cutover`
- `current_topology_milestone_fifteen_planner_seed_support`
- `current_evidence_lookup_route_packet`
- `current_topology_compiled_product_reuse_route_packet`
- `current_evidence_lookup_reuse_route_packet`

Also removed one duplicate replay/undo packet rebuild in the ordinary-consumer proof lowering path and stopped public-proof architecture alignment from re-querying topology/spatial alignment summaries when the selected-route packet already carried the needed identities.

Measured exact-test improvements:
- `public_facade_exports_inspection_without_authority_construction`: ~304s -> ~33s
- `artifact_policy_can_suppress_rich_diagnostics_without_losing_operational_truth`: ~354s -> ~36s
- `denial_witness_localizes_conflict_vs_independence_failure`: ~234s -> ~35s

Remaining honest cold-start hotspot is the first construction of the replay/undo boundary authority inside `current_worth_workload_ordinary_consumer_cutover`, which still costs about 18-20s on a fresh process before caches take over.
