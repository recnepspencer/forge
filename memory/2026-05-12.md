# 2026-05-12

<<<<<<< HEAD
- Closed Forge Query Milestone 9.3.1 after the final audit. The audit found
  and fixed a Phase 6 bridge-envelope slope certification gap; full
  `forge-query` and `forge-runtime-bridge` test suites passed before writing
  the closeout.
- Drafted Forge Query Milestone 9.3.2 as a Query Basis Capability Lifecycle
  spec, wired it into the roadmap, and added the named 9.3.2 certification
  suite to the 9.3/runtime-gates test requirements.
- Audited the runtime bridge, forge-signal, and forge-relational public
  facades before starting 9.3.2 implementation. Added a lower-runtime API
  boundary map to the 9.3.2 spec: Query owns lifecycle wrapping and receipts;
  bridge owns bridge proof/binding, signal owns derived runtime evidence, and
  relational owns truth/history/snapshot authority.
- Tightened 9.3.2 with an Adapter Shape Contract requiring every
  lower-runtime adapter to name its owning facade/API and carry only
  facade-returned identities, digests, receipts, denials, counters, support
  posture, authority labels, and Query lifecycle proof fields.
- Audited the 9.3.2 plan for typestate and sequencing shortcomings. Added a
  Typestate Proof Contract, then softened the phase-order language into an
  implementation sequencing/review discipline rather than a separate code gate.
  The compiler-hard requirement remains the lifecycle proof chain itself.
- Added target DX requirements to the 9.3.2 plan after reading
  `_docs/more_guidelines/dx_laws.md`: common paths must read like intent,
  expensive/boundary work must be explicit, golden transcripts must compile,
  and proof internals/lower-runtime stitching must stay out of ordinary caller
  code.
- QA'd the 9.3.1 causal inspection DX hardening. Found and fixed missing
  support-discovery and public transcript coverage; added consumer docs,
  plan/cost/policy accessors, support rows, and compile-only public facade
  examples. Focused DX, broad causal inspection, and compile-fail boundary
  tests passed.
- Added a final adversarial confidence pass for 9.3.1 causal inspection:
  table-driven tests now cover reason/outcome mismatches, missing-evidence
  common-path failures, future explanation-family denials before bridge
  assembly, and redaction/materialization policy identity stability.
- Reviewed the completed Branch-Native Resource Effects And Response Lenses
  plan after workflow editor implementation exposed product gaps. Confirmed the
  plan closed branch-native effect substrate and collection/advanced topology
  proof, but not mutation response lenses, write-result-to-read reconciliation,
  granular detail field/path/region lenses, create placement, remove deletion,
  identity migration, partial response mapping, or multi-family mutation
  convergence. Authored the pt-2 spec
  `_docs/forge_signal_wasm/resource_mutation_response_reconciliation_plan.md`,
  inserted it into the roadmap as Milestone 11, and updated the docs index.
- QA'd the pt-2 resource mutation response reconciliation spec for phase
  linearity and practical implementation guidance. Split identity migration
  into its own dependency phase before create placement, renumbered later
  phases, split the migration and create placement proof families, and added
  implementation guidelines covering phase spine, file ownership, public-method
  admission rules, target reconciliation questions, and concrete docs/test
  examples.


- Closed Forge Query Milestone 9.3.1 after the final audit. The audit found
  and fixed a Phase 6 bridge-envelope slope certification gap; full
  `forge-query` and `forge-runtime-bridge` test suites passed before writing
  the closeout.
- Drafted Forge Query Milestone 9.3.2 as a Query Basis Capability Lifecycle
  spec, wired it into the roadmap, and added the named 9.3.2 certification
  suite to the 9.3/runtime-gates test requirements.
- Audited the runtime bridge, forge-signal, and forge-relational public
  facades before starting 9.3.2 implementation. Added a lower-runtime API
  boundary map to the 9.3.2 spec: Query owns lifecycle wrapping and receipts;
  bridge owns bridge proof/binding, signal owns derived runtime evidence, and
  relational owns truth/history/snapshot authority.
- Tightened 9.3.2 with an Adapter Shape Contract requiring every
  lower-runtime adapter to name its owning facade/API and carry only
  facade-returned identities, digests, receipts, denials, counters, support
  posture, authority labels, and Query lifecycle proof fields.
- Audited the 9.3.2 plan for typestate and sequencing shortcomings. Added a
  Typestate Proof Contract, then softened the phase-order language into an
  implementation sequencing/review discipline rather than a separate code gate.
  The compiler-hard requirement remains the lifecycle proof chain itself.
- Added target DX requirements to the 9.3.2 plan after reading
  `_docs/more_guidelines/dx_laws.md`: common paths must read like intent,
  expensive/boundary work must be explicit, golden transcripts must compile,
  and proof internals/lower-runtime stitching must stay out of ordinary caller
  code.
- QA'd the 9.3.1 causal inspection DX hardening. Found and fixed missing
  support-discovery and public transcript coverage; added consumer docs,
  plan/cost/policy accessors, support rows, and compile-only public facade
  examples. Focused DX, broad causal inspection, and compile-fail boundary
  tests passed.
- Added a final adversarial confidence pass for 9.3.1 causal inspection:
  table-driven tests now cover reason/outcome mismatches, missing-evidence
  common-path failures, future explanation-family denials before bridge
  assembly, and redaction/materialization policy identity stability.
- Drafted Forge Query Milestone 9.3.3 as an Authority-Scoped Effect Execution
  Pipeline spec. It makes effect execution a proof chain from raw intent
  through lowered execution plans, execution receipts, and self-describing
  envelopes; updated the roadmap pointer and added the named 9.3.3
  certification suite to the 9.3/runtime-gates test requirements.
- Re-read the actual `forge-query`, `forge-relational`,
  `forge-runtime-bridge`, and `forge-signal` public APIs before tightening the
  9.3.3 plan. Updated the spec and 9.3/runtime-gates suite so 9.3.3 is
  explicitly grounded in existing Query workflow/effect surfaces
  (`WorkflowContextBinding`, `QueryWorkflowDeclaration`,
  `LoweredMutationIntentDeclaration`, `LoweredMergeWorkflowDeclaration`,
  `QueryWritebackDeclaration`, `ForgeQueryWriteReceipt`) and existing lower
  runtime authority artifacts (`RawStrategyCommitRequest`,
  `MergeExecutionRequest`, `BridgeWritebackDeclaration`,
  `TruthWritebackRequest`/`TruthWritebackReceipt`) rather than a generic
  greenfield pipeline vocabulary.
=======
- Created and switched to branch `forge-foundational`.
- Restored `forge-foundational` roadmap/vision docs from `worktree-4` into the branch.
- Drafted `_docs/forge-foundational/milestone-1.md` for the Aspec-native canonical value and aspect-state substrate, using the exact `AspectValue` vocabulary from `worktree-4` `forge-relational`.
- QA-revised Milestone 1 to make aspect contracts, struct aspect values, masks, absence/null/default/clear law, evolution posture, equivalence basis, compile-time enforcement targets, and linear phase gates explicit.
- Added `_docs/forge-foundational/test-requirements.md` because the implementation strategy changed: build foundational completely first, then refactor adopting crates in large migrations. The test bar now requires standalone compile-fail, property, hostile, golden, composition, and migration-readiness evidence before broad crate adoption.
- After pulling `origin/master`, confirmed `crates/forge-proof` exists and revised the Forge Foundational Milestone 1 plan to use `forge-proof` for proof-bearing progression surfaces: contract validation, evolution classification, authoritative-state admission, compatibility lowering, and digest-preparation readiness. Plain aspect values, keys, masks, locators, and identity wrappers remain lightweight `forge-foundational` vocabulary.
- Began Forge Foundational Milestone 1 implementation with the Phase 1 crate boundary: registered `crates/forge-foundational`, added a curated facade, named private responsibility homes, introduced a `forge-proof::Artifact` boundary declaration, and added trybuild compile-fail tests proving internal homes and boundary payload fields are sealed.
- Implemented Forge Foundational Milestone 1 Phases 2 and 3 in one larger batch: canonical relational aspect-value families, scalar wrappers, aspect keys/contracts, struct aspect shapes/values, phantom-typed masks, evolution/equivalence law, and proof-bearing contract validation through `forge-proof::Artifact` and `TransitionOutcome`. Added focused runtime tests plus trybuild gates for no generic document variant, mask-mode separation, raw values not satisfying validated APIs, and sealed struct-value fields.
- Code-quality QA found the initial `forge-foundational` implementation was not scalable: `aspects.rs` was a god file, `values.rs` was too broad, and tests were phase/provenance-shaped. Refactored into roadmap-shaped production modules under `values/`, `aspects/contracts`, `aspects/structs`, `aspects/masks`, `aspects/evolution`, `aspects/validation`, `boundary/`, and directory homes for future compatibility/canonicalization/identity/locator work. Moved tests under `tests/certification/` and boundary-specific `tests/ui/*` directories, leaving only Cargo harnesses at test root.
- Began Forge Foundational Milestone 2 with the canonical basis grammar slice: added rule versions, typed domains, loci, entry ids, entry kinds, basis values, canonicalization cost counters, basis sequences/bundles, proof-phase markers, and `forge-proof::Artifact` readiness for basis and bundle construction. Added facade exports, runtime certification for ordering/duplicate/domain/bundle law, and compile-fail tests proving raw sequences, raw indexes, and direct sequence field construction cannot satisfy readiness boundaries. Verification passed fmt, full `cargo test -p forge-foundational`, and clippy with warnings denied.
- QA-hardened Milestone 2 Phase 1 after review: removed the `AspectValue` escape hatch from canonical basis values, introduced explicit integer/float width carriers, rejected whitespace-shaped rule-version identities, rejected duplicate semantic entry keys even when values differ, and made canonical basis artifacts carry real `forge-proof` proof sets instead of `NoProofs`. Hardened `forge-proof` itself with authority-scoped `Proof<P, A>`, a proof-bearing current-basis artifact constructor, non-copy proof tokens, and compile-fail coverage for proof authority substitution.
- Finished the follow-up `forge-proof` hardening pass requested before Milestone 2 continues: removed the legacy unscoped proof authority/default, added proof-kind authorization through `AuthorityProves<P>`, required same-authority proof sets for current-basis construction, updated docs and trybuild snapshots to the scoped proof model, and cleared `forge-proof`/`forge-foundational` clippy with `-D warnings`.
- QA pass on the five `forge-proof` hardening items found stale `Proof<P>` examples in the Milestone 2 spec and added missing hostile compile-fail evidence for unapproved proof-kind minting plus mixed-authority current-basis proof sets. Full `forge-proof` and `forge-foundational` tests and `-D warnings` clippy passed afterward.
- Continued Forge Foundational Milestone 2 Phase 2 with identity and locator canonical-basis builders. Added proof-bearing `CanonicalBasisReadyArtifact` preparation for identity categories and locator categories, facade exports, hostile certification for category collapse, locator authority/path/mask-mode preservation, and a compile-fail boundary proving raw identity wrappers cannot satisfy ready-basis APIs. Focused tests, full `forge-foundational` tests, clippy with `-D warnings`, and diff whitespace checks passed.
- Continued Milestone 2 Phase 2 by promoting contract, mask, authoritative state, and authoritative patch surfaces from legacy digest-preparation entries into real `CanonicalBasisReadyArtifact` builders. Added shared aspect-value-to-canonical-basis lowering, facade exports, hostile certification for declaration order, mask mode/path order, numeric width distinctions, struct field truth, patch clear/set distinction, and compile-fail coverage proving legacy digest-ready artifacts cannot satisfy canonical-basis APIs. Full `forge-foundational` tests, clippy with `-D warnings`, and diff whitespace checks passed.
- QA-loop on the Milestone 2 Phase 2 builders found three real issues: semantic value families were being lowered into prefixed text, contract canonical basis used debug/packed string shortcuts, and empty patches could not become canonical-basis evidence. Fixed those by adding typed canonical basis value carriers for decimal/bigint/rational/refs/temporal values, splitting contract facts into explicit entries with explicit enum vocabularies, and representing empty patches as explicit no-op evidence. Added hostile tests for native-vs-JSON state parity, decimal-vs-text category adjacency, and empty patch canonicalization. Rechecked `forge-foundational` and `forge-proof` with full tests and `-D warnings` clippy.
- Implemented Forge Foundational Milestone 2 Phase 3 comparison readiness and mismatch basis. Added `CanonicalEquivalenceBasis`, `CanonicalComparisonReadyArtifact`, structured `CanonicalComparisonOutcome`, and separate mismatch evidence with smallest-locus reporting. Added explicit comparison preparation through `forge-proof` proofs for equivalence-basis declaration and mismatch-loci binding, plus facade exports. Certification now covers exact equivalence, compatibility-lowered equivalence, missing/additional/value/kind mismatches, structured unsupported version/domain/projection comparisons, blind-consumer-readable mismatch fields, and a trybuild gate proving raw ready basis artifacts cannot call semantic comparison APIs. Full `forge-foundational` tests, compile-time boundaries, clippy with `-D warnings`, and diff whitespace checks passed.
- Implemented Forge Foundational Milestone 2 Phase 4 export fixture bundles. Added `CanonicalExportReady`, `CanonicalExportBundle`, manifest rows with rule version/domain/producer shape/equivalence basis/entry count/cost, harness seed/debt markers, semantic export comparison, first canonical mismatch reporting, and trust-boundary bridge/readmission helpers using `forge-proof` boundary freshness. Certification covers manifest completeness, fixture-name/order-insensitive semantic comparison, manifest-vs-basis mismatch separation, first mismatch loci, readmission after boundary crossing, and a trybuild gate proving boundary-bridged exports cannot satisfy current export APIs before readmission. `forge-foundational` full tests, compile-time boundaries, clippy with `-D warnings`, and diff whitespace checks passed.
- Continued Forge Foundational Milestone 2 Phase 5 with digest algorithm slots and derived digest values. Added typed single-sequence/domain-bundle/export-bundle digest slots, `CanonicalDigestDerivationReady` proof artifacts, deterministic fixture digest derivation from canonical basis/export evidence, algorithm/input metadata, production-crypto debt markers, and checked denials for unsupported algorithm, rule-version mismatch, domain mismatch, and shape mismatch. Added certification for sequence/bundle/export digest derivation, digest metadata, equal-looking cross-domain collision hostility, digest-vs-equivalence separation, and trybuild gates proving raw bytes, plain basis payloads, digest values, and wrong-shaped slots cannot satisfy digest derivation/equivalence APIs. `forge-foundational` full tests, compile-time boundaries, clippy with `-D warnings`, and diff whitespace checks passed.
- Implemented Forge Foundational Milestone 2 Phase 6 production-test readiness certification. Added `CanonicalProductionTestReadyArtifact`, `CanonicalProductionReadinessCertified`, a sealed `CanonicalProductionReadinessAuthority`, machine-checkable certified-surface/pressure/compile-fail/golden/property/forge-harness/assumption/non-assumption/debt/phase-gate/fixture inventories, and facade APIs for certifying and requiring production readiness. Added certification tests for complete inventory, hostile pressures, fixture-to-harness mapping, linear phase gates, and topology separation, plus trybuild gates proving prose closeouts, plain reports, and caller-minted readiness authority cannot satisfy readiness-required APIs. `forge-foundational` full tests, compile-time boundaries, clippy with `-D warnings`, and diff whitespace checks passed.
- Closed an additional Milestone 2 implementation-hardening slice: production readiness now carries explicit `CanonicalCostCounterEvidence` for basis construction, Milestone 1 surface lowering, export manifest rows, and digest input metadata, and the readiness checklist requires it. Added a `canonicalization/proof_carriage` certification home proving basis, bundle, comparison, export, digest derivation, and production readiness artifacts expose the named proof-set surfaces required by the spec. `forge-foundational` focused tests, full tests, clippy with `-D warnings`, and diff whitespace checks passed.
- Closed another Milestone 2 readiness-hardening implementation batch: added `CanonicalCertifiedSurfaceEvidence` rows binding every certified surface to hostile-pressure evidence, blind-consumer test files, compile-fail boundaries, and golden artifacts where required. The production readiness checklist now requires per-surface evidence, and tests verify every evidence path points at a real certification file. `forge-foundational` focused production-readiness tests, full tests, clippy with `-D warnings`, and diff whitespace checks passed.
- QA-loop on the certified-surface evidence manifest found two readiness proof holes: compile-fail evidence was enum-only instead of tied to concrete UI fixture files, and equivalence/mismatch certified surfaces lacked required golden artifact inventory. Fixed both by adding compile-fail evidence paths to `CanonicalCertifiedSurfaceEvidence`, adding `EquivalenceBasis` and `MismatchBasis` golden artifact variants/fixture rows, binding those artifacts to the relevant certified surfaces, and testing real file existence. Full `forge-foundational` tests, clippy with `-D warnings`, and diff whitespace checks passed.
- QA-tests/code-quality QA found Milestone 2 readiness was still too ceremonial and export readiness was structurally non-scalable. Split the 645-line export module into authority/bundle/comparison/preparation/readmission homes, bound every certified surface to explicit cost-counter evidence, made production-readiness inventories exact/no-duplicate instead of contains-only, added dedicated golden equivalence/mismatch artifacts, and updated trybuild snapshots after the split. `cargo test -p forge-foundational`, `cargo clippy -p forge-foundational --all-targets --no-deps -- -D warnings`, and `git diff --check` passed.
- Started Forge Foundational Milestone 3 with typed profile vocabulary and canonical profile-basis participation. Added a `profiles` responsibility home, profile families for diagnostic richness/support/compatibility/admission/retention/descriptive elision, profile-set composition with duplicate/incoherent denial cases, canonical-basis lowering through `CanonicalBasisDomain::Profile` and `CanonicalBasisEntryKind::Profile`, facade exports, runtime certification, and compile-fail tests proving raw profile entries/private profile set construction cannot satisfy profile-basis APIs. `cargo test -p forge-foundational`, `cargo clippy -p forge-foundational --all-targets --no-deps -- -D warnings`, and `git diff --check` passed.
- Continued Milestone 3 with profile attachment contracts. Added `FoundationalProfiledArtifact<T>`, profile attachment policies, attachment denial vocabulary, `attach_foundational_profile_to_artifact`, and profile-basis evidence carried alongside the attached boundary artifact. Certification now proves a `forge-proof` boundary artifact can carry a reduced-richness profile without mutating authoritative payload truth, and trybuild proves a raw artifact cannot satisfy profiled-boundary APIs. Full `forge-foundational` tests, clippy with `-D warnings`, and diff whitespace checks passed.
- QA-loop on Milestone 3 profile attachment found the attachment API was still too broad: it accepted arbitrary payloads and carried no target-kind evidence. Fixed by adding `FoundationalProfileAttachmentTarget`, `FoundationalProfileAttachmentTargetKind`, requiring profile targets to opt in, carrying target-kind evidence in `FoundationalProfiledArtifact`, and adding trybuild coverage proving plain payloads cannot receive foundational profiles. Reverified full `forge-foundational` tests, clippy with `-D warnings`, and diff whitespace checks.
- Continued Milestone 3 with profile identity derivation. Added `FoundationalProfileIdentity` and `derive_foundational_profile_identity`, which re-admits composed profiles through `CanonicalBasisDomain::Profile` and the canonical single-sequence digest slot instead of accepting raw digest values. Added certification for order-independent profile identity, semantic profile changes producing different digests while preserving authoritative truth, explicit digest debt, and trybuild gates proving profile identity fields are private and raw canonical digests cannot satisfy profile-identity APIs. Full `forge-foundational` tests, clippy with `-D warnings`, and diff whitespace checks passed.
- Continued Milestone 3 with central profile materialization/elision controls. Added typed optional descriptive surfaces, materialization seams, disposition decisions, `FoundationalProfileMaterializationPlan`, and `plan_foundational_profile_materialization`; added `DescriptiveElisionProfile::OperationalSummary` for centrally suppressing history/replay/lineage/provenance/forensic diagnostics without touching authoritative truth. Certification now covers full-fidelity materialization, replay/history-only suppression, operational-summary suppression of every optional descriptive surface, empty/duplicate surface denials, and forensic diagnostics rejecting forensic-suppressing elision. Trybuild now proves materialization plan fields are private and raw strings cannot stand in for descriptive surfaces. Full `forge-foundational` tests, clippy with `-D warnings`, and diff whitespace checks passed.
- QA-loop on Milestone 3 materialization found the default planner could still produce partial plans, making omitted surfaces implicit, and it carried no cost/exhaustiveness evidence. Fixed by making `plan_foundational_profile_materialization` exhaustive over the sealed `FOUNDATIONAL_DESCRIPTIVE_SURFACES`, moving partial requests behind explicit `plan_selected_foundational_profile_materialization`, adding `FoundationalMaterializationCost`, and testing exhaustive vs non-exhaustive behavior. Also removed an unnecessary panic-shaped static invariant from the exhaustive planner. Full tests, clippy with `-D warnings`, and diff checks passed.
- Continued Milestone 3 with the missing certification posture profile family. Added `CertificationPostureProfile::{Uncertified, EvidenceBacked, ProductionCertified}`, canonical profile tokens, facade exports, profile-family ordering, and composition denials requiring certified support to carry production certification and requiring production certification to declare durable retention. Updated profile composition and identity certification so production profile identity now includes certification posture as a canonical profile-basis entry. Focused profile tests, profile trybuild, full `forge-foundational` tests, clippy with `-D warnings`, and diff checks passed.
- Per implementation-batch closeout rule, added a machine-checkable Milestone 3 profile readiness report instead of inventing another profile feature. `foundational_profile_milestone3_readiness_report` now enumerates certified profile surfaces, hostile pressures, compile-fail boundaries, linear phase gates, owning certification tests, and runtime non-assumptions. Added profile readiness certification tests proving the report covers every Milestone 3 surface and points at real owning evidence paths. Focused profile readiness tests, profile trybuild, full `forge-foundational` tests, clippy with `-D warnings`, and diff checks passed.
- QA-loop on Milestone 3 readiness found two proof holes: runtime non-assumptions were raw strings instead of typed vocabulary, and compile-fail boundaries could be listed without one concrete UI evidence fixture per boundary. Fixed by adding `FoundationalProfileRuntimeNonAssumption`, `FoundationalProfileCompileFailEvidence`, a `ProfileSetFieldsArePrivate` boundary, exact checklist coverage for boundary evidence and hostile pressures, and stronger readiness tests for real evidence paths. Rechecked focused profile readiness, profile certification, profile trybuild, full `forge-foundational` tests, clippy with `-D warnings`, and diff checks.
- Started Forge Foundational Milestone 4 with boundary artifact category and materialization contracts. Added an `artifacts` responsibility home with sealed `Summary`, `Report`, `Artifact`, and `Receipt` category markers, role/source/seam vocabulary, explicit materialization input/output types, cost counters, profile-elision decisions at the materialization seam, and denials that keep branch/merge/commit evidence in Milestone 5. Certification now proves category distinction, source-layout parity, visible elision/cost, receipt/authority rejection, and Milestone 5 non-ownership. Trybuild proves materialized boundary fields are private and summaries cannot satisfy report APIs. Focused artifact tests, full compile-fail suite, and full `forge-foundational` tests passed.
- Continued Milestone 4 by making materialized boundary artifacts participate in canonical basis and digest preparation. Added `CanonicalBasisDomain::BoundaryArtifact`, artifact category/role/materialization/attachment entry kinds, `FoundationalBoundaryAttachmentPoint`, and `prepare_foundational_materialized_boundary_for_canonical_basis`. Materialized boundaries now carry their composed profile, profile decisions, attachment-point inventory, and cost evidence into replayable canonical basis without including crate-local source layout as semantic meaning. Certification proves profile/cost basis evidence, source-layout parity when cost is equal, and deferred diagnostic/provenance attachment points; trybuild now proves a raw materialized boundary cannot satisfy canonical-basis-ready APIs. Full `forge-foundational` tests, compile-fail suite, and clippy with `-D warnings` passed.
>>>>>>> origin/forge-foundational


- Created and switched to branch `forge-foundational`.
- Restored `forge-foundational` roadmap/vision docs from `worktree-4` into the branch.
- Drafted `_docs/forge-foundational/milestone-1.md` for the Aspec-native canonical value and aspect-state substrate, using the exact `AspectValue` vocabulary from `worktree-4` `forge-relational`.
- QA-revised Milestone 1 to make aspect contracts, struct aspect values, masks, absence/null/default/clear law, evolution posture, equivalence basis, compile-time enforcement targets, and linear phase gates explicit.
- Added `_docs/forge-foundational/test-requirements.md` because the implementation strategy changed: build foundational completely first, then refactor adopting crates in large migrations. The test bar now requires standalone compile-fail, property, hostile, golden, composition, and migration-readiness evidence before broad crate adoption.
- After pulling `origin/master`, confirmed `crates/forge-proof` exists and revised the Forge Foundational Milestone 1 plan to use `forge-proof` for proof-bearing progression surfaces: contract validation, evolution classification, authoritative-state admission, compatibility lowering, and digest-preparation readiness. Plain aspect values, keys, masks, locators, and identity wrappers remain lightweight `forge-foundational` vocabulary.
- Began Forge Foundational Milestone 1 implementation with the Phase 1 crate boundary: registered `crates/forge-foundational`, added a curated facade, named private responsibility homes, introduced a `forge-proof::Artifact` boundary declaration, and added trybuild compile-fail tests proving internal homes and boundary payload fields are sealed.
- Implemented Forge Foundational Milestone 1 Phases 2 and 3 in one larger batch: canonical relational aspect-value families, scalar wrappers, aspect keys/contracts, struct aspect shapes/values, phantom-typed masks, evolution/equivalence law, and proof-bearing contract validation through `forge-proof::Artifact` and `TransitionOutcome`. Added focused runtime tests plus trybuild gates for no generic document variant, mask-mode separation, raw values not satisfying validated APIs, and sealed struct-value fields.
- Code-quality QA found the initial `forge-foundational` implementation was not scalable: `aspects.rs` was a god file, `values.rs` was too broad, and tests were phase/provenance-shaped. Refactored into roadmap-shaped production modules under `values/`, `aspects/contracts`, `aspects/structs`, `aspects/masks`, `aspects/evolution`, `aspects/validation`, `boundary/`, and directory homes for future compatibility/canonicalization/identity/locator work. Moved tests under `tests/certification/` and boundary-specific `tests/ui/*` directories, leaving only Cargo harnesses at test root.
- Began Forge Foundational Milestone 2 with the canonical basis grammar slice: added rule versions, typed domains, loci, entry ids, entry kinds, basis values, canonicalization cost counters, basis sequences/bundles, proof-phase markers, and `forge-proof::Artifact` readiness for basis and bundle construction. Added facade exports, runtime certification for ordering/duplicate/domain/bundle law, and compile-fail tests proving raw sequences, raw indexes, and direct sequence field construction cannot satisfy readiness boundaries. Verification passed fmt, full `cargo test -p forge-foundational`, and clippy with warnings denied.
- QA-hardened Milestone 2 Phase 1 after review: removed the `AspectValue` escape hatch from canonical basis values, introduced explicit integer/float width carriers, rejected whitespace-shaped rule-version identities, rejected duplicate semantic entry keys even when values differ, and made canonical basis artifacts carry real `forge-proof` proof sets instead of `NoProofs`. Hardened `forge-proof` itself with authority-scoped `Proof<P, A>`, a proof-bearing current-basis artifact constructor, non-copy proof tokens, and compile-fail coverage for proof authority substitution.
- Finished the follow-up `forge-proof` hardening pass requested before Milestone 2 continues: removed the legacy unscoped proof authority/default, added proof-kind authorization through `AuthorityProves<P>`, required same-authority proof sets for current-basis construction, updated docs and trybuild snapshots to the scoped proof model, and cleared `forge-proof`/`forge-foundational` clippy with `-D warnings`.
- QA pass on the five `forge-proof` hardening items found stale `Proof<P>` examples in the Milestone 2 spec and added missing hostile compile-fail evidence for unapproved proof-kind minting plus mixed-authority current-basis proof sets. Full `forge-proof` and `forge-foundational` tests and `-D warnings` clippy passed afterward.
- Continued Forge Foundational Milestone 2 Phase 2 with identity and locator canonical-basis builders. Added proof-bearing `CanonicalBasisReadyArtifact` preparation for identity categories and locator categories, facade exports, hostile certification for category collapse, locator authority/path/mask-mode preservation, and a compile-fail boundary proving raw identity wrappers cannot satisfy ready-basis APIs. Focused tests, full `forge-foundational` tests, clippy with `-D warnings`, and diff whitespace checks passed.
- Continued Milestone 2 Phase 2 by promoting contract, mask, authoritative state, and authoritative patch surfaces from legacy digest-preparation entries into real `CanonicalBasisReadyArtifact` builders. Added shared aspect-value-to-canonical-basis lowering, facade exports, hostile certification for declaration order, mask mode/path order, numeric width distinctions, struct field truth, patch clear/set distinction, and compile-fail coverage proving legacy digest-ready artifacts cannot satisfy canonical-basis APIs. Full `forge-foundational` tests, clippy with `-D warnings`, and diff whitespace checks passed.
- QA-loop on the Milestone 2 Phase 2 builders found three real issues: semantic value families were being lowered into prefixed text, contract canonical basis used debug/packed string shortcuts, and empty patches could not become canonical-basis evidence. Fixed those by adding typed canonical basis value carriers for decimal/bigint/rational/refs/temporal values, splitting contract facts into explicit entries with explicit enum vocabularies, and representing empty patches as explicit no-op evidence. Added hostile tests for native-vs-JSON state parity, decimal-vs-text category adjacency, and empty patch canonicalization. Rechecked `forge-foundational` and `forge-proof` with full tests and `-D warnings` clippy.
- Implemented Forge Foundational Milestone 2 Phase 3 comparison readiness and mismatch basis. Added `CanonicalEquivalenceBasis`, `CanonicalComparisonReadyArtifact`, structured `CanonicalComparisonOutcome`, and separate mismatch evidence with smallest-locus reporting. Added explicit comparison preparation through `forge-proof` proofs for equivalence-basis declaration and mismatch-loci binding, plus facade exports. Certification now covers exact equivalence, compatibility-lowered equivalence, missing/additional/value/kind mismatches, structured unsupported version/domain/projection comparisons, blind-consumer-readable mismatch fields, and a trybuild gate proving raw ready basis artifacts cannot call semantic comparison APIs. Full `forge-foundational` tests, compile-time boundaries, clippy with `-D warnings`, and diff whitespace checks passed.
- Implemented Forge Foundational Milestone 2 Phase 4 export fixture bundles. Added `CanonicalExportReady`, `CanonicalExportBundle`, manifest rows with rule version/domain/producer shape/equivalence basis/entry count/cost, harness seed/debt markers, semantic export comparison, first canonical mismatch reporting, and trust-boundary bridge/readmission helpers using `forge-proof` boundary freshness. Certification covers manifest completeness, fixture-name/order-insensitive semantic comparison, manifest-vs-basis mismatch separation, first mismatch loci, readmission after boundary crossing, and a trybuild gate proving boundary-bridged exports cannot satisfy current export APIs before readmission. `forge-foundational` full tests, compile-time boundaries, clippy with `-D warnings`, and diff whitespace checks passed.
- Continued Forge Foundational Milestone 2 Phase 5 with digest algorithm slots and derived digest values. Added typed single-sequence/domain-bundle/export-bundle digest slots, `CanonicalDigestDerivationReady` proof artifacts, deterministic fixture digest derivation from canonical basis/export evidence, algorithm/input metadata, production-crypto debt markers, and checked denials for unsupported algorithm, rule-version mismatch, domain mismatch, and shape mismatch. Added certification for sequence/bundle/export digest derivation, digest metadata, equal-looking cross-domain collision hostility, digest-vs-equivalence separation, and trybuild gates proving raw bytes, plain basis payloads, digest values, and wrong-shaped slots cannot satisfy digest derivation/equivalence APIs. `forge-foundational` full tests, compile-time boundaries, clippy with `-D warnings`, and diff whitespace checks passed.
- Implemented Forge Foundational Milestone 2 Phase 6 production-test readiness certification. Added `CanonicalProductionTestReadyArtifact`, `CanonicalProductionReadinessCertified`, a sealed `CanonicalProductionReadinessAuthority`, machine-checkable certified-surface/pressure/compile-fail/golden/property/forge-harness/assumption/non-assumption/debt/phase-gate/fixture inventories, and facade APIs for certifying and requiring production readiness. Added certification tests for complete inventory, hostile pressures, fixture-to-harness mapping, linear phase gates, and topology separation, plus trybuild gates proving prose closeouts, plain reports, and caller-minted readiness authority cannot satisfy readiness-required APIs. `forge-foundational` full tests, compile-time boundaries, clippy with `-D warnings`, and diff whitespace checks passed.
- Closed an additional Milestone 2 implementation-hardening slice: production readiness now carries explicit `CanonicalCostCounterEvidence` for basis construction, Milestone 1 surface lowering, export manifest rows, and digest input metadata, and the readiness checklist requires it. Added a `canonicalization/proof_carriage` certification home proving basis, bundle, comparison, export, digest derivation, and production readiness artifacts expose the named proof-set surfaces required by the spec. `forge-foundational` focused tests, full tests, clippy with `-D warnings`, and diff whitespace checks passed.
- Closed another Milestone 2 readiness-hardening implementation batch: added `CanonicalCertifiedSurfaceEvidence` rows binding every certified surface to hostile-pressure evidence, blind-consumer test files, compile-fail boundaries, and golden artifacts where required. The production readiness checklist now requires per-surface evidence, and tests verify every evidence path points at a real certification file. `forge-foundational` focused production-readiness tests, full tests, clippy with `-D warnings`, and diff whitespace checks passed.
- QA-loop on the certified-surface evidence manifest found two readiness proof holes: compile-fail evidence was enum-only instead of tied to concrete UI fixture files, and equivalence/mismatch certified surfaces lacked required golden artifact inventory. Fixed both by adding compile-fail evidence paths to `CanonicalCertifiedSurfaceEvidence`, adding `EquivalenceBasis` and `MismatchBasis` golden artifact variants/fixture rows, binding those artifacts to the relevant certified surfaces, and testing real file existence. Full `forge-foundational` tests, clippy with `-D warnings`, and diff whitespace checks passed.
- QA-tests/code-quality QA found Milestone 2 readiness was still too ceremonial and export readiness was structurally non-scalable. Split the 645-line export module into authority/bundle/comparison/preparation/readmission homes, bound every certified surface to explicit cost-counter evidence, made production-readiness inventories exact/no-duplicate instead of contains-only, added dedicated golden equivalence/mismatch artifacts, and updated trybuild snapshots after the split. `cargo test -p forge-foundational`, `cargo clippy -p forge-foundational --all-targets --no-deps -- -D warnings`, and `git diff --check` passed.
- Started Forge Foundational Milestone 3 with typed profile vocabulary and canonical profile-basis participation. Added a `profiles` responsibility home, profile families for diagnostic richness/support/compatibility/admission/retention/descriptive elision, profile-set composition with duplicate/incoherent denial cases, canonical-basis lowering through `CanonicalBasisDomain::Profile` and `CanonicalBasisEntryKind::Profile`, facade exports, runtime certification, and compile-fail tests proving raw profile entries/private profile set construction cannot satisfy profile-basis APIs. `cargo test -p forge-foundational`, `cargo clippy -p forge-foundational --all-targets --no-deps -- -D warnings`, and `git diff --check` passed.
- Continued Milestone 3 with profile attachment contracts. Added `FoundationalProfiledArtifact<T>`, profile attachment policies, attachment denial vocabulary, `attach_foundational_profile_to_artifact`, and profile-basis evidence carried alongside the attached boundary artifact. Certification now proves a `forge-proof` boundary artifact can carry a reduced-richness profile without mutating authoritative payload truth, and trybuild proves a raw artifact cannot satisfy profiled-boundary APIs. Full `forge-foundational` tests, clippy with `-D warnings`, and diff whitespace checks passed.
- QA-loop on Milestone 3 profile attachment found the attachment API was still too broad: it accepted arbitrary payloads and carried no target-kind evidence. Fixed by adding `FoundationalProfileAttachmentTarget`, `FoundationalProfileAttachmentTargetKind`, requiring profile targets to opt in, carrying target-kind evidence in `FoundationalProfiledArtifact`, and adding trybuild coverage proving plain payloads cannot receive foundational profiles. Reverified full `forge-foundational` tests, clippy with `-D warnings`, and diff whitespace checks.
- Continued Milestone 3 with profile identity derivation. Added `FoundationalProfileIdentity` and `derive_foundational_profile_identity`, which re-admits composed profiles through `CanonicalBasisDomain::Profile` and the canonical single-sequence digest slot instead of accepting raw digest values. Added certification for order-independent profile identity, semantic profile changes producing different digests while preserving authoritative truth, explicit digest debt, and trybuild gates proving profile identity fields are private and raw canonical digests cannot satisfy profile-identity APIs. Full `forge-foundational` tests, clippy with `-D warnings`, and diff whitespace checks passed.
- Continued Milestone 3 with central profile materialization/elision controls. Added typed optional descriptive surfaces, materialization seams, disposition decisions, `FoundationalProfileMaterializationPlan`, and `plan_foundational_profile_materialization`; added `DescriptiveElisionProfile::OperationalSummary` for centrally suppressing history/replay/lineage/provenance/forensic diagnostics without touching authoritative truth. Certification now covers full-fidelity materialization, replay/history-only suppression, operational-summary suppression of every optional descriptive surface, empty/duplicate surface denials, and forensic diagnostics rejecting forensic-suppressing elision. Trybuild now proves materialization plan fields are private and raw strings cannot stand in for descriptive surfaces. Full `forge-foundational` tests, clippy with `-D warnings`, and diff whitespace checks passed.
- QA-loop on Milestone 3 materialization found the default planner could still produce partial plans, making omitted surfaces implicit, and it carried no cost/exhaustiveness evidence. Fixed by making `plan_foundational_profile_materialization` exhaustive over the sealed `FOUNDATIONAL_DESCRIPTIVE_SURFACES`, moving partial requests behind explicit `plan_selected_foundational_profile_materialization`, adding `FoundationalMaterializationCost`, and testing exhaustive vs non-exhaustive behavior. Also removed an unnecessary panic-shaped static invariant from the exhaustive planner. Full tests, clippy with `-D warnings`, and diff checks passed.
- Continued Milestone 3 with the missing certification posture profile family. Added `CertificationPostureProfile::{Uncertified, EvidenceBacked, ProductionCertified}`, canonical profile tokens, facade exports, profile-family ordering, and composition denials requiring certified support to carry production certification and requiring production certification to declare durable retention. Updated profile composition and identity certification so production profile identity now includes certification posture as a canonical profile-basis entry. Focused profile tests, profile trybuild, full `forge-foundational` tests, clippy with `-D warnings`, and diff checks passed.
- Per implementation-batch closeout rule, added a machine-checkable Milestone 3 profile readiness report instead of inventing another profile feature. `foundational_profile_milestone3_readiness_report` now enumerates certified profile surfaces, hostile pressures, compile-fail boundaries, linear phase gates, owning certification tests, and runtime non-assumptions. Added profile readiness certification tests proving the report covers every Milestone 3 surface and points at real owning evidence paths. Focused profile readiness tests, profile trybuild, full `forge-foundational` tests, clippy with `-D warnings`, and diff checks passed.
- QA-loop on Milestone 3 readiness found two proof holes: runtime non-assumptions were raw strings instead of typed vocabulary, and compile-fail boundaries could be listed without one concrete UI evidence fixture per boundary. Fixed by adding `FoundationalProfileRuntimeNonAssumption`, `FoundationalProfileCompileFailEvidence`, a `ProfileSetFieldsArePrivate` boundary, exact checklist coverage for boundary evidence and hostile pressures, and stronger readiness tests for real evidence paths. Rechecked focused profile readiness, profile certification, profile trybuild, full `forge-foundational` tests, clippy with `-D warnings`, and diff checks.
- Started Forge Foundational Milestone 4 with boundary artifact category and materialization contracts. Added an `artifacts` responsibility home with sealed `Summary`, `Report`, `Artifact`, and `Receipt` category markers, role/source/seam vocabulary, explicit materialization input/output types, cost counters, profile-elision decisions at the materialization seam, and denials that keep branch/merge/commit evidence in Milestone 5. Certification now proves category distinction, source-layout parity, visible elision/cost, receipt/authority rejection, and Milestone 5 non-ownership. Trybuild proves materialized boundary fields are private and summaries cannot satisfy report APIs. Focused artifact tests, full compile-fail suite, and full `forge-foundational` tests passed.
- Continued Milestone 4 by making materialized boundary artifacts participate in canonical basis and digest preparation. Added `CanonicalBasisDomain::BoundaryArtifact`, artifact category/role/materialization/attachment entry kinds, `FoundationalBoundaryAttachmentPoint`, and `prepare_foundational_materialized_boundary_for_canonical_basis`. Materialized boundaries now carry their composed profile, profile decisions, attachment-point inventory, and cost evidence into replayable canonical basis without including crate-local source layout as semantic meaning. Certification proves profile/cost basis evidence, source-layout parity when cost is equal, and deferred diagnostic/provenance attachment points; trybuild now proves a raw materialized boundary cannot satisfy canonical-basis-ready APIs. Full `forge-foundational` tests, compile-fail suite, and clippy with `-D warnings` passed.
