# 2026-05-12

- Continued the wasm forms milestone with a controller-local step artifact slice.
  Added `signals.form({ steps })` declarations with stable step ids, groups,
  ordering, dynamic skipped/blocked/removed/unavailable posture, dependency
  breadth, and derived step reports over existing validation, messages, dirty,
  patch, availability, and admission truth. Exposed `form.steps()` through
  runtime diagnostics and public TypeScript types, added type-smoke coverage,
  and focused runtime tests for step derivation plus hostile topology/posture
  denials. Verification passed focused forms tests, aggregate signals runtime,
  package surface typecheck, and forms line-count checks.
- Continued Phase 4 with regulated admission binding evidence. Admission
  execution now derives current source, semantic patch, schema, and binding
  digests and passes them to admission resolvers. Regulated
  approval/signature/review/reason artifacts require actor and policy digests,
  carry expected/current binding evidence, and mark stale when old evidence no
  longer matches source, patch, or schema truth. Added public type surface,
  type-smoke coverage, hostile stale-evidence tests, and denial tests for
  missing actor/policy evidence. Verification passed focused forms tests,
  aggregate signals runtime, package surface typecheck, and forms line-count
  checks.
- QA loop on regulated admission binding found that actor and policy drift were
  not actually part of staleness because the artifact used the resolver's
  current actor/policy as both expected and current truth, and the binding
  digest did not include actor/policy evidence. Added explicit current
  actor/policy evidence, included expected/current authority evidence in the
  binding digest, surfaced stale binding reasons through admission blockers,
  and strengthened tests for actor, policy, source, patch, and schema drift.
- Reread the full forms product surface plan to keep the whole milestone vision
  in view before continuing implementation. Continued Phase 4 with dynamic
  availability topology for controls, groups, and sections in addition to fields
  and actions. Availability declarations now expose `control`, `group`, and
  `section` builders, region artifacts preserve field membership, summaries
  include scope counts, step readiness projects blocked group/section regions to
  the affected step fields, and non-field draft policies are denied. Added
  public type surface, type-smoke coverage, focused topology/denial tests, and
  aggregate runtime import coverage. Verification passed focused availability
  tests, all forms tests, aggregate signals runtime, package typecheck, and
  forms line-count checks.
- Continued Phase 4 closeout with explicit breadth/cost evidence for dynamic
  availability, admission, and controller-local steps. Availability reports now
  expose declaration, dependency-read, region-membership, blocking-artifact, and
  scope counters. Admission reports expose declaration, dependency-read,
  field/action scope, regulated artifact, and stale regulated artifact counters.
  Step reports expose declaration, field-membership, dependency-read, blocker,
  patch projection, validation projection, and message projection counters.
  Strengthened runtime tests with exact counter assertions and updated public
  types/type-smoke coverage. Verification passed focused forms tests, aggregate
  signals runtime, package typecheck, and forms line-count checks.
- QA loop on the breadth/cost evidence found that counter names did not state
  whether they represented current derived-scan evidence or future incremental
  scheduling bounds, and step projection counters could double-count form-level
  artifacts without exposing a unique count. Added explicit `costBasis:
  "derivedFullReportScan"` and `incrementalStatus: "notIncremental"` metadata
  to availability/admission/step counters, split step total projections from
  unique validation/message projections, and strengthened tests with a form-wide
  message projected into multiple steps.
- Continued Phase 5 with the action declaration and planning foundation. Forms
  now materialize a default `submit` action plus custom and controller-local
  step actions, lower every action into a proof-bearing plan with readiness,
  patch, validation, availability, admission, policy posture, idempotency, and
  digest evidence, and expose `form.actions()`, `form.actionPlan(id)`, and
  action diagnostics. Submit defaults to non-empty patch denial, custom actions
  can allow empty patches, destructive/idempotency/host-effect posture is
  explicit, and step navigation actions stay route-free/controller-local.
  Verification passed focused action/admission/step forms tests, aggregate
  signals runtime, package surface typecheck, and line-count checks.
- Continued Phase 5 with action result artifacts and repeated-attempt policy.
  Added `form.attemptAction(id)` and `form.actionHistory()` as controller-local
  planning/attempt artifacts that consume lowered action plans without running
  async/resource effects yet. Results now expose accepted, denied, no-op, and
  superseded outcomes, recovery actions for blockers, result digests, and
  collapse/supersede/queue/deny idempotency behavior for repeated matching plan
  digests. Verification passed focused action tests, aggregate runtime,
  package surface typecheck, diff whitespace check, and line-count checks.
- QA loop on Phase 5 action attempts found and fixed three proof issues:
  `allowEmpty` was incorrectly treated as no-op even for effectful custom
  actions, supersession history did not name the superseded attempt, and
  recovery emitted duplicate focus-first actions. A second pass found that
  action plan digests did not include host-effect/action-schema proof, and
  regulated action admission evidence was not tied to the exact action plan
  digest. Added effect/action-schema digests, regulated action binding
  attestations, stronger tests, and type-smoke coverage. Verification passed
  focused action tests, package surface typecheck, aggregate runtime, diff
  whitespace check, and line-count checks.
- Continued Phase 5 closeout with form verification packages for action
  diagnostics/history proof. Added action report digests for catalog,
  readiness/admission, plan digest set, and submit plan. Added
  `form.verification()` with source/draft/effective/patch/readiness/validation/
  availability/admission/step/action/lifecycle/diagnostics-history digests and
  a visible derived-scan performance envelope. Split action verification tests
  into their own file to keep line caps clean. Verification passed focused
  action/verification tests, package surface typecheck, aggregate runtime, diff
  whitespace check, and line-count checks.
- Started Phase 6 with action execution lifecycle artifacts. Added
  `form.executeAction(id)`, terminal settlement methods (`fulfillAction`,
  `rejectAction`, `cancelAction`, `timeoutAction`, `retryAction`), and
  `actionExecutionHistory()`. Effect-backed actions now produce pending
  operation artifacts tied to action plan/attempt digests; denied plans do not
  start effects; stale completions produce typed stale artifacts; superseded
  pending executions are recorded; server rejection messages are marked
  `source: "server"` rather than local validation. Verification digests now
  include action execution lifecycle history. Verification passed focused
  action execution tests, package surface typecheck, aggregate runtime, diff
  whitespace check, and line-count checks.
- QA loop on Phase 6 action execution found that stale completion artifacts
  were returned but not recorded in execution history, pending completions did
  not compare against current action plan digests after form truth changed,
  canonical fulfillment payloads were stored by reference, and retry could
  restart stale/non-retryable executions without recording the denial. Fixed
  all four: stale completions and invalid retries now append lifecycle
  artifacts, fulfill/reject compare pending plan digest against current form
  truth, canonical values are cloned into artifacts, retries are limited to
  rejected/timed-out/cancelled terminal operations, and public types now expose
  the plan/attempt snapshots already present on execution artifacts.
- QA-tests pass on Phase 6 action execution tests found repeated inline
  single-field/action form setup and lifecycle assertions that checked only
  spot outcomes rather than exact ordered history. Rewrote
  `form_action_execution.test.mjs` with narrow local fixture/proof helpers and
  exact `actionExecutionHistory()` result-kind assertions for pending,
  fulfilled, denied, cancelled, stale, superseded, rejected, and retry paths.
  Confirmed the forms test directory remains under the 10-file cap.
- Code-quality QA pass on Phase 6 action execution found two structural proof
  weaknesses: stale completion artifacts named only the target operation id
  without target action/plan/execution identity, and canonical server values
  were cloned but still mutable through the returned artifact. Execution
  stale artifacts now carry target action, target plan digest, and target
  execution digest when known; canonical values are recursively frozen after
  cloning; retry lookup reuses the named operation-history helper. Focused
  execution tests, aggregate runtime tests, package surface typecheck, and
  diff whitespace checks passed.
- Continued Phase 6 after rereading the full forms product spec. Added async
  validation declarations with trigger policy, a runtime async validation
  lifecycle store, controller methods to start/fulfill/reject/cancel/timeout
  async validations, stale completion denial tied to current form truth, and
  async validation history/digests in diagnostics and verification packages.
  Focused tests now cover pending readiness, stale fulfillment after draft
  change, server rejection mapping, cancellation, timeout, malformed trigger
  declarations, and undeclared starts. Verification passed focused async
  validation tests, aggregate signals runtime, package surface typecheck, diff
  whitespace check, and line-count checks.
- Continued Phase 6 with server canonicalization convergence. Added a
  canonicalization store that projects fulfilled server canonical values as
  authoritative form source/effective truth while the underlying source basis
  still matches, resets draft after canonical fulfillment, records
  canonicalization artifacts, and yields back to newer authoritative source
  drift instead of hiding resource/signal updates. Diagnostics and verification
  now include canonicalization history/digests/counters, and public form types
  expose canonicalization artifacts. Focused action execution tests, aggregate
  signals runtime, package surface typecheck, diff whitespace check, and
  line-count checks passed.
- QA loop on Phase 6 async validation/canonicalization found two proof gaps:
  starting a newer async validation did not supersede the older active
  operation, allowing an older completion with the same basis to overwrite the
  newer pending artifact; canonicalization history recorded only the canonical
  value digest, not the canonical value snapshot needed for replay-visible
  diagnostics. Fixed both by adding per-validation active-operation
  supersession with terminal `superseded` lifecycle artifacts and stale old
  completions, and by storing a recursively frozen canonical value snapshot in
  canonicalization artifacts. Focused async/action tests, aggregate runtime,
  package surface typecheck, diff whitespace check, and line-count/test-count
  checks passed.
- Continued forms performance hardening for deep collection equality. Added a
  semantic equality comparator that returns structural counters, changed dirty
  and patch planning to take one source/effective snapshot per derived-scan
  call instead of snapshotting inside each field loop, and exposed equality
  breadth/cost evidence on field dirty, whole-form dirty, and patch plans.
  Added hostile coverage with a nested 48-item collection proving deep
  collection comparisons are named and source/effective snapshots remain one
  per `dirty()`/`patchPlan()` call. Focused kernel tests, aggregate runtime,
  package surface typecheck, diff whitespace check, and forms line/test count
  checks passed.
- QA loop on the deep collection equality hardening found two performance/
  semantic honesty gaps: patch planning counted raw/omitted/cleared set sizes
  instead of exact declared fields skipped by the planner, and the comparator
  inherited broad object semantics that could treat keyless non-record values
  such as dates as equal. Fixed by naming comparable-field selection with exact
  skip counters and by restricting structural object comparison to true records
  while adding stable Date/Map/Set digests. Added regression coverage for raw
  input, omitted, cleared, Date, and Map behavior. Focused forms tests,
  aggregate runtime, package surface typecheck, diff whitespace check, and
  forms line-count checks passed.
