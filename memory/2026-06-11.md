# 2026-06-11

- M6.5 Phase 7 QA loop found and fixed transform workload proof gaps: reorientation is posture evidence, not coordinate movement; zero translation is denied as no transform evidence; each cancellation replay entry must independently be 16 or 64 steps instead of passing through summed split counts.
- Transform admitted evidence now exposes typed `TransformEvidenceKind` rows for coordinate change, posture change, and cancellation replay. Denials remain in `UnsupportedTransformWorkload`, not in admitted evidence.
- Verification after the QA fix: focused transform workload contracts, full worth-spatial public API contract, compile-fail UI suite, cargo check across worth-spatial/worth-kernel/worth-topo, cargo fmt, and touched-file line counts all passed. The existing MB overlap storm test still takes over 60 seconds but completes.
- Follow-up qa-tests/code-quality pass found two more Phase 7 issues: no explicit 64-step hostile cancellation admission proof, and the transform counter name `posture_rows` was dishonest because it counted all evidence rows. Fixed by adding 64-step hostile admission, combined-profile denial, exact parity reason assertions, and renaming the transform counter to `evidence_rows`.
- Ran `qa-tests` and `code-quality-qa` on M6.5 Phase 9 user responses. Found and fixed three issues: no-options tests only covered dirty input and missed missing-evidence replay denial, `WorthUserResponseSource` did not have an explicit compile-fail forgery guard, and user-response source construction mixed public source typing with adapter logic. Added a real missing-retained-artifacts replay branch test, compile-fail source forgery proof, split source adapters from source types, moved overlap policy choices into the choices module, and renamed the human response constructor to `from_source_summary`. Verification passed: focused user-response tests, full spatial public API contract, spatial UI trybuild, focused MB-M6-1 user outcome matrix, cargo check, cargo fmt, git diff check, and phase-local line/directory audits.
- M6.5 Phase 8 implemented real retained replay workload in worth-spatial. `ReplayWorkload` now consumes `RetainedArtifactSet` built from retained planar facts plus projection-consumed facts, replays through retained historical inspection, emits typed replay evidence/parity receipts, rejects missing artifacts/projection/drift with human-readable denials, and fixes retained replay vocabulary to bind to the upstream transform receipt identity instead of the declaration string.
- Phase 8 verification: `cargo test -p worth-spatial public_api -- --nocapture`, `cargo test -p worth-spatial --test ui -- --nocapture`, `cargo check -p worth-spatial`, `cargo fmt`, plus new-file line counts and directory file counts all passed.
- Phase 8 QA loop found the original implementation was not complete enough because the spec explicitly named `RetainedWorkload` and required replay to expose the retained basis/checkpoint. Fixed by adding `RetainedWorkload`, `CapturedRetainedWorkload`, `RetainedArtifactCaptureReceipt`, first-class retained basis and replay checkpoint identities on replay receipts, capture-time drift rejection, and compile-fail proof that capture receipts cannot be forged.
- Phase 8 qa-tests/code-quality pass found two further quality gaps: retained replay tests used substring identity checks where exact identity proof was available, and capture/replay orchestration methods hid too many semantic steps inline. Fixed by asserting exact retained artifact, historical replay, projection, and replay evidence identities; adding retained capture missing-input denial tests; and decomposing capture/replay production methods into named phase steps.
- Implemented M6.5 Phase 11 operator harness. Replaced the old row-count-only kernel operator workload gate with a proof-bearing `WorkloadOperator` declaration/admission/run/receipt/outcome harness, typed `UnsupportedOperatorFamily` denials, human-readable support reasons, and `CoplanarOverlapWorkloadOperator` consumption of projected/transform/retained-replay workload proof. Added spatial operator evidence rows and a real supported-path public contract using counter-backed topology, binding, support, projection, transform, and replay receipts. Verification passed: focused kernel/spatial operator contracts, full kernel public API contract, full spatial public API contract, kernel and spatial compile-fail suites, cross-crate cargo check, cargo fmt, and line/directory audits.
- Phase 11 QA loop found and fixed the first implementation's important honesty gaps: operator declaration evidence was still string-backed instead of Forge Query-backed; the concrete coplanar overlap operator lived in kernel instead of worth-spatial; generic receipt-set constructors let tests forge operator outcomes without a concrete operator; and the spatial operator accepted arbitrary evidence rows. Fixed by adding query-backed operator declaration receipts, moving the concrete coplanar operator into worth-spatial, restricting operator evidence row construction to concrete spatial receipts, and requiring projection/transform/replay stage rows to be admitted and counter-backed before execution.
- Phase 11 qa-tests/code-quality pass found two more issues: support/admission receipts were still a hand-built string digest instead of a Forge Query-backed support row, and the operator denial tests only attacked projection while transform and retained replay could regress independently. Fixed by adding a `WorkloadOperatorSupportFamily` Query declaration row, making support construction fallible through Query, removing substring assertions from support digests, making required operator evidence stages an internal typed enum instead of fallback matching arbitrary stages, and expanding the spatial operator test into a missing/manual/counterless matrix for projection, transform, and retained replay.
- M6.5 Phase 18 QA loop found and fixed proof gaps in the open/unbounded planar posture workload: `TopologySeed::open_wire()` was implemented but not exercised by the MB-M6-6 matrix, the bounded-surrogate guard only rejected a small whitelist of fake shapes, and the posture response identity did not bind tightly enough to unsupported-surface and clean-fail evidence. Fixed by adding the real open-wire matrix branch, rejecting any attempted bounded surrogate, making diagnostic evidence first-class on the open posture receipt/counters, and binding response identity to topology/support/clean-fail identities. Verification passed: focused MB-M6-6 tests, full spatial public API contract, spatial UI trybuild, kernel workload catalog tests, cross-crate cargo check, cargo fmt, git diff check, and phase-local line/directory audits.
- Ran `qa-tests` and `code-quality-qa` on M6.5 Phase 18 open/unbounded posture. Found and fixed that the test named `mb_m6_6_unbounded_half_space_planar_posture` was actually proving the open-sheet unsupported branch, not the half-space policy branch. Added a real half-space subject, made `OpenPlanarPostureReceipt` expose structured `open_input_kind` and diagnostic subject kind, and strengthened matrix assertions for every branch. Also fixed a production/test proof gap where clean-fail diagnostics could carry the wrong subject locality while the final user outcome still passed; certification now rejects mismatched diagnostic subjects for the posture case. Removed an open-sheet-only dead helper and renamed open topology binding setup so test support no longer implies sheet-only semantics. Verification passed: focused MB-M6-6 tests, spatial UI trybuild, full spatial public API contract, cross-crate check, kernel workload catalog tests, cargo fmt/check, git diff check, and phase-local line/directory audits.
- Implemented M6.5 Phase 19 projection-consumed fact parity. Added production `projection_fact_parity` workload/facade with typed lanes, status, counters, receipt, denial kinds, evidence-basis constructors from complete ledger and real retained/projection/recovery/local-rebuild/diagnostic receipts, plus user-response adapter and compile-fail receipt forgery proof. MB-M6-7 tests now run real catalog topology/binding/projection/transform/replay ledger plus Forge Query retained facts, projection-consumed facts, recovery, diagnostics, local rebuild, and branch mismatch/denied-upgrade outcomes with human-readable lane names. The real workload path needs a larger per-test stack on Windows, so MB-M6-7 wraps test bodies in a 16MB thread instead of depending on `RUST_MIN_STACK`. Verification passed: focused MB-M6-7 tests, full spatial public API contract, spatial UI trybuild, cross-crate cargo check, cargo fmt/check, git diff check, and phase-local line/directory audits.
- Phase 19 QA loop found two important honesty gaps after implementation: parity lanes were initially over-trusting the ledger basis stamp instead of comparing receipt-derived retained/projection/recovery/diagnostic links, and representative catalog acceptance tried to use stage-only retained replay for cube/thin-feature workloads. Fixed by adding cross-lane receipt basis links, removing the replay-from-ledger shortcut, adding a production catalog `with_retained_replay_artifacts()` mode, and expanding the representative MB-M6-7 proof through cube, coplanar overlap storm, thin-feature wall, and retained cancellation chain with full replay artifacts.
- Implemented M6.5 Phase 20 boolean-readiness final-boss workload. Added `PlanarBooleanReadinessWorkload` in worth-spatial with required-stage counters, real ledger guards, parity-to-readiness closure over retained/projection/recovery/diagnostic receipts, typed denials, human-readable user-response mapping, facade exports, compile-fail receipt sealing, and MB-M6-8 tests for admitted readiness, the outcome matrix, and kernel-summary substitution rejection. The final-boss test reuses the MB-M6-7 real parity stack, but rebuilds diagnostics and local-rebuild parity against the same recovery/retained/projection/motion receipts so parity and M7 readiness cannot certify disjoint real stacks. Verification passed: focused MB-M6-8 tests, full worth-spatial public API contract, spatial UI trybuild, cross-crate check for worth-spatial/worth-kernel/worth-topo, cargo fmt/check, git diff check, and phase-local line/directory audits.
- Phase 20 QA loop found two real gaps: the boolean-readiness workload was not registered in the geometry support/applicability authority even though the spec required a support-matrix row, and the certifier cloned the M7 readiness bundle on the proof path. Fixed by adding a Query-native `PlanarBooleanReadinessWorkload` declaration family, registering the support/applicability rows with explicit pre-M7/non-mutating posture, adding an MB-M6-8 support-matrix proof, and changing certification to move the readiness bundle out of the evidence basis.
- Phase 20 `qa-tests`/`code-quality-qa` found the MB-M6-8 outcome matrix still had reason-string blocker theater for clean-fail, unsupported, and orientation-localization branches. Fixed by adding typed `PlanarBooleanReadinessBlockerEvidence`, a `CleanFailure` denial kind mapped to `DirtyInput`, receipt-backed blocker constructors for dirty clean-fail, unsupported surface support, and orientation diagnostic receipts, and matrix assertions that denial evidence digests exactly match the consumed production receipts. Verification passed: focused MB-M6-8, full spatial public API contract, spatial UI trybuild, cross-crate check, cargo fmt/check, git diff check, and line/directory audits.
- Implemented M6.5 Phase 21 legacy synthetic fixture fence. Replaced hand-filled `M6PremetabossEvidenceRow::passed(...)` closeout rows with `M6PremetabossPlatformTarget` rows backed by complete workload ledgers and guard checks, added `M6LegacyFixtureFence` so inventory-classified legacy/unit/platform surfaces participate in closeout identity, made missing fence and synthetic end-to-end claims deny with human-readable reasons, and added a compile-fail proof that the old hand-filled MB evidence constructor is gone from the public surface. Kernel closeout consumption now mirrors the same real M7 readiness receipt instead of creating `kernel-evidence:*` rows. Verification passed: focused closeout tests, full spatial public API contract, spatial UI trybuild, kernel closeout proof, cross-crate check, cargo fmt/check, git diff check, and Phase 21 line/directory audits.
- Phase 21 QA loop found the closeout platform target path was still too fake-friendly: all MB families were registered from generic counter-backed ledgers, and the public API exposed readiness/generic-ledger shortcuts. Fixed by wiring closeout rows to the real MB receipt subjects, deleting the generic/readiness target constructors, adding a compile-fail guard for generic ledger MB targets, and changing the kernel proof to assert it cannot synthesize closeout from readiness summaries. Verification passed: focused spatial closeout, kernel summary denial, spatial UI trybuild, full spatial public API contract, cross-crate cargo check, cargo fmt/check, git diff check, and line/directory audits.
- Phase 21 `qa-tests`/`code-quality-qa` found two remaining honesty issues: closeout evidence rows dropped platform source-row pressure before tests could assert it, and the readiness-summary constructor removal lacked its own compile-fail guard. Fixed by preserving source rows on `M6PremetabossEvidenceRow`, strengthening MB closeout target tests to assert workload posture, unique digest, family-specific real receipt wording, and source-row minimums, renaming the hand-filled ledger guard test to match its actual proof surface, and adding the readiness-summary compile-fail fence. Verification passed: focused closeout target test, focused closeout suite, kernel summary denial, spatial UI trybuild, full spatial public API contract, cross-crate check, cargo fmt/check, git diff check, and line/directory audits.
- Fixed the MB-M6-1 honesty gap reported after Phase 21: the coplanar overlap storm operator no longer accepts platform evidence alone and now requires real `CoplanarOverlapContractReceipt` extractions. The MB1 main test, outcome matrix, and tiny-rotation denial were moved off the old synthetic rectangle fixture path and onto a storm extraction subject that drives the production overlap extractor, with counters for extraction receipts, candidate pair breadth, segment contacts, shared intervals, islands, ambiguous contacts, and policy exits. Also tightened transform evidence so MB1/operator guards require coordinate-changing transform rows, not merely transform step counts, and added a posture-only adversarial guard test. Verification passed: focused MB1/operator/inventory/guard tests, full worth-spatial public API contract, spatial UI trybuild, cross-crate check, cargo fmt/check, git diff check, and line/directory audits.
