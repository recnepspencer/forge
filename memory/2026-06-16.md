# 2026-06-16

- Worth UI M4 Phase 4B implemented prepared content-slot facts across the
  native validation app boundary. Worth UI now exposes a read-only
  `WorthUiContentSlotCatalog` derived from prepared layout topology plus the
  canonical artifact, and the validation app projects it into
  `ValidationPageContentPlan` with private construction. The Products sample
  intentionally declares content out of layout order while tests prove mounted
  order follows layout traversal. Compile-fail fixtures now block internal
  content lowerer imports, local slot maps, direct egui layout authority,
  preminted content plans, visual-only certification, and direct `worth_query`
  dependency.
- Phase 4B QA found and fixed two architectural weaknesses: the egui boundary
  guard tolerated non-renderer shell/page-host UI contact, and the content-slot
  catalog initially derived slot/surface assignments by zipping topology slots
  with canonical mounts. The shell now exposes no egui-facing methods, the
  renderer owns page-host rendering, the guard only allows renderer/app egui
  imports, and Worth UI builds content-slot assignments from file-authored
  content-slot lowering before verifying canonical mount order.
- Phase 4B qa-tests/code-quality pass found two weak compile-fail fixtures and
  one underpowered dependency guard. The egui fixture now calls the real
  `ValidationWorkspaceShell::restore_from_snapshot` API with raw egui rects and
  fails on the typed restore snapshot boundary. The content-certification
  fixture now passes a real prepared `ValidationPageContentPlan` to the runtime
  receipt gate and fails because only a harness receipt can certify runtime
  evidence. The WORTH Query manifest guard now rejects direct, table-form,
  target-specific, and renamed `package = "worth-query"` dependency forms.
- Worth UI M4 Phase 4C.5 implemented runtime-owned theme capability reload
  admission. `WorthUiRuntimeHost` now owns the active `CapabilitySnapshot`,
  prepares typed `WorthUiCapabilityReloadRequest` values from source-authored
  theme-token deltas, activates valid candidates at the runtime boundary, and
  rebinds the native header from runtime evidence. The validation app no longer
  denies valid theme edits locally; tests now prove valid theme edits mutate the
  runtime snapshot and rebind the header, malformed edits preserve runtime/header
  truth, combined source+theme reloads keep both evidence entries visible, and
  compile-fail guards prevent app code from minting capability reload evidence
  or prepared reloads.
- Phase 4C.5 QA found and fixed two real gaps: prepared theme capability reloads
  were bound only to the runtime instance, so an older prepared candidate could
  overwrite a newer active snapshot, and theme-token replacement hid repeated
  linear lookup breadth behind edited-token evidence. Capability activation now
  rejects stale active snapshot drift, validation tests prove stale prepared
  reloads cannot overwrite newer theme truth, theme-token replacement builds one
  index instead of rescanning per token, and evidence separately reports edited
  token count versus full theme-family rebuild breadth.
- Phase 4C.5 qa-tests/code-quality QA tightened the proof and API shape. Theme
  capability reload tests moved out of the broad reload-loop file and now
  pressure activation, malformed input, unknown tokens, duplicate tokens,
  multi-token cost evidence, and stale prepared replay. The capability reload
  request changed from a one-field theme struct to an enum-dispatched request
  so future action/state reload families do not turn the API into a bag of
  fields. Focused tests, validation-app tests, worth-ui lib tests, and the
  runtime authority compile-fail suite passed; the CI line-cap shell helper was
  unavailable locally because bash is not on PATH, so touched files were checked
  with targeted line counts instead.
- Worth UI hot-reloading Phase 7 implemented query-bound runtime change
  integration. The runtime now has query reload lowering input/receipt surfaces
  that consume existing Query support receipts, binding comparisons, live rebind
  plans, projection fact receipts, state snapshot receipts, and virtualized
  frame targets. Query facts now lower into exact runtime fact families for live
  views, result posture, projection facts, computed views, state snapshots,
  recovery, inspection, and bounded virtualized data frames; denied Query support
  or denied live rebinds produce sealed denial receipts and no changed facts.
  Runtime host admission now converts query lowering receipts into common
  admitted runtime change evidence. Tests prove narrow fact lowering,
  projection dependency intersection, support/live denial behavior, sealed proof
  construction, raw binding/local state rejection, facade importability, and full
  `cargo test -p worth-ui` passed after refreshing one stale launch trybuild
  baseline from `host.rs` to `host_launch.rs`.
- Phase 7 QA found and fixed the important denial-path bug: query reload
  lowering was building changed facts before proving support/live rebind
  admission, and counters/digests could imply unconsumed projection proof
  receipts had participated in denied lowerings. The lowerer now checks support
  and live-rebind denial before consuming query proof receipts, reports consumed
  projection/state/effect counters only for admitted/equivalent lowerings,
  includes proof receipt digests only when consumed, preserves inspected live
  rebind shape in denied-live evidence, and has hostile tests for both consumed
  and unconsumed digest behavior. QA also split oversized query reload tests and
  validation reload driver support to keep changed Rust files under the
  400-line cap.
- Phase 7 qa-tests/code-quality QA tightened proof pressure and structure. The
  tests now include a mixed preserved/changed Query comparison that proves the
  lowerer emits only exact changed facts instead of a global Query flag. Query
  binding identity conversion now fails closed in both Phase 7 query reload
  lowering and the older runtime fact taxonomy lowerer instead of silently
  skipping impossible invalid identities inside a trusted boundary. The query
  reload receipt module was split so fact mapping, counter derivation, digest
  derivation, proof receipt input, and support denial evidence each have named
  files. Full worth-ui lib tests and runtime authority compile-fail tests pass.
- Worth UI hot-reloading Phase 8 implemented the first native validation-app proof slice. The validation app now renders a Product Summary page from runtime observation, page-host receipts, and the same reload evidence log used by tests. Runtime reload evidence entries carry query comparison/rebind counters and changed-fact count into the visible UI. Structural guards prevent validation-app imports of runtime internals, direct query proof construction, direct WORTH Query dependency, and local product-state maps. QA also uncovered and fixed a shared projection-rebind receipt bug: `previous_frame_digest`/`rebound_frame_digest` now report the projection plan's actual frame/equivalence digest rather than the projection proof digest.
- Phase 8 qa-tests/code-quality QA found that product-summary evidence was still too stringly and under-tested for capability-family reloads. The product summary now preserves typed evidence status through `ValidationProductSummaryEvidenceStatus`, the model file was renamed from renderer-sounding `evidence_panel` to `evidence_summary`, capability reload log entries carry changed-fact counts as well as edited-family widths, and tests prove command/projection policy evidence reaches the manual UI projection with exact touched and changed-fact widths.
- Phase 9 compiler enforcement sweep added hot-reload authority certification fixtures to `runtime_reload_authority_compile`: validation changed facts cannot be WORTHd, raw validation/capability family evidence cannot replace admitted runtime change evidence, projection dependency declarations cannot replace admitted projections, and facade import coverage now names the hot-reload proof spine. Targeted hot-reload authority compile tests, older runtime authority compile tests, and validation-app native-boundary guards pass.
- Phase 9 QA loop found and fixed a certification gap around Query live-rebind proof surfaces. Added compile-fail guards for `WorthUiQueryLiveRebindPlan`, `WorthUiQueryLiveRebindEntry`, `WorthUiQueryBindingPreservation`, `WorthUiQueryBindingRebind`, `WorthUiQueryBindingRetirement`, `WorthUiQueryBindingDriftDenial`, and `WorthUiQueryStateSnapshotReceipt`. The hot-reload authority suite now has 42 passing compile guards.
- Phase 9 qa-tests/code-quality QA tightened that pass further: the validation app native-boundary guard now rejects Query live-rebind proof surfaces, and the combined Query outcome compile-fail fixture was split into separate preservation/rebind/retirement/denial fixtures for sharper failure localization. Hot-reload authority compile guards now pass at 45 tests.
- Worth UI hot-reloading Phase 10 implemented reload storm closeout proof surfaces on the existing runtime certification spine. Added sealed replay certification, projection rebuild-breadth certification, projection rebind batch digests, and supplementary visual capture receipts under `runtime/reload_storm_certification`, exported through the facade without public field construction. Hostile tests now prove semantic replay convergence, scenario identity mismatch denial, sparse changed-fact rebuild bounds, mixed denied/rebuilt projection rows, visual capture requiring prior runtime/projection proof, and steady-frame rejection of post-reload source parsing. Runtime authority compile guards now pass at 48 tests and block app-side construction of the new Phase 10 proof receipts.
- Phase 10 qa-tests/code-quality QA found that replay convergence still did not independently certify projection frame digest convergence. Added sealed projection-frame replay certification under `runtime/reload_storm_certification`, hostile frame-drift and real-boundary projection rebind convergence tests, a compile-fail guard blocking public proof construction, and semantic reload-storm test names with no `phase10` provenance leak.
- Revised the hot-reloading spec and roadmap after recognizing M4S had acceptance evidence for component appearance, density, dropdown mode, and native manual proof but not enough phase gates to force them before closeout. Added mandatory Phases 11-19 for typed appearance/density values, appearance/density reload families, component reload family, dropdown projection contract, header product proof, page-slot/component interaction proof, mixed product storm, compiler enforcement, and native manual verification app closeout.
- Added explicit closeout requirements to all 19 hot-reloading phases so each phase has a non-negotiable "not complete until..." gate tied to proof-bearing APIs, compile-fail enforcement, runtime receipts, bounded rebuild evidence, and native manual validation where relevant.
- Clarified the hot-reloading spec status: Phases 1-10 are completed foundation/audit history, and active implementation resumes at Phase 11. Do not replan or rebuild 1-10 unless QA finds a regression against their closeout bars.
- Tightened M4S Phases 11-19 according to `MENTALITY.md`: Phase 11 now requires canonical appearance/density descriptors instead of overloaded theme tokens; Phase 12 requires canonical equivalence/no-op proof and counters; Phase 13 requires component compatibility and state preserve/drop/deny receipts; Phase 14 separates dropdown mode from selected-value runtime state; Phase 15 requires source-shaped manual reload inputs; Phase 18 requires an enforcement matrix; Phase 19 requires a manual verification matrix.
## Worth M7.2 Boolean Candidate Indexing

- Replaced planar boolean segment-pair "full cross-product" event worklist behavior with a Query-admitted candidate index path.
- The metaboss event extraction proof now asserts 576 possible pairs, 90 indexed candidates emitted, and 486 pairs culled before predicate/event classification.
- Important lesson: broad-phase culling must happen after operand-side validation, otherwise invalid right-side operands can be silently skipped before the side-mismatch denial is produced.
- Follow-up repair implemented a first-class Query-owned candidate-index product: proof-bearing candidate rows now carry canonical candidate identity, carrier/source edge provenance, broad-phase reason, envelope basis, frame/precision basis, counters, strategy, and fallback posture. Predicate binding and its public contract now bind candidate rows rather than raw pair work items.
- QA tightened the candidate-index proof: the adversarial locality test now uses unique endpoint identities, the candidate-index execution exposes consuming ownership via `into_rows`, and public tests now use the Phase 0 contract names for Query-owned rows/counters/culls, no full-breadth fallback, and predicate binding from candidate rows. Remaining Phase 0 evidence-stage-index/operator-harness row-scan debt is broader than the candidate-index batch and must not be silently certified by these tests.
- Implemented Worth M7.3 phases 6-10 split-planning substrate: event ledger receipts now carry carrier rows for source-edge lookup; `planar_boolean_edge_splitting` derives a carrier/event participation index, point split candidates, admitted point candidates, interval split candidates, and admitted interval candidates. Public kernel tests drive the new path from the real metaboss event ledger, and crate-local denial tests cover NaN/out-of-domain point parameters, collapsed interval ranges, NaN interval ranges, and contradictory interval sense.
- QA-loop for M7.3 phases 6-10 found two real misses and fixed them: split candidate extraction was still traversing stored event vectors instead of consuming row-local event identities from the participation index, and exact point endpoint admission did not preserve source/projected endpoint identity. The participation index now owns event lookup maps, extraction traverses rows, participation rows/candidate identities carry endpoint facts, admitted exact endpoint point candidates expose endpoint identities, and tests cover canonical ordering, endpoint identity preservation, ordered interval admission, reversed-sense preservation, facade fences, event ledger contracts, and metaboss contracts.
- Ran `qa-tests` plus `code-quality-qa` on M7.3 phases 6-10. Fixed test proof gaps by adding malformed ledger/index denial tests for unknown carrier, unknown grouped point event, duplicate reference collapse, missing index-owned point event, and missing index-owned interval event. Added public compile-fail fences proving split participation indexes, point/interval candidates, and admitted point/interval candidates cannot be WORTHd by external callers. Fixed a structural cost smell by replacing `row_for_carrier`'s linear scan with a carrier-row offset map. Verification passed for focused split tests, spatial public boundary trybuild, kernel public split contract, and `cargo check`.
- Implemented M7.3 phases 11-14 edge-splitting proof chain: point split posture classification, raw per-edge schedule assembly, canonical schedule ordering with explicit order keys/digests, and duplicate split normalization with retained provenance and contradiction denial. Important lesson: duplicate cut keys cannot include every semantic posture field as the only grouping basis, or same-edge/same-parameter contradictory reports become separate cuts instead of a denial; normalization now checks a broader contradiction basis before grouping compatible duplicates.
- QA-loop for M7.3 phases 11-14 found and fixed real gaps: endpoint-only no-op lacked a direct proof, raw schedule `source_event_groups` was initially undercounted from schedule count instead of distinct event identities, duplicate normalization used debug/string posture identity, `-0.0` and `0.0` could drift as distinct parameter identities, normalized cuts lacked duplicate report identity, interval schedule entries were being dropped by point duplicate normalization, and the proof-chain test file exceeded the 400-line cap. Added canonical parameter bits, retained interval entry identities, duplicate report identity, typed same-parameter contradiction basis, stronger hostile tests, and split test support into its own module.
- Ran `qa-tests` plus `code-quality-qa` on M7.3 phases 11-14. Found the test suite still implied rather than explicitly proved three spec-critical cases: per-source-edge grouping, malformed mixed-source schedule rejection, and equal-parameter point-vs-interval ordering. Added named hostile tests for those plus an identity-based ordering test that proves ordering does not depend on debug/display strings. Focused edge-splitting tests now pass 25/25; public kernel split API tests pass 4/4; spatial compile-fail facade fences pass. Broader `worth-spatial` still has the known unrelated `structure_guard::spatial_crate_remains_kernel_dependency_pure` failure.
- Final closeout pass before moving deeper into M7.3 found one more counter semantics smell: raw split schedule counters collapsed shared-endpoint no-ops into endpoint-only no-op accounting. Patched `PlanarBooleanRawEdgeSplitScheduleCounters` to expose `shared_endpoint_noop_entries` separately, made schedule assembly count `SharedEndpoint` and `EndpointNoOp` distinctly, and added a shared-endpoint proof asserting raw shared endpoint count is 2 while endpoint-only no-op count is 0. Verification remained green for focused split tests, public kernel split tests, trybuild public facade fences, `cargo check`, and the edge-splitting line cap. Full `worth-spatial` remains red only on the known unrelated kernel-dependency structure guard.
- Began M7.3 Phase 0A candidate-index Query product authority. Patched the spec to split Phase 0 into 0A candidate-index product authority, 0B workload evidence stage index product, and 0C operator harness/spatial operator binding. Implementation tightened `PlanarBooleanSegmentPairEnumerationReceipt` so it owns/delegates to `PlanarBooleanSegmentCandidateIndexProduct` instead of copying product fields as parallel truth, added `candidate_index_product()` and `certifies_production_candidate_discovery()`, included query plan digest in pair-enumeration identity, changed candidate envelope identity to use float bits instead of debug-rendered floats, and added hostile tests proving product-owned rows/counters/culls/fallback posture plus product-derived work-item consumption. Updated the compile-fail receipt fixture to attack the new product-owning shape. Verification passed focused segment-pair/candidate-index tests, kernel public API segment-pair tests, spatial trybuild facade fences, and `cargo check`; full `worth-spatial` remains red only on the known unrelated kernel-dependency structure guard.
- QA-loop for M7.3 Phase 0A found that crate-local construction could still create candidate-index products whose rows, counters, culls, skips, fallback posture, or row identities lied while the product shape looked authoritative. Fixed by making `PlanarBooleanSegmentCandidateIndexProduct::new` return a denial-bearing `Result`, adding `product_validation.rs` as the invariant gate, rejecting row/counter mismatch, fallback posture mismatch, cull/skip reconciliation mismatch, and duplicate candidate row identities. Kept pair-enumeration product files under the 400-line cap. Focused candidate-index, segment-pair, kernel public API, spatial trybuild facade fences, and `cargo check` pass; broad `cargo test -p worth-spatial` remains red only on `structure_guard::spatial_crate_remains_kernel_dependency_pure`.
- Implemented M7.3 Phase 0B workload evidence stage-index product. `WorkloadEvidenceLedger` now owns a `WorkloadEvidenceStageIndexProduct` that builds typed stage offsets during ledger construction, rejects duplicate stages before completion, exposes product identity/counters/stage lookup, and centralizes boolean receipt matching. `WorkloadEvidenceGuard` now consumes the index product instead of scanning raw rows, the public facade exports the index/counters, and a trybuild fence proves external callers cannot hand-fill the index product. Verification passed the stage-index unit tests, public workload vocabulary contract, spatial compile-fail facade fence, `cargo check`, line-cap scan, and `git diff --check`; Phase 0C still needs to migrate kernel operator harness/spatial operator binding raw-row compatibility paths onto the index product.
- QA-loop for M7.3 Phase 0B found two in-scope authority/perf issues and fixed them: stage-index counters and identity were initially recomputed from rows after construction instead of being owned by the product artifact, and `stage_offsets` used a heap `Vec` even though evidence stages are a fixed registry. The product now stores counters and index identity computed during construction and uses a fixed `[Option<usize>; WorkloadEvidenceStage::STAGE_COUNT]` offset table. The compile-fail fence was tightened so it still proves external hand-fill is impossible without muddying the denial with a type mismatch.
- Implemented M7.3 Phase 0C operator harness/spatial operator binding. Added typed `WorkloadEvidenceStageLink`/`WorkloadEvidenceStageLinkSet` products derived from the stage-index product, rejected duplicate required-stage link requests, and introduced kernel `OperatorEvidenceBinding` so operator runs consume stage-index-owned links rather than raw `Vec<WorkloadEvidenceRow>`. Migrated coplanar overlap operators, receipts, outcome receipt sets, and storm workload link checks onto typed links; removed the raw `from_consumed_evidence` entry point and string-prefix linkage. Added compile-fail fences for raw operator construction, hand-filled stage links, and hand-filled `OperatorEvidenceBinding`. Focused checks passed for `cargo check`, stage-index tests, workload operator contracts, workload vocabulary contracts, spatial trybuild fences, and kernel compile-fail fences; the shell line-cap script could not run because `bash` is unavailable on PATH, so a targeted PowerShell count confirmed all Phase 0C files remain under 400 lines.
- QA-loop for M7.3 Phase 0C found a real foreign-link gap: typed stage links could still be produced from receipt-backed rows mixed across workloads, and the coplanar operator compared projection identity but not exact transform-stage identity from the certification context. Fixed by adding `WorkloadEvidenceStageBinding` metadata to receipt-backed evidence rows, including it in stage-index identity, validating transform->projection and retained-replay->transform bindings during `link_required_stages`, comparing operator context transform-stage identity exactly, adding hostile public tests for foreign transform/replay receipt mixtures, adding a crate-local stage-index mismatch test, and extending compile-fail fences so stage binding metadata cannot be hand-filled. Re-ran focused checks successfully.
- Ran `qa-tests` plus `code-quality-qa` on M7.3 Phase 0C. Hardened the operator public contract so admitted runs prove stage-index identity and consumed link-set identity are propagated into kernel receipts, refactored coplanar overlap operator context/extraction identity fields into typed private bindings, and split over-cap touched catalog files into constructor and topology-seed modules. Focused operator, stage-index, workload vocabulary, workload catalog, spatial/kernel compile-fail fences, `cargo check`, scoped raw-surface scans, modified-file line caps, and `git diff --check` are green; broad full-crate caveat remains the known unrelated structure-guard issue from earlier passes.
- Implemented M7.3 Phase 1 topology/query registration blueprint in `worth-topo`: added `edge_split_blueprint` with explicit operator classifications, required Query surfaces, validator runtime lanes, public closeout certification, facade exports, public API contract tests, and trybuild fences proving edge-split blueprint rows are not externally WORTHable. Verification passed `cargo check -p worth-topo`, `cargo check -p worth-spatial -p worth-kernel`, full `worth-topo` public API contract, `worth-topo` UI trybuild contract, line-cap scan, raw-surface scan, and `git diff --check`. Broad `cargo test -p worth-topo --lib` is still blocked by unrelated existing facade/test export drift, not by the Phase 1 code.
- QA-loop for M7.3 Phase 1 found and fixed real blueprint authority gaps: the closeout could certify any internally consistent row subset instead of requiring the exact Phase 1 operator/validator rows and exact Query/runtime lanes, not-yet-real split topology mutations (`SplitEdge`, `SplitIntersectedEdges`, `InsertVertexOnEdgeForTJunction`, `SplitEdgeAtOverlapInterval`) were incorrectly classified as admitted declaration/grouped families, and the crate-visible row constructor path was available in production. The closeout now rejects missing required rows and lane mismatches, public tests assert required-row closeout proof, future split topology mutations are support-gated, and arbitrary row construction is restricted to a `cfg(test)` hook. Reverified with format, cross-crate check, public API contract, UI trybuild contract, scoped residue scans, line caps, and diff hygiene.
- Ran `qa-tests` plus `code-quality-qa` on M7.3 Phase 1. Test QA found string-based proof obligations and sampled public lane coverage were too weak; production now exposes typed `EdgeSplitOperatorProofObligation` / `EdgeSplitValidatorProofObligation`, public tests assert typed obligations, exact required Phase 1 Query/runtime lanes, and every future topology mutation remains support-gated. Code-quality QA found `closeout.rs` mixed closeout reporting, required-row tables, and lane-honesty validation; split into `closeout.rs`, `lane_honesty.rs`, and `required_phase_1_rows.rs`. Verification passed format, `cargo check -p worth-topo -p worth-spatial -p worth-kernel`, full `worth-topo` public API contract, UI trybuild facade contract, line caps, scoped residue scans, and diff hygiene.
- Implemented M7.3 Phase 3 split-request boundary: added a proof-bearing `PlanarBooleanEdgeSplitRequest` product in `worth-spatial` that admits only from a real event ledger plus production candidate-index consumption gate, carries candidate-index product/query-plan identities and event-ledger/reduced-pair lineage, exposes split counters, rejects missing/manual/foreign/counterless evidence and non-production gates, implements Boolean split evidence, and has public facade + trybuild fences so external callers cannot WORTH it. Kernel workload composition now has `require_boolean_split`, with boolean stage requirements split into a child module to keep files under cap. Verification passed focused edge-split request unit tests, kernel public split-request and edge-splitting contracts, spatial public-boundary trybuild fences, `cargo check -p worth-spatial -p worth-kernel -p worth-topo`, line-cap scan, and `git diff --check` with only existing LF/CRLF warnings.
- QA-loop for M7.3 Phase 3 found two proof gaps and fixed them: split request admission only tested event-ledger mismatch even though production validation also binds downstream-consumption, reduced-pair, and segment-pair-enumeration lineage, and the spec-required raw point/interval/segment substitution fence was only implicit. Added hostile crate-local mismatch tests for downstream, reduced-pair, and segment-pair-enumeration swaps, plus a public trybuild fence proving raw point events, raw interval events, and raw segment carriers cannot enter `PlanarBooleanEdgeSplitRequestInput`. Reverified focused split-request tests, kernel public split-request contract, spatial public-boundary trybuild fences, cross-crate check, line caps, and diff hygiene.
- Ran `qa-tests` plus `code-quality-qa` on M7.3 Phase 3. Test QA found the public workload requirement only proved receipt-backed split success, not rejection of manual or foreign BooleanSplit evidence. Added public kernel tests for manual split evidence and foreign split receipt denial through `WorthWorkload::require_boolean_split`. Code-quality QA found the spatial compile-fail registry was nearing the line cap with a long edge-splitting registration cluster; tightened the edge-splitting registrations through the existing local `cf!` macro, reducing the file to 378 lines. Verification passed focused split-request tests, public split-request contracts, spatial trybuild fences, cross-crate check, line caps, format, and diff hygiene.
- Implemented M7.3 Phase 4 split scope and policy admission. Added `split_scope_admission` under planar boolean edge splitting with explicit edge-surgery scope class, degeneracy/determinism/overlap policy values, policy outcome kinds, denial kind for empty source-carrier scope, counters, canonical identity, facade exports, and admission from `PlanarBooleanEdgeSplitRequest` only. Tests prove scope admission preserves Query candidate-index lineage from the split request, denies carrierless scope before schedule/source-edge recovery, preserves machine outcome kind and event-ledger identity, and does not substitute scope-admission evidence for the existing split-request BooleanSplit workload receipt. Added public metaboss contract coverage and compile-fail fences proving external callers cannot WORTH scope admission or feed raw point/interval/carrier inputs. Verification passed focused spatial scope tests, kernel public scope contract, spatial trybuild fences, cross-crate check, line caps, and `git diff --check` with only existing LF/CRLF warnings.
- QA-loop for M7.3 Phase 4 found one real architectural proof-surface issue: `PlanarBooleanEdgeSplitScopeAdmission` implemented `BooleanEvidenceReceipt`, which let it create a receipt-backed `BooleanSplit` row even though Phase 4 is only scope/policy admission and the actual workload split receipt remains `PlanarBooleanEdgeSplitRequest`. Removed the evidence impl, replaced the public substitution test with a missing-split-evidence proof, and added a compile-fail fence proving scope admission cannot satisfy `BooleanEvidenceReceipt`. Reverified focused scope tests, kernel public scope contract, spatial trybuild fences, cross-crate check, line caps, residue scan, and diff hygiene.
- Ran `qa-tests` plus `code-quality-qa` on M7.3 Phase 4. Test QA found the public policy-outcome mapping proof only covered the admitted variant, so it would not catch accidental flattening of unsupported/blocked/denied/policy-required/integrity-mismatch outcomes. Added an all-variant public kernel test for `PlanarBooleanEdgeSplitPolicyOutcomeKind` to `PlanarBooleanOutcomeKind` mapping and stable-name parity, plus crate-local assertions that admitted and unsupported scope-admission outcomes preserve the expected `WorkloadStageSupport` posture. Code-quality QA found no further structural changes needed; the `split_scope_admission` files remain cohesive and all stay comfortably under the 400-line cap. Verification passed focused scope tests, public kernel scope contract tests, spatial public-boundary trybuild, cross-crate check, and `git diff --check` with only existing LF/CRLF warnings.
- Implemented M7.3 Phase 5 source-edge carrier recovery. Added `PlanarBooleanSplitSourceEdgeCarrierSet` and proof-bearing recovered carrier rows that recover from `PlanarBooleanEdgeSplitScopeAdmission` plus the matching event ledger, preserve operand/face/loop/edge/carrier/endpoint/projection/local-frame/precision lineage, expose counters and canonical identity, and deny scope/ledger mismatch, coordinate-only carrier provenance, and unknown grouped carrier references. Migrated `PlanarBooleanSplitEventParticipationIndex` to consume recovered carrier sets through `from_recovered_carriers` and removed the production `from_event_ledger` escape hatch. Public metaboss edge-splitting contracts now run the full gate -> request -> scope -> source-carrier recovery -> participation index chain and assert the recovered carrier-set identity reaches the index. Added compile-fail fences proving recovered carriers/sets cannot be hand-built and recovery cannot be fed raw carrier vectors. Verification passed focused source recovery tests, participation tests, public kernel edge-splitting contracts, spatial trybuild facade fences, cross-crate check, no-`from_event_ledger` residue scan, line caps, and `git diff --check` with only existing LF/CRLF warnings.
- QA-loop for M7.3 Phase 5 found and fixed two proof/keying gaps: participation-index identity omitted recovered carrier-set authority, and recovered carrier source-edge offsets/counters keyed by source-edge identity alone instead of operand side plus source edge. Added regression tests for participation-index identity authority and same source-edge names across left/right operands, then split source-edge recovery test support to keep Phase 5 files under the 400-line cap. Reverified focused source recovery tests, participation tests, public kernel edge-splitting contracts, spatial trybuild facade fences, cross-crate check, scoped residue scans, touched-file line caps, and `git diff --check` with only existing LF/CRLF warnings.
- Ran `qa-tests` plus `code-quality-qa` on M7.3 Phase 5. Test QA found recovery tests were still too narrow around denial pressure: they only attacked missing source-edge and grouped-carrier references, while production also has hard-fail provenance and interval-reference branches. Added hostile tests for missing face/loop/local-frame/projection/precision/endpoint/projected-endpoint provenance and unknown interval carrier references. Code-quality QA found `build_participation_index` had become a compressed proof workflow and that participation indexing accepted recovered carrier sets with matching event-ledger identity but foreign segment-carrier-set authority. Refactored the builder into named semantic steps, added typed `CarrierSetIdentityMismatch`, and added a regression proving foreign recovered carrier-set authority is rejected. Reverified focused source recovery and participation tests, public kernel edge-splitting contract, spatial compile-fail facade fences, cross-crate check, residue scans, line caps, and `git diff --check` with only existing LF/CRLF warnings.
- Implemented M7.3 Phase 6 split event participation index hardening. Fixed `carriers_indexed` to count canonical index rows instead of ledger carrier copies, added hostile tests for grouped interval coverage denial, duplicate-carrier row counter integrity, duplicate group reference collapse, and stable carrier/group order variation. Added a public compile-fail fence proving `PlanarBooleanSplitEventParticipationRow::new` cannot be used as a raw-string construction path. Verification passed focused participation and source recovery tests, public kernel edge-splitting contract, spatial compile-fail facade fences, cross-crate check, later-phase ledger-rescan residue scan, line caps, and `git diff --check` with only existing LF/CRLF warnings.
- Ran `qa-tests` plus `code-quality-qa` on M7.3 Phase 6. Found one real counter/proof gap: orphan-reference denials exposed an enum but not the rejected-orphan count required by the Phase 6 contract, leaving the counter surface meaningful only on successful indexes. Added rejection-count evidence to `PlanarBooleanSplitEventParticipationDenial`, routed unknown carrier/grouped point/grouped interval failures through the orphan denial path, and asserted the count in hostile tests. Also cleaned adjacent source-edge recovery tests back under the 400-line cap. Reverified focused recovery/participation tests, public kernel edge-splitting contract, spatial trybuild facade fences, cross-crate check, ledger-rescan residue scan, touched-file line caps, and diff hygiene.
- Implemented M7.3 Phase 7 point split candidate extraction hardening. Added a named carrier-parameter binding step for point events, made malformed parameter/participation mismatches produce proof-bearing denials with rejected-missing-parameter counts, and preserved the real metaboss nuance that deduplicated/high-valence retained participants without operand parameter facts are retained provenance but not emitted split candidates. Added hostile tests for missing index-owned events, retained participant skip, unauthorized parameter, conflicting same-carrier parameter facts, and point-event order stability. Verification passed focused point candidate tests, participation/source recovery tests, public kernel edge-splitting metaboss contract, spatial trybuild facade fences, cross-crate check, residue scans, touched-file line caps, and diff hygiene.
- QA-loop for M7.3 Phase 7 found two small but real proof/naming gaps: the spec-required preservation test was only covered indirectly, and conflicting same-carrier parameter facts used a misleading "missing parameter" denial constructor. Added the named `point_split_candidates_preserve_event_kind_coordinate_and_parameter_facts` proof, renamed the missing-carrier-parameter denial test to match the spec obligation, and split the denial constructor so conflict denials use parameter-binding language. Reverified focused point candidates, participation, source recovery, public kernel edge-splitting contracts, spatial public-boundary trybuild fences, cross-crate check, format, line caps, and residue scans.
- Ran `qa-tests` plus `code-quality-qa` on M7.3 Phase 7. Test QA found the public metaboss point-candidate preservation contract only asserted non-empty candidate fields, so a candidate could carry plausible but wrong ledger facts and still pass; strengthened it with an exact ledger event+carrier expected-fact map. Code-quality QA found the public contract file exceeded the 400-line cap after strengthening, conflict denials still reported through a missing-parameter counter, and `extraction.rs` mixed production lowering with a growing hostile harness. Moved point-candidate expected facts into edge-splitting support, split conflicting-parameter denial evidence from missing-parameter evidence, and moved point split candidate tests into `point_split_candidates/tests.rs`. Reverified focused point/participation tests, public kernel edge-splitting contract, spatial trybuild facade fences, cross-crate check, format, line caps, diff hygiene, and residue scans.
- Implemented M7.3 Phase 8 point parameter domain admission. `PlanarBooleanPointSplitCandidateSet` now lowers into proof-bearing admitted point candidates only through parameter-domain validation: non-finite and out-of-domain parameters deny without clamping, near-endpoint values remain interior, and exact endpoints must preserve source/projected endpoint identity. Added typed denial counters including missing exact endpoint identity, exposed the point candidate set through the facade for public proof, strengthened the kernel metaboss contract to prove admitted candidates reconcile exactly with candidate rows, and added a compile-fail fence for admitted point candidate set forging. Verification passed focused Phase 8 tests, point candidate tests, public kernel edge-splitting contracts, spatial trybuild facade fences, cross-crate check, format, line caps, residue scans, and diff hygiene.
- QA-loop for M7.3 Phase 8 found and fixed two proof/structure gaps: parameter-domain classification reused the public admission denial enum and needed an `unreachable!` branch for endpoint identity, and the public metaboss proof only required exact endpoint identities to exist instead of proving they matched the candidate's start/end endpoint facts. Split the classifier into a parameter-only in-domain classifier with direct typed denials, strengthened public endpoint assertions to compare against the source candidate endpoint identities, and added a signed-zero exact-start-endpoint regression. Reverified focused Phase 8 tests, public kernel edge-splitting contracts, spatial facade trybuild fences, cross-crate check, format, line caps, residue scans, and diff hygiene.
- Ran `qa-tests` plus `code-quality-qa` on M7.3 Phase 8. Test QA found the denial suite only attacked single-candidate sets, so a future implementation could filter invalid rows and still look correct locally. Added `split_point_parameter_domain_denies_mixed_candidate_sets_instead_of_filtering_bad_rows` to prove the admission product is all-or-nothing and reports the poisoned candidate identity. Code-quality QA found the Phase 8 production topology cohesive after the prior split: `point_parameter_admission` owns one responsibility, files are predictive and under cap, and `parameter_domain.rs` now reads as semantic admission steps without classifier/denial topology collapse. Reverified focused Phase 8 tests, public kernel edge-splitting contracts, spatial facade trybuild fences, cross-crate check, format, line caps, residue scans, and diff hygiene.
- Implemented M7.3 Phase 9 interval split candidate extraction hardening. Added a row-local source-interval binding step so interval candidates are emitted only from participation-index-owned event identities, with exact carrier/source-interval agreement, source parameter range, normalized interval, interval kind, source sense, frame, and precision facts preserved. Added proof-bearing denials/counters for missing index-owned interval events and missing source ranges, hostile tests for carrier mismatches, all-or-nothing mixed sets, anti-parallel reversed sense, distinct interval kinds, stable row-order identity, and candidate identity separation. Strengthened the public metaboss contract to compare interval candidates against exact ledger source-interval facts and added a compile-fail fence proving interval candidate sets are not externally WORTHable. Verification passed focused Phase 9 tests, public kernel edge-splitting contracts, spatial facade trybuild fences, cross-crate check, format, line caps, residue scans, and diff hygiene.
- Ran `qa-tests` plus `code-quality-qa` on M7.3 Phase 9. Test QA found the public metaboss interval proof still allowed wrong recovered source-edge binding because it only asserted `source_edge_identity` was non-empty while checking ledger interval facts exactly. Tightened the public helper so expected interval candidate facts join event-ledger interval facts with recovered-carrier source-edge facts by carrier identity, and candidates now must match the exact recovered source edge. Code-quality QA found the Phase 9 production topology cohesive after the earlier split: extraction, source-interval binding, identity, candidate shape, and denial files each own one predictable responsibility. Reverified focused Phase 9 tests, public kernel edge-splitting contracts, spatial facade trybuild fences, cross-crate check, format, line caps, residue scans, and diff hygiene.
- QA-loop on M7.3 Phase 10 found the existing interval parameter admission substrate was not complete enough: production logic mixed range classification, source-sense policy, and inline tests; denial evidence lacked per-kind counters; the hostile suite missed infinities, no-clamp out-of-domain pressure, signed-zero collapse, mixed-set all-or-nothing, and product forging. Fixed by splitting `interval_parameter_admission` into named range-domain and source-sense admission steps, adding typed denial constructors/counters for non-finite/out-of-domain/collapsed/contradictory-sense cases, moving tests to `tests.rs`, adding hostile Phase 10 tests, and adding a compile-fail fence for admitted interval candidate set forging. Reverified focused Phase 10 tests, Phase 9 interval candidate tests, public kernel edge-splitting contracts, spatial facade trybuild fences, cross-crate check, format, line caps, no-clamp/residue scan, and diff hygiene.
- Ran `qa-tests` plus `code-quality-qa` on M7.3 Phase 10. Test QA found the real metaboss/public proof still only checked interval admission counts, zero collapsed denials, and existence of one reversed row; a filtering/reordering/substitution implementation could plausibly pass while crate-local hostile tests stayed green. Strengthened the public helper with `assert_interval_domain_admission_matches_candidates`, proving candidate-set identity, inspected/admitted counts, all denial counters, one-to-one candidate identity/order preservation, exact ordered admitted ranges, and reversed-source preservation on the real metaboss path. Code-quality QA found the Phase 10 production structure cohesive: range-domain proof, source-sense admission, admission orchestration, denial evidence, counters, candidate product, and tests have predictable homes under cap. Reverified focused Phase 10 tests, public kernel edge-splitting contract, spatial compile-fail facade fences, cross-crate check, format, line caps, no-clamp/residue scan, and diff hygiene.
- Implemented M7.3 Phase 12 raw edge split schedule hardening. Propagated participation-index lineage from point/interval candidate sets through point/interval admission and point posture classification, made raw schedule assembly reject foreign point/interval products before grouping, and added raw schedule tests for source-edge grouping, foreign candidate-set denial, and duplicate raw participation preservation. Added a public metaboss raw schedule proof that runs the real chain into `PlanarBooleanRawEdgeSplitScheduleSet` and asserts lineage, counters, source-event groups, source-edge grouping, and raw candidate identity retention. Updated admitted point/interval candidate-set compile-fail snapshots for the new private lineage field. Verification passed focused raw schedule/proof-chain tests, public kernel edge-splitting contract, spatial public-boundary trybuild, cross-crate check, format, touched-file line caps, and diff hygiene.
- QA-loop for M7.3 Phase 12 found two real proof/authority gaps and fixed them: raw schedules were keyed by `source_edge_identity` alone, which could merge distinct recovered source-edge carriers sharing a source-edge label, and the public raw-schedule proof used set membership for retained candidate identities, hiding multiplicity loss. Raw schedules now carry and key by source-edge plus carrier identity, schedule identity includes carrier authority, public/metaboss proof asserts carrier-key consistency and exact candidate multiplicity, source-event group counting is point/interval namespace-aware, and hostile tests cover same-source-edge different-carrier non-merging plus point/interval event textual identity collision. Reverified focused raw schedule tests, proof-chain tests, public kernel edge-splitting contract, spatial public-boundary trybuild, cross-crate check, format, line caps, and diff hygiene.
- Ran `qa-tests` plus `code-quality-qa` on M7.3 Phase 12. Found the public raw-schedule proof still missed a schedule-cardinality assertion and raw schedule assembly hid semantic operator steps in one production function plus an inline test module. Fixed by deriving expected source-edge schedule count from distinct source-edge/carrier keys, decomposing assembly into named point insertion, interval insertion, source schedule building, and raw-entry creation steps, and moving the malformed mixed-source schedule test into the module test file. Reverified focused raw schedule tests, proof-chain tests, public kernel edge-splitting contract, spatial public-boundary trybuild, cross-crate check, format, line caps, and diff hygiene.
- Implemented M7.3 Phase 13 canonical schedule ordering hardening. Ordered schedules now preserve source-edge plus carrier authority, order keys expose named semantic basis fields, ordering is decomposed into key-building/sorting/ordinal assignment/schedule-building steps, and construction validates raw schedule-set identity, raw entry multiplicity, source-edge/carrier preservation, exact raw-entry consumption, and sorted order keys. Added hostile ordering tests for same-source-edge distinct carriers, raw multiplicity, signed-zero canonical keying, raw schedule order variation, non-finite denial, missing tie-breaker denial, and explicit key basis. Public metaboss proof now certifies ordered schedule counts, raw identity, source-edge/carrier authority, raw-entry multiplicity, and sorted explicit keys; compile-fail fences prove ordered schedule and order key construction are not externally WORTHable. Verification passed focused ordering tests, proof-chain tests, public kernel edge-splitting contract, spatial public-boundary trybuild, cross-crate check, format, line caps, and diff hygiene.
- QA-loop for M7.3 Phase 13 found a real spec/architecture gap: event-group identities were canonical in the event ledger and participation index, but split candidates/raw schedules/order keys dropped them, so canonical ordering could pass while losing required event-graph authority. Fixed by carrying event-group identities from participation rows into point/interval split candidates, admitted/postured candidates, raw schedule entries, canonical order keys, validation, public metaboss proof, and compile-fail attacker fixtures. Also tightened public order-key proof from `entry_kind_rank <= 2` to exact typed rank matching. Verification passed focused candidate/order/raw/proof-chain tests, public kernel edge-splitting contract, spatial trybuild facade fences, cross-crate check, format, line caps, and diff hygiene.
- Ran `qa-tests` plus `code-quality-qa` on M7.3 Phase 13. Found that tests proved the explicit event-group order key but did not prove the replay digest committed to that same order-key basis; production `schedule_order_digest` only hashed ordinal plus raw entry identity, so a digest could miss event-group/order-key drift. Fixed the digest to append the named order-key basis fields directly, added `split_schedule_order_digest_commits_to_event_group_order_basis`, and named the schedule-set authority sort helper instead of leaving set-order tie-breaks in an inline closure. Reverified focused ordering/proof-chain tests, public kernel edge-splitting contract, spatial compile-fail facade fences, cross-crate check, format, line caps, and diff hygiene.
- Implemented M7.3 Phase 14 duplicate split normalization. Duplicate point cuts now normalize through explicit grouping, same-parameter frame/precision contradiction checks, normalized split-cut products with duplicate report identity, source/carrier/parameter/frame/precision/event-group authority, retained interval entry provenance, exact counters, and schedule identities that commit to retained intervals. Public metaboss proof now runs raw -> ordered -> normalized schedules and asserts normalized counters, raw point provenance, event/predicate/event-group retention, and retained interval preservation. Added facade compile-fail fences for normalized split cuts, schedules, and schedule sets. Important nuance from the real metaboss: same carrier/parameter with different split posture can be legitimate distinct cut authority; contradiction denial must target incompatible numerical basis, not posture disagreement. Verification passed focused duplicate normalization tests, proof-chain tests, public kernel edge-splitting metaboss contracts, spatial public-boundary trybuild, cross-crate check, format, line caps, and diff hygiene with only existing LF/CRLF warnings.
- QA-loop for M7.3 Phase 14 found and fixed three real issues: normalized cut identity used a coarser split-producing/no-op kind rank than the duplicate grouping key, so distinct equal-parameter postures could collide under hostile provenance; the internal `PlanarBooleanDuplicateSplitCutKey` leaked through the public facade; and the strengthened duplicate-normalization test file exceeded the 400-line cap. Fixed by sharing one private duplicate cut kind-rank basis between grouping and cut identity, adding a hostile equal-parameter posture identity regression, adding the explicit spec-named counter test, sealing the duplicate key out of the facade with a compile-fail fence, and splitting test fixtures into `tests_support.rs`. Reverified focused duplicate normalization tests, proof-chain tests, public kernel edge-splitting metaboss contracts, spatial public-boundary trybuild, cross-crate check, format, line caps, and diff hygiene with only existing LF/CRLF warnings.
- Ran `qa-tests` plus `code-quality-qa` on M7.3 Phase 14. Test QA found normalized provenance still had two multiplicity hazards: production deduped raw provenance entry identities, and the public metaboss proof could self-certify a dropped/collapsed raw point row because it compared normalized provenance counters only to normalized rows. Fixed production provenance to preserve a canonical multiset, added a duplicate raw-entry identity regression, made the public proof consume every raw point row exactly once through a point-only multimap, and required retained provenance rows to equal raw point rows. Code-quality QA also tightened the duplicate cut-key constructor from crate-wide to module-private visibility. Reverified focused duplicate normalization tests, proof-chain tests, public kernel edge-splitting contracts, spatial facade trybuild fences, cross-crate check, format, touched-file line caps, and diff hygiene.
- Implemented M7.3 Phase 15 endpoint no-op and boundary split normalization. Endpoint authority now carries from point events into raw schedule entries and normalized cuts, and the new endpoint-boundary normalization product emits proof-bearing endpoint contact decisions instead of zero-length fragments for endpoint no-op/shared-endpoint/T-junction-boundary cases while rejecting missing endpoint authority, contradictory interior no-op/shared-endpoint posture, and boundary interior splits that would create zero-length fragments. Added counters, canonical identities, facade exports, public metaboss proof, hostile unit tests, and compile-fail fences for endpoint decisions/normalized schedules/schedule sets. Verification passed focused endpoint-boundary tests, proof-chain tests, public kernel edge-splitting contracts, spatial public-boundary trybuild, cross-crate check, format, touched-file line caps, and diff hygiene with only existing LF/CRLF warnings.
- QA-loop for M7.3 Phase 15 found and fixed two real gaps: Phase 15's operator/validator vocabulary existed in spatial code and spec but was missing from the worth-topo edge-split blueprint registry, and the public metaboss helper reconciled total endpoint decisions without exact per-posture counter proof while allowing a looser boundary T-junction fragment assertion than production actually permits. Added `CollapseEndpointNoOpSplits`, `RecordEndpointContactDecision`, `ValidateEndpointNoOpSplitPolicy`, and `RejectEndpointSplitThatWouldCreateZeroLengthFragment` to the blueprint as prepared spatial products, registered the endpoint policy/zero-length rejection validation lanes, added local/public blueprint tests, and strengthened the kernel public metaboss proof to assert exact endpoint-noop/shared-endpoint/T-junction decision counters plus no boundary point cuts in fragment construction. Reverified focused spatial/kernel/topo public contracts, cross-crate check, spatial trybuild facade fences, format, line caps, and diff hygiene.
- Ran `qa-tests` plus `code-quality-qa` on M7.3 Phase 15. Test QA found the hostile suite still missed exact end-endpoint authority preservation, interior endpoint-no-op contradiction denial, and malformed shared-endpoint authority; added tests for all three. Code-quality QA found the public `PlanarBooleanEndpointBoundarySplitAction` was decorative because production used a private duplicate action enum, and `endpoint_contact_decisions()` hid an allocation behind a cheap-looking accessor. Production now uses the public action enum as the actual classifier result and returns endpoint contact decisions as an iterator instead of allocating a `Vec`. Reverified focused endpoint-boundary tests, proof-chain tests, kernel/topo public contracts, spatial compile-fail facade fences, cross-crate check, format, line caps, and diff hygiene.
- Implemented the long-term split-decision-log hardening slice: added a Query-owned split decision log domain/lowered plan/result, exact coverage manifest/receipt, typed decision reasons, operational truth digest, and row recording split under `worth-spatial`; sealed raw phase-stop construction out of production/public facade with compile-fail fences; added kernel public-contract support for real metaboss products and an edge-split localization-to-outcome bridge. Focused spatial decision-log tests, spatial UI trybuild, cross-crate check, format, line caps, and diff hygiene passed. Caveat: `worth-kernel --test public_api_contract` exact metaboss tests timed out, and a pre-existing candidate-index metaboss contract also timed out, so the kernel public-contract runtime needs a separate performance investigation before calling that broad proof green.
