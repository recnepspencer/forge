## WORTH Query 9.9 Phase 14 QA

- Ran the Phase 14 QA loop for the graph obligation consumer kit.
- Fixed support pins so they can pin executor budget identity, not only
  kind/lane/status.
- Fixed in-memory proof so it exposes selected obligation rows and rejects empty
  or mismatched proofs during adoption.
- Fixed selector coverage so adoption fails when declared coverage does not
  cover registered obligation selectors.
- Expanded residue rows to require owner, introduced phase, blocker, cap, and
  removal trigger.
- Expanded local ceremony audit patterns to include manual pre-check,
  invariant-pack, private-validator, phase-chain, and local-legality folklore.
