2026-04-20

- Closed out Forge Store Milestone 13 with `_docs/forge-store/milestone-13-closeout.md`, marked the spec closed, and added the roadmap closeout pointer.
- Focused closeout verification passed:
- Ran a Milestone 11 closeout-readiness QA loop after Phase 4/5 scheduler work. Found and fixed a real spec/code drift: `DerivedFamilyRebuild` and `MaintenanceAudit` were only vocabulary, not scheduler-admitted containers. Added private declaration payloads, durable persistence, scheduler mappings, execution container phases, tests, and compile-fail constructor privacy coverage. Milestone 11 focused verification passed afterward.
- Wrote `_docs/forge-store/milestone-11-closeout.md`, marked `_docs/forge-store/milestone-11.md` completed, and added the Milestone 11 closeout/current-state block to `_docs/forge-store/forge_store_roadmap.md`.
  `cargo test -p forge-store milestone_13_certification -- --test-threads=1`,
  `cargo test -p forge-store tiering -- --test-threads=1`, and
  `cargo test -p forge-store --test phase_boundaries_compile_fail -- --test-threads=1`.
- Forge UI todo demo lesson: do not bypass the Forge UI component/theme crates for demo speed. The user expects `forge-ui-components` buttons/cards/chips/inputs/tabs and the Forge theme surface, not raw egui sketch chrome.
- Forge UI todo demo correction: the normal UI library should not depend on metallic shader callbacks. `FgButton` now paints with egui primitives, and `forge-ui-theme` no longer exports/compiles the backdrop/metal/shader pipeline modules for the standard UI surface.
- Forge UI todo demo architecture correction: the UI should not own a lock-backed `TodoTruth` or manually admit runtime bridge writebacks. The app-level seam should be a high-level Forge Query workspace facade; runtime/relational/writeback machinery belongs behind Query.
