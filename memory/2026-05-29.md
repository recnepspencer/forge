- worth-relational canonical aspect-set residue cleanup: deleted the remaining `CanonicalAspectSet` vocabulary from the live source tree and local aspect truth map. Publication patch data now owns `aspect_key_ordering.rs::ordered_aspect_keys`, while patch records, canonical deltas, history traces, transaction summaries, and tests carry ordered `worth_foundational::facade::AspectKey` lists instead of a phantom relational set carrier. Verified with `cargo fmt -p worth-relational`, `cargo check -p worth-relational --tests`, `cargo test -p worth-relational canonical_deltas -- --nocapture`, `cargo test -p worth-relational record_local_aspect_history_reads_committed_patch_truth -- --nocapture`, `cargo test -p worth-relational update_entity_fields_canonical_delta_uses_authoritative_patch_evidence -- --nocapture`, `cargo check -p worth-query --tests`, and `cargo check -p worth-runtime-bridge --tests`.
- worth-relational aspect refactor: validation catalog canonical identity batch removed the production `serde_json::to_string(registration)` authority seam. `validation/data/catalog.rs` is now a `catalog/` directory with `mod.rs` for the public catalog API, `canonical_registration_identity.rs` for typed registration/rule/contract byte grammar, `canonical_registration_tags.rs` for enum tag tables, `relation_integrity_registration_plan.rs` for relation-plan registration assembly, and test-only `registration_examples.rs`/`tests.rs`. `InvariantCatalog::canonicalized()` and `canonical_registration_digest()` now sort/hash explicit typed bytes, preserving foundational `AspectFieldLocator` aspect/path identity for unique entity aspect-field rules. Verified with `cargo test -p worth-relational validation::data::catalog -- --nocapture` and `cargo check -p worth-relational --tests`; only existing warnings remain (`diagnostic_label`, `entity_aspect_plan`).
- Commit-strategy aspect-field reconciliation hard-break batch removed loose `field_name: String` authority from `AspectFieldReconciliationInput`/`Output`. The public request/output now carry `AspectFieldLocator`; native strategy bytes gained explicit aspect-field-locator encode/decode; the strategy schema names bumped to v2 with no v1 fallback; execution validates the supplied locator against the lowered foundational scalar entity aspect plan before emitting `AspectFieldPatch`. Updated focused strategy tests, facade strategy tests, and history certification setup to construct locators. Verified with `cargo fmt -p worth-relational`, `cargo check -p worth-relational --tests`, and `cargo test -p worth-relational aspect_field_reconciliation -- --nocapture`; only existing warnings remain (`diagnostic_label`, `entity_aspect_plan`).
- Schema continuity field-identity hard-break batch moved `SchemaDiffDetail` field changes from raw `field_name: Arc<str>` to foundational `FieldKey` carriers. Canonical schema boundary fingerprints now sort/hash the typed field key, and schema-continuity diagnostics project `RelationalDiagnosticValue::FieldPath(vec![FieldKey])` directly instead of reparsing strings or emitting `invalid_field_name` fallback objects. Updated schema/history/CDC/durability/complexity transition fixtures to construct `field_key(...)` at the declaration boundary, and added a regression assertion that explicit schema-transition trace details expose the field path projection. Structural QA found no remaining diagnostic-time field parsing/fallback in the migrated slice; the two touched over-cap production files are already on the Rust line-cap allowlist. Verified with `cargo fmt -p worth-relational`, `cargo test -p worth-relational schema_continuity -- --nocapture`, `cargo test -p worth-relational explicit_schema_transition_is_lowered_into_canonical_commit_artifacts -- --nocapture`, and `cargo check -p worth-relational --tests`. Existing worth-relational warnings remain `diagnostic_label` and `entity_aspect_plan`.
- Grouped truth foundational carrier hard-break batch deleted duplicate relational key/value carriers (`RelationalAspectBindingKey`, `RelationalAspectCellValue`, and the leftover `RelationalGroupingValue`). `RelationalAuthoritativeRowArtifact` now stores `BTreeMap<AspectKey, AspectValue>`, grouped projection contracts require foundational `AspectKey` values at construction, and grouped member identity/grouping values are direct `AspectValue`s. The only remaining string-to-`AspectKey` conversion is the external runtime-bridge `SnapshotReadRequest::aspect_label()` boundary; malformed labels and malformed aspect bytes now fail before row-set materialization. Residue scan found no deleted wrapper names. Verified with `cargo fmt -p worth-relational`, `cargo test -p worth-relational row_set -- --nocapture`, `cargo test -p worth-relational grouped_projection -- --nocapture`, and `cargo check -p worth-relational --tests`; only existing warnings remain (`diagnostic_label`, `entity_aspect_plan`).
- History aspect-filter duplicate-set deletion batch removed `RequestedAspectSet`, its custom serde impls, and its facade export. `AspectFilter` now carries the existing canonical `CanonicalAspectSet` over foundational `AspectKey`, so history and query filters no longer maintain a second aspect-key-set carrier. Updated direct history/query filter tests and support helpers to construct canonical aspect sets directly; residue grep found no `RequestedAspectSet` under `crates/worth-relational/src`. Verified with `cargo fmt -p worth-relational`, `cargo check -p worth-relational --tests`, `cargo test -p worth-relational record_local_aspect_history_reads_committed_patch_truth -- --nocapture`, and `cargo test -p worth-relational planned_query_execution_supports_aspect_filtered -- --nocapture`; existing warnings remain (`diagnostic_label`, `entity_aspect_plan`).
- Durable merge replay typed-authority batch removed diagnostic-field readback as the source for rebuilding merge commits. Canonical commit envelopes now carry \\PublishedMergeExecutionAuthority\\ for real prepared-merge executions; publication passes it from \\MergeCommitMutationPlan\\, metadata-only envelopes pass none, and \\durability/authority/runtime_rebuild/merge_plan.rs\\ reconstructs merge commit mutation plans from the typed envelope authority instead of \\RelationalDiagnosticFields\\ / JSON count extraction. Added a durability regression that clears persisted merge diagnostics and still recovers through typed merge authority. Verified with \\cargo fmt -p worth-relational\\, \\cargo check -p worth-relational --tests\\, \\cargo test -p worth-relational execute_prepared_merge_survives_durability_append_and_recovery -- --nocapture\\, and \\cargo test -p worth-relational durability_contract_replays_merge_from_typed_authority_when_diagnostics_are_absent -- --nocapture\\; existing warnings remain \\diagnostic_label\\ and \\entity_aspect_plan\\.
- Grouped truth canonical-byte digest batch deleted the grouped truth aspect-value text codec and moved row-set/projection digest authority to \\grouped_truth/canonical_digest.rs\\. Row-set and grouped projection digests now hash domain-tagged canonical bytes with length-prefixed identities, aspect keys, and shared \\spect_wire::encode_aspect_value\\ bytes instead of display strings. Added a regression proving raw string \\symbol:7\\ and symbol-backed string \\Symbol(7)\\ produce distinct row-set digests. Structural QA found no meaningful composition issues; all touched production files remain under the line cap. Verified with \\cargo fmt -p worth-relational\\, \\cargo check -p worth-relational --tests\\, \\cargo test -p worth-relational grouped_projection -- --nocapture\\, and \\cargo test -p worth-relational row_set -- --nocapture\\; existing warnings remain \\diagnostic_label\\ and \\entity_aspect_plan\\.
- Commit-strategy intent-scope target hard-break batch removed the remaining string field bag from strategy merge descriptors and merge conflict overlap. `StrategyMergeDescriptor` now carries typed `AspectFieldPatchTarget` scope targets, native strategy intent-scope extraction returns targets from lowered authoritative patch plans, strategy scope digests encode target canonical bytes with v2 domain tags, and merge conflict dedup/overlap compares typed aspect-field targets instead of `Vec<String>`. This also fixed a downstream codec drift where `strategy.aspect` scope extraction still decoded an old string field instead of the v2 `AspectFieldLocator`. Added regressions proving same field path under different aspect keys yields different strategy scope digests and that merge descriptors round-trip typed targets. Structural QA found two minor ownership issues and fixed them by making field-patch target readback share `read_target()` and making strategy digest terms reuse `AspectFieldPatchTarget::to_canonical_bytes()`. Verified with `cargo fmt -p worth-relational`, `cargo check -p worth-relational --tests`, `cargo test -p worth-relational commit_strategies -- --nocapture`, `cargo test -p worth-relational aspect_value_canonical_codec -- --nocapture`, and `cargo test -p worth-relational update_entity_fields -- --nocapture`. Existing worth-relational warnings remain `diagnostic_label` and `entity_aspect_plan`.
- Canonical-delta field-materialization denial hard-break batch removed `raw_field_name` and the misleading `materialized_binding_evidence.rs` helper bucket. Entity field binding materialization failures that require authoritative patch evidence now carry `AspectFieldPatchTarget` through `CanonicalDeltaError` and `AspectDeltaFailureFields` instead of storing the field as a formatted string. The remaining lifecycle helper moved to `lifecycle_transition_evidence.rs`, whose filename now matches its single responsibility. Added a canonical-delta regression proving the generated `AspectDeltaFailure` conflict fields carry the typed target. Structural QA found no remaining raw-field helper residue and all touched files stay below the 400-line cap. Verified with `cargo fmt -p worth-relational`, `cargo check -p worth-relational --tests`, and `cargo test -p worth-relational canonical_deltas -- --nocapture`. Existing worth-relational warnings remain `diagnostic_label` and `entity_aspect_plan`.
- Query/index field-predicate hard-break batch moved `QueryScope::{Entity,Relation}Field{Equals,AnyOf}` predicate values from `String` to foundational `AspectValue`, encoded query-scope digests with shared aspect canonical bytes, and made packetized/index-backed execution lower query values to `AuthoritativeFieldComparisonKey` instead of manufacturing string-family keys. Deleted `AuthoritativeFieldComparisonKey::from_query_string` and the storage `authoritative_field_display_value_by_name` helpers so read callers must use typed `FieldKey` access. Added a query plan-key regression proving `Int64(1)` and `String("1")` no longer collide at the query predicate digest seam. Structural QA extracted packetized comparison-key lowering into a named helper and found no remaining string-query helper residue. Verified with `cargo fmt -p worth-relational`, `cargo check -p worth-relational --tests`, `cargo test -p worth-relational field_predicate_plan_keys_preserve_aspect_value_family -- --nocapture`, `cargo test -p worth-relational comparison_key_preserves_aspect_value_family_even_when_display_collides -- --nocapture`, `cargo test -p worth-relational index_observation_paths -- --nocapture`, and `cargo test -p worth-relational derived_index_contract_entity_field_equals_executes_through_real_index_path_with_storage_parity -- --nocapture`. Existing warnings remain `diagnostic_label` and `entity_aspect_plan`.
