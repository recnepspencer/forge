- Started on `master`, switched to `query-repair`, fetched origin, and fast-forwarded `query-repair` from `3b1263dd` to current `origin/master`/`master` at `d0b5a4b0`.
- Compared `_docs/worth-runtime-bridge/aspect_native_refactor.md` against `_docs/worth-query/aspect_native_query_refactor.md`. Query's plan had the right zones but was not yet as enforceable as the runtime bridge spec.
- Strengthened the WORTH Query aspect-native refactor doc with active-spec status, hard-break rules, hostile condition, eradication standard, production JSON allowlist contract, legacy authority baseline, target skeleton, phase acceptance evidence, forbidden hybrids, batch-plan template, verification policy, and first-slice sequencing.
- After the user called out arch law 41, strengthened `_docs/worth-query/aspect_native_query_refactor.md` again so Law 41 is a primary driver: explicit mutation/read proof ladders, sealed/private proof states, weaker-proof promotion denial, proof-state skeleton files, phase acceptance checks, forbidden hybrids, and batch-plan fields for consumed/produced proof states.
- After the user emphasized mechanical enforcement over relying on search, updated the same spec to say residue scans are diagnostic only. Closeout must be proven by private/sealed proof states, transition-consuming type signatures, compile-fail/public-boundary tests, visibility fences, and source-derived/facade export audits.
- After the user noted runtime bridge tests consumed roughly 80% of refactor time, updated the Query spec's verification policy: live in red during structural rewrites, use `cargo check -p worth-query` as the cheap navigation loop, avoid runtime/trybuild/certification/broad tests until the proof slice is coherent, and treat failing legacy tests as expected fallout unless they directly prove the active boundary.
- Began implementation slice 1 for WORTH Query aspect-native mutation authority. Added sealed/native mutation intent proof-state files under `crates/worth-query/src/runtime/mutation/native_intent`, moved mutation operation types out of the oversized aspect file, changed `WORTHQueryAspectValue` to store parsed native desired aspect state instead of JSON/path fields, and routed lowering/assertion/backend diagnostics through terminal JSON projections only.
- Wired memory workspace field patches to consume foundational `AspectValue` directly and to use parsed native `AspectKey`/`CanonicalFieldPath` when available, with a narrow declared-terminal fallback for plain compatibility paths. Removed the old external JSON scalar lowering hook from this path.
- Verification checkpoint: `cargo check -p worth-query` is clean with no warnings. No broad tests were run, per the fast refactor/test-cost rule.
- Continued implementation slice 1. Converted existing-truth probe request/field authority to parsed native aspect targets plus foundational `AspectValue`; backend adapter JSON is now compatibility ingress and probe responses are checked against requested native targets. `WORTHQueryWriteCommand::UpdateAspect` now stores one admitted `WORTHQueryAspectValue` instead of raw `aspect_path: String` + `serde_json::Value`; production constructors and fixtures now admit through the native wrapper.
- Export audit: native proof-state types remain crate-internal under `runtime::mutation::native_intent` and do not leak through `runtime.rs` or facade exports. Verification checkpoint remains `cargo check -p worth-query` clean with no warnings; still no broad tests.
- Continued implementation slice 1 through delete/touch commands. Added public admitted `WORTHQueryAspectTouch` wrapper with private parsed target. Delete command variants now store `Vec<WORTHQueryAspectTouch>` (`touched_aspects`) instead of raw `Vec<String>` touch authority; `touched_aspect_paths()` is now a terminal projection helper. Runtime lowering, preview receipt shaping, batch symbolic lowering, and certification write authority now consume admitted touches. Verification checkpoint: `cargo check -p worth-query` clean with no warnings.

## Query aspect-native refactor checkpoint

- Kept `query-repair` on top of current `origin/master`; `origin/master` is an ancestor of `HEAD` after fetching.
- Backend mutation execution now requires `WORTHQueryBackendAdmissibleMutation`; raw `WORTHQueryWriteCommand` no longer satisfies `write`/`write_batch` in production.
- Existing-truth probe adapters now return native `WORTHQueryExistingTruthProbeField` in production; old `(String, Value)` tuple signatures remain only in runtime test support until the focused test-support slice.
- Retained scalar facts and scalar alignment now retain `AspectValue`, with a private retained-scalar row admission proof state localizing JSON materialization ingress.
- Verification stayed intentionally narrow: repeated `cargo check -p worth-query` green, no broad `--tests` run.

## Query aspect-native refactor checkpoint 2

- Added `WORTHQueryBackendAdmissibleMutation` as the backend execution proof fence and kept `cargo check -p worth-query` green.
- Updated runtime test support adapters/shims to stop teaching `(String, Value)` existing-truth probes and `aspect.value()`; they now build native probe fields or use terminal projections.
- Added a private retained-scalar row admission carrier so retained scalar facts/alignment retain `AspectValue` after one localized JSON materialization ingress.
- Made public derived/computed materialization JSON APIs explicit terminal projections: `terminal_json_rows_projection`, `push_terminal_json_row`, `replace_terminal_json_rows`, `retain_terminal_json_rows`, and `terminal_json_computed_rows_projection`; raw row access is runtime-local.
- Kept verification intentionally narrow: `cargo check -p worth-query` green; no broad test compile/run yet.

## Query aspect-native workflow substrate checkpoint

- Removed the workflow/effect mutation-lowering field named `desired_aspect_fields_external_json` from source and docs, replacing it with native `AspectFieldPatch` flow through `MutationLoweringInput::IntentReconciliation`, domain capability workflow authoring, and workflow semantics.
- Added a native authoring helper for single string field patches and converted effect lifecycle/lower-runtime certification fixtures that mutate `name` to use it instead of external JSON ingress.
- Workflow semantics identity now digests canonical patch bytes rather than serializing desired fields through JSON.
- Verification stayed on the fast loop: `cargo fmt -p worth-query`, focused residue audits, and `cargo check -p worth-query` green. No broad tests were run.

## Query aspect-native projection field fact checkpoint

- Converted `ConsumedFieldValueFact` from `serde_json::Value` to foundational `AspectValue`, so display and derived scalar consumed facts cannot be constructed from raw JSON.
- Relational row-set extraction now passes native projected aspect values directly; bridge row-set extraction admits only scalar validated aspect payloads and rejects struct payloads for consumed field facts.
- Query read, live binding, retained binding, and query-context extraction now route external row values through a named extraction-local scalar admission helper before constructing field facts.
- Fact-set digests for consumed field facts now use native scalar text instead of JSON canonical text.
- Verification stayed fast: `cargo fmt -p worth-query`, line-cap audit clean, focused residue scan, and `cargo check -p worth-query` green with no broad tests.

## Query aspect-native grouped projection fact checkpoint

- Converted `ConsumedMembershipFact` and grouped `ConsumedRelationEndpointFact` member/grouping values from `serde_json::Value` to foundational `AspectValue`.
- Relational and bridge grouped projection extraction now pass native grouped member values directly; the old native-to-consumption JSON projection shim was deleted and removed from identity-boundary inventory sources.
- Consumed fact-set identity and grouped certification oracle comparison now digest native grouping values instead of JSON canonical text.
- Verification stayed narrow: `cargo fmt -p worth-query`, focused projection residue scan, line-cap audit, and `cargo check -p worth-query` green with no broad tests.

## Query aspect-native runtime effect payload checkpoint

- Converted `WORTHQueryEffectDelivery` to store native `WORTHQueryEffectPayload` instead of retaining `serde_json::Value` as delivery meaning.
- Effect routing now builds native payloads for always/expression outcomes instead of `json!` objects.
- Pending-intent handoff hashing and runtime intent declaration bridge through explicit `terminal_json_payload_projection` while the delivery itself remains native.
- Exported `WORTHQueryEffectPayload` through the runtime facade. Verification stayed narrow: `cargo fmt -p worth-query`, focused effect payload audit, line-cap audit clean, and `cargo check -p worth-query` green.

## Query aspect-native memory workspace touch checkpoint

- Converted `WORTHQueryMutationDelta` and `WORTHQueryLivePatch` to retain `WORTHQueryAspectTouch` instead of raw touched aspect path vectors.
- Memory workspace insert/update receipts now preserve parsed touch proof from `WORTHQueryAspectValue`; preview receipts and delete paths consume existing admitted touches where available.
- Updated computed routing, effect routing, live subscription delivery, preview relevance, batch aggregation, inspection, and certification reporting to use native touches or explicit terminal path projections.
- Verification stayed deliberately cheap: `cargo fmt -p worth-query`, focused direct-field audit, line-cap audit clean, and `cargo check -p worth-query` green. No broad tests were run.

## Query aspect-native retained binding scalar checkpoint

- Removed retained-binding projection consumption's direct walk over `terminal_json_rows_projection`; the extractor now asks `WORTHQueryDerivedMaterializationResult` for retained scalar values.
- Added crate-local `retained_row_count` and `retained_scalar_value_at` accessors so retained binding consumes native `AspectValue` while preserving missing-field/invalid-shape errors.
- JSON materialization remains localized in the runtime materialization surface rather than leaking into projection consumption. Verification: `cargo fmt -p worth-query`, focused retained-binding residue audit, line-cap audit clean, and `cargo check -p worth-query` green.

## Query aspect-native mutation operation evidence checkpoint

- Converted `WORTHQueryAspectMutationOperation` to retain `WORTHQueryAspectTouch` instead of an aspect path string; path access is now projection for reporting/digest callers.
- Converted symbolic aspect references, symbolic aspect resolution evidence, and graph-composition resolution entries to retain admitted touches.
- Added a denied-path proof enum for existing-truth assertion/probe denials so valid denied paths are admitted touches and invalid denied input is explicitly rejected text.
- Verification stayed cheap: `cargo fmt -p worth-query`, focused mutation residue scan, line-cap audit clean, and `cargo check -p worth-query` green.

## Query aspect-native verified assumption checkpoint

- Converted `WORTHQueryVerifiedAssumptionSet` to store asserted aspects as `WORTHQueryAspectTouch` instead of raw path strings.
- `WORTHQueryVerificationReadSetBreadth` and graph-composition assumption summaries now count/aggregate admitted touches first and project path text only for digest/reporting evidence.
- Existing-truth assertion verification now preserves parsed targets from `WORTHQueryAspectValue` into verified assumption evidence.
- Verification stayed narrow: `cargo fmt -p worth-query`, focused assumption-set scan, line-cap audit clean, and `cargo check -p worth-query` green.

## Query aspect-native effect delivery touch checkpoint

- Converted `WORTHQueryEffectDelivery` and `WORTHQueryEffectPayload` changed-trigger aspects to retain `WORTHQueryAspectTouch`.
- Effect declaration admission now validates trigger/input/output aspect declarations as admissible touches before routing.
- Effect routing now aggregates live/computed trigger changes as native touches and uses path projections only for matching/reporting.
- Split effect routing aspect helpers into a small sibling module to keep `runtime/effect/routing.rs` under the Rust line cap. Verification: `cargo fmt -p worth-query`, focused effect residue scan, line-cap audit clean, and `cargo check -p worth-query` green.

## Query aspect-native computed patch checkpoint

- Converted `WORTHQueryDerivedPatch` to store changed/produced aspects as `WORTHQueryAspectTouch` instead of raw aspect path strings.
- Computed derived view admission now validates dependency and produced aspects as admissible touches before patch construction.
- Effect routing consumes derived patch touches directly for computed triggers instead of readmitting patch path strings.
- Verification stayed narrow: `cargo fmt -p worth-query`, focused computed/effect residue scan, line-cap audit clean, and `cargo check -p worth-query` green.

## Query aspect-native retained refresh context checkpoint

- Converted `WORTHQueryRetainedRefreshContext` to store touched aspects as `WORTHQueryAspectTouch`.
- Computed routing now passes mutation delta touches into retained refresh context construction directly.
- Path access on refresh context is terminal projection over native touches. Verification: `cargo fmt -p worth-query`, focused computed refresh scan, and `cargo check -p worth-query` green.

## Query aspect-native preview execution evidence checkpoint

- Converted `WORTHQueryPreviewExecutionEvidence` to retain `WORTHQueryAspectTouch` values for preview-routed aspect changes instead of raw aspect path strings.
- Split preview execution evidence constructors so pending intent strategy labels are recorded as strategy subjects, not fake aspect paths.
- Preview live/computed/effect routing now keeps native touches for evidence and uses terminal path projections only for declaration matching/reporting.
- Updated the directly coupled preview assertions to inspect `intent_strategy_name` or explicit terminal aspect path projections. Verification stayed narrow: `cargo fmt -p worth-query`, focused preview residue scan, line-cap audit clean, and `cargo check -p worth-query` green.

## Query aspect-native batch receipt inspection checkpoint

- Converted `WORTHQueryBatchWriteReceipt`, batch receipt aggregates, unified batch inspection, and component inspection to retain `WORTHQueryAspectTouch` values for touched aspects instead of raw touched aspect path vectors.
- Authoritative batch writes now pass native touches from the combined mutation receipt into batch receipt construction.
- Batch and component digest/reporting paths use terminal touched-aspect projections, but stored evidence remains native.
- Verification stayed cheap: `cargo fmt -p worth-query`, focused batch/inspection residue scan, line-cap audit clean, and `cargo check -p worth-query` green.

## Query aspect-native graph touch descriptor checkpoint

- Converted graph touch descriptor rows, read touch shapes, command-derived rows, descriptor inventory, and obligation lookup derivation to retain `WORTHQueryAspectTouch` values.
- Declared mutation graph descriptors and graph obligation touch selectors now admit touched aspect paths through `WORTHQueryAspectTouch` before constructing descriptor/selector evidence.
- Added invalid-aspect-path denial states for graph touch descriptors and graph obligation registration.
- Verification stayed narrow: `cargo fmt -p worth-query`, focused graph touch residue scan, line-cap audit clean, and `cargo check -p worth-query` green.

## Query aspect-native computed declaration checkpoint

- Converted `WORTHQueryDerivedView` dependency and produced aspect fields to retain `WORTHQueryAspectTouch` values instead of raw string vectors.
- Derived-view authoring still accepts path text, but declarations retain admitted touches and expose path text through terminal projection accessors.
- Computed admission now trusts the proof-bearing declaration instead of revalidating projected strings.
- Computed inspection evidence now retains dependency/produced touches and projects strings only for digest/reporting. Verification: `cargo fmt -p worth-query`, focused computed declaration scan, line-cap audit clean, and `cargo check -p worth-query` green.

## Query aspect-native program command template checkpoint

- Converted `WORTHQueryAspectValueTemplate` and `WORTHQueryWriteCommandTemplate::UpdateAspect` to retain `WORTHQueryAspectTouch` instead of raw aspect path strings.
- Template binding now projects path text only when constructing the already-native `WORTHQueryAspectValue`.
- Verification stayed cheap: `cargo fmt -p worth-query`, focused program template residue scan, line-cap audit clean, and `cargo check -p worth-query` green.

## Query aspect-native memory workspace row checkpoint

- Converted memory workspace aspect-projection rows so they no longer retain a JSON object beside native aspect values.
- `WORTHQueryEntity` now stores external projection path values as native `AspectValue` and derives JSON only through terminal row projection helpers.
- Live-binding and row-like projection consumption read native external-path values before falling back to explicitly external JSON rows.
- Deleted the old projection-consumption JSON row path walker. Verification: `cargo fmt -p worth-query`, focused memory workspace row scan, line-cap audit clean, and `cargo check -p worth-query` green.

## Query aspect-native retained materialized row checkpoint

- Added `WORTHQueryRetainedMaterializedRow` as the retained proof carrier for derived materialization rows, with private storage and explicit terminal JSON scalar-leaf admission.
- Converted derived materialization result/runtime materialization storage and retained upstream computed inputs to retain those native rows instead of raw `serde_json::Value` vectors.
- Retained scalar facts now read `AspectValue` directly from retained materialized rows; computed upstream propagation uses retained rows between derived views and projects JSON only for terminal export/inspection.
- Verification stayed cheap: `cargo fmt -p worth-query`, focused materialization/upstream residue scans, line-cap audit clean, and `cargo check -p worth-query` green. No broad tests were run.

## Query aspect-native derived patch payload checkpoint

- Added `WORTHQueryDerivedPatchPayload` and changed `WORTHQueryDerivedPatch` storage/constructors so raw `serde_json::Value` no longer sits directly inside the patch proof state.
- Replaced the derived patch `payload()` accessor with `terminal_json_payload_projection`; the later native-proof slice removed the public fallback JSON constructor and moved fallback patches to `WORTHQueryDerivedPatchPayload::empty`.
- Updated the directly coupled computed payload assertions that were already touched to use the terminal projection vocabulary.
- Verification stayed narrow: focused computed payload scan, line-cap audit clean, and `cargo check -p worth-query` green. Broader test-maintainer constructor migration is intentionally deferred to a focused test-support slice.

## Query aspect-native retained materialization execution checkpoint

- Converted derived materialization execution and shared-read published artifact generation to construct `WORTHQueryDerivedMaterializationResult` from retained materialized rows instead of projecting through `Vec<Value>`.
- Gated `WORTHQueryDerivedMaterializationResult` terminal JSON row admission to test-only helpers; production construction now uses retained rows.
- Added explicit terminal JSON compatibility names (`terminal_json_read_derived`, `terminal_json_materialize`) plus retained-result accessors (`read_derived_result`, `materialize_result`), while leaving old `read_derived`/`materialize` aliases as public read-surface cleanup debt.
- Verification stayed cheap: `cargo fmt -p worth-query`, focused materialization execution scan, line-cap audit clean, and `cargo check -p worth-query` green.

## Query aspect-native mutation metadata checkpoint

- Converted `WORTHQueryMutationMetadata` storage from `BTreeMap<String, serde_json::Value>` to a private `WORTHQueryMutationMetadataValue` evidence carrier.
- Metadata insertion still serializes arbitrary caller metadata at ingress, but retained storage is encoded terminal evidence text rather than a reusable JSON authority value.
- Write-receipt digest, causal observation, and bridge mutation-authority identity paths now read `terminal_json_text()` from the metadata carrier; the old causal JSON stabilizer helper was deleted.
- Verification stayed narrow: `cargo fmt -p worth-query`, focused metadata residue scan, line-cap audit clean, and `cargo check -p worth-query` green.

## Query aspect-native program value checkpoint

- Added `WORTHQueryProgramValue` and moved program operation inputs, outputs, and value-expression literals off direct `serde_json::Value` storage.
- Program expression evaluation and command-template binding now retain the program value carrier until the explicit terminal JSON projection boundary needed by aspect mutation construction.
- `WORTHQueryOperationInput`/`WORTHQueryOperationOutput` now expose `from_program_value` plus terminal JSON projection helpers while keeping old JSON constructors as compatibility ingress.
- Verification stayed cheap: `cargo fmt -p worth-query`, focused program residue scan, line-cap audit clean, and `cargo check -p worth-query` green. Broader program/preview tests are expected to need a focused assertion-vocabulary cleanup later.

## Query aspect-native intent/materialization API checkpoint

- Added `WORTHQueryIntentInput` and moved `WORTHQueryIntentDeclaration` storage/input access off direct `serde_json::Value`; `input()` now returns the carrier and terminal JSON reads must use named projection helpers.
- Exported `WORTHQueryIntentInput` through runtime/facade and patched directly coupled transcript/certification intent authorities to use named input accessors.
- Flipped derived materialization convenience APIs so `runtime.read_derived` and `workspace.materialize` return retained `WORTHQueryDerivedMaterializationResult`; terminal JSON export is now explicit through `terminal_json_read_derived`/`terminal_json_materialize`.
- Added `WORTHQueryDerivedMaterializationResult::row_count()` and updated the aspect API certification row/doc example to avoid teaching `materialized_rows = workspace.materialize(...)`.
- Verification stayed intentionally narrow: `cargo fmt -p worth-query`, focused residue scans, line-cap audit clean except Git line-ending warnings, and `cargo check -p worth-query` green. No broad tests were run.

## Query aspect-native retained materialization publication checkpoint

- Added native retained-row publication through `WORTHQueryRetainedMaterializedRow::from_scalar_values` plus `WORTHQueryDerivedViewMaterialization::{replace_retained_rows,push_retained_row,retain_retained_rows}`.
- Gated terminal JSON materialization mutators to cfg-test compatibility and converted runtime API/aspect API harness maintainers to publish native scalar-value rows.
- Renamed serde-based retained artifact decoders to explicit terminal JSON names: `terminal_json_decode_single_row`, `terminal_json_decode_row_pair`, and `terminal_json_decode_row_triple`; patched docs, test call sites, and forbidden-fallback audit needles.
- Verification stayed on the cheap path: `cargo fmt -p worth-query`, focused public-surface residue scans, line-cap audit clean except Git line-ending warnings, and `cargo check -p worth-query` green. No broad tests were run.

## Query aspect-native retained materialization test-support checkpoint

- Added `retained_test_row` and `retained_string_test_row` helpers for runtime tests so support maintainers publish native retained rows without reopening terminal JSON row mutation.
- Converted runtime program support, shared-read support, hostile/concurrent bridge maintainers, phase-nine support fixtures, and computed materialization fixture maintainers to use retained row publication.
- Focused residue scan now leaves terminal row mutator names only on cfg-test compatibility helper definitions and historical/spec references that explicitly describe them as test-only.
- Verification stayed cheap: `cargo fmt -p worth-query`, focused terminal-mutator/decode residue scan, and `cargo check -p worth-query` green. No broad tests were run.

## Query aspect-native derived patch payload native-proof checkpoint

- Converted `WORTHQueryDerivedPatchPayload` from a named `serde_json::Value` wrapper into a private native payload proof enum: empty payload, foundational scalar payload, or retained materialized row payloads.
- Removed the public `WORTHQueryDerivedPatchPayload::from_terminal_json` constructor; fallback routing now uses `WORTHQueryDerivedPatchPayload::empty`, and public maintainers must construct payload proof from native scalar or retained-row carriers.
- Runtime API stabilization and aspect API certification maintainers now reuse retained row payload proofs instead of building patch payloads from terminal JSON.
- Verification stayed cheap: `cargo fmt -p worth-query`, line-cap audit clean except Git CRLF warnings, focused derived-payload residue scan, and `cargo check -p worth-query` green. No broad tests were run.

## Query aspect-native intent input native-proof checkpoint

- Converted `WORTHQueryIntentInput` from a named `serde_json::Value` wrapper into a private native input tree with null, bool, number text, string, array, and object variants.
- Changed public `WORTHQueryIntentDeclaration::strategy_commit` to require `WORTHQueryIntentInput`, so public runtime-intent declarations cannot promote raw JSON into the retained input proof state.
- Kept terminal JSON lowering runtime-local for effect-triggered intent payloads and removed the old borrowed terminal-input accessor; authority adapters now use native `input_string_field` for collection extraction.
- Converted production/certification fixture intent declarations that still used `json!` payloads to object-shaped `WORTHQueryIntentInput` construction.
- Verification stayed cheap: `cargo fmt -p worth-query`, focused intent-input residue scan, line-cap audit clean except Git CRLF warnings, and `cargo check -p worth-query` green. No broad tests were run.

## Query aspect-native program value native-proof checkpoint

- Converted `WORTHQueryProgramValue` from a named `serde_json::Value` wrapper into a private native value tree with null, bool, number text, string, array, and object variants.
- Changed public `WORTHQueryValueExpr::literal`, `WORTHQueryOperationInput::new`, and `WORTHQueryOperationOutput::new` to require `WORTHQueryProgramValue`, so public program authoring cannot promote raw JSON into retained program-value proof states.
- Program expression evaluation now assembles object and array results from retained program values rather than projecting children to JSON and readmitting them.
- Runtime live-read program outputs use crate-local terminal JSON lowering into the program value carrier; directly coupled cfg-test program fixtures still need a focused assertion-vocabulary cleanup before broad test compilation is expected to be clean.
- Verification stayed cheap: `cargo fmt -p worth-query`, focused program-value residue scan, line-cap audit clean except Git CRLF warnings, and `cargo check -p worth-query` green. No broad tests were run.

## Query aspect-native program value test-support checkpoint

- Converted directly coupled runtime program and preview-operation cfg-test fixtures to construct `WORTHQueryProgramValue` for operation inputs and value-expression literals instead of passing `serde_json::Value` through the public program authoring API.
- Focused scans no longer find the old raw-JSON program literal/input/output constructor vocabulary in runtime test/application/doc surfaces.
- Verification stayed cheap: `cargo fmt -p worth-query`, line-cap audit clean except Git CRLF warnings, focused program-test vocabulary scans, and `cargo check -p worth-query` green. No broad tests were run.

## Query aspect-native retained upstream input checkpoint

- Changed public `WORTHQueryRetainedUpstreamInputs::new` so computed upstream rows must be supplied as `WORTHQueryRetainedMaterializedRow` carriers instead of `Vec<serde_json::Value>`.
- Kept terminal JSON retained-upstream fixture construction behind `terminal_json_test_inputs` under cfg-test only, and cfg-gated retained materialized row JSON admission plus JSON scalar admission helpers so production no longer carries them.
- Existing production routing already builds retained upstream inputs from retained rows, so the compiler confirmed the constructor change was localized to test vocabulary.
- Verification stayed cheap: `cargo fmt -p worth-query`, focused retained-upstream/terminal-row scans, line-cap audit clean except Git CRLF warnings, and `cargo check -p worth-query` green. No broad tests were run.

## Query aspect-native effect declaration touch checkpoint

- Converted `WORTHQueryEffectTrigger` and `WORTHQueryEffectExpression` trigger/input/output declarations from retained path strings to `WORTHQueryAspectTouch` carriers.
- Changed public effect trigger/expression constructors and the workspace effect builder trigger/condition methods so effect declaration authority requires admitted touch values instead of authoring strings.
- Updated effect routing, delivery payloads, inspection, and inspection identity to consume retained touches and project path text only for matching against changed terminal paths, JSON export, reports, and digests.
- Patched directly compiled certification fixtures to construct explicit `WORTHQueryAspectTouch` values. Runtime test fixtures still contain expected string-call-site fallout and should be handled in a focused test-support pass, not by broad test compilation during the main refactor.
- Verification stayed cheap: `cargo fmt -p worth-query`, focused effect-declaration residue scans, touched-file line counts under 400, and `cargo check -p worth-query` green. The CI line-cap shell guard could not complete in this PowerShell session because Git Bash timed out after two minutes; no broad tests were run.

## Query aspect-native computed patch constructor checkpoint

- Changed public `WORTHQueryDerivedPatch::incremental` and `WORTHQueryDerivedPatch::whole_refresh_materialized` so derived patch maintainers must supply `WORTHQueryAspectTouch` values instead of raw aspect path strings.
- Runtime computed fallback now reuses mutation delta touches when possible and only admits produced-aspect declarations into touches at the patch boundary.
- Runtime API stabilization and aspect API finalization maintainers now pass retained delta touches or explicitly admitted produced-aspect touches into derived patches.
- Verification stayed cheap: `cargo fmt -p worth-query`, focused computed-patch constructor scans, `cargo check -p worth-query` green, and touched-file line counts checked. `runtime/computed/surface.rs` remains over 400 lines but is already listed in `scripts/ci/workspace_rust_line_cap_allowlist.txt`; no broad tests were run.

## Query aspect-native derived view declaration checkpoint

- Changed `WORTHQueryDerivedView::new` and `WORTHQueryDerivedView::produces` so proof-bearing derived declarations require `WORTHQueryAspectTouch` dependency/produced aspects instead of raw aspect path strings.
- Tightened the workspace computed builder reads/produces storage to carry `WORTHQueryAspectTouch` values into derived declarations.
- Runtime computed routing and production harness maintainers now consume `dependency_aspect_touches`/`produced_aspect_touches` directly instead of projecting strings and readmitting them at patch time.
- Updated aspect API finalization and runtime API transcript certification examples so computed declarations call `reads`/`produces` with explicit touch carriers.
- Verification stayed cheap: `cargo fmt -p worth-query`, focused derived-view constructor/source-vocabulary scans, touched-file line checks with existing allowlist entries for `program.rs`, transcript rows, and runtime transcripts, and `cargo check -p worth-query` green. No broad tests were run.

## Query aspect-native program template constructor checkpoint

- Changed `WORTHQueryAspectValueTemplate::new` so program write-command templates must receive `WORTHQueryAspectTouch` instead of raw aspect path strings.
- Changed `WORTHQueryTypedPort::with_required_aspect` and retained required aspect storage so typed port aspect requirements use `WORTHQueryAspectTouch`.
- Focused scans now find the old string-shaped program template/typed-port constructors only at the intended `WORTHQueryAspectTouch::new` admission point.
- Verification stayed cheap: `cargo fmt -p worth-query`, `cargo check -p worth-query` green, focused program-template scans, and program.rs line-cap allowlist confirmation. No broad tests were run.

## Query aspect-native existing-truth probe request checkpoint

- Changed `WORTHQueryExistingTruthProbeRequest::new` so proof-bearing probe requests must receive `WORTHQueryAspectTouch` values instead of raw aspect path strings.
- Removed retained probe request aspect path storage; request digests and reporting now derive terminal path projections from retained touches.
- Workspace ergonomic `probe_existing` still accepts authoring path text, but lowers it into touches before constructing the probe request.
- Routing certification fixtures now build probe requests with explicit touch carriers.
- Verification stayed cheap: `cargo fmt -p worth-query`, warning-free `cargo check -p worth-query`, focused probe request scans, and touched-file line counts under 400. No broad tests were run.

## Query aspect-native retained upstream accessor checkpoint

- Removed the runtime-local `WORTHQueryRetainedUpstreamInputs::computed_rows() -> Vec<Value>` accessor that made retained upstream computed rows look like JSON authority.
- Added `retained_computed_rows()` so runtime internals access retained materialized rows directly.
- Kept terminal JSON only in explicit projection/decode helpers; `decode_single_computed_row` now projects retained rows to JSON at the decode boundary.
- Verification stayed cheap: `cargo fmt -p worth-query`, warning-free `cargo check -p worth-query`, focused retained-upstream accessor scans, and confirmed `runtime/computed/surface.rs` is already line-cap allowlisted. No broad tests were run.

## Query aspect-native delete touch constructor checkpoint

- Changed `WORTHQueryDeleteMutationBuilder::touch` and `touches` so delete mutation touch declarations require `WORTHQueryAspectTouch` instead of raw aspect path strings.
- The builder already retained touches internally, so this closes the remaining weaker-value promotion at the public proof-bearing builder boundary.
- Verification stayed cheap: `cargo fmt -p worth-query`, warning-free `cargo check -p worth-query`, focused delete-builder signature scans, and touched-file line count under 400. No broad tests were run.

## Query aspect-native graph read touch shape checkpoint

- Changed `WORTHQueryGraphReadTouchShape::new` so graph read touch shapes require `WORTHQueryAspectTouch` values instead of raw aspect path strings.
- Runtime read obligation dispatch now admits read-family field/filter/order paths into touches before constructing graph touch descriptor evidence.
- Verification stayed cheap: `cargo fmt -p worth-query`, warning-free `cargo check -p worth-query`, focused graph-read-touch constructor scans, and touched-file line counts under 400. No broad tests were run.

## Query aspect-native clear aspect constructor checkpoint

- Changed `WORTHQueryAspectValue::new_clear` and `WORTHQueryAspectMutationBuilder::clear` so clear-aspect mutation authority requires `WORTHQueryAspectTouch` instead of raw aspect path strings.
- Added a consuming `WORTHQueryAspectTouch::into_parsed_target` helper so clear-aspect proof construction can reuse the parsed target without re-parsing terminal text.
- Patched aspect API finalization clear/rejection fixtures to construct explicit touches for clear operations.
- Verification stayed cheap: `cargo fmt -p worth-query`, warning-free `cargo check -p worth-query`, focused clear-constructor signature scan, and touched-file line checks with `rows.rs` already allowlisted. No broad tests were run.

## Query aspect-native symbolic identity touch checkpoint

- Changed `WORTHQuerySymbolicAspectReference::same_batch_entity_identity`, `WORTHQueryAspectMutationBuilder::symbolic_entity_identity`, and graph relation symbolic identity authoring so same-batch symbolic identity authority requires `WORTHQueryAspectTouch` instead of raw aspect path strings.
- Removed the redundant `Result` from symbolic-reference construction because path admission now happens before the reference proof state can be built.
- Verification stayed cheap: `cargo fmt -p worth-query`, warning-free `cargo check -p worth-query`, focused symbolic identity signature/call-site scans, and touched-file line counts under 400. No broad tests were run.

## Query aspect-native native/evidence set constructor checkpoint

- Changed `WORTHQueryAspectValue::new_set_native` and crate-local `new_set_evidence_identity` so native/evidence set-aspect construction requires `WORTHQueryAspectTouch` instead of raw aspect path strings.
- Updated same-batch symbolic aspect resolution to reuse the retained symbolic reference touch when materializing the resolved evidence identity aspect, avoiding terminal path projection plus re-admission.
- Verification stayed cheap: `cargo fmt -p worth-query` and warning-free `cargo check -p worth-query`. No broad tests were run.

## Query aspect-native set aspect constructor checkpoint

- Changed `WORTHQueryAspectValue::new`, `WORTHQueryAspectValue::new_set`, `WORTHQueryAspectMutationBuilder::aspect`, and graph relation set-aspect authoring so proof-bearing set-aspect construction requires `WORTHQueryAspectTouch` instead of raw dotted path strings.
- Updated compiled program binding, lower-runtime-routing fixtures, intent-admission certification fixtures, and the backend receipt fixture to pass explicit touches or reuse retained touches.
- Focused old-call scans for `new_set("...")` and `.aspect("...")` in non-test source returned no matches; `program.rs` remains over 400 lines but is already allowlisted.
- Verification stayed cheap: `cargo fmt -p worth-query`, warning-free `cargo check -p worth-query`, focused signature/call-site scans, and touched-file line checks. No broad tests were run.

## Query aspect-native set value constructor checkpoint

- Changed `WORTHQueryAspectValue::new`, `WORTHQueryAspectValue::new_set`, `WORTHQueryAspectMutationBuilder::aspect`, and graph relation set-aspect authoring so mutation values must be foundational `AspectValue` instead of arbitrary `Serialize` payloads.
- Added a native scalar projection from retained `WORTHQueryProgramValue` to `AspectValue`, so program write-template binding no longer lowers mutation values through terminal JSON.
- Removed the now-dead desired-aspect compatibility JSON lowering path and patched compiled fixtures to construct native string `AspectValue` values.
- Verification stayed cheap: `cargo fmt -p worth-query`, warning-free `cargo check -p worth-query`, focused scans for old generic/compatibility constructor vocabulary, and line-count checks with `program.rs` already allowlisted. No broad tests were run.

## Query aspect-native graph descriptor operation checkpoint

- Changed graph touch descriptor rows, descriptor constructors, obligation selectors, and lookup-key derivation so declared aspect operations retain `WORTHQueryAspectMutationOperation` values instead of stringified `set:path` evidence.
- Changed descriptor/selector declared mutation collection paths so touched aspect evidence requires `WORTHQueryAspectTouch`; support-matrix fixtures now construct explicit touches and typed set/clear operations.
- Kept terminal `kind:path` text only as digest/reporting projection while matching and lookup derive from typed operations and admitted touches.
- Verification stayed cheap: `cargo fmt -p worth-query`, warning-free `cargo check -p worth-query`, focused graph descriptor/selector residue scans, and touched-file line checks under 400. No broad tests were run.

## Query aspect-native graph read admitted aspect key checkpoint

- Changed admitted graph-read projection, predicate, ordering, boolean predicate, predicate selectivity, and field-authority rows so admitted aspect identity retains foundational `AspectKey` instead of raw aspect strings.
- Schema-reference admission now lowers authoring aspect text into `AspectKey` at the admitted-reference boundary and passes native keys through boolean admission, selectivity normalization, and access-authority construction.
- Removed the unused graph touch selector string-authoring aspect-path adapter; selector aspect matching now requires an admitted `WORTHQueryAspectTouch`.
- Verification stayed cheap: `cargo fmt -p worth-query`, warning-free `cargo check -p worth-query`, focused read-admission/string-constructor residue scans, and touched-file line checks under 400. Remaining `aspect: String` matches in this area are denial/error evidence only. No broad tests were run.

## Query aspect-native retained decode boundary checkpoint

- Changed `decode_single_retained_row` so terminal row decoding receives retained materialized row carriers instead of `&[serde_json::Value]`.
- Updated derived materialization and retained-upstream decode paths to pass retained rows directly; terminal JSON projection now happens only inside the explicitly terminal decode boundary.
- Updated computed materialization inspection identity to receive retained rows and project JSON only while constructing reporting digest text.
- Verification stayed cheap: `cargo fmt -p worth-query`, warning-free `cargo check -p worth-query`, focused retained-decode residue scans, and line checks with `runtime/computed/surface.rs` already allowlisted. No broad tests were run.

## Query aspect-native program output admission checkpoint

- Removed the crate-visible `WORTHQueryOperationOutput::from_terminal_json` constructor so production code can no longer mint operation output proof state directly from arbitrary terminal JSON.
- Made `WORTHQueryProgramValue::from_terminal_json` private to `program.rs`; live-read program outputs now flow through `WORTHQueryOperationOutput::from_live_read_entities`, which accepts retained `WORTHQueryEntity` carriers and performs terminal projection only inside the named live-read output boundary.
- Updated runtime and preview program read-live execution to pass entity carriers instead of prebuilding `Value::Array` payloads.
- Verification stayed cheap: `cargo fmt -p worth-query`, warning-free `cargo check -p worth-query`, focused constructor residue scans, and line-count checks with `program.rs` allowlisted and `runtime_reads_programs.rs` explicitly listed in the refactor doc. No broad tests were run.

## Query aspect-native effect intent input admission checkpoint

- Replaced the runtime-visible `WORTHQueryIntentInput::from_terminal_json` constructor with `WORTHQueryIntentInput::from_effect_payload`.
- Effect-triggered runtime intent admission now passes the retained `WORTHQueryEffectPayload` carrier directly instead of projecting terminal JSON before constructing intent input proof state.
- Kept terminal payload lowering private to the named effect-payload boundary inside intent input construction, so generic `serde_json::Value` can no longer be promoted to `WORTHQueryIntentInput` by runtime callers.
- Verification stayed cheap: `cargo fmt -p worth-query`, warning-free `cargo check -p worth-query`, focused residue scans, and touched-file line counts under 400. No broad tests were run.

## Query aspect-native projection scalar extraction checkpoint

- Added `consumed_scalar_value_from_entity_path` as the single projection-consumption adapter from a `WORTHQueryEntity` field path to foundational `AspectValue`.
- Updated live binding and row-like read-result extraction to request native scalar values from that adapter instead of separately reading `external_aspect_value`, cloning terminal row-path JSON, and decoding `serde_json::Value` at each fact construction site.
- Kept the terminal JSON scalar decoder private to the adapter, so consumed field fact construction now sees native `AspectValue` values mechanically.
- Verification stayed cheap: `cargo fmt -p worth-query`, warning-free `cargo check -p worth-query`, and focused residue scans showing terminal row-path scalar decoding isolated in `consumed_scalar_value.rs`. `row_like.rs` remains over 400 lines but is explicitly listed in the refactor doc's line-cap watch/exemption area. No broad tests were run.

## Query aspect-native external projection constructor checkpoint

- Removed the public `WORTHQueryEntity::from_external_projection` raw `serde_json::Value` constructor from production vocabulary.
- Added public `WORTHQueryEntity::from_external_projection_values` for native external projection construction from foundational `AspectValue` maps.
- Kept the unavoidable query-context row artifact on an explicitly named crate-local `from_terminal_json_external_projection` boundary, and changed synthetic read rows to publish native scalar projection values instead of building a JSON object.
- Verification stayed cheap: `cargo fmt -p worth-query`, warning-free `cargo check -p worth-query`, and focused constructor scans showing production callers use only the native constructor or the named crate-local terminal boundary. `memory_workspace/mod.rs` remains over 400 lines but is explicitly listed in the refactor doc's migration line-cap section. No broad tests were run.

## Query aspect-native live/derived handle marker checkpoint

- Added `WORTHQueryNativeRow` as the Query-native row marker for phantom live and derived view handle types.
- Changed `WORTHQueryLiveView` and `WORTHQueryDerivedViewHandle` defaults from `serde_json::Value` to `WORTHQueryNativeRow`, so public handle construction no longer teaches terminal JSON as the default row model.
- Updated internal dynamic live/derived handle construction to use the native marker and removed the now-unused preview `serde_json::Value` import.
- Verification stayed cheap: `cargo fmt -p worth-query`, warning-free `cargo check -p worth-query`, and focused marker scans showing no production `T = Value`, `LiveView<Value>`, or `DerivedViewHandle::<Value>` defaults remain outside cfg-test compatibility. Over-limit touched files are either CI-allowlisted or explicitly listed in the refactor doc. No broad tests were run.

## Query aspect-native entity terminal accessor checkpoint

- Removed unqualified production `WORTHQueryEntity::external_row`, `external_row_path`, and `into_external_row` vocabulary.
- Added native `external_scalar_value` for foundational scalar lookup and terminal-named JSON access through `terminal_json_row_projection`, `terminal_json_row_path_projection`, and `into_terminal_json_row_projection`.
- Updated projection consumption, read-composition materialization, and program live-read output lowering to use either native scalar access or explicitly terminal JSON projection.
- Verification stayed cheap: `cargo fmt -p worth-query`, warning-free `cargo check -p worth-query`, and focused scans showing no production `pub fn external_row`, `pub fn external_row_path`, or `into_external_row` names remain. Remaining `external_row()` matches are cfg-test/test fixtures. No broad tests were run.

## Query aspect-native terminal program port vocabulary checkpoint

- Renamed the broad `WORTHQueryPortType::Json` variant to `WORTHQueryPortType::TerminalJson`.
- Kept the port compatibility behavior unchanged while making terminal JSON an explicit program-port shape rather than a generic value authority vocabulary.
- Verification stayed cheap: `cargo fmt -p worth-query`, warning-free `cargo check -p worth-query`, and focused scans showing no production `WORTHQueryPortType::Json`, `PortType::Json`, or `Json,` variant residue in the program/runtime/harness areas. No broad tests were run.

## Query aspect-native retained field path checkpoint

- Added `WORTHQueryRetainedFieldPath`, a retained-row field path carrier backed by foundational `CanonicalFieldPath`.
- Changed `WORTHQueryRetainedMaterializedRow` storage and `from_scalar_values` construction to require `BTreeMap<WORTHQueryRetainedFieldPath, AspectValue>` instead of `BTreeMap<String, AspectValue>`.
- Updated production retained-row publishers in aspect API finalization and runtime API stabilization harness maintainers to admit field paths before constructing retained materialized rows.
- Terminal JSON row projection now derives dotted path text from the retained field-path carrier rather than treating strings as the retained row key.
- Verification stayed cheap: `cargo fmt -p worth-query`, warning-free `cargo check -p worth-query`, line-count checks, and focused scans confirming the retained row constructor now requires `WORTHQueryRetainedFieldPath`. No broad tests were run.

## Query aspect-native retained scalar fact field path checkpoint

- Changed retained scalar row/fact extraction storage to use `WORTHQueryRetainedFieldPath` instead of string field keys.
- Retained scalar field fact reporting now derives terminal field-key text through `terminal_field_key_projection`, while value lookup and fact construction retain the admitted field path carrier.
- Invalid retained scalar field requests now fail during field-path admission before row lookup.
- Verification stayed cheap: `cargo fmt -p worth-query`, warning-free `cargo check -p worth-query`, line-count checks, and focused scans confirming string-backed retained scalar field storage patterns are gone from the retained row/fact carrier files. No broad tests were run.

## Query aspect-native retained field-path enforcement checkpoint

- Removed string lookup from `WORTHQueryRetainedMaterializedRow`; retained row value access now requires `WORTHQueryRetainedFieldPath`.
- Threaded retained field-path carriers through derived materialization lookup, retained scalar fact extraction, retained scalar alignment, and retained-binding projection fact extraction.
- Public ergonomic field-key methods remain authoring/terminal projection boundaries, but internal extraction and comparison now use admitted retained field paths.
- Verification stayed cheap: `cargo fmt -p worth-query`, warning-free `cargo check -p worth-query`, focused residue scans, and touched-file line counts under 400. No broad tests were run.

## Query aspect-native native touch comparison checkpoint

- Made `WORTHQueryParsedAspectTarget` and `WORTHQueryAspectTouch` orderable/hashable by their native parsed target state.
- Changed aspect mutation builder duplicate detection, delete touch duplicate detection, aspect mutation operation equality/order/hash, and existing-truth probe response matching to compare native touch/target carriers instead of terminal aspect path strings.
- Kept terminal aspect-path text for diagnostics and digest/reporting projection only.
- Verification stayed cheap: `cargo fmt -p worth-query`, warning-free `cargo check -p worth-query`, focused residue scans for old string dedupe/comparison patterns, and touched-file line counts under 400. No broad tests were run.

## Query aspect-native existing-truth probe field checkpoint

- Changed `WORTHQueryExistingTruthProbeField::new_native` so backend probe fields must be constructed from an admitted `WORTHQueryAspectTouch`, not a raw aspect-path string.
- Updated stateful bridge, test verification, permissive verification, and intent-admission certification adapters to preserve request touches into returned probe fields.
- Added native `field_for_touch` lookup on `WORTHQueryExistingTruthProbe`; the string lookup now re-enters through touch admission and delegates to the native lookup.
- Removed the stale `InvalidProbedAspectPath` probe denial variant because invalid probe-field paths are no longer constructible at field construction time.
- Verification stayed cheap: `cargo fmt -p worth-query`, warning-free `cargo check -p worth-query`, focused constructor/denial residue scans, and touched-file line counts under 400. No broad tests were run.

## Query aspect-native consumed projection field-path checkpoint

- Added `ConsumedProjectionFieldPath`, backed by foundational `CanonicalFieldPath`, for consumed projection field facts.
- Changed `ConsumedFieldValueFact` so field identity is stored as the typed field-path carrier while public `field_key()` remains terminal/reporting projection text.
- Updated live-binding, retained-binding, query-context, and row-like extraction to admit declared field text into `ConsumedProjectionFieldPath` before constructing consumed field facts.
- Retained extraction also keeps the separate `WORTHQueryRetainedFieldPath` for retained-row lookup, so retained source lookup and consumed fact publication now each use their own native proof carrier.
- Verification stayed cheap: `cargo fmt -p worth-query`, warning-free `cargo check -p worth-query`, focused residue scans showing no string-backed consumed field fact storage or raw-field constructor calls, and line-cap checks with `row_like.rs` already named in the refactor doc line-cap watch list. No broad tests were run.

## Query aspect-native projection fact request field-path checkpoint

- Added `ProjectionFactFieldPath`, backed by foundational `CanonicalFieldPath`, at the projection fact request layer.
- Changed `ProjectionFactRequest::DisplayField` and `ProjectionFactRequest::DerivedScalarField` to carry `ProjectionFactFieldPath` instead of raw `String`.
- Reused `ProjectionFactFieldPath` inside `ConsumedFieldValueFact`, replacing the narrower `ConsumedProjectionFieldPath` wrapper so request admission and consumed fact publication share the same proof carrier.
- Updated `BoundProjectionFactFamily` and the live, retained, query-context, and row-like extractors to pass the admitted request field path into consumed fact construction instead of re-parsing field text during extraction.
- Exported `ProjectionFactFieldPath` through projection consumption and the foundation facade for direct typed enum construction.
- Verification stayed cheap: `cargo fmt -p worth-query`, warning-free `cargo check -p worth-query`, focused residue scans showing no string-backed field request variants or consumed field wrapper residue, and touched-file line counts under 400. No broad tests were run.

## Query aspect-native graph-read field-key checkpoint

- Changed admitted graph-read projection, predicate, and ordering field rows to store foundational `FieldKey` instead of raw field strings.
- Graph-read schema reference admission now lowers request field text into `FieldKey` at the same boundary that admits aspect keys and schema field kinds.
- Threaded native field keys through admitted boolean predicate leaves, predicate selectivity rows, and predicate/ordering field authority rows.
- Kept terminal `field()` text only as reporting/digest projection; downstream proof rows now consume `native_field_key().clone()` instead of reparsing or passing string field names.
- Verification stayed cheap: `cargo fmt -p worth-query`, warning-free `cargo check -p worth-query`, focused scans showing targeted graph-read field carriers use `FieldKey` with only denial/error field text remaining, and touched-file line counts under 400. No broad tests were run.

## Query aspect-native row-like field map checkpoint

- Changed row-like projection extraction internals so materialized row field bags are keyed by `ProjectionFactFieldPath` instead of raw strings.
- Relational, bridge, and Query read entity rows now admit row field text into projection fact field-path carriers before entering shared materialized-row extraction.
- Shared materialized-row lookup now requires `&ProjectionFactFieldPath`; terminal field text is used only for external row access and diagnostics.
- Split row field-path admission into `row_like_field_paths.rs`, keeping `row_like.rs` under the 400-line rule while preserving cheap mechanical enforcement.
- Verification stayed cheap: `cargo fmt -p worth-query`, warning-free `cargo check -p worth-query`, focused scans confirming string-keyed `AspectValue` row maps are gone from projection extraction, and touched-file line counts under 400. No broad tests were run.

## Query aspect-native memory workspace row carrier checkpoint

- Split `memory_workspace::entity_row` out of the workspace module and moved `WORTHQueryEntity` row storage there.
- Changed `WORTHQueryEntityRow::AspectProjection` storage so aspect truth is keyed by foundational `AspectKey` and external projection values are keyed by foundational `CanonicalFieldPath`, not raw string maps.
- Row accessors now admit terminal aspect/path strings before lookup; terminal JSON row projection is derived from native field-path storage.
- Workspace projection from relational records now constructs the native row maps directly, while the remaining `BTreeMap<String, AspectValue>` entrypoint is an explicit terminal ingress that immediately lowers into `CanonicalFieldPath` storage.
- Verification stayed cheap: `cargo fmt -p worth-query`, warning-free `cargo check -p worth-query`, focused storage scans, and touched-file line counts under 400. No broad tests were run.

## Query aspect-native native touch dedupe checkpoint

- Changed preview live-aspect relevance dedupe from `BTreeMap<String, WORTHQueryAspectTouch>` to `BTreeSet<WORTHQueryAspectTouch>`.
- Changed read-obligation touch-shape dedupe from string-keyed touch maps to native touch sets.
- Terminal aspect path text remains only for request matching and reporting projections; dedupe now requires admitted touch carriers.
- Verification stayed cheap: `cargo fmt -p worth-query`, warning-free `cargo check -p worth-query`, focused scans confirming the targeted string-keyed touch maps are gone, and touched-file line counts under 400. No broad tests were run.

## Query aspect-native external projection row ingress checkpoint

- Narrowed `WORTHQueryEntity::from_external_projection_values` to crate-private native row construction that requires `BTreeMap<CanonicalFieldPath, AspectValue>`.
- Removed the remaining `BTreeMap<String, AspectValue>` lowering helper from memory workspace entity rows.
- Updated synthetic read materialization fallback to construct native `CanonicalFieldPath` keys before creating the entity row.
- Verification stayed cheap: `cargo fmt -p worth-query`, warning-free `cargo check -p worth-query`, focused scans showing no string-keyed `AspectValue` maps remain in memory workspace/read materialization row construction, and touched-file line counts under 400. No broad tests were run.

## Query aspect-native terminal row path removal checkpoint

- Replaced query-context read materialization's terminal JSON entity rows with native `CanonicalFieldPath` to `AspectValue` row construction.
- Removed the `WORTHQueryEntityRow::ExternalProjection` terminal JSON variant and its constructor.
- Removed projection-consumption scalar extraction's JSON fallback; row-like extraction now consumes native entity scalar values directly.
- Replaced read-composition identity and relation lookups with native scalar access instead of `terminal_json_row_path_projection`.
- Deleted the unused terminal JSON row path walker from `WORTHQueryEntity`.
- Verification stayed cheap: `cargo fmt -p worth-query`, warning-free `cargo check -p worth-query`, focused scans showing no `from_terminal_json_external_projection`, `ExternalProjection`, `terminal_json_row_path_projection`, or JSON scalar fallback residue, and touched-file line counts under 400. No broad tests were run.

## Query aspect-native retained decode vocabulary checkpoint

- Renamed production derived materialization decode APIs from `terminal_json_decode_*` to retained-row vocabulary: `decode_single_retained_row`, `decode_retained_row_pair`, and `decode_retained_row_triple`.
- Updated bundle and artifact binding decode flow to call the retained-row APIs directly.
- Left old terminal-json decode names only as `#[cfg(test)]` compatibility shims for existing test debt.
- Verification stayed cheap: `cargo fmt -p worth-query`, warning-free `cargo check -p worth-query`, focused scans confirming terminal decode names in surface files are test-only, and touched-file line counts under 400. No broad tests were run.

## Query aspect-native retained decode audit checkpoint

- Updated projection-consumption forbidden-fallback audit seams from terminal JSON decode names to retained decode seam names.
- Renamed audit variants to `DecodeRowPair` and `DecodeRowTriple`, matching the existing runtime fallback seam vocabulary and retained-row API names.
- Updated the refactor plan text so the Phase 3 retained materialization publication slice no longer claims serde row decode helpers are terminal-named production APIs.
- Verification stayed cheap: `cargo fmt -p worth-query`, warning-free `cargo check -p worth-query`, focused scans showing no stale `TerminalJsonDecode` or terminal decode seam language in the audit/doc targets, and touched-file line counts under 400. No broad tests were run.

## Query aspect-native retained row public accessor checkpoint

- Made `WORTHQueryDerivedMaterializationResult::retained_rows` public so callers can inspect retained native rows directly.
- Made `WORTHQueryDerivedMaterializationResult::retained_scalar_value_at_path` public for typed retained field-path lookup.
- Made `WORTHQueryRetainedMaterializedRow::field_value_at` public and added a read-only `scalar_values` iterator over retained field paths and foundational `AspectValue`s.
- This gives downstream code an ergonomic native retained-row path before terminal JSON projection helpers.
- Verification stayed cheap: `cargo fmt -p worth-query`, warning-free `cargo check -p worth-query`, focused accessor visibility scans, and touched-file line counts under 400. No broad tests were run.

## Query aspect-native effect intent input checkpoint

- Removed the production effect-payload-to-intent hop that rebuilt `WORTHQueryIntentInput` from `payload.terminal_json_projection()`.
- `WORTHQueryIntentInput::from_effect_payload` now consumes native `WORTHQueryEffectPayload` fields directly: condition and admitted aspect touches.
- Intent input digesting now derives deterministic digest material from the retained input tree instead of serializing the terminal JSON projection.
- The terminal JSON input projection remains only as an explicit projection boundary.
- Verification stayed cheap: `cargo fmt -p worth-query`, warning-free `cargo check -p worth-query`, focused scans for the removed JSON promotion/digest path, and touched-file line counts under 400. No broad tests were run.

## Query aspect-native read receipt row digest checkpoint

- Added native row digest material on `WORTHQueryEntity` over admitted `AspectKey`, `CanonicalFieldPath`, and foundational `AspectValue` maps.
- Changed read receipt materialized result digest construction to consume native row digest parts instead of serializing `terminal_json_row_projection()`.
- Terminal JSON row projection remains an explicit export helper, not the read receipt digest substrate.
- Verification stayed cheap: `cargo fmt -p worth-query`, warning-free `cargo check -p worth-query`, focused scans confirming `read_receipt_support.rs` no longer serializes terminal JSON rows, and touched-file line counts under 400. No broad tests were run.

## Query aspect-native derived materialization terminal fence checkpoint

- Fenced derived materialization terminal JSON shortcut APIs (`terminal_json_read_derived`, `terminal_json_materialize`, terminal row projection helpers, retained upstream terminal mutation helpers) behind `#[cfg(test)]`.
- Promoted native retained upstream access (`retained_computed_rows`) to public so callers have a retained-row path instead of terminal projection.
- Made the now-test-only `serde_json::Value` import in `runtime.rs` cfg-test as well, keeping the production compile surface warning-free.
- Verification stayed cheap: `cargo fmt -p worth-query`, warning-free `cargo check -p worth-query`, and focused context scans showing the terminal materialization shortcut names guarded by `#[cfg(test)]`. `runtime/computed/surface.rs` remains over 400 lines but is explicitly listed in the refactor plan and CI line-cap allowlist. No broad tests were run.

## Query aspect-native retained decode fence checkpoint

- Fenced the generic retained-row serde decode path behind `#[cfg(test)]`, including `decode_single_computed_row`, materialization-result/bundle/artifact decode helpers, and retained row pair/triple decode shims.
- Left production callers with materialization objects, retained rows, and typed retained scalar access instead of JSON-backed `DeserializeOwned` shortcuts.
- The remaining `serde_json::from_value(row.terminal_json_projection())` retained decode bridge is now test-only compatibility debt.
- Verification stayed cheap: `cargo fmt -p worth-query`, warning-free `cargo check -p worth-query`, focused context scans proving decode helpers and the JSON decode bridge are cfg-test, and touched-file line counts with `runtime/computed/surface.rs` still allowlisted/plan-listed. No broad tests were run.

## Query aspect-native computed materialization identity checkpoint

- Added native retained-row digest parts on `WORTHQueryRetainedMaterializedRow`, derived from admitted retained field paths and foundational `AspectValue` digest text.
- Changed computed materialization inspection identity to consume those native digest parts instead of projecting retained rows to terminal JSON and serializing them.
- Verification stayed cheap: `cargo fmt -p worth-query`, warning-free `cargo check -p worth-query`, focused scan confirming the old `serde_json::to_string(&row)` materialization identity path is gone, and touched-file line counts checked. No broad tests were run.

## Query aspect-native effect payload handoff checkpoint

- Added native digest material on `WORTHQueryEffectPayload`, composed from native `WORTHQueryAspectTouch` digest parts instead of terminal JSON payload projection text.
- Changed pending effect-intent handoff identity to consume `pending_delivery.payload().native_digest_material()` rather than `terminal_json_payload_projection().to_string()`.
- Split effect delivery helper/projection functions and effect handle type into small sibling modules so touched runtime effect files stay under the 400-line cap.
- Verification stayed cheap: `cargo fmt -p worth-query`, warning-free `cargo check -p worth-query`, focused scans confirming the pending handoff no longer serializes terminal payload JSON, and touched-file line counts all under 400. No broad tests were run.

## Query aspect-native mutation value evidence checkpoint

- Added native desired-aspect digest material over `WORTHQueryDesiredAspectValue` and foundational `AspectValue` families.
- Exposed `WORTHQueryAspectValue::native_digest_material()` so mutation evidence can consume parsed aspect targets plus native desired values without serializing terminal JSON.
- Changed backend mutation authority evidence, existing-truth assertion aspect evidence, and existing-truth probe field value digests to use native digest material. Terminal JSON strings remain for bridge lowering/reporting compatibility.
- Verification stayed cheap: `cargo fmt -p worth-query`, warning-free `cargo check -p worth-query`, focused mutation residue scans, and touched-file line counts all under 400. No broad tests were run.

## Query aspect-native computed patch terminal fence checkpoint

- Fenced computed patch payload terminal JSON projection APIs behind `#[cfg(test)]`; production derived patches now retain native payload carriers without a default terminal payload projection API.
- Fenced retained materialized row terminal JSON projection and its JSON path setter behind `#[cfg(test)]` after production identity paths moved to native row digest parts.
- Kept retained scalar terminal projection available where retained scalar fact compatibility still explicitly uses it.
- Verification stayed cheap: `cargo fmt -p worth-query`, warning-free `cargo check -p worth-query`, focused cfg-context scans, and touched-file line counts checked. No broad tests were run.

## Query aspect-native mutation metadata checkpoint

- Changed mutation metadata values from arbitrary `Serialize` lowered into retained JSON text to explicit validated text metadata.
- Removed production `terminal_json_text` metadata vocabulary; mutation authority and write/causal receipt identities now consume `native_digest_text()`.
- Changed aspect/delete mutation builders so metadata authoring accepts text directly instead of teaching JSON serialization as the metadata substrate.
- Verification stayed cheap: `cargo fmt -p worth-query`, warning-free `cargo check -p worth-query`, focused metadata residue scan, and touched-file line counts all under 400. No broad tests were run.

## Query aspect-native effect terminal projection fence checkpoint

- Fenced `WORTHQueryEffectPayload::terminal_json_projection`, `WORTHQueryEffectDelivery::terminal_json_payload_projection`, and their JSON array helper behind `#[cfg(test)]`.
- Production effect handoff and intent input paths now consume native payload/touch accessors and native digest material; terminal payload JSON remains test compatibility only.
- Verification stayed cheap: `cargo fmt -p worth-query`, warning-free `cargo check -p worth-query`, and focused cfg-context scans. No broad tests were run.

## Query aspect-native intent terminal projection removal checkpoint

- Removed unused intent input terminal JSON projection helpers instead of preserving dead compatibility API.
- Production intent declaration/input surfaces now retain and digest the native input tree only; no `serde_json::Value` import remains in `runtime/intent`.
- Verification stayed cheap: `cargo fmt -p worth-query`, warning-free `cargo check -p worth-query`, focused intent residue scan, and touched-file line counts under 400. No broad tests were run.

## Query aspect-native program and enforcement checkpoint

- Removed the live-read program output path that projected `WORTHQueryEntity` rows to terminal JSON before readmitting them into `WORTHQueryProgramValue`.
- Added native program row construction from retained `CanonicalFieldPath -> AspectValue` entity projections.
- Added `WORTHQueryEntity::external_projection_values()` as a crate-local native iterator so downstream code can consume projected native values without terminal row JSON.
- Changed retained scalar fact and retained row digesting to use variant-aware native `AspectValue` digest text instead of `retained_scalar_terminal_json(value).to_string()`.
- Split retained scalar value conversion/projection/digest helpers into `runtime/surface/retained_scalar_values.rs`, bringing `retained_scalar_facts.rs` back under the 400-line cap.
- Added `tests/aspect_native_query_compile_fail.rs` with focused trybuild fixtures proving raw JSON cannot satisfy program inputs or derived patch retained-row payloads, and proving private program/effect payload states cannot be WORTHd through public struct literals.
- Verification stayed careful: `cargo fmt -p worth-query`, warning-free `cargo check -p worth-query`, and the narrow `cargo test -p worth-query --test aspect_native_query_compile_fail` target only. No broad suites were run.

## Query aspect-native assertion denial evidence checkpoint

- Renamed existing-truth assertion denial optional expected/found evidence from external JSON strings to native value digest strings.
- Changed assertion denial evidence identity tags from `expected`/`found` to `expected_native_value`/`found_native_value`.
- Updated internal and public-bridge test verification adapters to pass native digest text for expected/found values instead of `serde_json::Value::to_string()`.
- Added an aspect-native compile-fail fixture proving the old `expected_external_value_json` and `found_external_value_json` accessors are gone.
- Verification stayed careful: `cargo fmt -p worth-query`, warning-free `cargo check -p worth-query`, focused residue scans for the old accessor names, and the narrow `cargo test -p worth-query --test aspect_native_query_compile_fail` target only. No broad suites were run.

## Query aspect-native existing-truth probe field checkpoint

- Removed cached `external_value_json` storage from `WORTHQueryExistingTruthProbeField`; the field now retains native foundational `AspectValue` plus native digest evidence.
- Replaced the public old-name accessor with explicit `terminal_json_projection_string()` for compatibility reporting/export contexts.
- Updated runtime and public bridge tests that inspect probe values to use the terminal projection name instead of teaching external JSON as the field carrier.
- Added an aspect-native compile-fail fixture proving `WORTHQueryExistingTruthProbeField::external_value_json()` is gone.
- Verification stayed careful: `cargo fmt -p worth-query`, warning-free `cargo check -p worth-query`, and the narrow `cargo test -p worth-query --test aspect_native_query_compile_fail` target only. No broad suites were run.

## Query aspect-native program terminal projection fence checkpoint

- Fenced `WORTHQueryProgramValue::terminal_json_projection`, `WORTHQueryOperationInput::terminal_json_value_projection`, and `WORTHQueryOperationOutput::terminal_json_value_projection` behind `#[cfg(test)]`.
- Removed the broad production `serde_json::Value` import from `program.rs`; terminal JSON conversion now exists only as a cfg-test helper.
- Added an aspect-native compile-fail fixture proving public callers cannot use retained program values or operation IO as terminal JSON escape hatches.
- Changed command-declared mutation evidence lowering to use `WORTHQueryAspectValue::native_digest_material()` instead of `terminal_json_projection_string()` for declared aspect value identity rows.
- Verification stayed careful: warning-free `cargo check -p worth-query` after each slice and the narrow `cargo test -p worth-query --test aspect_native_query_compile_fail` target only. No broad suites were run.

## Query aspect-native retained scalar and entity terminal fence checkpoint

- Fenced `WORTHQueryRetainedScalarFieldFact::terminal_json_projection` behind `#[cfg(test)]`; public callers now use the retained native `AspectValue` accessor instead of terminal JSON.
- Fenced `retained_scalar_terminal_json` itself behind `#[cfg(test)]` after production callers no longer used it.
- Fenced `WORTHQueryEntity::terminal_json_row_projection` and `WORTHQueryEntity::into_terminal_json_row_projection` behind `#[cfg(test)]`, leaving native entity accessors as the production path.
- Marked the supporting memory-workspace external JSON projection modules as test-only after entity terminal row projection moved out of production.
- Added aspect-native compile-fail fixtures proving public callers cannot call the removed retained-scalar or entity terminal JSON projection methods.
- Verification stayed careful: warning-free `cargo check -p worth-query` and the narrow `cargo test -p worth-query --test aspect_native_query_compile_fail` target only. No broad suites were run.

## Query aspect-native broad test checkpoint

- User asked for whole crate tests despite compile cost; ran `cargo test -p worth-query`, which failed during compilation before any tests executed.
- Continued with `cargo test -p worth-query --no-run` to keep the full test surface compile-checked without repeatedly running expensive suites.
- Migrated several broad-test frontiers to native carriers: public bridge existing-truth probe fields, public bridge hostile certification retained rows, public bootstrap/submission/runtime-backed read tests, graph obligation descriptor fixtures, graph composition public bridge fixtures, in-memory backend facade tests, backend trait test implementations, and domain/effect intent fixtures.
- Current broad-test frontier remains compile-red in projection-consumption fixtures (`WORTHQueryEntity::from_external_projection`, JSON comparisons against native `AspectValue`) and graph-obligation selector fixtures still using raw string selectors/old `touched_aspect_paths` fields.

## Query aspect-native broad test checkpoint 2

- Ran `cargo test -p worth-query --no-run` repeatedly as the broad-test compile frontier, plus `cargo fmt -p worth-query` after migration slices.
- Migrated projection-consumption fixtures from terminal JSON rows to native `WORTHQueryEntity::from_external_projection_values` and `AspectValue` assertions.
- Migrated graph-obligation selector/descriptor fixtures to `WORTHQueryAspectTouch`, native mutation operations, and `touched_aspects`.
- Migrated stateful bridge runtime support away from removed JSON entity constructors; live entities now rebuild native external projection values.
- Migrated runtime assembly/support-profile tests and much of `runtime/tests/computed.rs` to native touch lists, native aspect values, and retained-row derived patch payloads.
- Current broad-test compile frontier has moved past `computed.rs`; it is now red in effect tests and older runtime fixtures still using raw aspect string arrays, raw `UpdateAspect { aspect_path, value }`, JSON intent inputs, and string builder `.aspect(...)` calls.

## Query aspect-native full crate test checkpoint

- Ran `cargo test -p worth-query --no-run`; the full crate test surface compiled successfully.
- Ran the full `cargo test -p worth-query` suite after the broad fixture migration. The run executed tests instead of failing at compile time, and ended red: 2576 passed, 22 failed, 0 ignored, in 310.81s.
- The remaining failures cluster around retained materialized row shape expectations (`{"value": ...}` native/object rows versus old scalar terminal JSON), one retained row decode expecting a string, and verify-existing denial digest expectations that now include native aspect-path/op context.
- This confirms the next repair pass should stay mostly on assertion/fixture alignment and avoid re-running the full suite until cheaper `cargo check` / `cargo test --no-run` frontiers are green again.

## Query aspect-native whole crate green checkpoint

- Repaired the remaining runtime failures after the first whole-crate run: memory workspace native declaration parsing, retained-row test assertions, computed/effect/intent retained payload assertions, and verify-existing native denial digest expectations.
- Flushed stale trybuild suites in focused passes while preserving mechanical boundary intent: aspect-native query, bridge truth identity, phase boundaries, domain capabilities, graph obligation, graph read access, graph touch descriptor, intent admission, and runtime receipts.
- Updated public compile-fail fixtures to native intent inputs, native aspect keys/field keys, native touch field names, and current backend/receipt shapes so tests fail at the intended sealed boundary instead of old terminal JSON/string fields.
- Final verification: `cargo fmt -p worth-query` and full `cargo test -p worth-query` returned `EXIT:0`; lib tests reported 2598 passed, 0 failed, and all integration, trybuild, and doc-test blocks completed green.

## Query aspect-native external JSON ingress quarantine checkpoint

- Audited the active refactor spec after the whole-crate green run and found the provisional `aspect_field_authoring/external_json_ingress.rs` production module still available.
- Quarantined `external_json_ingress` behind `#[cfg(test)]` and removed its crate-level re-export; `external_json_projection` remains test-only.
- Migrated production/certification/harness callers to `single_native_string_aspect_field_patch` instead of `single_aspect_field_patch_from_external_json(..., json!(...))`.
- Updated `_docs/worth-query/aspect_native_query_refactor.md` so the production JSON allowlist is currently empty and the external JSON ingress quarantine is recorded as a Phase 4 checkpoint.
- Verification stayed careful and focused: `cargo fmt -p worth-query`, `cargo check -p worth-query`, `cargo test -p worth-query aspect_field_authoring --lib`, `cargo test -p worth-query --test aspect_native_query_compile_fail`, and scans showing the old external ingress helper is confined to its cfg-test module.

## Query aspect-native mutation/probe terminal JSON fence

- Fenced `WORTHQueryAspectValue`, `WORTHQueryDesiredAspectValue`, and `WORTHQueryExistingTruthProbeField` terminal JSON projection helpers behind `#[cfg(test)]`.
- Migrated the public bridge runtime seeded probe certification to assert `foundational_value()` instead of terminal JSON strings.
- Added aspect-native trybuild cases proving public callers cannot call aspect-value or probe-field terminal JSON projection accessors.
- Verification: `cargo check -p worth-query`, `cargo test -p worth-query --test aspect_native_query_compile_fail`, and focused `cargo test -p worth-query --test public_bridge_runtime_bootstrap public_bridge_runtime_builder_lane_supports_seeded_existing_truth_probe` passed.

## Query certification fixture native handle markers

- Replaced intent-admission effect certification fixture live/effect generic markers from `serde_json::Value` to `WORTHQueryNativeRow`.
- Covered both the standalone certified effect fixture and legacy/canonical delegation parity effect paths.
- Verification: `cargo check -p worth-query` passed. A focused `cargo test -p worth-query worth_query_intent_admission_representative_family_report --lib` compiled the lib test target but matched 0 tests, so it was useful only as compile coverage.

## Query harness native handle markers

- Replaced runtime API stabilization transcript live/computed/effect handle markers from `serde_json::Value` to `WORTHQueryNativeRow`.
- Replaced aspect API finalization certification row live/computed handle markers from `serde_json::Value` to `WORTHQueryNativeRow`.
- Verification: `cargo check -p worth-query` passed, and the harness/intent-admission marker scan for `Value` handle declarations returned no matches.

## Query consumer-kit native handle marker

- Replaced consumer-kit in-memory backend behavior test live-view markers from `serde_json::Value` to `WORTHQueryNativeRow`.
- Verification: `cargo check -p worth-query` and focused `cargo test -p worth-query in_memory_test_runtime_executes_public_insert_and_live_read --lib` passed. The focused test still reports pre-existing unused `serde_json::json` warnings elsewhere.

## Query dead terminal JSON shim cleanup

- Removed unused cfg-test terminal JSON row mutators from `WORTHQueryDerivedViewMaterialization`; retained-row native helpers remain the mutation path.
- Removed stale unused `serde_json::json` imports from domain capability/effect lifecycle tests and the unused shared-read JSON helper.
- Verification: `cargo check -p worth-query` passed, and focused `cargo test -p worth-query in_memory_test_runtime_executes_public_insert_and_live_read --lib` passed without the previous warning clutter.

## Query retained decode alias cleanup

- Removed cfg-test `terminal_json_decode_*` aliases from derived materialization result, bundle, and artifact binding surfaces.
- Updated runtime computed, post-write artifact, and intent-admission tests to call retained decode methods directly.
- Verification: `cargo check -p worth-query`, `cargo test -p worth-query derived_materialization_bundle --lib`, `cargo test -p worth-query batch_write_retained_artifact_keeps_receipt_inspection_and_exact_retained_binding_together --lib`, and `cargo test -p worth-query materialize_intent_common_path_helper_executes_through_canonical_handoff --lib` passed.

## Query retained materialization constructor fence

- Removed the remaining cfg-test `WORTHQueryDerivedMaterializationResult` constructors that accepted `Vec<serde_json::Value>` (`new`, `from_terminal_json_rows`, and the JSON-row `test_only` path).
- Migrated retained scalar, retained alignment, derived artifact binding, and retained-live projection-consumption fixtures to construct `WORTHQueryRetainedMaterializedRow` values from `WORTHQueryRetainedFieldPath -> AspectValue` maps.
- Added a cfg-test `test_only_retained_rows` constructor for cross-module fixtures so test support can still build retained materialization results without admitting terminal JSON rows into the proof carrier.
- Verification: `cargo check -p worth-query`, `cargo test -p worth-query retained_scalar --lib`, `cargo test -p worth-query retained_live --lib`, and `cargo test -p worth-query decode_row --lib` passed; the deleted constructor scan returned no matches.

## Query retained upstream JSON ingress deletion

- Removed `WORTHQueryRetainedUpstreamInputs::terminal_json_test_inputs` and migrated its single computed upstream test to `from_retained_computed_rows` with an explicit retained row.
- Deleted `WORTHQueryRetainedMaterializedRow::from_terminal_json_row` and the now-dead JSON-to-`AspectValue` scalar converter, leaving retained-row construction on explicit native field paths and foundational values.
- Removed `WORTHQueryRetainedUpstreamInputs::terminal_json_computed_rows_projection` after computed refresh tests switched row-count logic to `retained_computed_rows`.
- Verification: `cargo check -p worth-query`, `cargo test -p worth-query retained_upstreams_decode_single_computed_rows_through_query_runtime_floor --lib`, `cargo test -p worth-query upstream --lib`, and `cargo test -p worth-query --test aspect_native_query_compile_fail` passed.

## Query runtime test native handle markers

- Migrated src-hosted runtime tests from `serde_json::Value` handle markers to `WORTHQueryNativeRow` for live, maintained derived/computed, and effect handles wherever the generic type was only a marker.
- Covered computed, live, effect, preview, intent-admission, live-state, mutation, graph-composition, shared-read, assembly, and runtime support test modules, including shared-read pinning hostile matrix helper signatures.
- Left real JSON fixture/support values in place only where they model terminal projections, external rows, or serialized documents rather than handle authority.
- Verification: `cargo fmt -p worth-query`, `cargo check -p worth-query`, `cargo test -p worth-query --lib`, and `cargo test -p worth-query --test aspect_native_query_compile_fail` passed. The handle-marker residue scans across `crates/worth-query/src` and `crates/worth-query/tests` returned no matches for the old `Value` handle spellings, including maintained-derived declarations.

## Query terminal JSON projection compile-fail extension

- Added aspect-native trybuild fixtures proving public callers cannot call terminal JSON projection accessors on `WORTHQueryDerivedPatchPayload`, `WORTHQueryDerivedPatch`, `WORTHQueryEffectPayload`, or `WORTHQueryEffectDelivery`.
- This extends the existing mechanical fences for retained rows, aspect values, program values, entity rows, and probe fields, keeping terminal JSON helpers out of public authority vocabulary.
- Verification: `TRYBUILD=overwrite cargo test -p worth-query --test aspect_native_query_compile_fail`, normal `cargo test -p worth-query --test aspect_native_query_compile_fail`, and `cargo check -p worth-query` passed.

## Query mutation delta native touch constructor fence

- Removed the string-path `WORTHQueryMutationDelta::new(Vec<String>)` constructor entirely and made `WORTHQueryMutationDelta::from_touched_aspects` the public constructor.
- Migrated runtime receipt construction, computed patch deltas, certification fixtures, transcript authorities, public bridge runtime support, and runtime test authorities to pass retained `WORTHQueryAspectTouch` values.
- Added `WORTHQueryWriteCommand::declared_aspect_touches()` so receipt code can use the command's native touch state directly while `declared_aspect_paths()` remains projection/reporting vocabulary.
- Added a trybuild fixture proving public callers cannot call the removed string constructor; the residue scan now finds `WORTHQueryMutationDelta::new` only in that compile-fail fixture.
- Verification: `cargo fmt -p worth-query`, `cargo check -p worth-query`, `cargo test -p worth-query --lib`, `TRYBUILD=overwrite cargo test -p worth-query --test aspect_native_query_compile_fail`, and normal `cargo test -p worth-query --test aspect_native_query_compile_fail` passed.

## Query mutation delta terminal projection naming

- Removed the neutral `WORTHQueryMutationDelta::aspect_paths()` alias so mutation deltas expose native authority through `touched_aspects()` and dotted strings only through `terminal_aspect_paths_projection()`.
- Migrated production digest/reporting code and src-hosted tests to the explicit terminal projection name.
- Added a trybuild fixture proving public callers cannot call the removed mutation-delta path alias.
- Verification: `cargo fmt -p worth-query`, `cargo check -p worth-query`, `cargo test -p worth-query --lib --no-run`, `TRYBUILD=overwrite cargo test -p worth-query --test aspect_native_query_compile_fail`, and normal `cargo test -p worth-query --test aspect_native_query_compile_fail` passed; the narrow residue scan finds the alias only in the compile-fail fixture.

## Query write command declared touch alias fence

- Removed the neutral `WORTHQueryWriteCommand::declared_aspect_paths()` method and its lowering helper so declared mutation authority flows through `declared_aspect_touches()` and `declared_aspect_operations()`.
- Runtime-backend subject digest construction now counts native declared touches instead of projecting command state to path strings.
- Added a trybuild fixture proving public callers cannot call the removed command path alias; the compiler points them toward `declared_aspect_touches()`.
- Verification: `cargo fmt -p worth-query`, `cargo check -p worth-query`, `cargo test -p worth-query --lib --no-run`, `TRYBUILD=overwrite cargo test -p worth-query --test aspect_native_query_compile_fail`, and normal `cargo test -p worth-query --test aspect_native_query_compile_fail` passed; the narrow residue scan finds the method call only in the compile-fail fixture.

## Query live patch terminal projection naming

- Removed the neutral `WORTHQueryLivePatch::aspect_paths()` alias so live patches expose native authority through `touched_aspects()` and dotted strings only through `terminal_aspect_paths_projection()`.
- Added a trybuild fixture proving public callers cannot recover live patch path strings through the removed neutral alias.
- Verification: `cargo fmt -p worth-query`, `cargo check -p worth-query`, `cargo test -p worth-query --lib --no-run`, `TRYBUILD=overwrite cargo test -p worth-query --test aspect_native_query_compile_fail`, and normal `cargo test -p worth-query --test aspect_native_query_compile_fail` passed; the narrow residue scan finds the live patch alias only in the compile-fail fixture.

## Query derived patch terminal projection naming

- Removed the neutral `WORTHQueryDerivedPatch::aspect_paths()` alias so derived patches expose native authority through `aspect_touches()` and dotted strings only through `terminal_aspect_paths_projection()`.
- Added a trybuild fixture proving public callers cannot recover derived patch path strings through the removed neutral alias; the compiler suggests `aspect_touches()`.
- Verification: `cargo fmt -p worth-query`, `cargo check -p worth-query`, `cargo test -p worth-query --lib --no-run`, `TRYBUILD=overwrite cargo test -p worth-query --test aspect_native_query_compile_fail`, and normal `cargo test -p worth-query --test aspect_native_query_compile_fail` passed; the narrow residue scan finds the derived patch alias only in the compile-fail fixture.

## Query effect delivery terminal projection naming

- Removed the neutral `WORTHQueryEffectDelivery::aspect_paths()` alias so effect deliveries expose native authority through `aspect_touches()` and dotted strings only through `terminal_aspect_paths_projection()`.
- Migrated runtime effect inspection identity and intent-admission handoff binding digests to the explicit terminal projection name.
- Added a trybuild fixture proving public callers cannot recover effect delivery path strings through the removed neutral alias; the compiler suggests `aspect_touches()`.
- Verification: `cargo fmt -p worth-query`, `cargo check -p worth-query`, `cargo test -p worth-query --lib --no-run`, `TRYBUILD=overwrite cargo test -p worth-query --test aspect_native_query_compile_fail`, and normal `cargo test -p worth-query --test aspect_native_query_compile_fail` passed; the narrow residue scan finds the effect delivery alias only in the compile-fail fixture.

## Query existing-truth probe request terminal projection naming

- Removed the neutral `WORTHQueryExistingTruthProbeRequest::aspect_paths()` alias so probe requests expose native authority through `aspect_touches()` and dotted strings only through `terminal_aspect_paths_projection()`.
- Migrated the legacy workspace-routing certification comparison to the explicit terminal projection name at the legacy path execution boundary.
- Added a trybuild fixture proving public callers cannot recover probe request path strings through the removed neutral alias; the compiler suggests `aspect_touches()`.
- Verification: `cargo fmt -p worth-query`, `cargo check -p worth-query`, `cargo test -p worth-query --lib --no-run`, `TRYBUILD=overwrite cargo test -p worth-query --test aspect_native_query_compile_fail`, and normal `cargo test -p worth-query --test aspect_native_query_compile_fail` passed; the narrow residue scan finds the probe request alias only in the compile-fail fixture.

## Query preview execution terminal projection naming

- Removed the neutral `WORTHQueryPreviewExecutionEvidence::aspect_paths()` alias so preview execution evidence exposes native authority through `aspect_touches()` and dotted strings only through `terminal_aspect_paths_projection()`.
- Added a trybuild fixture proving public callers cannot recover preview execution path strings through the removed neutral alias; the compiler suggests `aspect_touches()`.
- Verification: `cargo fmt -p worth-query`, `cargo check -p worth-query`, `cargo test -p worth-query --lib --no-run`, `TRYBUILD=overwrite cargo test -p worth-query --test aspect_native_query_compile_fail`, and normal `cargo test -p worth-query --test aspect_native_query_compile_fail` passed; the broad neutral-alias scan confirms the removed `aspect_paths()` public aliases only remain in compile-fail fixtures.

## Query read touch row native emptiness

- Removed the neutral internal `WORTHQueryGraphTouchReadRow::aspect_paths()` alias.
- Runtime read-obligation dispatch now checks `aspect_touches().is_empty()` directly instead of projecting read touch rows to dotted strings to decide whether a read observes aspect paths.
- Verification: `cargo fmt -p worth-query`, `cargo check -p worth-query`, `cargo test -p worth-query --lib --no-run`, and a targeted scan proving read-obligation dispatch no longer calls the removed alias passed.

## Query write command touched path alias fence

- Removed the neutral `WORTHQueryWriteCommand::touched_aspect_paths()` method so touched mutation authority flows through `touched_aspects()`.
- Dotted strings remain available only through `terminal_touched_aspect_paths_projection()`.
- Added a trybuild fixture proving public callers cannot call the removed touched-path alias; the compiler suggests `touched_aspects()`.
- Verification: `cargo fmt -p worth-query`, `cargo check -p worth-query`, `cargo test -p worth-query --lib --no-run`, `TRYBUILD=overwrite cargo test -p worth-query --test aspect_native_query_compile_fail`, and normal `cargo test -p worth-query --test aspect_native_query_compile_fail` passed; a focused residue scan finds the alias only in the compile-fail fixture.

## Query retained refresh context touched path alias fence

- Removed the neutral `WORTHQueryRetainedRefreshContext::touched_aspect_paths()` method so retained refresh authority flows through `touched_aspects()`.
- Dotted strings remain available only through `terminal_touched_aspect_paths_projection()`, and the one computed test caller now uses that explicit projection vocabulary.
- Added a trybuild fixture proving public callers cannot call the removed retained-refresh touched-path alias; the compiler suggests `touched_aspects()`.
- Verification: `cargo fmt -p worth-query`, `cargo check -p worth-query`, `cargo test -p worth-query --lib --no-run`, `TRYBUILD=overwrite cargo test -p worth-query --test aspect_native_query_compile_fail`, and normal `cargo test -p worth-query --test aspect_native_query_compile_fail` passed; a focused residue scan finds the alias only in compile-fail fixtures.

## Query verified assumption asserted path alias fence

- Removed the neutral `WORTHQueryVerifiedAssumptionSet::asserted_aspect_paths()` method so verified assumption authority flows through `asserted_aspects()`.
- Dotted strings remain available only through `terminal_asserted_aspect_paths_projection()`, and the two bridge-backed verification execution assertions now use that explicit projection vocabulary.
- Added a trybuild fixture proving public callers cannot call the removed asserted-path alias; the compiler suggests `asserted_aspects()`.
- Verification: `cargo fmt -p worth-query`, `cargo check -p worth-query`, `cargo test -p worth-query --lib --no-run`, `TRYBUILD=overwrite cargo test -p worth-query --test aspect_native_query_compile_fail`, and normal `cargo test -p worth-query --test aspect_native_query_compile_fail` passed; a focused residue scan finds the alias only in the compile-fail fixture.

## Query batch write receipt touched path alias fence

- Removed the neutral `WORTHQueryBatchWriteReceipt::touched_aspect_paths()` method so batch receipt authority flows through `touched_aspects()`.
- Dotted strings remain available only through `terminal_touched_aspect_paths_projection()`, and batch receipt assertions/certification rows now use that explicit projection vocabulary.
- Added a trybuild fixture proving public callers cannot call the removed batch receipt touched-path alias; the compiler suggests `touched_aspects()`.
- Verification: `cargo fmt -p worth-query`, `cargo check -p worth-query`, `cargo test -p worth-query --lib --no-run`, `TRYBUILD=overwrite cargo test -p worth-query --test aspect_native_query_compile_fail`, and normal `cargo test -p worth-query --test aspect_native_query_compile_fail` passed; a focused residue scan finds receipt call sites no longer use the removed alias.

## Query batch write inspection touched path alias fence

- Removed the neutral `WORTHQueryBatchWriteReceiptInspection::touched_aspect_paths()` and `WORTHQueryBatchWriteComponentInspection::touched_aspect_paths()` methods so inspection authority flows through `touched_aspects()`.
- Dotted strings remain available only through `terminal_touched_aspect_paths_projection()`, and batch/graph-composition assertions now use that explicit projection vocabulary.
- Added trybuild fixtures proving public callers cannot call the removed inspection touched-path aliases; the compiler suggests `touched_aspects()`.
- Verification: `cargo fmt -p worth-query`, `cargo check -p worth-query`, `cargo test -p worth-query --lib --no-run`, `TRYBUILD=overwrite cargo test -p worth-query --test aspect_native_query_compile_fail`, and normal `cargo test -p worth-query --test aspect_native_query_compile_fail` passed; a focused residue scan finds the public aliases only in compile-fail fixtures.

## Query graph touch descriptor row internal path alias cleanup

- Removed the internal neutral `WORTHQueryGraphTouchDescriptorRow::touched_aspect_paths()` alias so graph touch descriptor row authority flows through `touched_aspects()`.
- Dotted strings remain available only through `terminal_touched_aspect_paths_projection()`.
- No public trybuild fixture was added because the row alias is crate-local implementation vocabulary; ordinary compilation enforces remaining internal use.
- Verification: `cargo fmt -p worth-query`, `cargo check -p worth-query`, and a focused residue scan passed; the removed neutral `touched_aspect_paths()` / `asserted_aspect_paths()` aliases now only appear in aspect-native compile-fail fixtures.

## Query effect payload neutral aspect alias fence

- Removed the neutral `WORTHQueryEffectPayload::input_aspects()`, `output_aspects()`, and `changed_aspects()` string accessors so effect payload authority flows through `input_aspect_touches()`, `output_aspect_touches()`, and `changed_aspect_touches()`.
- Added explicit terminal input/output aspect projection names; changed aspects already had `terminal_changed_aspects_projection()`.
- Added a trybuild fixture proving public callers cannot call the removed effect payload aspect aliases; the compiler suggests the native touch accessors.
- Verification: `cargo fmt -p worth-query`, `cargo check -p worth-query`, `TRYBUILD=overwrite cargo test -p worth-query --test aspect_native_query_compile_fail`, and normal `cargo test -p worth-query --test aspect_native_query_compile_fail` passed; a focused residue scan finds the removed neutral aliases only in the compile-fail fixture.

## Query derived view neutral aspect alias fence

- Removed the neutral `WORTHQueryDerivedView::dependency_aspects()` / `produced_aspects()` string accessors so derived declaration authority flows through `dependency_aspect_touches()` and `produced_aspect_touches()`.
- Added explicit terminal dependency/produced aspect projection names for reporting text.
- Removed the matching neutral `WORTHQueryComputedInspectionEvidence::dependency_aspects()` / `produced_aspects()` inspection aliases.
- Migrated preview routing and derived maintainer test helpers to keep `WORTHQueryAspectTouch` values instead of projecting strings and re-admitting them.
- Added trybuild fixtures proving public callers cannot call the removed derived declaration or computed inspection aliases.
- Verification: `cargo fmt -p worth-query`, `cargo check -p worth-query`, `cargo test -p worth-query --lib --no-run`, `TRYBUILD=overwrite cargo test -p worth-query --test aspect_native_query_compile_fail`, and normal `cargo test -p worth-query --test aspect_native_query_compile_fail` passed; a focused residue scan finds the removed neutral aliases only in compile-fail fixtures.

## Query effect inspection trigger aspect alias fence

- Converted `WORTHQueryEffectInspectionEvidence` trigger aspect storage from terminal strings to `WORTHQueryAspectTouch`.
- Removed the neutral `trigger_aspects()` accessor so effect inspection authority flows through `trigger_aspect_touches()`.
- Added `terminal_trigger_aspects_projection()` for reporting text, and preview relevance matching now makes that terminal comparison explicit.
- Added a trybuild fixture proving public callers cannot call the removed trigger-aspect alias; the compiler suggests `trigger_aspect_touches()`.
- Verification: `cargo fmt -p worth-query`, `cargo check -p worth-query`, `cargo test -p worth-query --lib --no-run`, `TRYBUILD=overwrite cargo test -p worth-query --test aspect_native_query_compile_fail`, and normal `cargo test -p worth-query --test aspect_native_query_compile_fail` passed.

## Query effect inspection condition aspect alias fence

- Converted `WORTHQueryEffectInspectionEvidence` condition input/output aspect storage from terminal strings to `WORTHQueryAspectTouch`.
- Removed the neutral `condition_inputs()` and `condition_outputs()` accessors so effect inspection authority flows through `condition_input_touches()` and `condition_output_touches()`.
- Added terminal condition input/output projection accessors for reporting text; effect inspection digests now project inside the digest boundary.
- Added a trybuild fixture proving public callers cannot call the removed condition aspect aliases; the compiler suggests the native touch accessors.
- Verification: `cargo fmt -p worth-query`, `cargo check -p worth-query`, `cargo test -p worth-query --lib --no-run`, `TRYBUILD=overwrite cargo test -p worth-query --test aspect_native_query_compile_fail`, and normal `cargo test -p worth-query --test aspect_native_query_compile_fail` passed.

## Query diagnostic aspect path alias fence

- Removed facade-visible diagnostic path aliases: `WORTHQueryExistingTruthAssertionDenial::asserted_aspect_path()`, `WORTHQueryExistingTruthProbeDenial::probed_aspect_path()`, `WORTHQueryExistingTruthProbeField::aspect_path()`, `WORTHQuerySymbolicAspectResolutionEvidence::aspect_path()`, and `WORTHQueryGraphCompositionResolutionEntry::aspect_path()`.
- Added explicit terminal projection names for those reporting/digest boundaries while preserving native/proof authority through existing touch-bearing accessors where available.
- Migrated graph composition evidence/digest helpers and existing-truth denial assertions to the terminal projection names.
- Added trybuild fixtures proving public callers cannot call the removed aliases.
- Verification: `cargo fmt -p worth-query`, `cargo check -p worth-query`, `cargo test -p worth-query --lib --no-run`, `TRYBUILD=overwrite cargo test -p worth-query --test aspect_native_query_compile_fail`, and normal `cargo test -p worth-query --test aspect_native_query_compile_fail` passed.

## Query graph-read proof-row key alias fence

- Removed neutral `aspect()` / `field()` string accessors from graph-read admitted proof rows and field authority rows that already carry foundational `AspectKey` / `FieldKey`.
- Added explicit terminal projection accessors while preserving native authority through `native_aspect_key()` and `native_field_key()`.
- Migrated boolean admission indexing to use native keys for admitted predicate rows; authoring request fields keep their ergonomic `aspect()` / `field()` vocabulary.
- Added aspect-native trybuild fixtures proving public callers cannot call the removed graph-read proof-row aliases.
- Verification: `cargo fmt -p worth-query`, `cargo check -p worth-query`, `cargo test -p worth-query --lib --no-run`, `TRYBUILD=overwrite cargo test -p worth-query --test aspect_native_query_compile_fail`, and normal `cargo test -p worth-query --test aspect_native_query_compile_fail` passed.

## Query retained scalar field key alias fence

- Removed neutral retained scalar field-key aliases: `WORTHQueryRetainedScalarFieldFact::field_key()`, `WORTHQueryRetainedScalarAlignmentFact::left_field_key()`, and `WORTHQueryRetainedScalarAlignmentFact::right_field_key()`.
- Retained scalar facts now keep native authority on `WORTHQueryRetainedFieldPath` accessors and expose strings only through explicit terminal projection accessors.
- Retained scalar fact-set and alignment digests now call terminal projection names at reporting/digest boundaries.
- Added aspect-native trybuild fixtures proving public callers cannot call the removed retained scalar field-key aliases.
- Verification: `cargo fmt -p worth-query`, `cargo check -p worth-query`, `cargo test -p worth-query --lib --no-run`, `TRYBUILD=overwrite cargo test -p worth-query --test aspect_native_query_compile_fail`, and normal `cargo test -p worth-query --test aspect_native_query_compile_fail` passed.

## Query aspect value path alias fence

- Removed the neutral `WORTHQueryAspectValue::aspect_path()` alias.
- Added `WORTHQueryAspectValue::aspect_touch()` for native authority and kept dotted strings behind `terminal_aspect_path_projection()`.
- Migrated memory-workspace lowering, mutation digest rows, public bridge support, and existing-truth test adapters to the terminal projection name only where they intentionally interact with digest text or legacy external rows.
- Added an aspect-native trybuild fixture proving public callers cannot call the removed aspect-value path alias.
- Verification: `cargo fmt -p worth-query`, `cargo check -p worth-query`, `cargo test -p worth-query --lib --no-run`, `TRYBUILD=overwrite cargo test -p worth-query --test aspect_native_query_compile_fail`, and normal `cargo test -p worth-query --test aspect_native_query_compile_fail` passed.

## Query aspect mutation carrier path alias fence

- Removed neutral `aspect_path()` aliases from `WORTHQueryAspectMutationOperation` and `WORTHQuerySymbolicAspectReference`.
- Added explicit `terminal_aspect_path_projection()` names and made `WORTHQuerySymbolicAspectReference::aspect_touch()` public so callers keep a native route after the string alias disappears.
- Migrated mutation admission identities, bridge writeback identity, write-receipt digests, batch digests, graph touch descriptor rows, obligation lookup keys, and mutation tests to terminal projection names only at digest/reporting/key projection boundaries.
- Tightened graph obligation selector matching to compare declared operation coverage through native `WORTHQueryAspectTouch` equality.
- Added aspect-native trybuild fixtures proving public callers cannot call the removed operation/reference path aliases.
- Verification: `cargo fmt -p worth-query`, `cargo check -p worth-query`, `TRYBUILD=overwrite cargo test -p worth-query --test aspect_native_query_compile_fail`, normal `cargo test -p worth-query --test aspect_native_query_compile_fail`, and `cargo test -p worth-query --lib --no-run` passed.

## Query entity aspect-key accessor native fence

- Changed `WORTHQueryEntity::aspect_value` to require a foundational `AspectKey` instead of raw `&str`.
- Changed `WORTHQueryEntity::aspect_values` to yield `(&AspectKey, &AspectValue)` instead of string-key tuples.
- Migrated projection-consumption field lowering to explicitly project the native key at the field-path lowering boundary, and updated memory workspace tests to build an `AspectKey`.
- Added an aspect-native trybuild fixture proving public callers cannot use the old string lookup or `(&str, &AspectValue)` iterator shape.
- Verification: `cargo fmt -p worth-query`, `cargo check -p worth-query`, `TRYBUILD=overwrite cargo test -p worth-query --test aspect_native_query_compile_fail`, normal `cargo test -p worth-query --test aspect_native_query_compile_fail`, and `cargo test -p worth-query --lib --no-run` passed.

## Query entity external field-path accessor native fence

- Changed `WORTHQueryEntity::external_scalar_value` to require a foundational `CanonicalFieldPath` instead of raw dotted `&str`.
- Removed the public `WORTHQueryEntity::external_aspect_value(&str)` alias.
- Migrated read-composition materialization, projection-consumption extraction, memory workspace tests, and consumer-kit backend behavior tests to construct native field paths before retained external projection lookup.
- Added an aspect-native trybuild fixture proving public callers cannot use the old dotted external scalar lookup or removed external aspect alias.
- Verification: `cargo fmt -p worth-query`, `cargo check -p worth-query`, `TRYBUILD=overwrite cargo test -p worth-query --test aspect_native_query_compile_fail`, normal `cargo test -p worth-query --test aspect_native_query_compile_fail`, and `cargo test -p worth-query --lib --no-run` passed.

## Query grouped artifact grouping-aspect alias fence

- Removed neutral `grouping_aspect()` aliases from grouped planning and grouped live proof artifacts that already carry foundational `AspectKey`.
- Added explicit `terminal_grouping_aspect_projection()` names while preserving native authority through `native_grouping_aspect_key()`.
- Migrated grouped baseline/execution code, view-shape tests, and milestone-eight certification harness reporting/projection sites to terminal projection names.
- Added aspect-native trybuild fixtures proving public callers cannot call the removed grouped planning/live aliases.
- Verification: `cargo fmt -p worth-query`, `cargo check -p worth-query`, `TRYBUILD=overwrite cargo test -p worth-query --test aspect_native_query_compile_fail`, normal `cargo test -p worth-query --test aspect_native_query_compile_fail`, and `cargo test -p worth-query --lib --no-run` passed.

## Query projection consumption fact alias fence

- Removed neutral `field_key()` aliases from `ProjectionFactRequest`, `BoundProjectionFactFamily`, and `ConsumedFieldValueFact`.
- Removed neutral `grouping_aspect()` aliases from `ConsumedMembershipFact` and grouped `ConsumedRelationEndpointFact`.
- Projection-consumption digest, eligibility, extraction, certification, and tests now use native proof carriers with explicit terminal projection only at digest/reporting/string compatibility boundaries.
- `ProjectionFactFieldPath` now exposes its validated `CanonicalFieldPath`, and live/read entity scalar extraction uses that typed path instead of reparsing dotted field text.
- Added aspect-native trybuild fixtures proving public callers cannot call the removed projection-consumption field/grouping aliases.
- Verification: `cargo fmt -p worth-query`, `cargo check -p worth-query`, `TRYBUILD=overwrite cargo test -p worth-query --test aspect_native_query_compile_fail`, normal `cargo test -p worth-query --test aspect_native_query_compile_fail`, and `cargo test -p worth-query --lib --no-run` passed.

## Query grouped binding proof alias fence

- Changed `QueryResultBindingProof` to store its source binding as an `AspectFieldKey` instead of separate source aspect/source field strings.
- Removed neutral `source_aspect()`, `source_field()`, and `field_key()` aliases from the grouped binding proof.
- Policy influence now consumes `source_field_key()` directly; digest/reporting call sites use `terminal_binding_aspect_projection()`.
- Added an aspect-native trybuild fixture proving public callers cannot call the removed grouped binding string aliases.
- Verification: `cargo fmt -p worth-query`, `cargo check -p worth-query`, `TRYBUILD=overwrite cargo test -p worth-query --test aspect_native_query_compile_fail`, normal `cargo test -p worth-query --test aspect_native_query_compile_fail`, and `cargo test -p worth-query --lib --no-run` passed.

## Query view-shape focus and delivery aspect alias fence

- Changed `ViewShapeDescriptor`, `ViewShapeDeliveryMetadata`, and `FocusedInspectorAspectPatchArtifact` to store focused inspector aspects as foundational `AspectKey` values instead of `String`.
- Removed neutral view-shape aspect aliases: `ViewShapeDescriptor::focused_aspect()`, `ViewShapeDescriptor::grouping_aspect()`, `ViewShapeDeliveryMetadata::focus_aspect()`, `ViewShapeDeliveryMetadata::grouping_aspect()`, and `FocusedInspectorAspectPatchArtifact::focus_aspect()`.
- Admission, planning, live execution, plan digesting, and view-shape tests now use native focus/grouping keys internally and terminal projection names only for digest/reporting strings.
- Added an aspect-native trybuild fixture proving public callers cannot call the removed view-shape aspect aliases.
- Verification: `cargo fmt -p worth-query`, `cargo check -p worth-query`, `TRYBUILD=overwrite cargo test -p worth-query --test aspect_native_query_compile_fail`, normal `cargo test -p worth-query --test aspect_native_query_compile_fail`, and `cargo test -p worth-query --lib --no-run` passed.

## Query validation proof-entry key alias fence

- Changed `ValidatedProjectionEntry`, `ValidatedResultShapeBinding`, `ValidatedPredicateEntry`, and `ValidatedOrderingEntry` to store foundational `AspectKey` / `FieldKey` values instead of authoring `AspectName` / `FieldName` pairs.
- Removed neutral validated-entry string aliases: `aspect()`, `field()`, `source_aspect()`, and `source_field()`.
- Collection planning, live relevance contracts, read scope classification, grouped view planning, and the validation certification harness now use native validation keys internally and terminal projection names only for legacy string-shaped constructors or digest/reporting text.
- Added an aspect-native trybuild fixture proving public callers cannot call the removed validated-entry string aliases.
- Verification: `cargo fmt -p worth-query`, `cargo check -p worth-query`, `TRYBUILD=overwrite cargo test -p worth-query --test aspect_native_query_compile_fail`, normal `cargo test -p worth-query --test aspect_native_query_compile_fail`, and `cargo test -p worth-query --lib --no-run` passed.

## Query collection ordering key-path alias fence

- Changed `OrderingKeyPath` to store foundational `AspectKey` / `FieldKey` values instead of neutral ordering aspect/field strings.
- Removed neutral public `OrderingKeyPath::aspect()` and `OrderingKeyPath::field()` aliases.
- Collection ordering digesting now uses explicit terminal projection accessors, while collection planning converts validated ordering entries into native keys at the planning boundary.
- Added an aspect-native trybuild fixture proving public callers cannot call the removed collection ordering key-path aliases.
- Verification: `cargo fmt -p worth-query`, `cargo check -p worth-query`, `TRYBUILD=overwrite cargo test -p worth-query --test aspect_native_query_compile_fail`, normal `cargo test -p worth-query --test aspect_native_query_compile_fail`, and `cargo test -p worth-query --lib --no-run` passed.

## Query live query field-key alias fence

- Changed `QueryFieldKey` to store foundational `AspectKey` / `FieldKey` values instead of neutral live query aspect/field strings.
- Removed neutral public `QueryFieldKey::aspect()` and `QueryFieldKey::field()` aliases.
- Live relevance matching still accepts bridge field deltas at the terminal ingress boundary, while live proof artifacts, detail patch digesting, and view-shape focus filtering now consume native query field keys internally.
- Added an aspect-native trybuild fixture proving public callers cannot call the removed live query field-key aliases.
- Verification: `cargo fmt -p worth-query`, `cargo check -p worth-query`, `TRYBUILD=overwrite cargo test -p worth-query --test aspect_native_query_compile_fail`, normal `cargo test -p worth-query --test aspect_native_query_compile_fail`, and `cargo test -p worth-query --lib --no-run` passed.

## Query authorized projection field-bag native fence

- Added `AuthorizedProjectionFieldPath` as the typed visible/masked/delivery/projection-consumption field carrier with foundational `AspectKey` / `FieldKey` storage and explicit terminal projection text.
- Replaced authorized projection, masked projection, policy delivery, optimizer input, and projection-consumption binding `Vec<String>` field bags with typed field paths.
- Removed neutral public string-bag accessors (`visible_fields`, `masked_fields`, `non_disclosing_fields`, `delivered_fields`, `authorized_visible_fields`) and added/used `*_field_paths` accessors instead.
- Migrated certification fixtures, policy-plan counts, live relevance comparisons, projection-consumption fixtures, and public bridge/entity tests to construct typed field paths before satisfying proof-bearing APIs.
- Added an aspect-native trybuild fixture proving public callers cannot call the removed authorized projection field-bag aliases.
- Verification: `cargo fmt -p worth-query`, `cargo check -p worth-query`, `cargo check -p worth-query --tests`, normal `cargo test -p worth-query --test aspect_native_query_compile_fail`, `cargo test -p worth-query --lib --no-run`, and focused alias scans passed.

## Query runtime derived materialization result-boundary alias fence

- Removed `WORTHQueryRuntime::read_derived(...)` so runtime callers must use the explicit `read_derived_result(...)` boundary and handle or acknowledge `WORTHQueryRuntimeError`.
- Migrated runtime computed/effect/intent/preview tests to `read_derived_result(...).expect(...)` or a local test helper over that result API.
- Preserved the native retained materialization carrier: `read_derived_result(...)` still returns `WORTHQueryDerivedMaterializationResult`, not terminal JSON rows.
- Added an aspect-native trybuild fixture proving public callers cannot call the removed unchecked runtime derived materialization alias; the compiler suggests `read_derived_result`.
- Verification: `cargo fmt -p worth-query`, `cargo check -p worth-query`, `cargo check -p worth-query --tests`, normal `cargo test -p worth-query --test aspect_native_query_compile_fail`, `cargo test -p worth-query --lib --no-run`, and focused alias scans passed.

## Query workspace materialization result-boundary alias fence

- Removed `WORTHQueryWorkspace::materialize(...)` so workspace callers must use `materialize_result(...)` or the fuller `materialize_intent(...)` proof ladder.
- Migrated aspect API certification rows and runtime tests from the deleted shortcut to `materialize_result(...).expect(...)`.
- Updated intent-admission surface inventory strings, doc-example certification, runtime API closeout wording, and current Query docs so they no longer teach `workspace.materialize(...)`.
- Preserved the native retained materialization carrier: `materialize_result(...)` still returns `WORTHQueryDerivedMaterializationResult`, not terminal JSON rows.
- Added an aspect-native trybuild fixture proving public callers cannot call the removed unchecked workspace materialization alias.
- Verification: `cargo fmt -p worth-query`, `cargo check -p worth-query`, `cargo check -p worth-query --tests`, normal `cargo test -p worth-query --test aspect_native_query_compile_fail`, `cargo test -p worth-query --lib --no-run`, and focused alias scans passed.

## Query existing-truth probe field lookup native fence

- Removed `WORTHQueryExistingTruthProbe::field(&str)` so probe result consumers cannot reparse raw dotted aspect paths at the verified existing-truth boundary.
- Runtime and public bridge probe assertions now use `field_for_touch(&WORTHQueryAspectTouch)`, keeping Law 41 proof-state typing alive from request construction through field lookup.
- Added an aspect-native trybuild fixture proving public callers cannot call the removed raw-string probe field alias.
- Verification: `cargo fmt -p worth-query`, `cargo check -p worth-query`, `cargo check -p worth-query --tests`, normal `cargo test -p worth-query --test aspect_native_query_compile_fail`, focused runtime tests for existing-truth probe and public bridge bootstrap lookup, and focused alias scans passed.
- Added the three touched pre-existing over-400-line verified-existing mutation test files to `scripts/ci/workspace_rust_line_cap_allowlist.txt`. Running the line-cap guard through Git Bash still failed on broader pre-existing unallowlisted files and a deleted tracked projection-consumption file from the dirty worktree, so it is not a green verification signal yet.

## Query live relevance authorized field native fence

- Changed `PolicyAwareLiveRelevanceContract` to retain `AuthorizedProjectionFieldPath` values after admission instead of a `Vec<String>` authorized field bag.
- Removed the public `authorized_fields()` alias; callers must use `authorized_field_paths()` and explicitly project terminal text for reporting/assertions.
- `admit_policy_aware_live_plan` still accepts requested relevance fields as terminal ingress strings, but converts admitted matches into typed authorized field paths immediately.
- Added an aspect-native trybuild fixture proving public callers cannot call the removed live relevance string-bag alias.
- Verification: `cargo fmt -p worth-query`, `cargo check -p worth-query`, `cargo check -p worth-query --tests`, normal `cargo test -p worth-query --test aspect_native_query_compile_fail`, focused `cargo test -p worth-query policy_live`, and focused alias scans passed.

## Query placeholder masking terminal request naming fence

- Removed neutral `requested_placeholder_fields()` aliases from `PolicyPlaceholderMaskingRequest` and `PolicyPlaceholderMaskingDenial`.
- Kept placeholder masking as terminal request ingress, but renamed access to `terminal_requested_placeholder_fields_projection()` so callers cannot treat the string bag as native authority.
- Masking checks still compare terminal requested text against typed masked `AuthorizedProjectionFieldPath` values from the narrowed authorized projection.
- Added an aspect-native trybuild fixture proving public callers cannot call the removed placeholder request/denial string-bag aliases.
- Verification: `cargo fmt -p worth-query`, `cargo check -p worth-query`, `cargo check -p worth-query --tests`, normal `cargo test -p worth-query --test aspect_native_query_compile_fail`, focused `cargo test -p worth-query policy_delivery`, and focused alias scans passed.

## Query retained scalar fact-set lookup native fence

- Removed `WORTHQueryRetainedScalarFactSet::field_value(&str)` so public callers cannot reparse raw retained field path text at lookup time.
- Retained scalar fact-set lookup now requires `WORTHQueryRetainedFieldPath` through `field_value_at(...)`, preserving the Law 41 proof-state carrier from retained field admission into fact consumption.
- Migrated the nested retained scalar fact-set test to construct typed retained field paths before lookup.
- Added an aspect-native trybuild fixture proving public callers cannot call the removed raw-string fact-set lookup alias.
- Verification: `cargo fmt -p worth-query`, `cargo check -p worth-query`, `cargo check -p worth-query --tests`, normal `cargo test -p worth-query --test aspect_native_query_compile_fail`, focused `cargo test -p worth-query retained_scalar_fact_set_reads_nested_fields`, line-count checks, and focused alias scans passed.

## Query artifact bundle target containment native fence

- Removed raw `includes_view_name(&str)` aliases from derived materialization and live artifact bundles.
- Added `includes_target(...)` containment checks over `WORTHQueryDerivedMaterializationTarget` and `WORTHQueryLiveArtifactTarget`, and changed binding validation to sort/deduplicate target carriers before projecting terminal view names for reports/digests.
- Updated computed runtime assertions to check derived bundle containment through a target carrier.
- Added an aspect-native trybuild fixture proving public callers cannot call the removed raw-string bundle containment aliases.
- Verification: `cargo fmt -p worth-query`, `cargo check -p worth-query`, `cargo check -p worth-query --tests`, normal `cargo test -p worth-query --test aspect_native_query_compile_fail`, line-count checks, and focused alias scans passed.
