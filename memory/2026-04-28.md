# 2026-04-28

- QA loop on Forge Signal Milestone C Phase 2C found two real issues in the retry-budget start. First, `Request` retry-budget scope was keyed by `request_id`, but retries mint a fresh request id while preserving generation, so the budget reset every retry attempt instead of spanning one logical request lineage. Fixed by moving retry-budget bookkeeping into a dedicated runtime helper at `logic/transaction/runtime/state/resource_retry_budget.rs` and keying request-scope spend by `ResourceGeneration`. Added focused coverage proving request budgets now accumulate across a retry lineage while remaining isolated across distinct lineages.
- The same QA pass surfaced that admitted retries were not receiving fresh timeout wakes, which meant timeout policy silently disappeared after the first retry attempt. Fixed the `SignalRuntime::admit_scheduled_resource_retry` path to schedule and attach a new timeout wake the same way fresh request and revalidation admissions do, then reverified retry lineage and timeout-wake truth together.
- Reverification after QA fixes passed with `cargo fmt -p forge-signal`, focused retry tests, `resource_compile_fail_boundaries_hold`, `cargo check -p forge-signal --tests`, full `cargo test -p forge-signal` (738 passed, 23 ignored), and `git diff --check` (line-ending warnings only). The production god-file pressure from this slice improved slightly: retry budget bookkeeping now lives in its own 85-line helper instead of adding more responsibility to `runtime/state/resource.rs`, though broader legacy test-file size remains a preexisting concern outside this focused fix.
- Implementation batch for Milestone C Phase 3B landed scoped timeout semantics instead of treating timeout as one undifferentiated duration. Added `PerAttemptTimeout` and `TotalRequestLifetimeTimeout` declarations, a new built-in total-lifetime timeout descriptor, and lineage-aware timeout lowering so timeout scope changes descriptor truth, parameter digests, and lowered bundle digests even when the raw duration stays the same.
- Runtime admission now preserves `generation_started_tick` on in-flight requests and uses it when scheduling timeout wakes across first attempt, revalidation, and retry admission. `TotalRequestLifetimeTimeout` computes remaining lineage budget from that start tick; once the lifetime window is exhausted, timeout-triggered retry is denied explicitly as `RetryTimeoutWindowExhausted` instead of pretending a zero-duration timeout is valid.
- Verification for the timeout-scope batch passed with `cargo fmt -p forge-signal`, focused `resource_policy`, `resource_retry`, `resource_timeout`, the new `resource_total_request_lifetime_timeout_denies_timeout_triggered_retry_after_lineage_deadline` scenario, `resource_compile_fail_boundaries_hold`, `cargo check -p forge-signal --tests`, full `cargo test -p forge-signal` (739 passed, 23 ignored), and `git diff --check` with only existing LF/CRLF warnings.
- Implementation batch for the next Milestone C Phase 3 slice landed explicit timeout outcome classification and proof-bearing heartbeat extension. Added `ProgressHeartbeatExtension`, `TerminalTimeout`, and `RevalidationEligibleTimeout` declarations and built-in descriptors, extended timeout decision plans with heartbeat duration plus outcome class, and made those choices affect parameter digests, semantic descriptor identity, and timeout artifacts rather than hiding them behind one fixed-timeout bucket.
- Runtime now exposes `extend_resource_timeout_heartbeat(...)` as a typed boundary that denies before wake allocation when the request is stale, terminal, missing a timeout wake, or not using a heartbeat-extendable timeout policy. Successful extension supersedes the prior timeout wake, attaches the new one, records `ExtendedResourceTimeoutHeartbeat`, and increments explicit heartbeat-extension telemetry instead of smuggling the work through ordinary timeout scheduling.
- Timeout artifacts now retain terminal vs revalidation-eligible outcome truth directly on `TimedOutResourceRequest`, and compile-fail coverage was expanded with `resource_timeout_heartbeat_extension_report_fields_are_private`. Reverification passed with `cargo fmt -p forge-signal`, focused `resource_timeout`, `resource_policy`, `resource_retry`, `resource_compile_fail_boundaries_hold`, `cargo check -p forge-signal --tests`, full `cargo test -p forge-signal` (742 passed, 23 ignored), and `git diff --check` with only the existing LF/CRLF warnings.
- Implementation batch for the next Milestone C Phase 3 slice landed inherited deadline authority for resource timeout admission. Added `TransactionInheritedDeadline` and `RuntimeInheritedDeadline` timeout declarations and built-ins, threaded transaction/runtime deadline intent through request and revalidation admission, and resolved inherited deadlines at admission time into explicit timeout authority evidence instead of pretending the timeout plan alone was sufficient.
- Runtime timeout admission now stores resolved timeout truth on `InFlightResourceRequest`: duration, absolute due tick, outcome class, deadline authority, and resolved decision digest. Timeouts now retain `ResourceTimeoutDeadlineAuthority` on `TimedOutResourceRequest`, inherited-deadline admissions increment dedicated telemetry, and retry/revalidation admission reuses resolved timeout evidence rather than rebuilding abstract timeout truth after the fact.
- Finishing this slice also forced a healthy ownership cleanup: `InFlightResourceRequest` is no longer `Copy`, its accessors now borrow instead of consuming, retry-budget helpers take shared references, and timeout artifact tests now assert against resolved timeout admission digests rather than stale plan-only digests. Compile-fail evidence for `TimedOutResourceRequest` was updated to include the new private `deadline_authority` field and constructor arity.
- Reverification for the inherited-deadline slice passed with `cargo fmt -p forge-signal`, focused `resource_timeout`, `resource_policy`, `resource_retry`, `resource_compile_fail_boundaries_hold`, `cargo check -p forge-signal --tests`, full `cargo test -p forge-signal` (745 passed, 23 ignored), and `git diff --check` with only existing LF/CRLF warnings.
- Strict QA on the inherited-deadline slice found one real production-grade defect: when a retry inherited a preserved deadline whose absolute due tick was already exhausted by the time backoff became ready, retry admission silently succeeded with no timeout wake instead of denying explicitly. That violated the Milestone C rule that replay-relevant policy truth must deny explicitly rather than degrade into adapter folklore or policy disappearance.
- Fixed the defect by making retry-timeout admission distinguish descriptor-disabled from inherited-deadline-exhausted, then denying retry admission as `RetryTimeoutWindowExhausted` at the runtime boundary before any new request becomes active. Added focused coverage proving an inherited-deadline retry whose backoff outlives the preserved deadline is denied, leaves no active in-flight node, retains the original timed-out request for audit, and increments the timeout-window denial counter.
- Reverification after the QA correction passed with `cargo fmt -p forge-signal`, the new focused inherited-deadline retry test, focused `resource_timeout`, `resource_retry`, `resource_policy`, `resource_compile_fail_boundaries_hold`, `cargo check -p forge-signal --tests`, full `cargo test -p forge-signal` (746 passed, 23 ignored), and `git diff --check` with only existing LF/CRLF warnings.
- Implementation batch for the next Milestone C Phase 4 slice landed lowered cancellation and supersession decision plans instead of treating those policies as raw declaration enums. Added `policy/cancellation.rs` and `policy/supersession.rs`, lowered `RuntimeDenialOnly` vs `BestEffortHostSignalAndRuntimeDenial` cancellation semantics plus `NewGenerationSupersedesPrior` supersession semantics into `LoweredResourceDescriptor`, and exposed sealed decision-plan accessors through the resource facade.
- Runtime cancellation now records two distinct truths: authoritative runtime-hard cancellation and optional non-authoritative host-advisory evidence. `CancelledResourceRequest` retains the cancellation decision digest plus optional `ResourceHostCancellationAdvisory`, while `ResourceSupersessionRecord` now retains a supersession decision digest so later replay/inspection can tell why a request was displaced instead of inferring policy from lifecycle shape alone.
- Telemetry now counts cancellation and supersession policy decisions explicitly, and splits runtime-hard cancellation from host-advisory emission so later closeout can prove we are not conflating “asked the host nicely” with “runtime state is already cancelled.” The runtime tests now cover both advisory and denial-only cancellation semantics, and supersession evidence now proves a policy decision digest was retained when a new generation superseded an older active request.
- Reverification for the cancellation/supersession slice passed with `cargo fmt -p forge-signal`, focused descriptor/cancellation/runtime tests, `resource_policy`, `resource_retry`, `resource_timeout`, `resource_compile_fail_boundaries_hold`, `cargo check -p forge-signal --tests`, full `cargo test -p forge-signal` (747 passed, 23 ignored), and `git diff --check` with only existing LF/CRLF warnings.
- Implementation batch for the next Milestone C Phase 4 slice landed explicit overlap-admission and old-host-work posture on supersession policy instead of keeping every supersession as one undifferentiated “newer wins” shape. Added built-in overlap declarations for `OverlappingGenerationRetainsOldHostWork` and `OverlappingGenerationCancelsOldHostWork`, expanded `ResourceSupersessionDecisionPlan` with overlap disposition plus old-host-work posture, and bound both into the supersession decision digest so replay-visible policy meaning changes when overlap/advisory behavior changes.
- Runtime supersession now emits explicit `ResourceOverlappingGenerationAdmission` evidence when a policy admits overlap, and that artifact can optionally carry `ResourceOldHostWorkCancellationAdvisory` when the policy requests best-effort cancellation of old host work. Importantly, the runtime still marks the older request superseded immediately, so host work may overlap externally while late completion remains non-authoritative and still denies as superseded inside `forge-signal`.
- Telemetry now separates overlap admission, old host work retained, and old host work advisory-cancelled lanes from generic supersession counts. Focused runtime coverage now proves both overlap-with-retained-host-work and overlap-with-advisory-cancel surfaces, and the retained-host-work test explicitly confirms a late completion from the older overlapping generation is still denied as `Superseded`.
- Reverification for the overlap/old-host-work supersession slice passed with `cargo fmt -p forge-signal`, focused `resource_policy`, `resource_supersession`, overlap-specific runtime tests, `resource_compile_fail_boundaries_hold`, `cargo check -p forge-signal --tests`, full `cargo test -p forge-signal` (750 passed, 23 ignored), and `git diff --check` with only existing LF/CRLF warnings.
- QA loop on the current Milestone C Phase 4 test surface found two meaningful weaknesses in the tests rather than the runtime code. First, the overlap advisory-cancel test was mostly artifact-shape checking: it proved an advisory existed, but did not prove the hostile condition that old host work finishing anyway still loses to runtime supersession authority. Second, the test surface did not yet prove that superseded completion denial survives the replay/restore surface as stable retained evidence, which the Phase 4 spec names explicitly.
- Tightened `resource_overlap_supersession_can_request_old_host_work_advisory_cancel` so it now proves distinct request/generation identity, retained superseded loser state, active winner state, and late-completion denial even when the overlap policy requested old-host-work advisory cancellation. Added `resource_overlap_supersession_replay_retains_superseded_denial_evidence_after_restore` so replay reconstruction after snapshot restore must preserve the retained superseded-denial digest instead of only preserving object shapes in memory.
- Reverified the strengthened adversarial tests with focused overlap/cancellation filters and then full `cargo test -p forge-signal` (751 passed, 23 ignored). `git diff --check` remained clean aside from the existing LF/CRLF warnings.
- Implementation batch for the next Milestone C Phase 4 slice landed cancellation grace-window evidence plus declared dependent-cancellation propagation. `ResourceNodeDeclaration` now carries optional `cancellation_grace_period` and `declared_dependent_cancellation_nodes`, those values are bound into the lowered cancellation decision plan and digest, and runtime cancellation now records `ResourceCancellationGraceWindow` plus `ResourceDependentCancellationPropagation` artifacts instead of keeping those semantics implicit.
- Runtime cancellation now recursively propagates only across the declared dependent footprint of active requests. Each affected request is still cancelled authoritatively in runtime state immediately, but retains its own policy decision digest, optional host-advisory evidence, and optional grace window. Telemetry now counts cancellation grace windows and dependent-propagation cancellations explicitly, and the top-level cancellation performance envelope uses the full cancelled footprint instead of pretending cancellation is always scalar.
- Hardened the proof surface too: added focused runtime tests proving declared grace windows are retained, recursive propagation cancels child and grandchild while leaving an undeclared sibling active, and the cancellation boundary reports honest admitted width. Added a new trybuild privacy fixture for `ResourceDependentCancellationPropagation`, refreshed the cancellation report fixture to cover the new field, and tightened the cancellation decision-plan/cancelled-request compile-fail fixtures so new private fields are explicitly part of the boundary proof instead of only surfacing as omitted-field notes.
- Reverification for the Phase 4 cancellation-footprint slice passed with `cargo fmt -p forge-signal`, focused `resource_cancellation` tests, `resource_compile_fail_boundaries_hold`, `cargo check -p forge-signal --tests`, full `cargo test -p forge-signal` (753 passed, 23 ignored), and `git diff --check` with only existing LF/CRLF warnings.
- Strict QA on the new cancellation-footprint slice found one real defect: dependent cancellation propagation was mutating child/grandchild resource state, but the runtime wrapper only retired the root request's timeout wake. That left propagated dependents with live scheduled timeout wakes after they were already cancelled, which is a hidden temporal-frontier leak and violates the milestone's cost-honesty/authority requirements.
- Fixed the defect by teaching `ResourceRuntimeState` to collect the full active cancellation footprint's timeout wakes before the resource-layer cancellation mutation runs, then retire each wake with `TemporalWakeRetirementReason::Cancelled`. Added focused coverage proving a parent cancellation that propagates to a child also retires both timeout wakes, leaves no scheduled/ready wakes behind, and retains both requests as cancelled history.
- Reverification after the QA correction passed with `cargo fmt -p forge-signal`, focused dependent-cancellation and resource-cancellation tests, `resource_compile_fail_boundaries_hold`, `cargo check -p forge-signal --tests`, full `cargo test -p forge-signal` (754 passed, 23 ignored), and `git diff --check` with only existing LF/CRLF warnings.
- Implementation batch for the next Milestone C Phase 4 slice landed explicit intent-equivalence coalescing on supersession policy. Added `ResourceSupersessionPolicyDeclaration::IntentEquivalentCoalescesToActive`, the corresponding built-in semantic name `signal.resource.supersession.intent-equivalent-coalesces-to-active`, and a lowered supersession decision-plan class that can admit a new logical request without allocating a new active generation when the incoming request intent is semantically identical to the current active request.
- Request admission now computes and preserves a canonical `ResourceRequestIntentDigest` on each in-flight request. When supersession policy permits coalescing and the incoming intent digest matches the active pending request for the same node, runtime keeps the existing winner active, records a fresh coalesced loser request as superseded history, emits a sealed `ResourceIntentEquivalenceCoalescing` artifact with winner/loser handles plus policy digest, and still denies any later loser completion as `Superseded`.
- The proof surface grew with runtime tests for winner/loser lineage preservation and late-completion denial, plus a new trybuild fixture proving `ResourceIntentEquivalenceCoalescing` fields remain private. While closing the batch, the full crate pass caught one honest regression: the built-in policy registry freeze-evidence test still hard-coded the old descriptor count. Fixed that test to bind against the registry's actual descriptor count instead of drifting magic numbers.
- Reverification for the coalescing slice passed with `cargo fmt -p forge-signal`, focused `resource_intent_equivalence_coalescing`, `resource_supersession`, `resource_compile_fail_boundaries_hold`, `cargo check -p forge-signal --tests`, full `cargo test -p forge-signal` (756 passed, 23 ignored), and `git diff --check` with only the existing LF/CRLF warnings.
- Implementation batch for the next Milestone C Phase 5 slice landed the first real revalidation proof lane instead of keeping revalidation as an `expected_active` convenience check. Added `policy/revalidation.rs`, the built-in `ResourceRevalidationPolicyDeclaration::ExplicitOrActiveHandleForced`, lowered `ResourceRevalidationDecisionPlan`, and threaded that decision plan through `LoweredResourceDescriptor` plus facade exports so revalidation now has its own descriptor-backed decision artifact rather than piggybacking on generic request admission shape.
- Runtime now mints sealed `ActiveResourceRevalidationProof` artifacts for active pending handles and exposes a dedicated `force_revalidate_resource_node(...)` path. Forced revalidation is policy-gated, validates the proof against current active runtime truth, supersedes exactly the proven active handle when admitted, denies stale/forged proofs as `ActiveHandleProofMismatch`, and denies force under explicit-only policy as `ForcedRevalidationPolicyDisabled`. The admitted revalidation artifact now retains explicit decision digest plus whether force consumed a proven active handle.
- This slice also tightened a nearby honesty gap while we were already in the boundary: revalidation admission performance now reports timeout-wake footprint instead of always pretending revalidation had zero temporal coordination cost when it scheduled a timeout wake. Telemetry grew explicit revalidation policy-decision, forced-revalidation, proof-check, policy-denial, and proof-mismatch counters.
- Proof coverage expanded with focused runtime tests for forced revalidation success, policy-disabled denial, stale-proof denial after supersession, and retry-vs-revalidation artifact separation, plus new compile-fail fixtures for `ActiveResourceRevalidationProof` privacy/non-deserializability and `ResourceRevalidationDecisionPlan` privacy. Reverification passed with `cargo fmt -p forge-signal`, focused forced revalidation and distinction tests, `resource_policy`, `resource_compile_fail_boundaries_hold`, `cargo check -p forge-signal --tests`, full `cargo test -p forge-signal` (760 passed, 23 ignored), and `git diff --check` with only existing LF/CRLF warnings.
- Implementation batch for the next Milestone C Phase 5 slice landed stale-after revalidation as a real runtime lane instead of leaving stale-after as a declaration-only hint. Added `policy/stale_after.rs`, lowered `ResourceStaleAfterDecisionPlan`, extended revalidation built-ins with `ExplicitOrStaleAfterFulfilled` and `ExplicitOrStaleAfterFulfilledOrActiveHandleForced`, and threaded stale-after decision truth through `LoweredResourceDescriptor`, facade exports, registry built-ins, and proof fixtures.
- Runtime now keeps explicit `stale_after_wake_by_node` ownership in `ResourceRuntimeState`, schedules stale-after wakes only after fulfilled completion commit when both the stale-after plan is enabled and the revalidation plan permits stale-after admission, and retires those wakes deliberately on fresh request admission, retry admission, explicit revalidation, and forced revalidation so stale temporal truth cannot linger after a node becomes active again.
- Added `active_resource_stale_after_wake_for_node(...)` plus `admit_stale_after_resource_revalidation(...)` to the runtime boundary. Stale-after revalidation now validates policy, wake identity, and fulfilled-only lifecycle truth explicitly, consumes the matched ready wake, clears the node-level stale-after proof, and admits a fresh revalidation generation with the consumed `ReadyTemporalWake` retained on `AdmittedResourceRevalidation`. Denials are now typed as `StaleAfterRevalidationPolicyDisabled`, `StaleAfterWakeMismatch`, and `StaleAfterRequiresFulfilledLifecycle`, with dedicated telemetry counters instead of collapsing everything into generic revalidation mismatch.
- Hardened the adversarial test surface too: added focused tests proving fulfilled completion schedules a stale-after wake, ready stale-after wake admits revalidation and carries retained wake evidence, policy-disabled stale-after revalidation denies even when given a stray ready wake, fulfilled-only gating denies stale-after revalidation before completion, and a fresh explicit request retires the stale-after wake before it can fire. Added a new trybuild privacy fixture for `ResourceStaleAfterDecisionPlan`, extended the non-deserializable proof-artifact fixture to cover it, and made the built-in registry freeze-evidence count bind to actual registration width rather than a magic number.
- Reverification for the stale-after slice passed with `cargo fmt -p forge-signal`, focused `resource_policy`, `stale_after`, `resource_compile_fail_boundaries_hold`, `cargo check -p forge-signal --tests`, full `cargo test -p forge-signal` (764 passed, 23 ignored), and `git diff --check` with only the existing LF/CRLF warnings.

- QA loop on the stale-after revalidation slice found two real issues. First, stale-after denial attempts were not incrementing
esource_revalidation_policy_decision_count, which under-reported control-plane work and violated the boundary-cost honesty expected by the performance laws. Fixed SignalRuntime::admit_stale_after_resource_revalidation(...) so the policy decision counter increments before validation and denial, then strengthened the denial test to prove the counter advances even when the policy rejects the wake.
- Second, the slice was missing the exact replay-strength proof row the Phase 5 spec calls for: stale-after revalidation after temporal restore. Added
esource_stale_after_revalidation_survives_snapshot_restore_with_same_ready_wake_truth, which proves a fulfilled node's stale-after wake survives snapshot/restore, promotes with the same wake identity, and still admits revalidation correctly after restore instead of relying on in-memory-only behavior.
- Reverification after the QA corrections passed with focused stale-after tests, the new restore test, cargo fmt -p forge-signal, full cargo test -p forge-signal (765 passed, 23 ignored), and git diff --check with only the existing LF/CRLF warnings.
- Implementation batch for the next Milestone C Phase 5 slice completed dependency-change revalidation as a real proof-gated runtime lane. The declaration and digest vocabulary for `ExplicitOrDependencyChange` / `ExplicitOrDependencyChangeOrActiveHandleForced` was already present, but runtime mint/admit behavior was still missing. Finished that lane by wiring `prove_dependency_change_resource_revalidation(...)` to mint sealed proof from actual runtime invalidation truth and `revalidate_resource_node_for_dependency_change(...)` to consume that proof at the boundary.
- The proof is intentionally narrow and honest: `DependencyChangeResourceRevalidationProof` now retains the resource node id, exact runtime `NodeState` (`Dirty` or `MaybeStale` only), and the revalidation decision digest. Admission rechecks all three before any mutation. If the node is clean, policy-disabled, or the proof's state/digest no longer matches current descriptor truth, the boundary now denies explicitly as `DependencyChangeRevalidationPolicyDisabled` or `DependencyChangeProofMismatch` instead of quietly rediscovering freshness inside the hot path.
- Dependency-change revalidation still obeys the active-request overwrite law. The new proof does not grant supersession authority by itself: if the node already has an active request, admission still denies through the normal `ActiveRequestRequiresExpectedHandle` path. When admitted, the revalidation artifact retains the consumed dependency-change proof directly on `AdmittedResourceRevalidation`, resolves timeout truth, and retires any stale-after wake so later inspection can tell exactly why the new generation exists.
- Hardened the proof boundary too: added hostile runtime tests proving invalidated-node admission, forged state-mismatch denial, and active-request overwrite denial; added trybuild fixtures for dependency-change proof privacy and non-deserializability; refreshed `resource_revalidation_decision_plan_fields_are_private.stderr` for the new private field; and extended `resource_policy_proof_artifacts_cannot_be_deserialized` so dependency-change proof joins the non-forgeable proof suite.
- Verification for the dependency-change batch passed with `cargo fmt -p forge-signal`, `cargo check -p forge-signal --tests`, focused dependency-change tests plus focused `resource_policy`, `stale_after`, and `forced_revalidation` coverage, `cargo test -p forge-signal resource_compile_fail_boundaries_hold -- --nocapture`, full `cargo test -p forge-signal` (768 passed, 23 ignored), and `git diff --check` with only the existing LF/CRLF warnings.
- Implementation batch for the next Milestone C Phase 5 slice completed observer-demand revalidation as a proof-gated runtime lane that consumes committed observation truth instead of rediscovering observation semantics inside the hot path. Added the built-in revalidation families `ExplicitOrObserverDemand` and `ExplicitOrObserverDemandOrActiveHandleForced`, threaded `permits_observer_demand_revalidation` through `ResourceRevalidationDecisionPlan`, expanded the built-in policy registry rows, and introduced a sealed `ObserverDemandResourceRevalidationProof` artifact retained on admitted revalidation reports.
- Runtime now exposes `prove_observer_demand_resource_revalidation(...)` and `revalidate_resource_node_for_observer_demand(...)`. Proof minting only succeeds for live declared resource nodes whose revalidation plan permits observer-demand and whose latest observation boundary contains a committed delivered event matching the node. The proof binds resource node id, observer id, handle id, an observation digest derived from the committed boundary event, and the revalidation decision digest. Admission rechecks both descriptor policy truth and the latest committed observation event before mutation, then resolves timeout truth, retires stale-after wake state, and records observer-demand admission telemetry explicitly.
- Observer-demand denials are now explicit and measurable: `ObserverDemandRevalidationPolicyDisabled` and `ObserverDemandProofMismatch` were added to the public denial taxonomy, telemetry now counts observer-demand admissions, proof checks, policy denials, and proof mismatches, and the generic revalidation denial accounting maps those classes into dedicated counters instead of burying them in generic mismatch lanes.
- Hardened the hostile proof/test surface too. Added runtime tests proving committed observation admits observer-demand revalidation, rollback-suppressed observation cannot mint a proof, forged observer/handle/digest proof denies as `ObserverDemandProofMismatch`, and observer-demand still does not bypass the active-request overwrite rule. Added new trybuild fixtures for observer-demand proof privacy and non-deserializability, extended the shared proof-artifact deserialize boundary fixture, and refreshed the revalidation decision-plan privacy fixture for the new private field.
- Verification for the observer-demand batch passed with `cargo fmt -p forge-signal`, focused `observer_demand` tests, focused `resource_policy`, `cargo test -p forge-signal resource_compile_fail_boundaries_hold -- --nocapture`, and full `cargo test -p forge-signal` (772 passed, 23 ignored). `git diff --check` stayed clean aside from the existing LF/CRLF warnings.
- Implementation batch for the next Milestone C Phase 5 slice landed lifecycle-gated revalidation as real proof-bearing runtime lanes instead of leaving terminal/fulfilled freshness intent implicit. Added four new revalidation declarations and built-ins: `ExplicitOrTerminalState`, `ExplicitOrTerminalStateOrActiveHandleForced`, `ExplicitOrFulfilledLifecycle`, and `ExplicitOrFulfilledLifecycleOrActiveHandleForced`. Extended `ResourceRevalidationDecisionPlan` with explicit `permits_terminal_state_revalidation` and `permits_fulfilled_lifecycle_revalidation` truth, bound those flags into the decision digest, and threaded the richer plan through registry lowering, facade exports, compile-fail privacy, and proof-artifact boundaries.
- Runtime now mints sealed `TerminalStateResourceRevalidationProof` and `FulfilledLifecycleResourceRevalidationProof` artifacts and exposes dedicated `prove_*` / `revalidate_*` boundaries for both lanes. Terminal-state proof requires a live node whose current lifecycle is terminal and whose descriptor permits terminal-state revalidation. Fulfilled-lifecycle proof is narrower: it only mints from `ResourceLifecycleClass::Fulfilled`, so timed-out or otherwise terminal nodes cannot masquerade as the fulfilled-only freshness lane. Admission rechecks the proof against current lifecycle and decision-digest truth, denies drift as `TerminalStateProofMismatch` or `FulfilledLifecycleProofMismatch`, and denies policy-disabled use with explicit lane-specific denial classes instead of collapsing those failures into generic revalidation mismatch.
- `AdmittedResourceRevalidation` now retains the exact terminal-state or fulfilled-lifecycle proof used for admission, and telemetry now counts terminal-state/fulfilled-lifecycle proof checks, policy denials, proof mismatches, and successful admissions explicitly. Added hostile runtime tests proving timed-out nodes can be revalidated through the terminal-state lane when allowed, terminal-state proof denies once lifecycle drifts back to pending, fulfilled nodes can be revalidated through the fulfilled-only lane, and non-fulfilled terminal states cannot even mint fulfilled-lifecycle proof. Added new trybuild fixtures proving both proof types remain private and non-deserializable, and refreshed the revalidation-decision-plan stderr to include the new private fields.
- Verification for the lifecycle-gated revalidation batch passed with `cargo fmt -p forge-signal`, focused `resource_terminal_state_revalidation`, `resource_fulfilled_lifecycle_revalidation`, and `resource_policy` tests, `cargo test -p forge-signal resource_compile_fail_boundaries_hold -- --nocapture`, `cargo check -p forge-signal --tests`, full `cargo test -p forge-signal` (776 passed, 23 ignored), and `git diff --check` with only the existing LF/CRLF warnings.
- QA loop on the lifecycle-gated revalidation slice found one real proof-integrity defect: `TerminalStateResourceRevalidationProof` and `FulfilledLifecycleResourceRevalidationProof` were only bound to lifecycle class plus decision digest, which meant a stale proof could be replayed after the node left that lifecycle and later re-entered the same class. In other words, a proof minted for one fulfilled or timed-out instance could still pass after a full `fulfilled -> revalidate -> fulfilled` or `timed-out -> revalidate -> timed-out` cycle, which violates the proof-carrying boundary the milestone and architecture laws require.
- Fixed the defect by binding both proof types to `ResourceLifecycleOrdinal` and validating that ordinal at admission time alongside lifecycle class and decision digest. That makes the proofs instance-specific instead of class-specific, so they expire as soon as the node undergoes any lifecycle transition even if it later returns to the same terminal or fulfilled class.
- Added hostile regression coverage proving stale lifecycle proofs now deny after exact class re-entry: one test for `timed-out -> revalidate -> timed-out` and one for `fulfilled -> revalidate -> fulfilled`. Refreshed the two trybuild privacy stderr fixtures because the stronger proof types now expose an additional private field omission in the compile-fail output.
- Reverification after the QA correction passed with focused terminal-state and fulfilled-lifecycle revalidation tests, `cargo test -p forge-signal resource_policy -- --nocapture`, `cargo test -p forge-signal resource_compile_fail_boundaries_hold -- --nocapture`, full `cargo test -p forge-signal` (778 passed, 23 ignored), `cargo fmt -p forge-signal`, and `git diff --check` with only the existing LF/CRLF warnings.
- Implementation batch to close Milestone C Phase 5 landed explicit revalidation freshness-decision artifacts and dedicated revalidation coalescing instead of relying on generic request-intent coalescing. Added sealed `ResourceRevalidationFreshnessClass`, `ResourceRevalidationFreshnessDecision`, and `ResourceRevalidationCoalescing` artifacts; stored freshness truth on in-flight revalidation requests; and taught admitted revalidation reports plus boundary performance envelopes to retain freshness/coalescing evidence with explicit `coalescing_width`.
- Generic request admission now takes an explicit `allow_intent_equivalence_coalescing` flag so ordinary request coalescing remains available for Phase 4 intent-equivalent supersession, but revalidation uses a dedicated preview/coalescing path. Duplicate revalidation only coalesces when both the canonical request intent digest and the exact freshness decision match the currently active pending revalidation. When that happens, runtime keeps the winner active, records a superseded loser history entry, increments `resource_revalidation_coalesced_count`, and returns a revalidation report retaining a sealed `ResourceRevalidationCoalescing` artifact plus the exact freshness decision that justified the winner.
- Freshness decisions are now explicit per revalidation trigger instead of implicit in call sites: explicit intent, forced active handle, stale-after wake, dependency-change proof, observer-demand proof, terminal-state proof, and fulfilled-lifecycle proof all lower into first-class `ResourceRevalidationFreshnessDecision` evidence. `InFlightResourceRequest` now retains the freshness class, freshness digest, and policy decision digest for active revalidation generations so revalidation coalescing and later replay surfaces do not have to rediscover semantics from hot-path state.
- Closed a real spec gap while doing this: Phase 5 required the hostile row where observer-demand races dependency-change, but the policy vocabulary could not actually express “both lanes allowed” on one descriptor. Added the built-in revalidation declarations `ExplicitOrDependencyChangeOrObserverDemand` and `ExplicitOrDependencyChangeOrObserverDemandOrActiveHandleForced`, along with registry rows and decision-plan lowering support, so the hostile non-coalescing race is now expressible honestly instead of being hand-waved in tests.
- Added adversarial runtime coverage proving duplicate explicit revalidation while pending coalesces onto the active winner and retains loser supersession evidence, while observer-demand and dependency-change revalidation for the same node do not coalesce across distinct freshness causes and instead deny through the active-request rule. Added compile-fail privacy coverage for `ResourceRevalidationFreshnessDecision` and `ResourceRevalidationCoalescing`, and extended the shared non-deserializable proof-artifact boundary so those new artifacts cannot be forged through serde.
- Verification for the Phase 5 closeout batch passed with `cargo check -p forge-signal --tests`, focused revalidation / observer-demand / stale-after / resource-policy coverage, `cargo test -p forge-signal resource_compile_fail_boundaries_hold -- --nocapture`, `cargo fmt -p forge-signal`, full `cargo test -p forge-signal` (780 passed, 23 ignored), and `git diff --check` with only the existing LF/CRLF warnings.
- Implementation batch started Phase 6 with the first real output-continuity slice instead of hand-wavy policy vocabulary. Added `policy/output_continuity.rs` with sealed `ResourceOutputContinuityDecisionPlan` / `ResourceOutputContinuityDecisionClass`, expanded `ResourceOutputContinuityPolicyDeclaration` with `HideWhilePending`, threaded the lowered decision plan through `LoweredResourceDescriptor`, exported it through `data/resource/mod.rs` and `facade.rs`, and added the new built-in descriptor row `signal.resource.output-continuity.hide-while-pending`.
- Runtime request/retry/timeout/cancellation behavior now actually consumes output-continuity policy instead of hard-coding `NoPriorOutput` everywhere. Pending admissions classify visibility from retained output history and the descriptor plan, so a follow-up request after fulfilled output now becomes `PriorOutputPreserved` under the default preserve policy and `OutputUnavailableByPolicy` under the hide-while-pending policy. Timeout and cancellation now carry forward the current pending visibility truth instead of erasing it back to `NoPriorOutput`, and completion commit now counts output-continuity decision work explicitly.
- Split replay/resource digest truth the way Phase 6 requires: lifecycle digest and output-continuity digest are now generated by different canonical functions. `ResourceReplayReconstructionReport` gained `output_continuity_digest()`, the replay digest now binds that separate visibility digest alongside lifecycle digest, and replay restore parity tests now assert that output continuity survives snapshot/restore the same way the rest of the resource summary does.
- Added explicit telemetry and boundary-cost honesty for this family: `ResourceTelemetry` now counts output-continuity decisions, classification width, previous output preserved/hidden, and terminal output preserved/hidden. `ResourceBoundaryPerformanceEnvelope` now retains `output_continuity_classification_width`, and request/retry/timeout/cancellation/completion/revalidation boundaries report that width instead of burying visibility work inside lifecycle counts.
- Hardened the proof surface with adversarial runtime tests proving preserve-vs-hide pending visibility behaves differently while lifecycle remains pending in both cases, plus that preserve/hide changes output-continuity digest without changing lifecycle digest across otherwise equivalent pending states. Added the compile-fail fixture `resource_output_continuity_decision_plan_fields_are_private.rs` and refreshed replay-report privacy stderr because `ResourceReplayReconstructionReport` gained the new private `output_continuity_digest` field.
- Verification for the Phase 6A output-continuity batch passed with `cargo check -p forge-signal --tests`, focused pending-visibility/replay/resource-policy tests, `cargo test -p forge-signal resource_compile_fail_boundaries_hold -- --nocapture`, `cargo fmt -p forge-signal`, full `cargo test -p forge-signal` (782 passed, 23 ignored), and `git diff --check` with only the existing LF/CRLF warnings.
- QA loop on the Phase 6A output-continuity slice found one real honesty defect: timeout and cancellation were carrying forward pending visibility state, which is fine, but they were also counting and reporting that carry-forward as if a fresh terminal output-policy classification had happened. Phase 6A has not lowered terminal visibility policy yet, so that overclaimed both semantic proof and boundary cost.
- Fixed it by narrowing timeout/cancellation to carry existing output continuity state without incrementing output-continuity decision counters or classification width, and by removing the premature terminal preserved/hidden telemetry lanes. Added hostile timeout/cancellation tests proving the carried visibility state survives while reported terminal classification work stays at zero.
- Reverification after the QA correction passed with `cargo fmt -p forge-signal`, focused timeout/cancellation/pending-visibility tests, `cargo test -p forge-signal resource_compile_fail_boundaries_hold -- --nocapture`, `cargo check -p forge-signal --tests`, full `cargo test -p forge-signal` (784 passed, 23 ignored), and `git diff --check` with only the existing LF/CRLF warnings.
- Implementation batch continued Milestone C Phase 6 with the observation half proper instead of stopping at output continuity. Added a real lowered observation policy family in `data/resource/policy/observation.rs` with sealed `ResourceObservationDecisionClass` / `ResourceObservationDecisionPlan`, built-in descriptors for `signal.resource.observation.lifecycle-only` and `signal.resource.observation.lifecycle-and-output`, and threaded the plan through `LoweredResourceDescriptor`, `data/resource/mod.rs`, and `facade.rs`.
- Added a new resource observation artifact surface in `data/resource/observation.rs`: sealed `ObservedResourceNodeState`, `ResourceObservationEvent`, and `ResourceObservationBatchReport`. Observation materialization now has its own `ResourceBoundaryKind::ObservationMaterialization` and `ResourceBoundaryPerformanceEnvelope::observation_materialization(...)`, so the cost of turning generic committed observation summaries into resource-specific output is explicit instead of piggybacking on unrelated lifecycle accounting.
- Runtime now materializes commit-bounded resource observation through `latest_resource_observation_batch_report()` in `logic/transaction/runtime/state/resource_observation.rs`. It consumes the latest committed generic observation boundary summary, filters matched nodes to declared resource nodes, materializes current resource lifecycle truth for each, includes output continuity only when the descriptor's observation decision plan allows it, preserves rollback-suppressed observation outcomes, and records candidate/coalescing/delivered widths plus output-continuity classification width in both telemetry and the returned performance envelope.
- Added hostile runtime coverage proving per-node observation policy actually changes delivered observation surface: lifecycle-only nodes omit output continuity while lifecycle-and-output nodes expose it in the same coalesced observation event, and rollback-suppressed observation still materializes current restored node truth without pretending a delivered observation committed. Added compile-fail privacy fixtures for `ResourceObservationBatchReport` and `ResourceObservationDecisionPlan`.
- Had one honest verification snag: the new trybuild stderr fixtures were semantically right but missed the compiler's current spacing/indentation, so `resource_compile_fail_boundaries_hold` failed until the two `.stderr` files were aligned exactly with actual rustc output. After that correction, reverification passed with focused observation tests, `cargo test -p forge-signal resource_compile_fail_boundaries_hold -- --nocapture`, full `cargo test -p forge-signal` (786 passed, 23 ignored), and `git diff --check` with only the existing LF/CRLF warnings.
- Implementation batch continued Milestone C Phase 6 with the next honest observation family slice: denied-completion observation plus retry-schedule observation. Expanded `ResourceObservationPolicyDeclaration` and the lowered `ResourceObservationDecisionPlan` with first-ship built-ins for `lifecycle-output-and-denied-completion`, `lifecycle-output-and-retry-schedule`, and `lifecycle-output-and-denied-completion-and-retry-schedule`, then registered the new built-in descriptors and policy digests in the resource policy registry.
- `ObservedResourceNodeState` now carries optional denied-completion and scheduled-retry evidence in addition to lifecycle, lifecycle ordinal, output continuity, and observation decision digest. The runtime observation materializer in `logic/transaction/runtime/state/resource_observation.rs` now increments explicit `resource_denied_completion_observation_count` and `resource_retry_schedule_observation_count` telemetry when those evidence lanes are actually delivered, while keeping observation delivery commit-bounded and rollback-safe through the existing generic observation boundary summary.
- Tightened the denied-completion evidence path so observation can report real denial truth without reconstructing it from folklore. `DeniedResourceCompletion` now retains an optional `ResourceNodeId`, and `ResourceRuntimeState` records `latest_denied_completion_by_node` at denial time. That lets observation surface the latest node-local denial evidence directly while still proving that denied-completion observation is non-applying: the hostile runtime test observes a malformed completion denial while lifecycle stays `Pending`.
- Added a mirrored `pending_retry_by_node` index so retry-schedule observation does not have to rediscover schedule truth by scanning request state at read time. Branch-restore logic rebuilds that index when retry handles get branch-epoch refreshed, retry schedule admission inserts it, and retry admission removes it when the pending retry becomes a new request generation.
- Added hostile runtime coverage proving both new observation lanes behave honestly: `resource_observation_batch_report_can_include_denied_completion_without_applying_it` proves denied-completion observation reports malformed denial evidence while lifecycle remains pending, and `resource_observation_batch_report_can_include_retry_schedule_without_retry_apply` proves scheduled retry evidence is observable while lifecycle remains `TimedOut` rather than masquerading as an admitted retry. Added a new compile-fail privacy fixture for `ObservedResourceNodeState`, and refreshed the denied-completion private-field fixture because the struct gained a new private `node` field and rustc changed the diagnostic shape accordingly.
- Verification for the Phase 6 denied-completion/retry-schedule observation batch passed with `cargo fmt -p forge-signal`, `cargo check -p forge-signal --tests`, focused `observation_batch_report` tests, focused `resource_policy`, `cargo test -p forge-signal resource_compile_fail_boundaries_hold -- --nocapture`, full `cargo test -p forge-signal` (788 passed, 23 ignored), and `git diff --check` with only the existing LF/CRLF warnings.
- QA loop on the Phase 6 denied-completion/retry-schedule observation slice found two real truth-leak issues. First, a fresh explicit request could supersede a timed-out lineage while the old pending retry wake stayed scheduled and still surfaced in node-local observation. Fixed runtime admission so fresh request win-path retires the prior retry wake with `TemporalWakeRetirementReason::Superseded` and clears `pending_retry_by_node`, then added hostile coverage proving the old wake cannot promote and observation no longer reports a retry schedule for that node.
- Second, node-local denied-completion observation was acting like accidental retention: once a malformed completion denial was recorded, observation could keep surfacing it after the node had already advanced authoritatively to a new request, retry, fulfilled completion, timeout, or cancellation. Fixed `ResourceRuntimeState` to clear `latest_denied_completion_by_node` on those authoritative lifecycle transitions and added hostile coverage proving denied-completion evidence disappears once the node reaches fulfilled state.
- Full-suite QA also exposed an older retry test whose expectation had become dishonest after the new supersession behavior: it still expected a superseded retry backoff wake to remain promotable and only deny later. Updated the test to assert the stronger invariant instead: once a newer request wins, the stale retry wake is retired before promotion and no superseded-retry denial is emitted.
- Reverification after the QA fixes passed with `cargo fmt -p forge-signal`, focused stale-denial and superseded-retry observation tests, focused `observation_batch_report`, `resource_policy`, and `resource_compile_fail_boundaries_hold`, full `cargo test -p forge-signal` (790 passed, 23 ignored), and `git diff --check` with only the existing LF/CRLF warnings.
- Implementation batch continued Milestone C Phase 6 with explicit terminal visibility policy for the resource lifecycle states we can honestly execute today: timeout, cancellation, and supersession. Extended `ResourceOutputContinuityPolicyDeclaration` and `ResourceOutputContinuityDecisionPlan` with built-ins for `HideAfterTimeout`, `HideAfterCancellation`, and `HideAfterSupersession`, then registered those policies in the frozen policy registry so terminal visibility is descriptor-backed instead of being inherited accidentally from pending state.
- Runtime output continuity now classifies terminal visibility from policy at the moment of timeout, cancellation, and supersession rather than blindly carrying pending visibility forward. This means a node can hide previous output while pending but restore it once the request times out or is cancelled, or explicitly hide it again at those terminal boundaries if the descriptor says so. Supersession artifacts now carry their own visibility truth too, so old-generation visibility is no longer hard-coded to `NoPriorOutput`.
- Boundary cost honesty moved with it: timeout, cancellation, request-admission supersession, intent-equivalence coalescing, and revalidation coalescing now report output-continuity classification width when they make a real visibility decision, and generic output-continuity telemetry counts those terminal classifications instead of pretending they are just carry-forward state.
- Added hostile/runtime evidence proving: hidden pending output can become preserved again at timeout/cancellation under the default terminal preserve posture; timeout preserve and timeout hide share lifecycle digest but differ in output-continuity digest; cancellation can explicitly hide previous output; and supersession policy can hide old-generation output while the new active pending lifecycle still preserves it. Reverification passed with `cargo fmt -p forge-signal`, focused visibility tests, `resource_policy`, `resource_compile_fail_boundaries_hold`, `cargo check -p forge-signal --tests`, and full `cargo test -p forge-signal` (793 passed, 23 ignored). `git diff --check` stayed clean aside from the existing LF/CRLF warnings.
- I explicitly did **not** fake "rejection visibility" in this batch. The spec lists it in Phase 6, but the runtime still has no real rejected lifecycle execution path to attach visibility semantics to. Better to leave that as the next honest gap than smuggle in decorative vocabulary with no runtime truth behind it.




- User clarified Worth migration should not preserve backwards compatibility for old worth-schema / worth-topo APIs. Prefer a full rewrite wherever necessary so Forge Query becomes the direct runtime/query spine rather than adding compatibility adapters around old entrypoints.


- Began Worth Forge Query Runtime Rewrite Gate implementation with Phase 1 query-native vocabulary in worth-schema. Added data::query vocabulary for Worth query collections, schema bases, Forge Query-safe aspect paths, aspect-family classification, and deterministic conversion helpers from WorthAspect / touched-aspect sets. Exported through worth_schema::facade. Verified focused query tests, cargo fmt -p worth-schema --check, full cargo test -p worth-schema, and git diff --check for touched files.


- User objected to Forge Query examples using serde_json::Value write payloads for authoritative writes. Clarified Worth rewrite rule: Worth topology writes must be aspect-native, not opaque JSON blobs. Updated the rewrite gate spec so Phase 2 explicitly hardens Forge Query with aspect-native/domain write commands and forbids JSON-blob topology write entrypoints when aspect-native mutation meaning is available.


- Began `_docs/forge-query/aspect-api-finalization-plan.md` implementation in `forge-query` with the aspect-native mutation facade foundation. Added public `ForgeQueryAspectValue` and `ForgeQueryAspectMutationBuilder`, new `workspace.insert/update/delete` and preview equivalents, new `ForgeQueryWriteCommand::InsertAspects/UpdateAspects` lowering through the current JSON-backed substrate, and updated stabilization transcripts/docs so aspect-native authoring is the preferred public story while JSON remains internal lowering debt only. Verified `cargo check -p forge-query --tests`, `cargo fmt -p forge-query`, `cargo test --manifest-path crates/forge-query/Cargo.toml --test phase_boundaries_compile_fail`, `cargo test -p forge-query`, and `git diff --check` for the batch.


- Continued the aspect API finalization work in `forge-query` with first-class ordered batch mutation support. Added `ForgeQueryMutationBatchBuilder`, `workspace.batch(...)`, `preview.batch(...)`, `ForgeQueryBatchWriteReceipt`, batch inspection artifacts/handle-contract coverage, runtime naming support for `batch`, focused behavior tests, compile-fail privacy coverage, and doc updates that keep batch semantics honest as ordered multi-write execution rather than overstating atomic substrate guarantees. Verified focused batch tests, `cargo test --manifest-path crates/forge-query/Cargo.toml --test phase_boundaries_compile_fail`, full `cargo test -p forge-query`, and `git diff --check` aside from Windows CRLF warnings.


- Ran a hostile QA pass on the new Forge Query batch mutation slice and fixed two foundation issues. First, `workspace.batch(...)` had been bulk at the facade but scalar in runtime routing, so shared computed/effect maintenance could rerun once per sub-write; corrected by adding backend/runtime batch execution plumbing and a single aggregate routing boundary for authoritative batch receipts, with a proof test that shared computeds are considered once per batch. Second, batch inspection did not clearly expose the component operations inside a batch; corrected by adding `ForgeQueryBatchWriteComponentInspection`, extending batch inspection/handle-contract coverage, and updating docs so the batch receipt is the canonical routing artifact while component operations remain inspectable evidence. Verified targeted batch tests, `cargo fmt -p forge-query`, full `cargo test -p forge-query`, and `git diff --check` aside from Windows CRLF warnings.


- Continued `_docs/forge-query/aspect-api-finalization-plan.md` with mutation receipt ontology hardening in `forge-query`. Added public `ForgeQueryMutationFamily`, stamped write and batch receipts with mutation family / declared target / basis lane posture, made `workspace.state(&write_receipt)` and `workspace.state(&batch_receipt)` return meaningful ready snapshots, extended unified inspection with family/target/basis evidence plus batch component operation details, updated focused runtime tests and compile-fail privacy fixtures, and verified with `cargo fmt -p forge-query`, `cargo test --manifest-path crates/forge-query/Cargo.toml --test phase_boundaries_compile_fail`, full `cargo test -p forge-query`, and `git diff --check` aside from Windows CRLF warnings.


- Continued the aspect API finalization plan with a public-doc and executable-example cleanup pass so the preferred mutation story stops teaching payload-first writes. Replaced remaining CRUD-style `workspace.write(ForgeQueryWriteCommand::Insert { payload: json!(...) })` examples across the main feature docs with `insert(...)` / preview `insert(...)`, reframed `workspace.write(...)` as the lower-level compatibility path in the overview and write-boundary docs, and updated runtime assembly support-profile tests so the preferred workspace DX proofs now exercise aspect-native mutation entrypoints. Verified with `cargo fmt -p forge-query`, targeted `runtime_workspace_*` tests, full `cargo test -p forge-query`, and `git diff --check` aside from Windows CRLF warnings.


- Closed the next aspect-finalization compatibility slice in `forge-query` by making payload-first mutation posture machine-checkable instead of purely documentary. Added `runtime/mutation_compatibility.rs` with `ForgeQueryMutationApiCompatibilityReport` plus preferred/stable-compat/deprecated/support-gated rows, exposed the report through runtime/workspace/facade, marked `ForgeQueryWriteCommand::Insert` as deprecated payload-first compatibility, tightened the public naming contract to say so explicitly, added support-profile assertions and a dedicated compile-fail fixture proving deprecated payload-first insert usage is rejected under `#![deny(deprecated)]`, and cleaned up deliberate internal compatibility warnings so the compile-fail proof remains the loud signal. Verified with `cargo fmt -p forge-query`, focused `runtime_public_mutation_compatibility_report_marks_payload_insert_deprecated`, `cargo test --manifest-path crates/forge-query/Cargo.toml --test phase_boundaries_compile_fail`, full `cargo test -p forge-query`, and `git diff --check` aside from Windows CRLF warnings.


- Ran a hostile QA loop over the new Forge Query mutation-compatibility slice and fixed three architectural issues. First, the compatibility report and naming contract were incomplete because they omitted the still-public `ForgeQueryWriteCommand::InsertAspects` path; added explicit stable-compatibility classification and assertions so the report now covers the full public insert surface honestly. Second, production code had broad file-level `allow(deprecated)` suppressions that could hide future accidental deprecated usage; narrowed those down to exact compatibility branches and functions. Third, `src/runtime/surface.rs` had become a responsibility bag spanning mutation contracts, live surfaces, program receipts, and inspection artifacts; split it into `runtime/surface/{mutation,live,program,inspection_artifact}.rs` with a small `mod.rs` re-export layer so future growth does not keep compounding into a god file. Verified with `cargo fmt -p forge-query`, `cargo check -p forge-query --tests`, focused mutation-compatibility tests, compile-fail boundaries, full `cargo test -p forge-query`, and `git diff --check` aside from Windows CRLF warnings.


- Continued `_docs/forge-query/aspect-api-finalization-plan.md` with the Phase 6 closeout and substrate-handoff contract. Added public `ForgeQueryAspectApiFinalizationCloseout` in `runtime/aspect_api_closeout.rs`, exposed it through runtime/workspace/facade as `public_aspect_api_finalization_closeout()`, authored `_docs/forge-query/aspect-api-finalization-closeout.md`, and bound the doc to executable support-profile assertions so the safe-to-build-now / must-not-assume-yet / migration guidance stays synchronized with the real support matrix, naming contract, and mutation compatibility report. Verified with `cargo fmt -p forge-query`, focused closeout tests, `cargo check -p forge-query --tests`, compile-fail boundaries, full `cargo test -p forge-query`, and `git diff --check` aside from Windows CRLF warnings.


- Continued the aspect API finalization work in `forge-query` with typed aspect clear/reset support and a dedicated certification harness. Added `ForgeQueryAspectValue::new_set/new_clear`, `ForgeQueryAspectMutationBuilder::clear(...)`, runtime tests proving clear narrows touched-surface wakeups and rejects duplicate clear+set authoring, and a new `harness/aspect_api_finalization_certification` suite that certifies authoritative CRUD, typed clear semantics, preview batch lane isolation, and compatibility/closeout sync while also proving early typed denial for unsupported intent and duplicate authoring. Updated workspace/write docs to teach `task.clear(...)`, refreshed the compile-fail witness for the strengthened private `ForgeQueryAspectValue`, and verified with focused certification tests, `cargo check -p forge-query --tests`, `cargo test --manifest-path crates/forge-query/Cargo.toml --test phase_boundaries_compile_fail`, full `cargo test -p forge-query`, and `git diff --check` aside from Windows CRLF warnings.


- Ran a hostile QA loop over the typed clear/reset + aspect certification slice and fixed four real honesty/structure issues. Split the privacy compile-fail proof so `ForgeQueryAspectValue` and `ForgeQueryAspectMutationBuilder` each have their own witness, preserved authored mutation meaning by carrying declared set/clear operations into write receipts and unified inspection instead of collapsing everything to touched aspect paths, decomposed `harness/aspect_api_finalization_certification/builders.rs` into `digests.rs`, `fixture.rs`, and `rows.rs` to kill the new god file, and refreshed drifted trybuild stderr files for `runtime_write_receipt_inspection_constructor_private` and `runtime_aspect_mutation_privates_forbidden`. Verified with `cargo fmt -p forge-query`, `cargo check -p forge-query --tests`, `cargo test --manifest-path crates/forge-query/Cargo.toml --test phase_boundaries_compile_fail`, full `cargo test -p forge-query`, and `git diff --check` aside from Windows CRLF warnings.

