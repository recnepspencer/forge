# 2026-05-13

- Continued WORTH Signal WASM Milestone 11 Phase 2 after reading the coding
  guidelines and implementation-batch workflow. Closed the detail-field proof
  gap by adding sealed `detailField` proof/cost handling alongside existing
  `detailRegion` and `detailJsonPath` families.
- Phase 2 now exposes detail field proof through local patches, delivered
  mutation-response reconciliation, effect envelopes, counters, diagnostics,
  history, and package types. Accessor-backed detail field declarations are
  denied via descriptor inspection without invoking getters.
- Split touched oversized resource effect and patch execution modules into
  smaller helpers to keep code files under the 400-line workspace cap.
- Verified Phase 2 with focused response-authoring tests, package typecheck,
  and the full `resource.runtime.test.mjs` aggregate suite: 307/307 passing.
- Ran the QA loop on Phase 2. Found and fixed unsafe detail field declaration
  names (`__proto__`, `constructor`, `prototype`) plus an incomplete compact
  inverse type surface for detail JSON path rollback. Added hostile field-name
  tests and reverified the full resource runtime suite: 309/309 passing.
- Ran the dedicated test QA pass. Found an over-cap topology proof test file
  and split granular detail field/path/region proofs into
  `detail_granular_response.test.mjs`, keeping the topology directory at the
  10-file cap. Strengthened mutation-response JSON path and region assertions
  to check proof/cost counters instead of only patched values. Full resource
  runtime suite remained 309/309 passing.
- Started Milestone 11 Phase 3 with a real implementation batch for update/save
  response reconciliation beyond detail lines. Added exact mutation-response
  execution lanes for collection item replacement and summary patching from
  declared response fields, split the over-cap mutation plan file into payload
  digest and target-execution helpers, updated public mutation response types,
  and added focused certification tests. Package typecheck passed and the full
  resource runtime aggregate passed 317/317.
- Continued Phase 3 in order with validation/warning mapping rather than
  jumping to Phase 4. Added route-owned mutation response `diagnostics`
  declarations for `warnings` and `validation` fields, lowered them into typed
  mutation-response diagnostic facts with digests and extraction counters,
  exposed compact diagnostic facts through diagnostics summary, updated package
  types, and split route finalization/download helpers to keep touched files
  under cap. Authoring-response tests, package typecheck, and the full resource
  runtime aggregate passed 319/319.
- Ran the Phase 3 QA loop. Found and fixed two overclaims: exact reconciliation
  and diagnostics had leaked onto create/remove response lanes, and summary
  reconciliation could silently consume a whole detail payload when the declared
  summary field was absent. Restored create/remove to fallback-only target
  posture until later phases, added runtime and type denials, split over-cap
  declaration/test files, and reverified package typecheck plus the full
  resource runtime aggregate at 319/319 passing.
- Continued Phase 3 with server-confirmation classification for update/save
  mutation responses. Added a confirmation classifier that names preserved
  optimistic truth, consumed canonical truth, partial canonical truth, refetch
  required, and delivery awaited from the canonical plan execution artifacts;
  exposed compact confirmation kind/digest through diagnostics summary; updated
  public types and focused certification tests. Package typecheck passed,
  response-authoring tests passed 33/33, and the full resource runtime aggregate
  passed 322/322.
- Continued Phase 3 with update-target rollback and merge/rebase lifecycle
  proof. Added a mutation-response lifecycle proof module that derives exact,
  fallback, and awaiting-execution proof entries from target effect envelopes;
  surfaced compact rollback and merge/rebase digests through diagnostics
  summary; updated public mutation response and line summary types; and added
  focused detail, granular field, summary, and fallback certification tests.
  Package typecheck passed, response-authoring tests passed 36/36, diff and
  line-cap checks passed, and the full resource runtime aggregate passed
  325/325.
- Ran the QA loop on the Phase 3 lifecycle-proof slice. Found and fixed weak
  certification coverage for detail JSON path, detail region, and collection
  item lifecycle proof; strengthened rollback and merge/rebase digests so they
  include compact proof evidence instead of only kind/granularity labels; and
  corrected detail-region merge proof to carry declared `mergeGranularity`.
  Reverified package typecheck, response-authoring tests at 38/38, diff and
  line-cap checks, and the full resource runtime aggregate at 327/327.
- Continued Phase 3 with stale update/save target admission. Added submitted
  target basis snapshots for mutation-response planning, denied exact
  reconciliation when pending save responses settle after local visible-value
  drift or delivered basis drift, and surfaced stale-target fallback evidence
  plus target-basis counters in the public plan surface. Split materialized
  family binding and target-basis types to keep touched files under cap. Verified
  package typecheck, response-authoring tests at 40/40, diff and line-cap
  checks, and the full resource runtime aggregate at 329/329.
- Continued Phase 3 with paged update/save reconciliation proof. Certified
  resident paged item replacement and paged page-window summary patching from
  mutation response fields, added `summaryScope` evidence to exact summary
  execution artifacts, and split mutation-response lifecycle types to keep the
  public plan type under the workspace cap. Verified package typecheck,
  response-authoring tests at 42/42, diff and line-cap checks, and the full
  resource runtime aggregate at 331/331.
- Ran the QA loop on the Phase 3 closeout surface. Found and fixed a cost
  honesty defect where mutation-response `fallbackBreadth` counted every
  planned target instead of only fallback execution artifacts; exact
  reconciliations now report zero fallback breadth while fallback plans retain
  their true affected-target count. Added focused assertion coverage and
  reverified package typecheck, focused response-authoring tests 13/13, full
  resource runtime aggregate 331/331, diff whitespace check, and an equivalent
  PowerShell line-cap scan across 65 touched code/test files because `bash` was
  unavailable for the CI shell script in this environment.
- Completed the Phase 3 finish checklist. Reran the canonical Rust line-cap
  script through Git Bash; it executed but failed on unrelated pre-existing
  tracked Rust files outside the wasm package, so Phase 3 relied on the scoped
  touched-file line-cap scan instead. Ran `$qa-tests` on the Phase 3 proof
  surface, found directory-topology issues, moved save/update mutation-response
  tests under `authoring/response/save_update`, and moved touched lifecycle
  diagnostics/history tests into named subdirectories to satisfy the 10-file
  test-directory cap. Reverified focused moved tests, package typecheck,
  touched file line caps across 68 code/test files, diff whitespace, and full
  resource runtime aggregate 331/331. Phase 3 is ready to commit with the
  caveat that the workspace-wide Rust line-cap debt remains outside this
  milestone.


- Continued WORTH Query 9.3.2 Phase 5 after receipt/envelope QA. Added public
  basis lifecycle support discovery with digest and exact support-lookup
  counters, exported it through the facade, and tied discovery posture back to
  executable admission behavior for admitted, advisory, deferred, and
  unsupported lanes. Added compile-fail coverage preventing external
  construction of support discovery reports. Focused basis lifecycle tests,
  phase-boundary trybuild, fmt check, and full `cargo test -p worth-query`
  passed.
- Began WORTH Query 9.3.2 Phase 6 with the representative certification
  bundle slice. Added `BasisLifecycleCertificationBundle`, representative
  admitted/advisory/denied/lower-runtime-mismatch/future-neighbor/performance
  rows, required verification-output digest names, certification assembly
  counters, facade exports, and compile-fail coverage preventing external
  construction of certification bundles. Split certification row/output
  builders into child modules to keep files under the 400-line cap. Focused
  certification tests, phase-boundary trybuild, fmt check, and full
  `cargo test -p worth-query` passed.
- Ran a QA-loop pass over the Phase 6 certification slice. Found and fixed two
  honesty gaps: deferred/pending verification outputs now carry explicit
  `BasisLifecycleCertificationOutputPosture`, and the admitted certification
  row now snapshots counters across normalization, eligibility, scoped use,
  lower-runtime readmission, receipt emission, and envelope materialization.
  Added compile-fail coverage preventing external construction of certification
  rows and output digests. `cargo fmt --check -p worth-query`,
  `cargo test -p worth-query certification --quiet`, phase-boundary trybuild,
  and full `cargo test -p worth-query --quiet` passed.
- Continued Phase 6 with the golden-DX certification slice. Added the
  `basis_lifecycle()` fluent entrypoint and DX path types for current-head
  observation through envelope materialization, branch-head mutation
  preparation, preview-derived inspection advisory, support discovery, and
  typed denial handling. Replaced pending target-DX/golden-transcript
  certification outputs with a certified DX digest and added a compile-fail
  fixture proving DX drafts cannot be used as scoped proof tokens. `cargo fmt
  --check -p worth-query`, `cargo test -p worth-query dx --quiet`,
  `cargo test -p worth-query certification --quiet`, phase-boundary trybuild,
  and full `cargo test -p worth-query --quiet` passed.
- Continued Phase 6 with the migration/public-boundary audit slice. Added
  `BasisLifecycleMigrationAudit` with sealed rows and counters for
  branch/preview admission, read-composition basis context, subscription basis
  posture, causal inspection evidence, historical materialization basis,
  lower-runtime readmission evidence, and future-neighbor store/durable basis.
  Certified `compatibility_debt_registry_digest` from the migration audit
  digest and added compile-fail coverage preventing external audit-row
  construction. `cargo fmt --check -p worth-query`,
  `cargo test -p worth-query migration --quiet`,
  `cargo test -p worth-query certification --quiet`, phase-boundary trybuild,
  and full `cargo test -p worth-query --quiet` passed.
- Ran a QA-loop pass over the migration/public-boundary audit slice. Found
  that branch/preview admission and causal inspection were overstated as
  lifecycle-adapter-covered even though the existing code still exposes them
  as legacy/sibling basis consumers. Reclassified both as explicit
  compatibility debt, added an invariant test preventing hidden debt behind
  adapter posture, and bound the certification `compatibility_debt_registry`
  output back to the actual migration audit digest. `cargo fmt --check -p
  worth-query`, `cargo test -p worth-query migration --quiet`, `cargo test -p
  worth-query certification --quiet`, phase-boundary trybuild, and full
  `cargo test -p worth-query --quiet` passed.
- Ran a hostile `$qa-tests` pass against the 9.3.2 basis lifecycle tests and
  test requirements. Found that tests were still too sample-based around
  support metadata, certification was missing the required
  `lower_runtime_api_reuse_matrix_digest` and `adapter_shape_contract_digest`
  outputs, and basis lifecycle trybuild fixtures were in an over-cap flat
  directory. Added a sealed lower-runtime reuse matrix plus adapter shape
  digest, exhaustive support-matrix versus executable-admission checks,
  certification failure-digest derivation checks, control/hostile/parity lane
  assertions, and reorganized trybuild fixtures by proof responsibility.
  `cargo fmt --check -p worth-query`, focused certification/support/reuse
  tests, phase-boundary trybuild, and full `cargo test -p worth-query --quiet`
  passed.
- Corrected the 9.3.2 closeout stance after Esther challenged the idea that
  compatibility debt could remain. Continued with an implementation batch that
  added lifecycle adapter proofs for the five in-scope debt rows: branch/preview
  admission, read-composition basis context, subscription basis posture, causal
  inspection evidence, and historical materialization basis. The migration
  audit now reports zero compatibility debt for 9.3.2-owned surfaces; unsupported
  legacy postures become typed lifecycle denials rather than debt. Added facade
  exports and compile-fail coverage preventing external construction of adapter
  proofs. `cargo fmt --check -p worth-query`, focused basis lifecycle/migration/
  certification tests, phase-boundary trybuild, and full
  `cargo test -p worth-query --quiet` passed.
- Continued the no-debt 9.3.2 closeout push with an implementation batch that
  hardened performance-slope certification. Removed the stale `PendingPhase6`
  output posture, added sealed `BasisLifecyclePerformanceSlopeReport` and
  `BasisLifecycleSlopeDigest` evidence, and wired all seven required slope
  outputs to stage-specific lifecycle counter digests instead of the generic
  representative performance row. Added facade exports and compile-fail coverage
  preventing external slope digest construction. `cargo fmt --check -p
  worth-query`, focused basis lifecycle/certification/phase-boundary tests, and
  full `cargo test -p worth-query --quiet` passed; `PendingPhase6` scan is clean
  and basis lifecycle file/directory cap checks are clean.
- Ran a `$qa-loop` closeout pass against the 9.3.2 spec and coding guidelines.
  Found and corrected two closeout weaknesses: the lower-runtime reuse matrix
  still used ambiguous `reused_or_*` / compatibility-debt posture strings, and
  performance-slope rows exposed only counter digests instead of the exact
  counter facts required by the spec and performance laws. Reuse rows now assert
  unambiguous `reused` posture, and `BasisLifecycleSlopeDigest` carries sealed
  counter snapshots with exact counter assertions for each certified slope
  family. Refreshed trybuild stderr for the new private counter field. `cargo
  fmt --check -p worth-query`, `cargo test -p worth-query certification
  --quiet`, `cargo test -p worth-query basis_lifecycle --quiet`, phase-boundary
  trybuild, and full `cargo test -p worth-query --quiet` passed with 1126
  tests; line-cap and ambiguity scans were clean.
- Continued the 9.3.2 no-debt closeout with an implementation batch for Phase 6
  public-boundary audit certification. Added a sealed public-boundary audit for
  raw branch, snapshot, preview, tenant, policy, runtime snapshot, raw intent,
  and normalized intent surfaces, with each row naming the forbidden token,
  blocked entrypoint, required capability, and compile-fail enforcement proof.
  Wired `compile_fail_boundary_digest` to the real audit digest and added a
  trybuild fixture proving external code cannot construct audit rows. `cargo
  fmt --check -p worth-query`, public-boundary focused tests, basis lifecycle
  tests, phase-boundary trybuild, and full `cargo test -p worth-query --quiet`
  passed with 1128 tests; stale-marker scan, line-cap scan, and certification
  fixture-count cap were clean.
- Continued Phase 6 with proof-shape certification hardening. Replaced the
  generic `all_rows_digest` wiring for `basis_proof_shape_digest` and
  `basis_phase_progression_digest` with a sealed proof-shape audit covering
  phase skipping, raw identifier substitution, stale proof reuse, operation-lane
  WORTHry, and WORTHd lower-runtime authority witnesses. Added facade exports,
  audit row tests, certification output binding assertions, and a trybuild
  fixture proving external code cannot construct proof-shape audit rows.
  `cargo fmt --check -p worth-query`, focused proof-shape/certification tests,
  basis lifecycle tests, phase-boundary trybuild, full
  `cargo test -p worth-query --quiet` with 1130 tests, and
  `cargo test -p worth-runtime-bridge --quiet` passed; line-cap and stale-marker
  scans were clean.
- Ran a `$qa-loop` pass over the Phase 6 proof-shape audit. Found that the
  audit rows were sealed but still used untyped proof strings, with raw
  identifier substitution and stale proof reuse pointing at adjacent/weaker
  proof names. Added a typed `BasisLifecycleProofShapeEnforcement` family,
  bound every violation to an exact cataloged proof artifact, switched raw
  identifier substitution to the real public-boundary audit, and switched stale
  proof reuse to the runtime readmission denial proof. Refreshed the private-row
  trybuild fixture for the new sealed enforcement field. `cargo fmt --check -p
  worth-query`, `cargo test -p worth-query proof_shape --quiet`, `cargo test -p
  worth-query certification --quiet`, phase-boundary trybuild, `cargo test -p
  worth-query basis_lifecycle --quiet`, full `cargo test -p worth-query
  --quiet` with 1131 tests, and `cargo test -p worth-runtime-bridge --quiet`
  passed. Line-cap, stale-marker, wip, and stale proof-name scans were clean.
- Ran the final 9.3.2 closeout QA pass. Found and corrected the last
  certification-softness gap: `signal_basis_authority_digest` was still a
  deferred label, while `typestate_transition_digest` and
  `phase_artifact_manifest_digest` reused generic certification row summaries.
  Bound signal authority to the signal row in the lower-runtime reuse matrix,
  added a sealed `BasisLifecyclePhaseManifest` audit with ordered typestate and
  phase-manifest digests, exported the final closeout evidence, and added
  compile-fail coverage preventing external phase-manifest row construction.
  Added `_docs/worth-query/milestone-9.3.2-closeout.md`, marked the 9.3.2 spec
  closed, and linked the closeout from the roadmap. `cargo fmt --check -p
  worth-query`, focused phase_manifest/reuse/certification tests, phase-boundary
  trybuild, `cargo test -p worth-query basis_lifecycle --quiet`, full `cargo
  test -p worth-query --quiet` with 1135 tests, `cargo test -p
  worth-runtime-bridge --quiet`, line-cap scan, stale-marker scans, and
  `git diff --check` passed.
- Started WORTH Query 9.3.3 with Slice 1: effect intent, support discovery,
  and typed eligibility. Added a new `effect_lifecycle` module built on top of
  `ScopedMutationPreparationBasis` plus the existing workflow declaration and
  lowering-input APIs. The slice ships `RawEffectIntent`,
  `NormalizedEffectIntent`, a concrete effect-family/support matrix, typed
  `Admitted`/`Denied`/`RebindRequired`/`Deferred` eligibility outcomes, an
  `AdmittedEffectIntent` proof for later lowering phases, facade exports, and
  compile-fail coverage preventing external construction of normalized/admitted
  effect proofs and support rows. Focused effect-lifecycle tests,
  phase-boundary trybuild, and full `cargo test -p worth-query --quiet`
  passed.
- Ran a hostile QA pass on the first 9.3.3 slice and then shipped the
  correction batch instead of leaving the findings as commentary. Refactored
  `effect_lifecycle` into phase-honest files (`authoring_basis`, `normalized`,
  `eligibility`, `intent`, `admission`), replaced the fake-raw
  `QueryWorkflowDeclaration` input with real `WorkflowContextBinding +
  WorkflowDeclarationRequest`, carried basis authority/lifecycle/lower-runtime
  binding facts through normalization, made preview-backed writeback a real
  authorable rebind path, aligned support lookup counters between discovery and
  eligibility, and refreshed the effect-lifecycle trybuild fixtures to the new
  private field sets. `cargo fmt --package worth-query`, focused
  `cargo test -p worth-query effect_lifecycle --quiet`, phase-boundary
  trybuild, and full `cargo test -p worth-query --quiet` passed with 1141
  tests.
- Ran a second QA loop on the new 9.3.3 Phase 3 planning slice. Found three
  real foundation issues: `AuthorityScopedEffectPlan` exposed stringly-typed
  authority/strategy identities instead of compile-time enums, planning
  counters were faking a blended width via `max(support_rows, workflow_binding)`
  instead of reporting exact effect-lifecycle work, and the new plan proof
  trybuild fixture was added without its checked stderr. Corrected the plan to
  export typed `EffectAuthorityOwner` and `EffectStrategyIdentityTarget`,
  switched plan counters back to the exact admitted support-row count, added
  plan tests that prove common-path planning truth stays aligned with admitted
  workflow truth, refreshed the missing plan proof stderr, and updated a stale
  `QueryWritebackDeclaration` trybuild stderr path so the full boundary suite
  reflects current compiler output. `cargo fmt --package worth-query`,
  `cargo test -p worth-query effect_lifecycle --quiet`, `cargo test -p
  worth-query --test phase_boundaries_compile_fail --quiet`, and full
  `cargo test -p worth-query --quiet` passed with 1143 tests.
- Continued 9.3.3 in linear order with Phase 4A: lowered execution-plan
  fencing. Added `effect_lifecycle/lowering.rs` with sealed
  `LoweredEffectExecutionPlan`, concrete lowered artifact variants for
  mutation/merge/writeback, typed lowering denials derived from
  `WorkflowLoweringFailureClass`, and `AuthorityScopedEffectPlan::lower()` as
  the only effect-lifecycle path into runtime-backed execution artifacts.
  Extended effect-lifecycle counters with lowering facts, split the
  over-cap `effect_lifecycle/tests.rs` file into focused foundation/planning/
  lowering/support modules, and added compile-fail coverage proving raw and
  normalized intents cannot cross the lowering fence and lowered execution
  plans cannot be constructed externally. `cargo fmt --package worth-query`,
  `cargo test -p worth-query effect_lifecycle --quiet`, `cargo test -p
  worth-query --test phase_boundaries_compile_fail --quiet`, and full
  `cargo test -p worth-query --quiet` passed with 1147 tests.
- Ran a hostile QA loop over the 9.3.3 Phase 4A lowering fence and fixed two
  real softness points. First, `LoweredEffectExecutionPlan` was too thin and
  forced callers to crawl back through `AuthorityScopedEffectPlan` for ordinary
  lowered-shape inspection, so it now forwards the practical inspection facts
  directly: family, authority lane/owner, basis lane, invariant scope, preview
  posture, policy posture, lowering family, artifact policy, conflict
  footprint, strategy identity target, and staleness class. Second, the
  effect-level lowering counters were hardcoding width `1` instead of deriving
  exact lowering facts from the wrapped workflow artifact; they now carry the
  underlying lowering width and executor-rediscovery count forward honestly.
  Added tests proving lowered-plan inspection works directly and that
  effect-level counters match the underlying workflow counters. `cargo fmt
  --package worth-query`, `cargo test -p worth-query effect_lifecycle --quiet`,
  phase-boundary trybuild, and full `cargo test -p worth-query --quiet` passed
  with 1147 tests; one transient Windows linker lock on the compile-fail test
  binary cleared on immediate rerun.
- Continued 9.3.3 in strict phase order with Phase 4B: lowered execution
  boundaries. Added `effect_lifecycle/execution.rs` with
  `EffectExecutionAuthority`, `execute_lowered_effect_plan`, typed
  `EffectExecutionDenial` kinds, and sealed `ExecutedEffectPlan` /
  `ExecutedEffectAuthorityArtifact` wrappers over real lower-runtime outcomes.
  Mutation execution now runs through the real relational strategy authority
  chain (canonicalize -> execute -> authority-lower -> validate -> commit),
  merge execution runs through relational merge preparation/execution, and
  lowered writeback execution fails closed with a typed contract-assembly denial
  instead of inventing a fake Query-side bridge protocol. Extended
  `EffectLifecycleCounters` with execution facts, exported the new surface
  through the facade, added real execution tests plus compile-fail fixtures
  proving raw/normalized/admitted/scoped plans cannot execute, and refreshed
  the trybuild stderr set. `cargo fmt --package worth-query`, focused
  `cargo test -p worth-query effect_lifecycle --quiet`, `cargo test -p
  worth-query --test phase_boundaries_compile_fail --quiet`, and full
  `cargo test -p worth-query --quiet` passed with 1150 tests.
- Closed the practical Phase 5 gap for 9.3.3 with a receipt-surface batch
  instead of moving on early. Added sealed `EffectReceiptTransitionRules`
  covering inspect/envelope/diagnostics as implemented and projection/replay/
  portable export as explicitly deferred, fixed batch receipt decision traces so
  admitted-batch and lowered-batch identities no longer collapse into the
  executed batch digest, and enriched `SelfDescribingEffectEnvelope` with
  source-bearing fields for receipt/lowered/authority/counter provenance plus
  deferred-neighbor and transition digests. `EffectDiagnosticsRequest::forensic`
  now includes transition/source evidence, the public-surface inventory now
  advertises `transition_rules()`, and compile-fail coverage prevents external
  construction of the new transition artifact. `cargo fmt --package
  worth-query`, focused receipt/inventory/certification tests, effect-lifecycle
  phase-boundary trybuild, and full `cargo test -p worth-query --quiet` passed
  with the current branch state.
- Continued 9.3.3 in linear order with Phase 6A closeout unification. Added a
  sealed `EffectExecutionCertificationBundle` plus lane/output artifacts,
  unified seeded replay, Phase 4 hostile execution proof, and real Phase 5
  receipt/envelope/diagnostics surfaces under `certify_effect_execution_pipeline`,
  and added compile-fail coverage preventing external bundle construction.
  Split the initial closeout assembly into `closeout`, `closeout_artifacts`, and
  `closeout_receipts` so the new certification code stays under the 400-line
  cap. `cargo fmt --package worth-query`, focused certification tests,
  effect-lifecycle phase-boundary trybuild, and full `cargo test -p worth-query
  --quiet` passed; one transient Windows linker lock on the compile-fail test
  binary cleared on immediate rerun.
- Ran a hostile QA loop over the new 9.3.3 Phase 6A closeout bundle and fixed
  two real closure gaps. First, the unified certification bundle was only
  emitting a subset of the spec-required verification outputs, so the closeout
  now binds raw/family/authority/basis/scope/policy/strategy digests plus all
  seven required slope outputs to real phase artifacts and stage-local counter
  evidence. Second, the old `compile_fail_boundary_digest` and
  `effect_proof_shape_digest` were just label hashes; they now come from
  explicit closeout audits over named forbidden surfaces and ordered
  raw->normalized->eligibility->plan->lowered->receipt->envelope progression.
  Split the extra certification logic into `closeout_audits` and
  `closeout_slopes`, kept every touched file under the line cap, and refreshed
  the certification tests to assert the widened output set. `cargo fmt
  --package worth-query`, focused certification tests, and effect-lifecycle
  phase-boundary trybuild passed after the fixes.
- Continued 9.3.3 Phase 6 with public-boundary closure for certification
  itself. Narrowed the public `worth-query` certification surface so only the
  unified `certify_effect_execution_pipeline` bundle remains exported, while
  seeded replay and Phase 4 certification scaffolding stay internal. Added a
  `ProductionCertification` row to the effect-lifecycle public-surface
  inventory, updated inventory tests to assert there is exactly one public
  certification gate and no inventory entrypoint mentions the older scaffold
  APIs, and added trybuild coverage proving
  `certify_effect_lifecycle_seeded` and
  `certify_effect_lifecycle_phase4` are no longer reachable from the facade.
  Cleaned the internal certification scaffolding with honest dead-code
  annotations so the narrowed public boundary does not leave warning noise.
  `cargo fmt --package worth-query`, focused inventory/certification tests,
  effect-lifecycle phase-boundary trybuild, and full
  `cargo test -p worth-query --quiet` passed.
- Continued 9.3.3 Phase 6 with the missing golden-DX proof lane. Added
  `effect_lifecycle/certification/closeout_dx.rs` so closeout now certifies
  six real caller stories instead of reusing the public-surface inventory
  digest as both `effect_target_dx_digest` and
  `effect_golden_transcript_digest`. The new DX transcript lane exercises:
  common-path authoring, inspectable lowered plans, typed rebind handling,
  support discovery, batch-native execution, and receipt-first diagnostics.
  `certify_effect_execution_pipeline()` now binds a distinct target-DX digest
  from the code-owned surface inventory and a distinct golden-transcript digest
  from runtime-backed story evidence, and certification tests now prove the
  support/DX row names all six stories and that the two DX digests differ.
  `cargo fmt --package worth-query`, focused certification/receipt/inventory
  tests, and full `cargo test -p worth-query --quiet` passed; the new helper
  stays under the 400-line cap.
- Ran a QA loop on the new Phase 6B DX lane and fixed two softness points.
  First, the common-path transcript was too static, so the DX bundle now emits
  a real lifecycle-shaped story that includes actual admission, lowering,
  receipt, and envelope evidence from a runtime-backed mutation flow. Second,
  the support/DX row had been borrowing one support-discovery counter snapshot,
  which made the aggregate lane too soft; `EffectLifecycleCounters` now has a
  `combine()` helper and the DX lane folds counters across all six story
  transcripts instead of certifying a single narrow path. Certification tests
  now assert the common-path transcript includes the lifecycle chain and that
  the support/DX counter digest is non-default. `cargo fmt --package
  worth-query`, focused `effect_lifecycle::tests::certification`, and full
  `cargo test -p worth-query --quiet` passed cleanly after the correction.
- Continued 9.3.3 in linear order with Phase 6C closeout posture completion.
  Added `effect_lifecycle/certification/closeout_postures.rs` and extended the
  unified `EffectExecutionCertificationBundle` with explicit
  `AdvisorySurface`, `DeferredSurface`, `DeniedSurface`, and
  `MismatchDetectionSurface` lanes so the final closeout bundle now exposes the
  non-happy-path postures that the milestone spec requires instead of hiding
  them behind internal seeded/phase4 certification bundles. The new rows are
  assembled from the underlying seeded and hostile/runtime certification
  artifacts, aggregate real counters where the postures came from executable
  paths, and give `failure_digest` a source-honest final meaning. Certification
  tests now require the new lanes, require mismatch/deferred/denied failure
  evidence, and require `failure_digest` in the required output set. `cargo fmt
  --package worth-query`, focused `effect_lifecycle::tests::certification`, and
  full `cargo test -p worth-query --quiet` passed.
- Continued 9.3.3 with the next Phase 6 closeout artifact gap: proof-shape and
  performance certification are now first-class rows instead of only output
  digests. Added `effect_lifecycle/certification/closeout_meta.rs`, extended
  the unified lane enum with `ProofShapeSurface` and `PerformanceSurface`, and
  wired both rows into `certify_effect_execution_pipeline()`. The proof-shape
  row binds the audit digests for proof shape and phase progression, while the
  performance row binds all seven slope digests and aggregates counters across
  mutation, writeback, batch, and DX closeout surfaces so the row is backed by
  real work rather than a placeholder summary. The DX helper now carries its
  aggregated counters explicitly so closeout performance certification can reuse
  them honestly. Certification tests now require both rows, verify their detail
  contracts, and require the performance row to carry a non-default counter
  snapshot. `cargo fmt --package worth-query`, focused
  `effect_lifecycle::tests::certification`, and full
  `cargo test -p worth-query --quiet` passed cleanly.
- Ran a QA loop on the new closeout-meta slice and fixed two measurement
  honesty defects. First, the `PerformanceSurface` row had been double-counting
  mutation and batch work by folding in the full DX row counters even though
  those stories already reused mutation/batch evidence; the DX helper now
  carries `support_lookup_counters` separately and the performance row combines
  only unique mutation/writeback/batch counters plus the support-discovery
  counters. Second, the `ProofShapeSurface` row had only been certifying the
  mutation proof chain while presenting itself as the final effect-execution
  proof shape; closeout audits now include mutation, writeback, and batch
  raw->normalized->eligibility->plan->lowered->receipt->envelope evidence, and
  the proof-shape row now carries the widened counter snapshot. Certification
  tests now assert the proof/performance rows have distinct counter snapshots
  from each other and from the DX lane. `cargo fmt --package worth-query`,
  focused `effect_lifecycle::tests::certification`, and full
  `cargo test -p worth-query --quiet` passed cleanly after the corrections.
- Ran a hostile `$qa-tests` + `$code-quality-qa` pass over the 9.3.3
  `effect_lifecycle` certification/tests topology and fixed three structural
  violations instead of leaving them as commentary: the
  `src/effect_lifecycle/certification` directory was a flat 16-file pile, the
  `src/effect_lifecycle/tests` directory was a flat 14-file pile with an
  over-cap `foundation.rs`, and the trybuild support fixtures lived in an
  over-cap flat directory. Reorganized certification into `closeout/`,
  `phase4/`, and `seeded/` subdirectories; reorganized tests into
  `authoring/`, `execution/`, and `closeout/` families; split the
  foundation/support-contract tests into separate under-cap files; and moved
  UI support fixtures into `bundles/`, `inventory/`, and `receipts/`
  subdirectories with updated trybuild globs and refreshed stderr paths.
  `cargo fmt --package worth-query`, focused `effect_lifecycle` tests,
  effect-lifecycle phase-boundary trybuild, and full
  `cargo test -p worth-query --quiet` all passed after the refactor.
- Tightened the 9.3.3 spec after re-reading `_docs/more_guidelines/dx_laws.md`
  and noticing the DX contract under-described writeback. Updated
  `milestone-9.3.3.md` so the finished caller surface now explicitly requires
  bridge-backed writeback to read like the same Query-owned lifecycle as
  mutation/merge, added a concrete writeback common-path example, made
  denial/rebind expectations explicit for writeback, and hardened `Must Ship`,
  `Acceptance Evidence`, and `Closeout Standard` so mutation-only DX closure no
  longer qualifies as a complete 9.3.3 finish.
- Closed the last concrete 9.3.3 implementation blockers with a substantial
  Phase 6 batch. Added measured `batch_basis_reuse_count` and
  `authority_reopen_count` to `EffectLifecycleCounters`, kept the counter file
  under the workspace line cap via getter macro compression, added a
  production-owned `closeout_oracles` helper so the unified certification
  bundle now emits real `relational_oracle_digest` and
  `bridge_oracle_digest`, promoted seeded replay and exact counter outputs into
  the final closeout bundle, and expanded the golden DX transcript lane with an
  explicit bridge-backed writeback common-path story. Updated closeout
  certification tests to require the new story and the full named verification
  output set. `cargo fmt --package worth-query`, focused
  `effect_lifecycle::tests::certification` and `effect_lifecycle::tests::oracle`
  runs, `cargo test -p worth-query effect_lifecycle --quiet`, and full
  `cargo test -p worth-query --quiet` all passed.
- Ran the final hostile `$qa-loop` over the 9.3.3 closeout slice and found one
  real DX-certification honesty bug plus one related inventory softness. The
  new writeback DX transcript had claimed `using_basis(branch_head)` while the
  runtime-backed writeback evidence was actually tenant-scoped, and the public
  surface inventory still lacked an explicit writeback common-path row so the
  target-DX digest would not fail if writeback common-path support vanished.
  Corrected the transcript to `tenant_scoped`, added
  `EffectPublicSurfaceKind::WritebackCommonPath` plus its implemented inventory
  row, tightened the target-DX digest to require that row, and added inventory
  test coverage for the writeback common path. `cargo fmt --package
  worth-query`, focused closeout certification/inventory tests,
  `cargo test -p worth-query effect_lifecycle --quiet`, and the full
  `cargo test -p worth-query --quiet` suite all passed after the correction.
- Performed the final 9.3.3 closeout-readiness batch. Added
  `_docs/worth-query/milestone-9.3.3-closeout.md`, marked the 9.3.3 spec closed,
  and linked the closeout record from the roadmap so the milestone no longer
  sits in a contradictory draft-but-implemented state. This was a final-cleanup
  exception batch rather than new runtime behavior; it closes the milestone's
  documentation and roadmap boundary instead of changing code.

- Ran a final 9.3.3 closeout QA loop over the spec/test-requirement/closeout boundary. Found two doc-strength gaps: the named 9.3.3 suite still summarized DX in mutation-first terms even though writeback common-path closure is now required, and the closeout coverage map omitted the dedicated support test module. Tightened the named DX-story bullet to explicitly require both relational mutation/merge and bridge-backed writeback, added the support test module to the closeout coverage map, and made support/discovery certification explicit in the closeout proof summary.
- Ran a hostile `$qa-tests` + `$code-quality-qa` correction loop over the
  finished 9.3.3 effect-lifecycle surface. Found three real structural issues:
  `execution.rs` and `counters.rs` had drifted past the 400-line workspace cap,
  and `tests/ui/effect_lifecycle/proof` had exceeded the directory file-cap
  after the proof fixtures accumulated. Fixed the cap violations by splitting
  execution proof artifacts into `execution_artifacts.rs` and lifecycle counter
  profile constructors into `counter_profiles.rs`, then split the proof
  fixtures into `proof/construction` and `proof/postures` and updated the
  trybuild harness globs. Refreshed the moved `.stderr` files with
  `TRYBUILD=overwrite`, reran `cargo fmt --package worth-query`,
  `cargo test -p worth-query effect_lifecycle::tests::certification --quiet`,
  `cargo test -p worth-query --test phase_boundaries_effect_lifecycle_compile_fail --quiet`,
  `cargo test -p worth-query effect_lifecycle --quiet`, and full
  `cargo test -p worth-query --quiet`; all passed. Final structural scan:
  `execution.rs` 146 lines, `execution_artifacts.rs` 275, `counters.rs` 156,
  `counter_profiles.rs` 257, and the proof directory now has zero direct files
  with 6 in `construction/` and 8 in `postures/`.

- Drafted WORTH Query Milestone 9.3.4 as
  `_docs/worth-query/milestone-9.3.4.md`, defining declared projection
  consumption as a proof-bearing lifecycle from declaration through consumed
  fact receipt/envelope/certification. Updated the 9.3.4 roadmap section to
  point at the new spec and expanded
  `test-requirements-milestone-9_3-and-runtime-gates.md` with the named 9.3.4
  certification suite so the milestone is sequenced and test-bound instead of
  remaining a roadmap stub.
- Re-read the public facades for `worth-query`, `worth-runtime-bridge`,
  `worth-relational`, and `worth-signal` before tightening 9.3.4. Revised the
  spec to add an explicit `Required Crate Changes` section plus phase-by-phase
  crate work, making the intended boundary concrete: Query owns the new
  projection-consumption lifecycle, bridge/relational remain authority/source
  providers with only narrow allowed facade additions if a real gap appears,
  and signal stays out of consumed-fact ownership for 9.3.4.
- Ran a hostile QA pass over the 9.3.4 spec and tightened three soft spots:
  added an `Initial Source-Family Lock` so the first implementation slice is a
  closed set of concrete source artifacts instead of â€œany materialization,â€
  added a `Mechanical Enforcement Lock` naming the required compile-time proof
  boundaries, and made the common-path/DX and fact-accessor expectations more
  tangible. Also made the row-identity vs entity-identity trap explicit so we
  do not accidentally let host-side payload parsing become semantic authority.

- Refined milestone 9.3.4 with a new Target Finished Code Shape section grounded in dx_laws.md: common-path call sites, advanced phase-explicit call sites, typed denial/support posture, forbidden raw-row/raw-json ergonomics, and compile-time/autocomplete expectations for the public consumed-fact lifecycle.

- Reworked milestone 9.3.4 phase section into a mandatory sequential handoff chain: each phase now names allowed inputs, required output artifact, concrete engineer work, forbidden early work, and a completion gate that must pass before the next phase begins.

- Hardened the 9.3.4 named certification suite with explicit per-lane oracle posture, support-matrix traceability requirements, negative DX/public-boundary transcript requirements, and constrained randomized-generator metadata so production confidence does not depend on a vague harness notion of independence.

- Tightened milestone 9.3.4 eligibility semantics so warnings decorate admission (AdmittedWithWarnings) instead of existing as a standalone junk-drawer posture, and added a concrete support-matrix derivation mechanism based on the same source-family evaluators used by admission rather than a separately maintained derived table.
- Started 9.3.4 implementation with the first linear batch for Phase 1 and
  Phase 2. Added a new `projection_consumption` Query-owned subsystem with
  declaration authoring, closed fact-request taxonomy, source-family descriptors
  for read receipts / write receipts / query-context execution artifacts,
  binding context over authorized projection visibility, typed eligibility
  (`Admitted`, `AdmittedWithWarnings`, `Denied`, `Deferred`,
  `SourceMismatch`), and executable support discovery derived from the same
  evaluator seam used by admission. Exported the surface through the facade,
  added hostile unit coverage plus a compile-fail fixture proving declaration
  construction stays private, and kept contract binding / fact extraction /
  receipt / envelope work explicitly out of scope for the batch. `cargo fmt
  --all`, focused projection-consumption tests, compile-fail tests, and full
  `cargo test -p worth-query` passed.
- Ran a hostile `$qa-loop` over the first 9.3.4 foundation slice and corrected
  three real proof-boundary weaknesses. First, empty fact requests were being
  allowed to construct declarations, so declaration authoring now returns a
  typed `ProjectionConsumptionDeclarationError` and rejects `NoRequestedFacts`
  before a declaration exists. Second, source and binding result-shape meaning
  could drift silently, so declaration construction now rejects
  `SourceBindingResultShapeMismatch` when the source artifact and binding
  context do not agree on result-shape identity. Third, the source-mismatch API
  had misleading `expected()` / `actual()` accessors and support digests were
  collapsing warning/deferred detail, so the mismatch accessors now name
  `requested_fact_kind()` and `source_family()` honestly and support digests now
  include posture detail keys. Added unit coverage for empty-request rejection
  and source/binding mismatch rejection, exported the new declaration error
  through the facade, and reran `cargo fmt --all`, focused
  projection-consumption tests/trybuild, and full `cargo test -p worth-query`;
  all passed.
- Ran a combined `$qa-tests` + `$code-quality-qa` pass over the 9.3.4 Phase
  1/2 `projection_consumption` slice and corrected three additional weaknesses.
  First, the tests were still representative instead of architectural, so the
  module now has an exhaustive source-family Ã— fact-kind matrix test proving
  support discovery and executable eligibility stay synchronized across every
  current lane. Second, the projection-consumption trybuild boundary only
  guarded declaration privacy, so I added compile-fail fixtures for direct
  construction of eligibility proof artifacts and support report artifacts.
  Third, `support.rs` had an unnecessary extra dyn-wrapper trait layer that
  blurred the actual evaluator seam, so I collapsed it to one direct evaluator
  interface. Refreshed the trybuild stderr baselines, reran focused
  projection-consumption tests, the phase-boundary compile-fail suite, and full
  `cargo test -p worth-query`; all passed.
- QA-loop on WORTH Foundational Milestone 4 boundary artifact materialization found three issues and fixed them: profile materialization decisions were no longer encoded as attachment points, compatibility bridge origin is now explicit canonical debt rather than only a cost counter, and receipt-as-authority now produces the specific authority denial instead of being swallowed by the generic receipt-role denial. Reverified focused artifact certification, compile-fail boundaries, full `worth-foundational` tests, clippy with `-D warnings`, and `git diff --check`.
- Implementation batch corrected milestone order before further Milestone 4 work: added proof-bearing production-test readiness artifacts for Milestone 1 and Milestone 3, facade `certify_*`/`require_*` APIs, and compile-fail gates proving plain reports/prose and caller-minted readiness authorities cannot satisfy readiness-required APIs. Full `worth-foundational` tests, compile-fail suite, clippy with `-D warnings`, and diff checks passed.
- Implementation batch advanced WORTH Foundational Milestone 4 with planned-work and same-family descriptive artifact vocabulary: added private-field planned work and same-family composition artifacts, fail-closed Milestone 5 branch/merge/commit denials, canonical basis prep, facade exports, adversarial certification tests, and compile-fail gates proving plain artifacts cannot satisfy the new APIs. Focused certification tests, trybuild compile-fail suite, full `worth-foundational` tests, clippy with `-D warnings`, and diff checks passed.
- QA-loop on the latest Milestone 4 slice found and fixed two proof holes: planned-work canonical basis now embeds the wrapped materialized artifact basis so compatibility/profile/cost evidence cannot collapse, and same-family composition now rejects whitespace-only family/source/target symbols before canonical basis preparation. Reverified focused artifact certification, compile-fail boundaries, full `worth-foundational` tests, clippy with `-D warnings`, and diff checks.
- Implementation batch closed the WORTH Foundational Milestone 4 production-test readiness handoff: added artifact readiness vocabulary/inventory/report, a `worth-proof`-backed `FoundationalArtifactProductionTestReadyArtifact`, facade exports, readiness certification tests, and compile-fail gates proving prose, plain reports, and caller-minted readiness authority cannot satisfy readiness-required APIs. Split readiness inventory/report/vocabulary to keep files below the 400-line cap. Focused readiness certification, compile-fail suite, full `worth-foundational` tests, clippy with `-D warnings`, and diff checks passed.
- Implementation batch added role-gated authoritative boundary admission for WORTH Foundational Milestone 4: authoritative artifacts now pass through a checked wrapper before authority-required APIs, while support-only, projected, derived, planned, and receipt-bearing roles fail closed. The authoritative wrapper carries explicit admission evidence into canonical basis preparation, readiness inventory names the new hostile pressure, and a trybuild fixture proves support-only artifacts cannot satisfy authoritative boundary APIs. Full `worth-foundational` tests, compile-fail suite, clippy with `-D warnings`, and diff checks passed.
- QA-loop on the authoritative boundary admission slice found one readiness/spec honesty issue: the new authority admission gate was hidden under generic role separation and absent from the linear Milestone 4 phase gates. Fixed by adding explicit `AuthoritativeBoundaryAdmission` certified surface and phase gate before planned/same-family and production readiness. Rechecked focused artifact certification, compile-fail boundaries, full `worth-foundational` tests, clippy with `-D warnings`, and diff checks.
- Implementation batch hardened WORTH Foundational Milestone 4 production-test readiness evidence: added explicit synthetic runtime pressures, golden artifact evidence, property seed inventory, and fixture-manifest rows for artifact materialization/category/profile/authority/planned/same-family surfaces. The readiness checklist now enforces those fields and ties golden evidence to existing certification tests plus `worth-harness` expansion lanes. Focused readiness tests, compile-fail suite, full `worth-foundational` tests, clippy with `-D warnings`, line caps, and diff checks passed.
- Implementation batch split the Milestone 4 artifact readiness inventory out of one 358-line flat file into responsibility-owned inventory modules: surfaces, compile-fail evidence, production-test handoff, adoption assumptions/debt, phase gates, and path mapping. The public readiness report behavior stayed unchanged, but the next evidence edit now has an obvious home and each inventory file is well below the 400-line cap. Focused readiness certification, compile-fail suite, full `worth-foundational` tests, clippy with `-D warnings`, and diff checks passed.
- QA-loop on the split Milestone 4 readiness inventory found the remaining naive trap: evidence paths were still routed through raw filename strings with an `unknown.rs` fallback, and phase-gate order duplicated the evidence list. Fixed by making certification/UI evidence files typed enum variants with exhaustive path mappings and deriving the phase order from the phase evidence rows. Reverified focused readiness certification, compile-fail boundaries, full `worth-foundational` tests, clippy with `-D warnings`, line caps, and diff checks.
- Implementation batch started WORTH Foundational Milestone 5 with branch/merge/commit authority-transition vocabulary. Added a `transitions` responsibility home with typed branch/merge/commit ids, branch-local candidates, staged branch state, merge basis/verdict topology, proof-bearing committed-authority artifacts, commit parent/delta canonicalization, transition locators, branch/commit canonical-basis entries, facade exports, and trybuild gates proving branch candidates cannot satisfy committed-authority APIs and authority commit fields are private. Focused transition tests, full `worth-foundational` tests, clippy with `-D warnings`, line caps, and diff checks passed.
- Implementation batch advanced Milestone 5 with merge-verdict admission and commit-receipt boundaries. Added checked `FoundationalMergeCandidate` to `FoundationalMergeVerdict` admission so accepted/conflict/advisory/denied/superseded/stale-basis verdict shapes reject impossible conflict/resolution loci, added proof-bearing `FoundationalCommitReceiptArtifact` issuance from committed authority artifacts, canonical-basis lowering for merge verdicts and commit receipts, facade exports, hostile certification tests, and a trybuild gate proving merge candidates cannot satisfy commit-receipt APIs. Full `worth-foundational` tests, compile-fail suite, clippy with `-D warnings`, line caps, and diff checks passed.
- QA-loop on the Milestone 5 transition slice found two proof weaknesses and fixed them: commit receipts were too thin for blind consumers because they carried only commit id, branch id, and delta count, and transition canonical basis used Rust enum ordinals for visibility/parent/verdict categories. Receipts now carry parent-basis and committed-delta loci copied from the authority commit, receipt canonical basis includes that evidence, and transition canonical basis uses stable semantic text tokens for those categories. Focused transition certification, full `worth-foundational` tests, clippy with `-D warnings`, line caps, and diff checks passed.
- QA-tests pass over all WORTH Foundational tests since the previous test QA found three test-quality issues and fixed them: oversized flat trybuild UI directories, transition certification that used presence/count checks instead of exact canonical-basis assertions, and a newly over-400-line transition test file after hardening. Split UI fixtures into responsibility subfolders, updated readiness evidence paths and trybuild patterns, added exact branch/commit/merge canonical-basis assertions plus hostile branch producer parity, then split transition certification into branch/commit/merge/locator/helper modules. `cargo test -p worth-foundational`, compile-fail boundaries, clippy with `-D warnings`, line caps, directory caps, and diff checks passed.
- Code-quality QA on the latest WORTH Foundational structure found source-side topology debt: `canonicalization/basis` and `profiles` exceeded the direct-file directory cap, and `transitions/basis.rs` mixed branch, commit, receipt, merge, token, and entry construction responsibilities in one file. Fixed by splitting canonical basis into `grammar`, `construction`, and `readiness`; profiles into `vocabulary` and `artifact_binding`; and transition basis into branch/commit/receipt/merge/token/entry modules. Reblessed path-only trybuild snapshots, then reverified full `worth-foundational` tests, clippy with `-D warnings`, diff checks, and source/test line and directory caps.
- Recentered on WORTH Foundational Milestone 2 after the later-milestone detour. Split Milestone 2 canonicalization structural hotspots into responsibility homes (`equivalence`, export comparison, digest material, and contract preparation), added `_docs/worth-foundational/milestone-2-acceptance-matrix.md` to isolate the actual Milestone 2 acceptance surface, reblessed path-only trybuild snapshots after the move, and verified `cargo fmt -p worth-foundational`, full `cargo test -p worth-foundational`, compile-time boundaries, clippy with `-D warnings`, `git diff --check`, source/test line caps, and source/test direct-directory caps. Ready for the final Milestone 2 QA pass before closeout.
- Removed the uncommitted later-milestone implementation detour from `worth-foundational`: deleted the profile, boundary-artifact materialization, and branch/merge/commit transition homes plus their certification and UI fixtures; removed their facade/module/test harness exports; and stripped dormant profile/artifact/branch/merge/commit variants from the Milestone 2 canonical basis grammar and digest materialization. Verified the remaining crate surface has no later-milestone implementation symbol hits, and re-ran full `worth-foundational` tests, compile-time boundaries, clippy with `-D warnings`, diff checks, and line/directory caps.
- Final Milestone 2 QA using `qa-loop` and `qa-tests` found readiness evidence that was still too abstract: golden artifact evidence collapsed contract/mask/state/patch/compatibility under one broad bucket, and property seeds had no owning test or hostile-dimension evidence. Fixed by splitting golden evidence into exact surface rows, adding `CanonicalPropertySeedEvidence` with owning tests and harness lanes, strengthening readiness checklist/tests for exact fixture/property coverage, and updating the acceptance matrix from pending audit to final-QA-covered rows. Reverified full `worth-foundational` tests, clippy with `-D warnings`, diff checks, line caps, directory caps, and later-milestone residue search.
- Wrote `_docs/worth-foundational/milestone-2-closeout.md`, scoped to the canonical digest/canonicalization substrate. The closeout records completed surfaces, final QA fixes, proof evidence, verification counts, and explicit deferrals for crypto policy, profiles, artifacts/receipts, diagnostics, lineage/provenance, branch/merge/commit, performance/layout vocabulary, real runtime lowering, and generic harness/runtime work.
>>>>>>> origin/worth-foundational


- QA-loop on WORTH Foundational Milestone 4 boundary artifact materialization found three issues and fixed them: profile materialization decisions were no longer encoded as attachment points, compatibility bridge origin is now explicit canonical debt rather than only a cost counter, and receipt-as-authority now produces the specific authority denial instead of being swallowed by the generic receipt-role denial. Reverified focused artifact certification, compile-fail boundaries, full `worth-foundational` tests, clippy with `-D warnings`, and `git diff --check`.
- Implementation batch corrected milestone order before further Milestone 4 work: added proof-bearing production-test readiness artifacts for Milestone 1 and Milestone 3, facade `certify_*`/`require_*` APIs, and compile-fail gates proving plain reports/prose and caller-minted readiness authorities cannot satisfy readiness-required APIs. Full `worth-foundational` tests, compile-fail suite, clippy with `-D warnings`, and diff checks passed.
- Implementation batch advanced WORTH Foundational Milestone 4 with planned-work and same-family descriptive artifact vocabulary: added private-field planned work and same-family composition artifacts, fail-closed Milestone 5 branch/merge/commit denials, canonical basis prep, facade exports, adversarial certification tests, and compile-fail gates proving plain artifacts cannot satisfy the new APIs. Focused certification tests, trybuild compile-fail suite, full `worth-foundational` tests, clippy with `-D warnings`, and diff checks passed.
- QA-loop on the latest Milestone 4 slice found and fixed two proof holes: planned-work canonical basis now embeds the wrapped materialized artifact basis so compatibility/profile/cost evidence cannot collapse, and same-family composition now rejects whitespace-only family/source/target symbols before canonical basis preparation. Reverified focused artifact certification, compile-fail boundaries, full `worth-foundational` tests, clippy with `-D warnings`, and diff checks.
- Implementation batch closed the WORTH Foundational Milestone 4 production-test readiness handoff: added artifact readiness vocabulary/inventory/report, a `worth-proof`-backed `FoundationalArtifactProductionTestReadyArtifact`, facade exports, readiness certification tests, and compile-fail gates proving prose, plain reports, and caller-minted readiness authority cannot satisfy readiness-required APIs. Split readiness inventory/report/vocabulary to keep files below the 400-line cap. Focused readiness certification, compile-fail suite, full `worth-foundational` tests, clippy with `-D warnings`, and diff checks passed.
- Implementation batch added role-gated authoritative boundary admission for WORTH Foundational Milestone 4: authoritative artifacts now pass through a checked wrapper before authority-required APIs, while support-only, projected, derived, planned, and receipt-bearing roles fail closed. The authoritative wrapper carries explicit admission evidence into canonical basis preparation, readiness inventory names the new hostile pressure, and a trybuild fixture proves support-only artifacts cannot satisfy authoritative boundary APIs. Full `worth-foundational` tests, compile-fail suite, clippy with `-D warnings`, and diff checks passed.
- QA-loop on the authoritative boundary admission slice found one readiness/spec honesty issue: the new authority admission gate was hidden under generic role separation and absent from the linear Milestone 4 phase gates. Fixed by adding explicit `AuthoritativeBoundaryAdmission` certified surface and phase gate before planned/same-family and production readiness. Rechecked focused artifact certification, compile-fail boundaries, full `worth-foundational` tests, clippy with `-D warnings`, and diff checks.
- Implementation batch hardened WORTH Foundational Milestone 4 production-test readiness evidence: added explicit synthetic runtime pressures, golden artifact evidence, property seed inventory, and fixture-manifest rows for artifact materialization/category/profile/authority/planned/same-family surfaces. The readiness checklist now enforces those fields and ties golden evidence to existing certification tests plus `worth-harness` expansion lanes. Focused readiness tests, compile-fail suite, full `worth-foundational` tests, clippy with `-D warnings`, line caps, and diff checks passed.
- Implementation batch split the Milestone 4 artifact readiness inventory out of one 358-line flat file into responsibility-owned inventory modules: surfaces, compile-fail evidence, production-test handoff, adoption assumptions/debt, phase gates, and path mapping. The public readiness report behavior stayed unchanged, but the next evidence edit now has an obvious home and each inventory file is well below the 400-line cap. Focused readiness certification, compile-fail suite, full `worth-foundational` tests, clippy with `-D warnings`, and diff checks passed.
- QA-loop on the split Milestone 4 readiness inventory found the remaining naive trap: evidence paths were still routed through raw filename strings with an `unknown.rs` fallback, and phase-gate order duplicated the evidence list. Fixed by making certification/UI evidence files typed enum variants with exhaustive path mappings and deriving the phase order from the phase evidence rows. Reverified focused readiness certification, compile-fail boundaries, full `worth-foundational` tests, clippy with `-D warnings`, line caps, and diff checks.
- Implementation batch started WORTH Foundational Milestone 5 with branch/merge/commit authority-transition vocabulary. Added a `transitions` responsibility home with typed branch/merge/commit ids, branch-local candidates, staged branch state, merge basis/verdict topology, proof-bearing committed-authority artifacts, commit parent/delta canonicalization, transition locators, branch/commit canonical-basis entries, facade exports, and trybuild gates proving branch candidates cannot satisfy committed-authority APIs and authority commit fields are private. Focused transition tests, full `worth-foundational` tests, clippy with `-D warnings`, line caps, and diff checks passed.
- Implementation batch advanced Milestone 5 with merge-verdict admission and commit-receipt boundaries. Added checked `FoundationalMergeCandidate` to `FoundationalMergeVerdict` admission so accepted/conflict/advisory/denied/superseded/stale-basis verdict shapes reject impossible conflict/resolution loci, added proof-bearing `FoundationalCommitReceiptArtifact` issuance from committed authority artifacts, canonical-basis lowering for merge verdicts and commit receipts, facade exports, hostile certification tests, and a trybuild gate proving merge candidates cannot satisfy commit-receipt APIs. Full `worth-foundational` tests, compile-fail suite, clippy with `-D warnings`, line caps, and diff checks passed.
- QA-loop on the Milestone 5 transition slice found two proof weaknesses and fixed them: commit receipts were too thin for blind consumers because they carried only commit id, branch id, and delta count, and transition canonical basis used Rust enum ordinals for visibility/parent/verdict categories. Receipts now carry parent-basis and committed-delta loci copied from the authority commit, receipt canonical basis includes that evidence, and transition canonical basis uses stable semantic text tokens for those categories. Focused transition certification, full `worth-foundational` tests, clippy with `-D warnings`, line caps, and diff checks passed.
- QA-tests pass over all WORTH Foundational tests since the previous test QA found three test-quality issues and fixed them: oversized flat trybuild UI directories, transition certification that used presence/count checks instead of exact canonical-basis assertions, and a newly over-400-line transition test file after hardening. Split UI fixtures into responsibility subfolders, updated readiness evidence paths and trybuild patterns, added exact branch/commit/merge canonical-basis assertions plus hostile branch producer parity, then split transition certification into branch/commit/merge/locator/helper modules. `cargo test -p worth-foundational`, compile-fail boundaries, clippy with `-D warnings`, line caps, directory caps, and diff checks passed.
- Code-quality QA on the latest WORTH Foundational structure found source-side topology debt: `canonicalization/basis` and `profiles` exceeded the direct-file directory cap, and `transitions/basis.rs` mixed branch, commit, receipt, merge, token, and entry construction responsibilities in one file. Fixed by splitting canonical basis into `grammar`, `construction`, and `readiness`; profiles into `vocabulary` and `artifact_binding`; and transition basis into branch/commit/receipt/merge/token/entry modules. Reblessed path-only trybuild snapshots, then reverified full `worth-foundational` tests, clippy with `-D warnings`, diff checks, and source/test line and directory caps.
- Recentered on WORTH Foundational Milestone 2 after the later-milestone detour. Split Milestone 2 canonicalization structural hotspots into responsibility homes (`equivalence`, export comparison, digest material, and contract preparation), added `_docs/worth-foundational/milestone-2-acceptance-matrix.md` to isolate the actual Milestone 2 acceptance surface, reblessed path-only trybuild snapshots after the move, and verified `cargo fmt -p worth-foundational`, full `cargo test -p worth-foundational`, compile-time boundaries, clippy with `-D warnings`, `git diff --check`, source/test line caps, and source/test direct-directory caps. Ready for the final Milestone 2 QA pass before closeout.
- Removed the uncommitted later-milestone implementation detour from `worth-foundational`: deleted the profile, boundary-artifact materialization, and branch/merge/commit transition homes plus their certification and UI fixtures; removed their facade/module/test harness exports; and stripped dormant profile/artifact/branch/merge/commit variants from the Milestone 2 canonical basis grammar and digest materialization. Verified the remaining crate surface has no later-milestone implementation symbol hits, and re-ran full `worth-foundational` tests, compile-time boundaries, clippy with `-D warnings`, diff checks, and line/directory caps.
- Final Milestone 2 QA using `qa-loop` and `qa-tests` found readiness evidence that was still too abstract: golden artifact evidence collapsed contract/mask/state/patch/compatibility under one broad bucket, and property seeds had no owning test or hostile-dimension evidence. Fixed by splitting golden evidence into exact surface rows, adding `CanonicalPropertySeedEvidence` with owning tests and harness lanes, strengthening readiness checklist/tests for exact fixture/property coverage, and updating the acceptance matrix from pending audit to final-QA-covered rows. Reverified full `worth-foundational` tests, clippy with `-D warnings`, diff checks, line caps, directory caps, and later-milestone residue search.
- Wrote `_docs/worth-foundational/milestone-2-closeout.md`, scoped to the canonical digest/canonicalization substrate. The closeout records completed surfaces, final QA fixes, proof evidence, verification counts, and explicit deferrals for crypto policy, profiles, artifacts/receipts, diagnostics, lineage/provenance, branch/merge/commit, performance/layout vocabulary, real runtime lowering, and generic harness/runtime work.

- Continued 9.3.4 with a strict Phase 1 completion batch instead of widening Phase 2. Wired the dormant projection-consumption declaration-authoring module into the real public surface, exported ProjectionConsumptionAuthoringSurface and ProjectionConsumptionDeclarationBuilder, added explicit source-family coverage for relational row-set/grouped-projection and bridge row-set/grouped-truth authoring entry points, and added Query-owned declaration methods on read receipts, write receipts, and query-context execution artifacts. Hardened the boundary with a new trybuild fixture preventing external construction of authoring surfaces/builders, added Phase 1 authoring tests (including real relational artifact-backed declaration authoring), and made newly declared but not yet implemented source families fail closed as Deferred(SourceFamilySupportPending) rather than overclaiming support. cargo fmt --all, focused projection-consumption tests, the projection-consumption compile-fail suite, and full cargo test -p worth-query passed.
- Continued 9.3.4 with the real Phase 2 expansion batch. Replaced the blanket-deferred lower-runtime families with provenance-aware source capability profiles, so read receipts, write receipts, query-context executions, relational row sets/grouped projections, and bridge row sets/grouped truth views now expose executable first-slice admission behavior instead of family-only posture guesses. Write receipts now admit target identity, source reference, relation endpoint, and effect continuity only when the receipt actually carries that evidence; grouped sources admit membership/view-local identity but fail closed on entity identity; historical/preview-derived query context lanes fail closed where identity reopening would be dishonest. Added a dedicated Phase 2 hostile test module proving support discovery stays in sync with executable eligibility for real source instances, split projection source/provenance into a new `projection_consumption/source.rs` module to stay under the workspace line cap, and reran `cargo fmt --all`, `cargo test -p worth-query projection_consumption -- --nocapture`, `cargo test -p worth-query --test phase_boundaries_projection_consumption_compile_fail`, and full `cargo test -p worth-query`; all passed.
- Ran a hostile QA loop over the new 9.3.4 Phase 2 slice and corrected two semantic proof leaks. First, write receipts were overclaiming `source_reference` support by treating every receipt as if it carried provenance-backed source evidence; the source capability profile now admits source references only when the receipt actually has bridge provenance or symbolic target-reference evidence, and a hostile test now proves source-reference requests stay deferred otherwise. Second, query-context display-field requests were always tagged with `PreviewDerivedContext`, even on non-preview historical/current lanes; support evaluation now derives the warning from execution posture so non-preview payload-bound lanes report `QueryContextPayloadBound` honestly and only preview-derived lanes use the preview warning. Reran `cargo fmt --all`, focused projection-consumption tests, the projection-consumption compile-fail suite, and full `cargo test -p worth-query`; all passed. One parallel compile-fail run briefly hit a Windows linker file-lock on the test executable, but the rerun passed cleanly and no code change was needed for that tool artifact.
- Continued 9.3.4 with a real Phase 3 implementation batch for admitted contract binding. Added a new Query-owned `projection_consumption/contracts.rs` surface with `MaterializedProjectionContract`, `BoundProjectionFactFamily`, explicit source/support posture enums, and admitted-only `bind_contract()` on `AdmittedProjectionConsumption`. Extended the declaration binding context and authorized projection artifact so contracts carry canonical/narrowed result-shape digests, policy digest, tenant-schema basis digest, source identity, and source-reference identities. Added a dedicated Phase 3 test module plus compile-fail fixtures proving external callers cannot directly construct contracts and non-admitted eligibility results cannot bind them. Verified with `cargo fmt --all`, `cargo test -p worth-query projection_consumption -- --nocapture`, `cargo test -p worth-query --test phase_boundaries_projection_consumption_compile_fail`, and full `cargo test -p worth-query`; all passed.
- Continued 9.3.4 with the first honest Phase 4 extraction batch instead of jumping ahead to grouped/query-context lanes or receipts. Added a new semantic consumed-facts surface in `projection_consumption/consumed.rs` with family-specific fact accessors and exact extraction counters, plus source-specific extraction in `projection_consumption/extraction.rs` for relational row sets, bridge truth-view row-set surface reachability, and write-receipt aftermath/source-reference extraction. Row-set extraction now materializes entity identity, view-local identity, display-field, and derived-scalar facts from one frozen contract without reopening authority; write-receipt extraction now materializes target identity, source-reference, effect-continuity, and relation-endpoint facts from the carried evidence. Added hostile tests for missing field evidence and source-identity mismatch, plus compile-fail fixtures proving callers cannot directly construct a consumed fact set and cannot call a fake generic `contract.extract()`. Verified with `cargo fmt --all`, `cargo test -p worth-query projection_consumption -- --nocapture`, `cargo test -p worth-query --test phase_boundaries_projection_consumption_compile_fail`, and full `cargo test -p worth-query`; all passed.
- Ran a broader hostile sweep across all implemented 9.3.4 phases against the full milestone spec and the named test-requirements docs, not just the latest batch, and corrected two proof-strength gaps that were still too "good enough." First, declaration authoring could still pair a source with the wrong authorized projection so long as the caller handed over loose digests; `AuthorizedProjectionArtifact` now retains query/result-shape digests, `ProjectionConsumptionBindingContext` carries them, and declaration construction now rejects both source-vs-authorized-projection query drift and binding-vs-authorized-projection result-shape drift before a declaration exists. Second, query-context source-reference admission was overclaiming because the source family could admit `SourceReference` without binding any source-reference evidence; query-context sources now retain materialization-path / preview-provenance reference identities when present, support only admits source references when those identities actually exist, and Phase 3 contract tests now prove warning-bearing query-context contracts bind those identities. To keep the touched code within the workspace line-cap rules, I split source test-only constructors into `projection_consumption/source_test_support.rs` and brought `source.rs` back under 400 lines. Reran `cargo fmt --all`, `cargo test -p worth-query projection_consumption -- --nocapture`, `cargo test -p worth-query --test phase_boundaries_projection_consumption_compile_fail`, and full `cargo test -p worth-query`; all passed.
## 2026-05-13 - WORTH Query 9.3.4 Phase 4B remaining extraction sources

- Completed the remaining non-query-context Phase 4 extraction slice for `projection_consumption`.
- Split extraction into `extraction.rs`, `extraction_row_like.rs`, and `extraction_grouped.rs` to stay under the workspace line cap without hiding logic in a helper pile.
- Restored the strict row-set identity rule after the refactor: relational and bridge row sets now require explicit `"identity.id"` string evidence for `EntityIdentity`, while `ViewLocalIdentity` remains row-local.
- Reintroduced typed extraction failure for malformed declared field shapes via `ProjectionFactExtractionError::InvalidDeclaredFieldValueShape`.
- Added real extraction coverage for:
  - query read results
  - relational grouped projections
  - public reachability of bridge row-set and bridge grouped extraction entry points
- Added a new focused test module `tests_phase_four_remaining_sources.rs` so Phase 4 coverage stays under the line cap.
- Updated the projection-consumption compile-fail expectation after `ConsumedProjectionFactSet` gained the new `memberships` field.
- Verification passed:
  - `cargo fmt --all`
  - `cargo test -p worth-query projection_consumption -- --nocapture`
  - `cargo test -p worth-query --test phase_boundaries_projection_consumption_compile_fail`
  - `cargo test -p worth-query`

## 2026-05-13 - QA loop on 9.3.4 Phase 4B

- Hostile QA found two meaningful Phase 4 weaknesses and corrected them.
- Read-backed extraction had been proving only `read_graph_digest`, which allowed a contract to accept the wrong query/basis/result receipt tuple if the graph identity matched. Extraction now verifies the contract against the read receipt's `query_digest`, `basis_digest`, and `result_digest` and fails typed on drift.
- Grouped extraction counters were not source-surface exact. The grouped path now counts unique consumed row-local source surfaces instead of mirroring requested fact-family count.
- Strengthened tests:
  - read-backed extraction now uses the real `ProjectionConsumptionSource::from_read_receipt(...)` path with a shape-consistent binding
  - added a hostile read-receipt metadata drift test
  - added an exact grouped relation-endpoint width test
- Verification passed again:
  - `cargo fmt --all`
  - `cargo test -p worth-query projection_consumption -- --nocapture`
  - `cargo test -p worth-query --test phase_boundaries_projection_consumption_compile_fail`
  - `cargo test -p worth-query`

## 2026-05-13 - qa-tests + code-quality-qa structural cleanup on 9.3.4

- Ran a broader hostile test-harness and structure pass over `projection_consumption` after the Phase 4 extraction work.
- Corrected two real structural weaknesses:
  - the subsystem root had become a flat bucket, so extraction now lives under `projection_consumption/extraction/` and the phase tests now live under `projection_consumption/tests/`
  - the Phase 4 test files were duplicating declaration/admission/fixture builders, so the shared proof helpers now live in `tests/phase_four_support.rs`
- Also cleaned up grouped extraction readability so the exact source-surface counter math names `row_identity`, `member_identity`, and `grouping_value` surfaces explicitly instead of relying on one dense expression.
- Bridge extraction semantics are still only public-reachability-tested from `worth-query`; honest bridge materialization construction remains private to `worth-runtime-bridge`, so fuller semantic certification there would require a deliberately scoped lower-crate test-support seam rather than a Query-side workaround.
- Verification passed after the cleanup:
  - `cargo fmt --all`
  - `cargo test -p worth-query projection_consumption -- --nocapture`
  - `cargo test -p worth-query --test phase_boundaries_projection_consumption_compile_fail`
  - `cargo test -p worth-query`

## 2026-05-13 - 9.3.4 Phase 4C query-context extraction

- Finished the remaining Phase 4 extraction family by adding `MaterializedProjectionContract::extract_from_query_context_execution(...)`.
- Query-context extraction now:
  - validates query/basis/result/result-shape metadata against the bound contract
  - binds source identity the same way declaration/source binding does
  - extracts current/branch identity facts from query-context row identities
  - extracts display/derived facts as explicitly payload-bound string values
  - extracts source-reference facts from materialization-path / preview-provenance metadata
  - reports exact row-surface and source-evidence counters with zero authority reopen
- Split `query_context/execution.rs` under the workspace line cap by moving historical synthetic payload/result-digest helpers into `execution_synthetic.rs`, and added a narrow `execution_test_support.rs` constructor seam for honest unit certification.
- Added dedicated Phase 4 query-context tests for happy-path extraction, bound source-reference extraction, result-shape drift rejection, and missing source-reference rejection.
- Verification passed:
  - `cargo fmt --all`
  - `cargo test -p worth-query projection_consumption -- --nocapture`
  - `cargo test -p worth-query --test phase_boundaries_projection_consumption_compile_fail`
  - `cargo test -p worth-query`

## 2026-05-13 - 9.3.4 Phase 5 common-path receipt and envelope surface

- Continued 9.3.4 in order with the first real Phase 5 DX batch instead of
  stopping at manually issued receipts.
- Tightened `ConsumedProjectionFactSet` so receipt issuance is fully
  proof-derived:
  - the fact set now carries `source_identity`
  - `fact_set_digest` now binds `source_identity`
  - `issue_receipt()` no longer accepts a free caller-supplied source identity
- Strengthened the operational receipt/envelope surfaces:
  - `ProjectionConsumptionReceipt` now carries typed contract support posture
    and explicit deferred-neighbor summary
  - `SelfDescribingProjectionConsumptionEnvelope` now preserves the typed
    support posture and consumes the receipt's deferred-neighbor summary
  - renamed receipt envelope derivation to
    `projection_consumption_envelope()` so the public DX is honest instead of
    borrowing effect terminology
- Added a new `projection_consumption/dx.rs` common-path layer:
  - `ProjectionFactConsumptionAttempt`
  - `CompletedProjectionFactConsumption`
  - `ProjectionFactConsumptionPathError`
  - `WORTHQueryReadResult::consume_projection_facts(...)`
  - `WORTHQueryWriteReceipt::consume_projection_facts(...)`
  - `QueryContextExecutionArtifact::consume_projection_facts(...)`
  - source-surface support discovery helpers on read receipts, write receipts,
    and query-context execution artifacts
- Added Phase 5 tests covering:
  - proof-derived receipt issuance
  - receipt -> envelope derivation
  - common-path read-backed consumption
  - common-path write-backed consumption
  - warning-bearing common-path query-context consumption
  - support discovery helper reachability
- Refreshed the projection-consumption trybuild stderr fixtures after the
  stricter receipt/envelope/fact-set shapes added new private fields and typed
  support posture.
- Verification passed:
  - `cargo fmt --all`
  - `cargo test -p worth-query projection_consumption -- --nocapture`
  - `cargo test -p worth-query --test phase_boundaries_projection_consumption_compile_fail`
  - `cargo test -p worth-query`

## 2026-05-13 - QA loop on 9.3.4 Phase 5A

- Ran a hostile QA loop over the new Phase 5 common-path slice against the
  9.3.4 spec, named test requirements, and the core guideline docs.
- Corrected two meaningful weaknesses:
  - warning-bearing admission detail was being collapsed to a generic
    `AdmittedWithWarnings` posture once the contract/fact-set/receipt/envelope
    chain started, so Phase 5 now preserves concrete warning kinds through
    `ProjectionContractSupportPosture`, consumed fact-set digests, receipt
    digests, and envelope inspection accessors
  - the common-path DX tests only proved admitted stories, so they now also
    certify typed denial, typed deferred, and typed source-mismatch handling on
    the common path instead of leaving that part of the Phase 5 story social
- Added/updated tests for:
  - warning-kind preservation through receipt and envelope
  - common-path read denial for masked field requests
  - common-path write deferred posture when requested source-reference evidence
    is not actually carried
  - common-path write source-mismatch posture for unsupported fact families
- Verification passed after the correction loop:
  - `cargo fmt --all`
  - `cargo test -p worth-query projection_consumption -- --nocapture`
  - `cargo test -p worth-query --test phase_boundaries_projection_consumption_compile_fail`
  - `cargo test -p worth-query`

## 2026-05-13 - 9.3.4 Phase 5B golden DX and inspection closure

- Continued Milestone 9.3.4 in order and kept the work strictly inside Phase 5
  after the Phase 5A QA pass.
- Built receipt-first inspection helpers on the common-path result types in
  `projection_consumption/dx.rs`:
  - `CompletedProjectionFactConsumption` now exposes delegated inspection for
    source family, source identity, support posture, warning kinds, admitted
    fact-family count, extracted fact count, authority reopen count, deferred
    neighbors, and transition rules
  - `ProjectionFactConsumptionAttempt` now exposes typed inspection helpers for
    `completed`, `warnings`, `denied`, `deferred`, and `source_mismatch`
- Added compile-pass golden transcript fixtures for the intended public caller
  ergonomics:
  - common-path read-backed consumption
  - common-path write-backed consumption
  - common-path query-context consumption
  - explicit advanced-path declaration -> eligibility -> contract -> extraction
    -> receipt -> envelope flow
- Tightened the trybuild harness so compile-pass transcripts and compile-fail
  boundary fixtures are both deliberate instead of relying on one broad glob.
- Strengthened Phase 5 tests to prove:
  - the new inspection helpers are honest views over the same receipt chain
  - non-admitted attempts keep their typed posture through the helper surface
- Caught and fixed two implementation issues during verification:
  - a shadowed `authorized_projection(...)` helper name in Phase 5 tests
  - the compile-fail harness originally swept the new pass transcripts into the
    fail set
- Verification passed:
  - `cargo fmt --all`
  - `cargo test -p worth-query projection_consumption -- --nocapture`
  - `cargo test -p worth-query --test phase_boundaries_projection_consumption_compile_fail`
  - `cargo test -p worth-query`

## 2026-05-13 - 9.3.4 Phase 6A certification scaffold

- Started Phase 6 in order and kept the slice focused on executable support,
  boundary closure, proof-shape closure, and the first real certification
  bundle surface before the hostile topology/oracle closeout lane.
- Added a new Query-owned certification subdomain under
  `projection_consumption/certification` with:
  - executable certified source surfaces in `surfaces.rs`
  - behavior-derived family inventory and support matrix in `support_matrix.rs`
  - public-boundary audit in `boundary.rs`
  - proof-shape and phase-progression audit in `proof_shape.rs`
  - `ProjectionConsumptionCertificationBundle` and
    `certify_projection_consumption_closeout_core()` in `bundle.rs`
- Exported the new certification artifacts through
  `projection_consumption/mod.rs` and the public facade.
- Added a new compile-fail boundary fixture proving external callers cannot
  mint `ProjectionConsumptionCertificationBundle` or its nested certification
  artifacts directly.
- Caught and fixed two important issues during the batch:
  - the certification layer initially leaned on test-only source constructors,
    so the source module now exposes a crate-internal synthetic seam owned by
    production code
  - `support_matrix.rs` exceeded the workspace line cap, so the certified
    source-surface and traceability machinery was split into `surfaces.rs`
- The current closeout-core bundle emits digests for:
  - projection fact-family inventory
  - projection support matrix
  - projection public surface
  - target DX
  - golden transcripts
  - proof shape
  - phase progression
  - transition rules
  - compile-fail boundary
  - negative DX boundary
- Remaining Phase 6 work after this slice is the hostile topology/Worth lane,
  oracle manifest, seeded certification, and the final fully populated closeout
  bundle.
- Verification passed:
  - `cargo fmt --all`
  - `cargo test -p worth-query projection_consumption -- --nocapture`
  - `cargo test -p worth-query --test phase_boundaries_projection_consumption_compile_fail`
  - `cargo test -p worth-query`
