# 2026-05-13

- QA-loop on Forge Foundational Milestone 4 boundary artifact materialization found three issues and fixed them: profile materialization decisions were no longer encoded as attachment points, compatibility bridge origin is now explicit canonical debt rather than only a cost counter, and receipt-as-authority now produces the specific authority denial instead of being swallowed by the generic receipt-role denial. Reverified focused artifact certification, compile-fail boundaries, full `forge-foundational` tests, clippy with `-D warnings`, and `git diff --check`.
- Implementation batch corrected milestone order before further Milestone 4 work: added proof-bearing production-test readiness artifacts for Milestone 1 and Milestone 3, facade `certify_*`/`require_*` APIs, and compile-fail gates proving plain reports/prose and caller-minted readiness authorities cannot satisfy readiness-required APIs. Full `forge-foundational` tests, compile-fail suite, clippy with `-D warnings`, and diff checks passed.
- Implementation batch advanced Forge Foundational Milestone 4 with planned-work and same-family descriptive artifact vocabulary: added private-field planned work and same-family composition artifacts, fail-closed Milestone 5 branch/merge/commit denials, canonical basis prep, facade exports, adversarial certification tests, and compile-fail gates proving plain artifacts cannot satisfy the new APIs. Focused certification tests, trybuild compile-fail suite, full `forge-foundational` tests, clippy with `-D warnings`, and diff checks passed.
- QA-loop on the latest Milestone 4 slice found and fixed two proof holes: planned-work canonical basis now embeds the wrapped materialized artifact basis so compatibility/profile/cost evidence cannot collapse, and same-family composition now rejects whitespace-only family/source/target symbols before canonical basis preparation. Reverified focused artifact certification, compile-fail boundaries, full `forge-foundational` tests, clippy with `-D warnings`, and diff checks.
- Implementation batch closed the Forge Foundational Milestone 4 production-test readiness handoff: added artifact readiness vocabulary/inventory/report, a `forge-proof`-backed `FoundationalArtifactProductionTestReadyArtifact`, facade exports, readiness certification tests, and compile-fail gates proving prose, plain reports, and caller-minted readiness authority cannot satisfy readiness-required APIs. Split readiness inventory/report/vocabulary to keep files below the 400-line cap. Focused readiness certification, compile-fail suite, full `forge-foundational` tests, clippy with `-D warnings`, and diff checks passed.
- Implementation batch added role-gated authoritative boundary admission for Forge Foundational Milestone 4: authoritative artifacts now pass through a checked wrapper before authority-required APIs, while support-only, projected, derived, planned, and receipt-bearing roles fail closed. The authoritative wrapper carries explicit admission evidence into canonical basis preparation, readiness inventory names the new hostile pressure, and a trybuild fixture proves support-only artifacts cannot satisfy authoritative boundary APIs. Full `forge-foundational` tests, compile-fail suite, clippy with `-D warnings`, and diff checks passed.
- QA-loop on the authoritative boundary admission slice found one readiness/spec honesty issue: the new authority admission gate was hidden under generic role separation and absent from the linear Milestone 4 phase gates. Fixed by adding explicit `AuthoritativeBoundaryAdmission` certified surface and phase gate before planned/same-family and production readiness. Rechecked focused artifact certification, compile-fail boundaries, full `forge-foundational` tests, clippy with `-D warnings`, and diff checks.
- Implementation batch hardened Forge Foundational Milestone 4 production-test readiness evidence: added explicit synthetic runtime pressures, golden artifact evidence, property seed inventory, and fixture-manifest rows for artifact materialization/category/profile/authority/planned/same-family surfaces. The readiness checklist now enforces those fields and ties golden evidence to existing certification tests plus `forge-harness` expansion lanes. Focused readiness tests, compile-fail suite, full `forge-foundational` tests, clippy with `-D warnings`, line caps, and diff checks passed.
- Implementation batch split the Milestone 4 artifact readiness inventory out of one 358-line flat file into responsibility-owned inventory modules: surfaces, compile-fail evidence, production-test handoff, adoption assumptions/debt, phase gates, and path mapping. The public readiness report behavior stayed unchanged, but the next evidence edit now has an obvious home and each inventory file is well below the 400-line cap. Focused readiness certification, compile-fail suite, full `forge-foundational` tests, clippy with `-D warnings`, and diff checks passed.
- QA-loop on the split Milestone 4 readiness inventory found the remaining naive trap: evidence paths were still routed through raw filename strings with an `unknown.rs` fallback, and phase-gate order duplicated the evidence list. Fixed by making certification/UI evidence files typed enum variants with exhaustive path mappings and deriving the phase order from the phase evidence rows. Reverified focused readiness certification, compile-fail boundaries, full `forge-foundational` tests, clippy with `-D warnings`, line caps, and diff checks.
- Implementation batch started Forge Foundational Milestone 5 with branch/merge/commit authority-transition vocabulary. Added a `transitions` responsibility home with typed branch/merge/commit ids, branch-local candidates, staged branch state, merge basis/verdict topology, proof-bearing committed-authority artifacts, commit parent/delta canonicalization, transition locators, branch/commit canonical-basis entries, facade exports, and trybuild gates proving branch candidates cannot satisfy committed-authority APIs and authority commit fields are private. Focused transition tests, full `forge-foundational` tests, clippy with `-D warnings`, line caps, and diff checks passed.
- Implementation batch advanced Milestone 5 with merge-verdict admission and commit-receipt boundaries. Added checked `FoundationalMergeCandidate` to `FoundationalMergeVerdict` admission so accepted/conflict/advisory/denied/superseded/stale-basis verdict shapes reject impossible conflict/resolution loci, added proof-bearing `FoundationalCommitReceiptArtifact` issuance from committed authority artifacts, canonical-basis lowering for merge verdicts and commit receipts, facade exports, hostile certification tests, and a trybuild gate proving merge candidates cannot satisfy commit-receipt APIs. Full `forge-foundational` tests, compile-fail suite, clippy with `-D warnings`, line caps, and diff checks passed.
- QA-loop on the Milestone 5 transition slice found two proof weaknesses and fixed them: commit receipts were too thin for blind consumers because they carried only commit id, branch id, and delta count, and transition canonical basis used Rust enum ordinals for visibility/parent/verdict categories. Receipts now carry parent-basis and committed-delta loci copied from the authority commit, receipt canonical basis includes that evidence, and transition canonical basis uses stable semantic text tokens for those categories. Focused transition certification, full `forge-foundational` tests, clippy with `-D warnings`, line caps, and diff checks passed.
- QA-tests pass over all Forge Foundational tests since the previous test QA found three test-quality issues and fixed them: oversized flat trybuild UI directories, transition certification that used presence/count checks instead of exact canonical-basis assertions, and a newly over-400-line transition test file after hardening. Split UI fixtures into responsibility subfolders, updated readiness evidence paths and trybuild patterns, added exact branch/commit/merge canonical-basis assertions plus hostile branch producer parity, then split transition certification into branch/commit/merge/locator/helper modules. `cargo test -p forge-foundational`, compile-fail boundaries, clippy with `-D warnings`, line caps, directory caps, and diff checks passed.
- Code-quality QA on the latest Forge Foundational structure found source-side topology debt: `canonicalization/basis` and `profiles` exceeded the direct-file directory cap, and `transitions/basis.rs` mixed branch, commit, receipt, merge, token, and entry construction responsibilities in one file. Fixed by splitting canonical basis into `grammar`, `construction`, and `readiness`; profiles into `vocabulary` and `artifact_binding`; and transition basis into branch/commit/receipt/merge/token/entry modules. Reblessed path-only trybuild snapshots, then reverified full `forge-foundational` tests, clippy with `-D warnings`, diff checks, and source/test line and directory caps.
- Recentered on Forge Foundational Milestone 2 after the later-milestone detour. Split Milestone 2 canonicalization structural hotspots into responsibility homes (`equivalence`, export comparison, digest material, and contract preparation), added `_docs/forge-foundational/milestone-2-acceptance-matrix.md` to isolate the actual Milestone 2 acceptance surface, reblessed path-only trybuild snapshots after the move, and verified `cargo fmt -p forge-foundational`, full `cargo test -p forge-foundational`, compile-time boundaries, clippy with `-D warnings`, `git diff --check`, source/test line caps, and source/test direct-directory caps. Ready for the final Milestone 2 QA pass before closeout.
- Removed the uncommitted later-milestone implementation detour from `forge-foundational`: deleted the profile, boundary-artifact materialization, and branch/merge/commit transition homes plus their certification and UI fixtures; removed their facade/module/test harness exports; and stripped dormant profile/artifact/branch/merge/commit variants from the Milestone 2 canonical basis grammar and digest materialization. Verified the remaining crate surface has no later-milestone implementation symbol hits, and re-ran full `forge-foundational` tests, compile-time boundaries, clippy with `-D warnings`, diff checks, and line/directory caps.
- Final Milestone 2 QA using `qa-loop` and `qa-tests` found readiness evidence that was still too abstract: golden artifact evidence collapsed contract/mask/state/patch/compatibility under one broad bucket, and property seeds had no owning test or hostile-dimension evidence. Fixed by splitting golden evidence into exact surface rows, adding `CanonicalPropertySeedEvidence` with owning tests and harness lanes, strengthening readiness checklist/tests for exact fixture/property coverage, and updating the acceptance matrix from pending audit to final-QA-covered rows. Reverified full `forge-foundational` tests, clippy with `-D warnings`, diff checks, line caps, directory caps, and later-milestone residue search.
- Wrote `_docs/forge-foundational/milestone-2-closeout.md`, scoped to the canonical digest/canonicalization substrate. The closeout records completed surfaces, final QA fixes, proof evidence, verification counts, and explicit deferrals for crypto policy, profiles, artifacts/receipts, diagnostics, lineage/provenance, branch/merge/commit, performance/layout vocabulary, real runtime lowering, and generic harness/runtime work.
