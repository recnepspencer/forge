# 2026-04-27

- Forge Query Runtime API Public Stabilization Gate Phase 3A made async-safe state access executable through the canonical workspace facade. Added `ForgeQueryRuntimeStateTarget` and `workspace.state(...)` for live handles, computed handles, and facade-family support states; live/computed states now return digest-bound `Ready` snapshots from retained subscription/materialization evidence, while deferred temporal/async families return `Pending` snapshots with future `TemporalExecutionState`/`AsyncResourceState` lanes and unsupported families return `Unsupported` snapshots. The golden DX transcripts now use the public state boundary instead of manually forging ready evidence. Verified focused state/naming tests, runtime API stabilization harness, phase-boundary trybuild, `cargo check -p forge-query --tests`, full `cargo test -p forge-query`, and `git diff --check`.
- Forge Query Runtime API Public Stabilization Gate Phase 4A hardened public handle contracts and cleaned up computed naming. Added a sealed `ForgeQueryHandleContract` matrix covering live/computed/effect/receipt/preview/branch/future temporal-async handle families with authority lanes, required inspection sections, retained-artifact posture, deferred future posture, and digest binding to the public API support contract. Exposed it through `ForgeQueryRuntime::public_handle_contract()` and `workspace.public_handle_contract()`, exported the types through the facade, and added compile-fail privacy coverage. Renamed the raw workspace escape hatch from `computed_declaration` to `computed_definition` and removed the old phrase from internal computed inspection digest seeds, leaving only negative assertions that prove the old public name is absent. Verified formatting, focused naming/handle-contract tests, runtime API stabilization harness, phase-boundary trybuild, `cargo check -p forge-query --tests`, full `cargo test -p forge-query`, and `git diff --check`.
- QA on Phase 4A found the handle contract matrix still under-specified basis lanes and support posture, and silently omitted branch-bound handle reuse even though the stabilization spec/DX names it. Fixed by adding digest-bound `basis_lanes` and `support_status` fields/accessors to `ForgeQueryHandleContractRow`, requiring `basis-lane`, `support-posture`, `authority-lane`, and `inspection-digest` sections across every row, and adding an explicit unsupported `BranchBinding` row until branch handle reuse is genuinely implemented. Refreshed private-field trybuild proof and reverified focused handle-contract test, runtime API stabilization harness, phase-boundary trybuild, `cargo fmt -p forge-query --check`, `cargo check -p forge-query --tests`, full `cargo test -p forge-query`, and `git diff --check`.
- Continued Forge Signal aspect-capacity runtime policy migration with Phase 4B: capacity-typed planner apply lowering. Lowered task/execution carriers, prepared apply reuse resolution, serial stage apply inputs, deferred snapshot batching, and execution failure recording now carry graph capacity markers instead of falling back to the default aspect capacity. Added a u32 serial stage regression proving aspect 31 survives prepared dependency capture, serial lowering, batched apply, deferred snapshot publication, and committed dependency snapshots.
- Continued with Phase 4C: capacity-typed the stage scratch/finalize handoff for operational serial finalize. Added generic semantic finalize/reporting helpers and graph capacity diagnostics helpers, extended the u32 serial-stage regression to finalize task records/report counters, and documented the remaining retained diagnostic artifact fact model as a non-default-capacity non-claim.
- Ran a QA loop on Phase 4C. Fixed a parallel/default diagnostics regression where the default finalize path could call the generic retained-artifact blocker, collapsed duplicate serial finalize bodies behind an artifact-recorder callback, and fixed the u32 proof test so it compiles under `--features parallel`.
- Continued with Phase 4D: made `SignalRuntime` and `SignalRuntimeBuilder` carry a graph capacity marker through the runtime-owned graph field and builder typestate chain. Added a u32 runtime-builder proof showing aspect 31 survives graph construction into a built runtime without default-capacity collapse; transaction/branch/temporal runtime surfaces remain explicit next work.
- Continued with Phase 4E: made `SignalTransaction` carry the runtime graph capacity marker and made `SignalRuntime::begin` lend the capacity-typed graph into transaction state. Added a u32 transaction-begin proof showing aspect 31 survives into the staged graph; commit/evaluation remain default-capacity until their execution helpers are migrated.
- Continued with Phase 4F: made `ExecutionSnapshot` and `ExecutionReadView` capacity-typed and proved both the read-view finish path and runtime `prepare_with_context` helper preserve `AspectU32` high-aspect whole-node and partition-scoped dependency capture. Full planner/session execution was intentionally left default-capacity after compiler feedback showed adjacent contract, diagnostics, semantic finalize, and temporal prevalidation surfaces must migrate together.
- Continued with Phase 4G: made node contracts, evaluation conditions/config, condition evaluation context/resolvers, planner admission validation, and stage precompute/prevalidation carriers capacity-typed. Added u32 proofs that planner admission respects high-aspect contract/condition masks and that condition prevalidation defers a low-aspect filter without calling compute while preserving an aspect-31 dependency capture. Parallel precompute now uses capacity-generic telemetry/admission helpers, with capacity markers explicitly `Send + Sync`.
- Continued with Phase 4H: made planner execution, stage execution, graph execution facade, stage apply handoffs, execution reporting, and execution history projection capacity-typed. Added a u32 graph execution proof showing aspect 31 survives precompute, prepared evaluation, apply, report counters, and dirty cleanup. Restored retained diagnostics confidence by projecting history from capacity-typed graph state and making retained semantic artifacts fail loudly rather than truncate if a non-default capacity cannot fit the current diagnostic artifact model. Default and parallel compile checks are warning-clean; transaction commit/evaluation, branch/restore, temporal state, dynamic facade, and full non-default rich diagnostic artifact model remain next work.
- Continued with Phase 4I: made runtime execution and transaction evaluation facades capacity-typed, including shared runtime execution helpers, runtime/transaction request objects, graph GC/strategy bridges, dirty target collection, transaction staging/failure helpers, observation classification, temporal admission/evaluation helpers, and sparse rollback patch buffers. Added u32 proofs that both `SignalRuntime<AspectU32>` and `SignalTransaction<AspectU32>` can evaluate aspect 31 through their execution facades. Commit delivery remains an explicit next blocker because observation delivery and branch mutation ledgers are still default-capacity shaped; temporal previous-value references also remain default-capacity until temporal artifacts are genericized.
- Continued with Phase 4J: made the transaction commit/finalize/rollback boundary capacity-typed, genericized branch mutation ledgers and branch authority state over graph capacity, and let observation delivery carry capacity-derived policy/current-branch facts through the existing object-safe listener surface. Added u32 proofs that public transaction commit preserves aspect 31 into committed runtime graph state and that branch mutation ledger absorption preserves a high-aspect structural dependency delta. Full branch capture/restore, generic observer graph facades, and temporal previous-value artifacts remain the next honest blockers.
- Ran a QA loop over the recent Phase 4 capacity batches. Found and fixed a commit-boundary diagnostic regression: the generic commit path had made retained operational diagnostic fact recording a silent no-op. The corrected bridge preserves default-capacity retained explanation/provenance facts and rejects non-default retained operational diagnostic projections before commit side effects until the diagnostic artifact model is capacity-generic. Added default-retention and non-default-denial proof tests.
- Continued Forge Signal aspect-capacity runtime policy migration with Phase 4K: made observation listener/read context capacity-generic so non-default commit delivery no longer falls back to a default-only graph observer or panic boundary. Added a u32 listener proof where an observation callback reads aspect 31 through `ctx.graph()` after transaction commit. Kept the rich `RuntimeObserver` diagnostics/replay facade explicitly default-capacity because those artifact models still need their own generic migration.
- Fixed the Forge Signal Phase 4K regression fallout before continuing: updated the phase1 source-shape guard for generic packet impls, restored flow cause sampling by reconstructing compact retained explanation facts through a capacity-generic explanation resolver, and verified full `cargo test -p forge-signal` green again.
