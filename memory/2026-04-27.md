# 2026-04-27

- Forge Query Runtime API Public Stabilization Gate Phase 3A made async-safe state access executable through the canonical workspace facade. Added `ForgeQueryRuntimeStateTarget` and `workspace.state(...)` for live handles, computed handles, and facade-family support states; live/computed states now return digest-bound `Ready` snapshots from retained subscription/materialization evidence, while deferred temporal/async families return `Pending` snapshots with future `TemporalExecutionState`/`AsyncResourceState` lanes and unsupported families return `Unsupported` snapshots. The golden DX transcripts now use the public state boundary instead of manually forging ready evidence. Verified focused state/naming tests, runtime API stabilization harness, phase-boundary trybuild, `cargo check -p forge-query --tests`, full `cargo test -p forge-query`, and `git diff --check`.
- Forge Query Runtime API Public Stabilization Gate Phase 4A hardened public handle contracts and cleaned up computed naming. Added a sealed `ForgeQueryHandleContract` matrix covering live/computed/effect/receipt/preview/branch/future temporal-async handle families with authority lanes, required inspection sections, retained-artifact posture, deferred future posture, and digest binding to the public API support contract. Exposed it through `ForgeQueryRuntime::public_handle_contract()` and `workspace.public_handle_contract()`, exported the types through the facade, and added compile-fail privacy coverage. Renamed the raw workspace escape hatch from `computed_declaration` to `computed_definition` and removed the old phrase from internal computed inspection digest seeds, leaving only negative assertions that prove the old public name is absent. Verified formatting, focused naming/handle-contract tests, runtime API stabilization harness, phase-boundary trybuild, `cargo check -p forge-query --tests`, full `cargo test -p forge-query`, and `git diff --check`.
- QA on Phase 4A found the handle contract matrix still under-specified basis lanes and support posture, and silently omitted branch-bound handle reuse even though the stabilization spec/DX names it. Fixed by adding digest-bound `basis_lanes` and `support_status` fields/accessors to `ForgeQueryHandleContractRow`, requiring `basis-lane`, `support-posture`, `authority-lane`, and `inspection-digest` sections across every row, and adding an explicit unsupported `BranchBinding` row until branch handle reuse is genuinely implemented. Refreshed private-field trybuild proof and reverified focused handle-contract test, runtime API stabilization harness, phase-boundary trybuild, `cargo fmt -p forge-query --check`, `cargo check -p forge-query --tests`, full `cargo test -p forge-query`, and `git diff --check`.
- Forge Query Runtime API Public Stabilization Phase 5 added the public support matrix and fail-closed admission gate. Introduced sealed `ForgeQueryRuntimePublicSupportMatrix` / row evidence with stable rows, explicit deferred rows for temporal basis (9.4), async resources (9.5), mixed delivery (9.6), temporal/async certification (9.7), store parity (10), and durable restart/reload (11), all marked as extending the stabilized handle/state/lane/aspect/inspection facade with sibling public APIs forbidden. Added `ForgeQueryRuntime::public_support_matrix`, `workspace.public_support_matrix`, and `admit_public_api_family` so supported rows return sealed contracts while deferred/unsupported families deny typed and early. Updated runtime stabilization evidence counters and private-field trybuild proof. Verified formatting, focused support-matrix tests, runtime API stabilization harness, phase-boundary trybuild, and full `cargo test -p forge-query`.
- Forge Query Runtime API Public Stabilization Phase 6 added closeout certification and migration readiness. The runtime API stabilization artifact now includes a derived `RuntimeApiStabilizationCloseout` with stable/deferred/unsupported surfaces, compatibility-name guidance, safe-to-build-now assumptions, must-not-assume-yet boundaries, required verification commands, self-check answers, and a closeout digest derived from the golden transcript matrix plus support/handle/naming contracts. Added closeout tests requiring explicit answers for executable public-facade transcripts, typed future-neighbor denials, zero lower-runtime plumbing, support/admission synchronization, extension-ready handle/state/inspection contracts, and deferred temporal/async/store/durable scope. Added `_docs/forge-query/runtime-api-public-stabilization-closeout.md` as the downstream dependency contract and marked the stabilization plan as closeout implemented. Verified formatting, targeted closeout/support tests, `cargo check -p forge-query --tests`, phase-boundary trybuild, and full `cargo test -p forge-query`.
- QA on the Phase 6 closeout pass found three real issues before signoff: the closeout doc over-emphasized `workspace.intent(...)` while omitting stable `workspace.write(...)`, the doc was not mechanically tied to the certified harness artifact, and `runtime_api_stabilization/mod.rs` had turned into a mixed-responsibility god file. Fixed by splitting closeout derivation into `crates/forge-query/src/harness/runtime_api_stabilization/closeout.rs`, correcting the closeout contract doc to name `workspace.write(...)` as stable and `workspace.intent(...)` as admitted vocabulary but not yet compatibility-stable, and adding a test that binds the closeout markdown to the certified contract rows, deferred milestone owners, verification commands, and fail-closed wording. Reverified `cargo test -p forge-query runtime_api_stabilization`, `cargo check -p forge-query --tests`, full `cargo test -p forge-query`, and `git diff --check`.
- Added a feature-first Forge Query documentation index at `_docs/forge-query/reference/README.md` to close the gap between milestone/closeout docs and actual feature usage guidance. The index defines the non-negotiable rule that every stable public feature gets its own reference doc, establishes a canonical doc matrix for live views, computed, effects, subscriptions, branches/previews, state, inspection, aspects/authority lanes, support/admission, and broader query capability families, and explicitly calls out documentation as infrastructure debt rather than polish.
- Authored and installed a new `feature-doc-writer` skill in both the workspace (`skills/feature-doc-writer`) and Codex home (`C:\Users\Esther\.codex\skills\feature-doc-writer`). The skill is for product-facing feature documentation rather than milestone/engineering prose, and it encodes the workflow derived from Angular, Laravel, OpenAI, and Next.js docs: concept-first mental model, task-first stable entry points, small and realistic examples, explicit anti-patterns, support-boundary honesty, and a QA pass against real public surfaces/tests. Bundled references include a doc template and a QA checklist.
- Wrote the first four product-facing Forge Query feature docs directly under `crates/forge-query/docs`: `workspace-overview.md`, `live-views.md`, `computed.md`, and `effects.md`. These docs are feature-first rather than milestone-first, grounded in the stabilized workspace facade plus runtime support/profile tests, and explicitly preserve the public support boundary that effect declaration is stable while authoritative intent execution remains gated.
- Wrote the next four product-facing Forge Query feature docs under `crates/forge-query/docs`: `reads-observe-materialize.md`, `writes-and-intents.md`, `branches-and-previews.md`, and `state.md`. These docs explicitly distinguish stable direct-write and preview-local surfaces from support-gated intent execution, preserve the derive-only/default policy story for preview and branch sessions, and keep `state(...)` framed as a typed posture surface rather than domain data access.
- Wrote the next four Forge Query feature docs under `crates/forge-query/docs`: `inspection.md`, `aspects-and-authority-lanes.md`, `support-matrix-and-admission.md`, and `query-expressions-and-result-shapes.md`. These docs anchor inspection in the real sealed artifact families, make authority-lane vocabulary and aspect contracts explicit, spell out the fail-closed support/admission posture from the public stabilization closeout, and document query/result-shape authoring as an authoring-layer feature distinct from the ordinary runtime workspace facade.
- Wrote the next five Forge Query feature docs under crates/forge-query/docs: schema-validation.md, historical-diff-and-basis.md, lineage-and-correspondence.md, scopes-templates-saved-queries-and-view-shapes.md, and subscription-selection-and-diagnostics.md. Grounded them in the real implementation and strongest tests, then did a cleanup pass to remove helper drift and tighten examples around actual exported constructors, outcome accessors, and support boundaries for historical/store-backed/subscription lifecycle behavior.
- Added crates/forge-query/docs/minimal-crud-runtime-example.md as a short product-facing example showing the smallest believable Cars + People CRUD slice on the stabilized workspace facade: live views for each entity, one computed UI surface, direct writes, and touched-only routing evidence through write receipts and observation/materialization rather than hand-built backend/cache wiring.
- Wrote `_docs/forge-query/aspect-api-finalization-plan.md` to freeze Forge Query's public mutation story before the deeper aspect-native substrate rewrite. The plan explicitly stabilizes aspect-native create/update/batch/delete authoring, receipt/inspection alignment, support-matrix honesty, doc/example replacement, and compatibility closure while treating JSON-backed lowering as temporary internal debt and leaving `forge-relational` / store / bridge storage removal as the next layer underneath.
