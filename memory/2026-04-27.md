# 2026-04-27

- Forge Store S.0 Phase 1 added the audit input manifest and scan-budget vocabulary: declared scan roots, matched input files, input file digests, breadth summary, scan cost surface, manifest witness, delta accounting, typed scan-scope/stale-manifest rejections, and manifest counter projection. Roots reject workspace-global, absolute, generated, target, vendor, and similar broad/generated scopes; manifests reject duplicate paths and files outside admitted roots; manifests sort deterministically and produce a stable digest; deltas classify reused/rescanned/added/removed files. Added trybuild proof that `S0InputManifestWitness` cannot be forged externally. Verified focused S.0 tests, phase-boundary trybuild, full `cargo test -p forge-store`, and `git diff --check`.
- Forge Store S.0 Phase 1 added roadmap sequence consistency and prior-milestone physical-status vocabulary: `RoadmapSequenceStatusMatrix`, milestone spec/closeout declarations, prerequisite edges with typed waivers, sequence inconsistency classification, gate-readiness witnesses, semantic-vs-physical claim families, and `MilestonePhysicalStatusRow` with illegal pre-gate `PlatformGrade` rejection plus required deferred-sequence mapping for physical debt. Added focused runtime tests for mismatch/unclosed-prerequisite/gate blocking/typed waiver behavior and trybuild proof that `RoadmapGateReadinessWitness` cannot be forged externally. Verified focused S.0 tests, phase-boundary trybuild, full `cargo test -p forge-store`, and `git diff --check`.
- QA loop on the new S.0 milestone and counter surfaces found two foundation issues and fixed them immediately. First, `MilestoneStatusDeclaration::new` was rejecting closed milestones with missing predecessor evidence too early, which made the spec-required `MissingGatePredecessorEvidence` inconsistency impossible to represent and audit; the constructor now permits that shape and `RoadmapSequenceStatusMatrix` reports and blocks it. Second, `S0CounterSnapshot::with_milestone_status_rows` was pretending to count forbidden platform-grade claims by inspecting impossible empty milestone ids; replaced that with honest projections for roadmap edges, sequence inconsistencies, mismatch classes, row counts, and semantic-vs-physical claim counts. Added focused runtime coverage for missing predecessor evidence and honest counter projection. Verified `cargo fmt -p forge-store`, focused S.0 tests, phase-boundary trybuild, full `cargo test -p forge-store`, and then reran `git diff --check`.
- Forge Store S.0 Phase 1 added the semantic-versus-physical claim report and deferred physical guarantee map. Implemented `SemanticPhysicalClaimReport`, `SemanticPhysicalClaimReportRow`, `SemanticPhysicalClaimStatus`, `DeferredPhysicalGuaranteeMap`, `DeferredPhysicalGuaranteeRow`, and `DeferredPhysicalGuaranteeCategory`, all with deterministic canonical JSON write/validate gates and shared artifact-envelope/cost-surface reuse. Milestone rows now project into claim-family rows and concrete deferred guarantee rows, and deferred guarantees reject when the required anchor `S.*` sequence is missing for the category. Added counters integration for claim-report projection and unmapped deferred-guarantee tracking, plus trybuild proofs that claim-report and deferred-guarantee rows cannot be forged externally. Verified `cargo fmt -p forge-store`, focused `storage_foundation::s0` tests, `cargo test -p forge-store --test phase_boundaries_compile_fail`, full `cargo test -p forge-store`, and `git diff --check`.
- Forge Store S.0 Phase 1 added the deterministic terminology scanner, terminology risk report, and release/public claim gate. Implemented manifest-bounded scan plans and inputs, line-scoped allowlist classification for risky phrases, stable finding ids and report digests, canonical JSON write/validate gates, and release-surface filtering that turns public overclaim findings into typed rejections. Added honest counter projection for terminology findings and release/public claim rejections, plus trybuild proof that terminology finding rows cannot be forged externally. Verified `cargo fmt -p forge-store`, focused `storage_foundation::s0` tests, `cargo test -p forge-store --test phase_boundaries_compile_fail`, full `cargo test -p forge-store`, and `git diff --check` (clean aside from existing LF/CRLF warnings).
- QA loop on the S.0 terminology/release-gate slice found and fixed two foundation issues plus one silent ambiguity trap. First, terminology scan scopes were being treated as exact file paths instead of declared roots, which violated the shared scan-root model and could falsely reject honest nested scope inputs; scan paths are now normalized and admitted by root-prefix membership. Second, `ReleaseClaimReport::from_terminology_report` could claim configured release surfaces were scanned just because they were listed in the plan, even if the terminology report never covered them; the gate now rejects with `UnscannedReleaseSurface` unless every configured surface is actually present in the scanned report. Third, duplicate allowlist entries for the same `(path, line, phrase)` were silently overwritten in a `BTreeMap`; terminology scan now rejects them typed with `DuplicateAllowlistEntry`. Also corrected terminology counters to project unique evidence refs from deduplicated refs rather than assuming one per row. Added focused tests for nested-root scope admission, duplicate allowlist rejection, and missing release-surface coverage rejection. Verified `cargo fmt -p forge-store`, focused `storage_foundation::s0` tests, `cargo test -p forge-store --test phase_boundaries_compile_fail`, full `cargo test -p forge-store`, and `git diff --check` (clean aside from existing LF/CRLF warnings).
- Forge Store S.0 Phase 1 added the next evidence-bearing closeout lane: `TestMigrationNotes`, `HarnessMaturityReport`, and `StorageFoundationS1Handoff`. Implemented canonical JSON write/validate gates, deterministic digests, and raw-parse reconstruction for these artifacts so proof-bearing types remain sealed while still importable through machine-checkable schema gates. Migration notes now derive named-suite scope and admission status from milestone physical rows; harness maturity now enforces the required S.1 baseline subsystems and evidence-bundle readiness classification; S.1 handoff now requires shared accepted provenance across backend matrix, deferred guarantee map, terminology report, audit manifest, and complexity report, rejects stale inputs and public overclaim carryover, records required compile-time boundary fixtures and non-platform-grade debt, and exposes blocking predicates instead of relying on prose. Added trybuild proof that `StorageFoundationS1Handoff` cannot be forged externally, plus focused runtime tests for migration classification, harness maturity baseline rows, stale accepted-input rejection, and tampered handoff JSON rejection. Verified `cargo fmt -p forge-store`, focused `storage_foundation::s0` tests, `cargo test -p forge-store --test phase_boundaries_compile_fail`, full `cargo test -p forge-store`, and `git diff --check`.
- Forge Store S.0 Phase 1 added the canonical milestone physical-status matrix artifact and the first machine-checkable S.0 evidence bundle. Implemented `MilestonePhysicalStatusMatrix` with canonical JSON write/validate, deterministic digesting that includes roadmap sequence status plus prior-milestone rows, required-row completeness rejection, and typed hostile parse/tamper rejection. Implemented `S0EvidenceBundle`, certification matrix rows, evidence provenance, staleness rejection, regeneration requirement, accepted bundle witness, counter integration, and failure digest construction so S.0 can now certify the accepted artifact set instead of only holding the pieces in memory. Added focused tests for milestone-matrix round-trip and missing-row rejection, evidence-bundle stale complexity-digest rejection, and evidence-bundle JSON round-trip. Exported the new surfaces through the S.0 facade and crate root. Verified `cargo fmt -p forge-store`, focused `cargo test -p forge-store storage_foundation::s0`, `cargo test -p forge-store --test phase_boundaries_compile_fail`, full `cargo test -p forge-store`, and `git diff --check` (clean aside from existing LF/CRLF warnings).
- QA loop on the new S.0 milestone-matrix and evidence-bundle slice found four foundation honesty issues and fixed them before moving on. First, evidence provenance persisted a fake self digest (`pending:self`) inside the canonical artifact set; provenance now stores only upstream artifact digests, while bundle validation accounts for the bundle artifact separately. Second, certification rows were carrying a synthetic placeholder evidence ref instead of real artifact-backed refs; each row family now points at concrete upstream artifact digests via a shared `artifact_ref` helper. Third, evidence-bundle JSON validation was only trusting the outer digest, which meant semantic fields like `failure_digest`, artifact validation, and provenance could be edited and then re-digested; validation now recomputes and rejects `FailureDigestMismatch`, `ProvenanceMismatch`, and `ArtifactValidationMismatch`. Fourth, milestone-matrix JSON import rebuilt rows without reattaching gate-readiness witnesses, which would have caused honest future `PlatformGrade` rows to fail on parse; parse now derives the witness from the embedded roadmap sequence status before validating rows. Added focused tests for platform-grade milestone round-trip and failure-digest tamper rejection. Verified focused `storage_foundation::s0` tests, `cargo test -p forge-store --test phase_boundaries_compile_fail`, full `cargo test -p forge-store`, and `git diff --check` (clean aside from existing LF/CRLF warnings).
- QA loop on S.0 with an explicit god-file/domain-law focus found two structural violations and corrected them. First, [handoff.rs](C:/Users/Esther/Documents/Programming/forge_workspace/worktree_2/crates/forge-store/src/storage_foundation/s0/handoff.rs) was carrying both harness-maturity and S.1 handoff responsibilities even though the S.0 spec names them as separate artifact families that change and fail for different reasons; split the production code into [harness.rs](C:/Users/Esther/Documents/Programming/forge_workspace/worktree_2/crates/forge-store/src/storage_foundation/s0/harness.rs) and a slimmer [handoff.rs](C:/Users/Esther/Documents/Programming/forge_workspace/worktree_2/crates/forge-store/src/storage_foundation/s0/handoff.rs), then rewired exports/imports accordingly. Second, [mod.rs](C:/Users/Esther/Documents/Programming/forge_workspace/worktree_2/crates/forge-store/src/storage_foundation/s0/mod.rs) had become a 95 KB mega-test file, violating the domain-law rule that tests must teach the same decomposition as production; replaced the inline test block with a dedicated `tests/` module tree (`support.rs`, `artifact_manifest.rs`, `milestones_claims.rs`, `terminology_migration.rs`, `handoff_evidence.rs`, `capability.rs`) so S.0 tests now live beside the responsibilities they certify instead of one catch-all module. During the split, corrected one harness-readiness fence so `BackendTierFenceEnforcement` measures actual tier fencing rather than requiring debt-free platform rows. Verified `cargo fmt -p forge-store`, focused `cargo test -p forge-store storage_foundation::s0`, `cargo test -p forge-store --test phase_boundaries_compile_fail`, full `cargo test -p forge-store`, and `git diff --check` (clean aside from existing LF/CRLF warnings).
- Another god-file QA pass found one remaining production god file and one remaining test junk drawer, and both were split cleanly. [evidence.rs](C:/Users/Esther/Documents/Programming/forge_workspace/worktree_2/crates/forge-store/src/storage_foundation/s0/evidence.rs) was still mixing shared evidence/artifact vocabulary with the S.0 evidence-bundle runner, provenance, certification rows, and parse/build logic; split that into a slim shared-vocabulary [evidence.rs](C:/Users/Esther/Documents/Programming/forge_workspace/worktree_2/crates/forge-store/src/storage_foundation/s0/evidence.rs) plus bundle-specific [bundle.rs](C:/Users/Esther/Documents/Programming/forge_workspace/worktree_2/crates/forge-store/src/storage_foundation/s0/bundle.rs), then rewired the S.0 facade exports. The remaining combined test slice [handoff_evidence.rs](C:/Users/Esther/Documents/Programming/forge_workspace/worktree_2/crates/forge-store/src/storage_foundation/s0/tests/handoff_evidence.rs) also violated the same responsibility boundary by spanning harness/handoff, milestone-matrix, and evidence-bundle concerns; replaced it with [harness_handoff.rs](C:/Users/Esther/Documents/Programming/forge_workspace/worktree_2/crates/forge-store/src/storage_foundation/s0/tests/harness_handoff.rs), [milestone_matrix.rs](C:/Users/Esther/Documents/Programming/forge_workspace/worktree_2/crates/forge-store/src/storage_foundation/s0/tests/milestone_matrix.rs), and [evidence_bundle.rs](C:/Users/Esther/Documents/Programming/forge_workspace/worktree_2/crates/forge-store/src/storage_foundation/s0/tests/evidence_bundle.rs). Verified focused `cargo test -p forge-store storage_foundation::s0`, `cargo fmt -p forge-store`, `cargo test -p forge-store --test phase_boundaries_compile_fail`, full `cargo test -p forge-store`, and `git diff --check` (only existing LF/CRLF warnings from Git).
