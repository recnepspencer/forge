- Forge relational aspect refactor: continued hard-break migration. Current batch tightened entity field patch application denial so field-level foundational apply failures carry an `AspectFieldLocator`; whole-aspect apply failures use a separate aspect-key denial variant. Verified with `cargo check -p forge-relational --tests` and focused entity field patch denial tests. Remaining unrelated warning: unused `diagnostic_label` in `validation/data/custom_rule/errors.rs`.
- Next batch made publication `PatchRecord` carry typed `PublishedAuthoritativePatch` operations derived from `AuthoritativeRecordAspectPatch`, so durable patch records no longer only preserve the compatibility aspect set. Verified with `cargo check -p forge-relational --tests` and focused patch/entity-field publication tests.
- Follow-up batch moved history, inspection, bridge publication, and commit delta reporting off `PatchRecord.aspects` and onto authoritative published patch operations. A focused history test exposed that supplied authoritative patches were missing lifecycle supplements; fixed producer-side by combining supplied patches with non-authoritative canonical delta supplements before publication. Verified with `cargo check -p forge-relational --tests`, `cargo test -p forge-relational commit_inspection_is_canonical_and_not_story_shaped -- --nocapture`, `cargo test -p forge-relational merge_commit_inspection_stays_envelope_projected -- --nocapture`, and `cargo test -p forge-relational record_local_aspect_history_reads_committed_patch_truth -- --nocapture`. Existing unrelated warning remains: unused `diagnostic_label`.
- Next batch removed validation-local JSON reads from planned unique entity aspect-field evaluation. `unique_entity_fields/planned_field_values.rs` now asks authority mutation ingress for `ContractValidatedAspectArtifact`s and extracts `AspectValue` from validated payloads, while JSON compatibility remains confined to `payload_aspect_compatibility_ingress`. Verified with `cargo check -p forge-relational --tests` and `cargo test -p forge-relational complexity_budget_commit_boundary_unique_invariant_uses_merged_plan_lookup -- --nocapture`. Existing unrelated warning remains: unused `diagnostic_label`.
- Follow-up verification against the reported aspect-native regressions found the current tree already fixed the two-codec and JSON failure-path issues: `commit_strategies/data/native_codec.rs` delegates aspect values and field patches to `aspect_wire`, `UpdateEntityFieldsIntent` failure paths carry typed `EntityFieldAspectPatchDenial`, and field update planning applies foundational validated values/field-level patches. Verified with `cargo check -p forge-relational --tests`, `cargo test -p forge-relational aspect_value_canonical_codec -- --nocapture`, `cargo test -p forge-relational update_entity_fields -- --nocapture`, and `cargo test -p forge-relational entity_field_patch_application_denial_carries_contract_field_locator -- --nocapture`. Existing unrelated warning remains: unused `diagnostic_label`.
- Read-record payload annihilation batch removed `payload` from `EntityReadRecord` and `RelationReadRecord`. Materialized read/query surfaces now expose authoritative aspect state and field comparison keys; merge/adoption and payload retention tests use explicit storage payload access only where legacy payload persistence is the subject. Verified with `cargo check -p forge-relational --tests` and focused payload retention/profile tests. Existing unrelated warning remains: unused `diagnostic_label`.
- Invariant diagnostic batch moved `RelationalDiagnosticFields` to a typed diagnostic-value root with JSON retained only as a projection. The aspect-bearing unique-entity-field invariant diagnostic now stores `AspectKey`, `FieldPath`, `AspectValue`, and foundational `DiagnosticMask` as typed values instead of erasing them through serializable JSON DTOs. Verified with `cargo check -p forge-relational --tests` and `cargo test -p forge-relational diagnostic_projection -- --nocapture`. Existing unrelated warning remains: unused `diagnostic_label`.
- Diagnostic constructor hard-break batch removed implicit JSON conversion into `RelationalDiagnosticFields`: deleted `from_value`, `From<Value>`, and generic entry construction from raw `Into` at `RelationalDiagnosticsEntry::new`. Producers now either pass typed `RelationalDiagnosticValue` fields or explicitly call compatibility-named constructors (`from_compatibility_json`, `from_compatibility_serializable`, `from_compatibility_serializable_fields`). Verified with `cargo check -p forge-relational --tests`, `cargo test -p forge-relational diagnostic_projection -- --nocapture`, and `cargo test -p forge-relational filter_artifact_canonicalizes_nested_field_order -- --nocapture`. Existing unrelated warning remains: unused `diagnostic_label`.
- Aspect trace diagnostics batch removed compatibility JSON serialization from schema declaration/lowering traces and transaction evaluation/emission traces. Trace diagnostic artifacts now build typed `RelationalDiagnosticValue` trees carrying `AspectKey`, `FieldKey`, `AspectValue`, and `StructAspectValue`; projection helpers were split into responsibility-named `aspect_traces/diagnostic_fields.rs` child modules to keep trace data files below the 400-line cap. Verified with `cargo fmt -p forge-relational`, `cargo check -p forge-relational --tests`, `cargo test -p forge-relational detailed_trace_profile_emits_commit_side_aspect_trace_diagnostics -- --nocapture`, and `cargo test -p forge-relational schema_and_runtime_expose_consistent_aspect_declaration_and_lowering_traces -- --nocapture`. Existing unrelated warning remains: unused `diagnostic_label`.
- Mutation effect diagnostics batch removed compatibility serialization from `authority/mutation/effect_diagnostics.rs`. `RelationalDiagnosticValue` now has typed identity carriers for `PartitionId`, `KindId`, `EntityId`, and `RelationId`; mutation event diagnostics emit typed record identity fields directly instead of serde DTOs. Verified with `cargo fmt -p forge-relational`, `cargo check -p forge-relational --tests`, and `cargo test -p forge-relational aspect_traces_and_diagnostics_are_stable_across_supported_execution_models -- --nocapture`. Existing unrelated warning remains: unused `diagnostic_label`.
- Commit/history diagnostic batch removed JSON/serde diagnostic constructors from commit publication/history, savepoint rollback, replay retention, and aspect-history resolution traces. Added typed `VersionId` and `LineageId` diagnostic carriers; aspect-history trace projection moved into `history/data/aspect_history/diagnostic_fields.rs` so the history data file stays under the 400-line cap. Verified with `cargo fmt -p forge-relational`, `cargo check -p forge-relational --tests`, `cargo test -p forge-relational record_local_aspect_history_reads_committed_patch_truth -- --nocapture`, and `cargo test -p forge-relational aspect_history_digest_counts_roundtrip_as_u64 -- --nocapture`. Existing unrelated warning remains: unused `diagnostic_label`.
- Schema-continuity diagnostic batch removed serde/JSON compatibility construction from `authority/commit/phases/schema_continuity_diagnostics` and the test-only commit diff-preparation failure injector. Added typed diagnostic carriers for branch/schema/descriptor identity, converted schema diff field names through foundational `FieldKey` law, and split the schema-continuity diagnostic directory into shape DTOs, value terms, field conversion, and trace-entry assembly files under the line cap. Verified with `cargo fmt -p forge-relational`, `cargo check -p forge-relational --tests`, `cargo test -p forge-relational explicit_schema_transition_is_lowered_into_canonical_commit_artifacts -- --nocapture`, and `cargo test -p forge-relational declared_schema_transition_rejects_wrong_target_basis -- --nocapture`. Existing unrelated warning remains: unused `diagnostic_label`.
- Merge execution diagnostic batch removed serde compatibility DTOs from merge execution diagnostics. Replaced `merge/logic/execution_diagnostics.rs` with a directory split into orchestration, summary fields, record rows, aspect value fields, and policy boundary fields; merge diagnostics now emit typed `CommitId`, `BranchId`, record ids, foundational `AspectKey`, and `AspectValue` carriers. `RelationalDiagnosticFields` equality now compares projected diagnostic meaning so typed live diagnostics remain equal to persisted/recovered JSON projections without making JSON canonical. Verified with `cargo fmt -p forge-relational`, `cargo check -p forge-relational --tests`, `cargo test -p forge-relational execute_prepared_merge_reports_execution_counters_and_structural_summary -- --nocapture`, `cargo test -p forge-relational execute_prepared_merge_records_attempt_without_success_on_failure -- --nocapture`, and `cargo test -p forge-relational execute_prepared_merge_survives_durability_append_and_recovery -- --nocapture`. Existing unrelated warning remains: unused `diagnostic_label`.
- Publication/replay/index/retention diagnostic batch removed post-authority JSON diagnostic construction from the touched production emitters. Publication post-commit/failure diagnostics, replay comparison diagnostics, derived-index build diagnostics, and retention plan diagnostics now build `RelationalDiagnosticValue` trees directly, using typed `CommitId`, `BranchId`, `VersionId`, `SnapshotId`, `DerivedIndexId`, and `DerivedIndexGenerationId` carriers where those identities matter. Structural QA collapsed the retention diagnostic DTO into a responsibility-named diagnostic builder. Verified with `cargo fmt -p forge-relational`, `cargo check -p forge-relational --tests`, `cargo test -p forge-relational diagnostics_and_replay_are_emitted_for_commit -- --nocapture`, `cargo test -p forge-relational replay_contract_reports_derived_index_drift_at_digest_layer_when_artifacts_are_tampered -- --nocapture`, `cargo test -p forge-relational retention_plan_reports_explicit_replay_pins_for_deleted_relations_until_released -- --nocapture`, and `cargo test -p forge-relational derived_index_contract_failure_unknown_index_keeps_truth_reads_correct -- --nocapture`. Existing unrelated warning remains: unused `diagnostic_label`.
- CDC subscriber diagnostic batch removed compatibility serde diagnostic construction from `publication/cdc/diagnostics`. Subscriber request rejection, checkpoint resolution, recovery decision, continuation summary, continuation rejection, contract-upgrade, renegotiation, and boundary diagnostics now emit typed `RelationalDiagnosticValue` fields; added typed diagnostic carriers for `PatchStreamPosition` and `ReplaySchemaVersion`. Structural QA split continuation diagnostics into `continuation_artifacts/mod.rs`, `contract_fields.rs`, `boundary_fields.rs`, and `diagnostic_terms.rs` so orchestration, contract fields, boundary fields, and label terms are separate. Verified with `cargo fmt -p forge-relational`, `cargo check -p forge-relational --tests`, `cargo test -p forge-relational unsupported_continuation_failure_counts_current_boundary_when_no_prior_proof_exists -- --nocapture`, `cargo test -p forge-relational subscriber_stream_rejects_zero_batch_size -- --nocapture`, `cargo test -p forge-relational subscriber_stream_recovers_from_durable_canonical_envelopes_when_patch_stream_index_outlives_envelope -- --nocapture`, and `cargo test -p forge-relational subscriber_stream_applies_contract_upgrade_when_declared_supported -- --nocapture`. Existing unrelated warning remains: unused `diagnostic_label`.
- Durability authority diagnostic batch removed compatibility serde/JSON construction from `durability/authority/diagnostics`. Recovery compatibility, checkpoint selection, replay range, checkpoint creation, compaction, and segment append diagnostics now build typed `RelationalDiagnosticValue` trees with durable checkpoint ids, durable segment ids, commit ids, contract ids, verification layers, and recovery mismatch detail. Structural QA split recovery diagnostics into orchestration, compatibility fields, mismatch fields, and durable identity field terms to keep files under the 400-line cap. Verified with `cargo fmt -p forge-relational`, `cargo check -p forge-relational --tests`, `cargo test -p forge-relational durability_recovery_emits_compatibility_diagnostic_before_execution`, and `cargo test -p forge-relational durability_contract_compaction_only_removes_segments_covered_by_checkpoint`. Existing unrelated warning remains: unused `diagnostic_label`.
- Visibility read-path diagnostic batch removed compatibility serde DTOs from `visibility/materialization/read_records/reader/diagnostics.rs`. Snapshot read-path inspection, published snapshot handle reads, cache decisions, and cache misses now emit typed `RelationalDiagnosticValue` fields for `SnapshotId`, `VersionId`, booleans, counts, and read-policy labels. A focused test caught that the typed rewrite initially used the stale handle version instead of the authoritative published binding version; fixed the builder to use the resolved snapshot version. Verified with `cargo fmt -p forge-relational`, `cargo test -p forge-relational published_snapshot_read_diagnostics_use_authoritative_binding_version`, and `cargo check -p forge-relational --tests`. Existing unrelated warning remains: unused `diagnostic_label`.
- Simulation compiled-artifact diagnostic batch removed production JSON diagnostic construction from `simulation/logic/authority.rs`. `compile_execution_artifact` now names lane eligibility, source envelope lookup, artifact construction, and diagnostic publication steps; compiled artifact diagnostics emit typed `RelationalDiagnosticValue` fields for artifact id, source commit/version/branch ids, partition ids, topology freeze mode, and compiled record count. Verified with `cargo fmt -p forge-relational`, `cargo check -p forge-relational --tests`, and `cargo test -p forge-relational compiled_artifact`. Existing unrelated warning remains: unused `diagnostic_label`.
- Explicit payload compatibility-ingress batch removed blanket `From<serde_json::Value>` constructors for `RecordPayload` and `RecordPayloadImage`; JSON payload callers now use `from_compatibility_json` so compatibility input is visible instead of masquerading as native construction. Structural QA simplified remaining image-level call sites and confirmed no hidden serde_json payload conversions remain by grep. Verified with `cargo fmt -p forge-relational`, `cargo check -p forge-relational --tests`, and a focused `cargo test -p forge-relational record_payload_image` earlier in the batch. Existing unrelated warning remains: unused `diagnostic_label`.
- Custom invariant validation aspect-state batch removed validation-facing payload views: `StructuralPayloadView`, `payload_views()`, `entity_payload_view`, `relation_payload_view`, and planned-create payload accessors are gone. Custom invariant planner/execution contexts now expose `StructuralAspectStateView` over `AuthoritativeRecordAspectState`, and sparse overlay tests now prove base-partition read-through uses authoritative aspect state rather than structured payload fields. Verified with `cargo fmt -p forge-relational`, `cargo check -p forge-relational --tests`, `cargo test -p forge-relational sparse_speculative_overlay_reads_untouched -- --nocapture`, `cargo test -p forge-relational custom_registration -- --nocapture`, and `cargo test -p forge-relational touched_scope_tracks -- --nocapture`. Existing unrelated warning remains: unused `diagnostic_label`.
- Public payload surface hard-break batch removed `StructuredPayloadDocument` from `facade::payloads`, deleted implicit `StructuredPayloadDocument -> RecordPayload/RecordPayloadImage` constructors, and made `RecordPayload` opaque with explicit `from_compatibility_json` and `opaque_bytes` constructors instead of public JSON-shaped enum variants. The cfg(test) harness fixture boundary now stores raw `serde_json::Value` and lowers through `RecordPayload::from_compatibility_json`; structured field observation helpers are test-only residue rather than production/public payload API. Verified with `cargo fmt -p forge-relational`, `cargo check -p forge-relational --tests`, `cargo test -p forge-relational record_payload_image -- --nocapture`, and `cargo test -p forge-relational derive_merge_commit_mutation_plan_emits_source_authorized_create_intent -- --nocapture`. Existing unrelated warning remains: unused `diagnostic_label`.
- Lineage diagnostic compatibility-constructor hard-break batch removed public diagnostic JSON/serializable authoring constructors from `RelationalDiagnosticFields` and deleted the unused serializable-entry shortcut. Lineage candidate/promotion diagnostics now build typed `RelationalDiagnosticValue` objects through `lineage/logic/authority/diagnostic_fields.rs`, including a typed `CorrespondenceCandidateId` diagnostic carrier, instead of `serde_json::json!` payloads. JSON diagnostic projection remains private to serde deserialization and legacy compatibility projection only. Verified with `cargo fmt -p forge-relational`, `cargo check -p forge-relational --tests`, `cargo test -p forge-relational lineage_promotion_execution_reports_operational_anchor_drift_after_plan_lowering -- --nocapture`, and `cargo test -p forge-relational filter_artifact_canonicalizes_nested_field_order -- --nocapture`. Existing unrelated warning remains: unused `diagnostic_label`.
- Invariant violation diagnostic projection hard-break batch removed the final `CompatibilityJson` fallback from `validation/logic/invariant_authority/invariant_violation_diagnostic_projection`. Every invariant violation variant now projects as an explicit typed `RelationalDiagnosticValue` object using contract ids, kind ids, partition ids, entity/relation ids, field keys, diagnostic masks, aspect values, and label terms instead of serializing the projection enum. Split relation projection and shared typed value helpers into child modules under the 400-line cap. Verified with `cargo fmt -p forge-relational`, `cargo check -p forge-relational --tests`, and `cargo test -p forge-relational diagnostic_projection -- --nocapture`. Existing unrelated warning remains: unused `diagnostic_label`.
- Custom invariant provenance diagnostic hard-break batch removed the remaining serde-to-`CompatibilityJson` projection from `validation/logic/invariant_authority/diagnostic_projection.rs`. Custom invariant failure and trace diagnostics now project provenance through typed `RelationalDiagnosticValue` fields for observation kind, version ids, touched entity/relation/partition ids, structural counts, and traversal budget summary. Added `custom_invariant_provenance_diagnostic_projection.rs` and moved diagnostic projection tests into a sibling test file to keep production files under the line cap. Verified no `CompatibilityJson`/serde projection residue remains in `validation/logic/invariant_authority`, then ran `cargo fmt -p forge-relational`, `cargo check -p forge-relational --tests`, and `cargo test -p forge-relational diagnostic_projection -- --nocapture` (4 focused tests passed). Existing unrelated warning remains: unused `diagnostic_label`.
- Central diagnostic carrier hard-break batch deleted `RelationalDiagnosticValue::CompatibilityJson` from `diagnostics/data/fields.rs`. Persisted/deserialized diagnostic field JSON now rebuilds a bounded typed diagnostic tree (`Null`, booleans, signed/unsigned numbers, strings, arrays, objects) while retaining projected JSON only as presentation/recovery projection, not as a generic machine-truth carrier. Verified `rg` finds no `CompatibilityJson`, `RelationalDiagnosticValue::Compatibility`, or compatibility serialized diagnostic helpers anywhere in `crates/forge-relational/src`; `fields.rs` remains under the line cap. Ran `cargo fmt -p forge-relational`, `cargo check -p forge-relational --tests`, `cargo test -p forge-relational diagnostic_projection -- --nocapture`, and `cargo test -p forge-relational filter_artifact_canonicalizes_nested_field_order -- --nocapture`. Existing unrelated warning remains: unused `diagnostic_label`.
- Relation schema payload-class annihilation batch deleted `RelationPayloadClass` and all `payload_class`/`relation_payload_class` schema, merge, facade, example, validation harness, and test constructor surfaces. Relation field declarations now stand or fall only by `AspectBinding::RelationField` plus scalar/struct foundational `AspectContract` shape; the old topology-only rejection proof became a contract-shape proof. Verified `rg` finds no relation payload-class residue, `cargo fmt -p forge-relational`, `cargo check -p forge-relational --tests`, `cargo test -p forge-relational aspect_value_canonical_codec -- --nocapture`, and `cargo test -p forge-relational schema::aspects -- --nocapture`. Existing unrelated warning remains: unused `diagnostic_label`.
- Aspect precision duplicate-deletion batch removed relational `AspectPrecision` and the surviving degraded/opaque precision vocabulary from the trace/publication/history/report seam. Schema lowering and commit evaluation now carry foundational `AspectShape`; diagnostics project it through one shared typed `aspect_shape_diagnostic_value`; durable patch/history/report fields use `contains_opaque_aspect` / `opaque_aspect_*`. Verification passed: `cargo fmt -p forge-relational`, `cargo check -p forge-relational --tests`, `cargo test -p forge-relational aspect_emission_traces_use_indexed_target_lookup -- --nocapture`, `cargo test -p forge-relational detailed_trace_profile_emits_commit_side_aspect_trace_diagnostics -- --nocapture`, `cargo test -p forge-relational schema_and_runtime_expose_consistent_aspect_declaration_and_lowering_traces -- --nocapture`, and `cargo test -p forge-relational record_local_aspect_history_reads_committed_patch_truth -- --nocapture`. Existing unrelated warning remains: unused `diagnostic_label`.
- Mutation outcome payload-image excision batch removed `RecordPayloadImage`/payload snapshots from the post-authority mutation carrier. `RecordMutation`/`MutationOutcome` now carry authoritative aspect state and/or authoritative patches for entity and relation effects; relation lifecycle snapshots no longer preserve payload images as downstream evidence, and canonical-delta fixtures prove deltas materialize from authoritative state/patch artifacts rather than payload fallback. Verified with `cargo fmt -p forge-relational`, `cargo check -p forge-relational --tests`, and focused canonical delta tests: `entity_field_delta_requires_authoritative_state`, `entity_field_delta_materializes_authoritative_patch_not_payload`, and `relation_field_delta_materializes_authoritative_state_not_payload`. Structural QA found no payload-image residue in outcome/effect/canonical carriers and all touched production files stayed under 400 lines. Existing unrelated warning remains: unused `diagnostic_label`.
- Canonical delta evidence locator hard-break batch deleted the canonical-delta `BindingEvidence` and `AuthoritativePatchOperationEvidence` vocabulary. `CanonicalRecordAspectDelta` rows now carry `CanonicalAspectDeltaEvidence` with foundational `AspectValueLocator` / `AspectFieldLocator` at `LocatorAuthority::Authoritative`, and authoritative patch-derived field operations carry field locators rather than anonymous field-key tuples. Trace projection remains an outward view over this locator-bearing evidence. Verified with `cargo fmt -p forge-relational`, `cargo check -p forge-relational --tests`, and focused canonical delta tests: `entity_field_delta_requires_authoritative_state`, `entity_field_delta_materializes_authoritative_patch_not_payload`, `relation_field_delta_materializes_authoritative_state_not_payload`, `scalar_changed_binding_materializes_foundational_whole_aspect_set`, and `aspect_traces_and_diagnostics_are_stable_across_supported_execution_models`. Structural QA found no deleted evidence-name residue, no payload/JSON terms in `authority/mutation/canonical_deltas`, and all touched files stayed under 400 lines. Existing unrelated warning remains: unused `diagnostic_label`.
- Reported regression verification batch confirmed the current tree already fixes the user-reported aspect codec and update-field issues. `AspectFieldPatch` serde and native strategy codecs both route through shared `aspect_wire`; native codec returns `NativeCodecError` instead of panicking; update-field denial paths carry typed foundational locators. Verified with `cargo check -p forge-relational --tests`, `cargo test -p forge-relational aspect_value_canonical_codec -- --nocapture`, `cargo test -p forge-relational update_entity_fields -- --nocapture`, and `cargo test -p forge-relational entity_field_patch_application_denial_carries_contract_field_locator -- --nocapture`. Next high-blast seam remains transaction create/update intent payload carriers (`EntitySpec`, `BulkEntityCreateIntent`, `RelationSpec`, `BulkRelationCreateIntent`, `UpdateEntityIntent`) still pushing `RecordPayloadImage` into authority ingress.
- Transaction payload-image duplicate deletion batch removed `RecordPayloadImage` entirely. Transaction declarations, bulk import staging packets, authority create/bulk paths, replay fixtures, facade payload exports, and fintech/performance fixtures now use `RecordPayload` directly, so the remaining legacy seam is explicit instead of hidden behind an image/snapshot wrapper. Verified no `RecordPayloadImage`, `record_payload_image`, or `as_payload()` residue remains under `crates/forge-relational/src`; touched production files remain below the 400-line cap. Ran `cargo fmt -p forge-relational`, `cargo check -p forge-relational --tests`, and `cargo test -p forge-relational derive_merge_commit_mutation_plan_emits_source_authorized_create_intent -- --nocapture`. Existing unrelated warning remains: unused `diagnostic_label`. Next hard-break target: replace the now-exposed `RecordPayload` mutation intent/core authority path with foundational `AuthoritativeRecordAspectState` / `AuthoritativeRecordAspectPatch` and delete structured payload rescans from authority ingress.
- Entity payload-update hard-break batch deleted `UpdateEntityIntent`, `EntityMutationIntent::Update`, `authority/mutation/intents/update_entity.rs`, and the now-dead payload replacement/payload-update helpers. Entity updates now compile only through `UpdateEntityFieldsIntent` carrying `AspectFieldPatch`, which plans and applies foundational `AuthoritativeRecordAspectPatch` operations. Updated staging, merge planning, touched-scope, invariant, replay, strategy, and test fixtures to use field patches. Verified no `UpdateEntityIntent`, `EntityMutationIntent::Update(`, `update_entity.rs`, `entity_payload_replacement_patch`, or `apply_payload_update` residue remains under `crates/forge-relational/src`. Ran `cargo fmt -p forge-relational`, `cargo check -p forge-relational --tests`, and `cargo test -p forge-relational update_entity_fields -- --nocapture` (7 tests passed). Existing unrelated warning remains: unused `diagnostic_label`.
- Entity create/replacement hard-break batch removed `RecordPayload` from `EntitySpec`, replaced `BulkEntityCreateIntent.payloads` with `field_patches`, and deleted `ReplaceEntityIntent.replacement_fields`. Entity create, bulk create, and replacement now enter authority through `AspectFieldPatch` declarations, contract validation, authoritative state admission, and whole-aspect patch construction. Removed entity payload compatibility admission helpers; split creation planning into field classification, contract validation, state admission, and creation patch files. Merge adoption, unique planned-field validation, strategies, presentation fixture ingress, and tests now use field-patch declarations for entity creates. Verified `cargo fmt -p forge-relational`, `cargo check -p forge-relational --tests`, `cargo test -p forge-relational entity_replacement_reconciliation_strategy_replacement_declaration_applies_to_authority -- --nocapture`, `cargo test -p forge-relational bulk_create_entities_match_equivalent_singular_creates -- --nocapture`, and `cargo test -p forge-relational aspect_value_canonical_codec -- --nocapture`. Remaining warnings: unused entity payload storage accessor, unused invariant context entity_aspect_plan, and existing unused `diagnostic_label`.
- Relation create authority hard-break batch removed `RecordPayload` from `RelationSpec`, replaced `BulkRelationCreateIntent.payloads` with `field_patches`, and deleted relation payload compatibility ingress modules. Relation create, bulk create, endpoint updates, import staging, merge adoption, presentation fixtures, and coupled tests now move relation field declarations through `AspectFieldPatch`, foundational contract validation, authoritative state admission, and `AuthoritativeRecordAspectPatch` publication. Added `authority/mutation/intents/relation_field_creation_aspects/` with field classification, contract validation, state admission, creation patch construction, and endpoint authoritative-state update responsibilities. Verified no payload/JSON residue in the relation create/bulk-create authority path by grep, all new production files are below the line cap, `cargo check -p forge-relational --tests` passed, and focused tests passed for `relation_integrity`, `relation_field_delta_materializes_authoritative_patch_not_payload`, and `bulk_relation`. Remaining high-blast seam is the storage/durability payload substrate surfaced by unused payload accessors and arena payload carriers.
- Storage arena payload substrate hard-break batch removed `RecordPayload` storage from the live record arena and durability checkpoint images: `RecordArena` no longer has `payloads` or `payload_history`, `SlotInit` and authority allocation no longer accept payloads, `SlotView::payload` and storage `*_payload_at_version` accessors are gone, `RecordPayloadRevision` was deleted, and checkpoint images now persist authoritative aspect state only through metadata/extra images. Sparse overlay, merge, retention, runtime test support, durability/profile tests, and schema-transition recovery tests were updated to assert authoritative aspect/metadata history instead of payload history. Added replay error detail in durability recovery so failed authoritative replay exposes the underlying commit conflict. Greps confirm no `RecordPayload`, `StructuredPayloadDocument`, `payloads`, `payload_history`, or payload-at-version APIs remain in storage/durability/authority allocation surfaces. Verified `cargo fmt -p forge-relational`, `cargo check -p forge-relational --tests`, `cargo test -p forge-relational record_arena -- --nocapture`, focused sparse overlay tests, `cargo test -p forge-relational replay_retention_preserves_historical_live_entity_aspects_across_updates -- --nocapture`, `cargo test -p forge-relational fieldless_entity_create_commits_without_payload_storage -- --nocapture`, and the broader `cargo test -p forge-relational durability -- --nocapture`. Remaining seam: public `facade::payloads::RecordPayload` and `StructuredPayloadDocument` still exist as compatibility/public residue, plus payload-named tests/helpers outside storage.
- Public payload facade hard-break batch deleted the remaining `payloads` module and removed `facade::payloads::RecordPayload` / `StructuredPayloadDocument` from the public crate surface. Profile tests were split into `authoritative_aspect_state.rs` and `client_key_symbol_policy.rs`, and the canonicalization proof now uses declared aspect fields instead of undeclared JSON document keys. Removed the unused merge mutation-plan payload-missing error variant and renamed fintech helper surfaces to aspect-state/field-patch language. Verification passed with `cargo fmt -p forge-relational`, `cargo check -p forge-relational --tests`, focused profile tests, and `cargo test -p forge-relational aspect_value_canonical_codec -- --nocapture`. Existing unrelated warnings remain: unused `diagnostic_label` and unused `entity_aspect_plan`.
- Authoritative aspect-state conflict vocabulary batch removed the payload-era conflict/denial names from the authoritative admission seam. `EntityPayloadAspectStateDenied`/`RelationPayloadAspectStateDenied` and their denial/presentation/source-locator modules are now `EntityAuthoritativeAspectStateDenied`/`RelationAuthoritativeAspectStateDenied` with `UnsupportedAspectValue` and struct-denial variants. Deleted the unused `CompatibilityLoweringTransitionDenied` bridge variant and the stale cascade-delete `Payload` missing-state variant. Conflict detail strings now say declared field/aspect admission rather than payload lowering while preserving foundational `BoundarySourceLocator`, `ContractValidationDenial`, `AuthoritativeStateAdmissionDenial`, and patch denials. Verified `cargo fmt -p forge-relational`, `cargo check -p forge-relational --tests`, focused conflict tests, and bulk entity create. Remaining payload-word grep in this slice is only presentation bridge snapshot tests about external snapshot bytes.
- Presentation harness fixture declaration batch removed JSON `payload` fields from `FixtureEntity` and `FixtureRelation`. Harness fixtures now declare `AspectFieldPatch` directly and `presentation/harness/batches.rs` forwards those patches into entity/relation create specs without local `serde_json::Value` lowering or silent unsupported-value drops. Added `harness_fixture_loads_declared_aspect_field_patches` to prove fixture loading admits declared aspect fields through authority. Verified `cargo fmt -p forge-relational`, `cargo check -p forge-relational --tests`, and the focused harness fixture test. Existing unrelated warnings remain: unused `diagnostic_label` and unused `entity_aspect_plan`.
- Derived-index field target hard-break batch moved index/query field targets from loose strings onto foundational `FieldKey`. `DerivedIndexKind::{EntityField, RelationField}` and field query scopes now carry `FieldKey`; visibility packetization/execution and index-backed query execution pass `FieldKey` through to authoritative comparison-key reads instead of reparsing strings. Renamed `DerivedIndexPayload`/generation `payload` to `DerivedIndexEntries`/`entries` and `CorruptPayload` to `CorruptIndexEntries`, including performance budget wording. Verified with `cargo fmt -p forge-relational`, `cargo check -p forge-relational --tests`, `cargo test -p forge-relational derived_index -- --nocapture`, and the renamed focused corrupt-entries test. Existing unrelated warnings remain: unused `diagnostic_label` and unused `entity_aspect_plan`.
- Merge materialized-value evidence hard-break batch replaced `MaterializedAspectValuePayload`/`.payload` with `MaterializedAspectValueEvidence` carrying foundational `AspectValueLocator` for pinned visible aspects. Merge execution compilation now records authoritative whole-aspect locators; merge mutation derivation validates resolved values through locator aspect keys; merge execution diagnostics project typed locator values instead of raw aspect-key payloads. Structural QA split `merge/data/execution.rs` and touched `execution_mutation_plan.rs` into responsibility-named child modules so all touched production files are under the 400-line cap. Verified with `cargo fmt -p forge-relational`, `cargo check -p forge-relational --tests`, `cargo test -p forge-relational prepare_merge_execution -- --nocapture`, `cargo test -p forge-relational execute_prepared_merge_reports_execution_counters_and_structural_summary -- --nocapture`, `cargo test -p forge-relational derive_merge_commit_mutation_plan_emits_source_authorized_create_intent -- --nocapture`, and `cargo test -p forge-relational execute_prepared_merge_survives_durability_append_and_recovery -- --nocapture`. Existing unrelated warnings remain: unused `diagnostic_label` and unused `entity_aspect_plan`.

- Grouped truth aspect-key projection hard-break batch: relational grouped truth now parses projection/binding identities as foundational AspectKey, snapshot row materialization decodes canonical AspectValue bytes through an isolated snapshot_aspect_reads boundary, row artifacts expose aspect_values with RelationalAspectCellValue instead of field/value duplicate names, and grouped missing-value failures carry AspectKey. Broke the runtime bridge grouped projection seam from *_field_key/field_key to *_aspect_key/aspect_key, split bridge grouped truth-view tests out of the over-cap production file, and updated query grouped-projection fixtures/extraction to consume the new aspect-key/AspectValue boundary. Verified cargo fmt, cargo check -p forge-relational --tests, focused forge-relational row_set/grouped_projection tests, and focused forge-runtime-bridge grouped_truth_view tests. cargo check -p forge-query --tests still fails on pre-existing downstream payload/schema/update-intent hard-break fallout, now without grouped-truth-specific failures in the visible error set.
- Downstream query payload-surface blast batch fixed the `forge-query` consumers broken by relational's hard break. Added a query-side `relational_aspect_write` boundary that declares foundational scalar contracts and lowers query JSON boundary fields into `AspectFieldPatch`; exported `AspectFieldPatchTarget` from the relational facade. Memory workspace, effect-authority fixtures, merge-inspection fixtures, workflow tests, and effect lifecycle tests now create entities/relations/updates through declared aspect field patches and inspect authoritative aspect state instead of `RecordPayload`/`UpdateEntityIntent`/schema duplicate surfaces. Workflow mutation lowering now serializes native `IntentReconciliationInput` with `AspectFieldPatch` bytes instead of JSON strategy bytes, and the over-cap `workflow/lowering.rs` was split into `workflow/lowering/{terms,errors,counters,mutation,merge,writeback}.rs`. Renamed query mutation lowering input from `desired_payload` to `desired_fields_json` so the remaining JSON is visibly boundary input. Verified `cargo fmt -p forge-relational -p forge-query`, `cargo check -p forge-relational --tests`, `cargo check -p forge-query --tests`, `cargo test -p forge-query memory_workspace -- --nocapture`, `cargo test -p forge-query lowered_mutation_execution_runs_through_relational_strategy_authority -- --nocapture`, and `cargo test -p forge-query mutation_batch_executes_through_one_batch_native_lowered_plan -- --nocapture`. Remaining warnings are the existing `diagnostic_label` and `entity_aspect_plan` dead-code warnings in forge-relational.
- Forge-relational public examples hard-break batch removed stale example use of deleted payload/update/schema surfaces. `examples/support.rs` now declares demo `name` and `label` aspects with foundational scalar contracts, builds `AspectFieldPatch` declarations for entity creates, entity field updates, and relation creates, and uses `UpdateEntityFieldsIntent` instead of the removed payload update path. `derived_indexes.rs` now targets foundational `FieldKey`, and `snapshots.rs` prints preserved authoritative aspect state instead of payload. Structural QA kept touched example files under the line cap and fixed public-helper ordering in `support.rs`. Verified no `RecordPayload`, `UpdateEntityIntent`, `RelationPayloadClass`, `AspectPrecision`, or payload field residue remains in `crates/forge-relational/examples`; ran `cargo fmt -p forge-relational`, `cargo check -p forge-relational --examples`, `cargo check -p forge-relational --tests`, and all five public examples (`basic_runtime`, `snapshots`, `derived_indexes`, `history_and_replay`, `validation_and_durability`). Existing warnings remain `diagnostic_label` and `entity_aspect_plan`.
- Worth-schema downstream hard-break batch removed its stale imports/usages of relational `RecordPayload`, `RelationPayloadClass`, `AspectPrecision`, and `AspectComparator`. Worth schema declarations now build foundational contracts from `forge-foundational`; entity/relation domain aspect law is centralized in `data/aspects/domain_bindings.rs`; topology authority creates now lower into `AspectFieldPatch` and read duplicate/seed labels from authoritative aspect state instead of payload JSON. Split the over-cap gateway into `gateway.rs`, `gateway/failure_envelopes.rs`, `gateway/touched_aspect_scope.rs`, `gateway/tests.rs`, and `authority/aspect_field_patches.rs`; all touched files are below 400 lines. Verified no targeted legacy payload/declaration terms remain in `crates/worth-schema/src`, ran `cargo fmt -p worth-schema`, `cargo check -p worth-schema --tests`, `cargo test -p worth-schema authority_ -- --nocapture`, and `cargo check -p forge-relational --tests`. Existing forge-relational warnings remain `diagnostic_label` and `entity_aspect_plan`.
- Worth-topo downstream hard-break batch removed relational read-record payload dependence from the write-authority/query-runtime seam. Added `relational_aspect_boundary/{entity_field_declarations.rs,entity_authoritative_reads.rs}` so topology/naming creates lower into `AspectFieldPatch` declarations and duplicate checks, patch matching, materialized labels, query rows, and branch-parity certification read labels/persistent names from authoritative aspect state. Rewrote bridge snapshot egress off record payloads and onto authoritative state plus relation endpoint identities; fixed the relational bridge snapshot resolver to honor commit-envelope snapshot identities when active handle ids collide. Also fixed an adjacent worth-spatial owned-row iterator compile blocker. Verified no targeted legacy `RecordPayload`/read-record payload terms remain in touched worth-topo surfaces; ran `cargo fmt -p worth-topo`, `cargo fmt -p forge-relational`, `cargo fmt -p worth-spatial`, `cargo check -p worth-topo --tests`, `cargo check -p forge-relational --tests`, `cargo test -p worth-topo missing_persistent -- --nocapture`, `cargo test -p worth-topo milestone_one_bridge_routes_and_evaluates_seeded_commit -- --nocapture`, and `cargo test -p forge-relational runtime_bridge_snapshot_reader_prefers_active_snapshot_binding_over_later_commit_id_collision -- --nocapture`. Existing forge-relational warnings remain `diagnostic_label` and `entity_aspect_plan`.
- Presentation harness snapshot-egress hard-break batch removed JSON-shaped record serialization from `presentation/harness/adapter`. Snapshot capture now emits harness `Fields` projected from `EntityReadRecord` / `RelationReadRecord` authoritative aspect state, canonical aspect bytes via shared `aspect_wire`, and authoritative field comparison keys; run, diagnostics, and replay summaries are explicit typed projections rather than inline `json!` or generic `serde_json::to_value`. The default harness schema now declares real foundational scalar contracts for `name` and `label`, so harness mutations no longer depend on undeclared field admission. Structural QA found and removed the generic serde projection residue in summary builders. Verified no `json!`, `StructuredValue::Json`, generic `serde_json::to_value`, or payload-named locals remain in the touched harness adapter; ran `cargo fmt -p forge-relational`, `cargo check -p forge-relational --tests`, `cargo test -p forge-relational harness_runner_captures_snapshot_diagnostics_and_replay -- --nocapture`, and `cargo test -p forge-relational harness_fixture_loads_declared_aspect_field_patches -- --nocapture`. Existing forge-relational warnings remain `diagnostic_label` and `entity_aspect_plan`.
- Central diagnostic aspect-value projection batch removed serde enum-tag projection for foundational aspect carriers from `diagnostics/data/fields.rs`. `RelationalDiagnosticValue::AspectValue` and `StructAspectValue` now project to explicit diagnostic objects with `value_family` and shared `aspect_wire` canonical bytes; struct diagnostics project declared field keys plus each field value's canonical bytes. Added `diagnostics/data/fields/aspect_value_projection.rs` and focused tests proving scalar and struct aspect diagnostics no longer expose serde tags. Updated invariant diagnostic projection expectations to assert canonical-byte aspect value output. Structural QA kept touched files under the line cap and grep found no `serde_json::to_value` residue in the diagnostics package. Verified with `cargo fmt -p forge-relational`, `cargo check -p forge-relational --tests`, `cargo test -p forge-relational aspect_value_projection_uses_canonical_bytes_not_serde_tags -- --nocapture`, `cargo test -p forge-relational struct_aspect_value_projection_keeps_field_canonical_bytes -- --nocapture`, and `cargo test -p forge-relational failure_projection_emits_aspect_field_diagnostic_payload -- --nocapture`. Existing forge-relational warnings remain `diagnostic_label` and `entity_aspect_plan`.
- Merge aspect witness digest hard-break batch removed the serde tuple byte boundary from `merge/logic/execution.rs::aspect_shared_witness_digest`. Added `merge/logic/aspect_witness_digest.rs`, which builds explicit canonical witness bytes from the aspect key, side labels, merge component family tags, shared `aspect_wire::encode_aspect_value` bytes for scalar/struct aspect values, stable endpoint ids, and stable lifecycle tags before hashing. `MergeAspectComponent` no longer derives `Serialize`, and canonical codec errors now return `MergeExecutionCompilationError::UnsupportedAspectValueWitness` instead of relying on serde/expect. Verified the exact reported native codec panic/string-tag issue was already gone in current tree: `commit_strategies/data/native_codec.rs` routes aspect values through shared `aspect_wire` and returns `NativeCodecError`. Greps found no old `serde_json::to_vec(&(aspect_key, source_component, target_component))` witness boundary and no `native strategy codec does not support aspect value` panic. Ran `cargo fmt -p forge-relational`, `cargo check -p forge-relational --tests`, `cargo test -p forge-relational aspect_witness_digest -- --nocapture`, and `cargo test -p forge-relational prepare_merge_execution_compiles_exact_shared_record_plan -- --nocapture`. Existing forge-relational warnings remain `diagnostic_label` and `entity_aspect_plan`.
- Query canonical digest hard-break batch removed serde/JSON digest authority from the high-blast query/index certification seam. Plan keys, fragment keys, fragment reduction digests, traversal tie-breaker digests, and fallback parity digests now route through `query/data/canonical_digest/{mod,primitive_terms,scope_terms,read_record_terms}.rs`, which encodes typed query contracts, record ids, foundational `FieldKey`, authoritative aspect state, struct fields, lifecycle state, and shared `aspect_wire` `AspectValue` bytes instead of `serde_json::to_vec`. Deleted the generic `certification_digest<T: Serialize>` surface and kept the split files under the line cap. Greps found no `certification_digest`, `serde_json::to_vec`, JSON/payload terms in touched query digest/access surfaces. Verified with `cargo fmt -p forge-relational`, `cargo check -p forge-relational --tests`, `cargo test -p forge-relational fragment_reduction -- --nocapture`, `cargo test -p forge-relational derived_index_contract_certification_mode_emits_stable_parity_digest -- --nocapture`, and `cargo test -p forge-relational explicit_target_helper_generates_non_zero_deterministic_plan_key -- --nocapture`. Existing warnings remain `diagnostic_label` and `entity_aspect_plan`.
- Transaction bulk-plan digest hard-break batch removed generic serde certification from transaction bulk planning/admission. Added `transactions/data/bulk_plan_digest.rs`, which hashes typed canonical bytes for naming, lineage, and provenance plans: transaction ids, branch ids, client-key raw/symbol lanes, partition/kind/entity/relation ids, created/existing endpoint references, worker names, partition keys, and local-only flags. Deleted `transactions::data::certification_digest<T: Serialize>` from `primitives.rs`; `bulk_planning` and `bulk_admission` now call semantic digest constructors directly. Greps found no `certification_digest`, `serde_json::to_vec`, JSON macros, `StructuredPayloadDocument`, or `RecordPayload` residue under `src/transactions`. Verified with `cargo fmt -p forge-relational`, `cargo check -p forge-relational --tests`, `cargo test -p forge-relational bulk_mutation -- --nocapture`, and `cargo test -p forge-relational admission_rejects -- --nocapture`. Existing warnings remain `diagnostic_label` and `entity_aspect_plan`.
- Merge execution digest hard-break batch removed serde tuple/struct serialization from executable plan, schema snapshot, equality witness, and merge execution diagnostics digests. Added `merge/data/execution/canonical_digest/{mod,diagnostic_terms,enum_terms,plan_terms,schema_terms,value_terms}.rs`; digest terms now encode typed branch/commit/record ids, schema ids/revisions, foundational aspect keys and canonical aspect bytes, value locators, materialized aspect evidence, policy/causal/provenance classes, and query canonical read-record digests. Removed private serde digest row DTOs from `executable_plan.rs` and routed `execution_artifacts.rs` diagnostics through the shared execution digest grammar. Verified residue grep, all touched production files under the line cap, `cargo fmt -p forge-relational`, `cargo check -p forge-relational --tests`, and focused merge execution tests for exact-shared plan compilation, execution diagnostics/counters, and durability recovery. Existing warnings remain `diagnostic_label` and `entity_aspect_plan`.
- Commit strategy canonical digest hard-break batch verified the reported two-codec/panic issues are already gone in the current tree (`native_codec` routes aspect values through shared `aspect_wire` and returns `NativeCodecError`). Removed production commit-strategy JSON digest authority by deleting the generic `stable_digest` helper and adding `commit_strategies/data/canonical_digest/{mod,primitive_terms,descriptor_terms,mutation_program_terms,artifact_terms}.rs`. Descriptor, frozen registry, mutation program, lowering summary, preview validation summary/cost, runtime determinism, native scope, and fallback scope digests now use typed canonical terms, record ids, client keys, invariant masks, and shared aspect-field-patch bytes instead of `serde_json::to_vec`/`to_writer`. Verified no `stable_digest` or native-codec panic residue remains in commit strategy/aspect wire production code; only test serialization fixtures still use `serde_json::to_vec`. Ran `cargo fmt -p forge-relational`, `cargo check -p forge-relational --tests`, `cargo test -p forge-relational aspect_value_canonical_codec -- --nocapture`, `cargo test -p forge-relational strategy_commit_artifact_bundle -- --nocapture`, and `cargo test -p forge-relational commit_strategies::data -- --nocapture`. Broad `cargo test -p forge-relational commit_strategies -- --nocapture` exposed stale facade-level strategy fixture failures around undeclared `replicas` aspect admission and merge classification drift; data/native/registry tests passed within that run. Existing warnings remain `diagnostic_label` and `entity_aspect_plan`.
- Commit strategy merge-evidence batch fixed the downstream-blasting facade failures from the broad `commit_strategies` test filter. Native strategy intent scope fields now come from lowered authoritative `AspectFieldPatch` targets, so replica/aspect/intent strategies compare declared `aspect:path` field identities instead of mixing loose field names with patch target labels. Facade strategy tests use the declared strategy schema fixture for `replicas`, and replacement strategy fixtures explicitly declare all replacement fields instead of assuming legacy payload carry-forward. The reported shared-codec/native-panic/update-field JSON issues were audited and confirmed fixed in the current tree; the update-field struct tests still had a JSON fixture panic, so their setup now builds `AspectFieldPatch` targets directly. Verified `cargo fmt -p forge-relational`, `cargo check -p forge-relational --tests`, `cargo test -p forge-relational commit_strategies -- --nocapture`, `cargo test -p forge-relational aspect_value_canonical_codec -- --nocapture`, and `cargo test -p forge-relational update_entity_fields -- --nocapture`. Existing warnings remain `diagnostic_label` and `entity_aspect_plan`.
- Runtime config provenance digest batch removed JSON digest authority from commit-strategy registration override provenance. `config/data/profiles` now delegates override application out of the profile facade, and commit-strategy registration provenance uses an explicit domain-tagged canonical descriptor-digest set encoder under `override_application/commit_strategy_registration_digest.rs` instead of `serde_json::to_vec`. Added an order-independence proof for the registration digest detail. Verified `cargo fmt -p forge-relational`, `cargo test -p forge-relational commit_strategy_registration_provenance_digest_is_order_independent -- --nocapture`, and `cargo check -p forge-relational --tests`. Existing warnings remain `diagnostic_label` and `entity_aspect_plan`.
- Replay parity digest hard-break batch deleted the production `replay::data::stable_digest<T: Serialize>` / `serde_json::to_writer` authority seam. Replay digesting now uses `replay/data/digest/{primitive_terms,descriptor_terms,surface_terms,lineage_terms}.rs`, with domain-tagged typed byte constructors for descriptor parity, replay surfaces, lineage event/decision surfaces, strategy replay descriptors, authoritative patch operations, authoritative snapshot aspect state, and typed diagnostic values. Routed replay continuity, surface comparison, lineage authority, and derived-index replay parity through named digest constructors. Greps found no `stable_digest` or serde/JSON digest residue under `src/replay`. Verified `cargo fmt -p forge-relational`, focused replay digest tests, and `cargo check -p forge-relational --tests`. Existing warnings remain `diagnostic_label` and `entity_aspect_plan`.
- Reported aspect-wire regression batch re-audited the five reported seams. Current tree already routes native strategy `AspectValue` and `AspectFieldPatch` bytes through shared `aspect_wire`, returns `NativeCodecError` instead of panicking, applies `UpdateEntityFieldsIntent` through foundational authoritative patches, and carries typed entity field patch denials. Tightened aspect value codec wording from payload/serde language to canonical body/frame language and added native malformed-byte regression tests for aspect values and field patches. Verified with `cargo fmt -p forge-relational`, `cargo test -p forge-relational aspect_value_canonical_codec -- --nocapture`, `cargo test -p forge-relational update_entity_fields -- --nocapture`, and `cargo check -p forge-relational --tests`. Existing warnings remain `diagnostic_label` and `entity_aspect_plan`.
- Snapshot read aspect-byte egress batch hard-broke the bridge snapshot read API from generic payload bytes to `aspect_bytes`. `SnapshotReadRecord` now stores/exposes `aspect_bytes()` only, relational presentation snapshot reading emits `aspect_bytes`, grouped truth decodes `record.aspect_bytes()` through relational `aspect_wire`, bridge row-set failures say `AspectBytesDecodeFailure`, and structural fingerprints hash record aspect bytes instead of record payloads. Verified no snapshot-read payload accessor residue remains in targeted bridge/relational production surfaces. Ran `cargo fmt -p forge-runtime-bridge -p forge-relational`, `cargo check -p forge-runtime-bridge --tests`, `cargo check -p forge-relational --tests`, `cargo check -p forge-query --tests`, `cargo check -p worth-topo --tests`, `cargo test -p forge-runtime-bridge row_set -- --nocapture`, `cargo test -p forge-relational snapshot_reader -- --nocapture`, `cargo test -p forge-relational grouped_projection -- --nocapture`, and `cargo test -p forge-relational row_set -- --nocapture`. Existing forge-relational warnings remain `diagnostic_label` and `entity_aspect_plan`.
- Durable local-store native-file batch removed the remaining production JSON codec seam from `durability/log/local_store.rs`. Manifest, segment, and checkpoint files now use explicit native durable filenames plus `durability/log/native_file_codec.rs` with MessagePack encoding; deleted generic `read_json`/`write_json`, removed the legacy segment-wrapper/newline JSON fallback, and updated durability access/checkpointing plus test tamper helpers to use typed native segment/checkpoint functions. Verified with `cargo fmt -p forge-relational`, `cargo check -p forge-relational --tests`, and `cargo test -p forge-relational durability -- --nocapture` (45 passed, 1 ignored). Existing forge-relational warnings remain `diagnostic_label` and `entity_aspect_plan`.
- Storage/index comparison-key batch hard-broke authoritative field comparison keys from loose display strings to `AuthoritativeFieldComparisonKey` carrying shared `aspect_wire` canonical `AspectValue` bytes plus display-only projection. Read records, unique-field indexes, derived index entries, query/index execution, replay/query digests, commit strategies, validation uniqueness checks, and harness projection now compare typed keys; the legacy `structured_field_comparison_key` accessor is gone. Split query packet field matching into `visibility/materialization/read_records/reader/query_fragment_work/field_query_matching.rs` to keep the touched packet executor under the line cap. Added a regression proving `Int64(1)` and `String("1")` keep distinct comparison keys despite matching display text. Verified with `cargo fmt -p forge-relational`, `cargo test -p forge-relational comparison_key_preserves_aspect_value_family_even_when_display_collides -- --nocapture`, `cargo test -p forge-relational index_observation_paths -- --nocapture`, and `cargo check -p forge-relational --tests`. Existing forge-relational warnings remain `diagnostic_label` and `entity_aspect_plan`.
