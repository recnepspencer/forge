# 2026-04-23

- Ran a strict QA loop over Forge Query milestone 9.1 subscription diagnostics and certification tests, focusing on meaningful assertions rather than cosmetic digest/stage checks.
- Fixed a counter honesty/performance issue in subscription family selection: relationship-proof drift and exhausted bridge-family lookup budget now deny before claiming a family registry lookup occurred.
- Strengthened subscription diagnostics tests to assert typed denials, exact counter residue, selected family/width distinctions, support profile state, and policy/tenant/relationship-proof effects on both declaration and basis-request digests.
- Strengthened milestone 9.1 certification tests to require compile-fail `.stderr` baselines and hostile diagnostic evidence distinct from control-lane evidence.
- Verified `cargo test -p forge-query` passed: 587 unit tests plus trybuild compile-fail coverage. `git diff --check` passed with only existing CRLF warnings.
- Closed remaining milestone 9.1 spec-audit gaps: certification bundles now expose the spec-required verification outputs, representative direct/scope/template/saved and facade-helper rows are required, and policy/tenant/relationship-proof post-admission digest patching is required compile-fail coverage. Re-verified `cargo test -p forge-query` with 588 tests plus trybuild; `git diff --check` still only reports existing CRLF warnings.
- Created `_docs/forge-query/milestone-9.1-closeout.md`, documenting shipped scope, acceptance mapping, performance/counter closure, explicit deferred scope, later-milestone assumptions, QA outcome, verification baseline, and self-check.
- Created `_docs/forge-query/milestone-9.2.md` for active subscription lifecycle, sharing, query-shaped delivery, continuation/remap, and preview isolation. Also added the missing Milestone 9.2 named certification suite to `_docs/forge-query/test-requirements.md`.
- QA-tightened the Forge Query 9.2 spec: added registry-owned active lane semantics, family-typed `QuerySubscriptionMaintenanceDelta` and `QueryPatchGroup` variants, receipt-based acknowledgement/backpressure proof, bridge preview typestate/residue binding, concrete EmployeeRecord walkthroughs, compile-fail target names, and exact active lifecycle counters.
- Further hardened Forge Query 9.2 to encode performance architecturally: hot-path APIs now require sealed cost dimensions, budget/performance receipts, active lane lookup class, delivery density posture, allocation posture, and `ActiveDeliveryWorkPacket` evidence instead of relying on benchmark-only assertions or raw numeric knobs.
- Started Forge Query 9.2 Phase 1 implementation batch: added active subscription lane admission/runtime vocabulary, sealed active work budgets, registry-owned lane handles, Phase 1 lifecycle denials/counters, facade exports, unit tests, and trybuild constructor-boundary fixtures. Verified focused active lifecycle tests, compile-fail suite, full `cargo test -p forge-query`, and targeted `git diff --check` with only existing CRLF warnings.
- Ran a QA loop on Forge Query 9.2 Phase 1 and fixed the main architecture risks: split the active lifecycle god-file seed into focused modules, made active lane handles non-cloneable registry authority, replaced linear-scan registry lookup with digest index plus handle index/generation evidence, typed active budget dimensions instead of raw widths, and added a dedicated linear-scan denial counter. Re-verified focused lifecycle tests, compile-fail suite, and full `cargo test -p forge-query`.
- Built Forge Query 9.2 Phase 2 implementation batch: equivalent active lane admissions now join the registry-owned lane, consumer attachments are separate per-consumer artifacts with fanout reports, backpressure policy, acknowledgement frontier, and delivery cursor digests, acknowledgements require same-attachment receipts and advancing sequences, and sealed compile-fail boundaries cover attachment/frontier/receipt construction plus raw attachment budget widths. Verified focused active lifecycle/sharing tests, trybuild, full `cargo test -p forge-query` (600 tests), and targeted `git diff --check` with only existing CRLF warnings.
- Ran a Forge Query 9.2 Phase 1/2 completion pass: active lifecycle/delivery postures now use the milestone vocabulary, lookup-class and linear-scan debt counters are explicit, equivalent joins mark shared-lane posture and shared-lane counters, mismatched explicit joins deny with join-denial counters, and backpressure denial has a typed request/counter/test. Verified focused lifecycle/sharing tests, trybuild, full `cargo test -p forge-query` (603 tests), and targeted `git diff --check` with only existing CRLF warnings.
- Started Forge Store milestone 13.2 Phase 1 with operational participation vocabulary, support action typestate, performance/path admission vocabulary, counters, facade helpers, unit tests, and compile-fail constructor-boundary fixtures. Verified `cargo test -p forge-store` passed: 525 unit/integration tests plus trybuild compile-fail coverage; targeted `git diff --check` passed with only the existing CRLF warning on `crates/forge-store/src/lib.rs`.
- Ran a hostile QA loop on Forge Store milestone 13.2 Phase 1 and fixed four architectural weaknesses: exact operational bases now require portability evidence, public bypasses for translation/path/publication phase boundaries are sealed, batch receipt reuse is counted only on explicit reuse, and facade helpers route through backend counters instead of disposable pipelines. Added compile-fail fixtures for the sealed shortcuts and a backend counter regression test. Verified focused subscription-support tests, trybuild, full `cargo test -p forge-store` (526 tests), and targeted `git diff --check` with only existing CRLF warnings.
- Built Forge Store milestone 13.2 Phase 2 first slice: sealed support retention affected sets, retention/reclaim/compaction decision vocabulary, retention batch admission, post-action survival witnesses, reclaim consequence reporting, backend/facade methods, retention counters, unit/persistence tests, and trybuild constructor-boundary fixtures. Verified focused subscription-support tests, full `cargo test -p forge-store` (530 tests plus trybuild), and targeted `git diff --check` with only existing CRLF warnings.
- Closed the Forge Store milestone 13.2 Phase 2 support-side boundary: added affected-set digests, durable support retention participation records, retained/reclaimed/compacted/expired materialization in post-action reports, exact retained/reclaimed/compacted/expired counters, stronger family/origin/non-reclaim rejection tests, facade assertions, and a compile-fail fixture proving retention records cannot be forged. Verified focused Phase 2 tests, full subscription-support tests (52), trybuild, full `cargo test -p forge-store` (532 tests), and targeted `git diff --check` with only existing CRLF warnings.
- Ran an aerospace-grade QA loop on Forge Store milestone 13.2 Phase 2 and fixed four support-retention weaknesses: public data-bearing retention decisions were sealed behind crate-owned constructors, physical materialization now preserves exact/degraded/rebuildable/non-resumable decision semantics, retained/reclaimed materializations expose their required evidence details, and retained/reclaimed/compacted/expired/policy counters now increment only after successful consequence publication. Added compile-fail coverage for external retention-decision synthesis. Reverified focused Phase 2 tests, subscription-support tests, trybuild, full `cargo test -p forge-store` (532 tests), and targeted `git diff --check` with only existing CRLF warnings.
- Built Forge Query 9.2 Phase 3 first delivery slice: added query delivery windows, maintenance deltas, patch groups, active delivery work packets, performance receipts, batch-issued acknowledgement receipts, raw CDC/bridge invalidation denials, delivery density posture, delivery/patch/work-packet counters, facade exports, unit tests, and compile-fail boundary fixtures. Verified focused active delivery/sharing tests, trybuild, full `cargo test -p forge-query` (607 tests plus trybuild/doc-tests), and targeted `git diff --check` with only existing CRLF warnings.
- Ran an aerospace-grade QA loop on Forge Query 9.2 Phase 3 delivery and fixed four production-grade weaknesses: maintenance deltas now require typed `MaintenanceDeltaWidth`, delivery emission rejects work packets that exceed the opened window budget, collection order patches have distinct counter semantics, and work packets reject foreign-lane or mismatched lowering-report deltas before a batch can exist. Performance receipts now expose consumed, budgeted, and remaining width coherently. Verified focused active delivery tests, trybuild, full `cargo test -p forge-query` (610 tests plus trybuild/doc-tests), and targeted `git diff --check` with only existing CRLF warnings.
- Built Forge Query 9.2 Phase 4 continuation/remap slice: added typed continuation classes, admitted continuation evidence, continuation reports, typed remap width, continuation denials, delivery-window continuation application, continuation-to-maintenance-delta lowering, remap/advisory/identity-break counters, facade exports, unit tests, and compile-fail boundary fixtures. Kept preview promotion as a typed later-phase denial to avoid Phase 5 overclaim. Verified focused active continuation/sharing tests, trybuild, full `cargo test -p forge-query` (613 tests plus trybuild/doc-tests), and targeted `git diff --check` with only existing CRLF warnings.
- Built Forge Store milestone 13.2 Phase 3 first compatibility slice: added support-family version windows, manifest admission witnesses, decoded-row semantic access witnesses, compatibility affected sets, exact/degraded/rejected compatibility decisions, compatibility participation records/reports, backend/facade admission and publication, compatibility counters, unit/facade tests, and compile-fail coverage for external witness/decision construction. Verified focused Phase 3 tests, subscription-support tests (56), trybuild, full `cargo test -p forge-store` (536 tests plus trybuild/doc-tests), and targeted `git diff --check` with only the existing CRLF warning on `crates/forge-store/src/lib.rs`.
- Continued Forge Store milestone 13.2 Phase 3 by binding support compatibility admission to Milestone 12 read receipts/read-admission outcomes instead of loose manifest strings. Support compatibility reports now carry Milestone 12 receipt digests, relations, and rejection kinds; exact/degraded/rejected decisions are validated against receipt semantics; certification lane outcomes can be derived from support compatibility reports; and compile-fail coverage proves external callers cannot forge receipt witnesses. Verified focused Phase 3 tests, subscription-support tests (58), trybuild, full `cargo test -p forge-store` (538 tests plus trybuild/doc-tests), and targeted `git diff --check` with only the existing CRLF warning on `crates/forge-store/src/lib.rs`.
- Realigned Forge Query 9.2 implementation by phase order and built the Phase 5 preview isolation/discard/promotion slice: added typed `PreviewResidueWidth`, preview isolation artifacts, explicit residue classes/reports, discard closeouts, authoritative-sharing denial, promotion handoff evidence, preview counters, facade exports, unit tests, and compile-fail fixtures for raw residue widths and artifact construction. Verified focused preview isolation tests, trybuild compile-fail suite, full `cargo test -p forge-query` (618 tests plus trybuild/doc-tests), and targeted `git diff --check` with only the existing CRLF warning on `crates/forge-query/src/facade.rs`.
- Built Forge Query 9.2 Phase 6 first certification closure: added the milestone 9.2 certification requirements registry, a runtime-backed subscription lifecycle/sharing/delivery/continuation/preview certification adapter, required canonical and rejection rows, required verification-output digests, a width-bounded scale row, and compile-fail closure targets for active lifecycle shortcuts. Verified focused milestone 9.2 certification tests, trybuild compile-fail suite, full `cargo test -p forge-query` (626 tests plus trybuild/doc-tests), and targeted `git diff --check` with only existing CRLF warnings.
- Ran a hostile QA loop over the Forge Query 9.2 Phase 6 certification harness and corrected three proof weaknesses: delivery evidence now stays on the same `ActiveSubscriptionRuntime` that opened the lane/window, the equivalent-sharing row now emits real dual-consumer delivery/ack evidence from one shared lane instead of post-hoc digest patching, and the certification matrix now requires a posture-sensitive performance-receipt row plus inspectable counter evidence for sharing, preview residue, and scale-axis coverage. Reverified focused milestone 9.2 certification tests and full `cargo test -p forge-query` (628 tests plus trybuild/doc-tests); targeted `git diff --check` still reports only existing CRLF warnings.
- Built Forge Store milestone 13.2 Phase 4 first portability/replication/import participation slice: added support portability affected sets, manifest budgets, scope footprints, capsule support manifests, replicated bundles, omission reports, target import witnesses, imported semantic access, portability participation reports, backend/facade methods, counters, unit/facade tests, and compile-fail coverage for sealed import admission. Verified focused Phase 4 tests, subscription-support tests (65), trybuild, full `cargo test -p forge-store` (545 tests plus trybuild/doc-tests), and targeted `git diff --check` with only the existing CRLF warning on `crates/forge-store/src/lib.rs`.
- Ran an even harsher Forge Query 9.2 QA loop by reading the active lifecycle/sharing/delivery/continuation/preview/certification code against the milestone spec and performance laws. Fixed four concrete gaps: store-backed restart overclaim was a named but unreachable denial, preview discard ignored its admitted residue budget, fanout delivery counters were dead on emitted batches, and work-budget literal forgery had no dedicated typed-width compile-fail fixture. Added runtime/certification/unit/trybuild coverage for those gaps. Verified focused active lifecycle/sharing/preview/certification tests, compile-fail suite, full `cargo test -p forge-query` (631 tests plus trybuild/doc-tests), and targeted `git diff --check`.
- Built the next Forge Query 9.2 performance-architecture slice around allocation posture: replaced the narrow allocation policy with explicit `ActiveSubscriptionAllocationPosture` phases, threaded posture through lifecycle admission, delivery windows, work packets, and performance receipts, made heap denial/debt counters phase-accurate, updated milestone 9.2 certification builders to bind real allocation posture into digests, and expanded runtime/unit/trybuild coverage for wrong-phase, denied, and debt-explicit allocation paths. Verified focused active tests, milestone 9.2 certification, compile-fail suite, full `cargo test -p forge-query` (637 tests plus trybuild/doc-tests), and targeted `git diff --check`.
- Built the next Forge Query 9.2 Phase 6 closure slice around lifecycle closeout and support honesty: added runtime-tracked attachment registration and terminal lane closure, new `SubscriptionLifecycleCloseRequest`/`SubscriptionLifecycleCloseout`/close-error vocabulary, lifecycle closeout counters and performance receipts, expanded subscription support profiles to include active lifecycle and closeout support, updated milestone 9.2 certification builders to bind real lifecycle-close support/closeout evidence, and added runtime plus compile-fail coverage for terminal lifecycle boundaries. Verified focused closeout tests, milestone 9.2 certification, compile-fail suite, full `cargo test -p forge-query` (641 tests plus trybuild/doc-tests), and targeted `git diff --check`.
- Built the next Forge Query 9.2 Phase 6 closure slice around phase-local performance receipts: active admission, consumer attachment, continuation reports, preview discard/promotion, and lifecycle closeout artifacts now each carry posture-sensitive `SubscriptionPerformanceReceipt` evidence; milestone 9.2 certification bundles now bind combined multi-phase receipt digests instead of delivery-only evidence; focused lifecycle/sharing/continuation/preview tests assert the new receipt surfaces; and trybuild baselines were refreshed for the strengthened sealed artifacts. Verified focused `cargo test -p forge-query active_`, milestone 9.2 certification, refreshed compile-fail suite, full `cargo test -p forge-query`, and targeted `git diff --check`.
- Built the next Forge Query 9.2 Phase 6 shipped-certification slice: added public `SubscriptionLifecycleCertificationContext`, `SubscriptionLifecycleCertificationBundle`, and `certify_subscription_lifecycle(...)` so runtime-backed active subscription lifecycle proof is available outside the harness; derived lifecycle certification from admission/activation/scale plus active lane, attachment, delivery, continuation, acknowledgement, and closeout artifacts with typed source-mismatch denials; exported the new certification API through the subscription facade; added lifecycle certification runtime tests and a compile-fail fixture proving the shipped bundle constructor stays sealed. Verified focused subscription certification tests, milestone 9.2 certification tests, refreshed trybuild compile-fail coverage, full `cargo test -p forge-query` (644 tests plus trybuild/doc-tests), and targeted `git diff --check`.
- Ran another strict Forge Query 9.2 QA pass over the shipped lifecycle certification surface and fixed two proof-honesty gaps: external callers can no longer mint `SubscriptionLifecycleCertificationContext::admitted(...)` directly, and lifecycle counter evidence now binds the acknowledged frontier rather than the pre-ack attachment frontier. Added a compile-fail fixture for the sealed admitted-context constructor and a runtime assertion that certification counter evidence contains the acknowledged frontier digest. Reverified focused certification tests, compile-fail suite, full `cargo test -p forge-query`, and targeted `git diff --check`.
- Closed the remaining Forge Query 9.2 closeout blockers: the shipped lifecycle certification surface now accepts typed preview discard/promotion evidence and binds preview/support closure into certification output; milestone 9.2 harness lifecycle and preview rows now consume the shipped certification API instead of reconstructing lifecycle bundles by hand; and `_docs/forge-query/milestone-9.2-closeout.md` now records shipped scope, acceptance mapping, deferred debt, and verification baseline. Verified `cargo fmt -p forge-query`, focused subscription certification tests, milestone 9.2 certification tests, compile-fail suite, full `cargo test -p forge-query` (646 tests plus trybuild/doc-tests), and targeted `git diff --check`.
- Ran the most brutal final Forge Query 9.2 QA pass and found a real proof hole in preview certification: preview promotion handoffs were not residue-bound, and shipped lifecycle certification could accept a foreign preview handoff/closeout source as long as lane, attachment, and epoch matched. Fixed it by binding `PreviewSubscriptionPromotionHandoff` to the residue report digest, exposing lifecycle closeout source digests, and requiring shipped preview discard/promotion certification to match the exact preview closeout/handoff source digests. Added runtime assertions for the denial path, updated preview promotion tests, refreshed trybuild baselines, and reverified full `cargo test -p forge-query` (647 tests plus trybuild/doc-tests), milestone 9.2 certification, and `git diff --check`.
