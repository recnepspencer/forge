## Implementation - Milestone D Phase 3 keyed-family async capability and locality-aware refresh

- Kept Milestone D work in spec order instead of skipping ahead. This batch advanced Phase 3:
  - keyed-family async capability binding
  - aspect-/partition-local async refresh classification
  - file-structure cleanup so the new capability work did not harden into fresh god files

- Broke up the first oversized async-capability files before adding more behavior:
  - replaced `crates/worth-signal/src/data/async_node/capability.rs` with:
    - `crates/worth-signal/src/data/async_node/declaration.rs`
    - `crates/worth-signal/src/data/async_node/descriptor.rs`
    - `crates/worth-signal/src/data/async_node/family.rs`
  - replaced `crates/worth-signal/src/logic/transaction/runtime/state/async_capability.rs` with:
    - `crates/worth-signal/src/logic/transaction/runtime/state/async_capability/mod.rs`
    - `crates/worth-signal/src/logic/transaction/runtime/state/async_capability/declaration.rs`
    - `crates/worth-signal/src/logic/transaction/runtime/state/async_capability/admission.rs`
  - split async-node runtime tests out of the resource mega-file into:
    - `crates/worth-signal/src/tests/async_node_runtime.rs`
    - `crates/worth-signal/src/tests/async_node_family_runtime.rs`
    - shared helper `crates/worth-signal/src/tests/async_node_support.rs`

- Resulting line counts after cleanup:
  - `data/async_node/declaration.rs` -> 217
  - `data/async_node/descriptor.rs` -> 127
  - `data/async_node/family.rs` -> 50
  - `state/async_capability/declaration.rs` -> 139
  - `state/async_capability/admission.rs` -> 331
  - `state/async_capability/mod.rs` -> 53
  - `tests/async_node_family_runtime.rs` -> 179
  - `tests/async_node_runtime.rs` -> 340

- Added `AsyncKeyedNodeCapabilityBinding` as the Phase 3 family-binding proof artifact. It retains:
  - family
  - key
  - concrete node
  - registry digest
  - lowered bundle digest
  - payload-contract digest

- Added node-first family helpers on `DefinedKeyedComputation` in
  `crates/worth-signal/src/logic/transaction/runtime/async_keyed.rs`:
  - `async_capability_declaration(...)`
  - `declare_async_capability(...)`
  - `async_request_intent(...)`
  - `async_revalidation_intent(...)`

- Added locality-aware async admission classification:
  - new block classes:
    - `ContractAspectMismatch`
    - `PartitionScopeMismatch`
  - `AsyncNodeAdmissionClassification` now records:
    - dirty partition scope count
    - contract partition scope count
  - only condition/timing blocks can downgrade refresh to
    `RefreshEligibleNoNewLineage`
  - locality mismatches stay a hard admission block instead of being
    reinterpreted as lifecycle convenience

- Added telemetry for the new Phase 3 truths:
  - `async_node_family_binding_count`
  - `async_node_aspect_local_refresh_count`
  - `async_node_partition_local_refresh_count`

- Important hostile-test lesson from this batch:
  - an initial partition-local mismatch test dirtied the *source* node with a
    non-overlapping changed region and expected the async-capable dependent to
    block
  - that was the wrong adversarial shape because the graph correctly never
    dirtied the dependent for an irrelevant upstream partition change
  - the test was corrected to dirty the async-capable node itself with the
    mismatched region, which directly certifies the boundary we actually own:
    if mismatched dirty partition scope truth exists on the async-capable node,
    revalidation fails closed

- Added hostile/runtime coverage:
  - `async_keyed_node_capability_binding_preserves_family_identity_and_node_local_truth`
  - `async_node_partition_local_revalidation_blocks_when_changed_region_misses_contract_scope`
  - `async_node_partition_local_revalidation_matches_contract_scope_and_records_locality`

- Compile-fail boundary coverage now includes the new async-capability artifacts:
  - `async_keyed_node_capability_binding_fields_are_private`
  - `async_node_admission_classification_fields_are_private`
  - existing async-capability private-field fixtures refreshed and passing normally

- Verification passed:
  - `cargo fmt -p worth-signal`
  - `cargo test -p worth-signal async_node_ -- --nocapture`
  - `cargo test -p worth-signal resource_compile_fail_boundaries_hold -- --nocapture`
  - `cargo test -p worth-signal`
  - `git diff --check` clean aside from existing LF/CRLF warnings

- Full crate after this batch: `883 passed, 23 ignored`.

## Milestone D - Phase 4 interior async gates and first hierarchy surface

- Kept Milestone D in order and implemented the first real Phase 4 slice instead
  of jumping ahead:
  - interior async gate state reporting for ordinary graph nodes with async capability
  - first hierarchical async replay/cancellation surface
  - node-first cancellation wrapper so hierarchy behavior is exercisable through
    the capability-first API, not only the legacy resource lane

- Added new async-node artifacts under
  `crates/worth-signal/src/data/async_node/`:
  - `gate.rs`
    - `AsyncNodeDownstreamDependenceFact`
    - `AsyncNodeGateStateReport`
  - `hierarchy.rs`
    - `AsyncNodeHierarchyReplaySummary`
    - `AsyncNodeHierarchyCancellationReport`

- Added explicit runtime counters for the new Phase 4 work in
  `crates/worth-signal/src/data/telemetry.rs`:
  - `async_node_interior_gate_admission_count`
  - `async_node_hierarchical_propagation_count`

- Split the runtime work into dedicated state modules instead of feeding the
  async-capability files into god-file territory:
  - `crates/worth-signal/src/logic/transaction/runtime/state/async_capability/gate.rs`
  - `crates/worth-signal/src/logic/transaction/runtime/state/async_capability/hierarchy.rs`
  - `mod.rs` now exposes a small `is_interior_async_gate(...)` helper and keeps
    the surface factored

- Added the Phase 4 runtime surfaces:
  - `async_node_gate_state_report(node)`
  - `async_node_hierarchy_replay_summary(root)`
  - `cancel_async_node_request(handle, reason)`

- Important design choice from this batch:
  - restore rekeys branch-epoch handle identity
  - the first gate/hierarchy tests initially compared full restored request
    handles and failed for the right reason
  - the fix was not to weaken the guarantee, but to make the guarantee honest:
    gate and hierarchy digests now canonicalize semantic request identity as
    `(request_id, generation)` instead of treating branch-epoch churn as user-
    visible truth
  - this keeps replay/restore parity honest without pretending restored handles
    are byte-identical to pre-restore ones

- Added new dedicated runtime tests:
  - `crates/worth-signal/src/tests/async_node_gate_runtime.rs`
    - `async_node_interior_gate_report_tracks_dependency_shape_and_restores_identically`
    - `async_node_gate_state_report_rejects_undeclared_owner`
  - `crates/worth-signal/src/tests/async_node_hierarchy_runtime.rs`
    - `async_node_hierarchy_cancellation_propagates_and_replay_summary_restores_identically`

- Added compile-fail privacy coverage for the new proof artifacts:
  - `async_node_gate_state_report_fields_are_private`
  - `async_node_hierarchy_replay_summary_fields_are_private`
  - `async_node_hierarchy_cancellation_report_fields_are_private`

- One small proof-boundary correction from verification:
  - the new trybuild fixtures needed to import the async-node proof artifacts
    from `worth_signal::facade::*`, not the crate root
  - I kept the root export surface narrow rather than widening it just to make
    the fixtures easier

- File hygiene after the Phase 4 split remains acceptable:
  - no touched file crossed 400 lines
  - the new data/runtime/test files are all narrowly scoped
  - `state/async_capability/admission.rs` is still the largest nearby file at
    353 lines and remains the best next cleanup candidate if it grows again

- Verification passed:
  - `cargo fmt -p worth-signal`
  - `cargo test -p worth-signal async_node_gate_ -- --nocapture`
  - `cargo test -p worth-signal async_node_hierarchy_ -- --nocapture`
  - `TRYBUILD=overwrite cargo test -p worth-signal resource_compile_fail_boundaries_hold -- --nocapture`
  - `cargo test -p worth-signal`
  - `git diff --check` clean aside from the existing LF/CRLF warnings

- Full crate after this batch: `886 passed, 23 ignored`.

## QA loop - Milestone D Phase 4 gate/hierarchy proof hardening

- Ran a hostile QA pass against the new interior-gate / hierarchy slice and
  found two real weaknesses:
  1. the new gate and hierarchy surfaces were still reporting generic
     `SummaryRead`/resource boundary posture instead of owning named Phase 4
     measurement boundaries
  2. the hierarchy proof was restore-only on one branch; it did not directly
     certify branch-local replay/restore honesty across equivalent sibling
     branches

- Corrections:
  - added explicit `ResourceBoundaryKind` variants:
    - `AsyncNodeGateState`
    - `AsyncNodeHierarchyReplay`
    - `AsyncNodeHierarchyCancellation`
  - added dedicated performance-envelope constructors for those boundaries in
    `crates/worth-signal/src/data/resource/summary.rs`
  - updated:
    - `async_node_gate_state_report(...)`
    - `async_node_hierarchy_replay_summary(...)`
    - `cancel_async_node_request(...)`
    to record the new boundary kinds instead of hiding behind generic resource
    summary/cancellation posture
  - strengthened runtime proof:
    - gate test now asserts `AsyncNodeGateState`
    - hierarchy summary test now asserts `AsyncNodeHierarchyReplay`
    - added `async_node_hierarchy_restore_is_branch_local_and_checkpoint_honest`
      to prove:
      - inactive restore does not mutate sibling branch hierarchy truth
      - restored branch head stays checkpoint-honest
      - post-snapshot drift is erased on restore
      - equivalent restored sibling branches converge on identical hierarchy
        replay digests

- Useful small cleanup from the QA fix:
  - turned an otherwise-unused "feature drift" artifact into a stronger
    assertion that restore truly removes drift rather than merely matching a
    happy-path digest by coincidence

- Verification passed after QA corrections:
  - `cargo test -p worth-signal async_node_ -- --nocapture`
  - `cargo test -p worth-signal resource_compile_fail_boundaries_hold -- --nocapture`
  - `cargo test -p worth-signal`

- Full crate after QA corrections: `887 passed, 23 ignored`.

## Milestone D Phase 5 - public API and vocabulary sweep

- Continued Milestone D in spec order instead of jumping ahead. This slice was
  the public-boundary cleanup the milestone asked for: make async capability
  read like a node capability, keep compatibility aliases where useful, and
  stop exposing raw `NodeId -> async intent` shortcuts to external callers.

- Added a proof-bearing public handle:
  - `crates/worth-signal/src/data/async_node/capable.rs`
    - `AsyncCapableNode`
  - carries:
    - owning `NodeId`
    - registry digest
    - lowered bundle digest
    - payload-contract digest
  - owns public intent construction:
    - `request_intent()`
    - `request_intent_with_transaction_deadline(...)`
    - `revalidation_intent()`
    - `revalidation_intent_with_expected_active(...)`
    - `revalidation_intent_with_transaction_deadline(...)`

- Tightened the public boundary in
  `crates/worth-signal/src/data/async_node/request.rs`:
  - `AsyncNodeRequestIntent::new(...)` and
    `AsyncNodeRequestIntent::with_transaction_deadline(...)` are now
    `pub(crate)`
  - `AsyncNodeRevalidationIntent::new(...)`,
    `with_expected_active(...)`, and
    `with_transaction_deadline(...)` are now `pub(crate)`
  - public callers now mint intents through `AsyncCapableNode` rather than
    directly from `NodeId`
  - internal tests can still use the constructors where needed

- Added capability-first runtime/API helpers:
  - `SignalRuntime::attach_async_capability(...) -> AsyncCapableNode`
  - `SignalRuntime::async_capable_node(node) -> Option<AsyncCapableNode>`
  - keyed-computation helpers in
    `crates/worth-signal/src/logic/transaction/runtime/async_keyed.rs`:
    - `attach_async_capability(...)`
    - `async_capable_node(...)`
  - left lower-level declaration/freeze/lower APIs in place as compatibility
    vocabulary rather than ripping them out

- Added dedicated public API runtime tests in
  `crates/worth-signal/src/tests/async_node_public_api_runtime.rs`:
  - `attach_async_capability_returns_handle_that_owns_public_intent_building`
  - `keyed_attach_async_capability_reads_like_node_capability_not_subsystem_switch`

- Added compile-fail boundary proof:
  - `async_capable_node_fields_are_private`
  - `async_node_request_intent_constructor_is_private`
  - `async_node_revalidation_intent_constructor_is_private`

- Small but useful implementation correction during verification:
  - the trybuild fixture for `AsyncCapableNode` needed to import from
    `worth_signal::facade::core::AsyncCapableNode`, not
    `worth_signal::facade::AsyncCapableNode`
  - kept the facade narrow instead of widening exports just to make the
    fixture shorter

- Another small cleanup from implementation:
  - fixed a borrow-checker issue in keyed capability rediscovery by resolving
    the keyed node id before calling back into the runtime, rather than trying
    to borrow `runtime` twice in one expression

- File hygiene stayed healthy in this slice:
  - new `data/async_node/capable.rs` is small and isolated
  - new `tests/async_node_public_api_runtime.rs` keeps the public-boundary
    proof out of the larger runtime test files
  - no touched file crossed 400 lines

- Verification passed:
  - `cargo fmt -p worth-signal`
  - `cargo test -p worth-signal async_node_ -- --nocapture`
  - `TRYBUILD=overwrite cargo test -p worth-signal resource_compile_fail_boundaries_hold -- --nocapture`
  - `cargo test -p worth-signal`

- Full crate after this batch: `889 passed, 23 ignored`.

## Milestone D Phase 6 - diagnostics, replay, and historical parity closeout

- Continued Milestone D in spec order with the first real Phase 6 surface:
  prove that capability-first async nodes share the same replay, diagnostics,
  branch-restore, and retained-history story as the legacy resource-shaped
  path, without inventing a second truth model.

- Added new async-node history artifacts in
  `crates/worth-signal/src/data/async_node/history.rs`:
  - `AsyncNodeHistoricalParityReport`
  - `DeniedAsyncNodeHistoricalParity`
  - `AsyncNodeHistoricalParityDenialClass`

- The new report is intentionally richer than a one-line digest:
  - carries the attached node id
  - carries the current capability registry / bundle / payload digests
  - embeds the canonical `ResourceReplayReconstructionReport`
  - carries either:
    - `ResourceDiagnosticsSummary`, or
    - `ResourceDiagnosticsExpansionDenial`
  - carries the latest `ResourceBranchRestoreReport` when present
  - owns a parity digest over that whole story

- Added a named Phase 6 performance boundary:
  - `ResourceBoundaryKind::AsyncNodeHistoricalParity`
  - `ResourceBoundaryPerformanceEnvelope::async_node_historical_parity(...)`

- Added explicit async-node telemetry for the closeout surface:
  - `async_node_historical_parity_count`
  - `async_node_capability_broad_scan_denial_count`

- Implemented the runtime surface in
  `crates/worth-signal/src/logic/transaction/runtime/state/async_capability/history.rs`:
  - `async_node_historical_parity_report(&AsyncCapableNode, budget)`

- Important honesty rule in that runtime path:
  - the report only materializes if the public `AsyncCapableNode` handle still
    names the current canonical capability lineage
  - it denies instead of scanning around drift when:
    - owner is no longer live
    - capability was restored away / is no longer declared
    - registry digest drifted
    - lowered bundle digest drifted
    - payload-contract digest drifted
  - this is where `async_node_capability_broad_scan_denial_count` is charged:
    we refuse to silently hunt for â€œprobably the rightâ€ capability truth

- One small API judgment from this batch:
  - I started to add a keyed convenience helper for direct historical parity
    reads, then backed it out because it forced a mushier failure story
  - kept the cleaner surface instead:
    attach or rediscover an `AsyncCapableNode`, then ask the runtime for the
    historical parity report through that typed handle
  - this felt more honest than shipping a cute shortcut that hid missing-
    capability failure under panic or vague errors

- Added dedicated runtime tests in
  `crates/worth-signal/src/tests/async_node_history_runtime.rs`:
  - `async_node_historical_parity_report_matches_legacy_resource_truth`
  - `keyed_attached_async_node_historical_parity_still_uses_runtime_owned_truth`
  - `async_node_historical_parity_report_rejects_stale_handle_after_restore`

- The stale-handle restore test is the key hostile proof:
  - capture a snapshot before capability attachment
  - attach capability and keep the public handle
  - restore the older snapshot
  - require the old handle to deny as
    `AsyncNodeHistoricalParityDenialClass::UndeclaredCapability`
  - this proves restore can erase capability attachment honestly instead of
    letting an old public handle resurrect historical truth by accident

- Added compile-fail privacy coverage:
  - `async_node_historical_parity_report_fields_are_private`
  - `denied_async_node_historical_parity_fields_are_private`

- File hygiene stayed within the current rule set:
  - `data/async_node/history.rs` is 223 lines
  - `state/async_capability/history.rs` is 47 lines
  - `tests/async_node_history_runtime.rs` is 127 lines
  - no touched file crossed 400 lines

- Verification passed:
  - `cargo fmt -p worth-signal`
  - `cargo test -p worth-signal async_node_ -- --nocapture`
  - `TRYBUILD=overwrite cargo test -p worth-signal resource_compile_fail_boundaries_hold -- --nocapture`
  - `cargo test -p worth-signal`

- Full crate after this batch: `892 passed, 23 ignored`.
- QA loop for Milestone D Phase 6 found two real gaps in historical parity:
  - the parity report carried replay and diagnostics but not observation / explanation lineage
  - the test surface never exercised the diagnostics-denial lane, so the report could look honest while only proving the rich-summary path

- Corrected the parity surface by threading `Option<ResourceObservationBatchReport>` through:
  - `data/async_node/history.rs`
  - `state/async_capability/history.rs`
  - and binding that observation artifact into the parity digest

- Strengthened `async_node_history_runtime.rs`:
  - the legacy-truth parity test now registers an observer, commits an admitted completion, and proves the parity report preserves the same observation batch lineage as the direct runtime surface
  - added `async_node_historical_parity_report_carries_diagnostics_denial_without_losing_replay_truth`
    to prove retained-summary-only diagnostics denial still returns a parity report with canonical replay truth intact

- Small mechanical correction that fell out during the fix:
  - `ResourceObservationBatchReport`, `ResourceObservationEvent`, and
    `ObservedResourceNodeState` now derive `Deserialize` so the historical parity
    report's existing serde surface remains structurally true after carrying
    observation lineage

- Verification after QA corrections:
  - `cargo fmt -p worth-signal`
  - targeted parity tests passed
  - full `cargo test -p worth-signal` passed
  - full crate now: `893 passed, 23 ignored`
  - separate parallel runs of `async_node_` and `resource_compile_fail_boundaries_hold`
    hit a Windows linker file-lock race on `worth_signal-...exe`, but the same
    coverage passed inside the full serialized crate test run

- Built the next Milestone D Phase 6 closeout slice: capability-first vs legacy
  async equivalence.
  - Added `data/async_node/equivalence.rs` with:
    - `AsyncNodeCapabilityEquivalenceReport`
    - `DeniedAsyncNodeCapabilityEquivalence`
    - `AsyncNodeCapabilityEquivalenceDenialClass`
  - Added runtime surface in `state/async_capability/equivalence.rs` so a public
    `AsyncCapableNode` handle plus capability declaration can produce one
    digest-backed equivalence artifact or a typed denial
  - Added named boundary/counter support:
    - `ResourceBoundaryKind::AsyncNodeCapabilityEquivalence`
    - `ResourceBoundaryPerformanceEnvelope::async_node_capability_equivalence(...)`
    - `telemetry.resource.async_node_capability_equivalence_count`
  - Exposed the new proof-bearing types through the facade
  - Added hostile/runtime tests in `tests/async_node_equivalence_runtime.rs`:
    - rich leaf workload parity against direct legacy runtime truth
    - stale-handle denial after restore
  - Added compile-fail privacy fixtures:
    - `tests/ui/async_node_capability_equivalence_report_fields_are_private.rs`
    - `tests/ui/denied_async_node_capability_equivalence_fields_are_private.rs`

- One honest correction fell out during implementation:
  - the new equivalence report derived `Deserialize`, but nested
    `AsyncNodeCapabilityAliasLoweringProof` did not yet support it
  - fixed by deriving `Deserialize` on the alias-lowering proof instead of
    weakening the report surface

- File hygiene stayed under control for the touched slice:
  - `data/async_node/equivalence.rs` is 256 lines
  - `state/async_capability/equivalence.rs` is 83 lines
  - `tests/async_node_equivalence_runtime.rs` is 176 lines
  - `data/async_node/descriptor.rs` is 127 lines
  - no touched file crossed 400 lines

- Verification passed:
  - `cargo fmt -p worth-signal`
  - `cargo test -p worth-signal async_node_capability_equivalence_report_matches_legacy_runtime_truth_for_rich_leaf_workload -- --nocapture`
  - `cargo test -p worth-signal async_node_capability_equivalence_report_denies_stale_handle_after_restore -- --nocapture`
  - `cargo test -p worth-signal async_node_ -- --nocapture`
  - `cargo test -p worth-signal resource_compile_fail_boundaries_hold -- --nocapture`
  - `cargo test -p worth-signal`
  - full crate after this batch: `895 passed, 23 ignored`

- Built the next Milestone D Phase 6 closeout slice: canonical explanation
  lineage for capability-first async history and equivalence.
  - Extended `AsyncNodeHistoricalParityReport` to carry:
    - `Option<ExplanationSummary>`
    - `DiagnosticsAvailability` for explanation materialization
  - Bound explanation lineage into the historical parity digest so replay,
    observation, diagnostics, and explanation now all contribute to one
    capability-first history artifact
  - Extended `AsyncNodeCapabilityEquivalenceReport` with:
    - `explanation_digest: Option<String>`
    - `explanation_availability: DiagnosticsAvailability`
    so callers can compare capability-first vs legacy explanation truth without
    spelunking nested artifacts
  - Updated `state/async_capability/history.rs` to materialize explanation
    lineage through the runtime-owned graph observer and to account for the
    extra diagnostics work honestly in the parity boundary envelope
  - Updated `state/async_capability/equivalence.rs` to include explanation
    lineage in its top-level boundary accounting

- Important correction the tests forced us to make explicit:
  - resource diagnostics expansion denial and graph explanation-artifact denial
    are distinct policy surfaces
  - `retained_summary_only()` can deny rich resource diagnostics while
    explanation lineage still reconstructs under the active runtime policy
  - added dedicated hostile tests so we now prove both lanes instead of
    accidentally treating them as the same thing

- Strengthened async-node proof surface:
  - `async_node_historical_parity_report_matches_legacy_resource_truth`
    now compares direct explanation lineage and availability
  - `async_node_historical_parity_report_carries_diagnostics_denial_without_losing_replay_truth`
    now proves diagnostics denial while explanation still follows direct runtime
    policy truth
  - added `async_node_historical_parity_report_carries_explanation_denial_without_losing_replay_truth`
  - `async_node_capability_equivalence_report_matches_legacy_runtime_truth_for_rich_leaf_workload`
    now compares explanation digest and availability
  - added `async_node_capability_equivalence_report_carries_explanation_denial_without_losing_replay_truth`

- File hygiene stayed inside the line-count guardrails:
  - `data/async_node/history.rs` is 253 lines
  - `data/async_node/equivalence.rs` is 275 lines
  - `tests/async_node_history_runtime.rs` is 245 lines
  - `tests/async_node_equivalence_runtime.rs` is 226 lines
  - no touched file crossed 400 lines

- Verification passed:
  - `cargo fmt -p worth-signal`
  - targeted history/equivalence explanation tests passed
  - `cargo test -p worth-signal async_node_ -- --nocapture`
  - `cargo test -p worth-signal resource_compile_fail_boundaries_hold -- --nocapture`
  - `cargo test -p worth-signal`
  - full crate after this batch: `897 passed, 23 ignored`

## QA loop - Milestone D Phase 6 explanation/equivalence boundary

- Found and fixed one real proof-boundary gap in
  `async_node_capability_equivalence_report(...)`:
  - the report previously allowed a caller to pass a different async-capability
    declaration for the same node as the attached `AsyncCapableNode` handle
  - that could combine alias-lowering proof for declaration B with runtime truth
    from handle/declaration A and still emit an equivalence artifact
  - fixed by requiring registry, bundle, and payload digests from the public
    handle to match the alias-lowering proof before historical parity is
    consulted

- Added typed denial and hostile proof:
  - new denial class:
    `AsyncNodeCapabilityEquivalenceDenialClass::HandleDeclarationDigestMismatch`
  - new regression:
    `async_node_capability_equivalence_report_rejects_mismatched_declaration_for_same_node`

- Verification passed after the QA correction:
  - `cargo fmt -p worth-signal`
  - `cargo test -p worth-signal async_node_capability_equivalence_report_rejects_mismatched_declaration_for_same_node -- --nocapture`
  - `cargo test -p worth-signal async_node_capability_equivalence_report_matches_legacy_runtime_truth_for_rich_leaf_workload -- --nocapture`
  - `cargo test -p worth-signal async_node_ -- --nocapture`
  - `cargo test -p worth-signal`
  - full crate after QA fix: `898 passed, 23 ignored`

## Milestone D implementation batch - keyed and hierarchy historical closeout

- Built the next honest Phase 6 closeout slice instead of jumping ahead:
  - keyed capability-first historical parity and capability-equivalence wrappers
  - hierarchy-aware historical parity wrapper for interior async chains
  - explicit public proof artifacts for â€œthis is the same async truth, but now
    in keyed/family and hierarchy formâ€

- New async-node proof artifacts:
  - `AsyncKeyedNodeHistoricalParityReport`
  - `DeniedAsyncKeyedNodeHistoricalParity`
  - `AsyncKeyedNodeCapabilityEquivalenceReport`
  - `DeniedAsyncKeyedNodeCapabilityEquivalence`
  - `AsyncNodeHierarchyHistoricalParityReport`
  - `DeniedAsyncNodeHierarchyHistoricalParity`

- New explicit boundaries and counters:
  - `ResourceBoundaryKind::AsyncKeyedNodeHistoricalParity`
  - `ResourceBoundaryKind::AsyncKeyedNodeCapabilityEquivalence`
  - `ResourceBoundaryKind::AsyncNodeHierarchyHistoricalParity`
  - `async_keyed_node_historical_parity_count`
  - `async_keyed_node_capability_equivalence_count`
  - `async_node_hierarchy_historical_parity_count`

- Important implementation truth we made explicit:
  - hierarchy historical parity must not pretend pre-restore and post-restore
    parity digests are identical when branch-restore artifacts differ
  - restore also rewinds telemetry, so pre-restore proof reads do not survive
    snapshot restore as live counter truth
  - the tests now certify semantic replay/observation/explanation parity plus
    explicit restore provenance instead of flattening those into one fake
    all-purpose digest claim

- File hygiene stayed inside the line-count guardrails for new work:
  - `data/async_node/keyed_history.rs` is 229 lines
  - `data/async_node/keyed_equivalence.rs` is 208 lines
  - `data/async_node/hierarchy_history.rs` is 162 lines
  - `state/async_capability/keyed_history.rs` is 64 lines
  - `state/async_capability/keyed_equivalence.rs` is 81 lines
  - `state/async_capability/hierarchy_history.rs` is 65 lines
  - `tests/async_node_closeout_runtime.rs` is 290 lines
  - no new touched file crossed 400 lines

- Verification passed:
  - `cargo fmt -p worth-signal`
  - `cargo test -p worth-signal async_node_closeout_ -- --nocapture`
  - `cargo test -p worth-signal resource_compile_fail_boundaries_hold -- --nocapture`
  - `cargo test -p worth-signal async_node_ -- --nocapture`
  - `cargo test -p worth-signal`
  - full crate after this batch: `903 passed, 23 ignored`

## Milestone D implementation batch - milestone certification and closeout surface

- Kept Milestone D in spec order and closed the next honest gap: we now have a
  milestone-level certification surface for capability-first async nodes rather
  than a pile of disconnected proof artifacts.

- Added async-node certification artifacts under
  `crates/worth-signal/src/data/async_node/certification/`:
  - compile-time boundary proof
  - scenario matrix
  - performance closeout
  - final certification run

- New public proof-bearing types include:
  - `AsyncNodeCompileTimeBoundaryProof`
  - `AsyncNodeMilestoneDScenarioRow`
  - `AsyncNodeMilestoneDScenarioMatrix`
  - `AsyncNodeMilestoneDPerformanceCloseout`
  - `AsyncNodeMilestoneDCertificationRun`

- New acceptance coverage is explicit for all Milestone D lanes:
  - attachment equivalence
  - condition-gated admission
  - aspect/keyed capability
  - previous-value + temporal admission parity
  - interior async gate equivalence
  - hierarchy replay/cancellation
  - legacy alias compatibility
  - compile-time capability boundary

- Important implementation correction during the batch:
  - the first certification file came in too large, so it was split into a
    dedicated certification module tree instead of leaving a new god file in
    place
  - final line counts after cleanup:
    - `certification/mod.rs` 85
    - `certification/compile_time.rs` 64
    - `certification/matrix.rs` 340
    - `certification/performance.rs` 142
    - `certification/run.rs` 92
    - `tests/async_node_certification_runtime.rs` 358

- Added hostile/runtime proof:
  - full Milestone D certification run builds from real async-node reports
  - compile-time boundary proof rejects missing required fixtures
  - scenario matrix rejects mismatched gate vs historical lineage

- Added compile-fail privacy fixtures for:
  - `async_node_compile_time_boundary_proof_fields_are_private`
  - `async_node_milestone_d_scenario_matrix_fields_are_private`
  - `async_node_milestone_d_performance_closeout_fields_are_private`
  - `async_node_milestone_d_certification_run_fields_are_private`

- Verification passed:
  - `cargo fmt -p worth-signal`
  - `cargo test -p worth-signal async_node_milestone_d_ -- --nocapture`
  - `cargo test -p worth-signal async_node_ -- --nocapture`
  - `cargo test -p worth-signal resource_compile_fail_boundaries_hold -- --nocapture`
  - `cargo test -p worth-signal`
  - full crate after this batch: `906 passed, 23 ignored`

## Milestone D QA loop - certification surface proof hardening

- Ran a hostile QA pass on the new Milestone D milestone-level certification
  surface against `MENTALITY.md`, `arch_laws.md`, `domain_laws.md`,
  `perf_laws.md`, `milestone-d-plan.md`, and `test-requirements.md`.

- Found two real weaknesses in the certification layer:
  - the final certification run was too trusting of report shape and only
    counted row lengths instead of re-validating exact scenario/claim coverage
    and digest continuity
  - the performance closeout rows did not bind the actual boundary performance
    envelope, so the closeout could certify a named claim without preserving
    the scenario boundary posture it was supposed to prove

- Corrected both:
  - `AsyncNodeMilestoneDCertificationRun` now re-validates exact scenario
    coverage, evidence-lane counts, required compile-time fixtures, compile-time
    proof digest continuity, scenario-matrix digest continuity, exact
    performance-claim coverage, per-claim boundary/performance-envelope parity,
    and performance-closeout digest continuity
  - `AsyncNodeMilestoneDPerformanceCloseoutRow` now carries `scenario_id` plus
    the full `ResourceBoundaryPerformanceEnvelope`, and its digest binds that
    performance envelope instead of only the scenario digest

- Added hostile regressions proving the new boundary:
  - `async_node_milestone_d_certification_run_rejects_duplicate_scenario_coverage`
  - `async_node_milestone_d_certification_run_rejects_WORTHd_performance_envelope`

- Verification passed after the corrections:
  - `cargo fmt -p worth-signal`
  - `cargo test -p worth-signal async_node_milestone_d_ -- --nocapture`
  - `cargo test -p worth-signal async_node_ -- --nocapture`
  - `cargo test -p worth-signal resource_compile_fail_boundaries_hold -- --nocapture`
  - `cargo test -p worth-signal`
  - full crate after the QA loop: `908 passed, 23 ignored`

## Milestone D implementation batch - combined async-node nightmare suite

- Continued the strict Milestone D test-suite closeout instead of starting new
  substrate work.

- Refactored the oversized certification test surface by extracting shared
  workload and report-building helpers into:
  - `crates/worth-signal/src/tests/async_node_milestone_d_support.rs`
  - kept `async_node_certification_runtime.rs` small and focused on actual
    certification assertions

- Added a new hostile combined-suite lane in:
  - `crates/worth-signal/src/tests/async_node_nightmare_runtime.rs`

- The new nightmare workload now combines:
  - capability-first vs legacy alias equivalence
  - interior async gate behavior
  - keyed/family-local capability
  - async hierarchy
  - observation and explanation lineage
  - cancellation drift
  - snapshot restore / replay convergence

- Important correctness correction:
  - the first restore assertion was too strong in the wrong direction because
    it required explanation digest identity across pre-restore and restored
    worlds even though explanation lineage can legitimately carry restore
    provenance
  - the second restore assertion was also wrong because it compared restored
    hierarchy history against the drifted post-cancellation world
  - corrected both to the honest strict claims:
    - restored equivalence must preserve lifecycle/output/observation truth and
      explanation availability, while still carrying explanation lineage
    - restored hierarchy history must match the pre-drift checkpoint-honest
      baseline and must remain distinct from the drifted cancellation world

- Verification passed:
  - `cargo fmt -p worth-signal`
  - `cargo test -p worth-signal async_node_nightmare_ -- --nocapture`
  - `cargo test -p worth-signal async_node_milestone_d_ -- --nocapture`
  - `cargo test -p worth-signal async_node_ -- --nocapture`
  - `cargo test -p worth-signal resource_compile_fail_boundaries_hold -- --nocapture`
  - `cargo test -p worth-signal`
  - full crate after the batch: `909 passed, 23 ignored`

## Milestone D implementation batch - stricter gate and hierarchy race tests

- Continued the Milestone D strict-suite hardening with a focus on making the
  direct interior-gate and hierarchy lanes actually hostile instead of merely
  representative.

- Added a shared async-node completion helper in:
  - `crates/worth-signal/src/tests/async_node_support.rs`

- Added new focused strict-suite files:
  - `crates/worth-signal/src/tests/async_node_gate_visibility_runtime.rs`
  - `crates/worth-signal/src/tests/async_node_hierarchy_race_runtime.rs`

- New hard assertions now cover:
  - **mid-graph gate continuity under pending policy drift**
    - same lifecycle class, different output continuity truth
    - same committed output identity, different gate digest
    - continuity counters prove both preserve and hide lanes actually fired
  - **mid-graph gate continuity under rejection policy drift**
    - same rejected lifecycle class, different output continuity truth
    - late completions denied explicitly as `Rejected`
    - gate digest divergence proves visibility truth is not being flattened
  - **hierarchy stale-authority race**
    - descendant completion denies as `Cancelled` after propagated ancestor
      cancellation
    - the same descendant completion denies as `Stale` after restore rekeys
      branch-epoch handle identity
    - restored hierarchy replay stays distinct from the drifted cancelled replay
      world

- Useful corrections found while tightening the assertions:
  - request/rejection visibility was not an observation-batch boundary in the
    way the first test draft assumed, so the direct gate tests were moved onto
    the runtime-owned gate-state / continuity / denial / telemetry surfaces
    instead of certifying a fake observation promise
  - restore rewinds telemetry, so cancellation-vs-stale denial counters must be
    asserted on the correct side of the restore boundary rather than treated as
    cumulative across both worlds
  - the stricter tests temporarily pushed two files over the size ceiling, so
    the new pressure lanes were split into dedicated files and the touched test
    files were brought back under 400 lines

- Verification passed:
  - `cargo fmt -p worth-signal`
  - `cargo test -p worth-signal async_node_gate_ -- --nocapture`
  - `cargo test -p worth-signal async_node_hierarchy_ -- --nocapture`
  - `cargo test -p worth-signal async_node_ -- --nocapture`
  - `cargo test -p worth-signal`
  - full crate after this batch: `912 passed, 23 ignored`

## Milestone D implementation batch - restore-lineage and public rediscovery pressure

- Continued the Milestone D strict-suite hardening with a focused restore-churn
  lane for public handles and keyed capability lineage.

- Added:
  - `crates/worth-signal/src/tests/async_node_restore_lineage_runtime.rs`

- New hard assertions now cover:
  - **public handle restored before attachment**
    - restoring to a pre-attachment checkpoint leaves no rediscoverable handle
    - stale public handle historical parity denies explicitly as
      `UndeclaredCapability`
    - capability equivalence preserves the inner historical denial instead of
      certifying a shape-only report
  - **keyed restore + rebind lineage drift**
    - restore to a pre-attachment checkpoint erases the old keyed capability
    - reattaching the same keyed node with a different payload produces a new
      rediscoverable handle lineage
    - old binding + new handle and new binding + old handle both deny as
      `BindingHandleDigestMismatch`
    - same-node old declaration against the new keyed lineage denies through
      `HandleDeclarationDigestMismatch` rather than slipping through on node id
      alone

- This was an intentional strict-suite hardening exception rather than new
  substrate machinery; the right milestone advance here was to make the
  adversarial restore/declaration assertions sharper without feeding the
  existing test files into god-file territory.

- Verification passed:
  - `cargo fmt -p worth-signal`
  - `cargo test -p worth-signal async_node_restore_lineage_ -- --nocapture`
  - `cargo test -p worth-signal async_node_ -- --nocapture`
  - `cargo test -p worth-signal`
  - full crate after this batch: `914 passed, 23 ignored`

## Milestone D implementation batch - interior gate timeout and legality-drift parity

- Continued the Milestone D strict-suite hardening with a direct ownership pass
  on remaining gate and condition-gating requirement surfaces.

- Added:
  - `crates/worth-signal/src/tests/async_node_gate_churn_runtime.rs`

- New hard assertions now cover:
  - **interior async gate timeout visibility**
    - same `TimedOut` lifecycle truth under preserve-vs-hide timeout policies
    - no active request handle after timeout
    - same committed output identity but different continuity digest and gate
      digest
    - downstream dependence facts remain the same while visibility truth
      changes
  - **active gate legality drift while a lineage is live**
    - non-matching upstream dirtiness shifts revalidation into
      `RefreshEligibleNoNewLineage`
    - lifecycle stays `Pending`
    - in-flight width stays local to the existing lineage
    - transaction-scoped legality drift rolled back before commit preserves the
      baseline gate state exactly
    - restore plus identical upstream drift reproduces the same admission
      decision digest and the same gate truth

- Useful correction found while tightening the assertions:
  - restore rekeys branch-epoch handle identity even when request lineage is
    semantically the same, so the restore assertions were tightened around the
    canonical semantic pair `(request_id, generation)` rather than pretending
    restored handles remain byte-identical

- This was another deliberate strict-suite hardening exception rather than new
  substrate machinery; the right milestone advance here was direct adversarial
  proof for requirements `22` and `24`, not more implementation breadth.

- Verification passed:
  - `cargo fmt -p worth-signal`
  - `cargo test -p worth-signal async_node_gate_ -- --nocapture`
  - `cargo test -p worth-signal async_node_ -- --nocapture`
  - `cargo test -p worth-signal`
  - full crate after this batch: `916 passed, 23 ignored`

## Implementation - Milestone D strict-suite hardening for integrated keyed lineage and hierarchy restore churn

- Kept the work focused on the strict-suite gap instead of inventing more runtime surface:
  - split the overloaded nightmare support out of the milestone certification helper
  - added a second integrated nightmare lane that forces keyed declaration lineage drift, hierarchy restore churn, and explanation continuity into one coherent proof story

- File-structure cleanup:
  - `crates/worth-signal/src/tests/async_node_milestone_d_support.rs` is back to milestone certification input assembly
  - combined nightmare support now lives in:
    - `crates/worth-signal/src/tests/async_node_nightmare_support.rs`
  - restore-lineage nightmare support now lives in:
    - `crates/worth-signal/src/tests/async_node_restore_lineage_nightmare_support.rs`
  - `crates/worth-signal/src/tests/mod.rs` now registers the extra support module explicitly

- New integrated strict test:
  - `crates/worth-signal/src/tests/async_node_nightmare_runtime.rs`
    - `async_node_nightmare_restore_lineage_keeps_hierarchy_honest_and_rebinds_keyed_explanations`

- What the new lane now proves:
  - hierarchy historical parity is checkpoint-honest across restore even when keyed capability churn happened after the checkpoint
  - post-cancellation hierarchy drift stays distinct from the restored hierarchy world
  - stale keyed historical/equivalence requests after restore fail through explicit denial artifacts instead of certifying a report-shaped success
  - rebound keyed capability on the same keyed owner preserves family/key/node identity while changing payload/declaration lineage
  - old binding + new handle denies as keyed binding/handle digest mismatch
  - old declaration + rebound keyed lineage denies as `HandleDeclarationDigestMismatch`
  - keyed explanation availability and explanation summary remain stable across the restore/rebind churn

- Two useful honesty corrections fell out while building it:
  - the first draft snapshotted before the keyed owner existed, which only proved a dead-owner denial; the workload now materializes the keyed owner before snapshot so it certifies erased capability lineage on the same owner instead
  - the first draft treated policy bundle digest as if it encoded payload lineage; the stricter assertion now binds payload/declaration digests instead of demanding fake bundle drift

- Verification passed:
  - `cargo fmt -p worth-signal`
  - `cargo test -p worth-signal async_node_nightmare_workload_preserves_combined_capability_truth_across_restore -- --nocapture`
  - `cargo test -p worth-signal async_node_nightmare_restore_lineage_keeps_hierarchy_honest_and_rebinds_keyed_explanations -- --nocapture`
  - `cargo test -p worth-signal async_node_nightmare_ -- --nocapture`
  - `cargo test -p worth-signal async_node_ -- --nocapture`
  - `cargo test -p worth-signal`
  - `git diff --check` clean aside from the existing LF/CRLF warnings

- Full crate after this batch: `917 passed, 23 ignored`.

## Implementation - Milestone D strict-suite hardening for public handle rediscovery under restore churn

- Kept the focus on the remaining weak proof surface instead of growing new runtime machinery:
  - added a dedicated public-handle restore/relookup adversarial lane
  - left the original public API smoke tests small and readable

- New test file:
  - `crates/worth-signal/src/tests/async_node_public_api_restore_runtime.rs`
- Module registration updated in:
  - `crates/worth-signal/src/tests/mod.rs`

- What the new public-surface tests now prove:
  - plain async-capable nodes:
    - a public handle returned from `attach_async_capability(...)` remains semantically valid after restore when the attachment existed at the checkpoint
    - `runtime.async_capable_node(node)` rediscovery yields the same registry/bundle/payload lineage
    - historical parity and equivalence digests match exactly between the original handle and the rediscovered handle
    - explanation availability and explanation lineage stay identical across that public relookup
  - keyed async-capable nodes:
    - rediscovery after restore preserves the original public lineage when the attachment existed at the checkpoint
    - restore to a pre-attachment checkpoint plus rebound public attachment forces the old public handle to fail closed
    - the rebound public handle certifies against the rebound declaration, not the old one
    - old public handles deny through explicit drift artifacts instead of certifying by same-node coincidence

- Useful correction from the batch:
  - the first keyed stale-handle assertion expected `BundleDigestDrift`, but the runtime is honestly detecting the public rebind at the more specific `PayloadContractDigestDrift` layer
  - the test now asserts the actual semantic boundary instead of forcing the wrong artifact

- Verification passed:
  - `cargo fmt -p worth-signal`
  - `cargo test -p worth-signal async_node_public_api_ -- --nocapture`
  - `cargo test -p worth-signal async_node_restore_lineage_ -- --nocapture`
  - `cargo test -p worth-signal async_node_ -- --nocapture`
  - `cargo test -p worth-signal`
  - `git diff --check` clean aside from the existing LF/CRLF warnings

- Full crate after this batch: `919 passed, 23 ignored`.

## Implementation - Milestone D strict-suite hardening for public branch-churn rediscovery

- Kept the work focused on the remaining integrated strict-suite gap instead of growing runtime surface:
  - added a dedicated adversarial lane mixing public handle rediscovery, interior gate visibility churn, keyed public rebind drift, and branch-local restore behavior
  - kept it in its own file so the existing public API and restore-lineage suites stayed readable

- New test file:
  - `crates/worth-signal/src/tests/async_node_public_branch_churn_runtime.rs`
- Module registration updated in:
  - `crates/worth-signal/src/tests/mod.rs`

- What the new branch-churn tests now prove:
  - plain public async gate handles:
    - a pending/hidden-output drift on one branch does not contaminate a sibling branch
    - inactive restore reinstates the checkpoint-honest gate world instead of the drifted pending world
    - rediscovered public handles after restore preserve the same gate/history/equivalence semantic truth as the baseline branch
  - keyed public handles:
    - restore to a pre-attachment checkpoint erases the old public capability lineage on that branch
    - reattaching on the same keyed owner with a new payload/declaration forces the old public handle to fail closed
    - sibling branches can still attach and certify the original keyed lineage without inheriting the feature branch rebind drift
    - restoring the feature branch back to its attached checkpoint reinstates the original keyed public lineage honestly

- Useful honesty corrections from the batch:
  - full parity/equivalence artifact equality across restore was too provenance-sensitive; the test now compares the honest semantic sub-digests and availability surfaces instead of demanding fake byte-identical wrappers
  - keyed rebind churn could not be tested by double-attaching on one live branch, because the runtime correctly rejects a second lowered descriptor on the same node; the workload now restores to a pre-attachment checkpoint before rebound attach
  - feature-branch inactive restore had to target a branch-local attached snapshot, not the main branch's pre-attachment snapshot

- Verification passed:
  - `cargo fmt -p worth-signal`
  - `cargo test -p worth-signal async_node_public_branch_churn_ -- --nocapture`
  - `cargo test -p worth-signal async_node_ -- --nocapture`
  - `cargo test -p worth-signal`
  - `git diff --check` clean aside from the existing LF/CRLF warnings

- Full crate after this batch: `921 passed, 23 ignored`.

## Implementation - Milestone D strict-suite hardening for public rediscovery plus hierarchy explanation churn

- Kept the work on the remaining integrated test-requirement seam instead of adding more isolated polite cases:
  - added a single branch-churn lane that mixes a public mid-graph gate, a hierarchical async root, explanation continuity, sibling-branch stability, and inactive restore
  - kept it in a fresh file so the earlier public branch-churn suite stayed under control

- New test file:
  - `crates/worth-signal/src/tests/async_node_public_hierarchy_branch_runtime.rs`
- Module registration updated in:
  - `crates/worth-signal/src/tests/mod.rs`

- What the new lane now proves:
  - a feature branch can drift a public interior gate into pending / hidden-output state without contaminating a sibling branch
  - the same feature branch can create hierarchy drift by cancelling a descendant request after the checkpoint
  - inactive restore reinstates both:
    - checkpoint-honest gate visibility truth
    - checkpoint-honest hierarchy replay / explanation truth
  - rediscovered public handles after restore still preserve the same semantic gate/history truth
  - sibling branches remain stable across the feature restore and keep their own gate/hierarchy truth intact

- Useful honesty corrections from the batch:
  - the first draft tried to compare full explanation summaries across restore, but restore honestly drops execution-record / semantic-segment provenance even when the explanation is semantically the same
  - the test now compares a stable explanation signature (node, materialization mode, state, output-change, memoization/reuse posture, and direct-cause kinds) instead of asking restore to certify fake byte-for-byte explanation wrappers
  - a nested mutable borrow in the sibling rediscovery check was cleaned up by materializing the rediscovered public handle before requesting the hierarchy historical report

- Verification passed:
  - `cargo fmt -p worth-signal`
  - `cargo test -p worth-signal async_node_public_hierarchy_branch_ -- --nocapture`
  - `cargo test -p worth-signal async_node_ -- --nocapture`
  - `cargo test -p worth-signal`
  - `git diff --check` clean aside from the existing LF/CRLF warnings

- Full crate after this batch: `922 passed, 23 ignored`.

## Implementation - Milestone D strict-suite hardening for keyed certification explanation lineage

- Kept the work focused on the milestone-level proof boundary instead of inventing more runtime surface:
  - tightened the keyed/family scenario validation in the Milestone D certification matrix
  - added a hostile WORTHd-report regression proving the matrix now rejects keyed explanation-lineage drift instead of trusting same family/key/node shell alone

- Main implementation changes:
  - `crates/worth-signal/src/data/async_node/certification/matrix.rs`
  - `crates/worth-signal/src/tests/async_node_certification_runtime.rs`

- What got stricter:
  - keyed certification rows must now agree on:
    - family/key/node identity
    - registry digest
    - lowered bundle digest
    - payload-contract digest
    - underlying historical parity digest
    - explanation availability
    - explanation digest derived from the historical parity explanation summary

- New hostile regression:
  - `async_node_milestone_d_scenario_matrix_rejects_keyed_explanation_lineage_drift`
  - it WORTHs the keyed equivalence artifact by mutating the nested explanation digest and proves the milestone matrix now fails closed with the right compatibility denial instead of accepting a report-shaped fake

- Why this mattered:
  - before this batch, the certification matrix could be satisfied by keyed reports that shared family/key/node identity while quietly drifting in payload/explanation truth
  - that was too trusting for the strict Milestone D closeout bar

- Verification passed:
  - `cargo fmt -p worth-signal`
  - `cargo test -p worth-signal async_node_milestone_d_ -- --nocapture`
  - `cargo test -p worth-signal async_node_ -- --nocapture`
  - `cargo test -p worth-signal`
  - `git diff --check` clean aside from the existing LF/CRLF warnings

- Full crate after this batch: `923 passed, 23 ignored`.

## Implementation - Milestone D certification inputs now source harsher gate evidence cleanly

- Kept the work focused on closeout ownership instead of adding more runtime mechanics:
  - extracted the public gate + hierarchy branch-churn workload into reusable support
  - rewired the Milestone D certification input assembly so the gate scenario now uses the harsher restored public branch-churn evidence instead of only the older combined workload
  - kept hierarchy certification on the root-shaped combined lane so the milestone row still certifies the right semantic boundary while descendant cancellation pressure remains covered by its direct race lane

- Main file changes:
  - `crates/worth-signal/src/tests/async_node_public_hierarchy_branch_support.rs`
  - `crates/worth-signal/src/tests/async_node_public_hierarchy_branch_runtime.rs`
  - `crates/worth-signal/src/tests/async_node_milestone_d_support.rs`
  - `crates/worth-signal/src/tests/mod.rs`

- Why this mattered:
  - before this batch, the direct branch-churn lane and the milestone certification lane were proving adjacent truths but not sharing evidence
  - now the milestone matrix has to acknowledge the nastier restored public gate story directly, while the hierarchy certification row still uses evidence shaped correctly for root-level replay/cancellation agreement

- Useful corrections from the batch:
  - trying to force one shared workload to satisfy both descendant-cancellation direct testing and root-shaped certification ownership made the hierarchy row lie about its root
  - the better split was:
    - public branch-churn workload keeps descendant cancellation and explanation-continuity pressure for direct test ownership
    - milestone certification imports only the restored gate evidence from that lane
    - hierarchy certification continues using the combined workload that already has root-aligned replay/cancellation/parity truth
  - the refactor also cleaned up file shape:
    - `async_node_public_hierarchy_branch_support.rs`: 313 lines
    - `async_node_public_hierarchy_branch_runtime.rs`: 116 lines
    - `async_node_milestone_d_support.rs`: 165 lines

- Verification passed:
  - `cargo fmt -p worth-signal`
  - `cargo test -p worth-signal async_node_public_hierarchy_branch_ -- --nocapture`
  - `cargo test -p worth-signal async_node_milestone_d_ -- --nocapture`
  - `cargo test -p worth-signal async_node_ -- --nocapture`
  - `cargo test -p worth-signal`
  - `git diff --check` clean aside from the existing LF/CRLF warnings

- Full crate after this batch: `923 passed, 23 ignored`.

## Implementation - Milestone D closeout docs and roadmap closure

- Audited the Milestone D spec, roadmap Phase 4, and test-requirements `21`-`25`
  against the actual async-node runtime, public API, compile-fail, nightmare,
  and milestone-certification suites.
- The remaining real gap was documentation honesty, not missing substrate:
  - `milestone-d-plan.md` still said `Planned`
  - roadmap Phase 4 acceptance evidence was missing the stricter gate,
    hierarchy, and compile-time closeout lanes
  - there was no milestone closeout ledger yet

- Added `_docs/worth_signal/milestone-d-closeout.md` with:
  - implementation surface map
  - must-ship acceptance map
  - direct-vs-combined requirement ownership for Milestone D acceptance lanes
    and test requirements `21`-`25`
  - compile-time boundary map
  - final verification and explicit closeout decision

- Updated:
  - `_docs/worth_signal/milestone-d-plan.md` -> `Status: Closed` + closeout link
  - `_docs/worth_signal/worth_signal_temporal_async_roadmap.md` -> Phase 4 now
    points at the closeout, names the full acceptance set, and has an explicit
    closeout-status section around `AsyncNodeMilestoneDCertificationRun`

## Refactor - resource_runtime god-file split into categorized directory

- Broke `crates/worth-signal/src/tests/resource_runtime.rs` (~14.5k lines) into
  a categorized module directory at
  `crates/worth-signal/src/tests/resource_runtime/`.
- New top-level categories:
  - `policy_descriptor.rs`
  - `policy_restore_compatibility.rs`
  - `replay_availability.rs`
  - `declaration_and_visibility.rs`
  - `cancellation_and_supersession.rs`
  - `timeout_and_retry.rs`
  - `revalidation.rs`
  - `milestone_b.rs`
  - `milestone_c.rs`
  - `diagnostics.rs`
  - `observation.rs`
  - `retention.rs`
  - `completion_admission.rs`
  - `support.rs`
- Important cleanup lesson:
  - sibling test modules were implicitly depending on the old single-file
    namespace; the honest fix was to keep shared helpers imported in the parent
    `mod.rs` with `use support::*;` instead of pretending they were a public
    re-export
- Verification passed after the split:
  - `cargo fmt -p worth-signal`
  - `cargo test -p worth-signal`
- Full crate after the refactor: `923 passed, 23 ignored`
- Remaining biggest sub-slices inside the new directory, if we keep going:
  - `milestone_b.rs` 1690 lines
  - `revalidation.rs` 1450 lines
  - `policy_descriptor.rs` 1287 lines
  - `support.rs` 1206 lines
  - `replay_availability.rs` 1158 lines

## Refactor - milestone_b test bucket split into local categorized modules

- Broke `crates/worth-signal/src/tests/resource_runtime/milestone_b.rs` into a
  local module directory:
  - `crates/worth-signal/src/tests/resource_runtime/milestone_b/mod.rs`
  - `.../certification.rs`
  - `.../hostile_workloads.rs`
  - `.../support.rs`
- Shape after the split:
  - `hostile_workloads.rs` 784 lines
  - `certification.rs` 580 lines
  - `support.rs` 330 lines
- Important cleanup lesson:
  - nested child modules under `resource_runtime::milestone_b` still relied on
    the parent module namespace, so shared local helpers had to be made
    `pub(super)` inside the new local `support.rs`
  - one `#[test]` attribute got orphaned by the mechanical split, which was a
    good reminder to verify the first boundary in every generated slice
- Verification passed:
  - `cargo fmt -p worth-signal`
  - `cargo test -p worth-signal resource_milestone_b -- --nocapture`
  - `cargo test -p worth-signal`
- Full crate after this refactor still: `923 passed, 23 ignored`
- Next obvious resource-runtime breakup targets:
  - `revalidation.rs` 1450 lines
  - `policy_descriptor.rs` 1287 lines
  - `support.rs` 1206 lines

## Refactor - revalidation test bucket split into policy-lane modules

- Broke `crates/worth-signal/src/tests/resource_runtime/revalidation.rs` into a
  local module directory:
  - `crates/worth-signal/src/tests/resource_runtime/revalidation/mod.rs`
  - `.../active_explicit.rs`
  - `.../forced.rs`
  - `.../dependency_observer.rs`
  - `.../terminal_fulfilled.rs`
  - `.../stale_after.rs`
- New shape:
  - `dependency_observer.rs` 415 lines
  - `terminal_fulfilled.rs` 344 lines
  - `stale_after.rs` 335 lines
  - `active_explicit.rs` 223 lines
  - `forced.rs` 136 lines
- Important cleanup lesson:
  - this split did not need a local support file; the parent `revalidation`
    module was enough shared import surface for `raw_completion` and the common
    runtime/test namespace
- Verification passed:
  - `cargo fmt -p worth-signal`
  - `cargo test -p worth-signal resource_revalidation -- --nocapture`
  - `cargo test -p worth-signal`
- Full crate after this refactor still: `923 passed, 23 ignored`
- Next biggest resource-runtime buckets now:
  - `policy_descriptor.rs` 1287 lines
  - `support.rs` 1206 lines
  - `replay_availability.rs` 1158 lines

## Refactor - policy_descriptor test bucket split into proof-lane modules

- Broke `crates/worth-signal/src/tests/resource_runtime/policy_descriptor.rs`
  into a local module directory:
  - `crates/worth-signal/src/tests/resource_runtime/policy_descriptor/mod.rs`
  - `.../descriptor_identity.rs`
  - `.../retry_runtime.rs`
  - `.../validation_lowering.rs`
  - `.../registry_integrity.rs`
- New shape:
  - `retry_runtime.rs` 447 lines
  - `validation_lowering.rs` 437 lines
  - `descriptor_identity.rs` 284 lines
  - `registry_integrity.rs` 121 lines
- Important cleanup lesson:
  - the old file mixed registry-integrity and validation-denial tests in source
    order, but they are cleaner as separate semantic lanes even if that means
    the new `validation_lowering.rs` is assembled from two disjoint source
    ranges from the original file
- Verification passed:
  - `cargo fmt -p worth-signal`
  - `cargo test -p worth-signal resource_policy_ -- --nocapture`
  - `cargo test -p worth-signal`
- Full crate after this refactor still: `923 passed, 23 ignored`
- Next biggest resource-runtime buckets now:
  - `support.rs` 1206 lines
  - `replay_availability.rs` 1158 lines
  - `declaration_and_visibility.rs` 1013 lines

## Refactor - replay_availability test bucket split into proof-lane modules

- Broke `crates/worth-signal/src/tests/resource_runtime/replay_availability.rs`
  into a local module directory:
  - `crates/worth-signal/src/tests/resource_runtime/replay_availability/mod.rs`
  - `.../availability_basics.rs`
  - `.../replay_policy.rs`
  - `.../historical_descriptor.rs`
  - `.../registry_evidence.rs`
- Honest cleanup during the split:
  - restored missing `#[test]` markers on the first three
    replay-availability basics after the mechanical file cut
  - restored the `#[test]` marker on the first replay-policy test after an
    orphaned trailing attribute got peeled off the previous file
- Verification passed:
  - `cargo fmt -p worth-signal`
  - `cargo test -p worth-signal resource_replay_availability -- --nocapture`
  - `cargo test -p worth-signal`
- Full crate after this refactor still: `923 passed, 23 ignored`

## Refactor - declaration_and_visibility test bucket split into visibility lanes

- Broke
  `crates/worth-signal/src/tests/resource_runtime/declaration_and_visibility.rs`
  into a local module directory:
  - `crates/worth-signal/src/tests/resource_runtime/declaration_and_visibility/mod.rs`
  - `.../declaration_admission.rs`
  - `.../pending_visibility.rs`
  - `.../timeout_cancellation_visibility.rs`
  - `.../rejection_supersession_visibility.rs`
- Important boundary cleanup:
  - the split exposed that `raw_completion` was really a shared test helper,
    not a local convenience; promoted it to an explicit shared helper through
    the new module seam instead of relying on file cohabitation
  - child modules now pull the parent surface intentionally via `use super::*;`
    rather than hidden old-file scope bleed
- Verification passed:
  - `cargo fmt -p worth-signal`
  - `cargo test -p worth-signal resource_declaration -- --nocapture`
  - `cargo test -p worth-signal`
- Full crate after this refactor still: `923 passed, 23 ignored`

## Refactor - timeout_and_retry test bucket split into timeout/deadline/retry lanes

- Broke `crates/worth-signal/src/tests/resource_runtime/timeout_and_retry.rs`
  into a local module directory:
  - `crates/worth-signal/src/tests/resource_runtime/timeout_and_retry/mod.rs`
  - `.../timeout_wakes.rs`
  - `.../inherited_deadlines.rs`
  - `.../retry_backoff.rs`
- New shape:
  - `timeout_wakes.rs` 422 source lines before formatting cleanup
  - `inherited_deadlines.rs` 198 source lines before formatting cleanup
  - `retry_backoff.rs` 371 source lines before formatting cleanup
- Verification passed:
  - `cargo fmt -p worth-signal`
  - `cargo test -p worth-signal`
- Full crate after this refactor still: `923 passed, 23 ignored`
- Next biggest resource-runtime buckets now:
  - `support.rs` 1206 lines
  - `policy_restore_compatibility.rs` 993 lines
  - `milestone_c.rs` 972 lines

## Refactor - policy_restore_compatibility test bucket split into compatibility lanes

- Broke
  `crates/worth-signal/src/tests/resource_runtime/policy_restore_compatibility.rs`
  into a local module directory:
  - `crates/worth-signal/src/tests/resource_runtime/policy_restore_compatibility/mod.rs`
  - `.../exact_and_parameter.rs`
  - `.../retention_and_diagnostics.rs`
  - `.../replay_policy_combinations.rs`
- Chosen lane boundaries:
  - exact descriptor + parameter drift
  - retention / diagnostics posture compatibility
  - replay-policy combination behavior
- Verification passed:
  - `cargo fmt -p worth-signal`
  - `cargo test -p worth-signal resource_policy_restore_compatibility -- --nocapture`
  - `cargo test -p worth-signal`
- Full crate after this refactor still: `923 passed, 23 ignored`
- Remaining biggest resource-runtime files now:
  - `support.rs` 1206 lines
  - `milestone_c.rs` 972 lines
  - `observation.rs` 907 lines

## Refactor - milestone_c test bucket split into fixture + owning proofs

- Broke `crates/worth-signal/src/tests/resource_runtime/milestone_c.rs`
  into a local module directory:
  - `crates/worth-signal/src/tests/resource_runtime/milestone_c/mod.rs`
  - `.../support.rs`
  - `.../certification.rs`
  - `.../scenario_rejections.rs`
- Important structural cleanup:
  - extracted the repeated milestone-C evidence world into
    `resource_milestone_c_policy_fixture()`
  - kept the big certification assertions in `certification.rs`
  - reduced the restore-denial mismatch check to a short owning test in
    `scenario_rejections.rs`
- Verification passed:
  - `cargo fmt -p worth-signal`
  - `cargo test -p worth-signal resource_milestone_c -- --nocapture`
  - `cargo test -p worth-signal`
- Full crate after this refactor still: `923 passed, 23 ignored`
- Remaining biggest resource-runtime files now:
  - `support.rs` 1206 lines
  - `observation.rs` 907 lines
  - `cancellation_and_supersession.rs` 790 lines

## Refactor - observation test bucket split into lifecycle and side-effect lanes

- Broke `crates/worth-signal/src/tests/resource_runtime/observation.rs`
  into a local module directory:
  - `crates/worth-signal/src/tests/resource_runtime/observation/mod.rs`
  - `.../identity_and_admission.rs`
  - `.../staging_and_transactions.rs`
  - `.../batch_reports.rs`
  - `.../denied_and_retry.rs`
- Chosen lane boundaries:
  - identity / completion admission
  - staging and transaction lifecycle application
  - observation batch policy shape and rollback safety
  - denied-completion and retry-schedule observation side effects
- Verification passed:
  - `cargo fmt -p worth-signal`
  - `cargo test -p worth-signal resource_observation -- --nocapture`
  - `cargo test -p worth-signal`
- Full crate after this refactor still: `923 passed, 23 ignored`
- Remaining biggest resource-runtime files now:
  - `support.rs` 1206 lines
  - `cancellation_and_supersession.rs` 790 lines
  - `completion_admission.rs` 786 lines

## Refactor - cancellation_and_supersession test bucket split into policy lanes

- Broke
  `crates/worth-signal/src/tests/resource_runtime/cancellation_and_supersession.rs`
  into a local module directory:
  - `crates/worth-signal/src/tests/resource_runtime/cancellation_and_supersession/mod.rs`
  - `.../supersession.rs`
  - `.../coalescing.rs`
  - `.../direct_cancellation.rs`
  - `.../dependent_cancellation.rs`
- Chosen lane boundaries:
  - active supersession and late-completion behavior
  - intent-equivalence coalescing
  - direct cancellation truth and grace-window posture
  - dependent cancellation propagation and timeout-wake retirement
- Verification passed:
  - `cargo fmt -p worth-signal`
  - `cargo test -p worth-signal resource_cancellation -- --nocapture`
  - `cargo test -p worth-signal`
- Full crate after this refactor still: `923 passed, 23 ignored`
- Remaining biggest resource-runtime files now:
  - `support.rs` 1206 lines
  - `completion_admission.rs` 786 lines
  - `retention.rs` 680 lines

## Refactor - completion_admission test bucket split into staging/batch/denial lanes

- Broke `crates/worth-signal/src/tests/resource_runtime/completion_admission.rs`
  into a local module directory:
  - `crates/worth-signal/src/tests/resource_runtime/completion_admission/mod.rs`
  - `.../staging_and_commit.rs`
  - `.../batch_admission.rs`
  - `.../denial_classes.rs`
- Kept the split semantic rather than line-count-driven:
  - staging/commit effects
  - batch admission behavior
  - denial-class proof lanes
- One focused rerun hit a transient Windows/MSVC linker artifact failure; resolved
  cleanly with `cargo clean -p worth-signal`, after which the full crate passed.
- Verification passed:
  - `cargo fmt -p worth-signal`
  - `cargo test -p worth-signal`
- Full crate after this refactor still: `923 passed, 23 ignored`

## Refactor - retention test bucket split into compaction/history lanes

- Broke `crates/worth-signal/src/tests/resource_runtime/retention.rs`
  into a local module directory:
  - `crates/worth-signal/src/tests/resource_runtime/retention/mod.rs`
  - `.../lifecycle_compaction.rs`
  - `.../branch_and_denied_history.rs`
  - `.../retry_lineage.rs`
  - `.../late_completion.rs`
- Chosen lane boundaries:
  - lifecycle retention compaction policy and explicit limits
  - branch restore and denied-completion retained-history availability
  - retained retry-lineage pruning and typed availability
  - late completion denial preservation after compaction
- The first mechanical cut left dangling `#[test]` seams and dropped a few test
  attributes at chunk boundaries; fixed those before accepting the split so the
  test surface stayed intact rather than quietly shrinking.
- Verification passed:
  - `cargo fmt -p worth-signal`
  - `cargo test -p worth-signal resource_retention -- --nocapture`
  - `cargo test -p worth-signal`
- Full crate after this refactor still: `923 passed, 23 ignored`
- Remaining biggest resource-runtime files now:
  - `support.rs`
  - `diagnostics.rs`

## Refactor - diagnostics test bucket split into summary/policy/digest lanes

- Broke `crates/worth-signal/src/tests/resource_runtime/diagnostics.rs`
  into a local module directory:
  - `crates/worth-signal/src/tests/resource_runtime/diagnostics/mod.rs`
  - `.../summary_and_runtime.rs`
  - `.../expansion_policies.rs`
  - `.../digests.rs`
  - `.../support.rs`
- Chosen lane boundaries:
  - diagnostics truth and runtime-summary read behavior
  - expansion budget / policy denial behavior
  - provenance-digest drift checks
  - local helper support for malformed completion and summary builders
- The mechanical split surfaced a few dropped `#[test]` annotations and support
  visibility seams; fixed those before accepting the refactor so we didn't
  silently lose coverage.
- Verification passed:
  - `cargo fmt -p worth-signal`
  - `cargo test -p worth-signal resource_diagnostics -- --nocapture`
  - `cargo test -p worth-signal`
- Full crate after this refactor still: `923 passed, 23 ignored`

## Refactor - resource_runtime support nexus split by role

- Broke `crates/worth-signal/src/tests/resource_runtime/support.rs`
  into a local module directory:
  - `crates/worth-signal/src/tests/resource_runtime/support/mod.rs`
  - `.../observation.rs`
  - `.../async_workloads.rs`
  - `.../branch_replay.rs`
  - `.../lifecycle_and_replay_declarations.rs`
  - `.../timeout_and_cancellation_declarations.rs`
  - `.../revalidation_and_retry_declarations.rs`
  - `.../retry_helpers.rs`
- Chosen role boundaries:
  - observation records/listener glue
  - async hostile workload fixtures
  - branch replay fixture world
  - lifecycle/replay declaration builders
  - timeout/cancellation/observation declaration builders
  - revalidation/retry declaration builders
  - retry scheduling helper
- The first mechanical cut left a few function-signature seams at module
  boundaries and exposed that `pub(super)` was too narrow once helpers moved
  into child modules. Fixed both by repairing the split points and widening
  helper visibility only to `crate::tests::resource_runtime`.
- Verification passed:
  - `cargo fmt -p worth-signal`
  - `cargo test -p worth-signal`
- Full crate after this refactor still: `923 passed, 23 ignored`
- Remaining `resource_runtime` top-level god file pressure is effectively gone;
  the last obvious heavy bucket has been converted into categorized modules.
