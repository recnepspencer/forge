- Worth UI Milestone 4.1 Phase 1 cleanup: mounted product rendering now consumes a sealed composition-tree receipt instead of flat mounted-product nodes. Removed public product-view raw mounted-view/node getters, canonicalized composition graph admission order, split mounted-view facade exports, and added graph/tree/trybuild proof coverage for no flat scan, no renderer parse, and sealed traversal receipts.
- Worth UI Milestone 4.1 Phase 2 QA: fixed authored composition child sizing so `hug`/positive `fill(n)` admit into composition edge receipts and invalid sizing rejects at source. Added source-authority denials for composition control/interaction children that reference missing live-view declarations. Removed the generated live-view composition fallback from mounting so mounted product views must consume an admitted composition graph instead of inventing topology.
- Worth UI Milestone 4.1 Phase 6 implementation: added a formal `runtime/host_observation` lane so adapters draft neutral host measurements and runtime admits sealed observation receipts. Host observations now carry mounted-view basis, readiness, counters, host-measurement facts, stale/unknown/duplicate denials, measured product-view receipts, facade exports, native-boundary guards for measurement APIs, validation-app observation collection, and compile-fail sealing for admitted observation/measured-view receipts. Verified focused checks and tests.
- QA-looped Worth UI Milestone 4.1 Phase 6. Found and fixed two proof-chain gaps: host observation receipt identity did not include every admitted observation family and metric admission did not reject nonsensical scalar values; measured product-view construction also trusted admitted observations without re-checking the mounted-view basis. Added canonical observation identity, metric-basis denials, stale cross-mounted measurement rejection, hostile tests for reordered rows/changed facts/invalid metrics/mismatched basis, and split host observation files back under the 400-line cap. Reverified focused proof, boundary, compile-fail, and check commands.
- Worth UI Phase 6 test-hardening pass: replaced the validation app's swallowed host observation `.ok()` path with a testable neutral collector seam and explicit frame measurement proof (`admitted`, `denied`, or `unavailable`) that preserves admitted receipts, measured product-view receipts, and typed denials. Added host observation rebind receipts backed by the semantic-slice inventory so changed host measurement facts name PageHost consumers with zero source/artifact/renderer parse counters. Strengthened host measurement adapter boundary guards with declared adapter capabilities and added collector/rebind integration tests.
- Code-quality QA for Phase 6 host measurement found two structural issues and fixed both: the validation app's host observation collector had mixed egui adapter, neutral input, admission, target selection, and proof responsibilities in one 302-line file, and runtime `host_observation` had grown into an 11-file flat directory. Split validation host observation into `egui/native adapter`, input, measurement proof, runtime admission, and surface target files under an earned subdirectory; moved runtime rebind into `host_observation/rebind/{counters,receipt}`. Reverified line counts, native boundary, host measurement boundary, collector integration, rebind proof, and validation-app check.
- Worth UI Milestone 4.1 Phase 7 QA: found and fixed allocation proof gaps. Layout allocation now carries container policy receipts with kind/gap/padding token identity, resolved edge padding, cross-axis posture, and baseline metrics; unsupported graph children without mounted receipts now deny through typed layout allocation denials instead of being skipped. Added tests for policy tokens, baseline data, missing mounted children, and layout-only gap changes preserving composition graph identity; added compile-fail coverage that app code cannot mint layout allocation receipts. Verified focused checks, authority compile-fail, and line counts.
- Worth UI Milestone 4.1 Phase 8 implementation: added a runtime `viewport_boundary` lane for composition-owned scroll/clip/viewport/hit/focus/accessibility participation. Composition now admits `viewport_boundary` policy attachments; host observations retain viewport and scroll viewport rows; mounted composition trees expose admitted policy attachments; viewport receipts resolve from mounted product view + host observations + layout allocation and deny unsupported policies or nested composition scroll ownership. Validation rendering consumes descendant visibility from receipts. Semantic inventory treats viewport boundary and host measurement as product-meaning runtime slices with no false PageHost projection consumer until a real mounted measurement/viewport projection family exists.
- Worth UI Milestone 4.1 Phase 8 QA hardening: fixed viewport boundary proof to cover all graph descendants, not only direct children; added explicit measurement participation to descendant receipts; narrowed viewport consumed facts while preserving root mount/mosaic legality facts; removed renderer-local live-view clip rect; added nested descendant and mosaic authority proof coverage. Verified focused viewport tests, crate checks, and sealed receipt compile-fail coverage.
- Worth UI Phase 8 test-hardening implementation: added sealed effective viewport participation receipts so renderer adapters consume graph-ancestor-effective node posture instead of parent-local viewport lookups. Removed synthetic native scroll viewport injection from the validation host collector, added missing-host-scroll-observation denial proof, replaced brittle viewport test string replacements with named scenario fixtures, tightened node identity assertions, and added compile-fail sealing for effective participation receipts. Remaining honest surfaces: actual egui scroll mechanics/restoration, schema-backed viewport policy fields, and forbidden mosaic/local scroll handoff receipts still need dedicated production lanes.
- Worth UI Milestone 4.1 Phase 10 implementation: composition accessibility/focus participation now lowers from authored composition associations and mounted composition graph identity into sealed participation receipts with Query-backed obligation execution. Invalid label/description/error associations reject during live-view projection admission as typed `CompositionParticipation` denials instead of surviving to mount-time panic. Added Query adoption proof and compile-fail sealing for participation receipts.
- Worth UI Phase 10 test-hardening implementation: participation now carries graph-access-backed traversal receipts, rich accessibility relationship receipts, focus-scope receipts, row-based host accessibility inspection, and batch participation denial reports. Tests now use explicit accessibility scenario fixtures and assert relationship text/roles, focus scopes, traversal counters, multi-denial ordering, host inspection rows, and compile-fail sealing for the new proof types.
- Code-quality QA for Phase 10 composition participation: split the flat `runtime/composition_participation` directory into `admission`, `projection`, and `host_inspection`, renamed vague files to predictive names (`node_participation`, `accessibility_relationships`, `focus_scopes`), and split host inspection receipt shape from row projection. Reverified line caps, crate checks, accessibility participation tests, native-boundary tests, Query adoption, and runtime authority compile-fail coverage.
- Worth UI Milestone 4.1 Phase 11 implementation: live-view controls and interactions now mount as ordinary composition graph children through sealed child-binding receipts. Mounted control nodes carry state binding + host frame + graph child binding, mounted interactions carry graph child binding, composition-tree identity no longer reconstructs `live_view.control.*`/`live_view.interaction.*` locally, and validation rendering consumes mounted child receipts instead of grouped control/interaction render-plan lookups. Added focused child-binding/rebind tests, boundary guards, compile-fail sealing, and split oversized tests back under the line cap.
- Phase 11 QA hardening: corrected the mount direction so composition graph child rows select renderable controls/interactions and render-plan rows only attach payload receipts. Removed the old panic-based child-binding lookup, added tests for declared-but-unmounted controls/interactions not rendering or crashing, dropdown movement, submit interaction movement, and text-to-dropdown mounted identity preservation.
- Phase 11 test-hardening follow-up: mounted graph child selection now exposes sealed reconciliation receipts/counters with graph-consumed facts, mounted view receipts include that graph basis in consumed facts, and child-mounting tests use semantic live-view product scenarios plus exact topology assertions instead of raw source string replacement. Added compile-fail proof that app code cannot mint the reconciliation/counter receipts.
- Worth UI Milestone 4.1 Phase 12 implementation: added a standard expression capability catalog and graph-backed live-view expression projection lane. Existing conditional participation, readiness, and payload projections now lower through expression declarations/output receipts instead of local predicate/payload logic, with new runtime fact families, Query obligation semantics, graph execution receipts, facade exports, and compile-fail sealing for expression projection receipts. Verified `cargo check -p worth-ui`, expression catalog tests, Query adoption tests, and runtime authority trybuild.

- Phase 12 QA-loop: found expression implementation was descriptor-thin and too decorative in downstream projections. Fixed standard expression capability metadata, removed unknown-operator fallback, added typed expression admission reports, added missing standard operators, made readiness/payload consume expression output facts as authority, registered expression semantic slices/projection family, and strengthened validation/trybuild/catalog proofs. Verified focused worth-ui and validation-app checks/tests.
