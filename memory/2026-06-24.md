# 2026-06-24

- Continued the W607/Hadwiger-Nelson validation chain with H29 after H28
  passed. Hegel critiqued the candidate and approved it only as a strict
  parent-lift provenance/readiness checker, not a theorem claim. Implemented
  `crates/hadwiger-research/src/frontier_seeds/g27_w_circles_parent_lift_readiness_replay.rs`
  and
  `crates/hadwiger-research/examples/replay_g27_w_circles_parent_lift_readiness.rs`.
  The checker composes H28 row-family semantics with both standalone
  projected-parent and branch-slack lift replays; enforces exact summaries
  `80143` checked rows, `6` terminal parent-lift row occurrences, projected
  lift `(304, 613372392, 546085806, 67286586)`, and branch-slack lift
  `(623894447014, 559085319025, 64809127989, 8555)`; verifies all terminal
  parent-lift rows are canonical `parent_lift_1` rows regenerated from the
  digest-bound branch-slack/projection sources; records source SHA256 digests;
  and hard-codes `theorem_authority false`.
- Verification passed:
  `cargo run --release -p hadwiger-research --example replay_g27_w_circles_parent_lift_readiness`
  with output
  `checked_rows 80143 parent_lift_rows 6 theorem_authority false ids ["parent_lift_1"] status ParentLiftProvenanceReadinessPreflight`;
  `cargo check -p hadwiger-research --example replay_g27_w_circles_parent_lift_readiness`;
  and `cargo fmt -p hadwiger-research --check`. Debug-mode replay was too slow
  and initially left a stale process locking the Windows example binary; release
  mode completed in about seven minutes after compilation.
- H30 converts the validation chain into a hard theorem-boundary audit. Cicero
  approved the hypothesis only if it admitted a scoped certificate and blocked
  the target theorem. Implemented
  `crates/hadwiger-research/src/frontier_seeds/g27_w_circles_certificate_admission_gap_replay.rs`
  and
  `crates/hadwiger-research/examples/replay_g27_w_circles_certificate_admission_gap.rs`.
  The checker composes H26/H27/H28/H29, recomputes exact worst terminal
  objective rounding from the row-carrying terminal artifact, admits only the
  scoped `DeclaredMixedTerminalPartition` ceiling `586225`, compares against the
  W607 target `512933`, and requires `target_pass false` and
  `theorem_authority false`. Release replay passed with:
  `status CertificateAdmissionBlockedTarget admitted_scope DeclaredMixedTerminalPartition admitted_bound_floor 586224 admitted_bound_ceil 586225 target_bound 512933 target_pass false theorem_authority false`.
  Blockers are explicit: target bound not reached, weighted-alpha certificate
  is not a plane chromatic lower-bound claim, and the scope is not final W607
  root theorem authority. This is publishable proof-engineering boundary work,
  not a solved HN/W607 target.
- H31 tests whether stronger same-field branch node bounds compound. Newton
  approved this only as a tightly capped preflight. Implemented
  `crates/hadwiger-research/src/frontier_seeds/g27_same_field_mwis_odd_cycle_branch_preflight.rs`
  and
  `crates/hadwiger-research/examples/preflight_g27_same_field_mwis_odd_cycle_branch.rs`.
  The checker freezes `G27 8 -> W 301`, atom mask `101719589`, exact-side
  weight `61655`, dominant threshold `451278`, and root odd-cycle total gate
  `543428`; then branches with clique+odd-cycle LP node bounds instead of the
  retired clique-cover bound. Release run passed with:
  `atom_mask 101719589 dominant_vertices 521 exact_side_weight 61655 dominant_threshold 451278 root_total_odd_cycle_upper 543428 best_open_total_odd_cycle_upper 518612 nodes 29 open_nodes 28 pruned 2 max_depth 6 elapsed_millis 307672 odd_cycle_cuts 16600 max_node_millis 8337 status PromisingOddCycleBranchBound theorem_authority false`.
  This clears the continuation gate by moving best-open `24816` below the root
  and showing 2 threshold prunes, but remains `5679` above target and has no
  theorem authority until odd-cycle rows become replayable/rational metadata.
- H32a validates the H31 odd-cycle row layer as a kill-switch preflight, not a
  theorem claim. Lagrange critiqued H32 and forced the split: first prove row
  metadata is deterministic and semantically replayable; only then consider
  exact/rational dual replay. Implemented
  `crates/hadwiger-research/src/frontier_seeds/g27_same_field_mwis_odd_cycle_row_replay.rs`,
  `crates/hadwiger-research/src/frontier_seeds/g27_same_field_mwis_odd_cycle_row_replay_support.rs`,
  and
  `crates/hadwiger-research/examples/replay_g27_same_field_mwis_odd_cycle_rows.rs`.
  The replay captures root plus the first two threshold-pruned H31 nodes,
  validates clique rows and ordered odd-cycle witnesses, and runs twice to
  require byte-stable row metadata. Release result:
  `root_total_odd_cycle_bound 543428 pruned_nodes 2 checked_nodes 3 clique_rows 2706 odd_cycle_rows 859 max_odd_cycle_length 21 metadata_bytes 99988 row_digest c366ebe9e7b5da4ebcceba2da2777a7b8b4fa1a0b067e99819a6ab30bb9cbde4 status RowReplayStablePreflight theorem_authority false`.
  This passes H32a's compactness/stability gates, but objective ceilings are
  still solver-derived; H32b should attempt exact/rational dual replay on these
  same three row systems before any larger same-field branch is called
  proof-carrying.
- H32b exact dual replay nearest-rational probe failed usefully. Jason approved
  H32b only as a one-sitting proof-language falsifier with all implicit LP rows
  made explicit. Implemented
  `crates/hadwiger-research/src/frontier_seeds/g27_same_field_mwis_odd_cycle_dual_replay.rs`,
  `crates/hadwiger-research/src/frontier_seeds/g27_same_field_mwis_odd_cycle_dual_replay_support.rs`,
  and
  `crates/hadwiger-research/examples/replay_g27_same_field_mwis_odd_cycle_duals.rs`.
  The checker solves the explicit dual LP over singleton upper-bound, edge,
  clique, and odd-cycle rows, then rationalizes positive multipliers with
  denominator cap `1000000` and exact-checks coverage/objective with BigInt
  rational arithmetic. Release result:
  `checked_nodes 3 certified_pruned_nodes 0 explicit_rows 4205 positive_dual_rows 518 root_total_bound 0 max_denominator 999760 min_slack_floor -1 max_objective_excess 0 status DualCoverageFailed theorem_authority false`.
  Interpretation: complete dual candidates are accessible and compact, but
  nearest rational reconstruction undershoots coverage by less than one unit.
  Same-field branch remains non-proof-carrying unless a bounded conservative
  rationalization/repair preserves the exact H32a objective ceilings.
- H32c one-sided rational dual replay passed as scoped node-certificate
  preflight. Boyle approved only a predeclared one-sided policy: round every
  positive floating dual multiplier upward to denominator cap `1000000`, no new
  rows, exact coverage only, and unchanged objective ceilings. Added
  `crates/hadwiger-research/examples/replay_g27_same_field_mwis_odd_cycle_one_sided_duals.rs`
  and a separate checked facade entry. Release result:
  `checked_nodes 3 certified_pruned_nodes 2 explicit_rows 11830 positive_dual_rows 1496 root_total_bound 543428 max_denominator 1000000 min_slack_floor 0 max_objective_excess 0 status OneSidedOddCycleNodeDualReplayPreflight theorem_authority false`.
  This gives exact/rational authority for H31 root plus the two threshold
  pruned nodes: root stays `543428`, both pruned nodes certify below threshold,
  and exact coverage slack is nonnegative. Still not a full branch proof or HN
  theorem claim; next step should package the H31 explored tree/open frontier
  with these one-sided rational node duals.
- H33 branch-prefix packaging passed as proof-plumbing preflight. Fermat
  approved this only as semantic packaging around the H32c scoped certificates,
  not as new bound progress. Implemented
  `crates/hadwiger-research/src/frontier_seeds/g27_same_field_mwis_branch_prefix_replay.rs`
  and
  `crates/hadwiger-research/examples/replay_g27_same_field_mwis_branch_prefix.rs`.
  The checker composes the H32c one-sided rational dual gate once, then
  rebuilds the H31 branch prefix twice with semantic node identity
  (`branch_included`, `forced_included`, `excluded`, candidates, chosen weight,
  depth), branch-child checks, and deterministic prefix digest comparison.
  Initial release run used an over-expensive composition and was stopped after
  the wrapper timed out while the child process kept running; patched it to
  avoid recomputing H32c during determinism. Final release result:
  `expanded_nodes 29 pruned_nodes 2 open_frontier_nodes 28 best_open_total_bound 518612 root_total_bound 543428 h32c_certified_prunes 2 status BranchPrefixSemanticsPreflight theorem_authority false`.
  Verification passed:
  `cargo fmt -p hadwiger-research --check`,
  `cargo check -p hadwiger-research --example replay_g27_same_field_mwis_branch_prefix`,
  and the release example. Interpretation: the H31/H32c asterisk is now
  validated as deterministic branch-prefix proof plumbing with two exact
  rational prunes, but the open frontier remains `518612`, still `5679` above
  target, so this is not a full HN/W607 proof.
- H34a top-frontier shape diagnostic passed and redirected the next branch
  continuation. Plato critiqued the single worst-node continuation hypothesis:
  if the second-best open frontier node is close, improving only the top node
  cannot prove meaningful global progress. Implemented
  `crates/hadwiger-research/src/frontier_seeds/g27_same_field_mwis_frontier_shape.rs`
  and
  `crates/hadwiger-research/examples/diagnose_g27_same_field_mwis_frontier_shape.rs`.
  Release result:
  `open_frontier_nodes 28 tied_band_nodes 10 gap_to_second 69 best_open_total_bound 518612 top_open_totals [518612, 518543, 518471, 518441, 518343, 518123, 518105, 517777, 517741, 517649] top_open_depths [5, 4, 3, 3, 5, 6, 6, 6, 6, 6] status TiedFrontierBandRequiresTopKContinuation theorem_authority false`.
  Verification passed:
  `cargo fmt -p hadwiger-research --check`,
  `cargo check -p hadwiger-research --example diagnose_g27_same_field_mwis_frontier_shape`,
  and the release example. Interpretation: retire single-peek H34 before
  executing it; the frontier is a tied top band (`gap_to_second 69`, ten nodes
  within `1000`), so the next useful hypothesis is proof-carrying continuation
  over the top-k band.
- H34b best-first top-band collapse preflight retired the current same-field
  continuation lane. Lovelace approved the test only as a global best-first
  continuation over the H34a tied top band, with `top_k 10`, `band_width 1000`,
  `20` total expansions, `3` per origin, and `300s` wall cap. Implemented
  `crates/hadwiger-research/src/frontier_seeds/g27_same_field_mwis_top_band_collapse.rs`
  and
  `crates/hadwiger-research/examples/preflight_g27_same_field_mwis_top_band_collapse.rs`.
  Release result:
  `initial_best_total 518612 final_best_total 518543 final_tied_band_nodes 9 selected_origin_count 10 expanded_nodes 1 solver_pruned_descendants 0 open_frontier_nodes 29 elapsed_millis 302401 status BoundedContinuationNoUsefulProgress theorem_authority false`.
  Verification passed:
  `cargo fmt -p hadwiger-research --check`,
  `cargo check -p hadwiger-research --example preflight_g27_same_field_mwis_top_band_collapse`,
  and the release example. Interpretation: the tied band did not collapse;
  the global best improved only `69`, no new solver prunes appeared, and the
  continuation spent its five-minute cap on one expansion. Do not keep funding
  the same clique+odd-cycle top-band continuation without a stronger node-bound
  family or branch heuristic change.
- H35 LP-guided branch diagnostic passed as a useful branch-heuristic falsifier.
  Ramanujan approved only a small comparator: top H34a frontier node first,
  branch baseline by `degree * weight`, branch alternative by node-local
  clique+odd-cycle LP guidance `weight * x * (1-x)`, and compare worst-child
  odd-cycle totals. Implemented
  `crates/hadwiger-research/src/frontier_seeds/g27_same_field_mwis_lp_guided_branch.rs`,
  support module
  `crates/hadwiger-research/src/frontier_seeds/g27_same_field_mwis_lp_guided_branch_support.rs`,
  and
  `crates/hadwiger-research/examples/diagnose_g27_same_field_mwis_lp_guided_branch.rs`.
  Release result:
  `checked_nodes 1 useful_nodes 1 worse_nodes 0 top_relative_gain 2131 top_absolute_drop 5433 max_regression 0 elapsed_millis 1419354 status LpGuidanceUseful theorem_authority false`;
  row:
  `row 0 parent_total 518612 baseline_branch 87 lp_branch 383 lp_value_ppm 612331 lp_score 4247232738 baseline_worst_child_total 515310 lp_worst_child_total 513179 relative_gain 2131 absolute_drop 5433`.
  Verification passed:
  `cargo fmt -p hadwiger-research --check`,
  `cargo check -p hadwiger-research --example diagnose_g27_same_field_mwis_lp_guided_branch`,
  and the release example. Interpretation: branch heuristic, not just node
  bound, was materially hurting the top frontier node. LP-guided split leaves
  the worst child only `246` above target (`513179` vs `512933`), so H36 should
  test a tiny proof-carrying LP-guided continuation from this node for a new
  exact-rational prune or local below-target split. Still no theorem authority.
- H36 LP-guided micro-continuation passed and produced two new solver-prune
  candidates. Anscombe approved only a kill-switch micro-diagnostic: reconstruct
  the H34a top node, apply fixed H35 LP-guided branch `383`, then split the
  worse child once more with LP guidance. Implemented
  `crates/hadwiger-research/src/frontier_seeds/g27_same_field_mwis_lp_guided_micro.rs`
  and
  `crates/hadwiger-research/examples/preflight_g27_same_field_mwis_lp_guided_micro.rs`.
  Initial run incorrectly counted H33 prefix composition against the 600s
  micro cap and returned `RuntimeInconclusive`; patched the timer to start
  after the prefix gate. Final release result:
  `parent_total 518612 first_worst_total 513179 second_branch 223 final_worst_total 507877 additional_drop 5302 solver_prune_candidates 2 elapsed_millis 314823 status SolverPruneCandidateNeedsExactReplay theorem_authority false`.
  Verification passed:
  `cargo fmt -p hadwiger-research --check`,
  `cargo check -p hadwiger-research --example preflight_g27_same_field_mwis_lp_guided_micro`,
  and the release example. Interpretation: LP-guided branching can collapse
  the H34a top node locally below target. Both second-split children are
  solver prune candidates under dominant threshold `451278`, but theorem
  authority is still false until exact one-sided rational dual replay certifies
  these specific descendants and the rest of the frontier remains unresolved.
- H37 LP-guided micro-dual replay passed as scoped proof plumbing. Confucius
  approved certifying exactly the two H36 second-split children, not broadening
  to opportunistic nodes. Implemented
  `crates/hadwiger-research/src/frontier_seeds/g27_same_field_mwis_lp_guided_micro_dual.rs`,
  support module
  `crates/hadwiger-research/src/frontier_seeds/g27_same_field_mwis_lp_guided_micro_dual_support.rs`,
  and
  `crates/hadwiger-research/examples/replay_g27_same_field_mwis_lp_guided_micro_duals.rs`.
  Release result:
  `checked_nodes 2 certified_prunes 2 explicit_rows 6915 positive_dual_rows 929 final_worst_total 507877 max_denominator 1000000 min_slack_floor 0 max_objective_excess 0 row_digest 365df93fd126a50e6e0b38eed2e42a1efc8463175b838a9b5383d41e01af6d63 status ExactMicroPrunesCertified theorem_authority false`.
  Verification passed:
  `cargo fmt -p hadwiger-research --check`,
  `cargo check -p hadwiger-research --example replay_g27_same_field_mwis_lp_guided_micro_duals`,
  and the release example. Interpretation: the H36 LP-guided local collapse is
  now exact/rational node-certificate authority for the H34a top node subtree.
  This is the strongest scoped proof-carrying LP-guided result so far, but it
  still does not prove the global frontier because the tied open nodes remain.
  Next hypothesis should attempt the same LP-guided exact-promotion pattern on
  the rest of the H34a top band, beginning with the second node at `518543`.
- H38 LP-guided second-frontier preflight passed and funded a top-band
  framework. Ohm approved testing H34a frontier index `1` before generalizing
  H37, to avoid overfitting a one-node success. Implemented
  `crates/hadwiger-research/src/frontier_seeds/g27_same_field_mwis_lp_guided_second.rs`
  and
  `crates/hadwiger-research/examples/preflight_g27_same_field_mwis_lp_guided_second.rs`.
  The checker reconstructs the H34a frontier, selects deterministic index `1`,
  requires parent total `518543` and depth `4`, records semantic parent digest
  `b7170cc1f84843d87ff603f41f73b49da277be5dd4efacabb73ace7292e5c6a7`,
  then does two LP-guided splits and exact-replays the two second-split
  children if both are prune candidates. Release result:
  `parent_total 518543 first_branch 223 first_worst_total 514729 second_branch 384 final_worst_total 509857 solver_prune_candidates 2 certified_prunes 2 explicit_rows 7804 positive_dual_rows 986 max_denominator 1000000 min_slack_floor 0 max_objective_excess 0 parent_digest b7170cc1f84843d87ff603f41f73b49da277be5dd4efacabb73ace7292e5c6a7 row_digest 32208444486573d6698ed148dc2b0ffd67b3bb57f897ae9bb80bbf7e262c4fa5 status FundTopKFramework theorem_authority false`.
  Verification passed:
  `cargo fmt -p hadwiger-research --check`,
  `cargo check -p hadwiger-research --example preflight_g27_same_field_mwis_lp_guided_second`,
  and the release example. Interpretation: LP-guided exact-prune promotion now
  works for the first two H34a frontier nodes; the second node collapses below
  target to exact-certified local worst total `509857`. Next funded step should
  build the top-band framework over the remaining tied nodes. Still no theorem
  authority because the full frontier is not closed.
- H39 top-prefix exact preflight passed with the stricter three-leaf local
  partition demanded by Dirac. The original two-child plan was semantically
  weak because it would not exact-certify the untouched first-split sibling.
  Implemented
  `crates/hadwiger-research/src/frontier_seeds/g27_same_field_mwis_lp_guided_top_prefix.rs`
  and
  `crates/hadwiger-research/examples/preflight_g27_same_field_mwis_lp_guided_top_prefix.rs`;
  generalized `certify_children` to accept a bounded leaf slice and moved
  generic child-entry / node-digest helpers into
  `g27_same_field_mwis_lp_guided_branch_support.rs`. Release result:
  `checked_nodes 2 certified_nodes 2 certified_leaves 6 remaining_best_open_total 518343 status TopBandPrefixExactProgress theorem_authority false`.
  Node 2 exact-certified three leaves with terminal totals
  `[509911, 507987, 509987]`, parent digest
  `ff8554b582b3df577de0e4f1a8bbf31f6689f84e7e5a2527af74a49f3f4a884e`, row digest
  `b968f1dff685ac9ea0317dd4d2ca892986d9fafd49389528cb7d5ee7cf3f9eec`.
  Node 3 exact-certified three leaves with terminal totals
  `[509692, 506730, 509602]`, parent digest
  `5decf014facce0c904c9874452e20e7152ef4b37452d4504b3284d781f30c90b`, row digest
  `01118ff70f37a3323deed1c1558991e8e5da287b3399348e41d72bb3298f7ac1`.
  Verification passed: `cargo fmt -p hadwiger-research --check`,
  `cargo check -p hadwiger-research --example preflight_g27_same_field_mwis_lp_guided_top_prefix`,
  and the release example. Interpretation: H34a frontier prefix indices `0..3`
  now have scoped exact local subtree certificates below target; the best
  carried unresolved frontier node is index `4` at `518343`. This is stronger
  proof-carrying progress, but theorem authority remains false because the
  remaining frontier is not closed.
- H40 next-prefix exact preflight passed on H34a frontier indices `4` and `5`.
  Aquinas approved exactly this two-node scope, specifically requiring the
  depth-6 node at index `5` and rejecting opportunistic until-failure sweeps.
  Generalized the H39 top-prefix runner into profiles and added
  `preflight_g27_same_field_mwis_lp_guided_next_prefix_checked` plus
  `crates/hadwiger-research/examples/preflight_g27_same_field_mwis_lp_guided_next_prefix.rs`.
  The checker now computes and gates the carried unresolved best from the
  frontier instead of printing a constant. Release result:
  `checked_nodes 2 certified_nodes 2 certified_leaves 6 remaining_best_open_total 518105 status TopBandPrefixExactProgress theorem_authority false`.
  Node 4 exact-certified terminal totals `[510402, 510922, 512089]`, parent
  digest `b7d0b9f15b994797e087aa76e38739ae6ec04806369051896dd967b823c4d88b`,
  row digest `6099aa6337b45f0987d50d353ae7852e6424cf10973df07738a0a7b51a8a862b`.
  Node 5 exact-certified terminal totals `[511951, 501987, 508398]`, parent
  digest `060ec36608939a8458fe2e5cb11cc562457c623337ba4e300a6a2bbdb652cb2b`,
  row digest `434ebb2a78631df9757c4c6851673f908a406406ba7a996ecc4ce573a6843016`.
  Verification passed: `cargo fmt -p hadwiger-research --check`, cargo checks
  for both top-prefix examples, and the H40 release example. Interpretation:
  H34a frontier prefix indices `0..5` now have scoped exact local subtree
  certificates below target, including a depth-6 parent. The best unresolved
  frontier node is index `6` at `518105`, still above target; theorem authority
  remains false.
- H41 third-prefix exact preflight passed on H34a frontier indices `6` and `7`.
  Parfit approved exactly this two-node scope and rejected both index-6-only
  and remaining-suffix sweeps. Added
  `preflight_g27_same_field_mwis_lp_guided_third_prefix_checked`,
  `crates/hadwiger-research/examples/preflight_g27_same_field_mwis_lp_guided_third_prefix.rs`,
  and a contiguous-index guard in the shared top-prefix profile runner. First
  release attempt failed during rustc/LLVM compilation with OOM before the
  example ran; rerun succeeded with `RUSTC_WRAPPER=''` and `CARGO_BUILD_JOBS=1`.
  Release result:
  `checked_nodes 2 certified_nodes 2 certified_leaves 6 remaining_best_open_total 517741 status TopBandPrefixExactProgress theorem_authority false`.
  Node 6 exact-certified terminal totals `[511910, 502972, 507197]`, parent
  digest `726df3ae15fac725dfeeef889da562d92a4d0d0ad2a74c8b097dbad9ac880860`,
  row digest `5561a45b0cf0a5b6b530b4fbf5b2dec4f36bea49ff5b4c20c68d86b655e96161`.
  Node 7 exact-certified terminal totals `[510729, 502480, 507015]`, parent
  digest `5af208a1a0bfc1bc49b4480df8f257ca0cbc4bab09a90c69b2f650ca58b21132`,
  row digest `7383390433cce48b47579d551469af2b8b247486449aa0a1e8617c7c42c6e629`.
  Verification passed: `cargo fmt -p hadwiger-research --check`,
  `cargo check -p hadwiger-research --example preflight_g27_same_field_mwis_lp_guided_third_prefix`,
  and the release example. Interpretation: H34a frontier prefix indices `0..7`
  now have scoped exact local subtree certificates below target. The best
  unresolved frontier node is index `8` at `517741`, still above target; theorem
  authority remains false.
- H42 final top-pair exact preflight passed on H34a frontier indices `8` and
  `9`. Godel approved exactly this final pair and warned not to call the H42
  checker itself a standalone top-10 proof unless it re-composes predecessors;
  recorded as final-pair exact progress which combines with H37-H41 to close
  H34a indices `0..9`. Added
  `preflight_g27_same_field_mwis_lp_guided_final_top_pair_checked` and
  `crates/hadwiger-research/examples/preflight_g27_same_field_mwis_lp_guided_final_top_pair.rs`.
  H42 computes the actual carried `frontier[10]` total rather than using a
  guessed constant. Release result:
  `checked_nodes 2 certified_nodes 2 certified_leaves 6 remaining_best_open_total 517394 status TopBandPrefixExactProgress theorem_authority false`.
  Node 8 exact-certified terminal totals `[510908, 501835, 508118]`, parent
  digest `afaf0d5a5cf9f8a3f0b4658dc609bd290f199b33cc7ea03d822e62f487690eae`,
  row digest `08315a5797cea1f9ae06a71012090e1fd57caa42ead039c388615b9cc4eb1ae1`.
  Node 9 exact-certified terminal totals `[507682, 507419, 507125]`, parent
  digest `6f3693db9a82411d2f596d0fb0ecda47f5d332280516f093fe6e2d1ff2face75`,
  row digest `17cd07be167a71b4ebfadb6db9baf948900cff134718e9a97775cfaf135e4a52`.
  Verification passed: `cargo fmt -p hadwiger-research --check`, cargo checks
  for all four top-prefix examples, and the H42 release example. Interpretation:
  combined H37-H42 exact-certify the original H34a top-10 frontier band below
  target. The actual next unresolved frontier node is index `10` at `517394`,
  still above `512933`; theorem authority remains false. Next step should
  inspect the remaining 18-node frontier shape before choosing the next
  hypothesis.
- Added full remaining-frontier shape diagnostic after H42:
  `diagnose_g27_same_field_mwis_full_frontier_shape_checked` and
  `crates/hadwiger-research/examples/diagnose_g27_same_field_mwis_full_frontier_shape.rs`.
  Release result:
  `open_frontier_nodes 28 tied_band_nodes 10 gap_to_second 69 best_open_total_bound 518612 frontier_totals [518612, 518543, 518471, 518441, 518343, 518123, 518105, 517777, 517741, 517649, 517394, 517365, 517311, 517204, 517107, 516592, 516221, 516140, 516061, 515994, 515851, 515694, 515464, 514844, 514775, 514411, 513989, 513289] frontier_depths [5, 4, 3, 3, 5, 6, 6, 6, 6, 6, 6, 6, 3, 4, 6, 6, 6, 5, 5, 7, 5, 5, 6, 5, 5, 6, 7, 5] status TiedFrontierBandRequiresTopKContinuation theorem_authority false`.
  Interpretation: top-10 closure is meaningful, but 18 H34a frontier nodes
  remain above target, from index `10` at `517394` down to index `27` at
  `513289`. H43 should be based on this full shape, not momentum from the old
  top-10 story.
- H44 campaign scout passed and corrected the workflow from hand pair examples
  to a digest-bound closure-campaign artifact. Euclid approved the scope:
  freeze all 28 frontier nodes for provenance, mark `0..9` as already closed
  by H37-H42, scout unresolved suffix `10..27`, and perform no exact rational
  replay. Implemented
  `scout_g27_same_field_mwis_frontier_closure_campaign_checked`,
  `g27_same_field_mwis_frontier_closure_campaign.rs`, support module
  `g27_same_field_mwis_frontier_closure_campaign_support.rs`, and example
  `scout_g27_same_field_mwis_frontier_closure_campaign.rs`. The report is a
  canonical Hadwiger artifact of kind
  `g27_mwis_frontier_closure_campaign_scout_report`. Clean release executable
  result:
  `artifact_digest 6bd5dc2d60e45cb2f05bf0dd4beb7fcabf2ffae6684ddf8f383a2563601a6865 frontier_nodes 28 scout_rows 18 ready_count 18 failing_count 0 worst_terminal_total 512146 continuation_max_total 0 status campaign_scout_ready theorem_authority false`.
  Every suffix row `10..27` classified as
  `ready_for_exact_three_leaf_replay`; worst terminal total is `512146`, below
  target `512933`; no suffix continuation is needed at scout level. Verification
  passed: `cargo fmt -p hadwiger-research --check`,
  `cargo check -p hadwiger-research --example scout_g27_same_field_mwis_frontier_closure_campaign`,
  and direct release executable. Interpretation: H44 is a real publishability
  upgrade in workflow form: a reusable campaign manifest for suffix exact replay,
  not theorem authority yet. Next likely step is H45: consume the campaign scout
  and exact-replay suffix leaves in a bounded, manifest-driven campaign.
- H45 exact campaign chunk passed for suffix indices `10..13`. Archimedes
  approved option C: a resumable/chunked exact replay artifact keyed by the H44
  scout digest, not a one-off pair example or all-suffix gamble. Implemented
  `replay_g27_same_field_mwis_frontier_closure_exact_chunk_checked`, new support
  / gate / payload modules, artifact kind
  `g27_mwis_frontier_closure_exact_replay_report`, and example
  `crates/hadwiger-research/examples/replay_g27_same_field_mwis_frontier_closure_exact_chunk.rs`.
  H45 recomputes H44, requires source scout digest
  `6bd5dc2d60e45cb2f05bf0dd4beb7fcabf2ffae6684ddf8f383a2563601a6865`,
  checks full frontier provenance against scout nodes, recomputes each selected
  parent's LP-guided first branch, worse child, second branch, and three
  terminal leaves, then exact-replays each leaf with explicit row and positive
  dual digests. Release run passed:
  `artifact_digest ad9d452665ee980c7c1bb7292dee281487a4307e44db5a249163545ded061166 selected_range 10..14 checked_nodes 4 certified_nodes 4 certified_leaves 12 explicit_rows 41308 positive_dual_rows 5464 worst_terminal_total 510968 max_denominator 1000000 min_slack_floor 0 max_objective_excess 0 unresolved_suffix 14..28 status exact_chunk_certified theorem_authority false`.
  Node row digests: index `10`
  `468dc88348e75167a7f045fb705ec3d19684eb0ff9532123a22097a8486235bf`,
  index `11`
  `22d5ac4c49adfd473612b99788a9d38a8bbe21ae7bdea0483a2adda2cad989d2`,
  index `12`
  `356a0bbb0175c38fd81b40f4a97569a9a3039cf39c413d7c1022d0a6352f69ca`,
  and index `13`
  `854d39fa65d0f5d1790c1dfb165daf3f57dd0993c18c285150d903645177f03d`.
  Interpretation: combined H37-H42 plus H45 now exact-cover H34a frontier
  indices `0..13`, leaving suffix `14..27` unresolved. Next hypothesis should
  run the next trace-enabled exact chunk, probably `14..18` or `14..21`, and
  later add a composition checker; theorem authority remains false until full
  suffix coverage is composed.
