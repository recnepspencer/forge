Reviewed Worth roadmap and `worth-kernel` milestone 5 with the spec-designer
workflow. Updated `_docs/worth/worth_roadmap.md` lightly so Milestone 6 no
longer reads as merely old narrow planar predicates: it now inherits Milestone
5 binding/rebinding plus Query-native geometry work and frames M6 as exact
planar contracts, structural identity, retained/projection-consumed planar
facts, clean-fail behavior, and boolean-readiness certification. User asked not
to write the full M6 spec yet; next useful step is a thin phase sketch where
each phase owns one conceptual boundary.

Closed the Worth Geometry Query-Native hard-break proof debt on current
evidence. The stale red digest row was caused by rebinding prior identity using
a canonical key that was caught by the generic `.ends_with(".identity")` direct
identity collection; changed it to `rebinding.prior.binding_identity` and
updated the kernel proof. Also removed the stored rebinding projection receipt
sidecar from `PrimitiveRebindingDeclarationEntry` and normalized remaining
geometry-facing local `DefaultHasher` artifact helpers to shared
`truth_digest_parts(ArtifactIdentity)`. Verification was green for
`cargo check -p worth-spatial -p worth-kernel -p worth-topo`, full
`binding::tests` (60/60), `geometry_hard_break_closeout` (2/2), kernel/spatial/topo
public API contracts, and kernel/spatial/topo UI contracts.
