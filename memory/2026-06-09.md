# 2026-06-09

- First real session in the forge workspace. USER.md is still blank — fill in as I learn.
- Workspace: "Forge" — a layered Rust runtime stack (forge-relational / forge-signal / forge-runtime-bridge / forge-query) plus the Worth geometry/CAD stack (worth-geom, worth-spatial, worth-topo, worth-kernel) built on top of it.
- Reviewed forge-query's capabilities (via `crates/forge-query/docs/AI_README.md`, 1399 lines) and how worth-kernel consumes it in practice.
- Key observations: forge-query is huge (~2000 src files, 112 doc files). Core rule: "declare intent once, lower once, execute through canonical artifacts." Support matrix separates visibility from admission (fail-closed). worth-kernel honors it — admits Write/Inspect families before authoring, surfaces gap rows, never bypasses (enforced by a self-grepping `no_local_runtime_workaround_audit`).
- My critique to user: ceremony cost per downstream report is very high (repeated report+digest+error boilerplate); digests built from `format!("{:?}")` joined with "|" are fragile; worth-kernel mostly exercises the entry/admission/basis/receipt slice, not live views/subscriptions/effects/temporal.
- Big design discussion: forge-query is single-owner (`&mut self` everywhere, no Rc/RefCell stack-wide). MVCC concurrency never reaches the facade. Agreed fix per arch law 1: shared Send+Sync basis-bound read contexts + single-writer submission seam (journal = total intake order) + readers-never-evaluate published-artifact rule. Determinism survives: it lives in total commit order + ordered maintenance, not single-threadedness.
- Authored three new milestone specs per user's numbering (after 9.5): `_docs/forge-query/milestone-9.6.md` (canonical evidence-identity primitive, typed stop classes — kill `message.contains()` matching, canonical preview/branch label identity), `milestone-9.7.md` (concurrent read authority + deterministic submission, 6 phases, hard-gates milestone 10), `milestone-9.8.md` (consumer kit: report scaffolding, shipped bypass audit, support snapshot pinning; closes on worth-kernel adoption/deletion). Updated `forge_query_roadmap.md`: critical path now 9.5 → 9.6 → 9.7 → 9.8 → 10, plus proof gates and store-gated track bullets.
- Followed spec-designer skill (read MENTALITY, arch/composition/domain/perf laws + roadmap first). User's focus right now: turning forge-query into a true product/platform layer.
- Added illustrative before/after target-shape code blocks to all nine relevant phases across 9.6/9.7/9.8 (marked "not frozen API").
- Re-sliced all three specs to one-conceptual-detail phases per user request (agent-focus concern): 9.6 now 7 phases (stop-class taxonomy/admission-payload split; label artifact/intake split), 9.7 now 10 (adapter split/assembly enforcement; pinning/read-context; intake-journal/consumer-replay; publication/reader-consumption), 9.8 now 9 (registry/audit; snapshot/pins; adoption split into reports vs enforcement). All cross-references renumbered.
- Doc-fault audit verdict: hadwiger's invariant miss = consumer staleness (forge-2 doc names all four builder APIs); workflow-editor's legality graph = genuine doc ambiguity (fixed: added authoring-legality framing to registering-domain-invariants doc + AI_README graph-authoring section + anti-pattern bullet); testing guidance = absent from AI_README but user is building 9.8 today and will edit docs after. NOT yet applied: Hard Prohibitions "no hard-coded support posture" rule, and gap-closure tripwire in 9.8 Phase 3 pinning.
- Audited two external consumers (../workflow-editor, ../forge-2/crates/hadwiger-research) for gaps. KEY: hadwiger's invariant-registration "BlockedDraft" is stale — forge-2's forge-query builder already ships all four registration APIs it claims are missing (consumer drift, not a gap). forge-query has a private `memory_workspace` module — seed for a shipped test backend. Real new gaps folded into specs: foundational-digest alignment (9.6 Phase 1), consumer-facing journal replay (9.7 Phase 3), shipped in-memory test backend (9.8 new Phase 4, phases renumbered to 6). Bigger unaddressed cluster for a future milestone: session noun / runtime-owned read models (workflow-editor's 12-registry shadow projection), app-level lineage on runtime branches, compute/checker routing, bulk declaration lanes.
