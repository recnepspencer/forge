# 2026-06-09

Reviewed Worth roadmap and `worth-kernel` milestone 5 with the spec-designer
workflow. Updated `_docs/worth/worth_roadmap.md` lightly so Milestone 6 no
longer reads as merely old narrow planar predicates: it now inherits Milestone
5 binding/rebinding plus Query-native geometry work and frames M6 as exact
planar contracts, structural identity, retained/projection-consumed planar
facts, clean-fail behavior, and boolean-readiness certification. User asked not
to write the full M6 spec yet; next useful step is a thin phase sketch where
each phase owns one conceptual boundary.

Closed the Worth Geometry Query-Native hard-break proof debt on current
evidence. The stale red digest row was caused by rebinding prior identity using
a canonical key that was caught by the generic `.ends_with(".identity")` direct
identity collection; changed it to `rebinding.prior.binding_identity` and
updated the kernel proof. Also removed the stored rebinding projection receipt
sidecar from `PrimitiveRebindingDeclarationEntry` and normalized remaining
geometry-facing local `DefaultHasher` artifact helpers to shared
`truth_digest_parts(ArtifactIdentity)`. Verification was green for
`cargo check -p worth-spatial -p worth-kernel -p worth-topo`, full
`binding::tests` (60/60), `geometry_hard_break_closeout` (2/2), kernel/spatial/topo
public API contracts, and kernel/spatial/topo UI contracts.

- First real session in the WORTH workspace. USER.md is still blank - fill in as I learn.
- Workspace: "WORTH" - a layered Rust runtime stack (worth-relational / worth-signal / worth-runtime-bridge / worth-query) plus the Worth geometry/CAD stack (worth-geom, worth-spatial, worth-topo, worth-kernel) built on top of it.
- Reviewed worth-query's capabilities (via `crates/worth-query/docs/AI_README.md`, 1399 lines) and how worth-kernel consumes it in practice.
- Key observations: worth-query is huge (~2000 src files, 112 doc files). Core rule: "declare intent once, lower once, execute through canonical artifacts." Support matrix separates visibility from admission (fail-closed). worth-kernel honors it - admits Write/Inspect families before authoring, surfaces gap rows, never bypasses (enforced by a self-grepping `no_local_runtime_workaround_audit`).
- My critique to user: ceremony cost per downstream report is very high (repeated report+digest+error boilerplate); digests built from `format!("{:?}")` joined with "|" are fragile; worth-kernel mostly exercises the entry/admission/basis/receipt slice, not live views/subscriptions/effects/temporal.
- Big design discussion: worth-query is single-owner (`&mut self` everywhere, no Rc/RefCell stack-wide). MVCC concurrency never reaches the facade. Agreed fix per arch law 1: shared Send+Sync basis-bound read contexts + single-writer submission seam (journal = total intake order) + readers-never-evaluate published-artifact rule. Determinism survives: it lives in total commit order + ordered maintenance, not single-threadedness.
- Authored three new milestone specs per user's numbering (after 9.5): `_docs/worth-query/milestone-9.6.md` (canonical evidence-identity primitive, typed stop classes - kill `message.contains()` matching, canonical preview/branch label identity), `milestone-9.7.md` (concurrent read authority + deterministic submission, 6 phases, hard-gates milestone 10), `milestone-9.8.md` (consumer kit: report scaffolding, shipped bypass audit, support snapshot pinning; closes on worth-kernel adoption/deletion). Updated `worth_query_roadmap.md`: critical path now 9.5 -> 9.6 -> 9.7 -> 9.8 -> 10, plus proof gates and store-gated track bullets.
- Followed spec-designer skill (read MENTALITY, arch/composition/domain/perf laws + roadmap first). User's focus right now: turning worth-query into a true product/platform layer.
- Added illustrative before/after target-shape code blocks to all nine relevant phases across 9.6/9.7/9.8 (marked "not frozen API").
- Re-sliced all three specs to one-conceptual-detail phases per user request (agent-focus concern): 9.6 now 7 phases (stop-class taxonomy/admission-payload split; label artifact/intake split), 9.7 now 10 (adapter split/assembly enforcement; pinning/read-context; intake-journal/consumer-replay; publication/reader-consumption), 9.8 now 9 (registry/audit; snapshot/pins; adoption split into reports vs enforcement). All cross-references renumbered.
- Doc-fault audit verdict: hadwiger's invariant miss = consumer staleness (WORTH-2 doc names all four builder APIs); workflow-editor's legality graph = genuine doc ambiguity (fixed: added authoring-legality framing to registering-domain-invariants doc + AI_README graph-authoring section + anti-pattern bullet); testing guidance = absent from AI_README but user is building 9.8 today and will edit docs after. NOT yet applied: Hard Prohibitions "no hard-coded support posture" rule, and gap-closure tripwire in 9.8 Phase 3 pinning.
- Audited two external consumers (../workflow-editor, ../WORTH-2/crates/hadwiger-research) for gaps. KEY: hadwiger's invariant-registration "BlockedDraft" is stale - WORTH-2's worth-query builder already ships all four registration APIs it claims are missing (consumer drift, not a gap). worth-query has a private `memory_workspace` module - seed for a shipped test backend. Real new gaps folded into specs: foundational-digest alignment (9.6 Phase 1), consumer-facing journal replay (9.7 Phase 3), shipped in-memory test backend (9.8 new Phase 4, phases renumbered to 6). Bigger unaddressed cluster for a future milestone: session noun / runtime-owned read models (workflow-editor's 12-registry shadow projection), app-level lineage on runtime branches, compute/checker routing, bulk declaration lanes.

Wrote `_docs/worth-kernel/milestone-6.md` using `spec-designer`. The spec
frames M6 as exact planar contracts, structural identity, movement/rotation
posture, retained/projection-consumed planar facts, recovery, diagnostics,
local rebuild/rebinding parity, clean-fail boundaries, and boolean-readiness
bundles. It explicitly pre-plans WORTH Query use in every phase and requires
all `_docs/worth/m6-premetaboss.md` `MB-M6-*` suites for closeout.

- Corrected M6 docs after user pointed out Shewchuk predicates already exist in worth-math. Updated roadmap, milestone-6 spec, and m6-premetaboss to name worth-math as the certified predicate engine and M6 as the Query-native spatial authority/certification/retention layer around it.

- Expanded M6 plan with explicit planar contract support surfaces: PlanarLocalFrameCertificate, ProjectPointToCertifiedPlane2D, CertifiedSegmentSegment2D, CertifiedPolygonWinding2D, CertifiedSignedArea2D, CoplanarOverlapContractExtractor, PlanarContractBundleValidator, and PredicateCertificateConsumptionValidator. Added them as first-class M6 phases and aligned roadmap + pre-MetaBoss docs.

- Implemented M6 Phase 1 planar admission vocabulary in worth-spatial: planar contract families, runtime concerns, admission classes, Query posture, digest-backed admission matrix, facade exports, public API contract tests, and worth-kernel consumer proof. Also patched worth-topo recovery stop mapping for new WORTH Query drift/stale variants so kernel tests compile.

- QA-looped M6 Phase 1. Found and fixed proof gaps: clean-fail/admission causes are now typed as `PlanarAdmissionReason`, public tests prove all five admission classes, `MB-M6-1` through `MB-M6-8` have explicit `PlanarPremetabossInputFamily` rows with class + typed reason assertions, and pre-MetaBoss row digests now commit the input family plus backing matrix row. Verification passed: `cargo test -p worth-spatial --test public_api_contract -- --nocapture`, `cargo test -p worth-spatial --test ui -- --nocapture`, `cargo test -p worth-kernel binding::tests::planar_admission_phase_one -- --nocapture`, `cargo test -p worth-topo --test public_api_contract -- --nocapture`, `cargo test -p worth-topo --test ui -- --nocapture`, and `cargo check -p worth-spatial -p worth-kernel -p worth-topo`.

- Ran `qa-tests` and `code-quality-qa` on M6 Phase 1. Strengthened adversarial coverage: added a trybuild compile-fail proof that `PlanarAdmissionReceipt` cannot be WORTHd from public code, expanded kernel tests to prove all non-admitted classes fail closed, asserted receipt row-digest linkage, and required movement/rotation posture on every `MB-M6-*` admission row. Split planar admission implementation so classification lives in `classification.rs`, pre-MetaBoss projection lives in `premetaboss_rows.rs`, and `matrix.rs` owns only rows/receipts/matrix assembly. Moved new planar public-contract and compile-fail proofs into narrow `planar_admission/` folders so the batch does not add new direct files to already-crowded contract roots. Verification passed: `cargo test -p worth-spatial --test public_api_contract -- --nocapture`, `cargo test -p worth-spatial --test ui -- --nocapture`, `cargo test -p worth-kernel binding::tests::planar_admission_phase_one -- --nocapture`, and `cargo check -p worth-spatial -p worth-kernel -p worth-topo`.

- Implemented M6 Phase 2 exact planar predicate authority. Added `query_native_planar_predicate` in worth-spatial with WORTH Query domain/world/declaration family, canonical cyclic orient2d declarations, worth-math-backed certified predicate fact receipts, finite projected point rejection, certified-zero-before-repair denial, sealed receipt construction, and `facade::planar_predicates`. Added spatial public contract tests for cyclic authoring convergence, non-finite rejection, certified-zero denial, movement/rotation posture digest sensitivity, and compile-fail receipt WORTHry. Added worth-kernel consumer tests proving kernel consumes spatial receipts and cannot upgrade predicate uncertainty into a receipt. Verification passed: spatial public API contract, spatial UI trybuild, focused kernel Phase 2 tests, and `cargo check -p worth-spatial -p worth-kernel -p worth-topo`.

- QA-looped M6 Phase 2. Fixed structural and proof gaps: predicate evaluation errors now carry typed `PlanarPredicateEvaluationFailureKind`, certified-zero uncertainty carries precision metadata and counters, signed-zero coordinate drift is normalized in declaration/fact identity, declaration aspect contracts include projected points and coincidence policy, planar predicate authority is registered in geometry support/applicability matrices with explicit route-required and mutation-evidence-denied posture, and the Query-native predicate implementation was split into `domain.rs`, `authoring.rs`, and `facts.rs`. Added tests for support/applicability posture, signed-zero convergence, typed non-finite rejection, uncertainty counters, and full aspect contract. Verification stayed green.

- Ran `qa-tests` and `code-quality-qa` on M6 Phase 2. Strengthened the proof suite and test topology: split planar predicate public contracts into `surface_contract.rs`, `support_posture.rs`, `adversarial_authority.rs`, and `proof_fixture.rs`; added adversarial tests for odd authoring permutation digest separation and local-frame/topology/tolerance basis identity participation; tightened canonical basis counter assertions to exact values in spatial and kernel tests. Production query-native predicate files remained split by responsibility and under line caps. Verification passed for spatial public API, focused kernel Phase 2 tests, spatial UI trybuild, and cross-crate check.

- Implemented M6 Phase 3 planar precision certification. Added `planar_contracts/precision_basis` and `query_native_planar_precision` in `worth-spatial`, exposing `facade::planar_precision` with receipt-backed local feature-scale, world magnitude, normalization-scale, movement/rotation, tolerance-policy, and predicate-receipt basis. Registered `PlanarPrecisionCertification` in geometry inventory/support/applicability. Added spatial adversarial tests for 21-order scale separation, missing/contradictory basis denial, movement/rotation mismatch denial, and `MB-M6-3`; added compile-fail proof that precision receipts are not WORTHable; added kernel Phase 3 consumer tests proving kernel consumes spatial receipts without synthesis. Verification passed: spatial public API contract, spatial UI trybuild, focused kernel Phase 3 tests, and `cargo check -p worth-spatial -p worth-kernel -p worth-topo`.

- QA-looped M6 Phase 3. Found and fixed real completion gaps: precision basis builder no longer defaults missing local/world scale orders to `0`; missing scale orders have typed denials; normalization scale must match the declared local feature-scale order; counters now expose `precision_escalation_breadth` from consumed worth-math metadata; spatial/kernel tests assert these edge cases. Verification passed after correction: spatial public API contract, spatial UI trybuild, focused kernel Phase 3 tests, and cross-crate check. All new files remain under line caps.

- Ran `qa-tests` and `code-quality-qa` on M6 Phase 3. Strengthened tests with a non-zero worth-math adaptive `ExpansionB` fixture so precision escalation breadth is genuinely exercised rather than only mirroring Float64 metadata. Split the planar precision public-contract tests by proof responsibility (`scale_separation`, `basis_denial`, `movement_rotation`) and moved shared precision receipt assembly into the narrow proof fixture. Named the normalization-scale relative tolerance in production validation. Verification passed: focused planar precision public API tests, spatial UI trybuild, focused kernel Phase 3 tests, and cross-crate check. Phase 3 test directory has 7 files and all touched files remain under line caps.

- Implemented M6 Phase 4 planar local-frame certificates. Added `planar_contracts/local_frame`, `query_native_planar_local_frame`, and `facade::planar_local_frame` in `worth-spatial`, exposing a Query-backed retained certificate that consumes Phase 3 precision receipts, derives local axes through existing frame math, commits frame/origin/normal/scale/normalization/transform-chain/movement/tolerance/precision digests, and denies missing or invalid basis instead of synthesizing frame truth. Registered `PlanarLocalFrameCertificate` in geometry inventory/support/applicability with recovery and mutation evidence explicitly denied. Added spatial adversarial tests for scale separation, non-finite origin, zero/non-finite normal, missing transform digest, missing precision receipt, precision-basis mismatch, movement/rotation digest participation, semantic rotation invalidation, and compile-fail receipt WORTHry. Added kernel Phase 4 tests proving kernel consumes spatial frame receipts and cannot synthesize missing frame basis. Verification passed: focused local-frame public API tests, full spatial public API contract, spatial UI trybuild, focused kernel Phase 4 tests, and `cargo check -p worth-spatial -p worth-kernel -p worth-topo`. Bash was unavailable for the repo line-cap script, but a PowerShell line-count audit showed all Phase 4 files under 400 lines.

- QA-looped M6 Phase 4. Found and fixed two proof gaps: local-frame Query declaration identity now commits upstream precision declaration/envelope digests plus derived axis and scale-separation basis entries, and invalid normals are validated before precision receipt alignment so malformed frame geometry cannot be masked by an unrelated mismatch. Added adversarial tests for retained-basis declaration identity and invalid-normal precedence. Verification passed: focused local-frame public API tests, full spatial public API contract, spatial UI trybuild, focused kernel Phase 4 tests, and cross-crate check. Phase 4 files remain under line caps.

- Ran `qa-tests` and `code-quality-qa` on M6 Phase 4. Strengthened tests so local-frame declaration identity asserts exact canonical retained-basis values instead of mere locus presence, and added a spatial adversary proving transform-chain digest changes retained frame identity. Fixed production structure by extracting `basis_identity.rs` so Query canonical declarations and retained receipt digest parts come from one local-frame identity basis rather than duplicated lists. Verification passed: focused local-frame public API tests, full spatial public API contract, spatial UI trybuild, focused kernel Phase 4 tests, and cross-crate check. Phase 4 production/test directories remain under 10 files and all touched files remain under 400 lines.

- Implemented M6 Phase 5 certified plane-to-2D projection. Added `planar_contracts/projection_2d`, `query_native_planar_projection`, and `facade::planar_projection` in `worth-spatial`, with a Query-backed projection receipt that consumes certified local-frame receipts, derives 2D coordinates from declared local deltas rather than subtracting massive world coordinates, commits source point basis, local frame fact/declaration/envelope digests, movement/rotation posture, tolerance policy, and signed distance bits, and denies missing receipts, non-finite points/deltas, frame-basis mismatches, and off-plane repair/snap attempts. Registered `ProjectPointToCertifiedPlane2D` in geometry support/applicability with routing, inspection, and mutation evidence required while projection consumption and recovery remain gated. Added spatial adversarial tests for replay stability, frame digest sensitivity, scale separation, coplanar overlap storm, denial precedence, support posture, public facade shape, and compile-fail receipt WORTHry. Added kernel Phase 5 consumer tests proving kernel consumes spatial projection receipts without synthesizing projection truth. Split geometry support family contract lookup out of `geometry_support_posture.rs` to keep touched files under line caps. Verification passed: focused projection public API tests, full spatial public API contract, spatial UI trybuild, focused kernel Phase 5 tests, and `cargo check -p worth-spatial -p worth-kernel -p worth-topo`.

- QA-looped M6 Phase 5. Found and fixed two completion gaps: projection mutation evidence was only implied by canonical declaration entries despite the phase requiring mutation evidence now, and projection denials exposed typed kinds but not a machine-readable denial-basis locus for inspection. Added `ProjectPointToCertifiedPlane2DMutationEvidence` to receipts and public facade, added denial basis loci for source/basis/frame/delta/plane-distance failures, and strengthened spatial/kernel tests to assert the evidence and denial loci directly. Verification passed after correction: focused projection public API tests, full spatial public API contract, spatial UI trybuild, focused kernel Phase 5 tests, and cross-crate check. Phase 5 production/test files remain under 400 lines and `projection_2d` remains under the 10-file directory cap.

- Ran `qa-tests` and `code-quality-qa` on M6 Phase 5. Found and fixed proof/structure weaknesses: projection declaration identity and retained receipt fact digest parts were duplicated across Query authoring and certificate construction, denial-basis loci were not exhaustively pressured, and mutation evidence was not proven stable under replay or sensitive to source identity. Added `basis_identity.rs` as the single projection identity-basis source consumed by both Query canonical declarations and receipt digest parts. Strengthened spatial tests for missing source identity, missing point-basis digest, missing movement posture, missing tolerance policy, canonical frame origin/axis commitments, replay-stable mutation evidence, and source-identity-sensitive evidence. Verification passed: full spatial public API contract, spatial UI trybuild, focused kernel Phase 5 tests, and cross-crate check. `projection_2d` now sits exactly at the 10-file directory cap; all touched files remain under 400 lines.

- Implemented M6 Phase 6 certified segment-segment 2D classification after correcting the DX target away from raw `a0/b0` predicate wiring. Added `CertifiedProjectedSegment2D`, `CertifiedSegmentSegment2D::classify`, named segment contact policy, `compile(&CertifiedSegmentSegment2DContracts)`, and `plan.certify()` facade over a Query-native `CertifiedSegmentSegment2D` retained artifact. Internally the segment fact path derives the four `orient2d` predicate authority receipts from projected endpoint receipts and records their signs, precision metadata, projection fact digests, mutation evidence, counters, and Query envelope identity. Registered the surface in geometry inventory/support/applicability. Added spatial adversarial tests for all contact classes, readable-DX public surface, support posture, movement/rotation evidence sensitivity, and compile-fail receipt WORTHry; added kernel Phase 6 consumer proof. Verification passed: spatial public API contract, spatial UI trybuild, focused kernel Phase 6 test, and `cargo check -p worth-spatial -p worth-kernel -p worth-topo`. New `segment_segment_2d` directory is exactly 10 files and all audited files remain under 400 lines.

- QA-looped M6 Phase 6. Found and fixed real completion gaps: the public `SegmentContactPolicy` was not participating in the certified basis, topology basis could be omitted despite being part of the required identity/context, policy-required collinear overlap was not represented, and a public basis accessor could panic before certification while segment-basis fact errors collapsed denials to strings. Added contact policy identity to declarations/digests, required explicit topology and policy basis, added `RequireImprintForCollinearOverlap` yielding `PolicyRequiredOrUncertain`, made segment-basis errors carry typed denials, narrowed the orientation digest accessor, and added tests for missing topology and policy-required overlap. Verification passed again: spatial public API contract, spatial UI trybuild, focused kernel Phase 6 test, and cross-crate check.

- Ran `qa-tests` and `code-quality-qa` on M6 Phase 6. Found and fixed two quality issues: tests did not adversarially pressure mixed projected endpoint bases, and the public facade was implementing the intent/plan/certify workflow instead of acting as a pure aggregation boundary. Added `denial_and_basis.rs` tests for mixed frame and mixed movement/rotation projection bases denying before predicate certification. Moved `CertifiedProjectedSegment2D`, `CertifiedSegmentSegment2D`, `CertifiedSegmentSegment2DContracts`, plan, and policy workflow into `bindings/query_native_planar_segment_segment/workflow.rs`; reduced `facade/planar_segment_segment.rs` to re-exports. Verification passed again: spatial public API contract, spatial UI trybuild, focused kernel Phase 6 test, and cross-crate check. File/directory audit remains clean: segment contract dir 10 files, segment binding dir 5 files, segment test dir 7 files, all audited files under 400 lines.

- Implemented M6 Phase 7 certified polygon winding and loop containment. Added Query-native CertifiedPolygonWinding2D with readable projected-loop workflow, explicit topology loop basis rows, retained winding/containment receipt, predicate and segment-contact evidence, self-intersection/ambiguous-touch denial, movement/rotation and projection-basis validation, support/applicability registration, spatial adversarial public contracts, compile-fail receipt WORTHry proof, and kernel consumer proof. Verification passed: spatial public API contract, spatial UI trybuild, focused kernel Phase 7 test, cross-crate check, and line/directory audit (polygon contract dir 10 files, binding dir 5, winding test dir 6; no Phase 7 files over 400 lines).

- QA-looped M6 Phase 7. Found and fixed real gaps: winding was collecting certified predicate receipts but deriving winding from raw f64 shoelace, rotated equivalent loops were not proven to converge to the same retained fact identity, and nested-hole replayability was not explicitly asserted under the MB-M6-1 test name. Corrected winding to derive the admitted Phase 7 winding class from certified predicate signs, deny mixed/zero winding predicate evidence typed, canonicalize loop evidence under cyclic rotation, stabilize edge identities from projection facts, compute semantic loop-edge counters explicitly, and strengthen tests for rotation digest convergence and replay-stable nested-hole receipts. Verification passed: spatial public API, spatial UI trybuild, focused kernel Phase 7, cross-crate check, and line/directory audit.

- Ran qa-tests and code-quality-qa on M6 Phase 7. Found and fixed test/prod quality gaps: replaced debug-string denial assertion with typed CertifiedPolygonWinding2DFactError matching, added a cross-loop containment boundary-touch adversary, split segment-contact certification out of winding acts.rs into contacts.rs, added a distinct segment-contact certification denial kind, and changed winding identity entries to dynamic indexed loci so large/nested evidence rows do not collapse into extra labels. Verification passed: spatial public API, spatial UI trybuild, focused kernel Phase 7, cross-crate check, and line/directory audit (contract 10 files, binding 6, tests 6; all Phase 7 files under 400 lines).

- Implemented M6 Phase 8 certified signed area and degeneracy basis. Added Query-native `CertifiedSignedArea2D` in worth-spatial with a 10-file `signed_area_2d` contract dir, 5-file `query_native_planar_signed_area` binding dir, facade export, support/applicability/inventory registration, receipt WORTHry compile-fail proof, spatial adversarial tests for local-frame scale separation, sliver classification, tiny-hole classification, mismatched movement/rotation precision denial, MB-M6-3, and a worth-kernel consumer proof. Signed area consumes Phase 7 winding receipts plus Phase 3 precision receipts, uses projected local coordinates and local normalization scale, returns no repair action, and keeps recovery/boolean keep-discard out. Verification passed: full spatial public API contract (107 tests), spatial UI trybuild, focused kernel Phase 8 test, cross-crate check, and line/directory audit.

- QA-looped M6 Phase 8. Found and fixed real gaps: outside containment candidates were being treated as ordinary signed-area contribution instead of `PolicyRequired`, loop-edge counters were aliased to area-term counters, interval arithmetic was not participating in area classification despite the spec naming it, and zero/needle degeneracy pressure was not explicitly proven. Corrected signed-area accumulation to inspect outside candidates as policy-required without area guessing, separated loop-edges-walked from area-terms-evaluated, added interval sign screening before exact rational classification, strengthened localized causes with loop/edge or containment basis, and added adversarial tests for zero/sliver/needle plus outside-loop policy-required. Verification passed: full spatial public API contract (109 tests), spatial UI trybuild, focused kernel Phase 8 test, cross-crate check, and line/directory audit.

- Ran `qa-tests` and `code-quality-qa` on M6 Phase 8. Found and fixed test/prod quality gaps: signed-area test support mixed runtime Query handles with domain fixture assembly, production `accumulation.rs` under-named its real measurement/classification responsibility, and degeneracy localization used strings that forced weak substring assertions. Split `runtime_handles.rs` out of the signed-area test harness, renamed production `accumulation.rs` to `measurement.rs`, added typed `SignedAreaDegeneracyCause`, and updated tests to assert exact cause variants instead of string contains. Verification passed: full spatial public API contract, spatial UI trybuild, focused kernel Phase 8 test, cross-crate check, and line/directory audit.

- QA-looped M6 Phase 9 after implementation. Found and fixed one remaining canonicality gap: coplanar overlap containment rows and signed-area policy-required exit fact digests could still encode input face order under reversed authoring. Canonicalized containment row ordering and policy-exit consumed fact digest construction, then added reversed-authoring adversarial tests for dual containment rows and policy-required exits. Verification passed: focused planar overlap public API tests, full spatial public API contract, spatial UI trybuild, focused kernel Phase 9 test, cross-crate check, and Phase 9 line/directory audit.

- Implemented M6 Phase 10 planar contract bundle validation. Added `contract_bundle` planar contract receipts, Query-native `query_native_planar_contract_bundle`, `facade::planar_contract_bundle`, support/applicability registration with explicit boolean-readiness certification posture, public contract tests, compile-fail receipt WORTHry proof, and kernel Phase 10 consumer proof. The validator consumes complete Phase 1-9 receipt families, enforces topology basis, movement/rotation posture, projection-consumed rows, retained fact rows, diagnostic scope, and pre-boolean/no-imprint posture without synthesizing missing planar truth. Verification passed: focused bundle public API tests, full spatial public API contract, spatial UI trybuild, focused kernel Phase 10 test, and cross-crate check. Line/directory audit passed with new/touched Phase 10 Rust files under 400 lines.

- QA-looped M6 Phase 10. Found and fixed another contract-bundle identity gap: `within_planar_neighborhood(...)` was only checked for non-empty and did not participate in Query declaration identity or validate against retained overlap/winding/signed-area facts. Added neighborhood matching against consumed certified facts, committed the neighborhood as a canonical Query declaration entry, and added an adversarial stale-neighborhood denial test. Also fixed prior QA gaps in bundle validation: supplied receipts must be identity-linked through precision -> frame -> projections -> winding/segments -> signed-area -> overlap, and denial counters expose rejected missing-family rows. Split `worth-topo` topology mutation application error display into `error_display.rs` so the earlier WORTH Query recovery-stop mapping file stays under the 400-line cap. Verification passed: focused Phase 10 spatial public contracts, full spatial public API contract, spatial UI trybuild, focused kernel Phase 10 test, and cross-crate check; final line audit found no changed Rust files over 400 lines.

- Ran `qa-tests` and `code-quality-qa` on M6 Phase 10. Found and fixed a hostile test/proof gap: the bundle validator rejected missing projections but still accepted extra or duplicate projection-consumption receipts, letting counters/family rows claim unconsumed work as part of boolean readiness. Added bidirectional projection-consumption closure validation so supplied projections must be unique, consume the supplied local frame, be consumed by retained winding/signed-area/overlap/segment evidence, and no unrelated projection row can inflate the bundle. Added adversarial tests for stray same-frame projection receipts and duplicate projection receipts, and tightened complete-bundle counter assertions to exact family-row and retained-fact counts. Code-quality pass split projection closure out of `validation.rs` into `projection_consumption.rs`, restoring file-size discipline and making the validation orchestrator read as a checklist. Verification passed: full spatial public API contract, spatial UI trybuild, focused kernel Phase 10 proof, cross-crate check, and final line/directory audit.
