# 2026-07-18

- Completed a hostile QA/correction pass over Worth UI milestone 3.8.1 Phases
  8-10.
- Closed exact active-session identity, candidate-owned graph/catalog, atomic
  cutover, configured operational host, raw staging/activation bypass, and
  failure-localization gaps.
- Added cross-session compile/runtime proofs, real headless/egui host parity,
  successful structural cutover, and a 1,000-step mixed replacement storm.
- Final Worth UI evidence: 842/842 runtime library tests; public and
  certification compile-contract matrices green; all remaining package groups
  green; strict all-target/all-feature Clippy green; boundary-check and
  agent-context green; no non-allowlisted over-400-line Rust files in
  `workspaces/worth-ui`.
- The repository-root line-cap guard still has 147 non-allowlisted violations
  outside the Worth UI workspace. Do not represent the global line-cap gate as
  green.
- A follow-up hostile pass found that active replacement cutover still matched
  candidate allocation catalogs by semantic graph digest. Replaced the raw
  identity lane with an opaque `UiGraphAuthorityIdentity` shared by snapshots,
  mounted transitions, catalogs, and cutover; added an equal-digest foreign-
  graph denial/preservation proof. Runtime library closure is now 843/843,
  strict runtime Clippy and both constitutional checks remain green.
- The subsequent intent-based system audit closed four deeper lifecycle gaps:
  generation-local planning-inspection registry rebinding at cutover; honest
  denial for missing/foreign evidence refs despite equal numeric generations;
  semantic rollback of valid partial frames when a source callback unwinds;
  and active-session/candidate inspection and admission delegation after the
  prepared app is consumed by launch.
- Added behavioral proof that candidate evidence is isolated before cutover and
  becomes the active authority only after successful cutover. Canonical full
  Worth UI lane passed in 485 seconds, strict all-target/all-feature Clippy and
  both constitutional checks passed, and scoped Worth UI line caps passed.
- The exact repository-wide line-cap equivalent now reports 53 non-allowlisted
  files, all outside `workspaces/worth-ui`; the global gate is still not green.
- Completed Worth UI milestone 3.9 Phases 1-2: compile/test topology is budgeted
  at three batched compile-contract sessions, real-boundary/seam/timing evidence
  is machine-checked, and filesystem source acquisition now reads real `.wui`
  bytes through a stable two-pass snapshot.
- Added the production `notify` 8.2 native operating-system watcher with
  readiness, bounded quiet-window settlement, unchanged-snapshot suppression,
  transient-read retry, explicit unwatch shutdown, and public replacement-path
  proof using real Windows writes, rename-over replacement, deletion, and
  recreation. The same filtered application-contract target now runs in
  Windows CI without adding a Rust test target.
- Final phase evidence: all 133/62/3 executed compile contracts passed and the
  284/62/3 physical inventories reconcile exactly; 848 runtime tests, fast and
  hostile-certification lanes, strict workspace Clippy, dependency contract,
  boundary-check, agent-context, and all hostile Python topology checks passed.
  All 63 changed code/test files are under 400 lines; the known repository-
  global line-cap debt outside this slice remains unresolved.
- Hostile phase/intent QA then rejected four superficially green claims: the
  test's "atomic" replace had an absence window; imported-module churn asserted
  only package digests; the watcher notification channel was unbounded; and
  direct/initial snapshots claimed settlement without crossing a quiet window.
- Corrected them with a certification-only real same-filesystem atomic replace,
  canonical application/graph assertions for imported declaration churn, a
  bounded coalescing notification queue with retained resnapshot/failure
  signals, canonical root identity, and bounded real quiet-window acquisition.
  The timing validator now checks machine comparability and the 10-percent or
  reviewed-amendment result instead of trusting an unchecked label.
- Post-correction closure is green: 849 runtime tests, hostile certification,
  strict all-target/all-feature Clippy, 133/62/3 compile-contract sessions,
  topology/unit validators, dependency contract, boundary-check, and
  agent-context. All changed Rust/Python files remain within 400 lines.
- A final pre-Phase-3 QA found and corrected three additional gaps: broad proof-
  ledger rows could defer Query lowering and minimal headless obligations to
  later execution phases; the test-seam scanner missed `#[cfg(test)]` enum
  variants; and a watcher could overrun a deadline after consuming an event and
  then lose that change on return. The ledger now has phase-exact staged claims,
  enum variants are inventory-enforced, and deadline-limited settlement retains
  a coalesced resnapshot obligation. Real native-watcher continuation proof,
  851 runtime tests, 11 application contracts, strict targeted Clippy,
  topology, boundary-check, and agent-context are green.
- One more phase/intent QA found that Phase 3 still left allocation planning
  circularly dependent on the provisional execution-plan input; its Query
  lifetime test demanded future no-op/rebind/publication transitions too early;
  Phase 5 claimed real headless and zero-allocation behavior while deferring the
  durable envelope and independent allocator observer; and the proof ledger's
  broad Phase-1 public-lifecycle row could mask later plan obligations. The 3.9
  spec now requires a sealed non-executable pre-allocation projection followed
  by the sole post-commit plan-lowering authority, assigns Query lifetime rows
  to their first real phases, introduces the durable ordinary envelope and armed
  allocator in Phase 5, and splits public/Query/allocator ledger claims by phase.
  Proof-ledger, topology, timing validators, and diff hygiene are green.
- The final pre-build QA caught an enforcement mismatch the isolated ledger test
  missed: the integrated topology validator still required the old broad claim
  set and assigned the allocator observer to Phase 16. Its canonical phase map
  now matches the split ledger, including Phase-5 allocator observation and
  Phase-16 cross-lane reconciliation; the integrated topology gate is green.
- The same pass made phase truth explicit: Phase 2 no longer claims canonical
  plan preservation before Phase 5, every active slice from Phase 5 onward must
  use the Phase-4 atomic publication shell, and Phase 15 closes the final
  cross-lane transaction rather than first introducing atomicity.
- A final Phase-3 implementation QA found that prepared successor authority
  still correlated an admitted candidate by digest-based basis rather than the
  exact immutable artifact allocation. Prepared lowering authority now retains
  and checks shared `Rc` artifact identity for replacement and launch; a hostile
  equal-value/foreign-allocation regression proves denial. The legacy synthetic
  subsystem launcher is now an explicit, inventoried below-authority seam, and
  production no longer clones committed allocation input merely for test access.
