# 2026-06-25

- Implemented Milestone 10 Phase 16 wire-view product migration as a
  receipt-backed touched-read-stage lane. The old ordinary
  `derived_topology::wire_views` module was deleted, traversal wire
  interpretation now routes through the migrated product lane, wire-view
  execution input requires a read-stage receipt, the public migrated-products
  contract exports read-source/read-stage receipt APIs instead of the old direct
  source-row shortcut, and focused wire-view tests prove locality counters,
  old-authority residue caps, receipt binding, and product row half-edge
  breadth.
- QA of Phase 16 found that `WireViewReadSource::from_rows_with_query_reports`
  was still a production/public-looking raw row and digest fabrication seam.
  Tightened it to `cfg(test)` and removed it from the public migrated-products
  contract so ordinary consumers cannot certify wire-view read sources from
  caller-supplied rows, counters, and strings.
- Tightened Milestone 10 deletion honesty after Phase 16. Reclassified
  test-only `TopologyMaterializer` rows and historical Milestone 3 operator
  fallout rows as capped certification/bootstrap residue instead of ordinary
  migration blockers, split `CompleteTopologyBootstrap` from
  `WholeViewRebuild`, and locked the current deletion firewall to four true
  ordinary blockers: `interpret_topology_view`, shell legacy interpretation,
  vertex-disk legacy interpretation, and wire-view legacy interpretation.
- Continued Milestone 10 closeout cleanup by deleting the
  `legacy_interpretation.rs` modules from the migrated shell, vertex-disk, and
  wire product-family lanes. Their remaining old whole-view compatibility logic
  now lives under `derived_topology::traversal_views::*_compatibility`, and the
  inventory/source firewall points there explicitly instead of hiding it inside
  the new receipt-backed migrated product tree. Focused inventory and deletion
  closeout tests plus `cargo check -p worth-topo` pass.
- Repaired Milestone 10 final closeout tests so they no longer execute the old
  denied `.topology.materialized` retained surface just to build an operator
  artifact. The closeout test now binds operator cutover through declared
  touched-basis plus graph-obligation evidence, proves closeout logic with a
  clean test firewall, and separately proves current source still rejects until
  the four traversal compatibility blockers are cut. Projection row-decode
  helpers for interpreted/validation/diagnostics surfaces were fenced to
  `cfg(test)` so production code no longer exposes those old direct helper
  APIs.
- Finished the Milestone 10 final ordinary traversal cleanup. Deleted the old
  `traversal_views/facade.rs`, `shell_compatibility.rs`,
  `vertex_disk_compatibility.rs`, and `wire_compatibility.rs` files; moved the
  remaining non-ordinary bootstrap/certification interpretation into
  `bootstrap_interpretation.rs`; changed the final inventory from 4 migrate
  rows to 0 migrate / 8 delete rows; and updated deletion/final closeout tests
  so current source closes with zero old ordinary authority violations while
  certification/bootstrap residue remains capped and non-admissible.
- Completion-audited Milestone 10 against the Phase 17, Must Ship, and
  Acceptance Evidence requirements. The deeper `covered_sweep` test exposed
  that materialized-graph residue still treated deleted
  `materialize_query_input` as a required cap; fixed it so deleted ordinary
  authority lives in the deletion ledger while live bootstrap/certification
  residue remains capped. Verification passed for `cargo check -p worth-topo`,
  inventory, deletion closeout, milestone ten closeout, covered sweep,
  required sweep, and `cargo test -p worth-topo --test compile_fail_boundaries`.
- Implemented the first Milestone 11 Phase 11 cutover lane as a parallel
  lookup-consumed proof path. `worth-spatial` now has
  `evidence_lookup_stage_cutover` and `evidence_lookup_workload_cutover`
  modules plus facade/public-contract exports. The new path proves covered
  stage success from sealed spatial touch authority + stage receipt identity +
  selected lookup plan + execution receipt, records topology-derived receipt
  state as `NotRequired` or typed refs, lowers a Milestone 12 seed without
  rescan, and rejects raw-row/broad-receipt/caller-scan fallback by counter
  boundary. `worth-kernel` now has a parallel
  `LookupConsumedWorkloadComposition` wrapper that admits only the new handoff
  and checks stage-index identity instead of widening the old evidence-lane
  contract.
