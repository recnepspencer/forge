- Worth UI Milestone 4.1 Phase 18 QA-loop: found portal host admission was underusing the graph and containment model. Fixed discarded focus/dismiss policy, unvalidated portal owner identity, fake hardcoded obligation counts, and missing registered portal surface handling. Portal admission now selects portal-specific Query obligations, carries owner/focus/dismiss facts, denies impossible portal state before pancake-list mutation, and exposes focus/dismiss on entry receipts. Split portal fact ids, portal mounted node, portal graph authority, portal operation declarations, and portal facade exports to keep touched Worth UI files within the 400-line cap. Verified focused portal tests, worth-ui test check, and tracked Worth UI line-cap sweep.
- Code-quality QA for Phase 18 portal host: found a half-applied admission split and a compressed admission orchestrator shape. Cleaned `portal_host/admission.rs` into an orchestration facade, moved index construction, preflight denials, and per-entry resolution into named child modules, removed dangling helper fragments, and kept all Worth UI plus validation-app Rust files under the 400-line cap. Verified `cargo check -p worth-ui`, portal host admission tests, and sealed runtime authority trybuild coverage.
- Worth UI Milestone 4.1 Phase 19 implementation: added collection composition as a runtime/Query-owned primitive family that reuses the virtualized data lane instead of inventing another renderer virtualizer. Collections now admit source binding, stable item identity, visible window, item template, posture source, sticky/group metadata, graph access, and sealed mounted window receipts; unstable display/index identity, raw item sizing, local posture maps, and missing template graph access are denied. Added collection fact families, Query obligation semantics/operations/touch descriptors, mounted collection nodes, facade exports, focused runtime tests, and trybuild coverage proving collection receipts cannot be externally minted. Split receipts/counters/denials to keep new files under the 400-line cap.
- Phase 19 QA-loop: found collection mounted item identity was still row-index shaped (`collection.item.{row}`) even though admission denied index identity. Fixed it by requiring Query projection fact receipts for visible item identity, deriving sealed `WorthUiCollectionVisibleItemReceipt`s from those facts, denying missing identity projection facts, carrying consumed `query_projection_fact` evidence, and mounting item nodes from visible item receipts instead of renderer row indexes. Also removed proof-only visible-window naming and added compile-fail coverage proving visible item receipts cannot be minted by app code.
- Phase 19 test-hardening follow-up: replaced the loose collection identity fact vector with a sealed runtime `WorthUiCollectionIdentityProjectionReceipt` admitted from Query projection facts plus virtualized frame/materialization evidence. Collection composition now accepts that proof-bearing receipt instead of raw facts, rejects duplicate/surplus/missing/synthetic/stale/mismatched identity facts before mounted window construction, exposes rendered/measured item counters on the sealed mounted window receipt, and has trybuild coverage proving app code cannot mint collection identity or mounted collection receipts. Split collection diagnostics and query-graph descriptor/declaration helpers to keep touched Worth UI files under the 400-line cap.
- Code-quality QA for Phase 19 collection composition: found `collection_composition/admission.rs` and `collection_composition/receipt.rs` were under the line cap but structurally compressed. Split admission into entry/orchestrator plus `admission/validation.rs`, `admission/construction.rs`, and `admission/report.rs`; split collection receipt authority into per-receipt files under `receipt/` while preserving the public receipt facade. Reverified worth-ui check, collection composition tests, runtime authority trybuild, directory sizes, and touched-file line counts.
- Worth UI Milestone 4.1 Phase 20 implementation: added adaptive composition alternatives as runtime/query-graph authority. Adaptive posture now admits from host measurement observations plus typed density/theme/motion/input hints; selection consumes adaptive declaration/posture/composition facts and produces sealed graph-backed alternative selection receipts; state preservation requires explicit carry-forward declarations and denies changed alternatives without eligibility. Added adaptive runtime fact families, Query obligation semantics/touch descriptors/rule identities/operation plans, facade exports, focused integration tests using mounted product views and admitted host observations, and trybuild coverage proving adaptive receipts cannot be minted outside runtime authority. Verified `cargo check -p worth-ui`, `cargo test -p worth-ui --test adaptive_composition_runtime`, runtime-authority trybuild, and line caps for new adaptive files.
- Worth UI Phase 20 test-hardening follow-up: replaced caller-provided adaptive breakpoint points with token-only authored breakpoint declarations resolved through active density tokens, added graph-backed adaptive declaration admission receipts, raw posture draft denials, adaptive projection rebind receipts with no source/artifact/renderer parse counters, renderer boundary guards against adaptive branching in validation-app code, and stronger durable-state preservation coverage over real reconciliation receipts. Split adaptive integration tests into admission/selection/rebind/support modules under the 400-line cap. Verified focused adaptive tests, preservation tests, renderer guard, runtime authority trybuild, and `cargo check -p worth-ui`.
- Worth UI Milestone 4.1 Phase 21 implementation/test hardening: added a real motion timing token capability family, graph-bound composition motion subject receipts, source-authored motion recipe admission, active motion reconciliation receipts, runtime reduced-motion policy receipts, and deterministic easing sample receipts. Closed the important bypass where activation could be planned from raw subject IDs and raw reduced-motion booleans; activation now consumes a sealed graph subject receipt and optional reduced-motion policy receipt. Split motion test fixture support to keep files under the 400-line cap. Verified focused composition motion tests, runtime authority trybuild sealing, and `cargo check -p worth-ui`.
- Code-quality QA for Phase 21 motion: split the flat `runtime/composition_motion` directory into recipe admission, active motion, graph subject, and diagnostics boundaries; then split the touched over-cap `runtime_fact/fact_id.rs` constructor pile into platform, capability, query, composition, and primitive fact-id subfamilies. Reverified `cargo check -p worth-ui`, focused composition motion tests, runtime authority trybuild sealing, and touched-file/directory line counts.
- Worth UI Milestone 4.1 Phase 23 implementation: added a sealed runtime dev-hot-reload command/write-request lane, validation-app scenario catalog buttons that submit mounted live-view interactions and rewrite the watched live_view source through the existing reload loop, live-view manual reload target support, and runtime-mounted flow policy resolution for validation container policies. Updated the watched live_view source to a Phase 23 baseline with setup buttons for baseline/dropdown/row fit/row equal/stacked/plain payload/conditional extra. Verified worth-ui-validation-app check and focused scenario source parse test using the existing target cache.
- Worth UI Phase 23 first-four cleanup: replaced the validation-local flow policy match with a runtime composition local-layout policy recipe receipt, made mounted flow containers consume recipe/policy facts, admitted validation dev scenario effects into the normal live-view interaction lane, and added mounted diagnostic evidence rows for dev hot-reload file-write requests/denials. Added focused tests proving scenario click -> runtime submit -> dev command -> watched file write -> reload, policy edits rebind mounted flow without payload rebuild, and visible evidence rows expose the write request. Relaunched the validation app successfully.
- Worth UI Phase 23 manual stress check: reverted the half-started intrinsic-measurement signature edit in layout allocation, then authored a watched-source three-panel composition proof with a left full-height form panel and two right-side form/card panels. The app launched and survived a source-only hot-reload edit, but the test exposed hard gaps: watched live-view authoring does not yet declare true shell mosaic regions, mounted surface receipts are still hardcoded to `live_view.form_card`, and the validation renderer panics when no root surface receipt attaches to the composition tree.
- Worth UI Milestone 4.1 spec update: appended phases 25-29 to close the manual workbench stress-test gaps as first-class architecture: runtime witness/red-flag invariants, authored local-layout recipes, mounted surface authority, intrinsic measurement receipts, and renderer conformance checks. The intent is to stop treating invisible nodes, hardcoded Rust layout recipes, fake fallback geometry, and renderer drift as local bugs; they are now end-of-milestone enforcement gates.
- Worth UI Milestone 4.1 spec correction: after a deeper audit, reshaped the tail into phases 25-31 in a stricter order: renderable mount completeness, mounted surface authority, mosaic shell topology hot reload, authored local-layout recipes, intrinsic measurement, runtime witness, and renderer conformance. The key architectural lesson is that witness/conformance must certify already-real mounted graph, surface, mosaic, recipe, and measurement authority; otherwise they become after-the-fact explanations over missing foundations.
- Worth UI Milestone 4.1 Phase 25 implementation: added renderable mount coverage as a runtime-owned admission step between admitted composition graph access and product-view receipts. Product mounting now rejects admitted graph children that have no mounted node receipt, duplicate mounted identities, or unsupported renderable kinds, with sealed coverage receipts/reports/counters/digests and trybuild coverage proving app code cannot mint the receipt. Split product-view admission reports and counters into submodules to keep touched files under the 400-line cap. Verified worth-ui check, validation-app check, focused missing-mounted-node proof, runtime authority trybuild, and touched-file line counts.
- Phase 25 QA-loop corrections: found the initial implementation was too narrow. Fixed support posture so Text/Icon/MosaicRegion are unsupported until real mounted authority exists, canonicalized renderable coverage denial order/digests, added hostile tests for nested supported-but-unmounted surfaces and unsupported text/icon nodes, closed the public `WorthUiMountedViewReceipt` facade export so flat mounted nodes cannot be a product traversal API, added compile-fail coverage for that boundary, and split over-cap mounted/product proof files back under 400 lines. Reverified worth-ui and validation-app checks, focused proof tests, boundary tests, and runtime authority trybuild.

- Worth UI Phase 27 implementation pass: added runtime-owned mounted mosaic shell receipts, admission denials, and allocation receipts; product-view mounting now requires the page-host mosaic shell and carries shell graph/consumed-fact evidence. Validation host observation now scopes product measurement/rendering to the mounted page slot instead of full-window fallback, and missing shell slot frames become typed unavailable observation posture. Rejected local composition authoring of mosaic_region children so mosaics stay shell/page topology rather than inner component layout. Verified worth-ui and validation-app checks plus focused mounted-product and mosaic-shell topology tests; kept touched files under 400 lines.
