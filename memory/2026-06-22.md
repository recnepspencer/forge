- Created and switched to branch `forge-store` to investigate rebuilding Forge Store from a cleaner foundation.
- Initial architecture read: Roadmap 1 is semantic durability; Roadmap 2 requires a physical database foundation. Existing `crates/forge-store` already contains substantial semantic/code evidence and some S.0 work, but it is too large and entangled to use as the new physical foundation directly.
- Scaffolded nested workspace `workspaces/forge-store` with 36 small crates covering shared contracts, Roadmap 2 physical foundation, Roadmap 1 semantic durable programs, test support, certification, and a thin public facade. Verified with `cargo check --manifest-path workspaces\forge-store\Cargo.toml`.
