# 2026-06-22

## Query declaration terminal aspect projection helper fence

- Removed public terminal projection helpers from declaration publication and
  grouped aspect participation proof carriers; public callers must keep native
  `AspectFieldKey` slices instead of recovering terminal aspect-field strings.
- Narrowed the generic declaration aspect terminal projector to the
  `application` boundary. Grouped support now compares native key parts, and
  grouped/continuation digest paths use local `*_for_digest` reporting helpers.
- Deliberate `cargo check -p forge-query --tests` red was captured in
  `target/aspect-native-declaration-terminal-projection-red.log` and exposed
  the broad re-export plus grouped/continuation consumers.
- Added aspect-native trybuild coverage proving facade callers cannot call the
  removed declaration/grouped terminal projection helpers.
- Verification: `cargo fmt -p forge-query`, red then green `cargo check -p
  forge-query --tests`, green `cargo check -p forge-query`, trybuild overwrite
  plus normal `cargo test -p forge-query --test
  aspect_native_query_compile_fail`, focused `declaration_route_plan --lib`,
  focused `grouped --lib`, line-count checks, and focused scans.

## Query graph touch descriptor aspect lookup native fence

- Removed `ForgeQueryGraphTouchDescriptor::touches_aspect_path(&str)` so public callers cannot query graph touch evidence through raw dotted aspect path text.
- Added/used `touches_aspect(&ForgeQueryAspectTouch)` and changed graph obligation selector matching to pass retained typed touches directly into descriptor containment.
- Migrated descriptor identity and runtime graph-touch descriptor tests to assert aspect containment with typed touches.
- Added an aspect-native trybuild fixture proving public callers cannot call the removed raw-string graph touch descriptor lookup alias.
- Verification: `cargo fmt -p forge-query`, `cargo check -p forge-query`, `cargo check -p forge-query --tests`, normal `cargo test -p forge-query --test aspect_native_query_compile_fail`, focused `cargo test -p forge-query graph_touch_descriptor --lib`, focused `cargo test -p forge-query touch_descriptor --lib`, line-count checks, and focused alias scans passed.

## Query artifact bundle by-name lookup visibility fence

- Demoted derived materialization bundle/binding `materialization_by_name(...)` and live artifact bundle/binding `read_by_name(...)` to crate-private visibility.
- Public callers must now use typed derived handles/live views for artifact lookup, while internal projection-consumption extraction can still iterate over already-bound target names.
- Added an aspect-native trybuild fixture proving external callers cannot use raw view-name strings to access retained derived/live artifact contents from bundles or bindings.
- Verification: `cargo fmt -p forge-query`, `cargo check -p forge-query`, `cargo check -p forge-query --tests`, normal `cargo test -p forge-query --test aspect_native_query_compile_fail`, line-count checks, and focused visibility scans passed.

## Query retained upstream input raw-name lookup visibility fence

- Added public typed retained-upstream accessors `live_rows_for(&ForgeQueryLiveView<_>)` and `retained_computed_rows_for(&ForgeQueryDerivedViewHandle<_>)`.
- Demoted raw `live_rows(&str)` and `retained_computed_rows(&str)` accessors to crate-private visibility so external maintainers cannot recover upstream rows from arbitrary view-name text.
- Kept raw-name access internal for Query routing/tests that iterate already-declared upstream names.
- Added an aspect-native trybuild fixture proving public callers cannot use raw view-name strings to access retained live or computed upstream rows.
- Verification: `cargo fmt -p forge-query`, `cargo check -p forge-query`, `cargo check -p forge-query --tests`, normal `cargo test -p forge-query --test aspect_native_query_compile_fail`, focused `cargo test -p forge-query retained_upstream --lib`, focused `cargo test -p forge-query refresh_fallback_maintainer --lib`, and focused visibility scans passed.

## Query workspace live by-name lookup visibility fence

- Removed public `ForgeQueryWorkspace::read_live_by_name(...)` and migrated remaining tests to typed `read_live_result(&ForgeQueryLiveView<_>)`.
- Added public typed live helpers `state_live(&ForgeQueryLiveView<_>)`, `subscription_basis_digest(&ForgeQueryLiveView<_>)`, and `inspect_live(&ForgeQueryLiveView<_>)`.
- Demoted raw `state_live_by_name(...)`, `subscription_basis_digest_by_name(...)`, and `inspect_live_by_name(...)` to crate-private routing helpers.
- Added an aspect-native trybuild fixture proving external callers cannot use raw view-name strings for workspace live read, state, subscription digest, or inspection access.
- Verification: `cargo fmt -p forge-query`, `cargo check -p forge-query`, `cargo check -p forge-query --tests`, normal `cargo test -p forge-query --test aspect_native_query_compile_fail`, focused `cargo test -p forge-query read_obligation_dispatch --lib`, focused `cargo test -p forge-query in_memory_test_runtime_executes_public_insert_and_live_read --lib`, line-count checks, and focused visibility scans passed.

## Query retained scalar by-name extraction visibility fence

- Demoted `ForgeQueryDerivedArtifactBinding::consume_scalar_fields_by_name(...)` and `verify_scalar_alignment_by_name(...)` to crate-private visibility.
- Public retained scalar extraction/alignment now requires typed derived view handles through `consume_scalar_fields(...)` and `verify_scalar_alignment(...)`.
- Migrated retained scalar fact-set and alignment tests to construct typed derived handles instead of selecting materializations by raw view-name text.
- Added an aspect-native trybuild fixture proving external callers cannot use raw derived view names for retained scalar extraction or alignment verification.
- Verification: `cargo fmt -p forge-query`, `cargo check -p forge-query`, `cargo check -p forge-query --tests`, normal `cargo test -p forge-query --test aspect_native_query_compile_fail`, focused `cargo test -p forge-query retained_scalar_fact_set --lib`, focused `cargo test -p forge-query retained_scalar_alignment --lib`, line-count checks, and focused visibility scans passed.

## Query graph touch descriptor collection lookup native fence

- Added public `ForgeQueryGraphTouchDescriptor::touches_target_collection(&ForgeQueryMutationTargetCollectionIdentity)`.
- Demoted raw `touches_collection(&str)` to crate-private visibility so external callers cannot probe graph touch descriptor evidence with arbitrary collection text.
- Migrated descriptor identity and runtime graph-touch descriptor tests to use typed mutation target collection identities.
- Kept raw descriptor collection lookup only for internal graph obligation selector matching, where selector admission still owns terminal collection text.
- Added an aspect-native trybuild fixture proving public callers cannot call the removed raw-string descriptor collection lookup.
- Verification: `cargo fmt -p forge-query`, `cargo check -p forge-query`, `cargo check -p forge-query --tests`, normal `cargo test -p forge-query --test aspect_native_query_compile_fail`, focused `cargo test -p forge-query touch_descriptor --lib`, focused `cargo test -p forge-query graph_touch_descriptor --lib`, line-count checks, and focused visibility scans passed.

## Query mutation metadata key lookup native fence

- Added public `ForgeQueryMutationMetadataKey` and changed `ForgeQueryMutationMetadata::get(...)` to require `&ForgeQueryMutationMetadataKey` instead of raw `&str`.
- Kept metadata authoring ergonomic, but mutation metadata insertion now lowers through the same key admission token used by lookup.
- Migrated write receipt inspection, delete inspection, and refresh-maintainer metadata tests to construct typed metadata keys before lookup.
- Added an aspect-native trybuild fixture proving public callers cannot call mutation metadata lookup with a raw string.
- Verification: `cargo fmt -p forge-query`, `cargo check -p forge-query`, `cargo check -p forge-query --tests`, normal `cargo test -p forge-query --test aspect_native_query_compile_fail`, focused `cargo test -p forge-query write_receipt_inspection_retains_authored_mutation_metadata --lib`, focused `cargo test -p forge-query delete_with --lib`, focused `cargo test -p forge-query refresh_fallback_maintainer_receives_retained_mutation_metadata --lib`, line-count checks, and focused scans passed.

## Query mutation metadata entries raw-map fence

- Changed `ForgeQueryMutationMetadata::entries()` so it yields typed `ForgeQueryMutationMetadataKey` values with metadata values instead of exposing the raw `BTreeMap<String, ForgeQueryMutationMetadataValue>` storage map.
- Updated backend diagnostics, causal receipt helpers, and write-receipt digest construction to consume the typed iterator and project key text only at reporting/digest boundaries.
- Added an aspect-native trybuild fixture proving public callers cannot bind metadata entries to the old raw-map shape.
- Verification: `cargo fmt -p forge-query`, `cargo check -p forge-query`, `cargo check -p forge-query --tests`, `TRYBUILD=overwrite cargo test -p forge-query --test aspect_native_query_compile_fail`, normal `cargo test -p forge-query --test aspect_native_query_compile_fail`, focused `cargo test -p forge-query write_receipt_inspection_retains_authored_mutation_metadata --lib`, focused `cargo test -p forge-query delete_with --lib`, focused `cargo test -p forge-query refresh_fallback_maintainer_receives_retained_mutation_metadata --lib`, line-count checks, and focused raw-map scans passed.

## Query declarative projection field key native fence

- Changed `DeclarativeProjectionField` to retain an `AspectFieldKey` instead of separate aspect/field strings.
- Added public `source_field_key()` as the native route and made the old `aspect()` / `field()` string projections crate-local for internal lowering compatibility.
- Added an aspect-native trybuild fixture proving public facade callers cannot use the old projection-field string aliases.
- Verification: `cargo fmt -p forge-query`, `cargo check -p forge-query`, `cargo check -p forge-query --tests`, `TRYBUILD=overwrite cargo test -p forge-query --test aspect_native_query_compile_fail`, normal `cargo test -p forge-query --test aspect_native_query_compile_fail`, focused `cargo test -p forge-query declarative --lib`, focused `cargo test -p forge-query read_composition --lib`, and focused scans passed. Other declarative ordering/filter/writeback string aliases remain future work.

## Query declarative ordering field key native fence

- Changed `DeclarativeOrderingField` to retain an `AspectFieldKey` instead of separate aspect/field strings.
- Added public `source_field_key()` as the native route and made the old `aspect()` / `field()` string projections crate-local for internal lowering compatibility.
- Added an aspect-native trybuild fixture proving public facade callers cannot use the old ordering-field string aliases.
- Verification: `cargo fmt -p forge-query`, `cargo check -p forge-query`, `cargo check -p forge-query --tests`, `TRYBUILD=overwrite cargo test -p forge-query --test aspect_native_query_compile_fail`, normal `cargo test -p forge-query --test aspect_native_query_compile_fail`, focused `cargo test -p forge-query declarative --lib`, focused `cargo test -p forge-query read_composition --lib`, and focused scans passed. Other declarative filter/writeback string aliases remain future work.

## Query declarative predicate filter key native fence

- Changed `DeclarativeEqualityFilter`, `DeclarativeIntegerComparisonFilter`, `DeclarativeStringContainsFilter`, `DeclarativeSetMembershipFilter`, `DeclarativePresenceFilter`, and `DeclarativePredicateFilter` to expose predicate source fields through `AspectFieldKey`.
- Added public `source_field_key()` routes and made the old `aspect()` / `field()` string projections crate-local for internal canonical-query lowering compatibility.
- Added an aspect-native trybuild fixture proving public facade callers cannot use the old predicate-filter string aliases on concrete filters or the enum wrapper.
- Verification: `cargo fmt -p forge-query`, `cargo check -p forge-query`, `cargo check -p forge-query --tests`, `TRYBUILD=overwrite cargo test -p forge-query --test aspect_native_query_compile_fail`, normal `cargo test -p forge-query --test aspect_native_query_compile_fail`, focused `cargo test -p forge-query declarative --lib`, focused `cargo test -p forge-query read_composition --lib`, and focused scans passed. Remaining public `aspect()` / `field()` aliases in `declarative_live.rs` are branch-compare and writeback surfaces.

## Query declarative branch-compare field key native fence

- Changed `DeclarativeBranchCompareValue` and `DeclarativeBranchCompareFieldDelta` to retain `AspectFieldKey` instead of separate aspect/field strings.
- Branch comparison now joins left/right row values with typed field keys and constructs deltas without splitting dotted string keys.
- Added public `source_field_key()` routes; removed public branch-compare value string aliases and made branch-compare delta string projections crate-local for digest compatibility.
- Added an aspect-native trybuild fixture proving public facade callers cannot use the old branch-compare value/delta string aliases.
- Verification: `cargo fmt -p forge-query`, `cargo check -p forge-query`, `cargo check -p forge-query --tests`, `TRYBUILD=overwrite cargo test -p forge-query --test aspect_native_query_compile_fail`, normal `cargo test -p forge-query --test aspect_native_query_compile_fail`, focused `cargo test -p forge-query declarative --lib`, and focused scans passed. Remaining public `aspect()` / `field()` aliases in `declarative_live.rs` are the writeback surface.

## Query declarative writeback change field key native fence

- Changed `DeclarativeWritebackChange` to retain `AspectFieldKey` instead of separate aspect/field strings.
- Added public `source_field_key()` as the native route and made the old `aspect()` / `field()` string projections crate-local for digest compatibility.
- Added an aspect-native trybuild fixture proving public facade callers cannot use the old writeback-change string aliases.
- Verification: `cargo fmt -p forge-query`, `cargo check -p forge-query`, `cargo check -p forge-query --tests`, `TRYBUILD=overwrite cargo test -p forge-query --test aspect_native_query_compile_fail`, normal `cargo test -p forge-query --test aspect_native_query_compile_fail`, focused `cargo test -p forge-query writeback --lib`, focused `cargo test -p forge-query declarative --lib`, and focused scans passed. No public `aspect()` / `field()` aliases remain in `declarative_live.rs`.

## Query authoring selector field key native fence

- Changed public authoring selector access so `AspectFieldSelector`, `OrderingSelector`, `AuthoredResultShapeField`, and predicate selector carriers expose retained `AspectFieldKey` values through `source_field_key()` or `target_field_key()`.
- Demoted the old authoring `aspect()` / `field()` and result-shape `source_aspect()` / `source_field()` aliases to crate-private terminal projections for lowering and digest compatibility.
- Added an aspect-native trybuild fixture proving public facade callers cannot use the old authoring selector or authored result-shape string aliases.
- Verification: `cargo fmt -p forge-query`, `cargo check -p forge-query`, `cargo check -p forge-query --tests`, `TRYBUILD=overwrite cargo test -p forge-query --test aspect_native_query_compile_fail`, normal `cargo test -p forge-query --test aspect_native_query_compile_fail`, focused `cargo test -p forge-query authoring --lib`, focused `cargo test -p forge-query read_composition --lib`, line-count checks, and focused scans passed.

## Query bridge field delta key native fence

- Changed `BridgeFieldDelta` to retain a `QueryFieldKey` immediately after terminal bridge aspect/field ingress.
- Removed public `BridgeFieldDelta::aspect()` / `field()` aliases entirely; public consumers use `field_key()` and live patch extraction clones the retained key instead of rebuilding from raw strings.
- Added an aspect-native trybuild fixture proving public facade callers cannot use the removed bridge field delta string aliases.
- Verification: `cargo fmt -p forge-query`, `cargo check -p forge-query`, `cargo check -p forge-query --tests`, `TRYBUILD=overwrite cargo test -p forge-query --test aspect_native_query_compile_fail`, normal `cargo test -p forge-query --test aspect_native_query_compile_fail`, focused `cargo test -p forge-query live --lib`, focused `cargo test -p forge-query view_shape_live --lib`, line-count checks, and focused scans passed. `live/mod.rs` is already line-cap allowlisted.

## Query aspect touch terminal projection naming fence

- Removed the public neutral `ForgeQueryAspectTouch::aspect_path()` alias.
- Public callers now use `terminal_aspect_path_projection()` so dotted path text is visibly terminal/reporting-only while the touch retains native authoring target proof.
- Mechanically migrated runtime mutation, graph-touch descriptor, digest, preview, and test support call sites to the terminal projection name.
- Added an aspect-native trybuild fixture proving external facade callers cannot call the removed touch alias.
- Verification: `cargo fmt -p forge-query`, `cargo check -p forge-query`, `cargo check -p forge-query --tests`, `TRYBUILD=overwrite cargo test -p forge-query --test aspect_native_query_compile_fail`, normal `cargo test -p forge-query --test aspect_native_query_compile_fail`, focused `cargo test -p forge-query graph_touch_descriptor --lib`, focused `cargo test -p forge-query aspect_crud --lib`, line-count checks, and focused scans passed.

## Query graph touch selector native constructor naming fence

- Renamed `ForgeQueryGraphTouchSelector::aspect_path(...)` to `aspect_touch(...)` because the constructor consumes an admitted `ForgeQueryAspectTouch`, not raw dotted path authority.
- Kept the selector evidence kind string as `"aspect-path"` to avoid graph-obligation digest churn while making the Rust API boundary aspect-native.
- Migrated graph obligation support-matrix, lookup-selection, registration selector, and read-obligation hardening tests to the new constructor.
- Added an aspect-native trybuild fixture proving facade callers cannot use the removed selector constructor.
- Verification: `cargo fmt -p forge-query`, `cargo check -p forge-query`, `cargo check -p forge-query --tests`, `TRYBUILD=overwrite cargo test -p forge-query --test aspect_native_query_compile_fail`, normal `cargo test -p forge-query --test aspect_native_query_compile_fail`, focused `cargo test -p forge-query selector_matching --lib`, focused `cargo test -p forge-query graph_touch_descriptor --lib`, focused `cargo test -p forge-query lookup_selection --lib`, line-count checks, and focused scans passed.

## Query schema view typed lookup fence

- Changed `QuerySchemaView::field(...)`, `has_aspect(...)`, and `relation(...)` to require `AspectName`, `FieldName`, and `RelationName` instead of raw strings.
- Removed neutral `SchemaFieldView::aspect()` / `field()` and `SchemaRelationView::relation()` aliases; callers now use native `*_name()` carriers or explicit terminal projection helpers.
- Migrated validation, graph-read schema-reference admission, declarative traversal validation, typed-harness tests, and workspace declaration tests to preserve typed schema names into lookup.
- Added an aspect-native trybuild fixture proving facade callers cannot use raw schema lookups or removed schema field/relation neutral aliases.
- Verification: `cargo fmt -p forge-query`, `cargo check -p forge-query`, `cargo check -p forge-query --tests`, `TRYBUILD=overwrite cargo test -p forge-query --test aspect_native_query_compile_fail`, normal `cargo test -p forge-query --test aspect_native_query_compile_fail`, focused `cargo test -p forge-query validation --lib`, focused `cargo test -p forge-query schema_view --lib`, line-count checks, and focused scans passed.

## Query traversal selector relation naming fence

- Removed public neutral `TraversalSelector::relation()` alias.
- Callers now use `relation_name()` when retaining the `RelationName` proof carrier and `terminal_relation_projection()` only when intentionally emitting reporting, digest, materialization, or schema-reference boundary text.
- Migrated canonicalization, composition expansion/template instantiation, declarative-live lowering, read-composition materialization, schema-reference admission, and traversal workspace/test support call sites.
- Added an aspect-native trybuild fixture proving facade callers cannot call the removed traversal relation alias.
- Verification: `cargo fmt -p forge-query`, `cargo check -p forge-query`, `cargo check -p forge-query --tests`, `TRYBUILD=overwrite cargo test -p forge-query --test aspect_native_query_compile_fail`, normal `cargo test -p forge-query --test aspect_native_query_compile_fail`, focused `cargo test -p forge-query traversal --lib`, focused `cargo test -p forge-query read_composition --lib`, and focused scans passed.

## Query validated traversal relation proof reuse

- Removed public neutral `ValidatedTraversalEntry::relation()` alias.
- Validation output now offers `relation_name()` for retained `RelationName` proof and `terminal_relation_projection()` for collection/reporting text.
- Runtime read relationship-proof descriptor admission now consumes the validated `RelationName` directly instead of reparsing terminal relation text.
- Collection planning edge classification now uses the explicit terminal projection accessor.
- Extended the validated-entry aspect-native trybuild fixture to prove facade callers cannot recover neutral traversal relation text from validated entries.
- Verification: `cargo fmt -p forge-query`, `cargo check -p forge-query`, `cargo check -p forge-query --tests`, `TRYBUILD=overwrite cargo test -p forge-query --test aspect_native_query_compile_fail`, normal `cargo test -p forge-query --test aspect_native_query_compile_fail`, focused `cargo test -p forge-query validation --lib`, focused `cargo test -p forge-query read_composition --lib`, line-count checks, and focused scans passed.

## Query graph-read domain operation relation proof

- Changed `ForgeQueryAdmittedGraphReadDomainOperationReference` to store a `RelationName` proof carrier instead of raw relation-name `String` text.
- Changed `ForgeQueryGraphReadOperationRegistration` and admitted domain registered operations to retain `Vec<RelationName>` accepted relation sets.
- Public graph-read domain operation APIs now return `&RelationName` or `&[RelationName]`; terminal projection is explicit through `terminal_relation_projection()` or local `RelationName::as_str()` at digest/denial/comparison boundaries.
- Migrated declaration registration, registry matching, operation mapping, and the phase-three operation-resolution integration test to native accepted relation carriers.
- Added an aspect-native trybuild fixture proving facade callers cannot bind admitted graph-read domain operation relation authority as `&str` or `&[String]`.
- Verification: `cargo fmt -p forge-query`, `cargo check -p forge-query`, `cargo check -p forge-query --tests`, `TRYBUILD=overwrite cargo test -p forge-query --test aspect_native_query_compile_fail`, normal `cargo test -p forge-query --test aspect_native_query_compile_fail`, focused `cargo test -p forge-query --test graph_read_access_phase_three_operation_resolution`, line-count checks, and focused scans passed.

## Query graph-read admitted relation row proof

- Changed `ForgeQueryAdmittedGraphReadRelation` to store a `RelationName` proof carrier instead of raw relation-name `String` text.
- Schema-reference admission now preserves the typed traversal relation into admitted graph-read relation rows instead of projecting terminal text back into authority.
- Operation mapping compares admitted domain-operation relation names and admitted schema-reference relation names as native `RelationName` carriers.
- Access requirement derivation and graph-read phase-one tests now use `terminal_relation_projection()` only where row/authority/assertion text is intentionally emitted.
- Added an aspect-native trybuild fixture proving facade callers cannot use the removed `ForgeQueryAdmittedGraphReadRelation::relation()` string alias.
- Verification: `cargo fmt -p forge-query`, `cargo check -p forge-query --tests`, `TRYBUILD=overwrite cargo test -p forge-query --test aspect_native_query_compile_fail`, normal `cargo test -p forge-query --test aspect_native_query_compile_fail`, focused `cargo test -p forge-query --test graph_read_access_phase_one`, line-count checks, and focused scans passed.

## Query aspect value constructor and JSON-number validation fence

- Kept the refactor scoped after the user clarified this is not broad cleanup: expose JSON authority, replace it with aspect/native carriers, and move on.
- Made `ForgeQueryAspectValue::new_set` and `new_clear` crate-local and `new` test-only so public callers cannot directly mint parsed desired-aspect carriers from weaker pieces.
- Added an aspect-native compile-fail fixture proving external callers cannot call the direct aspect-value constructor; updated related alias-removal fixtures to construct through the public builder.
- Used a temporary `serde_json` dependency-demotion red probe with `cargo check -p forge-query` to expose production JSON dependencies mechanically instead of trusting text search alone.
- Replaced production `serde_json::Number` validation in program values and intent inputs with local JSON-number text validation; restored `serde_json` as a normal dependency because the remaining probe failures were consumer-kit JSON document contracts, not aspect authority.
- Verification: `cargo fmt -p forge-query`, `cargo check -p forge-query`, `cargo check -p forge-query --tests`, `TRYBUILD=overwrite cargo test -p forge-query --test aspect_native_query_compile_fail`, and normal `cargo test -p forge-query --test aspect_native_query_compile_fail` passed.

## Query retained materialized row constructor boundary

- Red-probed `ForgeQueryRetainedMaterializedRow::from_scalar_values` by making it crate-local; production `cargo check -p forge-query` stayed green, while `cargo check -p forge-query --tests` exposed the real cross-crate public-bridge hostile certification support consumer.
- Added `ForgeQueryRetainedMaterializedRowBuilder` as the explicit native maintainer/test authoring boundary over `ForgeQueryRetainedFieldPath` and foundational `AspectValue`, and exported it through runtime/facade.
- Migrated the public-bridge hostile certification support helper from direct retained-row proof construction to the builder.
- Added an aspect-native compile-fail fixture proving external callers cannot call the direct retained materialized row constructor; refreshed the retained-row terminal JSON ingress stderr.
- Verification: `cargo fmt -p forge-query`, `cargo check -p forge-query`, `cargo check -p forge-query --tests`, `TRYBUILD=overwrite cargo test -p forge-query --test aspect_native_query_compile_fail`, and normal `cargo test -p forge-query --test aspect_native_query_compile_fail` passed.

## Query projection consumption field request authority

- Removed neutral projection-consumption field request helpers: `ProjectMaterializedFacts::display_field`, `ProjectMaterializedFacts::derived_scalar_field`, and `ProjectionFactFieldPath::from_authoring_path`.
- Added typed/native request routes `display_field_path`, `derived_scalar_field_path`, and `ProjectionFactFieldPath::from_canonical_field_path`, plus explicit terminal ingress routes `terminal_display_field`, `terminal_derived_scalar_field`, and `ProjectionFactFieldPath::from_terminal_projection`.
- Deliberate red probes worked as intended: production `cargo check -p forge-query` first exposed seven certification/report fixture callers; after migration, `cargo check -p forge-query --tests` exposed the wider test/support/public-bridge consumers, which were mechanically migrated to the terminal names.
- Added an aspect-native compile-fail fixture proving facade callers cannot call the removed neutral request helpers or private neutral field-path constructor; updated the older projection-consumption field-key fixture to use explicit terminal construction before asserting removed aliases.
- Verification: `cargo fmt -p forge-query`, `cargo check -p forge-query`, `cargo check -p forge-query --tests`, `TRYBUILD=overwrite cargo test -p forge-query --test aspect_native_query_compile_fail`, normal `cargo test -p forge-query --test aspect_native_query_compile_fail`, focused `cargo test -p forge-query projection_consumption --lib`, and line-count checks passed.

## Query view-shape focused inspector constructor authority

- Changed focused-inspector neutral constructors to require native `AspectKey`: `ViewShapeDescriptor::inspector_detail_focused`, `ViewShapeDescriptor::identity_aware_inspector_detail_focused`, `DeclarativeLiveViewShape::inspector_focused`, and `DeclarativeLiveViewShape::identity_aware_inspector_focused`.
- Added explicit terminal string-ingress helpers for existing authoring/tests: `terminal_inspector_detail_focused`, `terminal_identity_aware_inspector_detail_focused`, `terminal_inspector_focused`, and `terminal_identity_aware_inspector_focused`.
- Red probes were useful: production `cargo check -p forge-query` exposed declarative live lowering and live subscription relevance matching; `cargo check -p forge-query --tests` exposed saved-query/view-shape/view-shape-live/certification fixture callers.
- Declarative live now retains the focused aspect as `AspectKey` and passes it directly into `ViewShapeDescriptor`; the live subscription relevance boundary projects with `focused_aspect.as_str()` only where it compares against terminal changed-aspect strings.
- Added an aspect-native compile-fail fixture proving facade callers cannot pass raw strings to the neutral focused-inspector constructors.
- Verification: `cargo fmt -p forge-query`, `cargo check -p forge-query`, `cargo check -p forge-query --tests`, `TRYBUILD=overwrite cargo test -p forge-query --test aspect_native_query_compile_fail`, normal `cargo test -p forge-query --test aspect_native_query_compile_fail`, focused `cargo test -p forge-query view_shape --lib`, line-count checks, and focused scans passed.

## Query placeholder masking request constructor fence

- Removed `PolicyPlaceholderMaskingRequest::new(...)` and replaced it with `terminal_requested_placeholder_fields(...)`.
- This is an adjacent terminal-boundary enforcement slice, not a core native proof-carrier conversion: placeholder masking still accepts terminal delivered-field text, but the API now makes that terminal nature explicit at construction and projection.
- Production `cargo check -p forge-query` stayed green after removing `new`; `cargo check -p forge-query --tests` exposed the three real test/certification callers, which were migrated to the terminal constructor.
- Added an aspect-native compile-fail fixture proving facade callers cannot use the removed neutral request constructor.
- Verification: `cargo fmt -p forge-query`, `cargo check -p forge-query`, `cargo check -p forge-query --tests`, `TRYBUILD=overwrite cargo test -p forge-query --test aspect_native_query_compile_fail`, normal `cargo test -p forge-query --test aspect_native_query_compile_fail`, focused `cargo test -p forge-query policy_delivery --lib`, line-count checks, and focused scans passed.

## Query computed patch observation handle fence

- Changed `ForgeQueryRuntime::drain_derived_patches(...)` and `ForgeQueryWorkspace::observe_computed(...)` to require `&ForgeQueryDerivedViewHandle<_>` instead of raw derived view-name strings.
- Kept a crate-local `drain_derived_patches_by_name(...)` helper for internal runtime routing that already owns declared view names.
- `cargo check -p forge-query` stayed green after the root break; `cargo check -p forge-query --tests` exposed 20 runtime/workspace computed patch consumers still passing literals or `handle.name()` strings.
- Migrated computed, preview, and workspace support tests to preserve/pass derived handles; the declaration-only computed test now creates a runtime-scoped handle after declaration.
- Added an aspect-native compile-fail fixture proving external callers cannot drain or observe computed patches by raw view-name text.
- Verification: `cargo fmt -p forge-query`, `cargo check -p forge-query`, `cargo check -p forge-query --tests`, `TRYBUILD=overwrite cargo test -p forge-query --test aspect_native_query_compile_fail`, normal `cargo test -p forge-query --test aspect_native_query_compile_fail`, focused `cargo test -p forge-query computed --lib`, line-count checks, and focused scans passed.

## Query artifact target raw-name constructor fence

- Demoted `ForgeQueryDerivedMaterializationTarget::new(...)` and `ForgeQueryLiveArtifactTarget::new(...)` so public callers cannot mint artifact targets from arbitrary view-name strings.
- Public construction now flows from typed derived handles and live views; artificial retained-live fixtures use explicit `cfg(test)` `test_only(...)` constructors.
- This is adjacent Law 41 enforcement, not a substitute for deeper aspect-value work.
- Verification: `cargo check -p forge-query`, `cargo check -p forge-query --tests`, normal `cargo test -p forge-query --test aspect_native_query_compile_fail`, focused `cargo test -p forge-query retained_live --lib`, line-count checks, and focused scans passed.

## Query aspect value and probe field JSON assertion removal

- Removed test-visible `ForgeQueryAspectValue::terminal_json_projection(...)` / `terminal_json_projection_string(...)` and the underlying desired-aspect JSON projection helper.
- Removed `ForgeQueryExistingTruthProbeField::terminal_json_projection_string(...)`.
- Migrated stateful bridge and existing-truth test adapters so external JSON rows are admitted back into `AspectValue` before assertion comparison; tests now assert probe values through native `foundational_value()`.
- Verification: `cargo fmt -p forge-query`, `cargo check -p forge-query`, `cargo check -p forge-query --tests`, normal `cargo test -p forge-query --test aspect_native_query_compile_fail`, focused `cargo test -p forge-query probe_existing --lib`, focused `cargo test -p forge-query bridge_backed_verification_execution --lib`, focused `cargo test -p forge-query verified_existing --lib`, focused `cargo test -p forge-query mixed_authority --lib`, line-count checks, and focused scans passed.

## Query program output JSON assertion removal

- Removed crate-test-only `ForgeQueryProgramValue::terminal_json_projection(...)` and operation input/output terminal JSON value projection helpers.
- Added native read-only program value inspection via `array_len()`, `field_path_value(&CanonicalFieldPath)`, `field_path_string_value(&CanonicalFieldPath)`, and indexed row field-path lookup so runtime tests can inspect program outputs without whole-tree JSON projection.
- Red-probed with `cargo check -p forge-query --tests`, which exposed only the program and preview operation assertions; migrated them to foundational field-path and array-length assertions.
- Verification: `cargo fmt -p forge-query`, `cargo check -p forge-query`, `cargo check -p forge-query --tests`, normal aspect-native compile-fail suite, focused program and preview operation tests, line-count allowlist confirmation, and focused scans passed.

## Query retained row JSON decode helper removal

- Removed the test-only retained-row JSON decoder module and crate-test JSON projection helpers for retained materialized rows, retained scalar facts, derived materialization results, derived patch payloads, and runtime/workspace terminal derived materialization shortcuts.
- Added native fail-closed retained-row accessors on derived materialization results and retained upstream inputs so tests keep cardinality checks without deserializing retained proof carriers through JSON.
- Migrated retained-upstream, derived materialization intent, materialization bundle, retained artifact binding, and post-write artifact tests to inspect `ForgeQueryRetainedMaterializedRow` + `ForgeQueryRetainedFieldPath` + `AspectValue` directly.
- Verification: fmt/checks, aspect-native compile-fail suite, and focused retained-row/materialization/artifact tests passed.

## Query effect and entity row JSON assertion removal

- Removed crate-test-only effect delivery/payload terminal JSON projection helpers and deleted the dead string-array JSON helper.
- Removed crate-test-only entity row terminal JSON projection helpers and deleted the test-only aspect-field/memory-workspace external JSON projection modules.
- Added native runtime test helpers for external scalar/string lookup and field-family detection over `CanonicalFieldPath` + `AspectValue`.
- Migrated graph-composition, preview, symbolic-reference, aspect CRUD, and basis-context assertions away from entity JSON rows; the basis-context red probe caught and fixed a false `read.value` path assumption.
- Verification: `cargo fmt -p forge-query`, `cargo check -p forge-query`, `cargo check -p forge-query --tests`, aspect-native compile-fail suite, and focused aspect CRUD, graph-composition, preview, basis-context, and symbolic-reference tests passed.

## Query runtime insert command test-helper JSON ingress removal

- Deleted the unused test-only `aspect_field_authoring::external_json_ingress` module.
- Changed runtime test `insert_command(...)` so test-authored inserts provide native `AspectValue` values instead of arbitrary serializable values lowered through `serde_json::to_value`.
- The red probe produced 74 `cargo check -p forge-query --tests` errors across computed/effect/live/preview/stop-class/assembly/intent/session-label tests; migrated those `insert_command(...)` values from `json!("...")` to `test_string_aspect_value(...)`.
- Split effect suppression tests out of the touched over-cap `runtime/tests/effect/delivery.rs` file so all touched non-allowlisted Rust files remain under 400 lines.
- Verification: `cargo check -p forge-query`, `cargo check -p forge-query --tests`, aspect-native compile-fail suite, and focused computed, effect, effect delivery, and preview promotion tests passed.

## Query bridge test-support external row native storage

- Converted internal stateful bridge runtime support rows from nested `serde_json::Value` objects to native `BTreeMap<CanonicalFieldPath, AspectValue>` rows.
- Existing-truth assertion/probe verification, live materialization, and grouped-baseline lookup now read native bridge support rows directly instead of walking JSON and re-admitting scalar leaves.
- Converted public bridge runtime support rows and seeded existing-truth state to native `AspectValue` storage; `seed_backend_authoritative_truth(...)` now requires `AspectValue`.
- The public red probe exposed six remaining `json!(...)` authoritative-truth seeds in public bridge/bootstrap integration tests; migrated them to native `text(...)` helpers.
- Verification: `cargo fmt -p forge-query`, `cargo check -p forge-query`, `cargo check -p forge-query --tests`, aspect-native compile-fail suite, focused bridge-backed verification/probe/live tests, and focused public bridge integration tests passed.

## Query existing-truth verification adapter native seed storage

- Converted `runtime/tests/support/adapters/existing_truth_verification.rs` from JSON-backed authoritative test truth to native `AspectValue` storage.
- Removed the local JSON-to-AspectValue converter and JSON digest helper; assertion/probe paths now compare and clone native aspect values directly.
- The red `cargo check -p forge-query --tests` probe exposed 16 `.with_value(..., json!(...))` authority seeds in bridge-backed verification and graph-composition existing-target tests; migrated them to `test_string_aspect_value(...)`.
- Verification: `cargo fmt -p forge-query`, `cargo check -p forge-query`, `cargo check -p forge-query --tests`, aspect-native compile-fail suite, focused bridge-backed verification and graph-composition tests, broad filtered `cargo test -p forge-query graph_composition --lib`, line-count checks, and focused scans passed.

## Query computed retained-row JSON assertion helper removal

- Removed computed test helpers `read_derived_json(...)`, `retained_value_json_rows(...)`, and `test_retained_scalar_json(...)`.
- Computed retained materialization and derived patch payload assertions now compare native `AspectValue` values through `read_derived_value_aspects(...)` and `retained_value_aspects(...)`.
- The red `cargo check -p forge-query --tests` probe exposed 13 stale computed assertion sites; migrated them to `test_string_aspect_value(...)` expectations.
- Removed the shared runtime test prelude re-export of `serde_json::Value` and updated a disabled computed metadata example so it no longer teaches `Value::String` patch payloads.
- Verification: `cargo fmt -p forge-query`, `cargo check -p forge-query`, `cargo check -p forge-query --tests`, aspect-native compile-fail suite, focused `cargo test -p forge-query computed --lib`, line-count allowlist confirmation, and focused scans passed.

## Query runtime support-profile JSON receipt summary removal

- Replaced `runtime/tests/assembly/support_profile/facade_phase_nine.rs::mutation_receipt_summary(...) -> serde_json::Value` with typed `MutationReceiptSummary` and `MutationDeltaSummary` structs.
- Submission-lane parity now compares native delta `ForgeQueryAspectTouch` vectors instead of terminal aspect-path arrays in a JSON document.
- Removed the shared runtime test prelude re-export of `serde_json::json`; runtime tests no longer receive `Value` or `json!` by default.
- Verification: `cargo fmt -p forge-query`, `cargo check -p forge-query`, `cargo check -p forge-query --tests`, aspect-native compile-fail suite, focused `cargo test -p forge-query phase_nine --lib`, line-count checks, and focused scans passed.

## Query consumer-kit terminal document JSON boundary naming

- Kept support snapshot/pinning JSON mutation tests as external serialized-document boundary tests, not aspect authority cleanup targets.
- Added explicit terminal document aliases/helpers: `TerminalSupportSnapshotDocumentJson`, `TerminalSupportPinContractDocumentJson`, `terminal_support_snapshot_json_*`, `terminal_support_pin_contract_document_json(...)`, and `terminal_support_pin_contract_json(...)`.
- Moved raw `serde_json::Value::String` / `Number` construction behind terminal document helper functions so the tests no longer teach JSON as ordinary assertion vocabulary.
- Verification: `cargo fmt -p forge-query`, `cargo check -p forge-query`, `cargo check -p forge-query --tests`, aspect-native compile-fail suite, focused `support_snapshot` and `support_pinning` tests, line-count checks, and focused scans passed.

## Query mutation delta terminal aspect-path projection removal

- Removed public `ForgeQueryMutationDelta::terminal_aspect_paths_projection()` so executed mutation delta evidence cannot be turned back into dotted path strings by facade callers.
- The red `cargo check -p forge-query --tests` probe exposed real consumers across live subscription relevance, write receipt digesting, consumer-kit equivalence reporting, memory workspace tests, runtime program/computed maintainers, mutation/live receipt assertions, and aspect API finalization certification rows.
- Terminal/reporting boundaries now use local projection helpers over `&[ForgeQueryAspectTouch]`; test maintainers and assertions propagate or compare native touches directly through `touched_aspects()` and `test_aspect_touches(...)`.
- Added an aspect-native compile-fail fixture proving facade callers cannot call the removed terminal delta projection.
- Split touched oversized live/aspect CRUD tests into sibling modules so non-allowlisted touched Rust files stay under the 400-line workspace cap.
- Verification: `cargo fmt -p forge-query`, `cargo check -p forge-query`, `cargo check -p forge-query --tests`, aspect-native compile-fail suite with and without `TRYBUILD=overwrite`, focused memory workspace/live/aspect CRUD/delete/verified existing/computed tests, moved-test filters, line-count checks, and focused scans passed.

## Query derived patch terminal aspect-path projection removal

- Removed public `ForgeQueryDerivedPatch::terminal_aspect_paths_projection()` so computed patch evidence cannot be turned back into dotted path strings by facade callers.
- Production `cargo check -p forge-query` stayed green after the break; `cargo check -p forge-query --tests` exposed five computed assertions still using the terminal projection.
- Migrated those assertions to compare `patch.aspect_touches()` with `test_aspect_touches(...)`, and kept computed inspection digest terminal projection local to `runtime/computed/surface.rs`.
- Added an aspect-native compile-fail fixture proving facade callers cannot call the removed derived-patch terminal aspect-path projection; the existing derived-patch terminal JSON stderr refreshed because the compiler no longer suggests that removed method.
- Verification: `cargo fmt -p forge-query`, `cargo check -p forge-query`, `cargo check -p forge-query --tests`, aspect-native compile-fail suite with and without `TRYBUILD=overwrite`, focused `cargo test -p forge-query computed --lib`, line-count allowlist confirmation, and focused scans passed.

## Query effect delivery terminal aspect-path projection removal

- Removed public `ForgeQueryEffectDelivery::terminal_aspect_paths_projection()` so effect delivery evidence cannot be turned back into dotted path strings by facade callers.
- The production red probe exposed effect-triggered intent handoff digesting as the remaining production consumer; it now uses a local terminal projection helper over `pending_delivery.aspect_touches()`.
- Effect inspection identity construction also projects terminal text locally, and effect delivery assertions compare native `aspect_touches()` with `test_aspect_touches(...)`.
- Added an aspect-native compile-fail fixture proving facade callers cannot call the removed effect delivery terminal aspect-path projection; the existing effect terminal JSON stderr refreshed because the compiler no longer suggests that removed method.
- Verification: `cargo fmt -p forge-query`, `cargo check -p forge-query`, `cargo check -p forge-query --tests`, aspect-native compile-fail suite with and without `TRYBUILD=overwrite`, focused `cargo test -p forge-query effect_delivery --lib`, line-count checks, and focused scans passed.

## Query preview execution terminal aspect-path projection removal

- Removed public `ForgeQueryPreviewExecutionEvidence::terminal_aspect_paths_projection()` so preview execution evidence cannot be turned back into dotted path strings by facade callers.
- Production `cargo check -p forge-query` stayed green after the break; `cargo check -p forge-query --tests` exposed three intent/preview execution assertions still comparing terminal path arrays.
- Migrated those assertions to compare `evidence.aspect_touches()` with `test_aspect_touches(...)`, keeping preview and intent evidence assertions on native `ForgeQueryAspectTouch` proof carriers.
- Added an aspect-native compile-fail fixture proving facade callers cannot call the removed preview execution terminal aspect-path projection.
- Verification: `cargo fmt -p forge-query`, `cargo check -p forge-query`, `cargo check -p forge-query --tests`, normal aspect-native compile-fail suite, focused preview execution binding and composed runtime surface intent tests, line-count checks, and focused scans passed.

## Query existing-truth probe request terminal aspect-path projection removal

- Removed public `ForgeQueryExistingTruthProbeRequest::terminal_aspect_paths_projection()` so probe requests cannot be turned back into dotted path strings by facade callers.
- The production red probe exposed one certification fixture consumer in the workspace legacy parity lane; it now uses a local terminal projection helper over `workspace_legacy_request.aspect_touches()`.
- Existing-truth probe routing, backend execution, and adapters continue to consume native `ForgeQueryAspectTouch` request evidence through `aspect_touches()`.
- Added an aspect-native compile-fail fixture proving facade callers cannot call the removed probe request terminal aspect-path projection.
- Verification: `cargo fmt -p forge-query`, `cargo check -p forge-query`, `cargo check -p forge-query --tests`, aspect-native compile-fail suite with and without `TRYBUILD=overwrite`, focused `cargo test -p forge-query routing --lib`, line-count checks, and focused scans passed.

## Query live patch terminal aspect-path projection removal

- Removed public `ForgeQueryLivePatch::terminal_aspect_paths_projection()` so live patch evidence cannot be turned back into dotted path strings by facade callers.
- Production and test compilation stayed green after the break, confirming live routing/reporting already uses native touches or local terminal projection helpers elsewhere.
- Added an aspect-native compile-fail fixture proving facade callers cannot call the removed live patch terminal aspect-path projection.
- Verification: `cargo fmt -p forge-query`, `cargo check -p forge-query`, `cargo check -p forge-query --tests`, aspect-native compile-fail suite with and without `TRYBUILD=overwrite`, focused `cargo test -p forge-query live --lib`, line-count checks, and focused scans passed.

## Query graph read touch shape terminal projection deletion

- Deleted unused `ForgeQueryGraphReadTouchShape::terminal_aspect_paths_projection()`.
- Graph touch descriptor read-row derivation already consumes native `ForgeQueryAspectTouch` slices through `aspect_touches()`.
- Follow-up scan now shows no public or crate-public `terminal_aspect_paths_projection(...)` methods under `crates/forge-query/src`; only `pub(super)` local terminal helpers remain in effect/preview boundary modules.
- Verification: `cargo check -p forge-query`, `cargo check -p forge-query --tests`, line-count checks, and focused scans passed.

## Query mutation evidence touched/asserted terminal projection removal

- Removed public touched/asserted terminal aspect-path projection helpers from write commands, batch receipts, retained refresh contexts, graph touch descriptor rows, unified batch/component inspections, and verified assumption sets.
- The production red probe exposed batch write digest construction; digest/reporting boundaries now project terminal text locally from native `ForgeQueryAspectTouch` slices.
- The test red probe exposed computed retained refresh, batch mutation, bridge-backed verification, graph composition, and aspect API finalization certification consumers; migrated assertions to native touch/assertion slices where possible.
- Added seven aspect-native compile-fail fixtures proving facade callers cannot call the removed projection helpers.
- Verification: `cargo fmt -p forge-query`, `cargo check -p forge-query`, `cargo check -p forge-query --tests`, aspect-native compile-fail suite, focused batch/preview-batch/graph-composition/bridge-backed verification/computed refresh tests, line-count allowlist checks, and focused public-method scans passed.

## Query computed/effect/program terminal aspect-list projection removal

- Removed public terminal dependency/produced aspect projections from computed inspection evidence and derived-view declarations.
- Removed public terminal input/output/changed aspect projections from effect payloads/expressions/triggers, and removed the dead graph descriptor row terminal declared-operation projection.
- The production red probe exposed effect inspection identity construction; it now projects terminal digest material locally from native trigger touches.
- The test red probe exposed five computed/intent assertions; migrated them to native `ForgeQueryAspectTouch` comparisons with `test_aspect_touches(...)`.
- Added eleven aspect-native compile-fail fixtures for the removed computed, derived-view, effect, and graph descriptor projection helpers.
- Verification: fmt, `cargo check -p forge-query`, `cargo check -p forge-query --tests`, trybuild with and without `TRYBUILD=overwrite`, focused computed inspection tests, focused composed intent/effect tests, line-count checks, and focused scans passed.

## Query effect inspection terminal aspect-list projection removal

- Removed public terminal trigger/condition input/condition output aspect projections from `ForgeQueryEffectInspectionEvidence`.
- The production red probe exposed preview effect relevance routing; it now compares against native trigger touches and localizes terminal string comparison to the preview routing map boundary.
- The test red probe exposed two effect delivery assertions; migrated them to native `ForgeQueryAspectTouch` comparisons.
- Added three aspect-native compile-fail fixtures for the removed effect inspection terminal projection helpers.
- Verification: fmt, `cargo check -p forge-query`, `cargo check -p forge-query --tests`, trybuild with and without `TRYBUILD=overwrite`, focused effect delivery and preview execution tests, line-count checks, and focused scans passed.

## Query view-shape grouped/focused terminal aspect projection removal

- Removed public terminal focus/grouping/binding aspect projection helpers from view-shape descriptors, delivery metadata, grouped planning artifacts, grouped binding proofs, grouped live state artifacts, grouped execution lane values, and focused inspector patch artifacts.
- Production red exposed admission digest construction, plan digest construction, and grouped execution mismatch diagnostics; those now call `.as_str()` locally on native `AspectKey` values only at digest/error boundaries.
- Test red exposed grouped view-shape assertions, grouped live baseline assertions, and milestone-eight grouped certification fixture selection; migrated assertions to native `AspectKey` comparisons and kept terminal text only for legacy fixture field selection.
- Added six aspect-native compile-fail fixtures for the removed view-shape/grouped/focused projection helpers.
- Verification: fmt, `cargo check -p forge-query`, `cargo check -p forge-query --tests`, trybuild with and without `TRYBUILD=overwrite`, focused grouped admission/baseline/delta tests, line-count allowlist checks, and focused scans passed.

## Query graph-read native row terminal key projection removal

- Removed public `terminal_aspect_key_projection()` and `terminal_field_key_projection()` helpers from graph-read selectivity rows, field authorities, admitted boolean predicate leaves, and admitted projection/predicate/ordering reference rows.
- Production `cargo check -p forge-query` stayed green; `cargo check -p forge-query --tests` exposed eight phase-four authority assertions still comparing terminal strings.
- Migrated those assertions to native `AspectKey` and `FieldKey` comparisons, and added four aspect-native compile-fail fixtures for the removed graph-read terminal key helpers.
- Verification: fmt, `cargo check -p forge-query`, `cargo check -p forge-query --tests`, trybuild with and without `TRYBUILD=overwrite`, focused graph-read phase-four authority/support tests, line-count checks, and focused scans passed.

## Query retained scalar field terminal projection removal

- Removed public terminal field-key projection helpers from retained scalar field facts and retained scalar alignment facts.
- Digest builders now derive terminal field text locally from native `ForgeQueryRetainedFieldPath` carriers only at the digest boundary.
- Production and test checks stayed green; added two aspect-native compile-fail fixtures for the removed retained scalar terminal field-key helpers.
- Verification: fmt, `cargo check -p forge-query`, `cargo check -p forge-query --tests`, trybuild with and without `TRYBUILD=overwrite`, focused retained scalar fact/alignment tests, line-count checks, and focused scans passed.

## Query symbolic graph resolution terminal projection removal

- Removed public `terminal_aspect_path_projection()` helpers from symbolic aspect resolution evidence and graph-composition resolution entries.
- Production red exposed batch-write inspection, graph-composition evidence, and mutation-evidence digest builders; they now project terminal path text locally from native `aspect_touch()` only at evidence identity boundaries.
- Test red exposed graph-composition assertions still comparing terminal path strings; migrated those assertions to native `ForgeQueryAspectTouch` comparisons.
- Added two aspect-native compile-fail fixtures for the removed symbolic/graph-composition terminal path helpers.
- Verification: fmt, `cargo check -p forge-query`, `cargo check -p forge-query --tests`, trybuild with and without `TRYBUILD=overwrite`, broad filtered graph-composition tests, line-count checks, and focused scans passed.

## Query collection/live key terminal projection removal

- Removed public terminal aspect/field projection helpers from `OrderingKeyPath` and `QueryFieldKey`.
- Collection ordering digesting, detail live patch digesting, and view-shape live focus rejection reporting now project terminal text locally from native `AspectKey`/`FieldKey` values only at digest/error boundaries.
- Production and test checks stayed green; added two aspect-native compile-fail fixtures for the removed collection/live terminal key helpers.
- Verification: fmt, `cargo check -p forge-query`, `cargo check -p forge-query --tests`, trybuild with and without `TRYBUILD=overwrite`, focused detail live and ordered collection live tests, line-count checks, and focused scans passed.

## Query aspect mutation operation terminal projection removal

- Removed public `ForgeQueryAspectMutationOperation::terminal_aspect_path_projection()`.
- Production red exposed intent-admission seed digesting, mutation-authority digesting, write/batch inspection digesting, graph-obligation lookup keys, graph-touch selector identities, and descriptor inventory reporting; they now project terminal text locally through `operation.aspect_touch()`.
- Test red exposed mutation aspect CRUD, batch, and delete assertions; migrated those assertion-formatting sites through the native operation touch.
- Added an aspect-native compile-fail fixture proving facade callers cannot call the removed mutation-operation terminal projection helper.
- Verification: fmt, `cargo check -p forge-query`, `cargo check -p forge-query --tests`, trybuild with and without `TRYBUILD=overwrite`, focused mutation aspect CRUD/batch/delete tests, line-count checks, and focused scans passed.

## Query aspect value terminal projection removal

- Removed public `ForgeQueryAspectValue::terminal_aspect_path_projection()`.
- Production red exposed memory-workspace field patch validation, aspect mutation builder duplicate diagnostics, existing-truth assertion evidence, and mutation lowering digest rows; they now project terminal text locally through `aspect.aspect_touch()`.
- Test red exposed internal/public bridge existing-truth verification adapters and bridge external-row support; those adapters now derive terminal row keys from each aspect value's native touch.
- Removed the now-dead parsed desired-aspect authoring-path helper and added an aspect-native compile-fail fixture for the removed aspect-value terminal projection helper.
- Verification: fmt, `cargo check -p forge-query`, `cargo check -p forge-query --tests`, trybuild with and without `TRYBUILD=overwrite`, focused existing-truth assertion and bridge-backed verification tests, public bridge compile sweep, line-count checks, and focused scans passed.

## Query symbolic aspect reference terminal projection removal

- Removed public `ForgeQuerySymbolicAspectReference::terminal_aspect_path_projection()`.
- Production red exposed intent-admission mutation seed identity construction and mutation lowering symbolic aspect reference digest rows; both now project terminal text locally through `reference.aspect_touch()`.
- Added an aspect-native compile-fail fixture proving facade callers cannot call the removed symbolic-reference terminal projection helper.
- Verification: fmt, `cargo check -p forge-query`, `cargo check -p forge-query --tests`, trybuild with and without `TRYBUILD=overwrite`, focused symbolic-reference runtime tests, and focused scans passed.

## Query existing-truth probe field terminal projection removal

- Added native `ForgeQueryExistingTruthProbeField::aspect_touch()` and removed public `terminal_aspect_path_projection()`.
- Production stayed green; the only implementation consumer was the probe digest row, which now projects terminal text locally through `field.aspect_touch()`.
- Added an aspect-native compile-fail fixture proving facade callers cannot call the removed probe-field terminal projection helper.
- Verification: fmt, `cargo check -p forge-query`, `cargo check -p forge-query --tests`, trybuild with and without `TRYBUILD=overwrite`, focused `probe_existing` and `verified_existing` runtime tests, line-count checks, and focused scans passed.

## Query existing-truth denial terminal projection removal

- Removed public terminal aspect-path projection helpers from `ForgeQueryExistingTruthAssertionDenial` and `ForgeQueryExistingTruthProbeDenial`.
- Added native denial accessors `asserted_aspect_touch()` and `probed_aspect_touch()` backed by typed denied-path storage; digest construction still localizes terminal projection internally.
- Deliberate `cargo check -p forge-query --tests` red exposed seven denial assertions across verify/probe/update/delete tests; migrated them to native `ForgeQueryAspectTouch` comparisons.
- Added two aspect-native compile-fail fixtures for the removed denial terminal projection helpers and refreshed the older denial alias fixtures to point compiler suggestions at native touch accessors.
- Split touched over-line-cap `verify_existing.rs` into `verify_existing.rs` plus `verify_existing_receipts.rs`.
- Verification: fmt, `cargo check -p forge-query`, `cargo check -p forge-query --tests`, trybuild with and without `TRYBUILD=overwrite`, focused `verify_existing`, `probe_existing`, `verified_update_existing`, and `verified_delete_existing` tests, line-count checks, and focused scans passed.

## Query validated entry terminal key projection removal

- Removed public terminal aspect/field key projection helpers from `ValidatedProjectionEntry`, `ValidatedPredicateEntry`, `ValidatedOrderingEntry`, and `ValidatedResultShapeBinding`.
- Deliberate `cargo check -p forge-query` red exposed collection ordering key construction, live relevance field classification, and grouped view-shape binding proof construction as production consumers; migrated them to project locally from native `AspectKey`/`FieldKey` accessors.
- Added four aspect-native compile-fail fixtures proving facade callers cannot call the removed validated-entry terminal key projection helpers.
- Verification: fmt, `cargo check -p forge-query`, `cargo check -p forge-query --tests`, trybuild with and without `TRYBUILD=overwrite`, focused `live`, `collection`, and `grouped` runtime tests, line-count checks, and focused scans passed.

## Query graph-read admission error terminal key projection removal

- Replaced terminal aspect/field storage on `ForgeQueryBooleanExpressionAdmissionError` and `ForgeQueryGraphReadSchemaReferenceAdmissionError` with native `AspectKey`/`FieldKey` storage.
- Removed public terminal aspect/field projection helpers from both graph-read admission error surfaces and exposed native key accessors instead.
- Added two aspect-native compile-fail fixtures proving facade callers cannot call the removed graph-read admission error terminal key projection helpers.
- Verification: fmt, `cargo check -p forge-query`, `cargo check -p forge-query --tests`, trybuild with and without `TRYBUILD=overwrite`, focused graph-read phase-one and phase-four requirement tests, line-count checks, and focused scans passed.

## Query schema field view terminal key projection removal

- Removed public terminal aspect/field projection helpers from `SchemaFieldView`; callers now retain typed `AspectName`/`FieldName` carriers instead of recovering schema keys as strings.
- `QuerySchemaView` digest construction now projects locally through native schema name accessors only at the schema identity boundary.
- Added an aspect-native compile-fail fixture proving facade callers cannot call the removed schema field terminal key projection helpers.
- Verification: `cargo fmt -p forge-query`, `cargo check -p forge-query`, `cargo check -p forge-query --tests`, trybuild with and without `TRYBUILD=overwrite`, focused schema view tests, line-count checks, and focused scans passed.

## Query projection fact field path terminal helper removal

- Removed public `ProjectionFactFieldPath::from_terminal_projection(...)` and `terminal_projection()`; facade callers now construct projection fact paths from foundational `CanonicalFieldPath`.
- Kept terminal field-path parsing/projection crate-local behind explicit boundary names for legacy authoring sugar, digest construction, diagnostics, and extraction errors.
- Deliberate production red exposed 18 consumers across domain capability identity, projection-consumption eligibility, extraction, retained binding, certification comparison, and identity digesting; test red exposed three terminal-text assertions that now compare canonical field paths.
- Added an aspect-native compile-fail fixture proving public callers cannot call the removed projection fact field-path terminal helpers.
- Verification: fmt, `cargo check -p forge-query`, `cargo check -p forge-query --tests`, trybuild with and without `TRYBUILD=overwrite`, focused `cargo test -p forge-query projection_consumption --lib`, line-count checks, and focused scans passed.

## Query authorized projection field path terminal helper removal

- Removed public `AuthorizedProjectionFieldPath::from_terminal_projection(...)` and `terminal_projection()`; facade callers now construct authorized projection fields from foundational `AspectKey`/`FieldKey` with `from_native_keys(...)`.
- Kept terminal field-path parsing/projection crate-local behind explicit ingress/boundary names for policy request ingress, digest/reporting text, and fixture setup.
- Deliberate production red exposed authorized projection digesting, policy delivery masking, policy live relevance, policy narrowing, projection-consumption eligibility, and certification fixtures; test red exposed authorized/policy assertions still mapping fields back to strings.
- Tests now inspect native aspect/field key pairs where they assert authorized field identity; harness terminal projection remains local only where feeding terminal request strings to current policy-live ingress.
- Split `authorized_projection/field_path.rs` out of `authorized_projection/artifacts.rs`, dropping the touched non-allowlisted artifacts file under the 400-line cap.
- Added an aspect-native compile-fail fixture proving public callers cannot call the removed authorized projection field-path terminal helpers.
- Verification: fmt, `cargo check -p forge-query`, `cargo check -p forge-query --tests`, trybuild with and without `TRYBUILD=overwrite`, focused authorized projection/policy delivery/policy live/policy narrowing/projection-consumption tests, line-count checks, and focused scans passed.

## Query retained field path terminal helper removal

- Removed public `ForgeQueryRetainedFieldPath::from_authoring_path(...)` and `terminal_projection()`; facade callers now construct retained field paths from foundational `CanonicalFieldPath`.
- Kept terminal retained-field parsing/projection crate-local behind explicit ingress/boundary names for current string authoring surfaces, diagnostics, digest construction, and projection-consumption boundary text.
- Deliberate production red exposed retained binding extraction, retained scalar fact admission, retained scalar alignment, and retained row digesting; test red exposed retained helpers and public bridge certification setup.
- Public bridge hostile certification setup now builds retained field paths from `CanonicalFieldPath`, keeping the public fixture native.
- Added an aspect-native compile-fail fixture proving public callers cannot call the removed retained field-path terminal helpers and refreshed the retained materialized row private-constructor fixture to use native path setup.
- Verification: fmt, `cargo check -p forge-query`, `cargo check -p forge-query --tests`, trybuild with and without `TRYBUILD=overwrite`, focused retained scalar tests, retained projection-consumption tests, public bridge hostile certification tests, public bridge reader-lane honesty tests, line-count checks, and focused scans passed.

## Query policy placeholder terminal field helper removal

- Replaced `PolicyPlaceholderMaskingRequest` and `PolicyPlaceholderMaskingDenial` terminal `Vec<String>` field storage with native `Vec<AuthorizedProjectionFieldPath>` proof carriers.
- Removed public terminal request construction/projection helpers; facade callers now use `from_authorized_field_paths(...)` and inspect `requested_placeholder_field_paths()`.
- Deliberate test red exposed policy delivery unit coverage and milestone nine certification rows still constructing `"secret.salary"` terminal request fields; migrated them to foundational `AspectKey`/`FieldKey` backed authorized field paths.
- Removed the unused crate-local terminal ingress after production `cargo check` showed no production path needed it.
- Added an aspect-native compile-fail fixture proving public callers cannot call the removed terminal placeholder request/projection helpers and refreshed adjacent placeholder alias stderr.
- Verification: fmt, `cargo check -p forge-query`, `cargo check -p forge-query --tests`, trybuild with and without `TRYBUILD=overwrite`, focused `policy_delivery --lib`, focused `milestone_nine --lib`, line-count checks, and focused scans passed.

## Query aspect touch terminal projection removal

- Removed public `ForgeQueryAspectTouch::terminal_aspect_path_projection()`; public callers now inspect the native `AspectKey` / optional `CanonicalFieldPath` accessors.
- Kept terminal aspect projection crate-local as `terminal_projection_for_boundary()` for digest/reporting/backend-key boundaries.
- Deliberate red exposed 80 production and 95 test call sites; public bridge support now formats terminal external keys locally from native touch accessors.
- Added a compile-fail fixture proving facade callers cannot call the removed touch terminal projection.
- Verification: fmt, `cargo check -p forge-query`, `cargo check -p forge-query --tests`, trybuild with and without `TRYBUILD=overwrite`, line-count checks, and focused scans passed.

## Query relation terminal projection removal

- Removed public terminal relation projection helpers from `TraversalSelector`, `SchemaRelationView`, `ForgeQueryAdmittedGraphReadDomainOperationReference`, and `ForgeQueryAdmittedGraphReadRelation`.
- Public callers must retain `RelationName` through `relation_name()` and project `as_str()` only at local assertion/reporting boundaries.
- Kept terminal relation projection crate-local as `terminal_relation_projection_for_boundary()` for digesting, canonicalization/reporting, graph-read rows, operation-resolution comparison, and read materialization.
- Deliberate production red exposed 13 call sites; `cargo check --tests` exposed four assertion/integration callers, including the public graph-read phase-one relation rows.
- Added four compile-fail fixtures proving facade callers cannot call the removed terminal relation projection helpers.
- Verification: fmt, `cargo check -p forge-query`, `cargo check -p forge-query --tests`, trybuild with and without `TRYBUILD=overwrite`, focused `traversal --lib`, focused `graph_read_access_phase_one`, focused `read_composition --lib`, line-count checks, and focused scans passed.

## Query projection-consumption terminal field request helper removal

- Removed public `ProjectMaterializedFacts::terminal_display_field(...)` and `terminal_derived_scalar_field(...)`, plus the declaration-builder forwarding methods.
- Facade callers now request display/derived scalar facts with `ProjectionFactFieldPath` values built from foundational `CanonicalFieldPath`.
- Added a crate-local segment helper for Query-owned fixtures so certification/regression code builds paths from explicit `FieldKey` segments instead of dotted request strings.
- Deliberate production red exposed seven certification/domain-capability fixture callers; `cargo check --tests` exposed 76 total test callers.
- Public bridge reader-lane support and projection-consumption golden UI examples now build native `ProjectionFactFieldPath` values.
- Added a compile-fail fixture proving facade callers cannot call the removed terminal field request helpers.
- Verification: fmt, `cargo check -p forge-query`, `cargo check -p forge-query --tests`, trybuild with and without `TRYBUILD=overwrite`, projection-consumption phase-boundary trybuild, focused `projection_consumption --lib`, line-count checks, and focused scans passed.

## Query view-shape focused inspector terminal constructor removal

- Removed the temporary public terminal focused-inspector constructors from `ViewShapeDescriptor` and `DeclarativeLiveViewShape`: `terminal_inspector_detail_focused(...)`, `terminal_identity_aware_inspector_detail_focused(...)`, `terminal_inspector_focused(...)`, and `terminal_identity_aware_inspector_focused(...)`.
- Facade callers must now use the native focused-inspector constructors with a foundational `AspectKey`; terminal strings are confined to local authoring/fixture/reporting edges that explicitly mint an `AspectKey`.
- `cargo check -p forge-query --tests` exposed 24 saved-query, view-shape, view-shape-live, milestone-eight, and milestone-nine-five consumers. Those call sites now use native constructors with explicit `AspectKey` values.
- Root UI identity-aware fixtures now use the native constructor so they keep testing bool shortcut/private-field boundaries instead of failing on the removed terminal helper.
- Verification: fmt, `cargo check -p forge-query`, `cargo check -p forge-query --tests`, aspect-native trybuild with and without `TRYBUILD=overwrite`, root phase-boundary trybuild with and without `TRYBUILD=overwrite`, focused `view_shape --lib`, focused `view_shape_live --lib`, line-count allowlist checks, and focused scans passed. The removed terminal constructor names survive only in the intentional aspect-native negative fixture.

## Query validated traversal terminal relation projection removal

- Removed public `ValidatedTraversalEntry::terminal_relation_projection()`; public callers now retain `RelationName` through `relation_name()`.
- Collection planning projects `relation_name().as_str()` only at the `TraversalEdgeClass` text boundary.
- Added a compile-fail fixture proving facade callers cannot recover terminal relation text from validated traversal entries.
- Verification: fmt, `cargo check -p forge-query`, `cargo check -p forge-query --tests`, trybuild with and without `TRYBUILD=overwrite`, focused validation and collection lib tests, line-count checks, and focused scans passed.

## Query aspect touch boundary projection root break

- Removed the crate-wide `ForgeQueryAspectTouch::terminal_projection_for_boundary()` root and replaced remaining text emission with explicit `aspect_path_text_for_boundary()` boundary projection.
- Added native touch containment comparison and migrated effect routing, computed routing, preview routing, retained refresh dedup, program derived-aspect dedup, and graph-composition selector/dedup logic away from dotted-string matching.
- The deliberate root break produced 80 production compiler errors before migration, giving the deeper consumer exposure requested.
- Added a compile-fail fixture proving facade callers cannot call the removed touch terminal boundary projection helper.
- Verification: fmt, `cargo check -p forge-query`, `cargo check -p forge-query --tests`, trybuild with and without `TRYBUILD=overwrite`, focused effect/computed/graph-composition lib tests, line-count checks, and focused scans passed.

## Query memory workspace declared aspect native lookup

- Replaced memory-workspace declared aspect rediscovery by terminal path string with native `ForgeQueryAspectTouch` matching against declared labels/external projection aliases.
- Field patch construction now carries the admitted aspect touch into relational locator construction and uses terminal projection only for diagnostics.
- Existing-truth missing-probe denial now has a private admitted-touch constructor so requested probe evidence remains typed until digest/report projection.
- Added a memory-workspace regression proving a whole-aspect declaration `title` accepts a native field touch `title.value`.
- Verification: `cargo fmt -p forge-query`, `cargo check -p forge-query`, `cargo check -p forge-query --tests`, focused `cargo test -p forge-query memory_workspace --lib`, focused `cargo test -p forge-query probe_existing --lib`, focused `cargo test -p forge-query verified_existing --lib`, line-count checks, and focused scans passed.
- Important judgment: this was useful Law 41 proof-flow hardening, but it stayed compiler-green. Next slice should break a broader root so `cargo check` exposes more consumers instead of relying on focused scans.

## Query existing-truth assertion denial native constructor

- Broke `ForgeQueryExistingTruthAssertionDenial::new(...)` so aspect-specific denials require `Option<ForgeQueryAspectTouch>` instead of `Option<String>`.
- Deliberate `cargo check -p forge-query --tests` red exposed public bridge verification support, runtime verification adapters, stateful bridge support, and stop-class fixtures still manufacturing assertion denial evidence from terminal strings.
- Migrated those callers to carry the asserted aspect touch or build a representative native touch in manual stop-class fixtures.
- Added an aspect-native trybuild fixture proving public callers cannot pass `Some("status.value".to_string())` to the assertion-denial constructor.
- Verification: `cargo fmt -p forge-query`, `cargo check -p forge-query`, red then green `cargo check -p forge-query --tests`, trybuild overwrite plus normal `cargo test -p forge-query --test aspect_native_query_compile_fail`, focused `cargo test -p forge-query verify_existing --lib`, focused `cargo test -p forge-query stop_class --lib`, focused `cargo test -p forge-query --test public_bridge_runtime_bootstrap`, line-count checks, and focused scans passed.

## Query existing-truth probe denial native constructor

- Broke `ForgeQueryExistingTruthProbeDenial::new(...)` so aspect-specific probe denials require `Option<ForgeQueryAspectTouch>` instead of `Option<String>`.
- Deliberate `cargo check -p forge-query --tests` red exposed intent-admission certification fixtures, runtime verification adapters, stateful bridge support, public bridge verification support, and stop-class fixtures still manufacturing probe denial evidence from terminal strings.
- Migrated those callers to carry the requested probe touch or build a representative native touch in manual stop-class fixtures.
- Removed the now-dead rejected-string branch from the shared denied-aspect path helper.
- Added an aspect-native trybuild fixture proving public callers cannot pass `Some("status.value".to_string())` to the probe-denial constructor.
- Verification: `cargo fmt -p forge-query`, `cargo check -p forge-query`, red then green `cargo check -p forge-query --tests`, trybuild overwrite plus normal `cargo test -p forge-query --test aspect_native_query_compile_fail`, focused `cargo test -p forge-query probe_existing --lib`, focused `cargo test -p forge-query verify_existing --lib`, focused `cargo test -p forge-query stop_class --lib`, focused `cargo test -p forge-query --test public_bridge_runtime_bootstrap`, line-count checks, and focused scans passed.

## Query graph obligation selector native lookup keys

- Broke public `ForgeQueryGraphTouchSelector::selector_value()` and `selector_kind()` so facade callers cannot recover graph-obligation selector authority as neutral strings.
- Replaced obligation-index lookup routing through selector kind/value text with internal `ForgeQueryGraphTouchSelectorClass` plus native accessors for relation ids, aspect touches, declared aspect operations, mutation families, lifecycle families, and read verbs.
- `AspectTouch` and `DeclaredAspectOperation` lookup keys now carry `ForgeQueryAspectTouch` / `ForgeQueryAspectMutationOperation`; terminal projection remains only for digest/report values.
- Split selector class/helper code into private registration modules to keep `touch_selector.rs` under the workspace line cap.
- Added aspect-native trybuild fixtures for both removed selector aliases. Verification: fmt, cargo check, cargo check --tests, trybuild overwrite plus normal trybuild, focused graph_obligation and graph_composition lib tests, line-count checks, and focused scans passed.

## Query derived patch scalar payload constructor fence

- Removed public `ForgeQueryDerivedPatchPayload::from_scalar_value(...)` and the scalar-only payload variant, leaving derived patch payloads as empty or retained materialized rows.
- Added an aspect-native trybuild fixture proving facade callers cannot create detached scalar derived patch payloads.
- Verification: fmt, cargo check, cargo check --tests, trybuild overwrite plus normal trybuild, focused computed lib tests, and focused scans passed.
- Important judgment: this was a narrow unused compatibility fence and produced no meaningful compiler red. Next query slice should deliberately break a live ingress/projection carrier with consumers.

## Query projection and retained field-path terminal ingress quarantine

- Removed generic `ProjectionFactFieldPath::from_terminal_ingress(...)`; row-like extraction now keeps dotted external field decoding in local boundary helpers, and identity/aspect-label paths are built through canonical field-path carriers.
- Deliberately removed crate-local `ForgeQueryRetainedFieldPath::from_terminal_ingress(...)` before migration. The red `cargo check -p forge-query --tests` exposed retained binding extraction, retained scalar fact admission, retained scalar alignment, runtime support, projection-consumption retained-live fixtures, and certification/transcript helpers.
- Retained binding extraction now converts requested projection fact paths directly through `canonical_field_path().clone()` into retained field paths instead of projecting terminal text and reparsing.
- Retained scalar fact/alignment string authoring remains ergonomic but is quarantined in boundary-named local helpers. Runtime/test/certification helpers now build retained paths from `CanonicalFieldPath`/`FieldKey` or scoped test helpers.
- Split retained scalar fact tests into `retained_scalar_facts_tests.rs` to keep touched Rust files under the 400-line cap.
- Added aspect-native trybuild coverage for the removed projection fact terminal ingress constructor. Retained ingress was crate-local, so the proof is internal compiler red plus focused scans proving no retained call sites remain.
- Verification: fmt, deliberate red then green `cargo check -p forge-query --tests`, green `cargo check -p forge-query`, trybuild overwrite plus normal trybuild, focused `projection_consumption --lib`, focused `retained_scalar --lib`, line-count checks, and focused scans passed.

## Query declarative/program JSON vocabulary native value slice

- Removed public `DeclarativeWritebackValue::StructuredJson(String)` by turning `DeclarativeWritebackValue` into a private `AspectValue` carrier with scalar constructors and an `aspect_value()` accessor.
- Declarative writeback digests now derive from native `AspectValue` structure instead of a structured-json enum branch.
- Renamed `ForgeQueryPortType::TerminalJson` to `ProgramValue`, and cleaned related internal JSON wording (`is_json_number_text`, "JSON number text", `extract_json_rows`) into canonical/program/entity language.
- Split `runtime/intent/input.rs` out of `runtime/intent/declaration.rs` after the line-cap check caught the touched declaration file over 400 lines.
- Added compile-fail fixtures proving callers cannot use the removed `StructuredJson` writeback variant or `TerminalJson` port type.
- Verification: fmt, deliberate red then green `cargo check -p forge-query --tests`, green `cargo check -p forge-query`, trybuild overwrite plus normal trybuild, focused `declarative --lib`, focused `program --lib`, line-count checks, and focused scans passed. The old JSON names now only appear in intentional negative fixtures.

## Query authorized projection field-path terminal ingress removal

- Removed reusable `AuthorizedProjectionFieldPath::from_terminal_ingress(...)` and `from_parts(...)` constructors from the proof carrier.
- Deliberate `cargo check -p forge-query --tests` red exposed authorized projection derivation, live policy admission, projection-consumption test/certification bindings, and domain capability certification fixtures as consumers.
- Authorized projection derivation now validates canonical result-shape aspect/field names locally and creates paths with native `AspectKey`/`FieldKey`.
- Live policy relevance keeps string authoring only in a scoped boundary helper. Projection/domain fixture helpers now mint native keys before `from_native_keys(...)`.
- Added compile-fail coverage proving facade callers cannot use terminal ingress or string-parts constructors for authorized projection paths.
- Verification: fmt, red then green `cargo check -p forge-query --tests`, green `cargo check -p forge-query`, trybuild overwrite plus normal trybuild, focused `authorized_projection --lib`, `policy_live --lib`, `projection_consumption --lib`, line-count checks, and focused scans passed. Removed constructors remain only in intentional negative fixtures.

## Query live policy relevance native admission

- Changed `admit_policy_aware_live_plan(...)` to accept `&[AuthorizedProjectionFieldPath]` instead of `&[String]`.
- Removed the temporary live-relevance string parser from policy admission; masked relevance denial now compares native authorized projection field paths.
- Deliberate `cargo check -p forge-query --tests` red exposed policy-live unit tests and milestone-nine certification rows that still passed string field bags.
- Policy-live tests now build relevance fields with `AspectKey`/`FieldKey`; milestone-nine certification clones narrowed visible field paths for admitted live plans and uses a native masked-field helper for hostile denial.
- Added compile-fail coverage proving facade callers cannot pass `Vec<String>` relevance fields to live policy admission.
- Verification: fmt, red then green `cargo check -p forge-query --tests`, trybuild overwrite plus normal trybuild, focused `policy_live --lib`, focused `milestone_nine_certification --lib`, line-count checks, and focused scans passed.

## Query memory/test-backend external projection native storage

- `ForgeQueryAspect` now stores `external_projection_path` as `CanonicalFieldPath`; `external_projection_path()` returns the native path, not `&str`.
- `ForgeQueryTestBackendSchema` now parses/stores external projection paths as `CanonicalFieldPath`, exposes native paths from `aspects()`, and passes them to `ForgeQueryAspect` without terminal string round-tripping.
- Added `InvalidProjectionPath` schema denial and trybuild coverage proving facade callers cannot treat either external projection accessor as `&str`.
- Verification: fmt, green `cargo check -p forge-query --tests`, trybuild overwrite plus normal trybuild, focused `memory_workspace --lib`, focused `consumer_kit::test_backend --lib`, and line-count checks passed.

## Query read/preview native touch construction

- Added native `ForgeQueryParsedAspectTarget::from_native_parts(...)` and `ForgeQueryAspectTouch::from_native_parts(...)` constructors.
- Runtime read obligation dispatch now builds projection/filter/ordering touches from native `AspectKey`/`CanonicalFieldPath` instead of formatting dotted paths and reparsing them.
- Preview live routing now builds request projection touches natively for delta matching; terminal affected-aspect maps remain an explicit next root.
- Graph touch descriptor inventory now counts declared/touched aspect sets as native touch/operation sets instead of `BTreeSet<String>` terminal path sets.
- Verification: fmt, green `cargo check -p forge-query --tests`, focused `read_obligation_dispatch --lib`, focused `preview --lib`, focused `graph_touch_descriptor --lib`, line-count checks, and focused scans passed.
- Important judgment: these were useful Law 41 proof-flow/storage slices, but they stayed mostly compiler-green. The next batch should break a live runtime carrier like preview/session affected-aspect maps so `cargo check` exposes consumers more aggressively.

## Query preview/live affected-aspect native propagation

- Preview session routing now keeps `live_affected` and `computed_affected` as `BTreeMap<String, Vec<ForgeQueryAspectTouch>>`, so live/computed/effect preview relevance no longer projects affected aspects to `Vec<String>` between stages.
- Removed the preview-local terminal aspect-path projection/reparse helper pair.
- Live subscription delivery relevance now compares native request field touches and native `AspectKey` focus/grouping values instead of projecting `delta.touched_aspects()` to strings and using `starts_with`.
- Verification: fmt, green `cargo check -p forge-query --tests`, focused `live --lib`, focused `preview --lib`, attempted `live_subscription --lib` (0 matching tests), line-count checks, and targeted scans passed.
- Important judgment: useful internal runtime enforcement, but still green. Next broader-red candidate should be a public/cross-module carrier such as computed/effect inspection aspect-list projection or write-receipt reporting if it currently retains terminal aspect lists as semantic source.

## Query computed/effect inspection aspect-list native evidence encoding

- Computed inspection identity now carries dependency, produced, and derived patch aspect lists as `ForgeQueryAspectTouch` slices until evidence encoding; removed the local terminal aspect-path projection helper.
- Effect inspection identity now maps trigger, condition input/output, and pending delivery touch slices directly from native touches at the digest/report boundary; removed `effect::delivery_helpers::terminal_aspect_paths_projection`.
- Verification: fmt, green `cargo check -p forge-query --tests`, focused `computed --lib`, focused `effect --lib`, line-count checks, and targeted scans passed.
- Important judgment: this was a valid internal authority cleanup, but it stayed compiler-green. Next batch should break a public/cross-module carrier more deeply to expose consumers through `cargo check` rather than relying on search.

## Query workspace live-view builder field-key native admission

- Changed public `ForgeQueryLiveViewBuilder::select(...)` and `order_by(...)` to require `AspectFieldKey` instead of dotted `aspect.field` strings, and exported `AspectFieldKey` through the facade.
- Deliberate `cargo check -p forge-query --tests` red exposed 226 builder consumers across public bridge, consumer-kit, runtime mutation/read, graph-composition/read-maintenance, aspect API certification, and runtime transcript paths. Migrated those call sites to explicit field-key construction.
- Added aspect-native trybuild coverage proving facade callers cannot pass string arrays/string ordering to the live-view builder.
- Verification: fmt, red then green `cargo check -p forge-query --tests`, green `cargo check -p forge-query`, trybuild overwrite plus normal trybuild, focused `workspace_declaration --lib`, focused `in_memory_test_backend_facade`, slice line-cap checks, and literal-builder residue scan passed.
- Added six touched pre-existing oversized mutation test files to the Rust line-cap allowlist with scoped removal triggers because explicit field-key construction pushed them over the cap.

## Query schema view constructor native-name admission

- Changed public `SchemaFieldView::new(...)` to require `AspectName` and `FieldName`, and `SchemaRelationView::new(...)` to require `RelationName`.
- Exported `AspectName` and `FieldName` through the facade and updated `forge_query_schema!` to mint native name carriers before constructing schema evidence.
- Deliberate `cargo check -p forge-query --tests` red exposed 129 schema constructor consumers across graph-read tests, runtime declaration, lower-runtime/intent certification fixtures, view-shape/live support, saved-query fixtures, and schema macro expansion.
- Workspace live-view declaration now admits builder-derived schema names in a scoped schema-admission helper before building `QuerySchemaView`; split helper/declaration modules kept touched files under the line cap.
- Added aspect-native trybuild coverage proving facade callers cannot pass raw strings to schema constructors.
- Verification: `cargo fmt -p forge-query`, red then green `cargo check -p forge-query --tests`, green `cargo check -p forge-query`, trybuild overwrite plus normal trybuild, focused `schema_view --lib`, focused `workspace_declaration --lib`, touched-file line counts, and structural raw-constructor residue check passed.

## Query policy aspect mask field-key native admission

- Changed `PolicyAspectMask` to store `BTreeMap<AspectFieldKey, ProjectionVisibility>` and require `AspectFieldKey` for `with_masked(...)` / `with_non_disclosing_use_only(...)`.
- Removed the `visibility_for_parts(...)` string lookup route. Authorized projection derivation now builds `AspectFieldKey` proof values from canonical aspect/field names before visibility checks.
- Deliberate `cargo check -p forge-query --tests` red exposed 32 raw mask consumers across authorized projection, policy narrowing, policy plan/execution seam, and milestone-nine certification; migrated them to explicit field-key construction.
- Added compile-fail coverage proving public callers cannot pass raw string pairs to policy mask construction; updated the older mask-mutation fixture so it still proves private entries.
- Verification: fmt, red then green `cargo check -p forge-query --tests`, green `cargo check -p forge-query`, trybuild overwrite plus normal trybuild, focused `authorized_projection --lib`, focused `policy_narrowing --lib`, line-count checks, and residue scan passed.

## Query mutation receipt touched-aspect native evidence encoding

- Write receipt committed-truth identity, batch write receipt identity, unified batch-write inspection digest inputs, component inspection digest construction, and aspect API finalization `touched_aspect_digest` now consume `ForgeQueryAspectTouch` values directly instead of pre-projecting touched aspects to `Vec<String>`.
- Added `runtime/surface/mutation/batch_receipt_identity.rs` to keep batch receipt evidence helpers scoped and bring `batch_receipt.rs` back under the 400-line cap.
- Widened `ForgeQueryAspectTouch::native_digest_part()` to crate-private so certification can digest native touch evidence without using terminal path helpers.
- Verification: fmt, red then green `cargo check -p forge-query --tests`, green `cargo check -p forge-query`, focused `batch_write --lib`, focused `aspect_api_finalization --lib`, line-count checks, and targeted residue scan passed.
- Judgment: useful Law 41 evidence cleanup, but not a broad red root. Next slice should break another public/cross-module terminal string authority seam.

## Query existing-truth probe shortcut native-touch admission

- Changed crate-local `ForgeQueryWorkspace::probe_existing(...)` to require `ForgeQueryAspectTouch` values instead of raw terminal aspect path strings.
- `ForgeQueryExistingTruthProbeRequest` now seals request digest evidence from `ForgeQueryAspectTouch::native_digest_part()` instead of a temporary terminal `Vec<String>` projection.
- Intent-admission routing certification clones native request touches into the workspace shortcut instead of projecting them back to strings.
- Deliberate `cargo check -p forge-query --tests` red exposed runtime probe, intent-admission routing, verified-existing, bridge-backed verification, mixed-authority, and delete/update verification consumers; all migrated to `test_aspect_touches(...)`.
- Verification: fmt, red then green `cargo check -p forge-query --tests`, green `cargo check -p forge-query`, focused `probe_existing --lib`, focused `intent_admission --lib`, line-count checks, and targeted scans passed.

## Query aspect-touch terminal projection runtime-local fence

- Narrowed `ForgeQueryAspectTouch::aspect_path_text_for_boundary()` to `pub(in crate::runtime)` so non-runtime modules cannot recover terminal dotted aspect text from native touches.
- Deliberate `cargo check -p forge-query --tests` red exposed seven outside-runtime consumers across consumer-kit equivalence reporting, intent-admission certification/eligibility/handoff hashing, and memory workspace diagnostics.
- Migrated those consumers to `native_digest_part()` evidence/reporting or native-digest fixture matching; no new compatibility projection helper was introduced.
- Verification: fmt, red then green `cargo check -p forge-query --tests`, green `cargo check -p forge-query`, focused `memory_workspace --lib`, focused `consumer_kit::test_backend --lib`, focused `intent_admission --lib`, line-count checks, and outside-runtime projection scans passed.

## Query aspect-touch terminal projection mutation-boundary fence

- Narrowed `ForgeQueryAspectTouch::aspect_path_text_for_boundary()` again to `pub(in crate::runtime::mutation)`.
- Deliberate `cargo check -p forge-query --tests` red exposed 25 production runtime consumers and 14 test consumers outside mutation, including computed/effect/inspection/graph-composition evidence paths and stateful bridge test adapters.
- Migrated non-mutation evidence consumers to `native_digest_part()`.
- Converted stateful bridge existing-truth test support so verification lookup keys use native digest material, and external rows derive `CanonicalFieldPath` from native aspect-touch parts instead of terminal dotted strings.
- Updated graph-composition expected snapshots to admit fixture aspect labels into `ForgeQueryAspectTouch` before comparing native digest material.
- Verification: fmt, red then green `cargo check -p forge-query --tests`, green `cargo check -p forge-query`, focused `computed --lib`, `effect --lib`, `batch_write --lib`, `graph_composition --lib`, line-count checks, and projection-fence scans passed.

## Query graph-obligation selector/index native evidence encoding

- Graph-obligation selector identity, declared aspect-operation selector values, declared mutation collection selector values, lookup-key values, and touch descriptor row evidence now encode `ForgeQueryAspectTouch::native_digest_part()` instead of terminal dotted aspect paths.
- Removed graph-composition helper names `touch_paths_projection(...)` and `terminal_declared_aspect_operation(...)`; selector kind evidence now says `aspect-touch`.
- Renamed graph-obligation vocabulary that described already-admitted native touches: `declared_aspect_touch_count()`, `ObservesAspect`, `TouchedAspect`, and `touched_aspect` evidence tags.
- Deliberate `cargo check -p forge-query --tests` red exposed remaining old-name consumers in runtime read-obligation dispatch, graph-touch descriptor tests, and read-obligation selector hardening; migrated them.
- Verification: fmt, red then green `cargo check -p forge-query --tests`, focused `graph_obligation --lib`, `graph_composition --lib`, `read_obligation_dispatch --lib`, `phase_boundaries_graph_touch_descriptor_compile_fail`, line-count checks, and targeted graph-composition vocabulary/projection scans passed.

## Query mutation aggregate evidence native-touch hard fence

- Mutation declared-aspect aggregate evidence, symbolic aspect-reference rows, existing-truth assertion/probe denials, assertion verification rows, probe fields, and probe rows now encode native touch digest material via `ForgeQueryAspectTouch::native_digest_part()` instead of terminal `aspect_path` evidence values.
- Renamed the internal denial helper from `ForgeQueryDeniedAspectPath` / `denied_aspect_path` to `ForgeQueryDeniedAspectTouch` / `denied_aspect_touch`.
- Duplicate aspect diagnostics now report native touch digest material, and `contains_all_aspect_paths(...)` became `contains_all_aspect_touches(...)`.
- Important mechanical win: after deleting the final consumers, removed `ForgeQueryAspectTouch::aspect_path_text_for_boundary()` and `ForgeQueryParsedAspectTarget::authoring_path_projection()`. Parsed native targets no longer retain the original authoring string after admission.
- Verification: fmt, green `cargo check -p forge-query --tests`, focused `probe_existing --lib`, `existing_truth --lib`, `mixed_authority --lib`, `graph_obligation --lib`, full aspect-native trybuild harness, line-count checks, and targeted scans proving the removed terminal helpers/evidence tags are absent.

## Query verified-assumption read-set native-touch evidence

- Renamed verification read-set breadth vocabulary from asserted aspect paths to asserted aspect touches: `distinct_asserted_aspect_touch_count`, `distinct_asserted_aspect_touches`, and `asserted_aspect_touch`.
- Graph-composition assumption summaries now collect native asserted touch digest parts directly; the old `terminal_asserted_aspect_paths_projection(...)` helper is gone.
- Updated runtime verification/graph-composition expected evidence rows and phase-boundary trybuild snapshots so public/private evidence surfaces teach native touch vocabulary.
- Verification: fmt, green `cargo check -p forge-query --tests`, focused `graph_composition --lib`, `existing_truth --lib`, `bridge_backed_verification_execution --lib`, full aspect-native trybuild harness, and full phase-boundaries compile-fail harness passed.

## Query declaration aspect contract native-key root break

- Started the next broad mechanical-red root: declaration aspect contracts, coverage, publication, and grouped aspect participation now retain `AspectFieldKey` values instead of `Vec<String>` semantic aspect labels.
- Restricted string-slice construction to unit-test builds and routed production digest/reporting edges through explicit terminal projection helpers.
- Production verification is green with `cargo fmt -p forge-query` and `cargo check -p forge-query`.
- Intentional red remains on `cargo check -p forge-query --tests`: 66 test/support consumers still compare typed aspect keys/publications/contracts to string fixtures. Next batch should migrate those consumers without restoring string-backed production authority.

## Query declaration aspect contract native-key root completed

- Migrated the 66 mechanically exposed test/support consumers after the declaration aspect root break.
- Added test-only declaration aspect fixture helpers that mint `AspectFieldKey` values and explicitly project terminal strings only for assertions about evidence text.
- Updated declaration capability, aspect, publication, legality, progression, orchestration, receipt, envelope, route, relational, signal, entry seam, and contribution-composed tests so production carriers remain typed.
- Verification: `cargo fmt -p forge-query`, green `cargo check -p forge-query --tests`, green `cargo check -p forge-query`, focused declaration/contribution test slices, `cargo test -p forge-query --test aspect_native_query_compile_fail`, touched-file line counts, and a targeted string-fixture residue scan passed.

## Query retained-scalar public API native field-path fence

- Closed the retained-scalar compatibility hole where public
  `ForgeQueryDerivedArtifactBinding::consume_scalar_fields(...)` and
  `verify_scalar_alignment(...)` still accepted raw field-path strings.
- Both APIs now require `ForgeQueryRetainedFieldPath` proof carriers; the old
  public/by-name string fallback helpers were removed.
- Added aspect-native trybuild coverage proving facade callers cannot pass raw
  string fields or string pairs into retained scalar extraction/alignment.
- Verification: `cargo fmt -p forge-query`, trybuild overwrite plus normal
  `cargo test -p forge-query --test aspect_native_query_compile_fail`, focused
  `cargo test -p forge-query retained_scalar --lib`, focused `cargo test -p
  forge-query projection_consumption --lib`, green `cargo check -p
  forge-query`, line counts, and the earlier deliberate red
  `cargo check -p forge-query --tests` captured in
  `target/aspect-native-retained-scalar-red.log`.

## Query live-read execution installation proof fence

- Removed crate-local `ForgeQueryRuntime::execute_live_read_by_name(...)` so
  live read execution consumes `ForgeQueryRuntimeLiveSubscriptionInstallation`
  proof state instead of terminal view-name strings.
- Production `ForgeQueryLiveArtifactTarget` values built from live views now
  carry the subscription installation, and live artifact bundle reads execute
  through that carrier.
- Deliberate red `cargo check -p forge-query --tests` exposed declaration
  upstream replay, live artifact bundle reads, intent-admission read tests, and
  live receipt tests; all were migrated to installation/handle-based execution.
- Verification: `cargo fmt -p forge-query`, red then green `cargo check -p
  forge-query --tests`, trybuild overwrite plus normal `cargo test -p
  forge-query --test aspect_native_query_compile_fail`, focused
  `live_receipt`, focused `live_artifact`, focused
  `runtime_read_live_delegates_to_canonical_live_read_execution`, line counts,
  and residue scans passed.

## Query memory-workspace aspect declaration native carrier fence

- Changed `ForgeQueryAspect` to store `ForgeQueryAspectTouch` and
  `CanonicalFieldPath` directly, and changed `ForgeQueryAspect::new(...)` to
  require those proof carriers instead of raw aspect/projection strings.
- Memory workspace declaration/projection logic now uses the stored touch rather
  than reparsing aspect labels.
- Consumer-kit test backend remains the authoring boundary: it validates raw
  schema labels into `ForgeQueryAspectTouch`, lowers projection paths into
  `CanonicalFieldPath`, and reports invalid native aspect labels with a new
  `InvalidAspectLabel` error kind.
- Verification: `cargo fmt -p forge-query`, red then green `cargo check -p
  forge-query --tests`, green `cargo check -p forge-query`, trybuild overwrite
  plus normal `cargo test -p forge-query --test
  aspect_native_query_compile_fail`, focused `memory_workspace`, focused
  `consumer_kit::test_backend`, line counts, and residue scans passed.

## Query aspect-touch public raw-string constructor removal

- Removed public `ForgeQueryAspectTouch::new(...)` so facade callers cannot mint
  central touch proof from raw dotted strings.
- Added native public constructors:
  `ForgeQueryAspectTouch::aspect(AspectKey)` and
  `ForgeQueryAspectTouch::field_path(AspectKey, CanonicalFieldPath)`.
- Quarantined remaining crate-local/test authoring text parsing behind
  `ForgeQueryAspectTouch::from_authoring_path(...)` so residual weaker input is
  mechanically visible by name.
- Deliberate red `cargo check -p forge-query --tests` exposed consumer-kit
  schema, intent-admission certification, graph composition, runtime support,
  public bridge, and stabilization transcript consumers. Those were migrated to
  the crate-local authoring parser or local native key/path fixture helpers.
- Added `aspect_touch_raw_string_constructor_removed.rs` trybuild coverage and
  refreshed setup in older negative fixtures to use native touch construction.
- Verification: `cargo fmt -p forge-query`, red then green `cargo check -p
  forge-query --tests`, trybuild overwrite plus normal `cargo test -p
  forge-query --test aspect_native_query_compile_fail`, and residue scan. Note:
  touched `tests/intent_admission_public_dx.rs` remains a pre-existing
  over-400-line public DX inventory and should be split in a later test-file
  cleanup.

## Query declarative live aspect-field constructor native-key fence

- Broke the next root more deeply instead of just cleaning residue:
  `DeclarativeProjectionField`, `DeclarativeOrderingField`, declarative
  predicate filters, `DeclarativeWritebackChange`, and
  `DeclarativeWritebackIntent::update_aspect` now require `AspectFieldKey`
  publicly.
- Kept raw aspect/field text only behind crate-local
  `*_from_authoring_parts(...)` boundaries for declarative lowering and
  fixtures that genuinely start from authoring text.
- Deliberate `cargo check -p forge-query --tests` red exposed 59 real consumers
  across declarative live lowering, runtime workspace declaration,
  read-composition lowering, certification fixtures, runtime schema support,
  and writeback tests.
- Added/updated aspect-native trybuild fixtures proving facade callers cannot
  pass raw strings into the declarative projection, ordering, predicate, or
  writeback constructors.
- Verification for the slice: `cargo fmt -p forge-query`; red then green
  `cargo check -p forge-query --tests`; green `cargo check -p forge-query`;
  trybuild overwrite plus normal `cargo test -p forge-query --test
  aspect_native_query_compile_fail`; constructor residue scans; line counts.
  `declarative_live.rs` is already line-cap allowlisted.

## Query retained-upstream dependency name proof fence

- Removed public retained-upstream raw-name proof minting:
  `ForgeQueryRetainedUpstreamInputs::new(...)` is runtime-only, raw
  `live_rows(...)` / `retained_computed_rows(...)` / single-row name lookup are
  gone from the public surface, and public access is now handle-based or
  declaration-scoped.
- Removed public `ForgeQueryDerivedView::depends_on_live_name(...)` and
  `depends_on_derived_name(...)`; the remaining raw dependency-name replay is
  crate-local and explicitly named `*_from_workspace_declaration(...)`.
- Deliberate `cargo check -p forge-query --tests` red exposed runtime computed
  maintainers and runtime program support still walking upstream rows by raw
  names. Those consumers now carry the derived declaration into lookup.
- Added aspect-native compile-fail fixtures for retained-upstream raw lookup /
  raw construction and derived-view raw dependency-name builders.
- Added focused runtime coverage proving declared retained computed row lookup
  succeeds for declared upstreams and fails closed for undeclared names.
- Verification: `cargo fmt -p forge-query`; red then green `cargo check -p
  forge-query --tests`; green `cargo check -p forge-query`; trybuild overwrite
  plus normal `cargo test -p forge-query --test aspect_native_query_compile_fail`;
  focused `cargo test -p forge-query
  retained_upstreams_decode_single_computed_rows_through_query_runtime_floor
  --lib`; residue scans.

## Query retained materialization maintainer proof fence

- Pulled the work back to aspect-native authority boundaries after Esther asked
  what a retained-row builder break had to do with aspect-native refactoring.
  The relevant claim: public/facade maintainers must not mint retained
  materialized row proof objects or patch payloads from proof rows directly.
- Removed the retained materialized row builder entirely; narrowed row-taking
  materialization and derived-patch-payload constructors to runtime-internal
  authority; added public scoped scalar transitions:
  `replace_retained_scalar_row`, `push_retained_scalar_row`, and
  `ForgeQueryDerivedPatchPayload::from_retained_scalar_values`.
- Deliberate red `cargo check -p forge-query --tests` exposed public bridge
  hostile certification plus runtime/aspect API certification maintainers that
  still used row proof ingestion. Migrated them to retained scalar transitions.
- Added compile-fail fixtures for removed retained-row builder and private
  retained-row payload construction; refreshed payload stderr. Verification:
  fmt, red/green `cargo check -p forge-query --tests`, green `cargo check -p
  forge-query`, normal trybuild, public bridge bootstrap, public bridge
  reader-lane honesty, hostile certification, runtime API stabilization, and
  aspect API finalization tests. `runtime/computed/surface.rs` is over 400 but
  already allowlisted.

## Query memory-workspace external projection row conversion

- Esther asked to convert item 4 and not stall on speculative blockers. Decided
  there were no blockers worth waiting on and converted the row substrate.
- `ForgeQueryEntityRow::AspectProjection` now stores native field values keyed
  by `CanonicalFieldPath`; `ForgeQueryAspect::external_projection_path()`
  remains a schema/import mapping, not a retained external row authority map.
- Removed/renamed public row APIs:
  `from_external_projection_values(...)` -> `from_native_field_values(...)`,
  `external_projection_values()` -> `native_field_values()` internally, and
  `external_scalar_value(...)` -> `scalar_value_at(CanonicalFieldPath)`.
  `external_aspect_value(...)` is gone.
- Deliberate red `cargo check -p forge-query --tests` exposed program,
  projection-consumption, runtime read composition, public bridge, memory
  workspace, and runtime support consumers. Migrated them to native field
  values and renamed test helpers to `test_native_*`.
- Added/expanded the aspect-native trybuild fixture so public callers cannot
  use the removed external row constructor, map accessor, string scalar lookup,
  or external aspect lookup.
- Verification passed: `cargo fmt -p forge-query`, `cargo check -p
  forge-query --tests`, `cargo check -p forge-query`, normal `cargo test -p
  forge-query --test aspect_native_query_compile_fail`, focused
  `memory_workspace --lib`, `runtime_backed_read_bootstrap`,
  `public_bridge_runtime_bootstrap`, `public_submission_lane_replacements`,
  `in_memory_test_backend_facade`, and `projection_consumption --lib`. Search
  for the removed row API names now finds only the negative fixture.

## Query backend-admissible mutation proof fence

- Continued the next root after Esther said "okay go ahead": mutation/backend
  proof-state split.
- Removed `ForgeQueryBackendAdmissibleMutation::into_command()` and changed the
  write-authority adapter contract, bridge mutation authority construction, and
  write-authority execution receipt construction to accept
  `ForgeQueryBackendAdmissibleMutation` instead of raw `ForgeQueryWriteCommand`.
- Deliberate red `cargo check -p forge-query --tests` exposed bridge-backed
  execution, consumer-kit backend, stateful/public bridge adapters,
  intent-admission certification write authority, runtime API transcript
  authority, lower-runtime representative fixtures, and runtime drift tests.
  Migrated each to consume the admitted proof in write-authority contracts
  while temporarily preserving the command view only where consumers still
  needed it.
- Added aspect-native compile-fail fixtures proving facade callers cannot call
  `from_admitted_command(...)`, cannot call the removed `into_command()`, and
  cannot construct the proof by struct literal.
- Verification passed: `cargo fmt -p forge-query`, red/green `cargo check -p
  forge-query --tests`, normal `cargo test -p forge-query --test
  aspect_native_query_compile_fail`, focused write-authority drift and signal
  invalidation receipt tests, `public_bridge_runtime_bootstrap`,
  `intent_admission --lib`, `intent_admission_public_dx`, and
  `public_submission_lane_replacements`. All touched files in this slice are
  under 400 lines.

## Query backend-admissible mutation family-shape slice

- Broke the admitted-mutation root more deeply after the first proof fence:
  `ForgeQueryBackendAdmissibleMutation` no longer stores a full
  `ForgeQueryWriteCommand` and no longer exposes `command()`.
- Added a private family-specific shape for insert, direct/existing/symbolic
  update, assertion, and direct/existing/symbolic delete. Admission still
  destructures a `ForgeQueryWriteCommand` at the crate-local transition, but
  downstream code cannot reconstruct or inspect the old command carrier.
- Write-authority execution receipts and bridge-backed drift checks derive
  subject identity directly from the admitted mutation proof. Consumer-kit,
  stateful/public bridge adapters, transcript authority, signal/write support,
  and intent-admission write fixtures now use read-only mutation accessors.
- Added/updated aspect-native compile-fail fixtures proving facade callers
  cannot call `from_admitted_command(...)`, cannot call removed
  `into_command()` or `command()`, and cannot forge the private `shape` field
  or private shape type by struct literal.
- Split `runtime/mutation/backend_admissible.rs` into the proof wrapper plus a
  private `backend_admissible/shape.rs` module so both files stay under 400
  lines.
- Split write-authority execution receipt routing into
  `lower_runtime_routing/adapters/runtime_backend/write_authority_receipt.rs`
  so the lower-runtime backend adapter also stays under 400 lines.
- Verification passed: `cargo fmt -p forge-query`, `cargo check -p
  forge-query --tests`, normal `cargo test -p forge-query --test
  aspect_native_query_compile_fail`, focused write-authority drift,
  signal-invalidation receipt, `public_bridge_runtime_bootstrap`,
  `public_submission_lane_replacements`, and `intent_admission --lib`.

## Query bridge writeback native effect-intent slice

- Converted the bridge writeback authority root that still minted a synthetic
  `forge.query.writeback` scalar marker. Bridge writeback now constructs a
  crate-private `ForgeQueryBridgeWritebackEffectIntent` from
  `ForgeQueryBackendAdmissibleMutation` and lowers into foundational
  `AuthoritativeRecordAspectPatch`.
- The new proof supports whole-aspect set/clear and single-field set/clear
  patch operations; nested field paths fail closed with an explicit unsupported
  denial instead of silently falling back to a weaker marker.
- Added runtime provenance coverage proving the bridge effect-intent canonical
  basis changes with the admitted `title.value` patch, includes authoritative
  patch evidence such as `title.field.value.set`, and no longer contains
  `forge.query.writeback`.
- Added aspect-native compile-fail coverage proving the new writeback
  effect-intent proof is not public facade API.
- Verification passed: `cargo fmt -p forge-query`, `cargo check -p
  forge-query --tests`, normal `cargo test -p forge-query --test
  aspect_native_query_compile_fail`, focused bridge writeback provenance,
  focused signal invalidation receipt, focused write-authority drift,
  `public_bridge_runtime_bootstrap`, and `public_submission_lane_replacements`.

## Query affected runtime view target proof carrier slice

- Converted runtime write, batch, and intent receipt affected view storage from
  raw `Vec<String>` into `ForgeQueryLiveArtifactTarget` and
  `ForgeQueryDerivedMaterializationTarget` proof carriers.
- Kept backend/source `affected_live_view_ids(...)` as terminal external
  ingress, but runtime routing now immediately admits those handles through a
  runtime-only live target constructor; computed affected derived handles are
  likewise admitted before receipts/effect routing.
- Removed the old public raw affected-id accessors from write receipts, batch
  receipts, intent receipts, and unified batch inspection. Added typed target
  accessors plus explicitly named terminal projection helpers.
- Effect routing, receipt aggregation, graph composition counters, feedback
  counters, and intent delivery counters now consume typed affected target
  carriers instead of treating terminal view-id strings as runtime authority.
- Added aspect-native compile-fail coverage proving the removed public
  affected-id receipt APIs stay removed.
- Updated the aspect API finalization insert/update/delete lane to use
  `delete_with(...)` and explicit aspect touches because bridge writeback now
  correctly rejects a bare delete with no admitted aspect operation.
- Verification passed: deliberate red then green `cargo check -p forge-query
  --tests`, overwrite and normal `cargo test -p forge-query --test
  aspect_native_query_compile_fail`, focused live mutation receipt routing,
  batch receipt aggregation, aspect API finalization preview rows, all
  `batch --lib` tests, focused effect delivery routing, focused
  `computed --lib`, and slice-specific line counts. The earlier full
  `live --lib` filter was not rerun after expectation updates; the directly
  failing live insert receipt test passed after the fix.

## Query write-command public variant construction fence

- Marked every `ForgeQueryWriteCommand` struct variant as `#[non_exhaustive]`.
  Runtime internals can still construct and match command phases, but
  public/facade callers can no longer mint lower-level command proof state with
  enum variant literals such as `ForgeQueryWriteCommand::Delete { ... }`.
- Added `write_command_variant_construction_not_public.rs` to the
  aspect-native trybuild harness. The fixture proves external callers receive
  `E0639: cannot create non-exhaustive variant using struct expression`.
- Updated the older declared-aspect-path negative fixture so it takes a command
  parameter instead of constructing `Delete` directly as setup.
- Split command tail accessors into
  `runtime/surface/mutation/command/accessors.rs`, bringing
  `runtime/surface/mutation/command.rs` back under the 400-line cap after the
  variant fences.
- Verification passed: `cargo fmt -p forge-query`, `cargo check -p
  forge-query --tests`, normal `cargo test -p forge-query --test
  aspect_native_query_compile_fail`, focused `mutation::aspect_crud --lib`,
  focused `runtime_write --lib`, and touched-file line counts. Plain cargo
  check stayed green because remaining command literal construction is
  crate-internal; the public facade break is enforced by trybuild.

## Query verified existing-truth assertion runtime minting fence

- Converted the public
  `ForgeQueryRuntimeExistingTruthVerificationAdapter::verify_existing_truth_assertion`
  hook from proof minting to pure verification. External adapters now return
  `Ok(())` or `ForgeQueryExistingTruthAssertionDenial`; they no longer return
  `ForgeQueryVerifiedExistingTruthAssertion`.
- `ForgeQueryBridgeBackedRuntimeBackend` now owns the verified assertion
  transition after adapter success, using `current_snapshot_identity()` as the
  proof basis.
- Restricted
  `ForgeQueryVerifiedExistingTruthAssertion::from_snapshot_identity(...)` to
  `pub(in crate::runtime)` and added the
  `verified_existing_truth_assertion_snapshot_constructor_not_public.rs`
  trybuild fixture proving facade callers cannot call it.
- Migrated certification, runtime test, and public bridge existing-truth
  adapters so they validate native aspect values and return success instead of
  fabricating snapshot-backed assertion proof objects.
- Verification passed: deliberate red then green `cargo check -p forge-query
  --tests`, `cargo fmt -p forge-query`, normal `cargo test -p forge-query
  --test aspect_native_query_compile_fail`, focused `verify_existing --lib`,
  focused `probe_existing --lib`, `public_bridge_runtime_bootstrap`, and
  touched-file line counts. Law 41 is doing the enforcement here: verification
  is the adapter act; proof-state transition belongs to the runtime.

## Query retained-upstream declared lookup handle fence

- Removed public raw-name declared-upstream lookup from
  `ForgeQueryRetainedUpstreamInputs`. Public maintainers can no longer call
  `declared_live_rows(...)`, `declared_retained_computed_rows(...)`, or
  `single_declared_retained_computed_row(...)` with arbitrary view-name text.
- Added handle-shaped public replacements:
  `declared_live_rows_for(...)`,
  `declared_retained_computed_rows_for(...)`, and
  `single_declared_retained_computed_row_for(...)`.
- Added aggregate declared-row-set iterators,
  `declared_live_row_sets(...)` and
  `declared_retained_computed_row_sets(...)`, for maintainers that need to
  count or snapshot every admitted upstream without naming one by string.
- Deliberate red `cargo check -p forge-query --tests` exposed the expected raw
  consumers in runtime computed maintainers and program support. The migrated
  maintainers now either use declaration-owned row-set iterators or capture
  concrete upstream handles before reading specific upstream rows.
- Extended the retained-upstream trybuild fixture so facade callers cannot call
  the removed raw declared lookup methods.
- Verification passed: deliberate red then green `cargo check -p forge-query
  --tests`, `cargo fmt -p forge-query`, trybuild overwrite plus normal
  `cargo test -p forge-query --test aspect_native_query_compile_fail`, focused
  `retained_upstreams --lib`, focused `derived_materialization_bundle --lib`,
  and a scan proving the removed raw declared lookup method names survive only
  in the negative fixture. The two over-400 touched computed files are already
  explicitly listed in the CI line-cap allowlist.

## Query artifact target-name projection fence

- Removed public neutral `target_view_names()` from retained materialization
  bundles, live artifact bundles, retained artifact bindings, and live artifact
  bindings. Public callers can no longer enumerate artifact target authority as
  plain view-name strings.
- Added typed `targets()` surfaces over
  `ForgeQueryDerivedMaterializationTarget` and `ForgeQueryLiveArtifactTarget`,
  plus explicit `terminal_target_view_names_projection()` methods for
  reporting/digest/source-reference boundaries.
- Removed retained-upstream `live_view_names()` and `computed_view_names()`
  enumeration from the public retained-upstream input surface.
- Converted projection-consumption retained/live extraction to iterate typed
  binding targets and resolve through target-shaped helpers. Source constructors
  now project names only at the terminal source-reference boundary.
- Removed raw-name binding lookup from live and derived artifact bindings.
  Removing the derived lookup deliberately exposed retained scalar facts and
  scalar alignment as hidden string consumers; those now thread
  `ForgeQueryDerivedMaterializationTarget` internally.
- Extended trybuild enforcement for removed bundle/binding target-name
  enumeration, removed binding raw-name lookup, and removed retained-upstream
  name enumeration.
- Verification passed: deliberate red then green `cargo check -p forge-query
  --tests`, `cargo fmt -p forge-query`, trybuild overwrite plus normal
  `cargo test -p forge-query --test aspect_native_query_compile_fail`, focused
  `derived_materialization_bundle --lib`, `post_write_artifact --lib`,
  `retained_scalar --lib`, `projection_consumption --lib`, final green
  `cargo check -p forge-query --tests`, and touched-file line counts. Only the
  already allowlisted computed files are over 400 lines.

## Query command declared-collection identity fence

- Removed public command/backend-admissible collection string authority:
  `ForgeQueryWriteCommand` and `ForgeQueryBackendAdmissibleMutation` no longer
  expose `declared_collection()` or `declared_collection_ref()`.
- Added `declared_collection_identity()` over
  `ForgeQueryMutationTargetCollectionIdentity`, and migrated consumer-kit
  backend admission, intent-admission seeds/handoffs, lower-runtime subject
  digesting, runtime write/batch/preview receipts, graph descriptor validation,
  public bridge adapters, and runtime authorities to carry identity proof
  instead of rebuilding collection targets from text.
- Kept command terminal projection crate-internal for receipt/report/legacy
  symbolic labels and removed the unused backend-admissible terminal projection.
- Added the `mutation_declared_collection_string_accessors_removed.rs`
  compile-fail fixture proving public callers cannot call the old string
  accessors or the command terminal projection escape hatch.
- Verification passed: deliberate red then green `cargo check -p forge-query
  --tests`, `cargo fmt -p forge-query`, trybuild overwrite plus normal
  `cargo test -p forge-query --test aspect_native_query_compile_fail`, focused
  identity seed, batch handoff, graph descriptor validation, and
  `public_bridge_runtime_bootstrap` tests, plus scans and touched-file line
  counts. This leans into Law 41: mutation target collection state is now a
  stage proof carrier, not a public terminal string.

## Query retained scalar target proof fence

- Converted retained scalar fact-set/alignment proof objects to retain
  `ForgeQueryDerivedMaterializationTarget` instead of detached view-name
  strings. Public access is now `target()`, `left_target()`, and
  `right_target()`.
- Narrowed derived/live artifact target `view_name()` aliases to crate-internal
  visibility and added explicit public `terminal_view_name_projection()` for
  reporting/export text.
- Added compile-fail fixtures proving facade callers cannot call target
  `view_name()` or retained scalar fact/alignment raw view-name aliases.
- Verification passed: `cargo check -p forge-query --tests`, `cargo fmt -p
  forge-query`, trybuild overwrite plus normal
  `cargo test -p forge-query --test aspect_native_query_compile_fail`, focused
  `retained_scalar --lib`, final `cargo check -p forge-query --tests`, scans,
  and line counts. This is another Law 41 tightening: retained scalar evidence
  preserves target proof and only projects terminal names at named boundaries.

## Query write receipt collection identity fence

- Converted `ForgeQueryWriteReceipt` declared/target collection state to
  `ForgeQueryMutationTargetCollectionIdentity` carriers. Public receipt
  authority is now `declared_collection_identity()` and
  `target_collection_identity()`.
- Removed public write-receipt `declared_collection()` and
  `target_collection()` aliases. String labels are available only through the
  explicit terminal projection methods.
- Migrated direct writes, batch summaries, intent receipt routing, preview
  receipts, graph lineage, inspection/digest construction, and public
  submission assertions to identity/projection-aware access.
- Added trybuild fixture
  `write_receipt_collection_string_aliases_removed.rs` proving facade callers
  cannot call the removed neutral receipt string aliases.
- Verification: deliberate red then green `cargo check -p forge-query --tests`,
  `cargo fmt -p forge-query`, trybuild overwrite plus normal compile-fail
  harness, focused `mutation::aspect_crud --lib`, focused
  `mutation::aspect_preview --lib`, `public_submission_lane_replacements`,
  final green `cargo check -p forge-query --tests`, scans, and line counts.
  Focused `mutation::delete --lib` remains red before receipt assertions on
  the existing delete/effect-admission path:
  `bridge writeback effect intent requires at least one admitted concrete
  aspect operation`.

## Query whole-delete writeback empty-patch proof fence

- Removed the Query-only rejection of empty native
  `AuthoritativeRecordAspectPatch` values during bridge writeback effect intent
  construction. Foundational and bridge already treat the empty patch as a
  valid native carrier, so Query should not require fake concrete aspect
  operations for whole-entity deletes.
- Added backend receipt coverage proving admitted `ForgeQueryWriteCommand::Delete`
  builds bridge authority with an authoritative-patch canonical basis and no
  set/clear operations.
- Verification: `cargo fmt -p forge-query`, focused
  `bridge_writeback_effect_intent --lib`, focused `mutation::delete --lib`,
  final green `cargo check -p forge-query --tests`, removed-denial scan, and
  line counts. The previously red delete-focused test is green now.

## Query existing-truth binding target collection identity fence

- Removed public `ForgeQueryExistingTruthTargetBinding::target_collection()`;
  authority now flows through `target_collection_identity()`, with
  `terminal_target_collection_projection()` reserved for named labels at
  reporting/adapter boundaries.
- Migrated backend authority, backend-admissible declared collection
  derivation, authoritative mutation routing, batch write receipt rows,
  runtime write reporting, probe/assertion digests, mutation evidence,
  previews, workspace graph errors, graph-composition labels, and bridge test
  adapters to identity/projection-aware access.
- Split denial support into `existing_truth_denial.rs` to keep the touched
  binding root under the workspace Rust line cap.
- Added trybuild fixture
  `existing_truth_binding_target_collection_alias_removed.rs`.
- Verification passed: `cargo fmt -p forge-query`, green
  `cargo check -p forge-query --tests`, green
  `cargo test -p forge-query --test aspect_native_query_compile_fail`, focused
  green `cargo test -p forge-query existing_truth --lib`, alias scans, and
  line counts. This is a Law 41 enforcement slice: existing-truth collection
  proof is typed identity state until an explicit terminal boundary.

## Query admitted aspect/touch root break

- Switched from incremental alias trimming to a deliberate red-first root
  break. Staged `cargo check -p forge-query --tests` failures reported 12,
  then 70, then 143 errors before Rust stopped, exposing consumers across
  backend adapters, writeback effects, computed/effect routing, preview,
  inspection, graph-composition descriptors, support runtimes, and harness
  rows.
- Removed neutral command/backend-admissible bag aliases:
  `aspect_values()`, `asserted_aspect_values()`, and `touched_aspects()`.
  Replacements are `admitted_aspect_values()`,
  `asserted_admitted_aspect_values()`, and `admitted_touched_aspects()`.
- Removed/renamed `touched_aspects()` on deltas, live patches, refresh
  contexts, batch receipts/inspections, component inspections, and graph touch
  descriptor rows to force admitted-touch wording throughout the pipeline.
- Renamed `ForgeQueryAspectTouch::native_digest_part()` to
  `admitted_touch_digest_part()` so digest/report consumers name the terminal
  digest role explicitly.
- Added trybuild fixtures for removed command/backend native bag aliases,
  mutation-delta touched-aspect alias, and aspect-touch digest alias; refreshed
  older aspect-value fixtures to use `admitted_aspect_values()` before proving
  aspect-value path/JSON aliases are still gone.
- Verification passed: `cargo fmt -p forge-query`, final green
  `cargo check -p forge-query --tests`, trybuild overwrite plus normal
  `cargo test -p forge-query --test aspect_native_query_compile_fail`, focused
  green `mutation::aspect_crud --lib`, green `public_bridge_runtime_bootstrap`,
  scans, and line counts. Lesson: this is the desired faster rhythm for the
  refactor: break roots deeply, use compiler red as the map, then sweep.

## Query aspect mutation builder set-aspect naming fence

- Committed and pushed the large aspect-native Query checkpoint as `5936ef69`
  on `query-repair`.
- Renamed mutation authoring builder `.aspect(...)` to `.set_aspect(...)` for
  both `ForgeQueryAspectMutationBuilder` and graph relation mutation builders,
  making set semantics explicit before the later desired-aspect proof-carrier
  break.
- Used a scoped search/replace only for touch-valued builder call shapes,
  deliberately leaving schema/test-backend string `.aspect(...)` APIs untouched.
- The root break produced 336 compiler-reported builder call errors; the scoped
  replacement updated 83 Rust files and returned
  `cargo check -p forge-query --tests` to green.
- Added an aspect-native trybuild fixture proving the old mutation-builder
  `.aspect(touch, value)` alias is gone.
- Verification: `cargo fmt -p forge-query`,
  `cargo test -p forge-query --test aspect_native_query_compile_fail`,
  `cargo check -p forge-query --tests`, focused
  `cargo test -p forge-query mutation::aspect_crud --lib`, and targeted residue
  scans passed.

## Query aspect touch constructor naming fence

- Broke the public `ForgeQueryAspectTouch::aspect(...)` and
  `ForgeQueryAspectTouch::field_path(...)` constructors, replacing them with
  `whole_aspect(...)` and `aspect_field_path(...)` to make whole-aspect versus
  aspect-field touch construction explicit at the root carrier.
- Ran a deliberate red `cargo check -p forge-query --tests`; rustc surfaced the
  expected constructor consumers and suggested the new names.
- Applied an exact scoped search/replace across `forge-query` source and tests,
  updating 36 files with no broad `.aspect(...)` accessor churn.
- Added `aspect_touch_neutral_constructors_removed.rs` as a trybuild fence
  proving the old neutral constructor names stay unavailable; refreshed the raw
  string constructor stderr because rustc now suggests the new names.
- Verification passed: `cargo fmt -p forge-query`,
  `cargo test -p forge-query --test aspect_native_query_compile_fail`,
  `cargo check -p forge-query --tests`, and targeted residue scans.

## Query existing-truth probe field constructor proof-state rename

- Broke `ForgeQueryExistingTruthProbeField::new_native(...)` and replaced it
  with `from_admitted_aspect_touch(...)`, so existing-truth backend probe rows
  name the proof state of the touch they consume instead of the vague native
  carrier category.
- Ran deliberate red `cargo check -p forge-query --tests`; rustc surfaced only
  the expected existing-truth probe field constructor consumers and suggested
  `from_admitted_aspect_touch`.
- Applied an exact scoped search/replace across the existing call sites, then
  added `existing_truth_probe_field_new_native_removed.rs` as a trybuild fence
  proving the old constructor spelling stays unavailable.
- Verification passed: `cargo fmt -p forge-query`,
  `cargo test -p forge-query --test aspect_native_query_compile_fail`,
  `cargo check -p forge-query --tests`, focused
  `cargo test -p forge-query existing_truth --lib`, and a targeted residue scan
  that only finds `new_native` in the compile-fail fixture/stderr.

## Query aspect value string ingress

- Corrected focus back to the real aspect-native goal: removing JSON-shaped /
  foundational scalar construction from Query writers rather than polishing
  architecture names.
- Added `ForgeQueryAspectValue::string(...)` as the Query-owned ingress for
  string scalar aspect values, then mechanically replaced direct
  `AspectValue::String(...)` construction across Query source, harnesses, and
  tests. Remaining `AspectValue::String(...)` hits are read-side pattern
  matches/decoders plus the helper body itself.
- Used `cargo check -p forge-query --tests` as the red/green driver; it exposed
  stale `.into()` chains, accidental pattern-match rewrites, and dead imports.
- Verification passed: `cargo check -p forge-query --tests`,
  `cargo test -p forge-query --test aspect_native_query_compile_fail`, and a
  constructor residue scan.

## Query mutation builder authored value fence

- Broke `ForgeQueryAspectMutationBuilder::set_aspect(...)` so public mutation
  authoring no longer accepts raw foundational `AspectValue`; callers must pass
  `ForgeQueryAuthoredAspectValue`.
- Added `ForgeQueryAuthoredAspectValue` as the public authored scalar wrapper
  and exported it through runtime/facade surfaces. The builder lowers it into
  the existing `ForgeQueryAspectValue` proof carrier internally.
- Kept `ForgeQueryAspectValue::string(...)` as the Query-owned foundational
  scalar helper for read/probe/retained/materialized contexts. A large compiler
  red map exposed the important distinction: authored mutation ingress is not
  the same phase as retained/read scalar truth.
- Split mixed test helpers into `authored_text(...)` for `.set_aspect(...)`
  and foundational `text(...)`/`test_string_aspect_value(...)` for assertions,
  probes, retained rows, snapshot reads, and bridge writeback scalar patches.
- Added `aspect_mutation_builder_rejects_foundational_value.rs` trybuild
  coverage proving facade callers cannot pass `AspectValue::Null` directly to
  `.set_aspect(...)`.
- Verification passed: `cargo fmt -p forge-query`, green and quiet
  `cargo check -p forge-query --tests`, normal green
  `cargo test -p forge-query --test aspect_native_query_compile_fail`, and
  focused green `cargo test -p forge-query mutation::aspect_crud --lib`.

## Query support document terminal JSON boundary fence

- Removed neutral support-document JSON API names from support snapshot and
  support pinning: `from_json`, `to_canonical_json`, `to_stable_json`,
  `load_support_snapshot_document`, and `load_support_pin_contract_document`.
- Replaced them with explicit terminal-document names:
  `from_terminal_json_document`,
  `to_canonical_terminal_json_document`,
  `to_stable_terminal_json_document`,
  `load_support_snapshot_terminal_json_document`, and
  `load_support_pin_contract_terminal_json_document`.
- Added `support_document_neutral_json_api_removed.rs` trybuild coverage
  proving facade callers cannot import/call the old neutral JSON names. Normal
  trybuild now proves the fence.
- Verification passed: `cargo fmt -p forge-query`,
  `cargo check -p forge-query --tests`, focused
  `cargo test -p forge-query support_snapshot --lib`, focused
  `cargo test -p forge-query support_pinning --lib`, and
  `cargo test -p forge-query --test aspect_native_query_compile_fail`.

## Query support document carrier visibility fence

- Removed `ForgeQuerySupportSnapshotDocument` and
  `ForgeQuerySupportPinContractDocument` from public `consumer_kit`, facade,
  and crate-root exports.
- Made both document structs crate-private and narrowed snapshot/contract
  `to_document()` helpers to crate-private.
- Public callers now either hold native support snapshot / support pin contract
  objects or explicit terminal JSON document text; they cannot name the
  serde-shaped document carrier as a public object.
- Refreshed support snapshot/pinning trybuild fixtures so document carrier
  construction is now denied by unresolved public imports instead of merely
  private fields.
- Verification passed: `cargo fmt -p forge-query`,
  `cargo check -p forge-query --tests`,
  `cargo test -p forge-query --test support_snapshot_facade`,
  `cargo test -p forge-query --test support_pinning_facade`,
  `cargo test -p forge-query --test aspect_native_query_compile_fail`,
  `cargo test -p forge-query support_snapshot --lib`, and
  `cargo test -p forge-query support_pinning --lib`.

## Query support terminal document text proof fence

- Added explicit terminal-document carriers:
  `ForgeQuerySupportSnapshotTerminalJsonDocument` and
  `ForgeQuerySupportPinContractTerminalJsonDocument`.
- Support snapshot/pinning terminal loaders now accept those carriers instead
  of bare `&str`; canonical/stable terminal export methods return the carriers
  instead of anonymous `String`.
- Updated support tests and facade trybuild examples to carry terminal JSON
  through the new proof types.
- Added `support_terminal_json_document_rejects_bare_string.rs` to prove facade
  callers cannot pass raw string JSON directly to either terminal loader.
- Verification passed: `cargo fmt -p forge-query`,
  `cargo check -p forge-query --tests`,
  `cargo test -p forge-query --test aspect_native_query_compile_fail`,
  `cargo test -p forge-query support_snapshot --lib`,
  `cargo test -p forge-query support_pinning --lib`,
  `cargo test -p forge-query --test support_snapshot_facade`, and
  `cargo test -p forge-query --test support_pinning_facade`.

## Query aspect closeout JSON authority wording fence

- Updated the executable aspect API closeout artifact and
  `_docs/forge-query/aspect-api-finalization-closeout.md` so they no longer say
  JSON removal is only future lower-crate work.
- New closeout wording treats terminal document JSON and external reference
  artifacts as non-authority import/export evidence, while JSON-shaped
  authority remains forbidden for downstream runtimes.
- Renamed the embedded Worth support-pin reference constant to
  `WORTH_QUERY_SUPPORT_PINS_TERMINAL_JSON_DOCUMENT`.
- Verification passed: `cargo fmt -p forge-query`,
  `cargo check -p forge-query --tests`, focused
  `cargo test -p forge-query runtime_public_aspect_api_finalization_closeout --lib`,
  focused `cargo test -p forge-query consumer_kit_closure --lib`, and a
  production scan showing only support snapshot/pinning terminal document
  serde encode/decode calls outside tests.

## Query phase-boundary raw JSON fixture cleanup

- Removed stale raw `serde_json` construction from the top-level
  phase-boundary compile-fail fixtures:
  `runtime_aspect_mutation_privates_forbidden.rs` and
  `runtime_payload_first_insert_command_missing.rs`.
- The first now proves `ForgeQueryAspectValue` cannot be public-struct-literal
  constructed without re-teaching path-plus-JSON fields; the second proves the
  payload-first `ForgeQueryWriteCommand::Insert` variant is unavailable without
  constructing a raw JSON payload.
- Refreshed affected trybuild stderr, including stale graph-composition and
  intent-receipt snapshots exposed by the overwrite pass.
- Verification passed: `cargo fmt -p forge-query`,
  `cargo check -p forge-query --tests`, and
  `cargo test -p forge-query --test phase_boundaries_compile_fail`.

## Query support external terminal ingress proof split

- Split support terminal JSON into native-derived terminal projection carriers
  and weaker external terminal ingress carriers.
- `ForgeQuerySupportSnapshotTerminalJsonDocument` and
  `ForgeQuerySupportPinContractTerminalJsonDocument` are now produced from
  native support exports; loaders accept only
  `ForgeQueryExternalSupportSnapshotTerminalJsonDocument` and
  `ForgeQueryExternalSupportPinContractTerminalJsonDocument`.
- Round-trip support tests now explicitly call
  `to_external_terminal_json_document()` before loading, so export and external
  ingress are no longer one flat proof state.
- Public facade exports include the new external ingress carriers, and the
  compile-fail bare-string fixture now proves the loaders reject raw `&str`
  because they require external terminal document proof types.
- Verification passed: `cargo fmt -p forge-query`,
  `cargo check -p forge-query --tests`,
  `cargo test -p forge-query --test aspect_native_query_compile_fail`,
  `cargo test -p forge-query support_snapshot --lib`,
  `cargo test -p forge-query support_pinning --lib`,
  `cargo test -p forge-query --test support_snapshot_facade`, and
  `cargo test -p forge-query --test support_pinning_facade`.

## Query embedded support pin terminal JSON source typing

- Converted the embedded Worth `query_support_pins.json` reference from a raw
  source string constant into
  `ForgeQueryExternalSupportPinContractTerminalJsonDocument`.
- Added static external-terminal constructors for the support terminal document
  carriers so embedded terminal artifacts can carry the same proof state as
  runtime external ingress.
- Changed consumer-kit closure certification sources from raw source text to
  `ForgeQueryConsumerKitCertificationSourceText`; the Worth support pin JSON
  row is now explicitly
  `ExternalSupportPinContractTerminalJsonDocument` while ordinary source files
  are `StaticSource`.
- Verification passed: `cargo fmt -p forge-query`,
  `cargo check -p forge-query --tests`,
  `cargo test -p forge-query consumer_kit_closure --lib`, and
  `cargo test -p forge-query consumer_residue --lib`.

## Query test backend schema aspect-touch storage

- Converted `ForgeQueryTestBackendSchema` internals from storing aspect labels
  as `String` to storing admitted `ForgeQueryAspectTouch` proof carriers.
- The public `.aspect(label, external_projection_path)` authoring method still
  accepts text, but parses and admits the touch at schema construction instead
  of retaining text and reparsing later in `memory_aspects()`.
- `schema.aspects()` now returns `(&ForgeQueryAspectTouch,
  &CanonicalFieldPath)`, and the aspect-native compile-fail fixture proves
  neither schema aspect touch nor projection path can be treated as `&str`.
- Verification passed: `cargo fmt -p forge-query`,
  `cargo check -p forge-query --tests`,
  `cargo test -p forge-query test_backend --lib`,
  `cargo test -p forge-query --test in_memory_test_backend_facade`, and
  `cargo test -p forge-query --test aspect_native_query_compile_fail`.

## Query graph obligation support-matrix static aspect proof

- Converted graph obligation support-matrix certification and selector
  perturbation representative touches from static raw aspect-path parsing to
  native `AspectKey` plus `ForgeQueryAspectTouch::whole_aspect`.
- Removed the support-matrix selector perturbation `aspect_touch(&str)` and
  `set_operation(&str)` helpers, so static `"capacity"` and `"boundary"` cases
  no longer rely on the authoring parser while assembling certification proof
  fixtures.
- Verification passed: `cargo fmt -p forge-query`,
  `cargo check -p forge-query --tests`,
  `cargo test -p forge-query graph_obligation --lib`,
  `cargo test -p forge-query --test graph_obligation_consumer_kit_facade`, and
  a targeted support-matrix residue scan for the removed authoring-parser calls
  and helper names.

## Query support hostile terminal JSON test quarantine

- Removed anonymous `TerminalSupport*DocumentJson = serde_json::Value` aliases
  from support snapshot and support pinning tests.
- Added test-only hostile terminal-document helpers for support snapshot and
  support pinning. Raw JSON mutation now lives behind
  `HostileSupportSnapshotTerminalDocument` and
  `HostileSupportPinContractTerminalDocument` instead of ordinary test bodies.
- Rewrote support snapshot terminal denial tests and support pinning stale /
  tampered / invalid terminal document tests to use named hostile mutation
  methods rather than direct JSON indexing.
- Verification passed: `cargo fmt -p forge-query`,
  `cargo check -p forge-query --tests`,
  `cargo test -p forge-query support_snapshot --lib`,
  `cargo test -p forge-query support_pinning --lib`, and a scan showing raw
  `serde_json::Value` in support snapshot/pinning only inside production
  terminal encode/decode files and the two hostile terminal-document test
  helpers.

## Query support terminal JSON codec boundary extraction

- Moved the remaining production support snapshot and support pinning
  `serde_json` encode/decode calls into explicitly named
  `terminal_json_codec.rs` modules.
- `ForgeQuerySupportSnapshotDocument` and
  `ForgeQuerySupportPinContractDocument` now delegate terminal JSON ingress /
  projection to those codec modules while keeping validation and native support
  proof construction in the document roots.
- Verification passed: `cargo fmt -p forge-query`,
  `cargo check -p forge-query --tests`,
  `cargo test -p forge-query support_snapshot --lib`,
  `cargo test -p forge-query support_pinning --lib`,
  `cargo test -p forge-query --test support_snapshot_facade`,
  `cargo test -p forge-query --test support_pinning_facade`, and a support
  scan showing production `serde_json` calls only in the two
  `terminal_json_codec.rs` files.

## Query intent-admission certification native aspect touches

- Removed static `ForgeQueryAspectTouch::from_authoring_path(...)` calls from
  `intent_admission/certification/fixtures`.
- Added `identity_id_touch()` and `title_value_touch()` helpers that build
  foundational `AspectKey`, `FieldKey`, and `CanonicalFieldPath` proof before
  constructing `ForgeQueryAspectTouch::aspect_field_path(...)`.
- Converted effect certification, routing seed/probe certification,
  authoritative intent execution, and legacy/canonical effect delegation parity
  fixtures to use those native touch helpers.
- Verification passed: `cargo fmt -p forge-query`,
  `cargo check -p forge-query --tests`,
  `cargo test -p forge-query intent_admission::certification --lib`,
  `cargo test -p forge-query --test intent_admission_public_dx`, and a fixture
  directory scan showing no remaining `from_authoring_path` calls.

## Query lower-runtime certification native aspect touches

- Removed static `ForgeQueryAspectTouch::from_authoring_path(...)` calls from
  the lower-runtime-routing certification surface fixture subtree.
- Added shared representative helpers for `title.value`, `status.value`, and
  `priority.value` that build foundational `AspectKey`, `FieldKey`, and
  `CanonicalFieldPath` before constructing `ForgeQueryAspectTouch`.
- Converted write-authority, signal-invalidation, projection-query-receipt,
  and causal-bridge representative fixtures to use those helpers.
- Verification passed: `cargo fmt -p forge-query`,
  `cargo check -p forge-query --tests`,
  `cargo test -p forge-query lower_runtime_routing::certification --lib`,
  `cargo test -p forge-query lower_runtime_routing --lib`, and a fixture
  subtree scan showing no remaining `from_authoring_path` calls.

## Query backend receipt test native aspect touches

- Removed static `ForgeQueryAspectTouch::from_authoring_path(...)` calls from
  `runtime/backend/receipts_tests.rs`.
- Added native receipt-test helpers for `title.value` and `status.value` using
  foundational `AspectKey`, `FieldKey`, `CanonicalFieldPath`, and
  `ForgeQueryAspectTouch::aspect_field_path(...)`.
- Verification passed: `cargo fmt -p forge-query`,
  `cargo check -p forge-query --tests`,
  `cargo test -p forge-query runtime::backend::receipts::tests --lib`,
  `cargo test -p forge-query bridge_writeback_effect_intent --lib`, and a
  targeted receipt-test scan showing no remaining static parser/string residue.

## Query aspect API finalization harness native aspect touches

- Removed static `ForgeQueryAspectTouch::from_authoring_path(...)` calls from
  the aspect API finalization certification harness.
- Added harness-level native touch helpers for `identity.id`, `title.value`,
  `description.value`, and `ui.batch_summary`, each built from foundational
  `AspectKey`, `FieldKey`, and `CanonicalFieldPath`.
- Converted canonical CRUD, typed-clear narrowing, batch, preview-batch, and
  duplicate-authoring rejection rows to use those helpers instead of local
  `aspect_touch(&str)` parser helpers.
- Verification passed: `cargo fmt -p forge-query`,
  `cargo check -p forge-query --tests`,
  `cargo test -p forge-query aspect_api_finalization_certification --lib`,
  `cargo test -p forge-query runtime_public_aspect_api_finalization_closeout --lib`,
  and targeted harness scans showing no `from_authoring_path`,
  `aspect_touch(...)`, or old dotted touch literal residue.

## Query runtime API stabilization transcript native aspect lowering

- Removed `ForgeQueryAspectTouch::from_authoring_path(...)` usage from the
  runtime API stabilization transcript harness.
- Added shared `transcript_aspect_touch(...)` lowering through foundational
  `AspectKey`, `FieldKey`, `CanonicalFieldPath`, and
  `ForgeQueryAspectTouch::aspect_field_path(...)`.
- Converted golden transcript computed dependencies, effect dependencies,
  authoritative transcript writes, and preview proof writes to use the native
  lowering helper instead of local `aspect_touch(...)` parser helpers.
- Verification passed: `cargo fmt -p forge-query`,
  `cargo check -p forge-query --tests`,
  `cargo test -p forge-query runtime_api_stabilization --lib`, and a targeted
  runtime-stabilization harness scan showing no remaining
  `from_authoring_path` or local `fn aspect_touch(...)` parser helpers.

## Query mutation evidence batch tests native aspect touch

- Removed the remaining static `ForgeQueryAspectTouch::from_authoring_path(...)`
  calls from `runtime/surface/mutation_evidence/batch/batch_tests.rs`.
- Added a native `title_value_touch()` helper using foundational `AspectKey`,
  `FieldKey`, `CanonicalFieldPath`, and
  `ForgeQueryAspectTouch::aspect_field_path(...)`.
- Verification passed: `cargo fmt -p forge-query`,
  `cargo check -p forge-query --tests`,
  `cargo test -p forge-query runtime::surface::mutation_evidence::batch --lib`,
  `cargo test -p forge-query existing_truth_mode_summary --lib`, and a targeted
  scan showing no `from_authoring_path` or `title.value` residue in the file.

## Query graph-composition fixture native touch lowering

- Removed the remaining `ForgeQueryAspectTouch::from_authoring_path(...)`
  dependence from the `runtime/mutation/graph_composition` test fixture
  subtree.
- Converted touch-descriptor, obligation registration, and obligation index
  fixture helpers to lower static fixture text through foundational
  `AspectKey`, `FieldKey`, and `CanonicalFieldPath` before constructing
  `ForgeQueryAspectTouch`.
- Fixed the helper to support both whole-aspect fixtures such as `weight` and
  aspect-field fixtures such as `topology.kind` without using Query's authoring
  parser.
- Verification passed: `cargo fmt -p forge-query`,
  `cargo check -p forge-query --tests`,
  `cargo test -p forge-query graph_composition --lib`,
  `cargo test -p forge-query graph_obligation --lib`, and a targeted
  graph-composition scan showing no remaining `from_authoring_path` or
  `aspect_path` hits.

## Query runtime support fixture native touch lowering

- Removed `ForgeQueryAspectTouch::from_authoring_path(...)` from shared runtime
  test support helpers, graph-composition assertion support, runtime command
  helpers, the test existing-truth verification adapter, and stop-class
  representative fixtures.
- Added shared `test_aspect_touch(...)` native lowering through foundational
  `AspectKey`, `FieldKey`, and `CanonicalFieldPath`.
- Converted memory-workspace unit tests and consumer-kit test-backend behavior
  tests away from local authoring-parser helpers to the same native fixture
  lowering pattern.
- Verification passed: `cargo fmt -p forge-query`,
  `cargo check -p forge-query --tests`,
  `cargo test -p forge-query runtime::tests::stop_class --lib`,
  `cargo test -p forge-query graph_composition --lib`,
  `cargo test -p forge-query memory_workspace --lib`,
  `cargo test -p forge-query test_backend --lib`, and a global parser scan
  showing remaining `from_authoring_path(...)` hits only in production
  authoring-ingress boundaries, private parser implementation, and compile-fail
  fixtures.

## Query authored touch ingress boundary naming

- Removed production `from_authoring_path(...)` vocabulary from Query aspect
  touch admission.
- Renamed the crate-private touch parser to
  `ForgeQueryAspectTouch::admit_authoring_ingress_text(...)` and the parsed
  target parser to `ForgeQueryParsedAspectTarget::parse_authoring_ingress_text(...)`.
- Updated `ForgeQueryTestBackendSchema::aspect(...)` and memory-workspace
  declared aspect matching to call the explicitly named authored-ingress
  boundary.
- Verification passed: `cargo fmt -p forge-query`,
  `cargo check -p forge-query --tests`,
  `cargo test -p forge-query test_backend --lib`,
  `cargo test -p forge-query memory_workspace --lib`, and a global parser scan
  showing production no longer contains `ForgeQueryAspectTouch::from_authoring_path`
  or `ForgeQueryParsedAspectTarget::from_authoring_path`.

## Query hostile support terminal test JSON carrier removal

- Removed raw `serde_json::Value` carriers from hostile support snapshot and
  support pinning terminal-document test helpers.
- The helpers now mutate native-produced terminal document text with bounded
  scalar replacements instead of carrying a general JSON object model.
- Verification passed: `cargo fmt -p forge-query`,
  `cargo check -p forge-query --tests`,
  `cargo test -p forge-query support_snapshot --lib`,
  `cargo test -p forge-query support_pinning --lib`, and a global JSON scan
  showing remaining `serde_json` only in the two production support terminal
  codec files plus three compile-fail raw JSON rejection fixtures.

## Query support terminal JSON allowlist enforcement

- Updated `_docs/forge-query/aspect_native_query_refactor.md` so the production
  JSON allowlist names the two surviving terminal codec files instead of the
  stale `None` row.
- Added
  `consumer_kit::support_snapshot::tests::runtime_boundary::production_serde_json_is_confined_to_support_terminal_codecs`,
  which scans production Query source and fails unless `serde_json` is confined
  to the support snapshot and support pinning terminal codec modules.
- Verification passed: `cargo fmt -p forge-query`,
  `cargo check -p forge-query --tests`,
  `cargo test -p forge-query production_serde_json_is_confined_to_support_terminal_codecs --lib`,
  `cargo test -p forge-query support_snapshot --lib`, and a global JSON scan
  showing production JSON confined to the terminal codecs.

## Query Foundational JSON compatibility guard

- Re-anchored the Query refactor plan around Forge Foundational as the owner of
  both aspect truth and transitional JSON compatibility.
- Added
  `support_terminal_json_codecs_do_not_become_aspect_compatibility_bridges`,
  which keeps the two surviving support terminal JSON codecs from importing
  foundational aspect contracts, aspect values, authoritative aspect carriers,
  or Foundational JSON compatibility lowering markers.
- Current stance: Query has no approved production JSON-as-aspect compatibility
  boundary. If a hard external JSON-as-aspect contract appears, it must
  immediately delegate to `forge_foundational::compatibility().json()` and
  return native validated or authoritative artifacts.
- Verification passed: `cargo fmt -p forge-query`,
  `cargo check -p forge-query --tests`, focused
  `cargo test -p forge-query support_terminal_json_codecs_do_not_become_aspect_compatibility_bridges --lib`,
  and a production scan showing `serde_json` still confined to the two support
  terminal codec files.

## Query admitted aspect value proof-carrier rename

- Renamed the remaining mutation value proof carrier from
  `ForgeQueryAspectValue` to `ForgeQueryAdmittedAspectValue` across production
  Query, facade exports, tests, integration harnesses, and compile-fail
  fixtures.
- Did not leave a compatibility alias, so consumers that still reach for the
  old weak name now fail to compile instead of preserving neutral vocabulary.
- The renamed carrier still stores parsed native desired-aspect state over
  foundational `AspectValue`; JSON is not part of its authority.
- Verification passed: `cargo fmt -p forge-query`,
  `cargo check -p forge-query --tests`,
  `cargo test -p forge-query --test aspect_native_query_compile_fail`,
  focused `cargo test -p forge-query support_terminal_json_codecs_do_not_become_aspect_compatibility_bridges --lib`,
  and a source/stderr scan showing no remaining `ForgeQueryAspectValue` in
  Query code or compile-fail outputs.

## Query admitted aspect value scalar factory privacy

- Removed public `ForgeQueryAdmittedAspectValue::string(...)`; it returned raw
  foundational `AspectValue`, not an admitted Query proof value.
- Replaced internal call sites with crate-local
  `ForgeQueryAdmittedAspectValue::native_string_value(...)`.
- Updated external integration fixtures to construct Foundational
  `AspectValue` directly where they assert retained/public bridge scalar
  material.
- Added compile-fail fixture
  `aspect_value_native_string_factory_private.rs` proving facade consumers
  cannot use the admitted proof carrier as a scalar factory.
- Verification passed: `cargo fmt -p forge-query`,
  `cargo check -p forge-query --tests`,
  `cargo test -p forge-query --test aspect_native_query_compile_fail`, and a
  residue scan showing external `native_string_value(...)` mentions only in the
  compile-fail fixture and expected stderr.

## Query memory workspace native field path vocabulary

- Renamed memory-workspace retained aspect state from
  `external_projection_path` to `native_field_path`, including
  `ForgeQueryAspect::native_field_path()` and the crate-local
  `ForgeQueryAspect::from_native_field_path(...)`.
- Converted test-backend schema storage and duplicate-path validation to
  `native_field_path`; the public authoring helper still accepts projection
  field path text but immediately lowers it to `CanonicalFieldPath`.
- Refreshed compile-fail coverage so the removed
  `external_projection_path()` accessor fails directly, while
  `native_field_path()` and `schema.aspects()` paths still cannot be treated as
  `&str`.
- Verification passed: `cargo fmt -p forge-query`,
  `cargo check -p forge-query --tests`,
  `cargo test -p forge-query --test aspect_native_query_compile_fail`,
  `cargo test -p forge-query memory_workspace --lib`,
  `cargo test -p forge-query test_backend --lib`, and residue scans showing
  production no longer contains `external_projection_path`,
  `from_native_external_projection_path`, or `external projection path`.

## Forge Store Roadmap 2 workspace scaffold

- Created and switched to branch `forge-store` to investigate rebuilding Forge
  Store from a cleaner foundation.
- Initial architecture read: Roadmap 1 is semantic durability; Roadmap 2
  requires a physical database foundation. Existing `crates/forge-store`
  already contains substantial semantic/code evidence and some S.0 work, but it
  is too large and entangled to use as the new physical foundation directly.
- Scaffolded nested workspace `workspaces/forge-store` with 36 small crates
  covering shared contracts, Roadmap 2 physical foundation, Roadmap 1 semantic
  durable programs, test support, certification, and a thin public facade.
  Verified with `cargo check --manifest-path workspaces\forge-store\Cargo.toml`.
