- worth-relational aspect refactor batch: made shared `aspect_wire::encode_aspect_value` and `encode_length_prefixed_aspect_value` infallible on the write side, matching the closed Milestone 1 `AspectValue` carrier matrix. Removed production encode-failure branches from diagnostics, grouped truth, storage comparison keys, merge/query/replay digests, witness keys, and presentation snapshot egress. Native strategy aspect-value encoding is now infallible; decode remains fallible. Adjusted worth-query test/certification callers of `encode_snapshot_aspect_read_value` after that helper became infallible. Verified with `cargo fmt -p worth-relational -p worth-query`, `cargo check -p worth-relational --tests`, `cargo check -p worth-query --tests`, `cargo check -p worth-runtime-bridge --tests`, `cargo test -p worth-relational aspect_value_canonical_codec -- --nocapture`, `cargo test -p worth-relational aspect_witness_digest -- --nocapture`, and `cargo test -p worth-relational grouped_projection -- --nocapture`.
- worth-relational diagnostic field-path carrier batch: changed `RelationalDiagnosticValue::FieldPath` from loose `Vec<FieldKey>` to foundational `CanonicalFieldPath`. Updated invariant diagnostic projection to carry the existing locator path instead of reconstructing from a slice, deleted the unused serde projection derive from that internal typed projection, and updated schema-continuity diagnostics plus replay diagnostic digest encoding to consume canonical field paths. Verified with `cargo fmt -p worth-relational`, `cargo check -p worth-relational --tests`, `cargo test -p worth-relational diagnostic_projection -- --nocapture`, `cargo test -p worth-relational schema_continuity -- --nocapture`, `cargo test -p worth-relational replay_digest -- --nocapture`, and `git diff --check`.
- worth-relational commit-strategy fixture cleanup: removed the remaining JSON-shaped byte fixtures under `crates/worth-relational/src/commit_strategies`, replacing them with native-looking opaque strategy bytes like `replicas=3`, `status=planned`, and `a=1`. This was test-surface detox only; strategy authority already uses typed native requests and `AspectFieldPatch`. Verified with `cargo fmt -p worth-relational`, `cargo check -p worth-relational --tests`, `cargo test -p worth-relational commit_strategies -- --nocapture`, focused residue scans, and `git diff --check`.
- worth-relational unmasked read surface cleanup: renamed raw read-record/projection bypasses, query digests, strategy observation counters, and runtime complexity counters to explicit `unmasked_*` names across `worth-relational` plus the `worth-query` memory workspace callsite. Typed projection methods (`entities<T>`, `entity<T>`, etc.) remain the projection-mask path; raw lanes are now mechanically visible for the next deeper split. Verified with `cargo fmt -p worth-relational -p worth-query -p worth-runtime-bridge`, `cargo check -p worth-relational --tests`, `cargo check -p worth-query --tests`, `cargo check -p worth-runtime-bridge --tests`, `cargo test -p worth-relational projection -- --nocapture`, `cargo test -p worth-relational visibility -- --nocapture`, exact-name residue scans, and `git diff --check`.
- worth-query row authority cleanup: made `WORTHQueryEntity` private-field/constructor-only and replaced the public JSON `payload` row surface with typed aspect-backed rows (`AspectValue` map plus explicit external projection). Memory workspace now builds rows from authoritative aspect state into typed aspect values first, with JSON retained only through `external_row()` / `into_external_row()` egress. Projection-consumption read-result extraction now consumes typed `AspectValue` material before falling back to external projections for synthetic/external rows. Updated runtime read composition, projection fixtures, stateful bridge support, public bridge integration support, and memory workspace tests away from direct row literals. Verified with `cargo fmt -p worth-query`, `cargo check -p worth-query --tests`, `cargo check -p worth-runtime-bridge --tests`, `cargo test -p worth-query memory_workspace -- --nocapture`, `cargo test -p worth-query projection_consumption -- --nocapture`, `cargo test -p worth-query read_composition -- --nocapture`, residue scans, and `git diff --check`.
- worth-relational diagnostic serde hard break: made `RelationalDiagnosticFields` typed-authority equality compare the typed `RelationalDiagnosticValue` tree directly instead of lossy external serde projection. External diagnostic serde projection is now terminal egress only: deserializing `RelationalDiagnosticFields` returns an explicit denial, and the external projection helper no longer has a JSON recovery visitor. Removed the external projection equality/recovery helpers and updated the diagnostic fields regression to assert typed authority is preserved while external JSON cannot be recovered as diagnostic fields. Verified with `cargo fmt -p worth-relational`, `cargo check -p worth-relational --tests`, `cargo test -p worth-relational diagnostic_serde_projection_is_terminal_egress_only -- --nocapture`, `cargo test -p worth-relational diagnostic_projection -- --nocapture`, residue scans, and `git diff --check`.
- worth-relational validation evaluator topology batch: deleted over-limit flat `relation_cardinality.rs` and `relation_traversal.rs`, replacing them with responsibility directories for cardinality maximum/minimum/minimum visible counts and traversal acyclicity/connectivity/planned successors/relation successors/traversal budget/visible entities. Preserved typed invariant violation fields and kept the touched slice free of JSON/payload/compatibility residue. Structural QA found that maximum cardinality collectors should consume one prepared relation scope instead of re-looking it up, and that connectivity minimum must not be relation-kind-scoped because source entity creation can violate it without touching a relation. Verified with `cargo fmt -p worth-relational`, `cargo check -p worth-relational --tests`, `cargo test -p worth-relational relation_integrity -- --nocapture`, `cargo test -p worth-relational cardinality -- --nocapture`, `cargo test -p worth-relational acyclic -- --nocapture`, `cargo test -p worth-relational connectivity -- --nocapture`, line-cap and residue scans, and `git diff --check`.
- worth-relational validation preparation planning topology batch: deleted over-limit flat `validation/execution/planning.rs` and replaced it with a `planning/` directory split into counters, execution-plan assembly, packet scope, packet selection, proof-boundary summary, test fault injection, and tests. Removed the unused `PlannedInvariantExecution` alias surfaced by `cargo check`. Preserved typed invariant packet registrations, proof-boundary summaries, preparation strategies, and relation-integrity plan-scope tests without adding JSON/payload/compatibility residue. Verified with `cargo fmt -p worth-relational`, `cargo check -p worth-relational --tests`, `cargo test -p worth-relational planner_ -- --nocapture`, `cargo test -p worth-relational relation_integrity -- --nocapture`, `cargo test -p worth-relational preparation -- --nocapture`, line-cap and residue scans, and `git diff --check`.
- worth-relational merge conflict topology batch: deleted over-limit flat `merge/logic/conflicts.rs` and replaced it with a `conflicts/` responsibility directory for candidate classification, authoritative visibility evidence, aspect evidence, strategy evidence, deletion/state classification, relation evidence, relation topology escalation, target-record resolution, and the existing ancestor record basis. Structural QA folded the tiny summary module back into `mod.rs` to keep the directory count disciplined. Preserved authoritative aspect-state comparison and foundational aspect locator/strategy evidence; touched slice has no JSON/payload/legacy/compatibility residue. Verified with `cargo fmt -p worth-relational`, `cargo check -p worth-relational --tests`, `cargo test -p worth-relational merge -- --nocapture`, line-cap and residue scans, and `git diff --check`.
- worth-relational merge lowering topology batch: deleted over-limit flat `merge/logic/lowering.rs` and replaced it with `merge/logic/lowering/` modules for aspect outcome lowering, record readiness, record intents, denial classification, record decisions, resolution/blocking, and decision-log digest basis. Preserved the aspect-native merge truth path through foundational aspect keys/values, lowered aspect contract plans, typed policy boundaries, and typed denial/execution bundles; no JSON/payload/legacy/compatibility residue in the touched lowering directory. Structural QA split denial classification out of `record_intents.rs` after the first pass found it becoming a mini-bucket. Verified with `cargo fmt -p worth-relational`, `cargo check -p worth-relational --tests`, `cargo test -p worth-relational merge -- --nocapture`, line-cap and residue scans, and `git diff --check`.
- worth-relational merge execution topology batch: deleted over-limit flat `merge/logic/execution.rs` and replaced it with `merge/logic/execution/` modules for prepared authority verification, bound executable plan compilation, executable record-plan compilation, aspect-plan materialization/witness digest compilation, and deleted-on-both-sides lineage continuity. Preserved aspect-native execution truth through foundational `AspectKey`, `LoweredAspectContractBinding`, typed `ExecutableAspectPlan`, `MaterializedAspectValue`, and canonical aspect witness digests; touched execution directory has no JSON/payload/legacy/compatibility residue. Verified with `cargo fmt -p worth-relational`, `cargo check -p worth-relational --tests`, `cargo test -p worth-relational merge -- --nocapture`, line-cap and residue scans, and `git diff --check`.
- worth-relational authority commit pipeline topology batch: deleted over-limit flat `authority/commit/pipeline.rs` and replaced it with an aspect-authority lifecycle directory: `transaction_entrypoint`, `authority_context`, `bulk_mutation_telemetry`, `draft_preparation_phase`, `invariant_phase`, `mutation_phase`, `history_resolution_phase`, `artifact_assembly_phase`, `publication_phase`, `complexity_delta`, and `rejection`. Preserved the single authoritative handoff through prepared working state, commit-boundary invariants, authoritative mutation, canonical foundational patch envelope assembly, durable append, and publication; no JSON/legacy/compatibility fallback was introduced. Structural QA forced a second extraction pass so `execute_authoritative_commit` became a phase table of contents instead of a new god function. Verified with `cargo fmt -p worth-relational`, `cargo check -p worth-relational --tests`, `cargo test -p worth-relational commit -- --nocapture`, line-cap and residue scans, and `git diff --check`.
- worth-relational commit-strategy execution data topology batch: deleted over-limit flat `commit_strategies/data/execution.rs` and replaced it with `execution/` modules for executor registration, canonical output artifacts, mutation-program proof carriers, execution drafts, observation context/metrics, visibility read-view enforcement, and local digest tests. Preserved native strategy outputs and mutation programs while keeping strategy observation on projection-mask/read-contract authority; no JSON/payload/legacy/compatibility residue was introduced. Verified with `cargo fmt -p worth-relational`, `cargo check -p worth-relational --tests`, `cargo test -p worth-relational commit_strategies -- --nocapture`, line-cap/residue scans, and `git diff --check`.
- worth-relational commit-strategy artifact proof topology batch: deleted over-limit flat `commit_strategies/data/artifacts.rs` and replaced it with `artifacts/` modules for merge descriptors, replay descriptors/runtime determinism basis, artifact bundle consistency validation, and local tests. Preserved typed `AspectFieldLocator` intent-scope targets and native replay/merge proof digests; no JSON/payload/legacy/compatibility residue was introduced. Verified with `cargo fmt -p worth-relational`, `cargo check -p worth-relational --tests`, `cargo test -p worth-relational commit_strategies -- --nocapture`, line-cap/residue scans, and `git diff --check`.
- worth-relational commit-strategy execution logic topology batch: deleted flat `commit_strategies/logic/execution.rs` and replaced it with an `execution/` directory for frozen executor registry proof, bound execution admission, read-contract admission, strategy invocation/panic containment, typed execution errors, and local execution tests. Preserved native strategy execution over projection-mask/read-contract authority; no JSON/payload/legacy/compatibility residue was introduced. Verified with `cargo fmt -p worth-relational`, `cargo check -p worth-relational --tests`, `cargo test -p worth-relational commit_strategies -- --nocapture`, line-cap/residue scans, and `git diff --check`.
- worth-relational schema continuity topology batch: deleted over-limit flat `schema/logic/continuity.rs` and replaced it with `continuity/` modules for typed transition validation, transition classification, canonical fingerprint construction, transition lowering, bundle admission, and typed continuity errors. Preserved typed foundational field carriers in schema-diff fingerprinting and typed continuity denial surfaces for durability/replay diagnostics; no JSON/payload/legacy/compatibility residue was introduced. Updated performance certification path metadata away from the deleted flat file. Verified with `cargo fmt -p worth-relational`, `cargo check -p worth-relational --tests`, `cargo test -p worth-relational schema_continuity -- --nocapture`, `cargo test -p worth-relational schema::continuity -- --nocapture`, line-cap/residue/stale-path scans, and `git diff --check`.
- worth-relational durability recovery access topology batch: deleted over-limit flat `durability/access.rs` and replaced it with `access/` modules for recovery-plan entrypoints, in-memory recovery planning, persisted checkpoint/tail-log selection, descriptor/canonical-basis authority continuity, typed schema-continuity issue mapping, descriptor version selection, and checkpoint/runtime basis mismatch detection. Preserved canonical envelope and descriptor-basis authority checks with typed continuity denials; no JSON/payload/legacy/compatibility residue was introduced. Updated the stale recovery-planning breadcrumb to the new access module. Verified with `cargo fmt -p worth-relational`, `cargo check -p worth-relational --tests`, `cargo test -p worth-relational durability -- --nocapture`, line-cap/residue/stale-path scans, and `git diff --check`.
- worth-relational performance authority observability topology batch: deleted the flat `performance/logic/access.rs` counter god file and replaced it with `performance/logic/access/` modules for test complexity observability, commit working-state/AOSOA/bulk/post-commit counters, validation counters, preparation counters, query projection counters, lineage counters, replay authority-basis counters, schema-continuity counters, merge counters, and inspection counters. Structural QA split the first pass further so read-query counters do not share a file with write-preparation counters, and lineage counters do not share a file with replay canonical-basis authority counters. Preserved the stable `PerformanceAccess` and `ReplayLineageAuthorityIndexedSource` exports while making descriptor/canonical-basis mismatch, merge policy basis, schema continuation, projection/read, and authoritative write observations spatially locatable; no JSON/payload/legacy/compatibility residue was introduced. Inspected `canonical_basis_ready_sequence.rs`; its `ready.payload()` call is a foundational readiness artifact accessor, not relational JSON authority. Verified with `cargo fmt -p worth-relational`, `cargo check -p worth-relational --tests`, `cargo test -p worth-relational complexity -- --nocapture`, `cargo test -p worth-relational performance -- --nocapture`, line-cap/residue/stale-path scans, and `git diff --check`.
- worth-relational performance data evidence topology batch: split the over-limit mixed `performance/data/mod.rs` into `complexity_contract.rs`, `runtime_complexity_counters.rs`, `complexity_contract_inventory.rs`, and a narrow export `mod.rs`. Preserved stable `ComplexityContract`, `ComplexityStatus`, `RuntimeComplexityCounters`, and test-only `COMPLEXITY_CONTRACTS` imports. Updated the complexity proof inventory away from stale deleted flat files (`logic/runtime/{transaction,apply,merge,read}.rs`, `validation/engine/evaluator.rs`, `inspection/logic/access.rs`, `merge/logic/{conflicts,execution}.rs`) to the current aspect-native authority modules such as `authority/commit/pipeline/transaction_entrypoint.rs`, `authority/mutation/*`, `visibility/materialization/read_records/reader/truth_record_access.rs`, `validation/engine/evaluator/{relation_endpoint_kind,relation_cardinality,relation_edge_rules}`, `inspection/logic/{graph,connectivity,retention,structural_identity,commit}.rs`, `visibility/retention/retention_authority.rs`, and `merge/logic/{conflicts/relation_topology,execution/authority_verification}.rs`. No JSON/payload/legacy/compatibility residue was introduced. Verification passed with `cargo fmt -p worth-relational`, `cargo check -p worth-relational --tests`, `cargo test -p worth-relational complexity -- --nocapture`, `cargo test -p worth-relational performance -- --nocapture`, line-cap/residue/stale-path scans, and `git diff --check` (only existing CRLF warnings).
- worth-relational schema continuity data descriptor topology batch: deleted over-limit flat `schema/data/continuity.rs` and replaced it with `continuity/` modules for descriptor semantics/canonical-basis support policies, boundary fingerprints, transition classifications/visibility/proof enums, field-key diff atoms, and transition/bridge/reconciliation descriptors. Preserved stable `schema::data` exports and typed foundational `FieldKey`/canonical-basis descriptor surfaces; no JSON/payload/legacy/compatibility residue was introduced. Compile QA restored ordering/hash derives on `FreeFormSchemaDiffIntent` because canonical fingerprint sorting compares free-form intent. Verified with `cargo fmt -p worth-relational`, `cargo check -p worth-relational --tests`, `cargo test -p worth-relational schema_continuity -- --nocapture`, `cargo test -p worth-relational schema::continuity -- --nocapture`, line-cap/residue/stale-path scans, and `git diff --check` (only existing CRLF warnings).
- worth-relational relation-integrity schema data topology batch: deleted over-limit flat `schema/data/relation_integrity.rs` and replaced it with `relation_integrity/` modules for typed contract IDs, declaration families, lowered execution-ready plans, plan catalogs, and canonical plan-revision digest basis. The plan-revision derivation now names each relation-integrity contract family as an explicit digest phase instead of hiding canonical schema authority in one broad helper body. Preserved stable `schema::data` exports and the typed relation-integrity schema authority surface; no JSON/payload/legacy/compatibility residue was introduced. Verified with `cargo fmt -p worth-relational`, `cargo check -p worth-relational --tests`, `cargo test -p worth-relational relation_integrity -- --nocapture`, `cargo test -p worth-relational schema:: -- --nocapture`, line-cap/residue/stale-path scans, and `git diff --check` (only existing CRLF warnings).
- worth-relational merge planning artifact topology batch: deleted over-limit flat `merge/logic/planning_artifact.rs` and replaced it with `planning_artifact/` modules for artifact materialization, schema semantic snapshots, explicit execution-authority contract, request/ancestry summaries, performance counters, and separate digest-basis builders for request/base/identity/causal, conflict evidence, policy aspect rows, and lowered aspect authorization rows. Preserved merge planning truth through typed aspect evidence keys/comparisons, policy ownership rows, lowered authorized aspect value surfaces, schema aspect-plan revisions, and canonical lowered artifact consumption; no JSON/payload/legacy/compatibility residue was introduced. Verified with `cargo fmt -p worth-relational`, `cargo check -p worth-relational --tests`, `cargo test -p worth-relational merge -- --nocapture`, line-cap/residue/stale-path scans, and `git diff --check` (only existing CRLF warnings).
- worth-relational lineage publication artifact topology batch: deleted over-limit flat `lineage/data/artifacts.rs` and replaced it with `artifacts/` modules for decision records, canonical decision logs, finalized event batches, finalization artifacts, published lineage artifacts, digest-basis rows, artifact counters, checkpoint artifacts, and test-only rejection artifacts. Preserved replay/durability lineage authority through canonical event ids, commit ids, event kinds, source/target ordering, decision classes, candidate ids, and rejection classes while keeping publication/checkpoint mutation separate from digest-basis construction; no JSON/payload/legacy/compatibility residue was introduced. Verified with `cargo fmt -p worth-relational`, `cargo check -p worth-relational --tests`, `cargo test -p worth-relational lineage -- --nocapture`, `cargo test -p worth-relational replay_digest -- --nocapture`, line-cap/residue/stale-path scans, and `git diff --check` (only existing CRLF warnings).
- worth-relational relation intent admission topology batch: deleted over-limit flat `authority/intent_merge/relation_validation.rs` and replaced it with `relation_validation/` modules for intent dispatch, relation creation/bulk creation admission, endpoint primitive admission, endpoint update admission, relation target admission, registered relation-kind schema admission, and existing relation identity scans/tests. Preserved typed relation topology authority through schema-controlled cross-context policy resolution, commit-scope live endpoint checks, stale relation target detection across working state/version basis, duplicate relation identity rejection, and adjacency-local relation identity scan counters without adding JSON/payload/legacy/compatibility residue. Updated the complexity proof inventory to the new relation creation admission path. Verified with `cargo fmt -p worth-relational`, `cargo check -p worth-relational --tests`, `cargo test -p worth-relational relation_validation -- --nocapture`, `cargo test -p worth-relational intent_merge -- --nocapture`, `cargo test -p worth-relational complexity_budget_relation_identity_validation_avoids_partition_scan -- --nocapture`, line-cap/residue/stale-path scans, and `git diff --check` (only existing CRLF warnings).
- worth-relational commit-strategy facade topology batch: removed the giant inline `#[cfg(test)]` universe from `commit_strategies/facade.rs`, leaving the public facade as a narrow API over native strategy canonicalization, execution, lowering, validation, and authoritative commit handoff. Added `commit_strategies/facade_tests/` modules for authority pipeline certification, successful replay/end-to-end strategy certification, merge strategy family conflicts, merge aspect-field conflict boundaries, replay recovery failures, and native strategy fixtures. Preserved native `AspectFieldPatch`/`AspectFieldLocator`/`AspectValue` strategy test coverage and introduced no JSON/payload/legacy/compatibility residue. Production Rust line-cap scan now shows only `crates/worth-relational/src/tests/**` files over 400 lines. Verified with `cargo fmt -p worth-relational`, `cargo check -p worth-relational --tests`, `cargo test -p worth-relational commit_strategies -- --nocapture`, line-cap/residue scans, and `git diff --check` (only existing CRLF warnings).
- worth-relational visibility complexity budget test topology batch: deleted over-limit flat `tests/complexity/contracts/visibility_budgets.rs` and replaced it with `visibility_budgets/` modules for snapshot visibility/cache pins, invariant materialization, retention/adjacency, partition-scoped projection, derived-index field lookup, and query packetization. Preserved typed aspect-native budget proofs through `AspectFieldLocator`, `AspectValue`, derived index parity, projection/query packet execution, authoritative visibility counters, and invariant materialization counters with no JSON/payload/legacy/compatibility residue. Verified with `cargo fmt -p worth-relational`, warning-clean `cargo check -p worth-relational --tests`, `cargo test -p worth-relational visibility_budgets -- --nocapture`, `cargo test -p worth-relational complexity_budget_ -- --nocapture`, line-cap/residue/stale-path scans, and `git diff --check` (only existing CRLF warnings).
- worth-relational commit complexity budget test topology batch: deleted over-limit flat `tests/complexity/contracts/commit_budgets.rs` and replaced it with `commit_budgets/` modules for contract registry coverage, partition-local commit counters, relation identity validation, unique invariant lookup, preparation packetization, relation-integrity counters, schema/CDC/recovery continuity, replay verification, and bulk-mutation admission. Moved relation-integrity and schema-transition setup into narrow fixture modules instead of leaving `mod.rs` as a fixture bucket. Preserved typed aspect-native proof surfaces through `AspectFieldPatch`, typed diagnostic-code denial assertions, authoritative replay/digest counters, and schema-continuity boundary counters with no JSON/payload/legacy/compatibility residue. Verified with `cargo fmt -p worth-relational`, warning-clean `cargo check -p worth-relational --tests`, `cargo test -p worth-relational commit_budgets -- --nocapture`, `cargo test -p worth-relational complexity_budget_ -- --nocapture`, line-cap/residue/stale-path scans, and `git diff --check` on the touched contracts area.
- worth-relational commit-strategy descriptor topology batch: deleted over-cap flat `commit_strategies/data/descriptor.rs` and replaced it with `descriptor/` modules for semantic name carriers, native read-contract law, and descriptor digest authority/deserialization. Public `commit_strategies::data` exports stayed stable while the descriptor record continues to deny digest drift at serde admission; no JSON/payload/legacy/compatibility residue was introduced. Removed the stale CI line-cap allowlist exemption for the deleted descriptor flat file. Verified with `cargo fmt -p worth-relational`, `cargo check -p worth-relational --tests`, `cargo test -p worth-relational commit_strategies -- --nocapture`, production line-cap/residue/stale-path scans, and `git diff --check` (only the existing CRLF warning on the allowlist file).
- worth-relational schema-continuity fingerprint topology batch: deleted over-cap flat `schema/logic/continuity/transition_canonical_fingerprint.rs` and replaced it with `transition_canonical_fingerprint/` modules for normalized transition rows, typed detail ordering, canonical hash writing, and transition-strength helpers. Preserved foundational `FieldKey` authority in schema diff details and deterministic canonical-basis preparation without JSON/payload/legacy/compatibility residue. Removed an older stale CI line-cap allowlist exemption for deleted `schema/logic/continuity.rs`. Verified with `cargo fmt -p worth-relational`, `cargo check -p worth-relational --tests`, `cargo test -p worth-relational schema_continuity -- --nocapture`, production line-cap/residue/stale-path scans, and `git diff --check` (only the existing CRLF warning on the allowlist file).
- worth-relational authoritative canonical delta engine topology batch: deleted over-cap flat `authority/mutation/canonical_deltas/engine.rs` and replaced it with `engine/` modules for mutation dispatch, entity authoritative delta evaluation, relation authoritative delta evaluation, state-view carriers, and canonical delta assembly. Preserved the aspect-native truth path through `AuthoritativeRecordAspectPatch`, `AuthoritativeRecordAspectState`, lowered aspect contract plans, typed `CanonicalDeltaError`, and foundational patch evidence with no JSON/payload/legacy/compatibility residue. Removed the stale CI line-cap allowlist exemption for the deleted engine file. Verified with `cargo fmt -p worth-relational`, `cargo check -p worth-relational --tests`, `cargo test -p worth-relational canonical_delta -- --nocapture`, `cargo test -p worth-relational mutation -- --nocapture`, production line-cap/residue/stale-path scans, and `git diff --check` (only the existing CRLF warning on the allowlist file).
- worth-relational merge data artifact topology batch: deleted the final known over-cap production file `merge/data/artifacts.rs` and replaced it with `merge/data/artifacts/` modules for aspect digest rows, execution authority contract, planning digest basis carriers, schema snapshot digest basis, planning artifact core, and typed inspection artifact admission. Preserved stable `merge::data` facade exports while keeping `AspectKey`, `AuthorizedAspectValueSurface`, typed lowered decisions, and merge inspection digests as the authority surfaces; no JSON/payload/legacy/compatibility residue was introduced. Removed the stale CI line-cap allowlist exemption for the deleted file. Verified with `cargo fmt -p worth-relational`, `cargo check -p worth-relational --tests`, `cargo test -p worth-relational merge -- --nocapture`, production line-cap/residue/stale-path scans, and `git diff --check` (only the existing CRLF warning on the allowlist file).
- worth-relational schema aspect test topology batch: deleted over-cap flat `tests/schema/aspects.rs` and replaced it with `tests/schema/aspects/` modules for declaration registration, identity declaration law, merge-policy admission, merge-policy trace exposure, merge-policy revision semantics, relation field contract shape, runtime lowering traces, and narrow foundational contract fixtures. Preserved tests around `AspectKey`, scalar/reference foundational contract shape, typed schema registry denials, merge-policy declarations, declaration/lowering diagnostic artifacts, and aspect plan lowering with no JSON/payload/legacy/compatibility residue. Removed the stale CI line-cap allowlist exemption for the deleted file. Verified with `cargo fmt -p worth-relational`, `cargo check -p worth-relational --tests`, `cargo test -p worth-relational schema::aspects -- --nocapture`, line-cap/residue/stale-path scans, and `git diff --check` (only the existing CRLF warning on the allowlist file).
- worth-relational transaction core test topology batch: deleted the over-cap flat `tests/transactions/core/mod.rs` body and replaced it with a module index plus responsibility files for runtime contracts, entity field-patch denials/evidence, struct field patch authority, commit artifact summaries, visibility aspect versions, publication trace diagnostics, parallel preparation, aspect patch publication, failure boundaries, snapshot/retention, bulk mutation planning, and same-commit relation endpoints. Preserved aspect-native proof surfaces through `AspectFieldPatch`, `AspectKey`, `FieldKey`, `AspectValue`, authoritative patch/state evidence, typed diagnostic denials, canonical field-path validation, projection/publication traces, and mutation-mask field-patch law without adding JSON/payload/legacy/compatibility residue. Removed the stale CI line-cap allowlist exemption for the deleted flat `mod.rs`; `relation_integrity.rs` remains the explicit over-cap next target. Verified with `cargo fmt -p worth-relational`, `cargo check -p worth-relational --tests`, `cargo test -p worth-relational transactions::core -- --nocapture`, line-cap/residue/stale-path scans, and `git diff --check` (only existing CRLF warnings).
- worth-relational relation-integrity transaction test topology batch: deleted the remaining over-cap `tests/transactions/core/relation_integrity.rs` god file and replaced it with `relation_integrity/` modules for endpoint-kind contract denials, cardinality commit denials, certification-boundary minimums, branch-local rejected commits, uniqueness, symmetry, endpoint-deletion integrity, contract counters, and narrow runtime fixtures. Preserved aspect-native relation integrity proof surfaces through `KindAspectContractDeclarations`, typed relation-integrity declarations, `AspectFieldPatch` relation specs, typed `InvariantViolationFields`, publication-stage certification failures, and branch-local authoritative patch/history evidence with no JSON/payload/legacy/compatibility residue. Removed the stale CI line-cap allowlist exemption; `transactions/core` now has no allowlisted files and all files are below 400 lines. Verified with `cargo fmt -p worth-relational`, `cargo check -p worth-relational --tests`, `cargo test -p worth-relational relation_integrity -- --nocapture`, transaction-core line-cap/residue/stale-path scans, and `git diff --check` (only existing CRLF warnings).
- worth-relational publication observability test topology batch: deleted over-cap flat `tests/publication/observability.rs` and replaced it with `observability/` modules for publication state snapshots, invariant diagnostics, execution-model aspect traces, basic harness publication artifacts, harness parity, lane metadata, failure visibility, preparation-failure certification, harness policy, mutation convergence, and shared fixtures. Preserved aspect-native proof surfaces through typed `RelationalDiagnosticValue` assertions, authoritative publication bundle/patch/replay surfaces, `AspectFieldPatch` fixture loading, aspect evaluation/emission trace parity, diagnostic delivery-class policy, and harness binary aspect snapshot evidence with no JSON/payload/legacy/compatibility residue. Removed the stale CI line-cap allowlist exemption and cleaned now-unused harness type reexports from test support. Verified with `cargo fmt -p worth-relational`, warning-clean `cargo check -p worth-relational --tests`, `cargo test -p worth-relational publication::observability -- --nocapture`, line-cap/residue/stale-path scans, and `git diff --check` (only existing CRLF warnings).
- worth-relational harness scenario runner topology batch: deleted over-cap flat `tests/harness/scenario/runner.rs` and replaced it with `runner/` modules for public scenario config/world types, deterministic operation selection, authoritative operation application, seeded/property orchestration, and MVCC/durable property runtime construction. Preserved aspect-native pressure through typed mutation intents, `AspectFieldPatch` replacement fields, publication checkpoints, explicit snapshot handles/releases, authoritative read refreshes, branch-local transactions, durable checkpoint/compaction operations, and CDC scenario replay consumers without JSON/payload/legacy/compatibility residue. Removed the stale CI line-cap allowlist exemption. Verified with `cargo fmt -p worth-relational`, warning-clean `cargo check -p worth-relational --tests`, `cargo test -p worth-relational publication::cdc::certification -- --nocapture`, line-cap/residue/stale-path scans, and `git diff --check` (only existing CRLF warnings).
- worth-relational CDC certification topology batch: deleted over-cap flat `tests/publication/cdc/certification.rs` and replaced it with `certification/` modules for snapshot pinning, savepoint stream truth, durable recovery/retention, schema boundary parity, seeded resume exactness, hostile resume pressure, property matrices, and narrow fixtures. Preserved aspect-native publication proof through `PublishedAuthoritativeRecordPatch`, subscriber continuation diagnostics/counters, canonical CDC/diff digest parity, and property replay truth; removed the fake always-false patch-detail check and replaced it with a real authoritative `name` aspect surface scan over foundational `AspectValue`/`InternedString` carriers. Removed the stale CI line-cap allowlist exemption. Verified with `cargo fmt -p worth-relational`, warning-clean `cargo check -p worth-relational --tests`, `cargo test -p worth-relational publication::cdc::certification -- --nocapture`, line-cap/residue/stale-path scans, and `git diff --check` (only existing CRLF warnings).
- worth-relational CDC subscriber-resume topology batch: deleted over-cap flat `tests/publication/cdc/subscriber_resume.rs` and replaced it with `subscriber_resume/` modules for checkpoint window batching, subscriber contract identity propagation, unchanged continuity, boundary visibility, contract-upgrade/renegotiation failures, normalized proof composition, mixed-boundary strongest-outcome handling, latest checkpoint continuation state, and crossed-boundary deduplication. Preserved typed aspect-native subscriber proof through `SubscriberResumeRequest`, `SubscriberContractDeclaration`, schema continuation descriptors/fingerprints, typed `RelationalDiagnosticValue` assertions, and canonical continuation proof metadata without JSON/payload/legacy/PatchRecord residue. Removed the stale CI line-cap allowlist exemption. Verified with `cargo fmt -p worth-relational`, warning-clean `cargo check -p worth-relational --tests`, `cargo test -p worth-relational publication::cdc::subscriber_resume -- --nocapture`, line-cap/residue/stale-path scans, and `git diff --check` (only existing CRLF warnings).
- worth-relational CDC savepoint-residue topology batch: deleted over-cap flat `tests/publication/cdc/savepoint_residue.rs` and replaced it with `savepoint_residue/` modules for subscriber CDC omission, nested savepoint patch/history/lineage/replay residue, and relation-integrity rollback diagnostic residue. Preserved existing aspect-native schema declaration edits by keeping `KindAspectContractDeclarations`/`aspect_contract_declarations`, `AspectFieldPatch` mutation fixtures, typed `DiagnosticCode` checks, direct aspect history/lineage history assertions, and canonical replay envelope lineage decisions without JSON/payload/legacy/PatchRecord or old `aspect_declarations` residue. Verified with `cargo fmt -p worth-relational`, warning-clean `cargo check -p worth-relational --tests`, `cargo test -p worth-relational publication::cdc::savepoint_residue -- --nocapture`, line-cap/residue/stale-path scans, and `git diff --check` (only existing CRLF warnings).
- worth-relational CDC subscriber-failure denial topology batch: deleted mixed flat `tests/publication/cdc/subscriber_failures.rs` and replaced it with `subscriber_failures/` modules for request admission, checkpoint identity/coverage, normalized continuation proof ceiling, continuation-summary coherence, durable descriptor-version mismatch, and authoritative boundary-binding mismatch. Preserved typed subscriber failure proof through `SubscriberResumeRequest`, `SubscriberCheckpoint` test proof mutators, `NormalizedContinuationProof`, `SubscriberContinuationSummary`, `SchemaBoundaryFingerprint`, typed schema transition fixtures with foundational `FieldKey`, and typed diagnostic assertions via `DiagnosticCode`/`RelationalDiagnosticValue`; no JSON/payload/legacy/PatchRecord or old declaration residue was introduced. Verified with `cargo fmt -p worth-relational`, warning-clean `cargo check -p worth-relational --tests`, `cargo test -p worth-relational publication::cdc::subscriber_failures -- --nocapture`, line-cap/residue/stale-path scans, and `git diff --check` (only existing CRLF warnings).
- worth-relational query derived-index test topology batch: deleted over-cap flat `tests/query/indexes.rs` and replaced it with `tests/query/indexes/` modules for branch-scope authority, entity field lookup, relation field lookup, parity/observability counters, and recovery/execution-model continuity. Preserved aspect-native proof surfaces through `AspectFieldLocator`, foundational `AspectValue`, typed `QueryAccessPath`/`IndexQueryRejectionClass` denials, and derived-index parity/recovery evidence without introducing JSON/payload/legacy/PatchRecord residue. Removed the stale CI line-cap allowlist exemption. Verified with `cargo fmt -p worth-relational`, warning-clean `cargo check -p worth-relational --tests`, `cargo test -p worth-relational query::indexes -- --nocapture`, line-cap/residue/stale-path scans, and `git diff --check` (only existing CRLF warning on the allowlist file).
- worth-relational planned-query execution test topology batch: deleted over-cap flat `tests/query/execution.rs` and replaced it with `tests/query/execution/` modules for explicit target reduction, entity/relation kind scans, aspect-filtered projection scans, neighborhood traversal, parallel packetization, and execution-model parity. Preserved aspect-native read proof through `ProjectionAspectFilter`, field-mask projection scopes, typed `AspectFieldPatch` fixture creation, authoritative storage execution, canonical traversal ordering, and reduction digest/counter parity without introducing JSON/payload/legacy/PatchRecord residue. Removed the stale CI line-cap allowlist exemption. Verified with `cargo fmt -p worth-relational`, warning-clean `cargo check -p worth-relational --tests`, `cargo test -p worth-relational query::execution -- --nocapture`, line-cap/residue/stale-path scans, and `git diff --check` (only existing CRLF warning on the allowlist file).
- worth-relational durability contract test topology batch: deleted over-cap flat `tests/durability/contracts.rs` and replaced it with `tests/durability/contracts/` modules for branch/aspect-history recovery, descriptor authority continuity, schema-transition continuity, rejected-commit/parent-closure recovery, merge replay continuity, checkpoint metadata recovery, and retention/inspection/store-failure boundaries. Preserved aspect-native durability proof through `KindAspectContractDeclarations`, authoritative record patch assertions, aspect-history digests, descriptor canonical-basis version parity, typed recovery mismatch classes, typed diagnostics, and `AspectFieldPatch` relation fixtures without introducing JSON/payload/legacy/PatchRecord or old declaration residue. Removed the stale CI line-cap allowlist exemption. Verified with `cargo fmt -p worth-relational`, warning-clean `cargo check -p worth-relational --tests`, `cargo test -p worth-relational durability::contracts -- --nocapture`, line-cap/residue/stale-path scans, and `git diff --check` (only existing CRLF warning on the allowlist file).

- worth-relational schema continuity test topology batch: deleted over-cap flat 	ests/schema/continuity.rs and replaced it with 	ests/schema/continuity/ modules for descriptor identity, transition admission/lowering, registry authority-basis rejection, commit artifact publication, commit admission denials, and publication bundle validation. Preserved aspect-native schema continuity proof through typed SchemaDiffAtom, SchemaBoundaryFingerprint, descriptor semantics/canonical-basis versions, CanonicalFieldPath, typed ConflictClass, typed diagnostics, and KindAspectContractDeclarations without JSON/payload/legacy/PatchRecord/old-declaration residue. Removed the stale CI line-cap allowlist exemption. Verified with cargo fmt -p worth-relational, warning-clean cargo check -p worth-relational --tests, cargo test -p worth-relational schema::continuity -- --nocapture, line-cap/residue/stale-path scans, and git diff --check (only existing CRLF warning on allowlist).

- worth-relational inspection test topology batch: deleted over-cap flat 	ests/inspection.rs and replaced it with 	ests/inspection/ modules for graph/scope summaries, structural identity evidence, historical record truth, commit-envelope projection, transaction staging inspection, and branch-local inspection behavior. Preserved aspect-native inspection proof through authoritative commit envelopes, uthoritative_record_patches, ordered changed aspect keys, branch-local aspect-history observations, structural fingerprint/lineage evidence, and explicit retention/reconstruction availability without JSON/payload/legacy/compatibility/PatchRecord or old patch.records residue. Removed the stale CI line-cap allowlist exemption. Verified with cargo fmt -p worth-relational, warning-clean cargo check -p worth-relational --tests, cargo test -p worth-relational inspection -- --nocapture (44 passed, 1 ignored performance baseline), line-cap/residue/stale-path scans, and git diff --check (only existing CRLF warning on allowlist).

- worth-relational history replay test topology batch: deleted over-cap flat 	ests/history/replay.rs and replaced it with 	ests/history/replay/ modules for replay contract admission, schema-continuity drift, aspect-history surfaces, lineage authority basis, derived-index/mixed-workload replay, and relation-integrity replay. Preserved aspect-native replay proof through authoritative record patches, canonical commit envelopes, typed replay mismatch/failure classes, schema continuation descriptors, aspect-history digests, derived-index aspect field locators, relation-integrity plan revisions, and KindAspectContractDeclarations without JSON/payload/legacy/compatibility/PatchRecord/old patch.records or spect_declarations residue. Removed the stale CI line-cap allowlist exemption. Verified with cargo fmt -p worth-relational, warning-clean cargo check -p worth-relational --tests, cargo test -p worth-relational history::replay -- --nocapture (22 passed), line-cap/residue/stale-path scans, and git diff --check (only existing CRLF warning on allowlist).
- worth-relational history milestone 7D phase D topology batch: deleted over-cap flat `tests/history/milestone_7d_phase_d.rs` and replaced it with `tests/history/milestone_7d_phase_d/` modules for deleted-record convergence, last-writer-wins/prefer-richer aspect merge resolution, monotonic counter merge resolution, merge-policy admission denials, prepared-merge denials, topology local rewire, topology region semantics, and shared aspect-native fixtures. Preserved aspect-native proof through `KindAspectContractDeclarations`, `DeclaredAspectContractBinding`, foundational `AspectKey`, `AspectFieldPatch`, typed merge-policy decision boundaries, authoritative changed-aspect patch evidence, topology evidence, typed diagnostics, and canonical digest basis without JSON/payload/legacy/PatchRecord or old declaration residue. Removed the stale CI line-cap allowlist exemption. Verified with `cargo fmt -p worth-relational`, warning-clean `cargo check -p worth-relational --tests`, `cargo test -p worth-relational history::milestone_7d_phase_d -- --nocapture` (15 passed), line-cap/residue/stale-path scans, and `git diff --check` (only existing CRLF warning on allowlist).
- worth-relational history milestone 8.5 strategy certification topology batch: deleted over-cap flat `tests/history/milestone_8_5.rs` and replaced it with `tests/history/milestone_8_5/` modules for certification proof carriers, foundational field-patch fixtures, persisted strategy runtime fixtures, primary strategy conflict proof, overlapping/disjoint aspect conflict proof, controller-sequence shared-truth proof, replay/recovery/hostile executor mismatch proof, replacement-lineage certification, and narrow top-level orchestration/assertions. Preserved aspect-native proof through `AspectFieldPatch`, `AspectFieldLocator`, `AspectKey`, `FieldKey`, `AspectValue`, native strategy canonical requests, merge digest bases, strategy replay surfaces, authoritative `authoritative_record_patches` recovery counts, and typed lineage digest evidence without JSON/payload/legacy/PatchRecord or old declaration residue. Removed the stale CI line-cap allowlist exemption. Verified with `cargo fmt -p worth-relational`, warning-clean `cargo check -p worth-relational --tests`, `cargo test -p worth-relational history::milestone_8_5 -- --nocapture` (2 passed, including kubernetes filter companion), line-cap/residue/stale-path scans, global over-cap scan, and `git diff --check` (only existing CRLF warning on allowlist).
- worth-relational history milestone 8.5 Kubernetes controller certification topology batch: deleted over-cap flat `tests/history/milestone_8_5_kubernetes.rs` and replaced it with `tests/history/milestone_8_5_kubernetes/` modules for certification proof carriers, aspect visibility comparison, foundational field-patch fixtures, persisted strategy runtime execution, merge planning assertions/evidence, staged controller sequence certification, replay/recovery helpers, and final recovered-history parity. Preserved aspect-native proof through `AspectFieldPatch`, `AspectFieldLocator`, `AspectKey`, `FieldKey`, `AspectValue`, typed strategy conflict/evidence digest bases, canonical replica value bytes, replay outcomes, branch-head evidence, and authoritative `authoritative_record_patches` recovery counts without JSON/payload/legacy/PatchRecord or old declaration residue. Removed the stale CI line-cap allowlist exemption. Verified with `cargo fmt -p worth-relational`, warning-clean `cargo check -p worth-relational --tests`, `cargo test -p worth-relational history::milestone_8_5_kubernetes -- --nocapture` (1 passed), line-cap/residue/stale-path scans, global over-cap scan, and `git diff --check` (only existing CRLF warning on allowlist).
- worth-runtime-bridge aspect-native Phase 3 routing batch: continued mask-native route slice identity and read lowering. `TruthDeltaSurface` now preserves foundational `AspectLocator`, optional `AspectFieldLocator`, explicit surface kind, and native committed-target basis; `BridgeSubscriptionSlice` now carries locator/mask/native-target canonical identity instead of ordering from presentation labels. Route canonicalization and continuity remap now use canonical native slice basis, while request keys remain adapter compatibility handles. Updated the bulk snapshot-delivery harness to assert preplanned snapshot preservation by identity set instead of brittle route-result order. Verified with `cargo fmt -p worth-runtime-bridge`, warning-clean `cargo check -p worth-runtime-bridge --tests`, `cargo test -p worth-runtime-bridge routing -- --nocapture` (23 passed), `cargo test -p worth-runtime-bridge snapshot -- --nocapture` (46 passed), residue/line-cap scans, and `git diff --check` (only expected CRLF warnings).
- worth-runtime-bridge Phase 3 QA loop: hostile QA found two real aspect-native gaps. Continuity prior-slice planning still preserved only `aspect_key + surface_label` and reconstructed successor masks later, which could silently widen an invalid signal-field label to whole-aspect semantics. Subscription declarations still canonicalized identity from `entity/aspect/surface_label/kind` instead of native target/mask basis. Fixed both by carrying foundational locators, optional field locators, projection masks, surface kind, and native target canonical basis through prior continuity slices and normalized subscription intents. Split continuity prior-slice proof into `continuity/requests/prior_slice.rs` to restore line-cap discipline. Added invalid foundational field-target coverage. Verified with warning-clean `cargo check -p worth-runtime-bridge --tests`, `cargo test -p worth-runtime-bridge subscription::declaration -- --nocapture` (15 passed), `cargo test -p worth-runtime-bridge subscription -- --nocapture` (130 passed), `cargo test -p worth-runtime-bridge continuity -- --nocapture` (47 passed), `cargo test -p worth-runtime-bridge routing -- --nocapture` (23 passed), `cargo test -p worth-runtime-bridge snapshot -- --nocapture` (46 passed), residue/line-cap scans, and `git diff --check` (only expected CRLF warnings).
- worth-runtime-bridge Phase 4 grouped truth/query-facing projection batch: row-set materialization now carries `BridgeMaterializedFieldProjection` with foundational aspect locators, optional field locators, projection masks, field identity, and canonical projection basis; row-set digests include that projection proof. Grouped projection contracts now parse binding labels into native `AspectKey` proofs and grouped truth lookup matches by native aspect key rather than raw field-label map lookup. Public exports include the new projection/error carriers. The downstream snapshot-read seam was also detoxed: `worth-relational` publication snapshot readers now emit native `AspectValue` into `SnapshotReadRecord`, grouped truth consumes `scalar_aspect_value()` rather than byte decode, and `worth-query` fixtures/consumers were updated from byte payload fixtures to native `AspectValue`/`SnapshotReadValue` projection. Verified with `cargo check -p worth-runtime-bridge --tests`, `cargo check -p worth-relational --tests`, `cargo check -p worth-query --tests`, `cargo test -p worth-runtime-bridge source -- --nocapture` (53 passed), `cargo test -p worth-runtime-bridge grouped -- --nocapture` (8 passed), `cargo test -p worth-runtime-bridge snapshot -- --nocapture` (46 passed), `cargo test -p worth-relational grouped_truth -- --nocapture` (5 passed), and `cargo test -p worth-relational snapshot_reading -- --nocapture` (3 passed).
- worth-runtime-bridge Phase 5A writeback intent batch: replaced caller-supplied writeback effect/domain-payload digests with `BridgeWritebackEffectIntent`, a bridge-native carrier backed by foundational `AuthoritativeRecordAspectPatch` plus `prepare_aspect_patch_for_canonical_basis` proof text and derived intent digest. Split over-cap `writeback/mapper.rs` into `mapper/{envelope,mapped_input,witness,record}.rs`, moved mapper/envelope/mapped-input/effect derivation to intent digests, stored the typed intent on admitted execution requests, and renamed retained diagnostics from domain payload to effect intent. Tightened intent class flow so the derived effect uses the effect intent class and the existing strategy-compatibility gate catches mismatches. Tests/harness now construct validated scalar foundational patches for fixture effects rather than passing raw digest strings. Verified with warning-clean `cargo check -p worth-runtime-bridge --tests` and `cargo test -p worth-runtime-bridge writeback -- --nocapture` (71 passed; expected caught-panic output from panic-mapping test). Residue scan found no `domain_payload_digest` / `domain_payload` production residue; touched non-allowlisted production files are below 400 lines. The workspace line-cap shell script could not run because `bash` is unavailable in this PowerShell environment.
- worth-runtime-bridge aspect-native spec hardening batch: revised `_docs/worth-runtime-bridge/aspect_native_refactor.md` to match the relational hard-break posture. Added an explicit eradication standard: production code, facades, retained diagnostics, certification fixtures, and harness helpers must not preserve evidence of prior JSON/raw-byte/string-label/digest-only authority except historic engineering docs or explicitly external I/O boundaries. Inserted new Phase 1 for crate-wide legacy authority eradication, renumbered the implementation phases, tightened compatibility language so it cannot mean fallback/shim/coexistence, strengthened forbidden hybrids, batch-plan requirements, target skeleton, acceptance evidence, and closeout criteria. Verified with phase-heading scan and `git diff --check` on the spec.
