Reviewed `forge-query` milestone `9.3.8` with focus on the new Phase `24b` addendum for aggressive declaration-entry aspect contracts and granularity.

Verified the spec now treats aspects as load-bearing for:
- semantic granularity
- ambiguity narrowing
- materialization honesty
- scope/permission/invariant enforcement
- later binding specificity

Implementation prep findings:
- current `target_binding` substrate is still identity/semantic-class oriented (`kind`, digests, binding semantics payloads) and does not yet carry declaration-entry aspect contracts
- Query runtime, relational, bridge, and signal already have real aspect contract foundations that should be reused rather than re-invented
- likely retrofit seam starts with one shared declaration-entry aspect contract vocabulary and then threads it through progression, foundational evidence/materialization, route/receipt/envelope, relational routing, bridge routing, signal compatibility, and orchestration planning
