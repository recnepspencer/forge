- Forge runtime bridge Milestone 16 implementation batch completed: added a multi-failure precedence certification lane that models `BasisBinding` as an independent source artifact, proves `BasisDrift` wins over checkpoint/replay/diagnostics drift, retains lower-precedence failures as suppressed causes, and seals the report behind compile-fail coverage. Full `cargo test -p forge-runtime-bridge` passed after adjusting older tests to inject explicit basis artifacts instead of relying on declaration drift as an implicit basis proxy.
- Forge runtime bridge Milestone 16 implementation batch completed and completion-audited: added canonical ordering hostility certification. New report proves reversed retained-artifact insertion order preserves source index digest, semantic digest set, field ordering, sealed bundle digest, and semantic-equivalence comparison; first-ship reference workload matrix now includes `CanonicalOrderingHostility`; exact counters and compile-fail constructor privacy are covered. Follow-up audit hardened `BridgeSubscriptionCertificationComparisonReport` so comparison reports bind left/right sealed bundle digests, preventing offline audit deduplication from collapsing distinct lanes with the same outcome shape. Full `cargo test -p forge-runtime-bridge` passed with 598 unit tests plus no-inc, trybuild, and doctests.
- Forge runtime bridge Milestone 16 implementation batch completed: added explicit stale checkpoint rejection certification. New `BridgeSubscriptionCertificationStaleCheckpointReport` proves checkpoint digest drift localizes as `CheckpointIncompatibility` at `CheckpointResumeOrReplay` without replay mismatch suppression, facade/counter/export surfaces are sealed, and the first-ship reference workload matrix now includes `StaleCheckpointRejection` with exact suite 35 coverage. Full `cargo test -p forge-runtime-bridge` passed with 599 unit tests plus no-inc, trybuild, and doctests.
- Forge runtime bridge Milestone 16 QA loop on stale checkpoint batch completed: fixed the reference workload lane so it compares fresh checkpoint baseline against stale checkpoint digest rather than present-vs-absent checkpoint state; hardened suite 35 to assert left/right bundle binding, exact one mismatch, and no replay-mismatch suppression; split the new facade stale-checkpoint proof test out of the large certification test file. Full `cargo test -p forge-runtime-bridge` passed.
