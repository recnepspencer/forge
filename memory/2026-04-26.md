# 2026-04-26

- QA on Forge Store 13.3 Phase 7B found and fixed weak suite honesty: hostile named-suite rows no longer derive from bundle-level digests alone, public suite construction now requires explicit per-lane evidence, lane evidence validates expected pass/rejection outcome and failure kind per row, missing/misclassified hostile evidence rejects, and trybuild proves lane evidence fields remain private. Verified focused trust tests, phase-boundary trybuild, subscription-support tests, and full `cargo test -p forge-store`.
- Forge Signal Milestone B advanced the batch completion admission boundary before Phase 9: added `ResourceCompletionBatchAdmissionReport`, `CompletionBatchAdmission` cost envelope, duplicate completion denial class/counters, canonical in-batch ordering, and in-batch duplicate/contradictory identity protection so one completion flood cannot mint multiple admitted proofs for the same request. Batch admission now has its own telemetry instead of pretending to be N scalar public calls. Verified focused batch tests, resource API trybuild, `cargo check -p forge-signal --tests`, full `cargo test -p forge-signal`, and targeted `git diff --check`.
- QA on Forge Signal Milestone B batch completion admission tightened the proof surface: added a contradictory same-identity completion flood test, pinned batch admission to exactly one performance-envelope boundary without incrementing scalar completion admission counters, replaced the denial-dropping batch report consumption helper with `into_parts`, and added trybuild coverage proving the batch report remains move-only. Reverified focused batch tests, resource API trybuild, full `cargo test -p forge-signal`, and targeted `git diff --check`.
- Forge Signal Milestone B Phase 9A added a cost-honest resource branch-restore summary boundary: `ResourceBranchRestoreReport`, `BranchRestore` performance envelope, latest restore report accessor, retained-summary and broad-rebuild-denial telemetry, facade exports, and private-field trybuild proof. Snapshot restore now reports restored in-flight width, retained summary width, denied broad rebuild count, and exact post-restore envelope accounting relative to snapshot telemetry. Verified focused restore/stale tests, resource API trybuild, `cargo check -p forge-signal --tests`, full `cargo test -p forge-signal`, and targeted `git diff --check`.
- Forge Signal Milestone B Phase 9B added `ResourceReplayReconstructionReport` and a `ReplayReconstruction` boundary so resource replay has deterministic canonical digests over retained lifecycle summaries, denied completion history, and in-flight request state. Added replay reconstruction telemetry, facade/runtime API `reconstruct_resource_replay_summary`, private-field trybuild proof, and a snapshot-restore parity test proving lifecycle/denial replay digests survive restore for retained history without hidden broad rebuild. Also fixed the branch-restore cost contract id collision by assigning distinct restore/replay contract ids. Verified focused replay test, resource API trybuild, `cargo check -p forge-signal --tests`, full `cargo test -p forge-signal`, and targeted `git diff --check`.
- Forge Query runtime API Batch 5C advanced effects beyond delivery-only routing: added pending write-intent effect declarations, pending intent counters on effect/write receipts, sealed phase evidence for delivery/suppression/expression failure/pending intent routes, inspection getters for pending intent and latest phase evidence, facade exports, and denial coverage preventing effect-triggered writes from targeting authoritative truth. The slice intentionally stages pending intent evidence only; real intent commit execution remains Batch 7. Verified focused write-intent effect tests, existing delivery routing test, runtime module tests, phase-boundary trybuild, `cargo check -p forge-query --tests`, full `cargo test -p forge-query`, and targeted `git diff --check`.
- QA on Forge Query Batch 5C fixed two honesty gaps: pending write-intent loop evidence no longer claims coalescing before coalescing exists, and effect inspection again counts suppressed/expression-failure terminal artifacts as pending drainable delivery evidence while keeping pending write-intents separate. Phase paths are now encoded as fixed internal posture rather than allocating a `Vec` per routed effect. Reverified focused effect tests, runtime module tests, phase-boundary trybuild, `cargo check -p forge-query --tests`, full `cargo test -p forge-query`, and targeted `git diff --check`.
- Forge Store 13.3 Phase 7C hardened named-suite pass lane evidence: public certified-pass lane construction now derives from `CertifiedSupportTrustReport` posture or zero-debt `SupportCertificationEvidenceBundle` counters, while the raw digest pass constructor is crate-private and covered by trybuild. Added posture-drift rejection for replicated/migrated/rebuilt exact lanes and counter-row rejection for non-counter rows. Verified `cargo fmt --all`, focused Phase 7 trust tests, phase-boundary trybuild, and full `cargo test -p forge-store` (634 tests plus trybuild/doc-tests).
- Forge Store 13.3 Phase 7D hardened hostile rejection lane evidence: public rejection lane construction now derives deterministic source/diagnostic/counter digests from real `SupportTrustFailure` evidence, the free-form rejection constructor is crate-private and covered by trybuild, and the Phase 7 lane helper now obtains hostile failures from production trust/certification paths instead of synthetic failure packets. Multi-drift lane now certifies the actual deterministic primary basis failure. Verified `cargo fmt --all`, focused Phase 7 trust tests, phase-boundary trybuild, and full `cargo test -p forge-store` (635 tests plus trybuild/doc-tests).
- Forge Store 13.3 closeout runner slice moved Phase 7 suite orchestration into a production-facing `SubscriptionSupportAccuracyCertificationRunner` that emits a named suite plus performance, access/debt, persistence-posture, and run-digest closeout artifacts. The runner validates exact certification/generic/domain counters, explicit future-owned access debt, in-memory-only persistence posture, and handoff digest binding to the supplied generic/domain artifacts. Facade exports and trybuild now cover the runner boundary. Verified `cargo fmt --all`, focused Phase 7 tests, phase-boundary trybuild, full `cargo test -p forge-store` (638 tests plus trybuild/doc-tests), and targeted `git diff --check`.
- Brutal QA on the Forge Store 13.3 closeout runner found two closeout-doc blockers and fixed them: the runner had accepted valid-but-wrong certification/domain counter regimes, and the performance/access closeout artifacts carried more counters than they exposed through accessors. The runner now enforces exact first-ship counter regimes for certification, generic, and domain closeout; exposes all closeout counters; and trybuild proves the full run artifact cannot be synthesized externally. Verified focused Phase 7 tests, phase-boundary trybuild, full `cargo test -p forge-store` (640 tests plus trybuild/doc-tests), and targeted `git diff --check`.
- Authored `_docs/forge-store/milestone-13.3-closeout.md` and linked it from the Forge Store roadmap. The closeout records shipped trust/certification surfaces, the machine-checkable `SubscriptionSupportAccuracyCertificationRun`, the trace matrix, named suite rows, exact counter regime, compile-time boundaries, explicit non-claims, and verification commands/results.
- Forge Query runtime API Batch 6A added preview handle binding evidence for live views, computed views, and effects under an explicit preview basis. `ForgeQueryPreviewSession::use_view`, `use_computed`, and `use_effect` now return sealed binding evidence; effect bindings evaluate preview policy before admission and default derive-only previews mute delivery/write-intent effects with zero residue. Preview outcomes expose binding/effect/residue counters, and facade/runtime exports include the new preview binding enums/evidence. Renamed preview effect booleans to admitted/admission language so they do not imply execution before preview effect execution exists. Verified focused preview binding tests, phase-boundary trybuild, `cargo check -p forge-query --tests`, runtime module tests, full `cargo test -p forge-query`, and targeted `git diff --check`.
- Forge Query runtime API Batch 6B added sealed preview closeout evidence to discard/promotion outcomes. `ForgeQueryPreviewCloseoutEvidence` now records closeout kind, basis evidence, binding family counts, staged preview-write cleanup, promoted write count, and separate residue classes for subscription, derived runtime, effect delivery, pending write intent, preview staging, and authoritative residue. Runtime tests prove discard closes staged preview writes without authoritative residue and promotion consumes staged preview writes through authoritative receipts without mutating preview state in place; trybuild proves closeout evidence cannot be forged. Verified focused closeout tests, phase-boundary trybuild, `cargo check -p forge-query --tests`, runtime module tests, full `cargo test -p forge-query`, and targeted `git diff --check`.
- QA on Forge Signal Milestone B Phase 9 branch/replay work tightened replay honesty: replay reconstruction now includes descriptor width and descriptor digest in the canonical replay basis, the replay reconstruction boundary is explicitly marked `Debt` instead of `Verified`, branch-restore tests no longer treat performance admitted count as the authority for restored in-flight width, and cost-contract IDs are pinned for completion staging (9), branch restore (13), and replay reconstruction (14). Reverified focused resource tests, resource API trybuild, `cargo check -p forge-signal --tests`, full `cargo test -p forge-signal`, and targeted `git diff --check`.
- Forge Signal Milestone B Phase 10A added the first resource certification surface: sealed `ResourceCertificationRecord`s, five required async/resource certification families, a builder for complete bundles, canonical bundle digests, bundle parity reports, facade exports, hostile missing/duplicate/drift tests, and trybuild proof that certification records cannot be forged externally. Verified focused resource certification tests, resource API trybuild, `cargo check -p forge-signal --tests`, full `cargo test -p forge-signal`, and targeted `git diff --check`.
- QA on Forge Signal Phase 10A found the certification record constructor was too permissive: external users could not set private fields, but public `ResourceCertificationRecord::passing` still accepted arbitrary digest strings. Fixed by making direct record construction crate-private and requiring family-specific builder methods to consume concrete resource reports: replay reconstruction, superseding request admission, rollback report, branch restore plus replay, and runtime summary plus in-flight boundary envelope. Added trybuild proving the generic constructor is private and tests proving missing supersession evidence rejects.
- Forge Signal Milestone B Phase 10B added `ResourceDiagnosticsSummary`, an explicit diagnostics provenance surface carrying runtime summary, latest branch restore report, debt-marked replay reconstruction, and a canonical provenance digest. Added `SignalRuntime::resource_diagnostics_summary`, facade exports, tests proving diagnostics summary reads preserve resource truth while incrementing explicit replay reconstruction telemetry, retained-denial drift changes provenance digest, and trybuild proving diagnostics summaries cannot be forged externally. Verified focused diagnostics/certification tests, resource API trybuild, `cargo check -p forge-signal --tests`, full `cargo test -p forge-signal`, and targeted `git diff --check`.
- Forge Signal Milestone B Phase 11A began performance contract certification for retained summary and diagnostics surfaces: added `ResourceRuntimeSummaryReadReport`, `ResourceDiagnosticsExpansionBudget`, diagnostics expansion envelopes/counters, budget-gated diagnostics denial before cold replay reconstruction, and sealed compile-fail fixtures for the new proof/budget objects. Retained summary reads now prove zero replay/cold reconstruction while diagnostics expansion reports explicit debt and budget consumption. Verified focused diagnostics/summary tests, resource API trybuild, `cargo check -p forge-signal --tests`, full `cargo test -p forge-signal` (705 passed, 23 ignored), and targeted `git diff --check`.
- QA on Forge Signal Milestone B Phase 11A found the diagnostics budget gate was still too easy to bypass and under-proven: the no-argument diagnostics summary performed unbounded cold replay reconstruction, denied budget expansion returned a generic error instead of sealed evidence, and admitted summaries did not bind the budget that authorized reconstruction. Fixed by replacing the cheap-looking API with explicit budget/unbounded-cold methods, adding `ResourceDiagnosticsExpansionDenial` with denied performance envelopes, binding expansion budget into diagnostics provenance digests, and adding drift/denial/private-field tests. Reverified focused diagnostics/summary tests, resource API trybuild, `cargo check -p forge-signal --tests`, full `cargo test -p forge-signal` (707 passed, 23 ignored), and targeted `git diff --check`.
- Forge Query runtime API Batch 6C hardened preview promotion admission: preview sessions retain the basis snapshot token, promotion now returns typed `Result` outcomes, stale authoritative bases reject before authority execution, write-authority failures return sealed denial evidence without silently dropping staged writes, and facade/runtime exports expose promotion-denial kind/evidence. Added hostile stale-basis and write-failure tests plus trybuild privacy coverage for `ForgeQueryPreviewPromotionDenialEvidence`. Verified `cargo fmt -p forge-query`, phase-boundary trybuild, `cargo check -p forge-query --tests`, runtime module tests, full `cargo test -p forge-query`, and targeted `git diff --check`.
- QA on Forge Query Batch 6C found and fixed a promotion atomicity gap: multi-write preview promotion now fails closed with typed `PreviewPromotionAtomicBatchUnsupported` evidence before any authority execution because Batch 6 has no transactional promotion primitive yet. Also split the giant inline runtime test module into `runtime/tests.rs`, dropping production `runtime.rs` from 4,590 lines to 1,584 while keeping the tests in the runtime subsystem. Reverified preview-promotion tests, phase-boundary trybuild, `cargo check -p forge-query --tests`, runtime module tests, full `cargo test -p forge-query`, and targeted `git diff --check`.
- Forge Query runtime API Batch 6D added preview-scoped execution evidence for bound handles: preview writes now route staged receipts through bound live views, computed views, and effect handles into sealed `ForgeQueryPreviewExecutionEvidence` without touching authoritative live patch, derived materialization, delivery, or pending intent queues. Closeout evidence now reports subscription, derived runtime, effect delivery, and pending write-intent residue from preview execution evidence, facade exports expose the evidence/read model, and trybuild proves execution evidence cannot be forged externally. Verified focused preview execution tests, phase-boundary trybuild, `cargo fmt -p forge-query -- --check`, `cargo check -p forge-query --tests`, runtime module tests, full `cargo test -p forge-query`, and targeted `git diff --check`.
- Forge Query runtime API Batch 7A added the first authoritative intent facade admission path: sealed public `ForgeQueryIntentReceipt`s, adapter-facing intent declarations/executions, runtime builder intent authority wiring, support-profile denial when `Intent` is claimed without an executable authority adapter, shared authoritative mutation routing for direct writes and admitted intents, facade exports, hostile input/support checks, and trybuild proof that receipts cannot be forged externally. This intentionally stages strategy execution behind an authority adapter; real relational strategy execution, branch/preview intent policy, effect-triggered intent execution, and replay/reference verification remain for later Batch 7 slices. Verified `cargo fmt -p forge-query`, focused intent tests, phase-boundary trybuild, `cargo check -p forge-query --tests`, runtime module tests, full `cargo test -p forge-query`, doc-tests, and targeted `git diff --check`.
- QA on Forge Query Batch 7A found and fixed three intent-boundary weaknesses: non-user-authored source lanes could target authoritative truth before branch/preview/effect/derived policy exists, strategy execution evidence could drift from the declared strategy before sealed receipt construction, and intent receipt digests did not bind invariant evidence plus the full exposed routing counter surface. Added intent-module admission helpers, moved bridge-backed signal routing behind execution-evidence validation, returned typed intent denial through the backend seam, expanded receipt counters/digest, and added hostile tests proving forbidden source lanes deny before authority execution and strategy drift denies before signal routing. Reverified focused intent tests, phase-boundary trybuild, `cargo check -p forge-query --tests`, runtime module tests, full `cargo test -p forge-query`, and targeted `git diff --check`.
- Forge Signal Milestone B Phase 11B encoded allocation posture into every resource boundary performance envelope: operational, retained-history, diagnostics, and facade-report allocation lanes are private envelope fields with public accessors, telemetry aggregates them through one `record_boundary_performance_envelope` primitive, and resource runtime/diagnostics emission sites now record allocation posture with the envelope count. Tests prove retained-summary reads charge only facade allocation, admitted diagnostics charge replay/diagnostics allocation without operational allocation, and denied diagnostics fallback performs no diagnostics allocation. Verified focused diagnostics/summary tests, `cargo check -p forge-signal --tests`, full `cargo test -p forge-signal` (707 passed, 23 ignored), and targeted `git diff --check`.
- Forge Signal Milestone B Phase 11C encoded resource density strategy selection into boundary performance envelopes. Added `ResourceDensityStrategy` with sparse indexed, bursty sorted/deduplicated, dense sorted/deduplicated, and not-applicable postures; request admission, scalar completion admission, and batch completion admission now report derived density strategy through the same serialized envelope used by certification digests. Resource telemetry now counts total/sparse/bursty/dense strategy selections, and tests prove sparse scalar completion, bursty pressure, and dense batch completion preserve lifecycle truth and exact strategy counters. Verified focused resource request/completion/batch tests, resource API trybuild, `cargo check -p forge-signal --tests`, full `cargo test -p forge-signal` (708 passed, 23 ignored), and targeted `git diff --check`.
- Forge Signal Milestone B Phase 11D added explicit lifecycle-retention compaction for terminal in-flight resource requests. Terminal cancelled/fulfilled/superseded/timed-out requests can now move from hot in-flight lookup into retained lifecycle history through `SignalRuntime::compact_resource_lifecycle_history`, with a sealed `ResourceLifecycleRetentionCompactionReport`, a `LifecycleRetentionCompaction` performance envelope, retained-history summary width, retained replay digest participation, branch-restore rekeying, and telemetry for hot compaction/retirement/reclamation/history writes. Late completions against compacted cancelled requests still deny as `Cancelled` rather than degrading to `UnknownRequest`, and trybuild proves the compaction report remains unforgeable. Verified focused compaction/regression tests, resource API trybuild, `cargo check -p forge-signal --tests`, full `cargo test -p forge-signal` (710 passed, 23 ignored), and targeted `git diff --check`.
- QA on Forge Signal Phase 11D found and fixed three production-grade gaps: compaction selected counts now only include terminal hot records that can actually be reclaimed; branch restore now accounts for retained and pruned in-flight history in retained-summary width; and retained lifecycle history now has an explicit retained-limit compaction path with pruned counters plus a tombstone lane so late completions against policy-pruned records deny as `RetainedHistoryUnavailable`, not `UnknownRequest`. Reverified focused compaction/restore tests, resource API trybuild, `cargo check -p forge-signal --tests`, full `cargo test -p forge-signal` (712 passed, 23 ignored), and targeted `git diff --check`.
- Forge Signal Milestone B Phase 12A hardened resource compile-time protocol boundaries. Added trybuild fixtures proving raw completions cannot be staged or committed, admitted completions cannot skip staging on runtime or transaction commit paths, cancellation proofs cannot be spent as completion proofs, and private resource runtime internals cannot be reached outside the facade path. Resource boundary trybuild now carries 45 fixtures. Verified `cargo fmt -p forge-signal`, resource API trybuild, `cargo check -p forge-signal --tests`, full `cargo test -p forge-signal` (712 passed, 23 ignored), and targeted `git diff --check`.
- Forge Signal Milestone B Phase 12B made invalid resource initial lifecycle policy states unrepresentable. Added `ResourceInitialLifecycleClass` as the only public constructor witness for `ResourceLifecyclePolicyDeclaration`, rejected hostile deserialization of non-`Unrequested` lifecycle classes, exported the witness through the facade, and added trybuild fixtures proving callers cannot forge the witness or pass `Pending`/terminal lifecycle truth as initial policy. Verified focused lifecycle policy test, resource API trybuild (48 fixtures), `cargo check -p forge-signal --tests`, full `cargo test -p forge-signal` (713 passed, 23 ignored), and targeted `git diff --check`.
- QA on Forge Signal Phase 12B found the compile-time policy boundary was solid, but the hostile data-boundary proof stopped at the witness/policy type rather than the full `ResourceNodeDeclaration` shape callers actually submit. Added regression coverage that corrupts serialized resource declaration lifecycle policy to `TimedOut` and proves deserialization rejects before lowering. Reverified focused lifecycle policy test, resource API trybuild, `cargo check -p forge-signal --tests`, full `cargo test -p forge-signal` (713 passed, 23 ignored), and targeted `git diff --check`.
- Forge Signal Milestone B closeout certification Batch 13A added a sealed `ResourceMilestoneBCertificationRun` artifact on top of the existing five-family resource certification bundle. The run requires a complete passing bundle, records exact required/certified/failed family counts, binds the bundle digest and per-family evidence digests into a canonical run digest, exports through the facade, and trybuild proves the run cannot be forged externally. Tests prove incomplete bundles reject and evidence drift changes the run digest. Verified focused closeout certification test, resource API trybuild (49 fixtures), `cargo check -p forge-signal --tests`, full `cargo test -p forge-signal` (714 passed, 23 ignored), and targeted `git diff --check`.
- Forge Signal Milestone B closeout Batch 13B added a sealed scenario matrix underneath the certification run. `ResourceMilestoneBScenarioId` rows are derived from concrete five-family certification records, the matrix has its own schema/summary/digest, the run now requires a same-bundle passing matrix and binds scenario row digests into the run digest, and facade exports/trybuild cover row, matrix, and run privacy. Hostile tests reject incomplete bundles and cross-bundle scenario matrices while proving evidence drift changes both matrix and run digests. Verified focused closeout test, resource API trybuild (51 fixtures), `cargo check -p forge-signal --tests`, full `cargo test -p forge-signal` (714 passed, 23 ignored), and targeted `git diff --check`.
- Forge Signal Milestone B closeout Batch 13C hardened the certification data boundary: sealed certification proof artifacts (`ResourceCertificationRecord`, summary, bundle, scenario row/matrix, and closeout run) are now serialization-only and no longer implement `Deserialize`, preventing private-field proof objects from being forged through JSON import. The closeout test still verifies exported run/matrix JSON shape, and a new trybuild fixture proves those artifacts fail a `DeserializeOwned` bound. Verified focused closeout test, resource API trybuild (52 fixtures), `cargo check -p forge-signal --tests`, full `cargo test -p forge-signal` (714 passed, 23 ignored), and targeted `git diff --check`.
- Forge Signal Milestone B closeout Batch 13D made the scenario matrix less family-shaped by adding sealed hostile completion evidence. The matrix now requires five certification-family rows plus four concrete runtime denial rows: late completion after supersession, cancellation, timeout, and malformed payload. `ResourceMilestoneBHostileScenarioEvidence` validates each supplied `ResourceCompletionAdmissionReport` against the expected denial class and completion-admission performance boundary, the run digest now includes those hostile row digests via the expanded matrix, and facade/trybuild cover the new hostile evidence privacy plus deserialization denial. Tests prove misclassified hostile evidence rejects before certification. Verified focused closeout test, resource API trybuild (53 fixtures), `cargo check -p forge-signal --tests`, full `cargo test -p forge-signal` (714 passed, 23 ignored), and targeted `git diff --check`.
- Forge Signal Milestone B closeout Batch 13E added sealed performance closeout evidence and made the final certification run require it. `ResourceMilestoneBPerformanceCloseout` now certifies four explicit performance claims: runtime summary reads remain zero-cold, diagnostics cold reconstruction is budget-admitted, diagnostics budget denial returns a typed denied-fallback envelope, and hostile completion denials remain scalar/non-transitioning. The run summary/digest now bind the performance closeout digest and per-claim evidence digests, facade exports expose the protocol, and trybuild proves the performance closeout artifacts are private and serialization-only. Verified focused closeout test, resource API trybuild (54 fixtures), `cargo check -p forge-signal --tests`, full `cargo test -p forge-signal` (714 passed, 23 ignored), and targeted `git diff --check`.
- Forge Signal Milestone B closeout Batch 13F hardened the performance closeout across the five certification-family scenarios. The closeout now requires nine performance claims total, adding explicit replay-debt, supersession-admission, rollback, branch-restore, and in-flight admission cost-shape checks before summary/diagnostics/hostile claims can pass. Each scenario claim digest binds the scenario row digest plus its performance envelope, and validators enforce boundary, cost posture, density strategy, allocation lanes, broad-rebuild denial, and lifecycle-transition counts where the family contract is exact. Verified focused closeout test, resource API trybuild, `cargo check -p forge-signal --tests`, full `cargo test -p forge-signal` (714 passed, 23 ignored), and targeted `git diff --check`.
- Formally closed Forge Signal Milestone B with `_docs/forge_signal/milestone-b-closeout.md`, linked it from the temporal/async roadmap, and marked `milestone-b-plan.md` as closed. The closeout artifact maps must-ship requirements, five certification families, nine scenario rows, nine performance claims, hostile condition coverage, counters/complexity contracts, compile-fail boundaries, explicit non-claims, residual risks, and final verification results. Verified `cargo fmt -p forge-signal`, focused closeout test, `cargo check -p forge-signal --tests`, resource compile-fail suite, full `cargo test -p forge-signal` (714 passed, 23 ignored plus 3 doc-tests), and targeted doc `git diff --check`.
- Collapsed Forge Query runtime god files after Batch 7A QA: split `runtime/tests.rs` into focused assembly/live/program/preview/intent/effect/computed modules with shared support, split preview production code into session/evidence/binding/outcome/aspect modules, and moved runtime builder/error/surface handle types out of `runtime.rs`. Follow-up cleanup split computed, intent, preview evidence/session helpers, and large preview/effect/assembly/support test fixtures into focused modules. Verified earlier runtime/full Forge Query tests before Forge Signal dirty work blocked fresh Forge Query compilation; latest focused verification is blocked by unrelated Forge Signal `ResourceRuntimeSummary::new` arity drift in another agent's files.
- Forge Query runtime API Batch 7B added effect-triggered pending write-intent execution through the same intent authority path as user-authored intents. Added sealed `ForgeQueryEffectIntentReceipt`, effect-triggered admission that only permits `EffectTriggered -> AuthoritativeTruth`, `execute_next_effect_write_intent`, facade exports, receipt digest binding to delivery/phase/intent evidence, and tests proving one pending intent is consumed while follow-up effect output remains deferred instead of recursively auto-executing. Also finished verifying the runtime god-file collapse: focused runtime tests, phase-boundary trybuild, full `cargo test -p forge-query`, `cargo fmt -p forge-query`, and targeted `git diff --check` all pass.
- Forge Query runtime API Batch 7C added the preview-local intent policy boundary: preview sessions can stage intent declarations as sealed `ForgeQueryPreviewIntentReceipt`s under `PreviewLocal -> PreviewTruth`, policy admission denies derive-only previews before authority execution, sandboxed preview intents produce pending-write-intent residue on discard, and authoritative intent authority is never called from preview staging. Added preview intent admission helpers, facade/runtime exports, closeout residue accounting, focused preview tests, and trybuild proof that preview intent receipts cannot be forged externally. Verified `cargo fmt -p forge-query`, focused preview/intent tests, phase-boundary trybuild, `cargo check -p forge-query --tests`, full `cargo test -p forge-query`, and targeted `git diff --check`.
- QA on Forge Query Batch 7C found and fixed two preview intent boundary weaknesses: generic `AuthoritativeAllowed` policy could admit preview-local staging even though the precise preview policy must be `SandboxedWriteIntent`, and pending-intent execution evidence used `PreviewTruth` as its source lane instead of preserving the distinct `PendingWriteIntent` authority lane. Added a hostile authoritative-policy regression and tightened evidence assertions. Reverified focused preview execution tests, phase-boundary trybuild, `cargo check -p forge-query --tests`, full `cargo test -p forge-query`, and targeted `git diff --check`.
- Forge Query runtime API Batch 7D added the branch-local intent policy boundary: introduced branch options/sessions, branch basis admission, sealed `ForgeQueryBranchIntentReceipt`, branch-local admission requiring `BranchLocal -> BranchLocalTruth` under explicit `SandboxedWriteIntent` policy, facade exports, and compile-fail privacy proof. Tightened intent support admission so branch/preview local staging also requires the backend support profile to advertise the target lane, with hostile branch and preview lane-denial tests proving no authoritative intent authority executes on unsupported lanes. Verified `cargo fmt -p forge-query`, focused branch and preview execution tests, phase-boundary trybuild, `cargo check -p forge-query --tests`, full `cargo test -p forge-query`, and targeted `git diff --check`.
- Forge Query runtime API Batch 7E added idempotent authoritative intent receipts: intent executions now carry `Mutating` vs `IdempotentNoop`, receipts expose and digest-bind the execution kind, no-op executions mint canonical receipts with commit/snapshot/invariant evidence but no deltas or signal invalidation routing, and empty mutating executions deny before routing. Effect-triggered no-op intents consume pending work without feedback. Verified `cargo fmt -p forge-query`, focused intent tests, phase-boundary trybuild, `cargo check -p forge-query --tests`, full `cargo test -p forge-query`, no trybuild `wip`, and targeted `git diff --check`.
- Forge Query runtime API Batch 7F added invariant-failure intent denial evidence: intent executions now include `InvariantViolation`, authorities can return invariant-failure artifacts with snapshot and invariant evidence, Query denies them before success receipt construction or signal/query routing, and `ForgeQueryIntentDenialEvidence` gives public sealed denial details/digests for future inspector work. Added hostile no-publication tests and trybuild privacy coverage. Verified `cargo fmt -p forge-query`, focused intent tests, phase-boundary trybuild, `cargo check -p forge-query --tests`, full `cargo test -p forge-query`, and targeted `git diff --check`.
- QA on Forge Query Batch 7F found and fixed two semantic-contract leaks: success/invariant/no-op outcomes no longer masquerade as a produced mutation digest, and denial evidence now records returned strategy identity/version/descriptor facts so strategy drift is inspectable from the sealed evidence alone. Intent receipts expose `outcome_digest` for all admitted outcomes and `produced_mutation_digest` only for true mutating executions; the plan wording was tightened to match. Reverified focused intent tests, phase-boundary trybuild, `cargo check -p forge-query --tests`, full `cargo test -p forge-query`, and targeted `git diff --check`.
- Forge Query runtime API Batch 8A began inspector hardening with intent receipt/denial explanations. Added sealed `ForgeQueryIntentReceiptInspection`, `ForgeQueryIntentDenialInspection`, and `ForgeQueryIntentInspectionDeliveryCounters`, facade exports, and runtime `inspect_intent_receipt` / `inspect_intent_denial` methods. Inspections preserve strategy identity, returned-strategy drift, source/target authority lanes, invariant evidence, outcome-vs-produced-mutation semantics, delivery counters, and digest binding without letting callers forge artifacts. Added focused success/no-op/invariant-denial/strategy-drift tests plus trybuild privacy fixtures. Verified `cargo fmt -p forge-query`, focused intent tests, phase-boundary trybuild, `cargo check -p forge-query --tests`, full `cargo test -p forge-query`, and targeted `git diff --check`.
- Forge Query runtime API Batch 8B added live handle subscription explanations. `inspect_live_view_explanation` now returns sealed `ForgeQueryLiveViewInspection` evidence derived from retained runtime subscription installation, including authority lane, query/view-shape digests, subscription family/declaration/bridge/admission/activation/basis/signal/active-lane/consumer attachment/cursor digests, budget/support evidence, and counter digests/counts. Facade exports expose the explanation types while trybuild proves callers cannot synthesize them. Added normal and grouped live inspection tests and verified `cargo fmt -p forge-query`, focused live tests, phase-boundary trybuild, `cargo check -p forge-query --tests`, full `cargo test -p forge-query`, and targeted `git diff --check`.
- Forge Query runtime API Batch 8C hardened computed handle inspection. `ForgeQueryComputedInspectionEvidence` now includes digest-bound declaration, dependency, produced-aspect, materialization, pending-patch, and overall inspection evidence; separates declaration incremental/fallback posture from observed pending patch family counts; and preserves derived-runtime authority lane semantics. Added nested computed and fallback-posture inspection tests, updated the sealed trybuild proof, and verified `cargo fmt -p forge-query`, focused computed tests, phase-boundary trybuild, `cargo check -p forge-query --tests`, full `cargo test -p forge-query`, and targeted `git diff --check`.
- Forge Query runtime API Batch 8D hardened effect handle inspection. `ForgeQueryEffectInspectionEvidence` now exposes digest-bound trigger, condition, declaration, pending-delivery, phase, and overall inspection evidence, plus explicit delivered/suppressed/expression-failure/pending-write-intent counts and latest delivery family. This makes effect inspection honest about routing posture, suppression, and loop-prevention phases without exposing forgeable constructors. Added focused delivery/write-intent inspection assertions, refreshed the sealed trybuild privacy fixture, and verified `cargo fmt -p forge-query`, focused effect tests, phase-boundary trybuild, `cargo check -p forge-query --tests`, full `cargo test -p forge-query`, and targeted `git diff --check`.
- Forge Query runtime API Batch 8E added preview/branch artifact explanation surfaces. New sealed inspection artifacts now explain preview handle bindings, preview outcomes/closeout posture, preview-local intent receipts, and branch-local intent receipts with lane, policy, basis, digest, and residue evidence. Runtime/facade inspection entrypoints were added, focused preview/branch tests now verify the explanations in derive-only and sandboxed-write-intent cases, and trybuild proves the new explanation artifacts cannot be forged externally. Verified `cargo fmt -p forge-query`, focused preview tests, focused branch tests, phase-boundary trybuild, `cargo check -p forge-query --tests`, full `cargo test -p forge-query`, and targeted `git diff --check` (line-ending warnings only).
- Forge Query runtime API Batch 9A began public surface hardening by removing effect/intent lane override escape hatches from the ordinary API. `ForgeQueryEffectDeclaration::{with_effect_policy, with_target_lane}` and `ForgeQueryIntentDeclaration::{with_source_lane, with_target_lane}` are now runtime-internal only, and new trybuild fixtures prove external callers cannot author low-level authority/policy shortcuts through the facade. Verified `cargo fmt -p forge-query`, phase-boundary trybuild with updated stderr, full `cargo test -p forge-query`, and targeted `git diff --check` (CRLF warnings only).
- Forge Query runtime API Batch 9B hardened preview/branch effect policy configuration. `ForgeQueryPreviewOptions` and `ForgeQueryBranchOptions` now expose named safe constructors (`derive_only`, `muted`, `redirected_delivery`, `sandboxed_write_intent`) while the raw `.with_effect_policy(...)` override is sealed to runtime-internal use, and new trybuild fixtures prove external callers cannot request `AuthoritativeAllowed` through preview/branch option builders. Migrated preview/branch runtime tests onto the hardened API. Verified focused preview/branch tests, phase-boundary trybuild, full `cargo test -p forge-query`, and targeted `git diff --check`.
- Forge Query runtime API Batch 9C hardened the compatibility backend posture so the old memory app stops masquerading as the ordinary path. `ForgeQueryMemoryApp::new` is now crate-private, public callers must opt into `ForgeQueryMemoryApp::compatibility_backend(...)`, the runtime builder gained `compatibility_in_memory_collections(...)`, and runtime support profiles now expose explicit backend posture (`Primary` vs `Compatibility`). Bridge-backed support profiles were corrected to stay `Primary` even though they reuse compatibility rows internally. Added support-profile/runtime-builder tests plus trybuild proof that the raw memory-app constructor is sealed externally. Verified `cargo fmt -p forge-query`, focused support-profile tests, phase-boundary trybuild, `cargo check -p forge-query --tests`, full `cargo test -p forge-query`, and targeted `git diff --check` (CRLF warnings only).
- QA on Forge Query Batches 8 and 9 against `_docs/forge-query/runtime-api-next-batch-implementation-plan.md` found and fixed three real spec gaps: ordinary facade code could still call grouped-baseline injection helpers even though Batch 9 requires caller-owned grouped baselines to disappear; inspect support metadata underclaimed the `PendingWriteIntent` lane despite Batch 8/9 inspection surfaces exposing it; and the normal effect inspection path did not carry the feedback/phase graph that the Batch 8 spec treats as part of the hidden runtime explanation. Fixed by removing grouped-baseline shortcut exports and adding compile-fail coverage, adding `PendingWriteIntent` to inspect support rows plus support-profile assertions, and embedding `ForgeQueryFeedbackPhaseGraphInspection` into effect/effect-intent inspection artifacts with focused runtime test coverage. Reverified focused effect/intent/live/support tests, compile-fail boundaries, full `cargo test -p forge-query`, and `git diff --check`.
- Forge Query runtime API Batch 9D advanced consumer migration and compatibility-path closure: deprecated the old `ForgeQueryRuntimeBuilder::in_memory_collections` spelling as compatibility debt, migrated runtime fixtures and the Forge UI todo workspace to `compatibility_in_memory_collections`, added a trybuild fixture proving strict ordinary consumers cannot keep using the ambiguous spelling, and added a Forge UI regression proving the demo workspace uses the runtime facade over an explicit compatibility backend. Verified `cargo fmt -p forge-query -p forge-ui`, phase-boundary trybuild, focused Forge UI todo regression, `cargo check -p forge-query -p forge-ui`, full `cargo test -p forge-ui`, full `cargo test -p forge-query`, and `git diff --check`.
- Forge Query runtime API Batch 9E performed the semantic shortcut closeout audit and added missing named hostile fixtures: intent execution artifacts cannot skip runtime admission and masquerade as inspected receipts or mutation receipts, derived-runtime source lanes cannot be retargeted to authoritative truth through public declarations, and preview options have no authoritative effect-policy constructor. Updated the Batch 9 plan with closeout evidence covering lane overrides, safe preview/branch options, compatibility backend posture, Forge UI facade usage, grouped baseline closure, intent execution admission, derived-to-truth closure, and existing subscription/active-lane shortcut guards.
- Final Batch 9 QA found and fixed a typed-boundary leak: `ForgeQueryRuntime::preview`, `branch`, and their `*_with_options` variants now return `Result` support denials instead of panicking when branch/preview support is unavailable. Also corrected the missing-backend message to point at `compatibility_in_memory_collections` or `backend`, and added support-profile regression coverage for unsupported preview/branch sessions.
- Wrote `_docs/forge-query/runtime-api-next-batch-closeout.md` and linked it from the runtime API next batch implementation plan. The closeout records the runtime-backed facade surface that consumes 9.1-9.3 subscription work: automatic live installation, query-shaped delivery, nested computeds, effects, branch/preview reuse, intent commits, inspection, compatibility migration, shortcut rejection, explicit non-claims, and the final verification baseline.
- Final test-quality QA for the Forge Query runtime API closeout found and fixed two proof gaps in the tests themselves: refresh-fallback computed inspection now exercises a real fallback declaration instead of an incremental maintainer, and a new adversarial composed runtime test proves live subscription evidence, nested computeds, conditional pending-intent effects, preview isolation, authoritative/effect/branch intents, inspection, and feedback phases together. Verified `cargo fmt -p forge-query`, focused tests, `runtime::tests`, full `cargo test -p forge-query` (757 tests plus compile-fail/doc coverage), and `git diff --check`.
- Designed the Forge Query Runtime API Public Stabilization Gate after the runtime API closeout and before 9.4-9.7. Added `_docs/forge-query/runtime-api-public-stabilization-plan.md`, inserted the gate into `forge_query_roadmap.md`, linked it from the runtime API closeout, and added the `Runtime API Golden DX And Async-Safe Facade Test` to `test-requirements.md`. The spec freezes workspace/surface/handle/state/aspect/lane/inspection vocabulary, golden DX transcript proof, async-safe state gates, support/debt posture, and the rule that temporal/async milestones must extend this facade rather than add parallel APIs.
- Final test-quality QA for Forge Signal Milestone B closeout found that the certification closeout test was proving completeness/digest wiring more strongly than exact requirement shape. Hardened `resource_milestone_b_certification_run_requires_complete_passing_bundle` with uniqueness checks for every scenario/hostile/performance row plus boundary, cost-contract, cost-posture, density-strategy, allocation-lane, lifecycle, denial, and diagnostics counters for all nine performance claims. Verified focused closeout test, resource compile-fail boundary suite, `cargo check -p forge-signal --tests`, full `cargo test -p forge-signal` (714 passed, 23 ignored plus 3 doc-tests), and targeted `git diff --check`.
- Rewrote `_docs/forge_signal/milestone-c-plan.md` into a Milestone A/B-quality spec for Async Resource Policy Families. The new spec anchors C on descriptor-backed policy registries, retry/backoff, timeout/deadline, cancellation/supersession, revalidation/freshness, observation/output continuity, retention/diagnostics/replay compatibility, policy proof tokens, performance envelopes, hostile certification families, compile-fail boundaries, and an explicit self-check. Verified targeted `git diff --check` (LF/CRLF warning only).
- QA pass on the Forge Signal Milestone C spec tightened practical implementation rails: added explicit QA findings, required production/test module topology, a proof-widening policy pipeline, compile-fail shortcut fixtures, strict custom-policy first-ship limits, a default-deny replay compatibility matrix, minimum built-in descriptor matrix, practical API anti-patterns, phase implementation rails, named scenario matrix rows, and performance closeout rows. Reverified targeted `git diff --check` (LF/CRLF warning only).
