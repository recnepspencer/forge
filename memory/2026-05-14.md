# 2026-05-14

- Ran a hostile QA loop over the 9.3.4 Phase 6A projection-consumption
  certification scaffold and corrected three real closure weaknesses.
- Fixed proof-binding softness in the certification bundle:
  - compile-fail and golden-transcript digests are now derived from the actual
    checked fixture and transcript file contents instead of hashes of proof
    names
  - added a dedicated `projection_consumption/certification/proof_artifacts.rs`
    catalog so audits, bundle outputs, and proof references share one source of
    truth
- Fixed a circular public-boundary claim:
  - raw row-like source promotion no longer cites the public-boundary audit as
    its own evidence
  - added a real trybuild fixture
    `projection_consumption_raw_source_has_no_consumed_fact_accessors.rs`
    proving lower-runtime row-set artifacts cannot be treated as consumed fact
    sets or issue receipts directly
- Tightened the proof-shape and support-traceability surfaces so they reference
  typed compile-fail proof artifacts instead of free-form proof-name strings.
- Removed an unused `ProjectionSourceFamily::all()` helper after the
  certification row no longer needed a family-count comparison against the raw
  source enum.
- Verification passed after the QA corrections:
  - `cargo fmt --all`
  - `cargo test -p forge-query projection_consumption -- --nocapture`
  - `cargo test -p forge-query --test phase_boundaries_projection_consumption_compile_fail -- --nocapture`
  - `cargo test -p forge-query`
- Ran a combined `$code-quality-qa` + `$qa-tests` pass over the
  `projection_consumption` implementation and harness.
- Corrected three hard structural/test-surface issues:
  - split oversized phase test files by real responsibility:
    - `tests/phase_five_receipts.rs` -> `tests/phase_five/common_path.rs` and
      `tests/phase_five/receipt_artifacts.rs`
    - `tests/phase_two_sources.rs` -> `tests/phase_two/writeback_and_grouped.rs`
      and `tests/phase_two/query_context_and_matrix.rs`
  - reorganized the overgrown trybuild directory
    `tests/ui/projection_consumption/` into:
    - `golden/`
    - `construction/`
    - `boundaries/`
  - updated the compile-fail harness and certification proof-artifact catalog
    to use the new responsibility-grouped paths
- Also removed one now-unused import exposed by the phase-two split.
- Verification passed after the structure refactor:
  - `cargo fmt --all`
  - `cargo test -p forge-query projection_consumption -- --nocapture`
  - `cargo test -p forge-query --test phase_boundaries_projection_consumption_compile_fail -- --nocapture`
  - `cargo test -p forge-query`
- Finished the substantial `9.3.4 Phase 6` closeout batch for
  `projection_consumption`.
- Added a real certification scenario layer under
  `crates/forge-query/src/projection_consumption/certification/`:
  - `fixtures.rs` for executable closeout source fixtures and completed
    lifecycle lanes
  - `oracles.rs` for independent control/hostile/parity oracle manifest and
    comparison digests
  - `seeded.rs` for deterministic seeded replay metadata and generator-class
    digests
  - `slopes.rs` for counter snapshot and phase-specific slope digests
  - `bundle_outputs.rs` plus a slimmed `bundle.rs` so the finished bundle stays
    within the workspace line cap
- Expanded `ProjectionConsumptionCertificationBundle` so it now emits the full
  required Phase 6 output surface:
  - lifecycle digests
  - source and source-receipt digests
  - oracle/oracle-manifest digests
  - support-traceability digest
  - seeded sequence/replay/generator-class digests
  - counter snapshot
  - authority reopen count
  - fact extraction width
  - all named slope digests
- Added a dedicated Phase 6 certification test file:
  `projection_consumption/tests/phase_six_certification.rs`
- Updated the certification bundle constructor compile-fail stderr fixture after
  the certification bundle and support-matrix shapes grew new private fields.
- Verification passed after the closeout implementation:
  - `cargo fmt --all`
  - `cargo test -p forge-query projection_consumption -- --nocapture`
  - `cargo test -p forge-query --test phase_boundaries_projection_consumption_compile_fail -- --nocapture`
  - `cargo test -p forge-query`
- Ran a skeptical reread of the full `9.3.4` spec and both test-requirements
  docs before declaring closeout confidence.
- Found and fixed two real Phase 6 weaknesses that were still softer than the
  spec claimed:
  - `projection_consumption/certification/seeded.rs` had only been hashing seed
    choices; it now executes real lifecycle and failure lanes for seeded rows,
    derives replay evidence from actual declaration/contract/fact-set/receipt
    or failure digests, and proves stable replay vs changed-seed divergence.
  - `projection_consumption/certification/fixtures.rs` and
    `projection_consumption/certification/oracles.rs` had a parity lane that was
    effectively the same helper path as control; parity now goes through the
    public authoring builder/surface path and normalizes to the same proof
    outputs as the direct declaration path.
- Added stronger certification tests for both corrections:
  - seeded rows now prove executed-lane-backed replay behavior
  - oracle parity now proves distinct authoring path normalization all the way
    through fact-set and receipt digests
- Verification passed again after the skeptical QA corrections:
  - `cargo fmt --all`
  - `cargo test -p forge-query projection_consumption -- --nocapture`
  - `cargo test -p forge-query --test phase_boundaries_projection_consumption_compile_fail -- --nocapture`
  - `cargo test -p forge-query`
- Filled the missing product-doc gap for `9.3.4` projection consumption.
- Added a new canonical feature doc at
  `crates/forge-query/docs/projection-consumption.md` that documents:
  - the common path on read results, write receipts, and query-context
    execution artifacts
  - support discovery
  - the advanced declaration -> eligibility -> contract -> extraction -> receipt
    path
  - receipt-first inspection and envelope usage
  - anti-patterns and current limits
- Added cross-links so the feature is discoverable from the surrounding doc set:
  - `docs/workspace-overview.md`
  - `docs/reads-observe-materialize.md`
  - `docs/read-composition.md`
  - `docs/inspection.md`
  - `docs/writes-and-intents.md`
- Updated `_docs/forge-query/reference/README.md` so projection consumption is
  now listed as a required feature-reference family rather than being left
  implicit in milestone docs.
