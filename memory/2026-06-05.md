# 2026-06-05

- Corrected the Hadwiger Phase 5 lower-bound framing: `chi(plane) >= 6` is not
  an implementation fixture gap, because it would require admitted
  non-5-colorability for a finite unit-distance graph, i.e. progress on the
  current Hadwiger-Nelson frontier. Updated the milestone to describe the
  generic `not-k-colorable => chi(plane) >= k + 1` sealed rule and added a
  regression test proving the `k = 5` lane remains blocked without real
  unit-distance plus non-5-colorability authority.
- QA found and fixed a Phase 5 authority-chain gap: lower-bound admission now
  requires colorability checker artifacts to carry a typed graph-version
  reference, colorability aspects to carry typed `k`, and proof admission to
  reject crossed checker/aspect evidence with `ArtifactMismatch`.
- Implemented Hadwiger Phase 6 retained recovery/explanation evidence. Added a
  private explanation subsystem with facade-only operations for checker
  rejection explanations, partial-admission explanations, and exact retention
  of Query recovery briefs. The artifacts retain blockers/checkers/aspects as
  source records and deliberately do not admit theorem authority. Full
  `cargo test -p hadwiger-research` and line-cap audit passed.
- Phase 6 QA found and corrected four authority-shape gaps: repair obligations
  are now typed request-time records instead of raw strings, admitted checker
  artifacts cannot be explained as rejections, Query recovery explanations keep
  the real Query stop-family classification, and missing/stale/conflict proof
  blockers materialize conservative escalation evidence.
- Implemented Hadwiger Phase 7 discovery loop foundation: retained evidence
  corpora, graph-resident failures, failure fingerprints, motifs, hypotheses,
  dead-end suppression proofs, experiment batches, discovery frontiers, derived
  frontier recomputation, counters, and compile-fail boundaries. The loop stays
  in-process and non-authoritative: no theorem authority, no Query invariant
  registration, no checker execution, and no fabricated Query receipts.
- Fixed Phase 7 QA findings before Phase 8: discovery suppression now consumes
  retained `HadwigerReusableNegativeEvidence` directly instead of fabricating a
  synthetic negative-evidence/graph-failure shim, and discovery readiness
  counters now reflect an actual Query declaration-entry readiness call only
  when Query recovery evidence participates.
- Implemented Hadwiger Phase 8 research graph invariant catalog draft:
  facade-only catalog/rule/violation/denial/registration-plan APIs, stable
  canonical artifacts, typed invariant violations for suppression/readiness
  legality, real Query invariant-denial materialization wiring when a caller
  supplies a `ForgeQueryLowerRuntimeBoundaryEnvelope`, and blocked registration
  planning that names Query runtime surfaces without registering invariants.
  Full `cargo test -p hadwiger-research --jobs 1`, fmt/check, Phase 8
  trybuild boundaries, and direct line-cap audit passed.
- Finished the Phase 8 Query-sourced denial hardening after adding Query's
  public lower-runtime boundary envelope source contract: Hadwiger denial
  requests now accept any sealed `ForgeQueryLowerRuntimeBoundaryEnvelopeSource`,
  retain source kind/digest plus envelope digest, preserve the old envelope
  compatibility method through the source path, and compile-fail raw strings,
  binding targets, external source impls, and source-field mutation.
- Implemented Hadwiger Phase 9 external-agent advisory intake. Added a private
  `agent_advisory` subsystem with facade-only source records, batch intake,
  motif/hypothesis/repair/proposal suggestions, declaration-scoped admission
  advisory materialized through Query contribution-composed transcripts, and
  proposal screening that stays advisory until Phase 7 suppression and Phase 8
  legality gates are used. Advisory artifacts remain non-theorem and non-Query
  invariant authority. Made `AdvisoryNoteDeclarationFamily` descriptive but
  bindable through a relational route so declaration-scoped Query contributions
  have a real target. Ordinary tests, ignored compile-boundary suite, fmt/check,
  and direct line-cap audit passed.
- Phase 9 QA fixed concrete gaps: declaration-advisory source identity now
  includes actual candidate/detail tokens instead of equal-length surrogates,
  contribution records require a real Query contribution digest, declaration
  advisory materialization now carries both admission and support contribution
  intent, grouped agent advisory is exposed through real Query grouped
  contribution surfaces, proposal screening checks Phase 8 catalog gate coverage,
  and overgrown trybuild boundary folders were split by proof surface.
- Implemented Hadwiger Phase 10 research cockpit and certification bundle:
  facade-only cockpit sessions, action packets, replay, purpose-indexed
  equivalence classes, tile/contact equivalence witnesses, cockpit reports,
  counters, and milestone certification bundles. The cockpit stays derived and
  non-authoritative: it dedupes/plans/explains next moves without executing
  experiments, running AI, registering invariants, or minting checker/proof
  authority. Fmt, check, full ordinary tests, ignored compile boundaries, and
  line/directory cap audits passed.
- Phase 10 QA tightened the cockpit foundation: action equivalence classes are
  now derived after all blockers are classified, partial-admission evidence
  produces explicit missing-checker proof-work blockers, retained tile
  witnesses produce tile-equivalence duplicate-checker blockers/counters,
  certification bundles inventory retained corpus evidence rows, and
  certification scenario rows now retain real digests instead of counts or
  booleans.
