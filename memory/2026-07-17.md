# 2026-07-17

## Worth Store C.1

- Implemented Worth Store C.1 phases 1-6: frozen proof inventory and preservation ledger, typed proof-product selection, workspace profiles and feature lanes, owner-build-closure policy, and six responsibility-shaped certification suites.
- The pre-cleanup inventory records 190 original testable targets and 2,318 proof identities; current validation preserves those identities through 103 targets. A fake UI-fixture discovery row and an artificial directory file-count claim are explicitly quarantined rather than silently counted as proof.
- `cargo store-smoke` now selects five exact behavioral scenario specimens plus one bounded compiler-boundary specimen. QA caught and fixed two false-green filter bugs before accepting the run.
- Consolidation exposed stale S10 fresh-process test names, an owner-DAG probe that relied on `nodes()[0]`, and hand-encoded 34-byte physical headers after the production header grew to 48 bytes. These now use qualified child identities, typed owner selection, and the production header encoder.
- Final bounded smoke passed 41 behavioral tests plus the compiler-boundary specimen; C.1 controller tests, proof preservation, Road 1 boundary-check, and agent-context checks were green. A blanket durability suite remains intentionally outside the ordinary loop and exceeded the 10-minute command ceiling because it includes the heavy S10 matrices.
- A second hostile C.1 QA pass found and fixed incomplete rustdoc discovery (ordinary and included-Markdown fences), false runnable cases created from closing non-Rust fences, concatenated `include_str!` line-coordinate errors, synthetic zero-match doctest filters, suffix-forgeable historical aggregate exclusions, renamed optional-dependency closure errors, substring-based feature authority, cross-owner textual test inclusion, and weak current-proof drift detection.
- Current executable evidence now has exact reverse parity for 103 libtest targets and 69 rustdoc feature lanes, including per-target test-name multiplicity, environment identity, and schema validation. Current proof bodies and certification suite source closures are fingerprint-sealed using repository-relative paths; unknown authority schemas and source-ownership escapes deny.
- The historical baseline and pre-consolidation execution evidence remain irrecoverably incomplete. Status files now say this plainly: current topology is strongly sealed, but Phase 6/C.1 cannot claim historical same-seed behavioral parity or closeout.

## Worth UI 3.8

- Closed Worth UI milestone 3.8 through Phase 22 on branch `ui`.
- Added receipt-backed allocation inspection, typed freshness/consumer admission,
  allocation counters and denial taxonomy, and hostile runtime certification.
- Corrected catalog activation so bindings and successor ledgers retain the
  same report-bearing committed receipt truth.
- Final evidence: runtime feature suite 817/817; certification 13/13; all other
  Worth UI packages green when run in resource-stable groups; boundary-check,
  agent-context, and diff hygiene green; every changed Rust file <=400 lines.
- Repository-wide strict Clippy and the root line-cap script still expose
  pre-existing baseline debt outside the milestone diff; do not represent those
  global gates as green.
