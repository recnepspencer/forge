## Worth M7.3 Phase 23 Closeout

- Finished split edge-chain ledger closeout by adding fast ledger replay and poisoned-authority tests.
- Important production lesson: split persistent naming and split decision-log affected-artifact identities must key overlap chains by the canonical overlap chain identity, not member-scoped row identities or persistent-name row identities. The ledger is the downstream authority boundary and binds by split artifact identity.
- Verification run: focused `worth-spatial` split ledger, split decision log, and persistent naming tests; `worth-kernel` public split-ledger target compiled with `--no-run`; cross-crate `cargo check` passed with existing topo warnings.
