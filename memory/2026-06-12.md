# 2026-06-12

- Worth UI M3 Phase 30 implemented the source-ingress/file-watch boundary.
  Added `runtime::source_ingress` with typed watcher events, source providers,
  debounce policy, source package revisions, candidate ordering receipts,
  watched artifact inputs, source ingress hooks, watched candidate submissions,
  denials, and counters. The runtime host now exposes `source_ingress(provider)`
  as an ingress/session builder that can debounce and lower into the existing
  replacement candidate pipeline but cannot mutate active runtime state.
  Hostile tests cover event-burst equivalence, raw-event non-authority,
  file/Rust artifact provider parity, reordered event determinism,
  partial-write/atomic-rename collapse, unstable partial-write denial,
  in-memory provider admission parity, and missing candidate material denial.
  Added facade trybuild coverage proving source-ingress types are importable,
  ordering receipts are not publicly mintable, and raw watcher events cannot
  declare dependency impact.
- Phase 30 QA loop found and fixed three ingress-boundary gaps: providers with
  both file source and Rust-authored artifact material could silently prefer
  artifact material, multiple artifact inputs could silently let the first
  artifact win without merge semantics, and empty source ingress hooks could be
  accepted as no-op outputs instead of failing closed. Added typed denials and
  hostile regressions for mixed candidate material, multiple artifact inputs,
  and empty hook output. Verification passed with `cargo check -p worth-ui`,
  focused source-ingress tests, the source-ingress facade trybuild suite, full
  worth-ui lib tests, scoped diff hygiene, and line-cap checks. The full crate
  trybuild-heavy suite still exceeds the local timeout window while passing
  many cases before timing out.
- Phase 30 qa-tests/code-quality pass found and fixed additional proof and
  structure issues: the facade compile-pass example accidentally blessed an
  invalid mixed file/artifact provider, source-lowering failures collapsed
  distinct package/parse/lowering failures into `NoCandidateMaterial`, ordering
  receipt drift only checked source digest instead of the full revision link,
  and the boundary test file was carrying fixture mechanics near the line cap.
  Fixed by making the facade pass example use a single material family,
  adding typed stage denials for source package, parse, artifact resolution,
  structure, binding, identity, and canonical assembly failures, checking the
  full ordering receipt/revision match, adding hostile regressions for package
  rejection, parse rejection, and receipt sequence drift, and moving fixture
  construction to `source_ingress_test_support`. Verification passed with
  `cargo check -p worth-ui`, focused source-ingress tests, source-ingress
  facade trybuild, full worth-ui lib tests, scoped diff hygiene, and line-count
  checks.
- Implemented Worth UI M3 Phase 31 counter taxonomy and measurement boundary.
  Added `runtime::measurement` with typed runtime counter families, measurement
  boundaries, frame-cost counters, Query evidence posture references,
  replay-stable counter packets, hot-path complexity contracts, certification
  denials, and a Foundational counter bridge that only lowers certified Worth
  UI counter evidence. The facade exposes inspection/certification types while
  trybuild fences prevent public field minting and unattributed work-bucket
  construction. Hostile tests cover deterministic replay ordering, missing
  hot-path boundaries, diagnostic richness not changing active plan digest,
  unattributed work rejection, elapsed-time-only frame-cost denial,
  Foundational lowering without Worth UI evidence, successful certified
  Foundational counter lowering, and Foundational boundary mismatch. Verification
  passed with `cargo fmt -p worth-ui --check`, `cargo check -p worth-ui
  --quiet`, `cargo test -p worth-ui measurement --lib --quiet`,
  `cargo test -p worth-ui --test runtime_measurement_facade_compile --quiet`,
  `cargo test -p worth-ui --lib --quiet`, `git diff --check`, and new-file
  line counts. Full `cargo test -p worth-ui --quiet` timed out in the broad
  trybuild-heavy matrix again; the leftover cargo/rustc process was stopped.
- Phase 31 QA loop found and fixed real proof-boundary gaps: arbitrary
  family-prefixed but invalid counter names could certify until bridge time,
  zero counted-work rows could certify hot-path claims, Query-owned counter
  families did not require Query evidence, plan lowering/assembly/admission
  were mapped to Foundational maintenance planning instead of counter-backed
  maintenance execution, and the Foundational bridge emitted loose specs/rows
  rather than a real counter-backed receipt and prepared performance basis.
  Fixed by validating counter and contract labels before certification,
  requiring nonzero Worth UI counted evidence, requiring Query evidence for
  Query-owned families, moving counted plan work to maintenance execution,
  carrying Foundational posture fields on `WorthUiComplexityContract`, and
  lowering certified packets through `performance_api::common_path` plus
  `lower_lane::basis`/`receipts`. Added regressions for all corrected gaps.
  Verification passed with fmt/check, focused measurement tests, measurement
  facade trybuild, full worth-ui lib tests, diff hygiene, and line counts. Full
  package `cargo test -p worth-ui --quiet` still timed out in the broad
  trybuild-heavy matrix; no leftover compiler processes remained.
- Implemented Worth UI M3 Phase 32 reload/lowering counter boundary. Added
  `runtime::reload_counter_boundary` with typed stop stages, sealed
  reload/lowering counter receipts, exact Phase 32 counter schema validation,
  Query evidence carry/rediscovery accounting, broad-scan denial, certification
  into Phase 31 measurement packets, and Foundational lowering through the
  existing counter-backed receipt bridge. Added hostile tests for equivalent
  reload counter replay, full artifact scan regression, invalid candidate
  stopped-boundary counters, repeated Query support rediscovery, missing/
  duplicate/unexpected counter rows, and complete Foundational lowering. Added
  facade trybuild coverage for importable public types, private receipt fields,
  and raw receipt denial at the Foundational bridge. Verification passed with
  formatting, `cargo check -p worth-ui`, focused Phase 32 runtime tests,
  dedicated Phase 32 facade trybuild, full worth-ui lib tests, diff hygiene,
  no trybuild WIP residue, and touched-file line caps. Full `cargo test -p
  worth-ui --quiet` again timed out in the broad trybuild-heavy matrix with no
  leftover compiler processes.
- Phase 32 QA loop found and fixed two proof-boundary gaps: receipt
  `certify()` originally wrapped a reload/lowering receipt without proving the
  exact Phase 32 row schema, and Query-bound counter evidence used a synthetic
  placeholder instead of binding to the carried Query support receipt digest.
  Moved schema validation into receipt seal/certification, required carried
  Query evidence for Query rebind packets, bound Query evidence to
  `WorthUiQuerySupportReceipt::receipt_digest()`, and replaced an internal
  measurement `expect` with typed construction denial propagation. Verification
  passed with focused Phase 32 tests, Phase 32 facade trybuild, `cargo check`,
  full worth-ui lib tests, formatting, diff hygiene, no WIP residue, and
  touched-file line caps.

- Worth UI Milestone 3 spec was created as a merged hot-runtime/execution-plan
  foundation, folding the old M3 hot reload and old M4 execution-plan/frame-cost
  roadmap concepts into one 37-phase milestone. The roadmap was updated so
  later milestones shift down and the later canvas/spatial milestone now
  consumes the M3 lane substrate instead of re-declaring it.
- Worth UI M3 was updated to consume Forge Foundational performance surfaces as
  boundary vocabulary/proof envelopes: Worth UI emits runtime and frame counters,
  then lowers them into Foundational claims, canonical bundles, counter-backed
  receipts, planned reports, certified bundles, and readiness closure. Foundational
  must not become the Worth UI frame runtime, counter store, lane executor, or
  plan topology.
- Worth UI M3 was tightened with ambitious-but-bounded support surfaces:
  lifecycle/frame-epoch activation, broader impact surfaces, keyed/moved
  identity, durable/transient state families, plan-node and egui boundary
  shapes, diagnostics tiers, source ingress edge cases, and typed extension hook
  admission. Hooks are admitted contributions for special mechanics, never
  bypasses around active plan, Query, state, lane, or certification authority.
- Worth UI M3 Phase 1 implementation started the runtime authority kernel:
  internal runtime modules now own active artifacts/plans, frame epochs,
  pending activation staleness checks, last-valid preservation, runtime
  lifecycle/status observations, shutdown receipts, and facade-only host
  observation. New tests cover active equivalence, last-valid ordering, stale
  activation rejection, diagnostics non-authority, shutdown receipts, and
  compile-fail facade boundaries.
- QA loop on M3 Phase 1 found and fixed authority-surface gaps: the public
  runtime host is no longer cloneable/comparable as app-owned value truth,
  launch frame-epoch override was removed from the public surface, active-plan
  bootstrap naming no longer calls the sealed launch plan a placeholder, the
  runtime module no longer uses broad dead-code suppression, and compile-fail
  tests now directly cover app-local active-state construction and forged
  last-valid receipt attempts.
- QA-tests/code-quality pass on M3 Phase 1 strengthened the runtime proof
  suite and structure: runtime tests moved from empty-artifact happy paths to
  non-empty canonical artifact meaning, added a distinct-artifact active-truth
  divergence test, added compile-fail coverage proving the runtime host cannot
  be cloned by app code, renamed the runtime test module by responsibility, and
  split active runtime authority into artifact, execution-plan, observation,
  and state files.
- Worth UI M3 Phase 2 implemented the replaceable artifact candidate boundary:
  a runtime candidate subsystem now seals canonical artifacts with digest-bound
  dependency metadata, preserves authoring lane and source cause as
  observational facts, exposes candidate basis for later admission, and rejects
  missing or stale metadata before Phase 3 can consume a candidate. Tests cover
  file/Rust lane equivalence, cause independence, missing proof rejection,
  stale dependency metadata even with spoofed matching digest, and dependency
  metadata participation in candidate basis.
- QA loop on M3 Phase 2 found and fixed proof/performance weaknesses: candidate
  ingress now derives artifact digest and dependency metadata once on the normal
  path, stale validation remains at the untrusted sealing boundary, dependency
  metadata identity no longer depends on `Debug` formatting, provenance handles
  are explicit candidate facts, and artifact-bearing candidate packets are
  move-only instead of cloneable.
- QA-tests/code-quality pass on M3 Phase 2 strengthened proof pressure and
  structure: lane-equivalence now runs through real file-source and Rust
  authoring lowerers into canonical artifacts instead of manual artifact
  fixtures, candidate proof packets gained compile-fail facade tests for
  non-cloneability and private bundle fields, and dependency metadata digest
  construction moved into its own candidate responsibility file.
- Worth UI M3 Phase 3 implemented the candidate admission boundary. Replacement
  candidates now carry a snapshot-bound lowering basis and Query support receipt
  derived from dependency metadata, active runtime truth exposes an admission
  basis, and `WorthUiCandidateAdmission` produces either a sealed
  `WorthUiAdmittedReplacementCandidate` or a typed admission report/denial
  before comparison or plan lowering can run. Hostile tests cover deterministic
  replay, snapshot mismatch ordering, deferred runtime posture, deferred Query
  support, receipt tamper detection, and facade compile-fail guards for raw
  candidates entering admitted paths or minting admitted packets.
- QA loop on M3 Phase 3 found and fixed a Query posture collapse: dependency
  metadata receipts originally froze hook identity but treated derived runtime
  Query hooks as supported. Admission now preserves deferred vs unsupported
  Query/runtime posture as separate typed denials, with tests that force real
  Query support and live-compatibility postures to fail before comparison or
  plan lowering.
- QA-tests/code-quality pass on M3 Phase 3 strengthened the proof suite without
  widening production structure: admission now has an integration-style test
  that lowers a real Query-owned Rust-authored binding into a canonical artifact,
  derives dependency metadata, seals a candidate, and proves the admission
  receipt carries the four Query runtime hooks. Receipt unit tests now pin
  disabled capability, deferred basis, deferred/denied live compatibility, and
  unsupported-over-deferred precedence.
- Worth UI M3 Phase 4 implemented the artifact-equivalence consumption boundary:
  runtime comparison now consumes the M2 artifact equivalence basis and admitted
  candidate receipts before any impact narrowing or plan lowering can run. The
  runtime exposes typed comparison evidence, no-op vs meaningful-difference
  outcomes, counters proving ordering, and denial paths for changed admission
  receipts or mismatched runtime equivalence basis. Tests cover same-basis
  determinism, diagnostic-richness irrelevance, meaningful-difference ordering,
  mismatched-basis rejection, and facade compile-fail proof that raw candidates
  cannot enter runtime comparison.
- QA loop on M3 Phase 4 found and fixed two boundary-evidence weaknesses:
  runtime equivalence-basis mismatch denials now carry the candidate basis as
  typed evidence, and the diagnostic/provenance independence proof now varies
  candidate provenance handles while requiring identical runtime comparison
  output. The comparison test also asserts the consumed artifact equivalence
  basis matches the runtime basis.
- QA-tests/code-quality pass on M3 Phase 4 found and fixed two further proof
  and structure gaps: Phase 4 now has an integration-style runtime comparison
  test that launches from parsed file-authored source and compares a
  Rust-authored candidate with reversed authored module order, proving runtime
  comparison consumes canonical artifact meaning rather than source-order or
  provenance folklore. Runtime comparison also has a Phase 4-local receipt-drift
  denial test proving changed admission evidence stops before artifact work.
  Comparator orchestration was renamed into explicit receipt, basis, and
  artifact-equivalence steps.
- Worth UI M3 Phase 5 implemented the replacement impact classification
  boundary. Runtime impact now has typed no-op, local subtree, structural,
  broad, lane-affecting, unsupported, command, token theme, accessibility,
  renderer resource, lane, durable-state receipt, counter, classification, and
  denial vocabulary. `WorthUiRuntimeHost::classify_replacement_impact` consumes
  both runtime comparison evidence and an admitted candidate, rejects changed
  admission receipts, keeps plan-lowering counters at zero, classifies
  lane-affecting surface changes before lowering, keeps token-only changes local,
  and rejects broad replacements without durable-state receipts. Tests cover the
  named Phase 5 hostile cases plus a facade compile-fail guard proving raw
  candidates cannot enter impact classification.
- QA loop on M3 Phase 5 found and fixed proof gaps in the impact boundary:
  impact classification now rejects runtime comparisons whose active basis or
  candidate artifact digest does not match the admitted candidate, source
  artifact equivalence carries a structured surface semantic delta so command
  slot changes can stay local while placement-class changes remain
  lane-affecting, and the Phase 5 fixtures were split under the workspace line
  cap. Full `cargo test -p worth-ui`, `git diff --check`, line-cap checks, and
  trybuild WIP checks passed after the fixes.
- QA-tests/code-quality pass on M3 Phase 5 found and fixed a multi-cause impact
  gap: a surface semantic change that affected both placement class and command
  slots previously kept the lane-affecting scope but lost the command impact
  posture. Artifact equivalence now records a combined
  surface-placement-and-command delta, impact classification preserves
  `BindingOnly` command posture even when lane impact dominates scope, and the
  tests now split token-only, command-only, and combined lane-plus-command
  scenarios. A fake identity helper in the artifact comparator was removed.
- Worth UI M3 Phase 6 implemented the dependency impact narrowing boundary:
  runtime narrowing consumes Phase 5 classifications plus admitted candidate
  dependency metadata, emits typed affected source modules, subtree digests,
  command/token/accessibility/renderer invalidations, lane impact, Query
  dependency invalidations, and lookup counters, and refuses forged Query
  support that lacks runtime-owned hooks. Tests prove equivalent metadata yields
  equivalent impact, module changes do not full-scan artifacts, Query-owned
  runtime links are preserved, Query-bound changes cannot narrow to UI subtrees
  alone, renderer resources do not broaden unrelated subtrees, and facade
  compile-fail guards prevent raw candidates or minted counters/results from
  crossing the authority boundary.
- QA loop on M3 Phase 6 found and fixed two boundary gaps: narrowing now
  verifies that an admitted Query-support receipt's runtime-hook count matches
  the candidate dependency graph's runtime-hook inventory, preventing forged
  partial Query posture from passing merely because some affected hook exists;
  and lane-affecting impact classifications now carry a dependency-backed
  replacement scope so Phase 6 can still report affected modules/subtrees while
  preserving lane parity. Added hostile tests for forged Query receipt counts
  and lane-affecting dependency narrowing scope. Verification passed with
  focused Phase 5/6 suites, runtime authority trybuild, `cargo check -p
  worth-ui`, full `cargo test -p worth-ui --quiet`, line-cap audit, WIP sweep,
  and diff hygiene.
- Worth UI M3 Phase 22 implemented the canvas/spatial lane boundary. Added a
  sibling runtime lane for canvas plan rows, typed viewport hit-test plans,
  overlay plans, draw/hit-test/tool-state hook wrappers, frame receipts,
  certification, counters, host methods, facade exports, and hostile tests for
  equivalent plans, domain geometry/renderer ownership rejection, command and
  selection identity preservation, raw domain hit-test denial, draw hook
  counter preservation, stale lane admission, missing hook/support, and
  non-canvas target claims. Verification passed: focused canvas runtime tests,
  canvas facade trybuild, `cargo check -p worth-ui`, `cargo test -p worth-ui`,
  line-cap audit, WIP check, and diff hygiene.
- Phase 22 QA loop found and fixed two completion gaps: pan/zoom was named in
  the spec but missing an explicit typed frame surface, and selection identity
  was represented by a hook digest rather than typed platform handles. Added
  `WorthUiCanvasViewportPlan` with typed zero-zoom denial, viewport transform
  lane/counter/receipt coverage, overlay replay equivalence coverage, and
  selection state-slot handles on canvas plans/receipts. Verification after QA:
  focused canvas runtime tests, canvas facade trybuild, `cargo check -p
  worth-ui`, full `cargo test -p worth-ui`, line-cap audit, WIP hygiene, and
  diff hygiene all passed.
- QA-tests/code-quality pass on M3 Phase 6 found and fixed weaker proof
  topology in the runtime impact narrowing tests and denials: renderer-resource
  narrowing now includes an unrelated surface and proves only the affected
  handle narrows, Query invalidation tests assert the exact surface set rather
  than substring presence, receipt-vs-metadata mismatch is separated from
  missing affected Query invalidations with a dedicated denial variant, and
  query-bound fixtures moved into a narrow test-support module to keep files
  under the workspace line cap. Verification passed with focused Phase 6 tests,
  Phase 5 replacement impact tests, runtime authority trybuild, `cargo check -p
  worth-ui`, full `cargo test -p worth-ui --quiet`, line-cap audit, WIP sweep,
  and diff hygiene.
- Worth UI M3 Phase 7 implemented the runtime identity match graph boundary:
  active and admitted candidate artifact nodes are indexed by stable identity
  seed, duplicate active or candidate identities fail closed before state
  reconciliation, source reordering does not affect correspondence, visually
  similar nodes with different identity do not match, keyed repeated-template
  items survive reorder while position-only template identity is rejected, and
  stable moved nodes produce explicit moved-node evidence. The runtime host now
  exposes `build_identity_match_graph` after impact narrowing, and the facade
  exports read-only graph/report/counter/evidence types while trybuild guards
  block raw candidates, public graph/counter construction, and identity seed
  contributions from directly preserving state. Verification passed with
  focused Phase 7 tests, Phase 5/6 regression suites, runtime authority
  trybuild, `cargo check -p worth-ui`, full `cargo test -p worth-ui --quiet`,
  line-cap audit, WIP sweep, and diff hygiene.
- QA loop on M3 Phase 7 found and fixed two identity-boundary proof gaps:
  matching now denies same-basis active/candidate nodes when their artifact node
  kinds differ, with explicit mismatch counters and a hostile regression test;
  and `WorthUiIdentitySeedContribution` no longer exposes any public or dormant
  admission-shaped constructor, with trybuild proving external code cannot mint
  an admitted contribution. Verification passed with focused Phase 7 tests,
  runtime authority trybuild, `cargo check -p worth-ui`, full
  `cargo test -p worth-ui --quiet`, line-cap audit, WIP sweep, and diff hygiene.
- QA-tests/code-quality pass on M3 Phase 7 strengthened identity-match proof:
  graph node observation tests now assert real active/candidate node side,
  kind, identity, stability, and durable-state eligibility instead of constant
  self-comparisons; same-basis/different-kind ambiguity is denied on active,
  candidate, and active-vs-candidate boundaries with exact counters; and the
  builder now rejects duplicate collisions before map insertion and avoids
  cloned key-set intersection allocation. Verification passed with focused
  Phase 7 tests, runtime authority trybuild, `cargo check -p worth-ui`, full
  `cargo test -p worth-ui --quiet`, line-cap audit, WIP sweep, and diff hygiene.
- Worth UI M3 Phase 8 implemented the replacement classification boundary:
  identity matches, impact classification, and dependency narrowing now lower
  into a typed `WorthUiNodeReplacementPlan` with explicit preserve, replace,
  drop, create, move, rebind, and lane-change transitions. Ambiguous identity
  graphs and digest mismatches fail before reconciliation, plan/counter fields
  are not publicly mintable, and lane changes are classified independently of
  structural moves, including same-handle lane semantic changes. Verification
  passed with focused Phase 8 tests, Phase 7 identity regressions, runtime
  authority trybuild, `cargo check -p worth-ui`, full `cargo test -p worth-ui
  --quiet`, line-cap audit, WIP sweep, and diff hygiene.
- QA loop on M3 Phase 8 found and fixed three boundary weaknesses:
  lane-affecting impact classifications now fail closed unless Phase 6 narrowing
  carries explicit lane-parity evidence; duplicate replacement classification
  detection now uses a phase-local identity set instead of scanning emitted
  classifications; and node replacement classifications expose an explicit
  `unrestored_durable_state_carry_permitted` predicate so drop/create/replace
  and lane-change transitions cannot be mistaken for ordinary durable-state
  carry-forward. Verification passed with focused Phase 8 tests, Phase 7
  identity regressions, runtime authority trybuild, `cargo check -p worth-ui`,
  full `cargo test -p worth-ui --quiet`, line-cap audit, WIP sweep, and diff
  hygiene.
- QA-tests/code-quality pass on M3 Phase 8 strengthened proof and structure:
  replacement classification tests now prove partial structural scope preserves
  unaffected identities and lane-affecting plans deny when lane evidence has no
  affected lane scope. The classifier gained an explicit denial for empty
  lane-affecting scope, replaced broad classification parameter plumbing with a
  named classification accumulator, removed a fake `Result` from transition
  classification, and dropped the replacement module's broad non-test
  `dead_code` allowance. Verification passed with focused Phase 8 tests, Phase
  7 identity regressions, runtime authority trybuild, `cargo check -p
  worth-ui`, full `cargo test -p worth-ui --quiet`, line-cap audit, WIP/allow
  sweep, and diff hygiene.
- Worth UI M3 Phase 9 implemented the durable UI state inventory boundary:
  runtime now exposes a sealed inventory builder after node replacement
  planning, platform-owned durable families for focus chain, scroll anchors,
  selection ranges, text edit buffers, splitter positions, tab state, and panel
  visibility, plus custom family hooks that must declare owner identity,
  replacement policy, and persistence posture before admission. Domain truth,
  missing owner/policy/posture, duplicate custom family claims, and custom hooks
  claiming reserved platform families fail before reconciliation; transient
  hover/pressed/capture/animation/gesture state defaults to drop. Verification
  passed with focused Phase 9 tests, runtime authority trybuild, full
  `cargo test -p worth-ui --quiet`, line-cap audit, WIP sweep, and diff
  hygiene.
- QA loop on M3 Phase 9 found and fixed three boundary weaknesses: inventories
  could previously be built without the complete platform-owned state family
  set, custom hooks could carry blank family or owner identity strings, and
  custom hooks could spoof platform ownership via the public platform owner
  constructor. The builder now fails closed on missing platform families before
  hook validation, rejects blank custom ids/owner identities, rejects platform
  owner identities on custom hooks, and gives built-in platform families
  distinct owner identities. Tests now cover incomplete platform inventory,
  malformed custom ids/owners, platform-owner spoofing, complete custom-family
  admission, and validation ordering. Verification passed with focused Phase 9
  tests, runtime authority trybuild, `cargo check -p worth-ui`, full
  `cargo test -p worth-ui --quiet`, line-cap audit, WIP sweep, and diff
  hygiene.
- QA-tests/code-quality pass on M3 Phase 9 strengthened both proof and
  topology: durable-state inventory tests now prove platform-family and custom
  hook registration order canonicalization, assert exact missing-platform
  family/counter evidence, and moved fixture construction into a dedicated
  `durable_state_inventory_test_support` module. Production state inventory
  files were split from one flat directory into family, inventory, ownership,
  and transient submodules so future durable-state work has predictable
  structural homes. Verification passed with focused Phase 9 tests, runtime
  authority trybuild, `cargo check -p worth-ui`, full `cargo test -p
  worth-ui --quiet`, line-cap audit, WIP sweep, and diff hygiene.
- Worth UI M3 Phase 10 implemented the durable state reconciliation boundary:
  runtime now exposes a sealed reconciliation planner after node replacement
  classification and durable state inventory. Reconciliation consumes only the
  Phase 8 node replacement plan plus Phase 9 inventory, emits deterministic
  per-node/per-family receipts, preserves focus/scroll/selection/text/splitter/
  tab/panel/custom state only through family-specific rules, removes orphan
  state on dropped nodes, rejects stale inventory digests, and leaves
  Query-backed selection identity as explicit posture evidence rather than
  host-local graph folklore. Facade compile tests prove reconciliation plans,
  receipts, carry-forward evidence, and replacement evidence cannot be minted
  externally. Verification passed with focused Phase 10 tests, runtime authority
  trybuild, `cargo check -p worth-ui`, full `cargo test -p worth-ui --quiet`,
  line-cap audit, WIP sweep, and diff hygiene.
- QA loop on M3 Phase 10 found and fixed four proof/discipline gaps:
  ambiguous node replacement plans now have a direct reconciliation-denial test,
  reconciliation receipt lookup uses sorted binary search instead of an
  unadvertised scan, custom lane-change reconciliation asserts the exact
  recreate/drop/carry counters, and reconciliation counters now have a
  compile-fail minting guard. The loop also split two over-cap runtime test
  files into focused modules so touched Rust files stay under 400 lines.
  Verification passed with focused reconciliation/inventory/identity tests,
  runtime authority trybuild, `cargo check -p worth-ui`, full `cargo test -p
  worth-ui --quiet`, touched-file line-cap audit, WIP sweep, and diff hygiene.
- QA-tests/code-quality pass on M3 Phase 10 strengthened hostile proof and
  structure: reconciliation tests now prove receipt cardinality for every
  node/family pair, active and candidate inventory digest drift, missing
  platform-family denial before receipt construction, and custom hook
  drop/replace outcomes declared by inventory policy. Per-family carry and
  replacement rules moved out of the planner into the named reconciliation
  family modules, and fake fallibility was removed from deterministic receipt
  construction. Verification passed with focused reconciliation tests, runtime
  authority trybuild, `cargo check -p worth-ui`, full `cargo test -p worth-ui
  --quiet`, touched-file line-cap audit, WIP sweep, and diff hygiene.
- Worth UI M3 Phase 11 implemented the Query binding comparison boundary:
  runtime now exposes `compare_query_bindings` after node replacement planning
  and impact narrowing, compares active/candidate Query-bound references by
  typed Query identity and structured posture, detects basis and denial
  presentation drift before live subscription reuse, reports missing
  active/candidate Query bindings, and rejects stale node plans or narrowing
  digests. The comparison consumes existing runtime dependency hooks and bound
  view-binding evidence instead of rebuilding Query state. Verification passed
  with focused Query binding comparison tests, `cargo check -p worth-ui`, full
  `cargo test -p worth-ui --quiet`, `cargo fmt -p worth-ui --check`,
  `git diff --check`, touched-file line-cap audit, and WIP sweep.
- QA loop on M3 Phase 11 found and fixed three boundary gaps: Query inspection
  posture no longer treats UI placement role as Query meaning, runtime hook
  evidence now stays typed instead of debug-stringly, and local async/result
  enums have a compile-fail guard proving they cannot replace
  `WorthUiQueryBindingPosture`. Support/admission posture now includes the
  existing Forge Query support receipt at the stable status/runtime-hook-count
  level, intentionally avoiding topology/handle details that would reintroduce
  false binding drift when placement changes but Query meaning does not.
  Verification passed with focused Query comparison tests, runtime authority
  trybuild, `cargo check -p worth-ui`, full `cargo test -p worth-ui --quiet`,
  format check, diff hygiene, line-cap audit, and WIP sweep.
- QA-tests/code-quality pass on M3 Phase 11 strengthened the Query comparison
  proof: added posture drift pressure for support/admission + live
  compatibility, async result hooks, recovery hooks, and result-shape identity
  rebind before projection reuse. The new support/live test exposed a real
  production bug where artifact-link evidence overwrote runtime-hook live
  posture; evidence extraction now records artifact links first and lets
  runtime hooks supply runtime-derived posture. `query_binding/evidence.rs` was
  also split so index construction and accumulator/digest mechanics have
  separate structural homes. Verification passed with focused Query binding
  tests, runtime authority trybuild, `cargo check -p worth-ui`, full
  `cargo test -p worth-ui --quiet`, format check, diff hygiene, line-cap
  audit, and WIP sweep.
- Worth UI M3 Phase 13 implemented the activation staging boundary. Runtime now
  has a sealed `activation_staging` module with `WorthUiStagedReplacement`,
  `WorthUiPendingActivation`, readiness/report/counter surfaces, and typed
  staging denials. `WorthUiRuntimeHost::stage_replacement_activation` consumes
  the admitted candidate plus impact, narrowing, node replacement, durable-state
  reconciliation, and Query live rebind outputs, verifies receipt/digest
  agreement, rejects partial bundles, and leaves active runtime/last-valid truth
  untouched on denial. Readiness and staged packets are observable through the
  facade but not publicly mintable. Verification passed with focused activation
  staging tests, runtime authority trybuild, `cargo check -p worth-ui`, full
  `cargo test -p worth-ui --quiet`, diff hygiene, line-cap audit, WIP sweep, and
  residue sweep.
- QA loop on M3 Phase 13 found and fixed activation staging gaps: partial
  bundles now require a sealed pending execution-plan lowering input in addition
  to reconciliation and Query rebind plans, staging rejects stale lowering input
  even when active/candidate digests match but plan cardinalities differ, and
  trybuild proves external code cannot mint the lowering input. The activation
  staging test fixture was split into dedicated support to keep touched files
  under the workspace line cap. Verification passed with focused activation
  staging tests, runtime authority trybuild, `cargo check -p worth-ui`, full
  `cargo test -p worth-ui --quiet`, diff hygiene, line-cap audit, WIP cleanup,
  and residue sweep.
- QA-tests/code-quality pass on M3 Phase 13 strengthened the activation staging
  proof suite: tests now reject active-digest drift on staged node plans and
  same-digest stale execution-plan lowering inputs across node-plan,
  reconciliation, and Query rebind cardinalities. The code-quality pass kept
  the activation staging folder split by packet, readiness/report/counter,
  denial, input, and stager responsibilities, with test scenarios separate from
  fixture orchestration. Verification passed with focused activation staging
  tests, runtime authority trybuild, full `cargo test -p worth-ui --quiet`,
  diff hygiene, line-cap audit, and residue sweep.
- Worth UI M3 Phase 14 implemented the execution-plan input boundary. Runtime
  now has a split `execution_plan_input` module with sealed basis/context,
  counters, denials, typed node inputs, plan-node families, egui boundary
  markers, and component-hook admission. `WorthUiRuntimeHost` exposes
  `prepare_execution_plan_input`, consuming `WorthUiPendingActivation` so plan
  lowering cannot accept raw source, admitted candidates, local strings, or
  hand-built counters. The preparer rechecks epoch/readiness and staged input
  cardinalities, preserves Query-owned binding identities, emits zero
  source-parse/registry-lookup counters, and denies unregistered hook families.
  Verification passed with focused execution-plan input tests, runtime authority
  trybuild, `cargo check -p worth-ui`, and full `cargo test -p worth-ui
  --quiet`. A PowerShell line-cap audit found only pre-existing over-cap
  worth-ui files: `capability_snapshot.rs`, `worth_ui_source_parser.rs`, and
  `tests/facade_compile.rs`.
- QA loop on M3 Phase 14 found and fixed two proof gaps. Query-bound
  `WorthUiPlanNodeInput` now carries Query posture evidence from live rebind
  outcomes, including projection-consumption, async-result, recovery,
  preservation receipt, and required rebind surfaces. Component hook admission
  no longer exposes an `Unregistered(String)` family variant through the
  facade; unregistered hook families are internal/test-only denial cases while
  public hook family observation represents admitted plan-node families only.
  Verification passed with focused execution-plan input tests, runtime
  authority trybuild, `cargo check -p worth-ui`, lib tests, full `cargo test -p
  worth-ui --quiet`, diff hygiene, and touched-file line checks.
- QA-tests/code-quality pass on M3 Phase 14 tightened the execution-plan input
  boundary further. Tests now prove stale activation and unregistered component
  hook denials do no hidden plan construction work by asserting all construction
  counters stay zero, and query posture preservation is checked for every
  query-owned plan node rather than the first matching input. Production
  preparer structure now names each admission gate before collection, with
  unregistered hook rejection happening before staged/query/component node input
  construction. Verification passed with focused execution-plan input tests,
  runtime authority trybuild, `cargo check -p worth-ui`, full `cargo test -p
  worth-ui --quiet`, `git diff --check`, and touched-file line checks.
- Worth UI M3 Phase 15 implemented compact runtime handle allocation. Added
  `runtime::handle_allocation` with sealed plan-local runtime/component/
  command/token/child-range/view-binding/lane/state-slot handles, allocation
  basis, receipt, generation, family widths, counters, denials, and allocator.
  `WorthUiRuntimeHost::allocate_runtime_handles` consumes Phase 14
  `WorthUiExecutionPlanInput`; allocation emits generic runtime handles for all
  plan inputs, specialized handles only when the family/evidence supports them,
  and Query view-binding handles only from Query-owned identity plus posture.
  Tests prove deterministic allocation, zero source parse/registry lookup/broad
  scan counters, Query evidence preservation, duplicate component handle denial,
  and stale receipt rejection after plan-shape change. Runtime authority
  trybuild now proves public code cannot mint handles/counters/receipts or
  resolve component/command handles by string on the frame path. Verification
  passed with focused handle allocation tests, runtime authority trybuild,
  `cargo check -p worth-ui`, full `cargo test -p worth-ui --quiet`,
  `git diff --check`, and line-cap checks.
- Worth UI M3 Phase 16 implemented executable plan topology assembly. Added
  `runtime::plan_topology` with runtime-owned `WorthUiExecutionPlan`,
  `WorthUiPlanTopology`, plan nodes, child ranges, lane partitions, read-only
  lookup indexes, egui plan boundary declarations, render resource refs,
  counters, denials, and assembler. `WorthUiRuntimeHost` now assembles topology
  from borrowed `WorthUiExecutionPlanInput` plus compact handle allocation,
  rejecting stale receipts, missing/mismatched runtime handles, orphaned
  child-range handles, and missing egui boundary declarations before plan
  construction. Tests prove deterministic topology, zero artifact-tree scans,
  no ambient egui contact, and facade compile-fail guards for forged execution
  plans, topology counters, lookup internals, and frame-time artifact scans.
- Phase 16 QA found and fixed substantive topology gaps. Plan topology assembly
  now consumes canonical region-structure proof carried forward from Phase 14
  plan lowering instead of emitting skeletal empty child ranges; structure-bound
  nodes reject missing topology input. Egui boundaries now expose concrete
  contact surfaces rather than only coarse egui family labels. Receipt
  validation counters now survive the successful path, and tests assert exact
  validation count, declared region structure, nonempty child ranges, missing
  topology denials, and concrete egui contacts. Verification passed with focused
  plan topology tests, runtime authority trybuild, `cargo check -p worth-ui`,
  full `cargo test -p worth-ui --quiet`, `git diff --check`, no trybuild `wip`,
  and touched-file line checks.
- Phase 16 qa-tests/code-quality pass found that topology tests still missed a
  real missing-child-range-handle bug and only smoke-tested egui contacts. The
  assembler now verifies expected child-range handle cardinality and exact plan
  indexes before activation. Tests now cover missing child-range handles,
  orphaned child-range handles with equal cardinality, exact egui contact sets,
  and exact region-structure counts against Phase 14 topology input. Code
  quality moved topology-input indexing out of the preparer into the topology
  input responsibility so plan-input preparation remains phase orchestration.
  Verification passed with focused topology/input tests, runtime authority
  trybuild, `cargo check -p worth-ui`, full `cargo test -p worth-ui --quiet`,
  diff hygiene, no trybuild `wip`, and touched-file line checks.
- Worth UI M3 Phase 17 implemented the execution-plan equivalence/digest
  boundary. Runtime now exposes sealed `WorthUiExecutionPlanDigest`,
  `WorthUiExecutionPlanEquivalence`, `WorthUiExecutionPlanEquivalenceBasis`,
  `WorthUiExecutionPlanEquivalenceCounters`, and
  `WorthUiPlanReuseClassification`, with host methods to digest and compare
  assembled plans. The digest folds handle allocation receipts, traversal
  topology, child ranges, lane partitions, lookup indexes, egui boundary
  contacts, and render-resource refs, while explicitly recording zero artifact
  scans, pointer-identity comparisons, and diagnostic-policy reads. Tests cover
  same-meaning reuse, handle/lane meaning drift, diagnostic policy
  independence, same artifact with different lane partitioning, lookup/egui
  surface participation, and facade compile-fail guards for forged digests,
  equivalence packets, counters, and pointer-equality substitution. Verification
  passed with focused Phase 17 tests, runtime authority trybuild, `cargo check
  -p worth-ui`, full `cargo test -p worth-ui --quiet`, diff hygiene, no
  trybuild `wip`, and touched-file/runtime-tree line-cap audits.
- Phase 17 QA loop found and fixed two principled gaps: digest tag assignment
  previously depended on Rust enum declaration order via `as u64`, so plan
  equivalence now uses explicit canonical tags for plan-node families, lanes,
  egui boundary inputs, and egui contacts; and the diagnostic-policy proof
  originally compared the same assembled plan through two hosts instead of
  independently lowering rich/minimal runtime pipelines. The diagnostic test
  now builds both plans through admission, impact, narrowing, identity,
  replacement, inventory, reconciliation, Query rebind, staging, plan input,
  handle allocation, and topology assembly, then compares digests. The added
  setup was split into named staging/assembly helpers to preserve test
  composition. Verification passed with focused Phase 17 tests, runtime
  authority trybuild, `cargo check -p worth-ui`, full `cargo test -p worth-ui
  --quiet`, diff hygiene, no trybuild `wip`, residue sweep, and line-cap
  checks.
- Phase 17 qa-tests/code-quality pass found and fixed deeper test pressure and
  structure gaps. Tests now directly prove that same-handle plans with added
  child-range topology rows change digest meaning, and that render-resource ref
  drift changes both digest and counters. Code quality split digest
  orchestration from canonical semantic tags and hash-fold mechanics via
  `canonical_tag.rs` and `hash_fold.rs`, keeping `digestor.rs` focused on the
  plan-equivalence boundary. Verification passed with post-fix focused
  plan-equivalence tests, runtime authority trybuild, `cargo check -p
  worth-ui`, full `cargo test -p worth-ui --quiet`, diff hygiene, no trybuild
  `wip`, and line-cap checks.
- Worth UI M3 Phase 18 implemented the plan provenance and inspection
  boundary. Added `runtime::plan_inspection` with sealed execution-plan
  inspection, node inspection, lane inspection, artifact-to-plan provenance,
  Query inspection-link packets, counters, denials, and inspector. The host now
  exposes `inspect_execution_plan(plan, plan_input)` so inspection derives from
  assembled plan truth plus lowered plan-input provenance without source
  archaeology. Tests prove artifact/capability origin, deterministic replay,
  preserved Query-owned inspection/projection/async/recovery links, mismatch
  denial before provenance construction, zero broad work counters, facade
  importability, private field fences, and rejection of local Query explanation
  substitution. Verification passed with focused plan-inspection tests, the new
  runtime plan-inspection trybuild suite, existing runtime authority trybuild,
  `cargo check -p worth-ui`, full `cargo test -p worth-ui --quiet`, diff
  hygiene, no trybuild `wip`, and line-cap checks.
- Phase 18 QA loop found and fixed four inspection-boundary gaps. Inspection
  now verifies that the supplied plan input is certified by the execution
  plan's handle receipt, preventing same-shape false provenance. Query
  provenance is named honestly as a binding surface rather than implying every
  posture is preservation. Inspection counters now expose plan-digest work
  instead of hiding that traversal. Trybuild fences now also prevent public
  minting of inspection counters and Query inspection-link packets. Verification
  passed with focused plan-inspection tests, runtime plan-inspection trybuild,
  existing runtime authority trybuild, `cargo check -p worth-ui`, full `cargo
  test -p worth-ui --quiet`, diff hygiene, no trybuild `wip`, and line-cap
  checks.
- Phase 18 qa-tests/code-quality pass found and fixed that inspection tests
  were too broad and permissive around Query provenance: they only required
  nonempty Query link strings, and a hostile assertion exposed production
  inspection labeling ordinary replacement-classified `QueryViewBinding` nodes
  as Query-owned provenance while omitting their capability reference. Plan
  inspection now assigns Query provenance only when Query-owned identity and
  posture evidence exist, otherwise preserving replacement-classification
  provenance with the binding identity basis as capability provenance. Tests
  now assert exact row-by-row derivation from plan topology, exact Query posture
  digests/surfaces/receipts, and the distinction between Query-owned and
  replacement-classified binding nodes. Code quality split expected provenance
  derivation into `plan_inspection_expected_provenance.rs`, bringing the
  boundary test file back under the line cap with a narrow support
  responsibility. Verification passed with focused plan-inspection tests,
  runtime plan-inspection trybuild, existing runtime authority trybuild,
  `cargo check -p worth-ui`, full `cargo test -p worth-ui --quiet`, diff
  hygiene, no trybuild `wip`, and line-cap checks.
- Worth UI M3 Phase 19 implemented execution lane taxonomy and hook admission.
  Added `runtime::lane_admission` with explicit execution lanes, cost regimes,
  failure modes, support rows/matrix, lane admission packets, Query support
  links, counters, diagnostics, extension hooks, and typed hook denials. The
  runtime host now admits execution lanes before topology activation, validates
  topology assembly against a lane-admission receipt, and admits only supported
  lane adapter hooks without letting hooks override active plan truth, Query
  posture, durable state carry-forward, lane taxonomy, or performance
  certification. Tests prove deterministic support descriptors, unsupported
  lane rejection before plan activation, distinct cost/failure regimes, private
  lane claim denial, authority-override hook denial, preserved lane counters,
  and Query-owned support links copied from Forge Query posture rather than
  reauthored. The first focused run exposed a real boundary issue: lowered plans
  include both replacement-classified binding nodes and Query-owned rebind
  entries, so admission now admits the Query lane once, preserves every proven
  Query support link, and only reports missing links when no Query-owned
  evidence exists. Added runtime authority trybuild coverage for the new facade
  types, private packet fields, private lane string admission, and forbidden
  active-plan override constructors. Verification passed with focused
  lane-admission tests, runtime authority trybuild, `cargo check -p worth-ui`,
  full `cargo test -p worth-ui --quiet`, diff hygiene, and line-cap checks.
- Phase 19 QA loop found and fixed deeper activation and proof gaps: the public
  topology assembly path now always runs lane admission before plan activation;
  Query-bound admission rejects component-lowered spoof nodes that lack
  Query-owned posture or transition evidence while still admitting legitimate
  replacement-classified Query binding rows; Query-bound support rows now carry
  honest query-bound descriptor posture; every spec-named supported hook point
  has a typed admission constructor; and Phase 19 compile-fail tests live in a
  separate lane-admission driver instead of bloating the older runtime authority
  suite. The lane-admission host methods also moved into a focused host
  extension module to keep `host.rs` under the workspace line cap. Verification
  passed with focused lane-admission tests, runtime authority trybuild,
  dedicated lane-admission trybuild, `cargo check -p worth-ui`, full
  `cargo test -p worth-ui --quiet`, format check, diff hygiene, no trybuild
  residue beyond the tracked `.gitignore`, and touched-file line-cap checks.
- Phase 19 qa-tests/code-quality pass found that unsupported Query lane tests
  were not asserting the diagnostic's typed cost/failure posture, and that the
  lane admission planner still hid collection, Query-link extraction, support
  validation, and admission construction inside one broad method. Fixed by
  carrying cost/failure posture on `WorthUiLaneSupportDiagnostic`, preserving
  QueryBound posture in unsupported test support rows, strengthening the
  unsupported Query denial assertion, and replacing the old
  `plan_lane_mapping.rs` file with a more predictive
  `lane_admission_planner.rs` split into named evidence collection and
  validation steps. Verification passed with focused lane-admission tests,
  runtime authority and dedicated lane-admission trybuild, topology regressions,
  `cargo check -p worth-ui`, full `cargo test -p worth-ui --quiet`, diff
  hygiene, no trybuild residue, and line-cap checks.
- Phase 20 implemented the ordinary widget/shell lane boundary. Added a
  certified `ordinary_lane` runtime module with typed ordinary execution lanes,
  ordinary lane plans, typed frame targets, frame receipts, counters, denials,
  and host APIs for preparing and executing ordinary frames. Execution now
  operates from the certified ordinary plan rows rather than source artifacts,
  registry strings, or full-plan scans; virtualized, canvas, and realtime claims
  are denied before they can enter the ordinary frame path. Tests use a richer
  plan-input fixture with component, command, token, and skipped Query rows, and
  assert equivalent traversal counters, typed-handle execution, source-free
  paths, nonordinary denial, and full-plan-scan certification failure. Added a
  dedicated trybuild harness proving ordinary facade visibility while rejecting
  public minting of plans/receipts, public counter mutation, string target
  construction, and raw execution-plan input at frame execution. Verification
  passed with `cargo fmt -p worth-ui`, `cargo check -p worth-ui --quiet`,
  `cargo test -p worth-ui --lib --quiet`, the dedicated ordinary trybuild
  harness, and full `cargo test -p worth-ui --quiet`. `git diff --check`
  passed apart from existing CRLF warnings; the bash line-cap script could not
  run because `bash` is unavailable in the PowerShell session, but the new
  Phase 20 Rust files are under 400 lines.
- Phase 20 QA loop found and fixed two real proof gaps. First, ordinary plan
  preparation only required admitted `OrdinaryWidgetShell` support, so an
  unrelated lane admission could have been paired with a command/style-bearing
  execution plan and still produced ordinary command/token rows. Fixed by
  requiring supported lane posture for every included ordinary-compatible row
  (`OrdinaryWidgetShell`, `CommandSurface`, `StyleToken`, `EguiBoundary`) and
  adding a mismatched-admission regression for command and style/token rows.
  Second, frame execution matched typed targets by plan index without checking
  handle generation. Fixed by rejecting stale typed-handle generations with a
  structured `TargetGenerationMismatch` denial and certification-failure
  counter. Reverified with formatting, `cargo check -p worth-ui --quiet`,
  `cargo test -p worth-ui --lib --quiet`, ordinary trybuild, full
  `cargo test -p worth-ui --quiet`, diff hygiene, and touched-file line counts.
- Phase 20 qa-tests/code-quality pass found that ordinary lane tests were still
  hand-shaping component/command/token rows by mutating a Query fixture, which
  weakened the proof of the real artifact-to-plan path. Fixed by adding
  `ordinary_lane_test_support`, which drives the fixture through artifact
  lowering, runtime comparison, impact narrowing, identity matching, durable
  state reconciliation, activation staging, execution-plan input lowering,
  handle allocation, lane admission, topology assembly, and ordinary-plan
  certification. The only synthetic ordinary row now enters through the
  registered component-hook lowering surface because commands do not yet have a
  first-class artifact node. The same QA pass found test-only frame target
  variants and counter mutators leaking into production shape; fixed by placing
  those variants, executor arms, and counter recorders behind `cfg(test)` and
  updating the compile-fail expectation to the stronger public error that
  `record_full_plan_scan` does not exist. Reverified with focused ordinary
  tests, ordinary facade trybuild, `cargo check -p worth-ui --quiet`, full
  `cargo test -p worth-ui --quiet`, `git diff --check` apart from existing CRLF
  warnings, and touched-file line counts.
- Implemented Worth UI M3 Phase 21 virtualized data lane. Added
  `runtime::virtualized_data_lane` with visible-range validation, virtualized
  data plans, Query patch posture receipts, typed data frame targets,
  frame receipts, counters, denials, certification, and host APIs for
  preparing/executing data frames. The lane plans only Query-owned
  `QueryViewBinding` rows with admitted Query support links, requires both
  `VirtualizedData` and `QueryBound` support, executes by visible row/column
  breadth, and rejects full-collection scans, offset-pagination substitutes,
  stale handles, absent plan indexes, non-data targets, and invalid visible
  ranges. Widened `WorthUiQueryLaneSupportLinks` so admitted Query evidence
  carries live compatibility, async result-state, inspection, projection
  consumption, and recovery digests into the data lane without reopening raw
  plan input. Added boundary tests using the real Query table binding pipeline
  plus a dedicated virtualized-data facade trybuild suite proving intended
  public types while rejecting public minting of plans/receipts/counters,
  raw query-string targets, and offset-page constructors. Verification passed
  with formatting, `cargo check -p worth-ui --quiet`, focused virtualized-data
  tests, ordinary-lane facade regression trybuild, virtualized-data trybuild,
  full `cargo test -p worth-ui --quiet`, `git diff --check` apart from existing
  CRLF warnings, residue scan, and line-cap checks.
- Phase 21 QA loop found and fixed two completion gaps. The spec-named
  `WorthUiVirtualizedDataLane` facade surface was missing, so receipts now
  expose row-list vs cell-grid breadth classification derived from validated
  visible ranges. Data-plan certification also under-bound Query posture by
  hashing only projection consumption; the digest now binds Query support,
  live compatibility, async result-state, inspection, projection consumption,
  recovery, and required Query surfaces, with regression tests forcing drift in
  each posture family. Reverified with focused virtualized-data tests,
  virtualized/ordinary facade trybuild, full `cargo test -p worth-ui --quiet`,
  residue scan, `git diff --check` apart from CRLF warnings, and line-cap
  checks.
- Phase 21 qa-tests/code-quality pass found that virtualized data tests were
  still too polite about cross-receipt pairing: a stale lane admission from a
  different plan input could be paired with an execution plan and still build a
  data plan if the Query links happened to line up by plan index. Lane
  admission now carries a sealed plan-input basis digest, virtualized data plan
  preparation rejects mismatched admission/plan pairs before row construction,
  and the hostile regression duplicates a Query input to preserve plausible
  Query support while changing the certified basis. The lane-admission
  compile-fail fixture now also proves the new digest field is private.
  Verification passed with focused virtualized-data tests, virtualized/lane/
  ordinary facade trybuild, `cargo check -p worth-ui --quiet`, full
  `cargo test -p worth-ui --quiet`, residue scan, line-cap checks, and
  `git diff --check` apart from existing CRLF warnings.
- Phase 22 qa-tests/code-quality pass found that the canvas spatial lane had
  learned to preserve selection identity from runtime handle allocation, but
  plan preparation did not yet reject a handle allocation receipt from a
  different plan. Fixed production by requiring the allocation receipt to match
  the execution-plan handle receipt and returning
  `HandleAllocationPlanMismatch` with certification-failure counters on drift.
  Strengthened the test by creating a valid drifted diagnostics-ref allocation
  through the real runtime path, then pairing it with the original execution
  plan to prove the guard. Also renamed the canvas hook family tuple fields so
  draw/hit-test/tool-state authority is explicit instead of `hooks.0/1/2`.
  Verification passed with `cargo fmt -p worth-ui`, focused canvas spatial
  tests, canvas spatial facade trybuild, `cargo check -p worth-ui --quiet`,
  full `cargo test -p worth-ui --quiet`, `git diff --check` apart from
  existing CRLF warnings, WIP residue scan, and PowerShell line-count checks.
- Implemented Worth UI M3 Phase 23 real-time overlay/HUD lane. Added
  `runtime::realtime_overlay_lane` with typed high-frequency frame policy,
  realtime overlay lane, HUD plan, renderer surface handle/admission, overlay
  hook, frame target, frame receipt, counters, denials, certification, host
  plan/execute APIs, facade exports, and a dedicated facade trybuild harness.
  The lane consumes the existing `RealtimeOverlayHud` support row,
  `RealtimeOverlayMechanics` hook admission, compact runtime handles,
  `RenderResourceRef` topology rows, command indexes, diagnostics posture, and
  handle/lane-admission receipts. It rejects missing hooks, wrong hook kinds,
  missing realtime support, stale lane admission, mismatched handle allocation,
  ordinary widget fallback, hidden ordinary layout traversal, and forbidden work
  counter suppression. Verification passed with `cargo fmt -p worth-ui`,
  `cargo check -p worth-ui --quiet`, focused realtime tests, realtime facade
  trybuild, full `cargo test -p worth-ui --quiet`, `git diff --check` apart
  from existing CRLF warnings, WIP residue scan, and line-count checks; the only
  line-cap hit was pre-existing `crates/worth-ui/tests/facade_compile.rs`.
- Phase 23 qa-loop found two substantive completion gaps: realtime HUD plans
  preserved command and diagnostics posture but not the spec-required
  accessibility posture, and `WorthUiHudPlan::renderer_surfaces()` hid a fresh
  allocation behind a cheap-looking accessor. Fixed the lane so accessibility
  plan indexes, counters, renderer-surface admission counts, frame receipts, and
  HUD plan digests all carry accessibility posture. Precomputed renderer
  surface admissions in the HUD plan and changed the accessor to return a
  slice. Added a stale renderer-surface generation rejection test to match the
  ordinary and virtualized typed-handle safety posture. Verification passed with
  focused realtime tests, realtime facade trybuild, `cargo check -p worth-ui
  --quiet`, full `cargo test -p worth-ui --quiet`, line-cap scan, and
  `git diff --check` apart from existing CRLF warnings.
- Phase 23 qa-tests/code-quality pass strengthened the realtime overlay/HUD
  lane tests from broad reason checks into hostile denial proofs. Added real
  planner coverage for `NoHudRows`, stale lane admission without render rows,
  and absent renderer-surface frame targets, with counter assertions proving
  rejection happens before row construction, renderer admission, or frame
  handoff. Split the grouped denial test into one-responsibility tests. The pass
  also refactored `WorthUiHudPlanBuilder::build` so HUD row extraction and
  renderer-surface admission construction live behind named semantic steps, with
  a `WorthUiRealtimePostureCounts` packet replacing loose command/
  accessibility/diagnostics count arguments. Verification passed with focused
  realtime tests, realtime facade trybuild, `cargo check -p worth-ui --quiet`,
  full `cargo test -p worth-ui --quiet`, line-cap scan, and `git diff --check`
  apart from existing CRLF warnings.
