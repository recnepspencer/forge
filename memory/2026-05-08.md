# 2026-05-08

## forge-signal-wasm worker runtime Phase 4

- Continued the worker runtime placement milestone after pulling `origin/master`
  to `80220e45f`.
- Implemented the next Phase 4 batch: a narrow main-thread-hosted callback
  execution boundary with closed worker-issued requests, paired result
  readmission, request digest validation, ambient graph read denial, typed
  failed/denied/unavailable reports, runtime mutation breadth, worker-first
  truth digest, and wasm `SignalWorkerRuntime` facade methods.
- Updated `_docs/forge_signal_wasm/worker_runtime_placement_plan.md` with the
  new Phase 4 evidence and remaining deferrals.
- Continued with a follow-up implementation batch adding hosted callback
  execution certification: Suite 8 evidence now binds placement eligibility,
  replay/import compatibility posture, closed request/result evidence, hosted
  execution digest, and worker-first truth digest, with stale evidence rejection.
- QA loop found and fixed a proof-surface weakness: hosted callback execution
  certification now uses worker-retained request/report evidence instead of
  trusting caller-supplied evidence back through the facade.
- Continued Phase 4 with worker callback capability transport posture:
  `SignalWorkerRuntime` can export/certify runtime-envelope callback
  unavailability artifacts and admit or deny worker runtime-envelope imports
  with typed zero-fallback reports.
- Added the follow-up worker callback reattachment import lane: callback-bearing
  runtime envelopes can be admitted with explicit live callback reattachments,
  while missing or unexpected reattachments are rejected and fallback remains
  zero.
- QA loop tightened the reattachment lane: reattached callbacks must preserve
  the exported read frontier, import reports now bind reattached callback IDs
  into their digest, and rejected/rebuild-failed reattachments dispose callback
  tokens instead of leaking registry entries.
- Continued with the next implementation batch: worker definition-envelope
  publication now has an explicit callback reattachment lane. The default
  portable publication API still denies callback-backed definitions, while the
  opt-in reattachment API admits live callbacks only when they preserve the
  exported read frontier, reports reattached callback ids/host capability
  transport/fallback/truth digest evidence, and disposes rejected or failed
  reattachment tokens.
- Continued with an atomic publication hardening batch: definition-envelope
  publication now preflights ids and dependencies before mutating worker truth,
  so callback-free portable publication and callback reattachment publication
  cannot leave partial sources/callbacks behind after rejection. Reattachment
  preflight rejection now disposes live callback tokens.
- QA loop found and fixed a deeper atomicity issue: publication preflight and
  mutation order disagreed for recipes that read reattached callbacks and for
  recipe-family reads. The fix introduced an explicit publication dependency
  plan, split planning into its own module to preserve line caps, added blocked
  read diagnostics, and preflights invalid recipe read aspects before mutation.
- Continued with a Phase 4 batch exposing multi-callback reattachment through
  the wasm worker facade. Runtime-envelope import and definition-envelope
  publication now have JS array batch methods while preserving scalar wrappers,
  and worker-host tests prove multiple reattachments are admitted through both
  lanes.
- Continued with a Phase 4 closeout certification batch: worker runtime shells
  now retain current import/publication evidence and expose
  `certifyWorkerCallbackPhase4Closeout`, which binds placement digests,
  zero-fallback import/publication evidence, callback counts, current worker
  truth, and a closeout digest while rejecting missing or stale evidence.
- QA tightened Phase 4 closeout certification: the package now names its
  `PublicationReattachmentWithPortableImportDenial` gate mode, exposes import
  and publication outcomes, requires callback-bearing portable import denial
  plus callback reattachment publication evidence, and rejects weaker
  non-callback zero-fallback publication evidence.
- Began Phase 5 with committed worker observation delivery. Added
  `deliverLatestObservation` and `certifyWorkerObservationDelivery`, a typed
  `observationDelivery` packet with delivery breadth/performance/truth/digest
  evidence, retained-packet certification, and tests for committed delivery,
  missing observation evidence, and cleared stale delivery evidence.
- Continued Phase 5 with committed worker output delivery. Added
  `deliverOutputs({ outputIds })` and `certifyWorkerOutputDelivery`, a typed
  `outputDelivery` packet with output values, delivery breadth, payload byte
  breadth, performance/truth/digest evidence, retained-packet certification,
  and tests for happy-path delivery plus empty, duplicate, unknown, and cleared
  delivery evidence rejection.
- QA tightened output delivery honesty: worker portable graph publication now
  carries validated `outputIds`, marks those recipes as public outputs only
  after successful publication, rejects source/non-recipe output roles before
  mutation, and `deliverOutputs` now refuses arbitrary non-output signal ids.
- Continued Phase 5 with diagnostics/history read cost honesty. Added worker
  `readDiagnosticsSummary`, `readDiagnosticsHistory`, and
  `certifyWorkerDiagnosticsSummaryRead`; summary reads now emit stable
  diagnostics summary and rich-read availability digests with zero cold
  reconstruction, while rich history reads carry separate cold-work
  attribution.
- Continued Phase 5 with worker lifecycle/disposal control. Added
  `attachObservationDelivery`, `detachObservationDelivery`, and
  `certifyWorkerLifecycleControl`; public observation delivery now requires an
  active worker-owned lifecycle subscription, detach emits typed
  `lifecycleControl` evidence, and unknown detach ids produce denial artifacts
  without mutating worker truth.
- QA tightened the lifecycle/delivery proof: observation delivery packets now
  carry the active lifecycle subscription count and canonical lifecycle digest,
  and certification revalidates both so detach or observer-set churn invalidates
  stale delivery certification.
- Continued Phase 5 with a closeout certification lane. Added
  `certifyWorkerPhase5Closeout` to bind retained observation delivery, output
  delivery, diagnostics summary read, current worker truth, active lifecycle
  digest, delivery breadth, and boundary performance into one Phase 5 proof
  package with rejection tests for missing evidence, mutation-cleared evidence,
  lifecycle churn, and detached lifecycle.
- Began Phase 6 with same-runtime branch restore capability certification.
  Added worker branch create/switch/snapshot facade methods plus
  `restoreBranchSnapshotWithCapabilityReport` and
  `certifyWorkerReplayRestoreCapability`; reports bind snapshot, replay/restore,
  restored branch truth, placement identity, capability availability, exact
  restore artifact, and zero fallback, with tests for missing evidence,
  mutation-cleared evidence, and capability churn after restore.
- QA tightened the Phase 6 replay/restore proof surface: restore reports and
  certification now also bind replay/import compatibility and lowered-plan
  identity digests, and certification revalidates lowered-plan identity against
  the current capability posture instead of relying on restored values alone.
- Continued Phase 6 with Suite 13 import/export callback unavailability
  certification. Worker shells now retain callback-unavailability export,
  portable import denial, and same-runtime callback reattachment import evidence
  as one historical sequence, then certify export/import/reattachment digests,
  callback-unavailability artifacts, host-capability transport count, and zero
  fallback while rejecting missing or weak evidence.
- Continued Phase 6 with Suite 12 checkpoint-plus-retained-history replay
  certification. Worker shells now retain checkpoint replay evidence, bind
  checkpoint/full replay/retained history/replay-restore digests plus current
  capability and lowered-plan identity, expose record/certify facade methods,
  and reject missing, stale, cross-branch, or empty-suffix checkpoint evidence.
- QA tightened the Suite 12 checkpoint replay certification: the package now
  names the exact checkpoint-plus-retained-history restore artifact, branch
  creation and snapshot capture clear stale historical evidence, and tests
  cover capability churn plus branch/snapshot topology churn after evidence
  capture.
- Continued Phase 6 with no-worker compatibility artifact certification.
  `SignalDiagnostics.workerUnavailableCompatibilityCertification` now proves
  explicit main-thread compatibility posture for environments without worker
  support, binding compatibility truth convergence, deployment/fallback policy,
  historical capability posture, replay/import compatibility, placement identity,
  `dedicatedWorkerUnavailable` incompatibility, and zero hidden fallback while
  rejecting non-convergent compatibility truth.
- QA tightened the no-worker compatibility package: it now exposes and binds
  concrete placement denial/fallback digests plus callback classification
  counts, so zero hidden fallback is inspectable rather than implied only by
  policy labels.
- Continued Phase 6 with a closeout certification bundle. Worker shells now
  retain certified replay/restore, checkpoint retained-history, and
  import/export callback-unavailability packages as historical artifacts, and
  `SignalWorkerRuntime.certifyWorkerPhase6Closeout` combines them with a freshly
  computed worker-unavailable compatibility package. The closeout binds all
  four Phase 6 artifacts, capability parity, callback transport counts, and
  zero fallback, with runtime and boundary tests passing.
- Began Phase 7 with performance-contract certification. Added
  `SignalDiagnostics.workerPhase7PerformanceContracts`, which exposes required
  worker boundary counters, complexity contracts, prohibited failure modes, and
  digests. The validator rejects missing required counters and forbidden vague
  cost bases like total graph size.
- QA tightened the Phase 7 performance-contract certification: bridge
  allocation posture is now explicit and digest-bound, required cost bases must
  be present for each named contract, duplicate coverage rows are rejected, and
  the static catalogue was split out to preserve the 400-line file cap.
- Continued Phase 7 with test-requirements certification. Added
  `SignalDiagnostics.workerPhase7TestRequirements`, which maps all 13 required
  proof families to concrete runtime/boundary test surfaces, certification
  surfaces, hostile requirements, and Section 16 acceptance artifacts. The
  package reports `CoveredPendingFinalCloseout` for all families so remaining
  final-bundle work is visible rather than implied.
- Continued Phase 7 with product-guidance certification. Added
  `SignalDiagnostics.workerPhase7ProductGuidance`, which makes worker-first the
  recommended runtime-owned graph posture, names the explicit compatibility
  lanes, forbids hidden fallback, and rejects vague or duplicate posture
  guidance.
- Continued Phase 7 with suite 0 closeout-readiness certification. Added
  `SignalWorkerRuntime.certifyWorkerPhase7CloseoutReadiness`, binding current
  Phase 5 delivery/diagnostics/lifecycle evidence, Phase 6 historical
  capability evidence, Phase 7 performance contracts, product guidance, test
  requirements, acceptance artifacts, and current worker truth while honestly
  reporting `Suite0TrackedPendingFinalCloseout`/`milestoneClosed = false` until
  the 13 proof families are no longer pending. Also fixed runtime-envelope
  import/export to preserve worker public output roles so imported callback
  reattachment evidence can still certify output delivery afterward.
- Finished the final Phase 7 closeout pass. Promoted the test-requirements
  matrix from `CoveredPendingFinalCloseout` to
  `ClosedByCanonicalCertification`, added the final
  `SignalWorkerRuntime.certifyWorkerPhase7Closeout` facade/package, required
  zero pending proof families, and made Suite 0 report
  `Suite0FinalCloseoutCertified` with `milestoneClosed = true`. Added hostile
  coverage for stale pending test-requirement rows so final closeout cannot
  silently fall back to readiness semantics. QA tightened the top-level package
  to expose proof-family, counter-catalog, complexity-contract, failure-mode,
  and bridge-allocation-posture digests directly rather than only through nested
  subpackage certification digests.
- Ran a final `$qa-tests` pass on the Phase 7 test surface. Moved Phase 7
  runtime tests into `runtime/tests/worker_runtime/phase7/` and boundary tests
  into `boundary/tests/phase7/` to bring both parent directories under the
  10-direct-file cap. Strengthened the boundary closeout test to assert the
  final top-level Section 16 digest fields, and updated the certified test
  requirements matrix to the new paths.

## Worth Milestone 3 and Domain Structure

- Resumed Milestone 3 after the query-domain side quest closed. Implemented direct suite-level acceptance proof rows for `topology_edit_digest`, `naming_edit_continuity_matrix`, `rejected_edit_scope_report`, and `edit_replay_parity_report`.
- The Milestone 3 hostile-suite report now aggregates those rows, closeout enforcement requires complete/nonempty proof rows for every required and rejected scenario, and the closeout requirements registry exposes the new required outputs.
- Public facade/API coverage now proves downstream callers can inspect the new proof rows without reconstructing scenario payloads. Verified focused Milestone 3 hostile-suite and closeout tests, the public contract test, `cargo fmt --package worth-topo --check`, full `cargo test -p worth-topo --quiet`, and `git diff --check`.

- Wrote Worth Topology Domain Structure Gate spec in _docs/worth/worth-topo-domain-structure-gate.md and inserted it into worth_roadmap.md between the closed Worth Read Composition Side Quest and further Milestone 3 widening. Key decision: worth-topo should rehome around brep, derived_topology, validation, editing, projection, certification, and test_support; topology-to-geometry binding remains a separate crate; projection is not query integration; provenance names are compatibility/audit only, not permanent structure.
- Continued Milestone 3 after the worth-topo domain-structure gate closeout. Added a direct validator-family coverage closeout surface (`MilestoneThreeValidatorFamilyCoverageRow` / `MilestoneThreeValidatorFamily`), made Milestone 3 validator expectations nonempty, and carried post-edit derived validation reports into accepted hostile edit scenarios while rejected scenarios prove rejection-locality coverage. Verified focused topology-operator closeout tests, closeout registry tests, UI trybuild, full `cargo test -p worth-topo --quiet`, fmt check, diff check, and source line caps.
- QA loop corrected the Milestone 3 validator-family surface: removed the false `AuthorityReferenceIntegrity` claim, added concrete `validator_names` evidence per family, enforced accepted-vs-rejected validator expectations, and moved Milestone 3 closeout tests under `certification/tests/topology_operator_closeout/` so the root test folder remains structure-guard clean.
- Continued Milestone 3 Phase 4 with direct edit-fallout breadth proof rows. The hostile suite now emits `MilestoneThreeEditFalloutBreadthRow` per scenario, honestly marking accepted scenarios as explicit `WholeViewFallback` derived-validation fallout until localized recompute exists and rejected scenarios as `RejectedBeforeDerivedWork`. Closeout requirements, facade contracts, and tests now enforce the row surface.
- Continued Milestone 3 Phase 4 with direct determinism-rule rows. The hostile suite now emits and enforces sequence-preserving edit-order rows, stable edit digest rows for every required scenario, stable rejection classification rows for rejected scenarios, and ambiguous local rewire tie-break evidence rows. QA fixed two proof-surface issues: edit-order determinism is now explicit instead of implied, and non-applicable diagnostic/tie-break proof booleans no longer claim success. Closeout requirements, facade contracts, hostile-suite tests, closeout tests, UI trybuild, full worth-topo tests, diff check, and source line caps all passed.
- Continued Milestone 3 Phase 4 with outcome-aware branch-local topology edit parity rows. `MilestoneThreeEditBranchLocalParityRow` now distinguishes accepted and rejected outcomes, carries rejected scenario/rejection-class evidence, and closeout enforcement requires both accepted branch-local edit parity and rejected branch-local diagnostic parity for the required rejected scenarios. Split branch-local closeout enforcement into `branch_local_acceptance.rs` to preserve line caps. Verified focused closeout tests, UI trybuild, full worth-topo tests, diff check, and worth-topo source line caps.
- Continued Milestone 3 Phase 5 with direct hostile certification category posture rows. The hostile suite now emits/enforces `MilestoneThreeHostileCertificationCategoryRow` entries for mutation pipeline integrity, primitive family closure, operator brutality, query/traversal brutality, non-manifold/radial brutality, degeneracy/corruption localization, determinism/order assault, diagnostics/failure taxonomy, and scale/depth/sustained pressure. Split category model/accessors out of the closeout report to keep `report.rs` at the 400-line cap. Verified focused closeout/operator tests, UI trybuild, full `cargo test -p worth-topo --quiet`, structure gate, fmt check, diff check, and worth-topo source line caps.
- QA loop corrected the hostile category posture rows: primitive topology family closure and query/traversal brutality were overclaiming `Certified` posture. Rows now carry direct `evidence_labels` and `gap_labels`; primitive closure is partial until missing canonical edit-family lanes (`WireClosed`, `SheetPatch`, `SolidShell`) are covered, query/traversal is partial until reads are proven over edited topology states, and scale/depth remains partial with named scale-sweep gaps. Full worth-topo tests, UI trybuild, structure gate, fmt check, diff check, and line caps passed.
- Continued Milestone 3 Phase 5 by closing the edited-state query/traversal gap. Added sealed `MilestoneThreeEditedTopologyQueryTraversalRow` evidence for post-edit local-rewire and loop-cycle reads over an accepted ambiguous local rewire, compared against an independent replay, exposed public facade accessors, and made `QueryTraversalBrutality` certify only from those direct edited-topology rows. Split closeout side-quest/category/test helpers to preserve the 400-line rule. Verified focused closeout/hostile tests, public facade contract, full `cargo test -p worth-topo --quiet`, UI trybuild, fmt check, structure guard, diff check, and PowerShell line-cap scan.
- Continued Milestone 3 Phase 5 by closing primitive topology family coverage. Added sealed `MilestoneThreePrimitiveFamilyClosureRow` evidence for replay-verified admitted local successor rewrites over `WireClosed(n)`, `SheetPatch(f)`, and `SolidShell(f)`, exposed facade accessors/required-output registry coverage, and made `PrimitiveTopologyFamilyClosure` certify from scenario coverage plus those direct primitive-family edit rows. Verified focused hostile/closeout tests, public closeout contract, full worth-topo tests, UI trybuild, structure guard, fmt check, diff check, and PowerShell line-cap scan.
- QA loop tightened the primitive topology family closure batch: extracted reusable local successor rewiring into `topology_operator_closeout/local_successor_rewire.rs` instead of letting an ambiguous scenario own it, made primitive closure replay compare full edit digests/edit-family evidence/derived validation row counts, and enforced direct proof-bearing closure rows for `WireClosed(n)`, `SheetPatch(f)`, and `SolidShell(f)`. Re-ran focused Milestone 3 closeout/hostile tests, public closeout contract, full worth-topo tests, UI trybuild, structure guard, fmt check, diff check, and line caps.
