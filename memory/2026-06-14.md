# 2026-06-14

- Worth UI M4 correction: a Vite/React browser harness was created by mistake
  and deleted. M4 harness implementation must be native Rust desktop over Worth
  UI/egui, consuming M1-M3 facade, artifact, runtime, execution-plan, state,
  command, diagnostics, reload, and frame-cost surfaces. Browser, web-view,
  Vite, React, DOM, and HTML/CSS harnesses are wrong-category implementation
  artifacts for this milestone.
- Worth UI M4 naming correction: the product artifact should be called a
  native validation app, not a harness. `worth-ui-harness` and `Harness*` names
  are existing lower-level test-support APIs only; prose should not confuse
  those support surfaces with the real app we use for end-to-end validation.
- Worth UI M4 Phase 1 implementation created `apps/worth-ui-validation-app`
  as a native Rust/egui app crate. It launches through public Worth UI facade
  surfaces, renders the first validation workbench shell, and includes tests
  for required shell surfaces, navigation context preservation, stable IDs,
  forbidden web artifacts, and no landing/demo route regression.
