# 2026-07-20

- Closed Worth UI milestone 3.9 through Phase 18. The spec and roadmap now mark
  Phases 1-18 closed.
- Phase 18 QA exposed and fixed two real issues: virtualized execution allocated
  Query authority/identity evidence during steady frames, and the Query lifetime
  validator still required Phase 15 `failed_publication` proof to remain
  `assigned` after its real lifecycle test existed.
- Final real-boundary proof uses production filesystem writes/watch settlement,
  Query Consumer Kit lifecycle, headless and egui execution, and independent
  allocator observation in hostile application contracts; these mechanisms stay
  outside the ordinary fast lane.
- Product/certification/host physical compile inventories remain frozen and
  nonredundant at 283/62/3 cases; their closing execution representatives are
  14/6/3 targets. Boundary-check, agent-context, topology validators, strict
  Clippy, formatting, and all Worth UI Rust line caps are green.
- Closing timing evidence records three raw samples per required measurement and
  explicit reviewed amendments. A discarded cold attempt failed only because
  isolated Cargo targets exhausted disk space; after scoped `cargo clean`, all
  three distinct cold targets passed.
- A later intent QA invalidated the old compiler-harness closure: Trybuild rebuilt
  the full platform in private target workspaces, producing a 659.162-second cold
  median and roughly 200-second warm runs. Compiler proof now has one checked-in
  certification fixture owner and exactly two ordinary Cargo sessions (all
  negative targets, then all positive targets) sharing the normal target graph.
  Exact canonical diagnostics and primary-error coverage replaced nonzero/sentinel
  logic and exposed two previously silent aggregate cases, which were split into
  independent compiler targets. Final compile medians are 157.246 seconds cold
  and 5.337 seconds warm; the one-shot full release smoke is 62.744 seconds.
- The milestone no longer requires three synchronous full-suite timing runs.
  Full is a release smoke; normal enterprise CI owns independent cached fast,
  compiler, hostile, dependency, quality, and platform jobs with per-job reports.
- Hostile Phase 7-9 architecture QA found and corrected a real graph seam: one Runtime Bridge could previously register semantic correspondences for multiple Signal graph instances, while Query exposed the underlying Bridge witness and raw Signal node/aspect targets.
- Runtime Bridge now rejects mixed-graph correspondence registries at construction and binds one Signal graph through an opaque `BridgeSignalGraphBinding`; installation and delivery use that binding instead of loose bridge/graph pairs.
- Query now retains an opaque Query-owned correspondence, exposes inspection counters and authoritative-change delivery without projecting Bridge or Signal ingredients, and keeps raw candidate creation internal to its operation-scoped registration path.
- Portable conditional authoring moved from a public 13-position constructor to a named, explicit, no-default builder. The Phase 9 spec now records the single-graph and facade-opacity laws.
- Verification: full Runtime Bridge (886 tests plus integration/UI/docs), full Query (2,695 tests plus integrations/docs), Query installation, host authoring facade, focused conditional/correspondence suites, boundary-check, agent-context, formatting, line caps for touched files, and diff checks are green. Strict whole-crate Clippy remains blocked by large pre-existing Bridge/Query/workspace debt; changed-file diagnostic screening found no findings and Query installation passes strict no-deps Clippy.
- Final integration QA found a reciprocal ownership hole: two independent Bridge runtimes could bind one Signal graph and maintain mutually invisible slot-allocation registries. Signal now retains one opaque aspect-lowering owner per executable graph; Runtime Bridge clones share their owner, while a second independently built runtime receives `SignalGraphLoweringOwner` at binding before allocation or mutation. The Phase 9 spec and graph-cohesion tests record this law.
- Post-fix verification: all 1,013 Signal tests plus its UI/boundary/doc suites, all 888 Bridge tests plus integration/UI/docs, and all 17 Query conditional integration tests are green; boundary-check, agent-context, formatting, diff checks, and the 328-file changed-Rust line-cap screen pass. The workspace-wide line-cap script remains red on unrelated pre-existing non-allowlisted files.
- A further hostile Phase 10-11 pass fixed exact direct-attempt identity, retained typed Bridge snapshot identity, moved authority admission before lower-runtime work without duplicate re-proof, precompiled semantic observation plans, strengthened installed lowering identity, and narrowed Query provenance so it exposes Query/Foundational meaning rather than raw Signal/Bridge topology.
- Conditional installations now live in an authoritative Query catalog with a disposable/rebuildable operation lookup; a public-path rebuild test proves exact Arc authority survives reconstruction. Signal failures now carry exact work counters through Bridge into Query, so failed compute/condition/comparator paths cannot falsely report zero work.
- Final verification after these fixes: 1,018 Signal tests, the full Runtime Bridge suite, 2,695 Query unit tests plus all integration/doc suites, and all 108 installed operating-world tests pass. Boundary-check, agent-context, formatting, diff hygiene, and the changed-Rust line-cap audit across 393 files are green.
- Finished Worth Store C.4 phases 13-15: fresh-process namespace discovery,
  eight-process ownership/confinement/death/re-admission, and the real-media
  partial-effect/barrier courtroom.
- The skeptical QA loop found and fixed process-local runtime identity
  collisions, concurrent first-creation classification, premature terminal
  counter capture, whole-file observer reads, and stale incomplete-scaffold
  test expectations.
- Executed and removed five real production mutants: file barrier omission,
  directory barrier omission, path escape, short-write promotion, and metadata
  lock authority. Each failed its intended causal predicate.
- C.4 closeout passed backend/format suites, public journeys, default/maximal
  compiler authority suites, all-feature clippy with warnings denied, the
  physical writer/catalog gate, boundary-check, and agent-context check.
- A final hostile C.4 audit strengthened the previously weak Phase 14/15
  oracles. Fault matches now retain the actual boundary context and terminal
  class; every one of the 22 seams reruns under source/binary/profile/format
  binding; contention compares exact post-lease behavior while retaining and
  bounding the inherently schedule-dependent absent-root scaffold prefix.
- The new courtroom caught a real root-parent synchronization bug: the result
  issued an operation identity but the interposer entered through an unbound
  setup call. The production boundary now carries that operation identity.
- C.4 backend (170 tests + doctests), all 16 process journeys, the complete
  44-run seam campaign, default/maximal compiler authority suites, strict
  Clippy, writer gate, boundary-check, and agent-context all pass. The global
  workspace line-cap script remains red on unrelated pre-existing files; all
  C.4 files are within the cap.
- The C.4 contract now states the pragmatic trust model honestly: Store never
  widens permissions, deployment declares one cooperating service-account
  boundary, and the OS lock supplies live exclusion; ACL inheritance cannot
  prove that an arbitrary same-credential process will cooperate. Scenario
  reports and a compact courtroom binding replace the former demand for one
  recursively self-certifying evidence bundle.
- A second hostile Phase 13-15 pass replaced shape-only media checks with
  predeclared exact manifest/byte oracles, exercised confinement through a real
  owner-bound open/write path, localized the real in-flight lease-release
  boundary, and retained the matched completed-byte prefix for every fault.
- Exact counter reruns exposed PID-width nondeterminism in `mutation.lock`;
  process observations now use a fixed ten-digit field and are parsed back to
  the actual process id. The full 44-run seam campaign then converged exactly.
- Full named C.4 journeys are certification-feature-only rather than silently
  weakening under default features. The spec now describes sudden process
  death honestly through pre-death counters, OS exit status, and successor
  acquisition instead of an impossible post-mortem in-memory counter.
- The broad C.3-C.4 boundary pass found that the entire Store runtime had been
  moved into `worth-store-physical-backend` and merely re-exported. That let a
  lower crate own and mint the composition authority contrary to both specs.
  Runtime admission, phase progression, observation, and shutdown now live in
  `worth-store`; the unpublished backend exposes only a narrow filesystem
  qualification outcome and cannot promote a runtime phase.
- The same pass found two false guards: the physical-writer gate scanned the
  abandoned backend runtime directory, and the C.3 construction fixture still
  described obsolete fields. Both now trace the actual Store-owned topology,
  and the maximal-feature fixture explicitly proves the backend has no runtime
  facade and cannot convert qualified media into runtime authority.
- Default-feature Clippy exposed a cross-crate `cfg(test)` mismatch hidden by
  maximal features. Certification progression now keys only off the shared
  Cargo feature. Qualification basis was split from backend admission, and
  confinement effect probes now require concrete certification authority.
- Closeout after the boundary repair: C.3 journeys, all 16 C.4 journeys, 167
  backend tests plus 28 doctests, both compiler-boundary products, default and
  maximal strict Clippy, all 19 runner tests, boundary-check, agent-context,
  diff check, and all C.3-C.4 source caps pass.
- A hostile test-topology pass found that `store-owner -p worth-store` selected
  every integration and trybuild target: its warm wall time was 85.2 seconds.
  Owner now selects only library/binary/example/bench unit harnesses and runs in
  1.3 seconds warm.
- Smoke previously issued twelve Cargo front-door calls and duplicated a slow
  compile-fail UI proof. It now validates and executes five exact behavioral
  selectors through one package-level nextest build, taking 6.5 seconds warm.
  The correction exposed and fixed a stale reopen-counter assertion.
- UI/scenario runners now pass explicit Cargo `--test` selections, so filtered
  lanes no longer compile the whole workspace integration surface. The complete
  warm UI certification product runs 27 tests across 18 binaries in 61.3
  seconds and remains outside owner/smoke feedback.
- A second hostile C.5 Phase 4-7 pass corrected point-local identity lookup,
  canonical root ordering, pre-allocation geometry rejection, sealed producer
  lengths, no-effect versus residue-producing stream failure posture, truthful
  free-space ranges, compatible-segment reuse after policy drift, and the
  reserved terminal physical identity value.
- The pass also completed independent golden bytes for segment, extent, and
  free-space manifests and added adversarial proof for damaged-tail producer
  ordering, interrupted segment rollover, forged free-space generations,
  zero-allocation geometry denial, and policy drift back to an older segment.
- C.5 verification is green: 37 physical record journeys, 125 physical-format
  owner tests, full Worth Store all-feature tests including compiler authority,
  strict Clippy, runner gates, boundary-check, agent-context, and scoped source
  caps.
- C.3/C.4 heavy targets declare their certification feature directly; the OS
  observer is `test = false` and no longer links an empty libtest harness. An
  exact Worth Store integration-target budget makes future linked-binary growth
  a deliberate review. Runner tests (21), strict lint, all 16 media journeys,
  boundary-check, agent-context, diff check, and touched-source caps pass.
- Worth UI 3.9 skeptical closure fixed full-set multi-removal publication,
  persistent/ref-counted invalidation indexes with exact delta counters,
  physical runtime responsibility topology, narrow named facades, the Phase 10
  exact-edge contract, and source-bound timing evidence. A Query merge exposed
  duplicate live-resource naming; installed Worth UI view identity now owns the
  Query resource name.
- The old compile-contract topology was measured honestly at 221.604 seconds
  warm and 664.040 seconds cold. The consolidated two-Cargo-session runner is
  1.616 seconds warm and 111.918 seconds cold; the warm fast lane improved from
  62.951 to 6.998 seconds. Schema-2 evidence binds the opening commit and exact
  closing working-tree bytes. The full Worth UI closure lane, strict Clippy,
  constitutional checks, topology checks, and all 2,868 Rust file caps pass.
- The final three Worth UI 3.9 QA findings were corrected without synthetic
  proof: the multi-removal contract now performs real atomic `.wui` rewrites
  through the active host's portal-anchor capability, persistent-index tests
  assert exact probe/copy counts, and dense planning/lane directories are split
  into semantic responsibility homes. All 2,848 current Worth UI Rust files are
  within the 400-line cap.
- Post-fix timing evidence was recaptured against the exact corrected source
  bytes in a fresh isolated target root. Medians are 0.559s targeted, 7.803s
  fast, 15.811s application contracts, 3.093s warm compile contracts, and
  117.477s cold compile contracts; the topology and evidence gates pass.
- Worth UI 3.9 was checkpointed and pushed as `d028f72b`, then `origin/master`
  was merged cleanly as `3accf1a3`, bringing in Worth Query 9.14 phases 12-14.
- Query's updated orientation confirmed that installed-operation replay,
  aftermath, and lineage do not own Worth UI live-view succession. The useful
  immediate precedent is preserving Query-owned typed stops and receipts:
  Worth UI domain discovery/rebind no longer duplicates denial enums, maps next
  actions to strings, or discards Query's semantic-equivalence receipt. The
  same correction was applied to measurement settlement: non-admitted Query
  projection outcomes now cross the UI boundary intact instead of collapsing
  into local `Denied`/`Deferred`/`Unavailable` categories.
- The post-merge Worth UI timing snapshot is intentionally stale because both
  master and the follow-up Query-binding simplification changed its bound source
  scope. Recapture it only after the remaining tweaks settle.
- Worth UI Milestone 3.9.1 was specified as the Query 9.14 consumer-path
  modernization immediately after closed 3.9. Its migration authority is a
  26-row search-seeded, manually adjudicated subsystem CSV with `replace`,
  `retain`, `transitional`, `diagnostic-only`, and `unrelated` dispositions.
  Search discovers candidates but no test may claim migration success from
  removed tokens or a clean grep. The ten phases install stable Query operation
  meaning, enter one operating world, retain Query-minted consumer/settlement
  proof, rebase UI replacement equivalence, preserve legitimate downstream UI
  derivations, isolate managed-live compatibility pending Query 9.14 phases
  17/19/23/24, and certify the real public journey in existing compile targets.
- A hostile C.5 Phase 8-14 follow-up corrected path-owned media access in the
  format crate, theoretical comparison counters, an overpowered C.6 candidate
  port, topology-only offline verification, policy-change routing corruption,
  incomplete scan cost receipts, and a one-record fault proof that could not
  establish batch atomicity.
- C.5 now independently validates physical page/extent payload frames, streams
  the large courtroom extent, re-fanouts root/segment/free-space trees when
  manifest capacity changes, keeps catalog publication mechanically in Store,
  and challenges failed three-record candidate identities through both fresh
  runtime admission and the independent offline walk.
- The follow-up gates are green: 60 physical record journeys, physical runtime
  UI, strict Clippy, format/offline owner tests, runner tests, developer smoke,
  Road 1 boundary-check, and agent-context. The global line-cap script remains
  red on extensive pre-existing unrelated violations; all 157 changed or
  untracked Rust paths in this work are at or below 400 lines.
