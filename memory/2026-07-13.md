# 2026-07-13

- Created branch `codex/worth-signals` from `wasm-fix`.
- Renamed `apps/forge-signal-demo` to `apps/worth-signals` and updated public branding to Worth Signals.
- Corrected the landing-page source: the real repository is `recnepspencer/worth-landing`, now cloned at `../worth-landing`; `../worth-ui` is a separate Forge worktree. Copied the real orange/gold zig-zag Worth SVG wordmark into the Signals header.
- Local Vite serving verified the Worth header, landing copy, and footer. Production build and lint still have pre-existing Forge WASM/type and lint failures unrelated to the branding rename.
- Applied the Worth landing page's charcoal-and-gold visual theme across Worth Signals while preserving the carousel's cyan, green, amber, purple, and pink capability accents.
- Updated carousel arrows and slide buttons to follow the active capability accent; removed duplicate Home/Docs footer links and right-aligned those links in the header.
- Reframed the Worth Signals landing hero around explicit, inspectable, reproducible application behavior, with supporting audience points for regulated industries, startups, and every application.
- Removed the centered Worth Signals eyebrow badge and rewrote all six carousel slides as concise, verb-led capability claims with one sentence of proof.
- Shifted the landing hero from gold to a neutral white/silver palette so the carousel capability colors remain the dominant accents; retained gold as a Worth brand detail in the logo and header.
- Demo 1 review found redundant `publishDisabled`/`summary` derivations, a live panel implemented with React state instead of Worth Signals, a self-linking “Open signals demo” CTA, repeated explanatory sections, and an unused older `DemoOne.tsx` implementation.
- Rebuilt Demo 1 as an explainable transfer decision running on the actual Worth WASM graph: one amount input drives a processing fee and review lane, with a propagation record, compact implementation sample, and single docs link. Removed comparison content, repeated preface/CTA, and the unused legacy `DemoOne.tsx`; fixed WASM initialization in the local bridge.
- Reworked demo-page hero layout so the Demo number and pager/navigation share one top row while the title and supporting copy span the full page width below.
- Corrected Demo 1's over-designed first pass: removed inherited and nested cards, runtime/status chrome, redundant hero messaging, and the empty initial propagation row; restored syntax-colored code and kept only a flat decision row, on-change trace, standalone code surface, and docs link.
