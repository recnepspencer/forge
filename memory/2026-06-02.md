- worth-runtime-bridge Phase 1 continuity identity closeout: hard-broke continuity mutation bundles away from caller-supplied digest/semantic string inputs by introducing typed continuity identity carriers for prior/successor/target/collection evidence, deleting the old semantic-input module, and forcing facade writeback tests through native identity constructors. Canonical/read-result bridge-local projection wording was tightened so remaining `payload()`/`bytes()` references are foundational API/value-family calls rather than bridge authority vocabulary. Verified with `cargo fmt -p worth-runtime-bridge`, `cargo check -p worth-runtime-bridge --tests`, focused writeback/snapshot/compile-fail tests, residue scans, and scoped `git diff --check` (only existing CRLF warnings). Remaining Phase 1 closeout needs a structured audit of public/test authority constructors and semantic digest-helper mirrors before declaring the eradication gate complete.

- worth-runtime-bridge Phase 1 downstream/native-test detox batch: updated worth-topo bridge registration fallout to consume runtime-bridge native aspect registration APIs with AspectKeySelector, TruthPatchTargetSelector, SnapshotReadContract::scalar, and SliceWideningPolicy::Disallow; removed stale aspect_label() read calls in topology snapshot adapters; renamed query-runtime write delta fallback_collection vocabulary to declared_target_collection because it is admitted command target evidence, not a fallback lane. Replaced migration-history UI fixtures policy_replay_bundle_digest_constructor_removed, route_request_string_authority_removed, and writeback_digest_authority_constructors_removed with native-named sealed/private-field proofs, and removed the route:sha256 prefix assertion from harness artifact tests. Verified with cargo fmt, cargo check -p worth-topo --tests, cargo check -p worth-runtime-bridge --tests, cargo check -p worth-query --tests, cargo check -p worth-relational --tests, cargo test -p worth-runtime-bridge --test phase_boundaries_compile_fail -- --nocapture, targeted residue scans for old fallback/label/digest-constructor terms, and scoped git diff --check (only existing CRLF warnings). Remaining Phase 1 target: deliberate production digest-family audit for current native families such as bridge-derived-writeback-effect, bridge-subscription-signal-strategy, and bridge-grouped-truth-view; surface topology appears mostly current TruthDeltaSurfaceKind domain vocabulary rather than surface_label authority, but still needs final audit before closeout.

- worth-runtime-bridge Phase 1 grouped/source digest-basis hardening: moved row-set and grouped-truth-view digest construction into named digest_basis modules deriving from typed materialized rows, projection canonical basis, validated value canonical basis, grouped projection contracts, and materialized row-set artifacts. Deleted the private arbitrary `new(&[String])` digest constructors and inline `digest_parts` assembly from the materializers; added proof tests that digest identity changes only through native evidence changes; split grouped-truth-view test support into a dedicated support module to restore the touched test file under the 400-line cap. Verified with `cargo fmt -p worth-runtime-bridge`, `cargo test -p worth-runtime-bridge source -- --nocapture`, `cargo check -p worth-runtime-bridge --tests`, `cargo test -p worth-runtime-bridge --test phase_boundaries_compile_fail -- --nocapture`, residue scans, line-cap scans, and scoped `git diff --check` (only existing CRLF warnings). Remaining Phase 1 target: continue digest-family and public/test constructor eradication for writeback-effect and subscription-signal strategy surfaces, then run the final Phase 1 no-shim residue gate.

- worth-runtime-bridge Phase 1 writeback-effect/subscription-signal digest authority batch: split the over-cap mixed `writeback/effect.rs` into `effect/{mod.rs,causality.rs,derived_effect.rs,feedback.rs}` so causality evidence, derived effect identity, and feedback context are separate proof-family modules. Replaced the remaining generic causality digest-domain helper with a closed private enum, keeping digest derivation artifact-specific and unreachable as arbitrary caller text. Tightened subscription signal strategy and admitted subscription canonical bases so they derive from declaration, validated basis binding, and strategy digests rather than only display identities. Added proof tests that signal strategy identity changes with validated basis evidence and derived writeback effect digest changes through typed `BridgeWritebackEffectIntent` patch basis. QA found and fixed the adjacent admitted-subscription handoff plus the generic digest-domain helper. Verified with `cargo fmt -p worth-runtime-bridge`, `cargo check -p worth-runtime-bridge --tests`, `cargo test -p worth-runtime-bridge writeback -- --nocapture`, `cargo test -p worth-runtime-bridge subscription::admission -- --nocapture`, `cargo test -p worth-runtime-bridge --test phase_boundaries_compile_fail -- --nocapture`, residue scans, line-cap scans, and scoped `git diff --check` (only existing CRLF warnings). Remaining Phase 1 target: run the broader no-shim residue gate across runtime-bridge production/tests and chase any remaining authority-constructor or legacy-vocabulary hits.

- worth-runtime-bridge Phase 1 authoritative mutation evidence closeout hard break: replaced the last migration-shaped public closeout vocabulary in `writeback/authority_evidence_closeout` with a final typed authority-evidence submodule split into `mod.rs`, `support_evidence.rs`, `closeout_readiness.rs`, and `digest_basis.rs`. Public support/closeout artifacts now expose typed carry-forward sections, admitted mutation evidence family enums, aggregate evidence digest enums, ready capabilities, deferred boundaries, and verification gates; removed old string-list test expectations and old names such as `safe_to_build_now`, `must_not_assume_yet`, `required_verification_commands`, and `aggregate_evidence_sections`. QA found and fixed a private digest-domain string smell by introducing a closed private `AuthorityEvidenceDigestDomain` enum so digest section selection is not caller-labelled even internally. Updated facade exports/tests and UI private-field compile-fail fixtures to prove the new sealed typed fields. Verified with `cargo fmt -p worth-runtime-bridge`, `cargo check -p worth-runtime-bridge --tests`, `cargo test -p worth-runtime-bridge authority_evidence_closeout -- --nocapture`, `TRYBUILD=overwrite cargo test -p worth-runtime-bridge --test phase_boundaries_compile_fail -- --nocapture`, `cargo test -p worth-runtime-bridge --test phase_boundaries_compile_fail -- --nocapture`, scoped no-shim residue scans, line-cap scans, and scoped `git diff --check` (only existing CRLF warnings). Remaining Phase 1 queue: digest helper mirror detox, string-vector authority list review, external I/O JSON quarantine audit, and final crate-wide Phase 1 residue gate.

- worth-runtime-bridge Phase 1 digest-helper mirror detox batch: removed the remaining caller-text digest artifact selection from pricing certification support by replacing string artifact names with closed PricingCertificationDigestArtifact variants and replacing preformatted String basis lines with typed PricingCertificationBasisEntry rows. Split pricing certification digest basis support into suite_25 and failure_and_summary submodules after the touched file exceeded the 400-line cap. Renamed causal-envelope and mutation-evidence closed digest artifact label methods/locals to digest_domain so production/test digest derivation no longer preserves label-authority vocabulary. Verified with cargo fmt -p worth-runtime-bridge, cargo check -p worth-runtime-bridge --tests, cargo test -p worth-runtime-bridge writeback -- --nocapture, cargo test -p worth-runtime-bridge causal_envelope -- --nocapture, cargo test -p worth-runtime-bridge pricing_shock -- --nocapture, explicit touched-file residue scans, touched-file line-cap scan, and scoped git diff --check (only existing CRLF warnings). Remaining Phase 1 queue: string-vector authority list review, external I/O JSON quarantine audit, and final crate-wide no-shim residue gate.

- worth-runtime-bridge Phase 1 planning closeout: added a structured Phase 1 Closeout Execution Plan to `_docs/worth-runtime-bridge/aspect_native_refactor.md`. The remaining work is now three explicit eradication gates: string-list authority carrier review/hard break, external I/O JSON quarantine/import-wall fixups, and crate-wide no-shim facade/current-test residue gate. The plan names likely hotspots, allowed directory skeletons only when aspect authority or line caps require splitting, carriers to introduce/remove, residue scans, and final closeout proof commands. No source code changes or tests were run because this was a docs/planning turn.

- worth-runtime-bridge Phase 1 Batch 1 string-list authority work: hard-broke harness stream command windows and speculation churn lifecycle certification off raw string lists. Stream adapter command text now terminates in `stream/command_ingress.rs`, producing `NativeStreamCommitWindow` with `TruthCommitIdentity` members; execution consumes typed windows only, and typed certification tests construct native windows directly plus a hostile empty-command ingress rejection. Speculation churn now retains `SpeculationPreviewLifecycleDigestSet` built from replay-bundle lifecycle digest evidence instead of `Vec<String>`, and the aggregate preview lifecycle digest derives from the typed set. Verified with `cargo fmt -p worth-runtime-bridge`, `cargo check -p worth-runtime-bridge --tests`, `cargo test -p worth-runtime-bridge stream -- --nocapture`, `cargo test -p worth-runtime-bridge speculation -- --nocapture`, stream/speculation residue scans, touched line-cap scan, and scoped `git diff --check` (only existing CRLF warnings). Remaining string-list Batch 1 targets include structural certification retained candidates, diagnostics preview-session identity index classification, writeback request digest fixtures, structural fingerprints, and test/domain presentation lists.

- worth-runtime-bridge Phase 1 efficiency planning: tightened `_docs/worth-runtime-bridge/aspect_native_refactor.md` with an ordered Phase 1 remaining workboard so closeout execution starts from exact files instead of broad rediscovery searches. The plan marks stream and speculation string-list gates complete, structural retained candidates in progress, and identifies the next blockers as diagnostics preview-session reservation index, in-memory writeback request digest log, and structural fingerprint equivalence/member digest lists. Later gates are causal-envelope retained digest locals, subscription/pricing/test presentation list classification, JSON quarantine/import wall, and final no-shim residue gate. No cargo tests were run because this was docs/planning only.

- worth-runtime-bridge Phase 1 string-list authority closeout progress: closed the structural retained-candidate gate by keeping retained candidates as `StructuralRetainedCandidateSet`, adding read-only cardinality accessors, and strengthening typed structural certification tests to prove retained candidate identities are non-empty, unique, and counter-aligned before terminal JSON export. Closed the diagnostics preview-session reservation gate by changing `reserved_preview_session_identities` from `BTreeSet<String>` to `BTreeSet<BridgePreviewSessionIdentity>` and hard-breaking `reserve_preview_session_identity` APIs to accept typed preview-session identities from the runtime facade rather than `&str`. Updated the Phase 1 workboard statuses. Verified with `cargo fmt -p worth-runtime-bridge`, `cargo check -p worth-runtime-bridge --tests`, `cargo test -p worth-runtime-bridge structural -- --nocapture`, `cargo test -p worth-runtime-bridge speculation -- --nocapture`, `cargo test -p worth-runtime-bridge diagnostics_explanations -- --nocapture`, targeted residue scans, touched-file line-cap scan, and scoped `git diff --check` (only existing CRLF warnings). Remaining next blocker: in-memory writeback request digest log (`request_digests: Vec<String>`), then structural fingerprint equivalence/member digest lists.

- worth-runtime-bridge Phase 1 efficiency planning: rewrote the Phase 1 closeout workboard in `_docs/worth-runtime-bridge/aspect_native_refactor.md` into four closed passes so future work starts from exact files instead of broad rediscovery. Pass A finishes authority-bearing string collections (`request_digests`, structural fingerprint member/digest lists, causal-envelope retained digest locals). Pass B classifies or types domain/test presentation lists. Pass C performs the JSON quarantine/import-wall audit. Pass D performs the final no-shim vocabulary and facade/current-test residue gate. This was docs-only; verified with targeted spec scans and `git diff --check` on the spec.

- worth-runtime-bridge Phase 1 Pass A/A5 closeout: deleted the dead `request_digests: Vec<String>` observational log from the in-memory writeback authority fixture and replaced the sibling formatted-string authority index with `RecordingTruthWritebackCommitKey`, derived from `TruthWritebackRequest` family and causality evidence. The fixture now stores `TruthWritebackReceipt` directly and uses `TruthWritebackReceipt::canonical_noop_from_prior_receipt` for duplicate causality instead of preserving an authority-artifact string. The required request-digest scan also surfaced the rejecting writeback authority's last request/receipt digest storage, which is now represented as a typed `RejectedTruthWritebackAttempt` in current worktree state. Verified with `cargo fmt -p worth-runtime-bridge`, `cargo check -p worth-runtime-bridge --tests`, `cargo test -p worth-runtime-bridge writeback -- --nocapture` (86 passed; expected caught panic output), targeted request-digest/formatted-key residue scans, touched-file line-cap scan, and scoped `git diff --check` (only existing CRLF warning on the in-memory fixture). Next Phase 1 blocker: A6 structural fingerprint equivalence/member digest lists.

- worth-runtime-bridge Phase 1 efficiency planning: collapsed the duplicated Phase 1 closeout plan in `_docs/worth-runtime-bridge/aspect_native_refactor.md` into one authoritative four-pass board. Pass A is now explicitly active with A6 structural fingerprint evidence in progress and A7 causal-envelope retained digest locals next; Pass B classifies/types remaining domain/test presentation lists; Pass C quarantines JSON behind foreign-ingress or terminal-export modules; Pass D is the final no-shim facade/current-test residue gate. The plan now includes a mandatory classification decision table so ambiguous scan hits are treated as blockers and typed instead of becoming another search loop. This was docs/memory only; no cargo tests were run.

- worth-runtime-bridge Phase 1 Pass A/A6 closeout: replaced structural fingerprint `equivalence_members: Vec<String>`, `record_digests: Vec<String>`, and old member evidence storage with typed `StructuralFingerprintRecordValueEvidenceSet` and `StructuralFingerprintEquivalenceMemberSet` proof carriers derived from validated snapshot read values. The top-level fingerprint basis now records named `record-values` and `equivalence-members` sections, the public facade exports the new evidence carrier types, and structural tests assert materialized fingerprints retain the named typed evidence sets. QA found and fixed the canonical-basis handoff gap where the top-level basis did not prove the equivalence-member set directly. Verified with `cargo fmt -p worth-runtime-bridge`, `cargo check -p worth-runtime-bridge --tests`, `cargo test -p worth-runtime-bridge structural -- --nocapture` (54 passed), A6 string-list residue scans, touched-file line-cap scan, and scoped `git diff --check` (only existing CRLF warnings). Next Phase 1 Pass A blocker: A7 causal-envelope retained digest local vectors.

- worth-runtime-bridge Phase 1 Pass A/A7 closeout: replaced causal-envelope retained-mapping counter/failure digest local vectors with `RetainedCausalMappingDigestBasis` and mirrored `ExpectedRetainedCausalDigestBasis` in tests. QA tightened the carrier API so public construction names semantic basis rows (`from_counter_values`, `from_retained_record_digests`) while generic collection mechanics remain private implementation detail. Verified with `cargo fmt -p worth-runtime-bridge`, `cargo check -p worth-runtime-bridge --tests`, `cargo test -p worth-runtime-bridge causal_envelope -- --nocapture` (30 passed), retained-digest vector/public-constructor residue scans, touched-file line-cap scan, and scoped `git diff --check` (only existing CRLF warnings). Updated the runtime-bridge aspect-native refactor spec: Pass A is complete; Pass B is active with B1 subscription product/component/lane ID classification next.

- worth-runtime-bridge Phase 1 Pass B/B1 closeout: hard-broke subscription reference workload manifests away from raw `Vec<String>` product/component/lane authority. Added typed `BridgeSubscriptionReferenceWorkload{Product,Component,Lane}Id` and ID-set carriers under `subscription/certification/manifest/workload_ids.rs`; `BridgeSubscriptionReferenceWorkloadManifestDraft` and `RuntimeBridge::declare_subscription_reference_workload_manifest` now require typed sets, sealed manifests retain typed IDs, and manifest sealing rejects empty declared IDs before canonical basis/digest authority is produced. Updated production certification helpers, facade tests, and harness suites 35-37 to use typed declared workload sets rather than raw vectors. QA found and fixed the empty-ID admission gap. Verified with `cargo fmt -p worth-runtime-bridge`, `cargo check -p worth-runtime-bridge --tests`, `cargo test -p worth-runtime-bridge subscription_certification -- --nocapture` (10 passed), `cargo test -p worth-runtime-bridge subscription::certification -- --nocapture` (32 passed), B1 raw-list residue scan, touched-file line-cap scan, and scoped `git diff --check` (only existing CRLF warnings). Updated the spec: B1 complete; B2 pricing ranked material/provenance list classification is next.

- worth-runtime-bridge Phase 1 efficiency planning: tightened `_docs/worth-runtime-bridge/aspect_native_refactor.md` so the remaining Phase 1 work is a structured closeout board instead of an open-ended scan. Pass B now explicitly requires finishing B2 pricing closeout before B3; B2 closeout includes typed ranked-material/provenance carriers, pricing-specific helper names, residue scans, and a required split if touched `pricing_support.rs` remains over the 400-line cap. B3 is constrained to `mapping/freezing/tests.rs` and `harness/tests/support.rs`, with exact classification rules for display-only versus authority-bearing lists. Pass C and D now have ordered execution gates for JSON quarantine and no-shim residue closure. This was a docs/planning turn; no cargo tests were run.

- worth-runtime-bridge Phase 1 Pass B/B2 closeout: finished the pricing ranked-material/provenance list gate. `PricingShockRankedMaterialDamageSet` now owns ranked material damage evidence, `PricingProvenanceAspectTextPacket` owns the fixed seven-field provenance packet, historical provenance tests use named accessors instead of positional text vectors, and the generic `read_packet_aspect_value_texts` helper is gone in favor of `read_pricing_provenance_aspect_text_packet`. Split simulation evidence into `harness/tests/pricing_support/simulation_evidence.rs` after `pricing_support.rs` exceeded the 400-line cap. Verified with `cargo fmt -p worth-runtime-bridge`, `cargo check -p worth-runtime-bridge --tests`, `cargo test -p worth-runtime-bridge pricing_shock -- --nocapture` (41 passed), B2 raw-list/old-helper residue scan, touched-file line-cap scan, and scoped `git diff --check` (only existing CRLF warning on `pricing_support.rs`). Updated the spec: B2 complete; B3 mapping/support display-list classification is next.

- worth-runtime-bridge Phase 1 Pass B/B3 and Pass B closeout: classified mapping freeze assertion pairs as terminal test display over typed `BridgeMappingFreezeContext` and renamed the helper to `sorted_context_mapping_id_assertion_pair` with a fixed `[&str; 2]` return instead of a raw string vector. Hard-broke merge harness ancestry helpers away from `Vec<&str>` by making `merge_declaration` accept typed `TruthCommitIdentity` parents and updating merge/mixed-family callsites to pass typed arrays. Verified with `cargo fmt -p worth-runtime-bridge`, `cargo check -p worth-runtime-bridge --tests`, `cargo test -p worth-runtime-bridge mapping::freezing -- --nocapture` (9 passed), `cargo test -p worth-runtime-bridge merge -- --nocapture` (50 passed), Pass B residue scan, touched-file line-cap scan, and scoped `git diff --check` (only existing CRLF warnings). Updated the spec: Pass B complete; Pass C JSON quarantine/import-wall audit is now active.

- worth-runtime-bridge Phase 1 Pass C JSON quarantine/import-wall closeout: exact JSON API scans found no `serde_json::Value`, `json!`, `serde_json::Map`, `from_value`, or `to_value` usage outside `terminal_report_export`/`json_projection` modules, and native non-harness authority modules do not import terminal report exporters. No source rewrite was required because the current JSON surface is already terminal harness/report projection. Updated the spec: Pass C complete; Pass D crate-wide no-shim facade/current-test residue gate is now active.

- worth-runtime-bridge Phase 1 Pass D and Phase 1 closeout: narrowed the no-shim residue scan to exact legacy/fallback/raw/payload/bytes/aspect_bytes/surface_label/domain_payload/arbitrary-digest/todo/unimplemented authority terms. Remaining hits are classified as foundational `Artifact::payload()` accessors in `canonical_basis.rs` and `snapshot/read_result.rs`, plus native digest-family prefixes in subscription signal strategy, derived writeback effect, and grouped-truth digest basis; none admit old executable authority. QA found two line-cap blockers during closeout, so the batch split subscription family registry unit tests into `subscription/family_registry_tests.rs` and moved writeback authority rejection retained-diagnostics assertions into existing writeback test support. Verified with `cargo fmt -p worth-runtime-bridge`, `cargo check -p worth-runtime-bridge --tests`, `cargo test -p worth-runtime-bridge subscription::family_registry -- --nocapture` (5 passed), `cargo test -p worth-runtime-bridge writeback::authority_rejections -- --nocapture` (7 passed; expected caught panic output), empty runtime-bridge line-cap scan, no-shim residue scan with only classified non-blockers, and scoped `git diff --check` (only existing CRLF warnings). Updated the spec: Phase 1 passes A-D complete; next target is Phase 2 native snapshot read contract audit/implementation.

- worth-runtime-bridge Phase 2 native snapshot/source boundary progress: source row-set materialization now retains foundational `ContractValidatedAspectArtifact` per materialized field instead of cloning a raw `SnapshotReadValue` out of validated snapshot records. Scalar/grouped access derives from the retained validated artifact, and row-set tests prove the stored canonical basis recomputes from that artifact. The raw `SnapshotReadValue` lane is now confined to the untrusted snapshot reader result and snapshot validation boundary; source scans for `SnapshotReadValue`/`read_value()` are clean. Also closed Phase 1 enforcement drift by removing all stale runtime-bridge entries from `scripts/ci/workspace_rust_line_cap_allowlist.txt` after a runtime-bridge line-cap scan proved no exemptions remain needed. Verified with `cargo fmt -p worth-runtime-bridge`, `cargo test -p worth-runtime-bridge snapshot -- --nocapture`, `cargo test -p worth-runtime-bridge source -- --nocapture`, `cargo check -p worth-runtime-bridge --tests`, source/snapshot JSON/bytes/raw-value residue scans, runtime-bridge line-cap scan, and scoped `git diff --check` (only existing CRLF warnings). Remaining Phase 2 target: audit/strengthen grouped/source error taxonomy for non-scalar grouped bindings and any remaining unsupported-value-before-materialization proof gaps.

- worth-runtime-bridge Phase 1 efficiency planning: updated `_docs/worth-runtime-bridge/aspect_native_refactor.md` with a strict restart board for finishing Phase 1 efficiently. The plan replaces open-ended broad searching with five gates: rebaseline residue manifests, authority collection leftovers, JSON terminal/import wall, no-test-shim current surface, and closeout proof. It names exact start files, allowed split skeletons, blocker classifications, residue commands, and verification rules. No cargo tests were run because this was a planning-only batch; lightweight spec heading scan and `git diff --check` on the spec passed, though the spec file is currently untracked in this worktree.

- worth-runtime-bridge Phase 1 strict restart Gate 2 progress: closed the authority-collection blockers in the current manifest. `BridgeSubscriptionSourceArtifactIndex` now derives index canonical basis through private typed index-basis/record-evidence carriers instead of a local `record_digests` string. Retained causal-envelope bulk-planning failure digest basis now accepts typed `BridgeBulkPlanningFailure` records in production and in the facade test mirror, not arbitrary retained-record digest strings. Renamed `source/record_digests.rs` to `source/packet_set_digest_basis.rs` and renamed `synthetic_*` packet-set digest helpers to observation-derived names. QA found and fixed the stale `synthetic` naming and an unnecessary transient digest-list vector in the source-artifact index basis. Verified with `cargo fmt -p worth-runtime-bridge`, `cargo test -p worth-runtime-bridge causal_envelope -- --nocapture`, `cargo test -p worth-runtime-bridge subscription_certification -- --nocapture`, `cargo check -p worth-runtime-bridge --tests`, Gate 2 residue scans, import-wall scan, runtime-bridge line-cap scan, and scoped `git diff --check` with only existing CRLF warnings. Remaining Gate 2 scan hits are classified pricing ranked-material typed domain/presentation evidence.

- worth-runtime-bridge Phase 1 efficiency plan update: tightened `_docs/worth-runtime-bridge/aspect_native_refactor.md` with a concrete current restart manifest. Gates 1 and 2 are marked complete; Gate 3 is active with one exact decision on `harness/adapter/terminal_report_export/json_projection.rs` because its terminal snapshot helpers construct `worth_harness::SnapshotPayload::Structured(StructuredValue::Json(...))`. The manifest records current pricing ranked-material hits as typed presentation/domain evidence, the authority import wall as clean, and runtime-bridge line caps as clean. Next work should inspect/classify or replace those three terminal snapshot helpers, then run the bounded Gate 4 no-test-shim scan and Gate 5 closeout proof instead of broad rediscovery.

- worth-runtime-bridge Phase 1 Gate 3/4 closeout batch: split terminal harness snapshot capture construction out of `harness/adapter/terminal_report_export/json_projection.rs` into `terminal_snapshot_capture.rs`, leaving JSON projection as `serde_json::Value` report rendering only. Renamed helpers to terminal snapshot capture value vocabulary and aliased the external `worth_harness::SnapshotPayload` type at the boundary so runtime-bridge names no longer teach payload authority. Gate 4 bounded no-test-shim scan over `facade/tests`, `harness/tests`, and `tests/ui` found only canonical digest `as_bytes()` mechanics and `digest_input_bytes` counter proof references. Verified with `cargo fmt -p worth-runtime-bridge`, `cargo check -p worth-runtime-bridge --tests`, `cargo test -p worth-runtime-bridge harness -- --nocapture`, `cargo test -p worth-runtime-bridge snapshot -- --nocapture`, `cargo test -p worth-runtime-bridge --test phase_boundaries_compile_fail -- --nocapture`, import-wall/no-shim/name-residue scans, runtime-bridge line-cap scan, and scoped `git diff --check`. Updated the spec: Gates 3 and 4 complete; Gate 5 final crate-wide no-shim classification remains before Phase 1 can be declared closed.

- worth-runtime-bridge Phase 1 closeout: completed Gate 5 final crate-wide no-shim classification and marked Phase 1 closed in `_docs/worth-runtime-bridge/aspect_native_refactor.md`. Remaining scan hits are classified as foundational `payload()`/`bytes()` accessors, canonical digest `as_bytes()` mechanics and digest-input counters, native digest-family prefixes derived from typed basis carriers, terminal export/capture JSON, and typed pricing ranked-material presentation evidence. No source/routing/writeback/diagnostics/subscription/facade authority import wall breach was found. Verification evidence for Phase 1 closeout: `cargo fmt -p worth-runtime-bridge`, `cargo check -p worth-runtime-bridge --tests`, `cargo test -p worth-runtime-bridge harness -- --nocapture`, `cargo test -p worth-runtime-bridge snapshot -- --nocapture`, `cargo test -p worth-runtime-bridge --test phase_boundaries_compile_fail -- --nocapture`, Gate 3 import-wall scan, Gate 4 no-test-shim scan, final crate-wide no-shim classification scans, runtime-bridge line-cap scan, and scoped `git diff --check`. Next phase is Phase 2 native snapshot read contract continuation.
- worth-runtime-bridge Phase 1 efficiency planning: added a short current-only `Phase 1 Efficient Finish Board` to `_docs/worth-runtime-bridge/aspect_native_refactor.md` above the older closeout/history tables. The board makes Phase 1 operational instead of archaeological: rebaseline once, rewrite blocker rows, quarantine terminal I/O, detox current tests after production authority is clean, and close with proof. It records the 2026-06-02 gates as complete and says not to reopen open-ended Phase 1 while the board is green; any fresh Phase 1-shaped blocker introduced by later phases must be fixed inside that phase.
- worth-runtime-bridge Phase 2 grouped binding denial hardening: grouped truth materialization now distinguishes missing identity/grouping binding aspects from present-but-unsupported native value families. The grouped materializer admits only retained contract-validated scalar artifacts for identity and grouping lanes; struct-valued bindings return typed unsupported-family errors with row identity, aspect key, value family, and validated canonical basis before member rows are materialized. Added hostile grouped tests that send struct contracts/values through the real snapshot validation and row-set materialization path for identity and grouping bindings. Verified so far with `cargo fmt -p worth-runtime-bridge` and `cargo test -p worth-runtime-bridge source -- --nocapture`; remaining verification in the active turn includes snapshot tests, package check, residue scans, line-cap scan, and scoped diff hygiene.

- worth-runtime-bridge Phase 1 efficiency planning restart: added a stricter Phase 1 efficiency restart overlay to `_docs/worth-runtime-bridge/aspect_native_refactor.md` so closeout work proceeds from five ordered batches rather than broad rediscovery. The plan starts with rebaseline plus `subscription/certification/source_artifact_index` closeout because that file is currently over the 400-line cap and owns subscription certification source-artifact digest/index evidence. It defines the required split skeleton, authority-collection residue gate, JSON terminal/import wall gate, no-test-shim current-surface gate, and closeout proof commands/scans. This was a planning-only turn; no cargo tests were run.

- worth-runtime-bridge Phase 1 Batch 1 source-artifact-index closeout: split the over-cap flat `subscription/certification/source_artifact_index.rs` into `source_artifact_index/{mod.rs,artifact_evidence.rs,artifact_record.rs,index_basis.rs,kind_index_basis.rs}`. The split makes typed artifact evidence, admitted input/record derivation, whole-index canonical basis, and per-kind semantic-source digest projection separate proof responsibilities. `BridgeSubscriptionSourceArtifactIndex::artifact_kind_digest` now delegates to `BridgeSubscriptionSourceArtifactKindIndexBasis` instead of assembling an anonymous local digest list. Also fixed the grouped binding denial warning by exporting `BridgeGroupedBindingValueFamily` through the public facade beside `BridgeGroupedTruthViewError` and making grouped tests consume that facade export. Verified with `cargo fmt -p worth-runtime-bridge`, warning-clean `cargo check -p worth-runtime-bridge --tests`, `cargo test -p worth-runtime-bridge subscription_certification -- --nocapture`, `cargo test -p worth-runtime-bridge subscription::certification -- --nocapture`, `cargo test -p worth-runtime-bridge source -- --nocapture`, `cargo test -p worth-runtime-bridge --test phase_boundaries_compile_fail -- --nocapture`, clean runtime-bridge line-cap scan, clean source-artifact-index authority-collection residue scan, touched no-shim classification scan, and scoped `git diff --check` with only existing CRLF warnings.

- worth-runtime-bridge Phase 1 efficiency planning: added a single active Phase 1 Finish Control Plan to `_docs/worth-runtime-bridge/aspect_native_refactor.md` so active work is no longer driven by overlapping historical closeout boards. The plan marks Gate 1 source-artifact-index complete and makes Gate 2 authority-collection residue the next execution target, with exact residue, JSON/API, import-wall, no-test-shim, and closeout verification commands. This was docs/memory only; `git diff --check` on the spec passed and no cargo tests were run.

- worth-runtime-bridge Phase 1 active Gate 2 closeout: reran the authority-collection residue manifest and removed the stale `retained_digests` bucket topology from causal-envelope retained mapping. The retained mapping root is now a family dispatcher, while `retained_artifact_digest/{planning_checkpoint,route_history_preview,source_structural_stream,writeback}` owns per-family typed artifact digest projection. Gate 2 residue now has one classified non-blocker in subscription historical-basis certification: `BridgeSubscriptionCertificationSemanticDigests::subscription_basis_digest()` comparison over typed certification bundles. Verified with `cargo fmt -p worth-runtime-bridge`, `cargo test -p worth-runtime-bridge causal_envelope -- --nocapture`, `cargo check -p worth-runtime-bridge --tests`, Gate 2 residue scans, stale-module-name scan, runtime-bridge line-cap scan, and scoped `git diff --check` (only existing CRLF warning on the touched retained-mapping root). Next active gate is Gate 3 JSON terminal/import wall.

- worth-runtime-bridge Phase 1 active finish control closeout: completed Gates 3-5 in the active plan and marked Phase 1 closed in `_docs/worth-runtime-bridge/aspect_native_refactor.md`. Gate 3 JSON/API hits are confined to terminal report export/capture topology, with only `subscription/counters::from_values` false positives outside terminal JSON paths; the authority import wall across source, routing, writeback, diagnostics, subscription, and facade is clean. Gate 4 current-test no-shim scan found only canonical digest `as_bytes()` mechanics and `digest_input_bytes` counter proof references, not executable legacy/shim authority. Gate 5 proof passed with `cargo fmt -p worth-runtime-bridge`, `cargo check -p worth-runtime-bridge --tests`, `cargo test -p worth-runtime-bridge --test phase_boundaries_compile_fail -- --nocapture`, runtime-bridge source line-cap scan, broad no-shim classification scan, and scoped `git diff --check` with only the existing CRLF warning on retained causal mapping. Remaining work should resume from Phase 2 native snapshot read contract continuation; Phase 1 should not be reopened unless a later edit creates a fresh blocker.

- worth-runtime-bridge Phase 1 efficiency planning: added a superseding `Phase 1 Current Finish Plan` to `_docs/worth-runtime-bridge/aspect_native_refactor.md` above the older Phase 1 active gate table. The new plan defines one bounded closeout loop: Gate 1 rebaseline manifest, Gate 2 authority-blocker rewrite, Gate 3 terminal JSON wall, Gate 4 current-test shim eradication, and Gate 5 proof. It classifies blocker versus terminal/canonical/presentation hits, names allowed split skeletons, and explicitly says not to reopen open-ended Phase 1 searching after proof. No cargo tests were run because this was docs-only; heading scan and `git diff --check` on the spec passed.

- worth-runtime-bridge Phase 3 patch-target error context progress: replaced the old patch-coordinate error context surface with sealed native `BridgePatchTargetCoordinate` evidence carrying entity identity, foundational aspect locator, optional field locator, surface kind, and target canonical basis. Committed patch envelope validation and routing eligibility missing-mapping errors now retain this native target coordinate. Added/verified compile-fail coverage proving external callers cannot construct `BridgePatchTargetCoordinate::new`. Structural QA split the over-cap mixed `error/context.rs` into `error/context/{mod.rs,mapping_freeze.rs,patch_routing.rs,delivery_replay_snapshot.rs}` so patch/routing target evidence, mapping-freeze evidence, and delivery/replay/snapshot context are separate responsibility files under the line cap. Verified with `cargo fmt -p worth-runtime-bridge`, `cargo check -p worth-runtime-bridge --tests`, focused input envelope/routing eligibility/routing surfaces tests, compile-fail suite, old-coordinate residue scan, touched no-shim vocabulary scan, runtime-bridge line-cap scan, and scoped `git diff --check` with only existing CRLF warnings. Remaining Phase 3 closeout: audit/prove relation-endpoint, region, partition, and facet target support/typed denial behavior and admitted route artifacts free of foreign labels.
- worth-runtime-bridge Phase 3 target-selector basis detox: replaced `TruthPatchTargetSelector` prefix-shaped `field:<path>` canonical output with native `target-selector|kind=...` basis text, and routed canonical ordering plus mapping-registry digest preparation through that basis. Added a narrow `mapping/freezing/tests/target_selector_basis.rs` proof and a route-eligibility matrix proving relation-endpoint, region, partition, and facet targets travel through committed-patch envelope construction, aspect registration, mapping lookup, and eligibility without field locators or prefix-parsed coordinates. Kept touched mapping/routing files under the 400-line cap. Verified with `cargo fmt -p worth-runtime-bridge`, `cargo test -p worth-runtime-bridge mapping::freezing -- --nocapture`, `cargo test -p worth-runtime-bridge routing::eligibility -- --nocapture`, `cargo check -p worth-runtime-bridge --tests`, target-selector prefix residue scan, touched no-shim scan, line-cap scan, and scoped `git diff --check` (only existing CRLF warnings). Remaining Phase 3 closeout: prove final route artifacts, not only eligibility entries, never retain foreign labels/prefix coordinates, and audit whether unsupported target category states are now fully unrepresentable or still need a typed denial before artifact construction.
- worth-runtime-bridge Phase 1 efficiency closeout plan: consolidated the active Phase 1 plan in `_docs/worth-runtime-bridge/aspect_native_refactor.md` into a single `Phase 1 Completion Control Board`. The board records Phase 1 as closed against the current 2026-06-02 manifest/proof, declares older Phase 1 tables historical migration notes only, and says future Phase 1-shaped blockers must be fixed inside the phase/edit that creates them rather than reopening broad Phase 1 archaeology. Verified with heading scan and `git diff --check` on the spec; no cargo tests were run because this was docs/control-board only.

- worth-runtime-bridge Phase 1 efficiency replanning: inserted a new `Phase 1 Final Operating Plan` at the top of the Phase 1 spec section and demoted older Phase 1 boards to history. The bounded current manifest says line caps are clean, JSON/API hits are terminal-only, and current-test no-shim hits are only canonical digest mechanics/counter byte proof. The one active Phase 1 blocker candidate is `harness/adapter/adapter_impl/policy/shared_artifacts.rs` exposing generic `combined_digest(label, values)` outside terminal export; next implementation should hard-break that into named policy certification digest-basis proof carriers before running the final import-wall/test-shim/closeout proof. Verified with `git diff --check` on the spec only; no cargo tests were run because this was planning/documentation.

- worth-runtime-bridge Phase 1 final Gate 1 closeout: deleted the non-terminal policy certification `combined_digest(label, values)` helper and replaced it with `harness/adapter/adapter_impl/policy/certification_digest_basis.rs`, which owns a closed `PolicyCertificationDigestArtifact` enum plus named digest-basis functions for provenance policy equivalence, replay, diagnostics, rejection failures, rejection diagnostics, ambient-leak policy/replay/diagnostics, semantic route policy, and empty terminal provenance report projection. QA found and fixed the initial rejection diagnostics digest because it used human `detail()` text; it now derives from typed rejection fields and rejection digest. Verified with 10-minute-timeout cargo commands: `cargo fmt -p worth-runtime-bridge`, `cargo test -p worth-runtime-bridge policy_certification -- --nocapture` (4 passed), `cargo test -p worth-runtime-bridge policy -- --nocapture` (61 passed), and `cargo check -p worth-runtime-bridge --tests`. Residue/import-wall/no-test-shim/line-cap scans are clean or have only classified terminal/canonical hits. The Phase 1 spec records Gate 1 proof and says Phase 1 can stop after diff hygiene; next work should continue to the next aspect-native phase target rather than reopening broad Phase 1 search.

- worth-runtime-bridge Phase 3 final route-artifact closeout: added `routing/planning/plan/planner_tests.rs` to prove relation-endpoint, region, partition, and facet targets survive full `RuntimeBridge::plan_envelope` route planning with final `BridgePlannedRoute::route_record_entries()` retaining committed-patch native target basis only, while planned read packets retain snapshot-read native target basis plus the expected subscription slice kind. Structural QA first found the proof was too fixture-heavy inside `planner.rs`; fixed by moving it into a dedicated planner test file and leaving `planner.rs` as production orchestration only. Updated the aspect-native spec to record that unsupported target categories are public-API unrepresentable via sealed constructors/compile-fail proofs and private invalid target shapes fail through typed `UnsupportedTruthDeltaSurface` before artifact construction; Phase 3 is now closed for the current manifest. Verified with 10-minute-timeout cargo commands: `cargo fmt -p worth-runtime-bridge`, `cargo test -p worth-runtime-bridge routing::planning::plan -- --nocapture` (1 passed), `cargo test -p worth-runtime-bridge routing::eligibility -- --nocapture` (2 passed), `cargo test -p worth-runtime-bridge routing::surfaces -- --nocapture` (5 passed), `cargo test -p worth-runtime-bridge --test phase_boundaries_compile_fail -- --nocapture`, and `cargo check -p worth-runtime-bridge --tests`. Residue scans found only pre-existing classified field-target/negative-proof hits, old `BridgePatchCoordinate`/`patch_coordinate()` scans had no hits, runtime-bridge line-cap scan was clean, and scoped `git diff --check` had only existing CRLF warnings.

- worth-runtime-bridge Phase 1 efficiency/control planning: added a single active `Phase 1 Structured Finish Plan` to `_docs/worth-runtime-bridge/aspect_native_refactor.md`. The plan says Phase 1 is effectively closed for the current 2026-06-02 manifest and should not be reopened for cleanup or archaeology. Future Phase 1 work is limited to one bounded manifest pass, rewriting exact blocker rows, detoxing coupled tests, proving the terminal JSON import wall, and closing with proof; any later Phase 1-shaped blocker must be fixed inside the phase that introduced it. This was a docs/planning-only change; `git diff --check` on the spec passed and no cargo tests were run.
- worth-runtime-bridge Phase 1 structured efficiency plan update: replaced the active Phase 1 section in `_docs/worth-runtime-bridge/aspect_native_refactor.md` with a single `Phase 1 Finish Execution Board`. The board demotes older Phase 1 plans to audit history, defines exact blocker classifications, allowed split skeletons, five execution gates, and the current closed blocker manifest. It explicitly forbids general cleanup, polish renames, and broad archaeology; future Phase 1 work is only a bounded rebaseline proof or exact blocker-row rewrite. Verified with heading scan and `git diff --check` on the spec; no cargo tests were run because this was docs-only planning.
- worth-runtime-bridge Phase 1 final proof and Phase 4 route/provenance identity batch: used the QA loop against Phase 1 closeout and fixed one real ambiguous blocker by replacing subscription certification semantic digest `label` selection with closed `BridgeSubscriptionCertificationSemanticDigestField` variants. Phase 1 closeout evidence now records clean authority-collection scan, clean authority import wall, clean runtime-bridge line-cap scan, terminal-only JSON hits, and current-test no-shim hits classified as canonical digest mechanics/counter proof. Then advanced Phase 4 by changing committed patch envelope digests from raw `patch|commit=...|item=...committed-patch-target...` basis text to `patch:sha256:*`, keeping raw basis private digest input only. Coupled route canonicalization/provenance tests now prove route basis and planning provenance carry `route-entry:sha256:*` and digest-shaped `patch-digest` without embedding entity/aspect/target/mapping/signal text; final route artifact tests now assert target basis stays in evidence fields while `TruthDeltaSurfaceIdentity` remains digest-shaped. Verified with `cargo fmt -p worth-runtime-bridge`, `cargo test -p worth-runtime-bridge subscription_certification -- --nocapture`, `cargo test -p worth-runtime-bridge subscription::certification -- --nocapture`, `cargo check -p worth-runtime-bridge --tests`, `cargo test -p worth-runtime-bridge --test phase_boundaries_compile_fail -- --nocapture`, `cargo test -p worth-runtime-bridge input::envelope -- --nocapture`, `cargo test -p worth-runtime-bridge routing::canonicalization -- --nocapture`, `cargo test -p worth-runtime-bridge routing::planning::plan -- --nocapture`, residue scans, touched-file line-cap checks, and scoped `git diff --check` (only existing CRLF warnings). Next Phase 4 target: continue mask-native routing/subscription closeout around route request-key/bulk route packet identities and any remaining places where route identity/provenance can be influenced by diagnostic breadth or presentation labels.
- worth-runtime-bridge Phase 1 closeout rebaseline blocker: the bounded Phase 1 scan found one production digest-family selector in `speculation/validation.rs`, where preview-session validation paired human labels with digest accessors. Replaced it with closed `PreviewSessionDeclarationDigestRequirement` variants for truth-view basis, source capability, request shape, and retained artifact schema. QA classified the remaining narrowed scan hit in `routing/canonicalization_tests.rs` as presentation-only assertion wording; terminal JSON import-wall and line-cap scans were clean. Verified with `cargo fmt -p worth-runtime-bridge`, `cargo test -p worth-runtime-bridge speculation -- --nocapture` (27 passed), `cargo test -p worth-runtime-bridge --test phase_boundaries_compile_fail -- --nocapture` (1 passed), `cargo check -p worth-runtime-bridge --tests`, targeted digest-label scans, and scoped `git diff --check` (only existing CRLF warning on the touched file). Next work should resume Phase 4 bulk route packet identity hardening, not reopen broad Phase 1 archaeology.

- worth-runtime-bridge Phase 1 efficiency planning: replaced the active Phase 1 execution board in `_docs/worth-runtime-bridge/aspect_native_refactor.md` with a tighter structured finish plan. The board now treats Phase 1 as a bounded closeout gate, not cleanup: classify scan hits into blocker/public API/test shim/terminal I/O/canonical mechanics/presentation-only, execute five batches (`P1-0` rebaseline, `P1-1` authority rewrite, `P1-2` test detox, `P1-3` JSON wall, `P1-4` closeout proof), edit only blocker files, and stop Phase 1 once proof is green. The current manifest records policy/subscription/speculation digest-family blockers closed, terminal JSON import wall closed, test shims closed, and line caps clean. This was docs-only; verified with heading scan and `git diff --check` on the spec. Next work should continue Phase 4 route packet identity hardening unless a new exact Phase 1 blocker row appears.

- worth-runtime-bridge Phase 4 bulk route identity hard break: kept `BridgeRouteIdentity` typed through canonical bulk workload requests, truth-delta routing packets, continuity remap packets, widening aggregation packets, reduced publications, and reduced widening artifacts. The bulk packet/reduction chain no longer stores route identity as raw `Arc<str>` authority; `.as_str()` remains only for digest-basis/presentation formatting. Strengthened bulk workload and packet-reduction tests to prove canonical request route members, packet route origins, continuity/widening origins, and reduced route identity lists match the typed planned-route identities. QA found no in-scope weakness after the rewrite. Verified with `cargo fmt -p worth-runtime-bridge`, `cargo test -p worth-runtime-bridge packet_reduction -- --nocapture` (11 passed), `cargo test -p worth-runtime-bridge bulk_workload -- --nocapture` (10 passed), `cargo check -p worth-runtime-bridge --tests`, clean targeted bulk route-identity residue scan, clean touched line-cap scan, and scoped `git diff --check` with only existing CRLF warnings. Next Phase 4 target: audit remaining request-key/string-scope construction around route region keys, canonical workload segment digests, and subscription slice/member scopes to distinguish presentation/partition strings from any remaining route authority.
- worth-runtime-bridge Phase 1 efficiency planning closeout: tightened the active Phase 1 Finish Execution Board in `_docs/worth-runtime-bridge/aspect_native_refactor.md` so Phase 1 runs from a bounded manifest instead of open-ended exploration. Added an operating budget, serial gate discipline, and a blocker-manifest template that requires exact files, classification, native replacement, coupled tests, and proof before editing. Verified the spec section and `git diff --check` for the spec; no cargo tests were run because this was docs/planning only. Current operational answer: Phase 1 is closed unless the bounded rebaseline finds an exact blocker row; otherwise resume Phase 4.
- worth-runtime-bridge Phase 4 bulk packet-region identity batch: replaced bulk planning's raw `route-partition` / `truth-view-partition` / `continuity-partition` string keys with typed `BulkPacketRegionIdentity` proof values carried by `DisjointPacketRegionSet` and `AdmittedPreparationPartitionSet`. Region identities are now digest-shaped `bulk-packet-region:sha256:*` artifacts derived from private packet locality basis, and parallel legality checks typed packet-region uniqueness before emitting `DisjointPacketRegionsCertified`; duplicate region overlap now has explicit typed legality/admission reasons. Strengthened bulk execution-plan tests to prove region identities are digest-shaped, hide raw snapshot text, are unique, and match admitted partitions. Verified with `cargo fmt -p worth-runtime-bridge`, `cargo test -p worth-runtime-bridge bulk_workload -- --nocapture` (10 passed), `cargo test -p worth-runtime-bridge packet_reduction -- --nocapture` (11 passed), `cargo check -p worth-runtime-bridge --tests`, clean old partition-key residue scan over bulk planning/tests, clean line-cap scans, and scoped `git diff --check` with only existing CRLF warnings. Next Phase 4 target: canonical workload request string member sets (`subscription_slice_members`, truth-view members, segment digests) and whether they should become typed proof carriers.
- worth-runtime-bridge Phase 1 structured finish replanning: replaced the active Phase 1 execution board with a two-row exact workboard instead of broad archaeology. Current blocker rows are bulk canonical workload request member string collections and historical continuity lineage resolved-key string collections; both must become typed proof/identity carriers before final Phase 1 closeout proof. The board forbids cleanup-only splitting and allows structure edits only when deleting a blocker, quarantining terminal I/O, or preserving the touched-file line cap. Verified with spec-section inspection and `git diff --check` on the spec; no cargo tests were run because this was docs/planning only.
- worth-runtime-bridge Phase 1 active row 1 closeout / Phase 4 bulk canonical request member identity batch: replaced canonical bulk workload request raw member collections with typed identities. The request now carries typed route, subscription-slice, commit, snapshot, and branch identities directly, plus new bulk-scoped continuity member, truth-view member, and workload segment identities. The old `workload_segment_digests` accessor/field is gone; segment identity is `bulk-workload-segment:sha256:*` derived from private native basis rather than exposed `segment|commit=...` text. Coupled bulk workload tests now prove typed member sets and non-leaking digest-shaped truth-view/segment identities. Verified with `cargo fmt -p worth-runtime-bridge`, `cargo test -p worth-runtime-bridge bulk_workload -- --nocapture` (10 passed), `cargo check -p worth-runtime-bridge --tests`, targeted residue scan, line-cap scan, and scoped `git diff --check` with only existing LF/CRLF warnings. Next active Phase 1 row: historical continuity lineage resolved keys.
- worth-runtime-bridge Phase 1 efficiency plan update: tightened `_docs/worth-runtime-bridge/aspect_native_refactor.md` into a five-gate finish protocol (`P1-F0` bounded rebaseline, `P1-F1` exact blocker rewrite, `P1-F2` touched-test shim detox, `P1-F3` terminal JSON/import-wall proof, `P1-F4` closeout proof) and added a concrete Row 2 execution plan for historical continuity lineage resolved keys. The plan forbids search churn, unrelated cleanup, and directory splitting unless the same edit deletes the blocker or preserves the line cap. Next source-changing batch should replace `canonical_resolved_lineage_keys` / `canonical_resolved_record_keys` with typed resolved-lineage and resolved-record identities, update direct continuity consumers/tests, clear old error/accessor vocabulary, and then run focused continuity/check/compile-fail proof. This was docs-only; `git diff --check` on the spec passed and no cargo tests were run.
- worth-runtime-bridge Phase 1 Row 2 and closeout proof: hard-broke historical continuity lineage authority away from raw resolved-key string collections. `BridgeHistoricalLineageAuthority` now accepts and stores `BridgeHistoricalResolvedLineageIdentity` and `BridgeHistoricalResolvedRecordIdentity`, enforces canonical ordering and duplicate denial on typed carriers, and exposes `canonical_resolved_*_identities()` only. Deleted old resolved-key accessor/error vocabulary and updated continuity resolution plus direct builder/facade/harness/test constructor callers to build typed identities directly; QA found and fixed a test helper that still accepted a raw successor string. Verified with `cargo fmt -p worth-runtime-bridge`, `cargo test -p worth-runtime-bridge continuity -- --nocapture` (48 passed), `cargo check -p worth-runtime-bridge --tests`, `cargo test -p worth-runtime-bridge --test phase_boundaries_compile_fail -- --nocapture`, targeted old-key/raw-vector residue scans, whole runtime-bridge line-cap scan, Phase 1 import-wall/no-shim scans, and scoped `git diff --check` (only existing LF/CRLF warnings). The active Phase 1 workboard is closed for the current manifest; next work should continue to the next aspect-native phase target, currently Phase 4 mask-native routing/subscription unless a newer exact blocker row appears.
- worth-runtime-bridge Phase 1 efficiency plan tightened: updated `_docs/worth-runtime-bridge/aspect_native_refactor.md` so the active Phase 1 board has one explicit operating skeleton (`P1-S0` through `P1-S6`), exact row-selection rules, test detox/import-wall/closeout gates, and an explicit current status of no open blocker rows. The planning decision is: Phase 1 is not reopened for cleanup, code size, naming, or broad scans; any future Phase 1-shaped concern must first become an exact blocker row with files and a native replacement, then be fixed inside that row or the phase that surfaces it. Verified with spec scans and `git diff --check` on the spec; no cargo tests were run because this was docs-only planning.
- worth-runtime-bridge Phase 4 bulk packet-reduction identity batch: removed the remaining string-scope authority in bulk packet reduction by carrying `BridgeSubscriptionSliceIdentity` through `TruthDeltaRoutingPacket`, `InvalidationReductionPacket`, and `ReducedBridgePublication`; deleted the old `reduced_target_scope()` packet accessor in favor of `reduced_subscription_slice_identity()`. Also replaced string reduction-family storage with closed `BridgeInvalidationReductionFamily` and replaced lowercased debug widening-class strings with the admitted `BridgeMappingWideningClass` enum through widening packets and reduced widening artifacts. Coupled packet-reduction tests now assert typed subscription-slice identity equality, typed reduction family, typed widening class, and reduced route identities. Verified with `cargo fmt -p worth-runtime-bridge`, `cargo check -p worth-runtime-bridge --tests`, `cargo test -p worth-runtime-bridge packet_reduction -- --nocapture` (11 passed), `cargo test -p worth-runtime-bridge bulk_workload -- --nocapture` (10 passed), `cargo test -p worth-runtime-bridge --test phase_boundaries_compile_fail -- --nocapture`, targeted residue scans, line-cap scan, and scoped `git diff --check` (only existing LF/CRLF warnings). Next Phase 4 target: continue auditing mask-native routing/subscription slice semantics outside bulk packet reduction, especially any remaining route/subscription scope construction where presentation labels can influence identity.

- worth-runtime-bridge Phase 1 efficiency plan tightening: revised the active Phase 1 Operating Skeleton in _docs/worth-runtime-bridge/aspect_native_refactor.md to make the remaining/possible Phase 1 work row-driven instead of search-driven. Added a Phase 1 Efficiency Packet requiring a one-shot residue manifest, exact blocker rows with old authority shape/native replacement/coupled callers/allowed skeleton/focused proof, edit-only-first-row discipline, row-local verification, and explicit invalid work such as cleanup-only splitting, broad grep loops, historical-doc audits, or terminal JSON formatting when the import wall is clean. Verified with spec section inspection and git diff --check on the spec. No cargo tests were run because this was planning-only.

- worth-runtime-bridge Phase 4 lowered invalidation identity batch: replaced lowered invalidation string-scope authority with typed BridgeInvalidationTargetIdentity using the shared BridgeIdentity tag system. BridgeInvalidationTarget now retains signal scope/routing mode for delivery plus native committed-patch target basis and TruthDeltaSurfaceIdentity proof; canonical invalidation and lowering-summary digests consume lowered identities instead of signal_scope:routing_mode keys. Added planned-route access to canonical invalidation targets and strengthened routing tests to prove identity strings are digest-shaped, do not leak signal scope or committed-patch basis text, retain native target basis only as evidence, and diverge for field vs region typed truth-delta surface proofs even under the same signal scope. QA found and fixed the first-pass bespoke identity wrapper by converting it to a tagged BridgeIdentity. Verified with cargo fmt -p worth-runtime-bridge, cargo test -p worth-runtime-bridge routing -- --nocapture (37 passed), cargo test -p worth-runtime-bridge --test phase_boundaries_compile_fail -- --nocapture, cargo check -p worth-runtime-bridge --tests, targeted old target-key residue scan, routing line-cap scan, and scoped git diff --check (only existing LF/CRLF warnings).

- worth-runtime-bridge Phase 1 efficiency plan tightened again: edited the active Phase 1 Finish Execution Board so Phase 1 now runs from one bounded manifest and one blocker queue. The plan requires exact blocker-row contracts before source edits, a row-local batch plan before implementation, edit-only-first-open-row discipline, row-local residue/proof, terminal JSON/import-wall proof, final closeout proof, and explicit invalid-work rules for cleanup-only splitting, broad grep loops, old-vocabulary renames, historical-doc audits, and terminal JSON formatting when the import wall is clean. Verified with git diff --check on the spec; no cargo tests were run because this was docs-only planning.

- worth-runtime-bridge Phase 4 frozen mapping registration identity batch: added BridgeFrozenMappingRegistrationIdentity at the mapping freeze boundary, derived from validated frozen mapping registration truth, and switched route-entry plus bulk mapping-registry digest bases to consume that typed proof identity instead of rebuilding registration selector/signal/routing text downstream. Added mapping-freeze proof that registration identities are digest-shaped/non-leaking and bulk workload proof that canonical planning identity changes through frozen registration identity without exposing signal scope. QA found and fixed the initial line-cap violation by moving the new test into mapping/freezing/tests/registration_identity.rs, and removed dead AspectKeySelector::canonical_label residue. Verified with cargo fmt, cargo check -p worth-runtime-bridge --tests, focused mapping::freezing/routing::canonicalization/bulk_workload tests, phase_boundaries_compile_fail, targeted residue/line-cap scans, and scoped git diff --check (only existing LF/CRLF warnings).

- worth-runtime-bridge Phase 1 efficiency planning: added a compact `Phase 1 Finish Sprint Plan` to `_docs/worth-runtime-bridge/aspect_native_refactor.md`. The plan defines the current closeout workflow as one rebaseline, exact blocker-row classification, first-open-row vertical rewrite, touched-test detox, focused row proof, and sprint closeout proof. It explicitly forbids cleanup-only splitting, repeated broad scans while rows are open, terminal JSON edits when the import wall is clean, and reopening Phase 1 for Phase 4 mask work unless an exact Phase 1 blocker row appears. No source code or cargo tests were run because this was a planning-only batch.

- worth-runtime-bridge Phase 4 subscription target/mask identity batch: added crate-local `BridgeSubscriptionSliceTargetIdentity`, derived from entity identity, admitted committed-patch target basis, projection mask, native surface kind, and subscription slice kind. `NormalizedSubscriptionSliceIntent` and `BridgeSubscriptionSlice` now carry the same typed target/mask proof; subscription declaration digest basis and lowering subscription-slice digest basis consume the proof identity instead of downstream full committed-patch target text. QA caught and fixed an initial overexposure risk by keeping the new proof type/accessors crate-local rather than public. Verified with `cargo fmt -p worth-runtime-bridge`, focused `subscription::declaration`, `routing::lowering`, and `routing::canonicalization` tests, `cargo check -p worth-runtime-bridge --tests`, `cargo test -p worth-runtime-bridge --test phase_boundaries_compile_fail -- --nocapture`, residue scans for public proof exposure/old slice basis handoffs, touched line-cap checks, and scoped `git diff --check` (only existing LF/CRLF warnings).

- worth-runtime-bridge Phase 1 efficiency planning: added a stricter `Phase 1 Finish Sprint Plan` packet to `_docs/worth-runtime-bridge/aspect_native_refactor.md`. Phase 1 closeout is now explicitly one bounded discovery pass, one blocker queue, row-local edits only, touched-test detox, import-wall/line-cap proof, and closeout record. A row cannot be edited until it names old authority shape, native replacement, choke point, direct callers, allowed skeleton, row scan, focused proof, and compile-fail need. Verified with `git diff --check` on the spec; no cargo tests were run because this was docs-only planning.

- worth-runtime-bridge Phase 4 route-entry proof closeout: finished the pending route-entry canonicalization hard break by deriving route-entry digest basis from typed `TruthDeltaSurfaceIdentity` and `BridgeFrozenMappingRegistrationIdentity` proofs only. Strengthened the coupled canonicalization test so the private route-entry basis rejects native target basis, entity text, aspect text, mapping id text, and signal scope text while route/planning provenance carry only `route-entry:sha256:*`. QA found no remaining in-scope architecture issue after the stronger proof. Verified with `cargo fmt -p worth-runtime-bridge`, `cargo test -p worth-runtime-bridge routing::canonicalization -- --nocapture`, `cargo test -p worth-runtime-bridge routing::planning::plan -- --nocapture`, `cargo check -p worth-runtime-bridge --tests`, `cargo test -p worth-runtime-bridge --test phase_boundaries_compile_fail -- --nocapture`, route-entry residue scan, touched line-cap scan, and scoped `git diff --check` (only existing LF/CRLF warning).

- worth-runtime-bridge Phase 4 bulk widening bounded-surface identity batch: found and removed the next exact Phase 4 flattening point where route-record `truth_surface_identity` and bulk widening `bounded_scope_identity` were stored/grouped as raw strings. `BridgeRouteRecordEntry` now retains `TruthDeltaSurfaceIdentity`; `WideningAggregationPacket`, reduction grouping, and `ReducedWideningAggregation` carry the typed surface identity through, with string projection only for digest-basis/public read access. Strengthened packet-reduction tests to prove packet and reduced widening artifacts share the same typed truth-delta surface identity and expose digest-shaped/non-leaking text. QA found and fixed one warning-bearing test-only typed accessor by gating it with `cfg(test)`. Verified with `cargo fmt -p worth-runtime-bridge`, `cargo test -p worth-runtime-bridge packet_reduction -- --nocapture`, `cargo test -p worth-runtime-bridge bulk_workload -- --nocapture`, `cargo check -p worth-runtime-bridge --tests`, `cargo test -p worth-runtime-bridge --test phase_boundaries_compile_fail -- --nocapture`, raw bounded-scope residue scan, touched line-cap scan, and scoped `git diff --check` (only existing LF/CRLF warnings).

- worth-runtime-bridge Phase 1 structured finish replanning: strict one-shot scan found exact remaining raw identity-carrier seams, so `_docs/worth-runtime-bridge/aspect_native_refactor.md` now reopens Phase 1 only for rows 4-7: bulk packet source/continuity identities, structural fingerprint snapshot carrier, speculation promotion/reuse branch bindings, and final closeout proof. The plan forbids broad archaeology and cleanup-only splitting; each row has exact files, native replacement, row scan, and focused proof. No cargo tests were run because this was docs/planning only.

- worth-runtime-bridge Phase 1 row 4 closeout: removed raw source/continuity identity carriers from bulk packet planning. `TruthDeltaRoutingPacket`, `TruthViewMaterializationPacket`, `ContinuityRemapPacket`, `ReducedTruthViewMaterialization`, and `ReducedContinuityRemap` now retain typed branch/commit/snapshot identities plus `BulkTruthViewMemberIdentity`/`BulkContinuityMemberIdentity`; reduction grouping, locality footprint, and reduction-output counting use typed identity sets instead of raw string tuples. Added `planner/member_identities.rs` for shared bulk member identity derivation and strengthened packet-reduction tests to prove packet/reduced artifacts preserve canonical typed member identities. Verified with `cargo fmt -p worth-runtime-bridge`, `cargo test -p worth-runtime-bridge packet_reduction -- --nocapture`, `cargo test -p worth-runtime-bridge bulk_workload -- --nocapture`, `cargo check -p worth-runtime-bridge --tests`, `cargo test -p worth-runtime-bridge --test phase_boundaries_compile_fail -- --nocapture`, clean row residue scan, clean line-cap scan, and scoped `git diff --check` with only existing LF/CRLF warnings. Next active Phase 1 row: structural fingerprint snapshot carrier.

- worth-runtime-bridge Phase 1 row 5 closeout: hard-broke structural fingerprints away from raw snapshot identity storage and string constructor input. `StructuralFingerprint` now retains `TruthSnapshotIdentity`, `from_snapshot_read_packet` requires the typed identity, `snapshot_identity()` returns the typed carrier, and `snapshot_identity_text()` is the explicit projection-only string accessor for canonical-basis/read presentation. Direct structural/facade tests now construct/assert typed snapshot identities rather than preserving string shims. Verified with `cargo fmt -p worth-runtime-bridge`, `cargo test -p worth-runtime-bridge structural -- --nocapture` (54 passed), `cargo check -p worth-runtime-bridge --tests`, `cargo test -p worth-runtime-bridge --test phase_boundaries_compile_fail -- --nocapture`, clean row residue scan for the old carrier/accessor shape, touched line-cap scan, and scoped `git diff --check` with only existing LF/CRLF warnings. Next active Phase 1 row: speculation promotion/reuse branch bindings.

- worth-runtime-bridge Phase 1 row 6 closeout: hard-broke speculation promotion/reuse proofs away from raw branch identity storage. `BridgePromotionAdmissibilityProof` and `BridgePreviewReuseEquivalence` now retain `TruthBranchIdentity` and `BridgeSignalBranchIdentity` from the declaration branch binding, compare typed carriers in proof matching, and render branch text only for canonical digest basis. Added direct owning-module tests that promotion and reuse proof carriers retain typed branch identities. During focused speculation proof, a pricing-shock standard-path assertion exposed non-contractual fanout ordering; updated that assertion to compare delivered invalidation targets as a set rather than preserving arbitrary diagnostics order. Verified with `cargo fmt -p worth-runtime-bridge`, `cargo test -p worth-runtime-bridge speculation -- --nocapture` (29 passed), `cargo check -p worth-runtime-bridge --tests`, `cargo test -p worth-runtime-bridge --test phase_boundaries_compile_fail -- --nocapture`, clean row residue scan for raw truth/signal branch identity fields, touched line-cap scan, and scoped `git diff --check` with only existing LF/CRLF warnings. Next active Phase 1 row: strict closeout proof.

- worth-runtime-bridge Phase 1 strict closeout proof: rows 4-7 in the active strict reopen queue are now closed. Final proof reused the focused Row 5 structural suite and Row 6 speculation suite, package check, and compile-fail boundary suite; `cargo fmt -p worth-runtime-bridge`, import-wall scan, and whole runtime-bridge line-cap scan were clean. The broad no-shim scan still emits expected canonical/foundational/terminal classes: foundational `payload()`/`bytes()` accessors, canonical digest `as_bytes()` mechanics, digest-input counter proof, terminal JSON report/export/capture modules, and presentation-only assertions. No executable authority blocker remains in the Phase 1 queue. Next work should continue the next aspect-native phase target rather than reopening Phase 1 archaeology unless a new exact blocker row is discovered inside that phase.

- worth-runtime-bridge Phase 1 efficiency plan correction: tightened the active Phase 1 finish board in _docs/worth-runtime-bridge/aspect_native_refactor.md so it no longer contradicts the closeout record. The strict reopen queue is now explicitly empty, rows 4-7 are retained as closed closeout evidence rather than active work, and any future Phase 1 concern must first become an exact blocker row with old authority shape, native replacement, direct scope, allowed skeleton, and focused proof before source edits. Verified with doc-section inspection and git diff --check on the spec; no cargo tests were run because this was planning-only.

- worth-runtime-bridge Phase 4 normalized surface target-carrier batch: made normalized truth-delta surfaces retain the native BridgeCommittedPatchTarget from committed patch items instead of parallel decomposed target authority. Surface normalization is now a pure committed-patch-target derivation with no aspect-registry input or fallible Result boundary; fine-grained classification remains the registration-dependent phase. Lowered subscription slices now consume the projection mask from the retained target rather than reconstructing it from field presence. QA found and fixed the first-pass no-op registry lookup plus duplicate surface_kind storage. Verified with cargo fmt -p worth-runtime-bridge, cargo test -p worth-runtime-bridge routing -- --nocapture (37 passed), cargo check -p worth-runtime-bridge --tests, targeted residue scans, routing line-cap scan, and scoped git diff --check (only existing LF/CRLF warnings).

- worth-runtime-bridge Phase 1 efficiency plan tightening: revised the active Phase 1 Finish Sprint Plan in `_docs/worth-runtime-bridge/aspect_native_refactor.md` into one six-step protocol (`P1-F0` through `P1-F5`) with a single bounded manifest, exact blocker-row readiness, first-open-row vertical rewrite, touched-test detox, row-local proof, and one final closeout proof. The plan now explicitly states the current strict Phase 1 queue is empty, forbids source edits/repeated broad scans/cleanup-only splitting/terminal JSON edits unless a later phase surfaces a concrete Phase 1-shaped blocker row, and marks the older lower Phase 1 finish protocol as historical closed evidence rather than a competing active plan. No cargo tests were run because this was docs/planning only.

- worth-runtime-bridge Phase 4 canonical lowering proof-consumption batch: removed downstream proof re-derivation in routing canonicalization. Route-entry ordering now consumes typed frozen mapping registration identities after normalized truth-delta surface identity instead of walking mapping id, selector, target selector, signal scope, specificity, and routing-mode text. Subscription slice digest bases and lowering summaries now consume the retained `BridgeSubscriptionSlice::canonical_basis()` instead of rebuilding a parallel key from slice target, snapshot read contract, and match status; the now test-only `slice_target_identity()` accessor is `cfg(test)`. QA found and fixed the initial proof gap by adding a direct native-registration ordering test through `validate_route_request`. Verified with `cargo fmt -p worth-runtime-bridge`, `cargo test -p worth-runtime-bridge routing::canonicalization -- --nocapture` (3 passed), `cargo test -p worth-runtime-bridge routing::lowering -- --nocapture` (1 passed), `cargo check -p worth-runtime-bridge --tests`, targeted duplicate-key residue scans, routing line-cap scan, and scoped `git diff --check` (only existing LF/CRLF warnings).

- worth-runtime-bridge Phase 1 efficiency packet: added a more structured `Phase 1 Finish Packet` to `_docs/worth-runtime-bridge/aspect_native_refactor.md`. It makes the current answer explicit: the strict Phase 1 queue is empty, Phase 1 source work is row-driven only, and future Phase 1-shaped concerns must name an old authority shape, native replacement, choke point, coupled callers, allowed skeleton, row scan, and focused proof before edits. The packet forbids cleanup-only splitting, repeated broad scans after a ready row, standalone renames, and terminal JSON edits when the import wall is clean. Verified with targeted spec scans and `git diff --check` on the spec; no cargo tests were run because this was planning-only.

- worth-runtime-bridge Phase 4 invalidation target surface-proof consumption batch: removed native committed-patch target basis text from `BridgeInvalidationTargetIdentity` derivation. Lowered invalidation targets now derive identity from signal scope, routing mode, and typed `TruthDeltaSurfaceIdentity`; native target basis remains retained as diagnostic evidence only. Added a direct lowered-target proof that identity is stable across different retained native basis text when the typed surface proof is unchanged, and changed fine-grained routing proof wording to assert identity variation through surface proof. Verified with `cargo fmt -p worth-runtime-bridge`, `cargo test -p worth-runtime-bridge routing::lowering::targets -- --nocapture` (1 passed), `cargo test -p worth-runtime-bridge fine_grained_precision -- --nocapture` (3 passed), `cargo check -p worth-runtime-bridge --tests`, targeted invalidation target basis residue scan, touched line-cap scan, and scoped `git diff --check` (only existing LF/CRLF warnings). Next Phase 4 target: continue routing/subscription seams where native target basis is still consumed as authority rather than retained evidence.

- worth-runtime-bridge Phase 4 subscription target-mask proof consumption batch: removed native committed-patch target basis text as the downstream argument to `subscription_slice_target_identity`. Declaration intents and lowered subscription slices now pass typed `BridgeCommittedPatchTarget` carriers into the proof boundary; a private `BridgeSubscriptionTargetMaskIdentity` is derived first, and `BridgeSubscriptionSliceTargetIdentity` consumes that digest-shaped target/mask proof plus entity identity and slice kind. Native target basis remains retained only as target evidence/projection text. QA caught and fixed the first-pass weakness where the slice identity still embedded full target canonical basis internally. Verified with `cargo fmt -p worth-runtime-bridge`, `cargo test -p worth-runtime-bridge subscription::declaration -- --nocapture` (17 passed), `cargo test -p worth-runtime-bridge routing::lowering -- --nocapture` (2 passed), `cargo test -p worth-runtime-bridge routing::canonicalization -- --nocapture` (3 passed), `cargo check -p worth-runtime-bridge --tests`, targeted residue scan, touched line-cap scan, and scoped `git diff --check` (only existing LF/CRLF warnings). Next Phase 4 target: continue mask-native routing/subscription seams where retained target evidence or presentation labels can still influence identity/canonical authority.
- worth-runtime-bridge Phase 1 structured finish planning: tightened the active Phase 1 sprint plan in `_docs/worth-runtime-bridge/aspect_native_refactor.md` into a two-mode protocol: Phase 1 is either `closed` with no source work, or `reopened-row` with one ready blocker row. The plan now explicitly forbids a third mode of broad searching, cleanup-only splitting, renaming, or readability grooming. Reopened source work cannot begin until the row packet names blocker, old authority shape, native replacement, choke point, direct scope, allowed skeleton, row scan, and focused proof. Verified with a targeted spec inspection and `git diff --check` on the spec; no cargo tests were run because this was docs-only planning.
- worth-runtime-bridge Phase 4 truth-delta surface target-mask proof consumption batch: removed native committed-patch target basis text from `TruthDeltaSurfaceIdentity` derivation. `TruthDeltaSurface` now derives a private `truth-delta-surface-target-mask:sha256:*` proof from typed `BridgeCommittedPatchTarget`, then derives public surface identity from entity identity, native surface kind, and that digest-shaped target/mask proof. Native target basis remains retained only as diagnostics/explanation evidence. QA found no structural blocker because the proof stays local/private and no facade construction surface was added. Verified with `cargo fmt -p worth-runtime-bridge`, `cargo test -p worth-runtime-bridge routing::surfaces -- --nocapture` (6 passed), `cargo test -p worth-runtime-bridge routing::canonicalization -- --nocapture` (3 passed), `cargo test -p worth-runtime-bridge routing::lowering -- --nocapture` (2 passed), `cargo test -p worth-runtime-bridge fine_grained_precision -- --nocapture` (3 passed), `cargo check -p worth-runtime-bridge --tests`, targeted residue scan with only the intended private tag hit, clean routing line-cap scan, and scoped `git diff --check` with only the existing LF/CRLF warning.

- worth-runtime-bridge Phase 1 efficiency planning: replaced the overlapping active Phase 1 finish/sprint packets in `_docs/worth-runtime-bridge/aspect_native_refactor.md` with one `Phase 1 Finish Operating Plan`. The plan now states Phase 1 is closed, has only `closed` and `reopened-row` modes, forbids search/cleanup/splitting as a third mode, requires a complete blocker-row packet before source edits, and keeps historical Phase 1 protocols as audit evidence only. Verified with targeted spec inspection and `git diff --check`; no cargo tests were run because this was planning-only.

- worth-runtime-bridge Phase 4 snapshot read target proof consumption batch: added/finished `SnapshotReadTargetIdentity` as the typed proof consumed by snapshot request canonical basis, correlation ids, packet digest basis, and canonical snapshot read ordering. Deleted the now-dead `SnapshotReadTarget::canonical_basis()` convenience surface after QA caught it as a possible re-opening lane. Native target basis remains retained on `SnapshotReadTarget` only as evidence/projection. Verified with `cargo fmt -p worth-runtime-bridge`, `cargo test -p worth-runtime-bridge snapshot -- --nocapture` (50 passed), `cargo test -p worth-runtime-bridge routing::canonicalization -- --nocapture` (4 passed), `cargo test -p worth-runtime-bridge routing::planning::plan -- --nocapture` (1 passed), `cargo check -p worth-runtime-bridge --tests`, clean row residue scans, clean touched-file line-cap scan, and scoped `git diff --check` with only existing LF/CRLF warnings.

- worth-runtime-bridge Phase 4 continuity prior-slice proof consumption batch: removed native target basis from `PriorSubscriptionSlice` canonical and logical dedup authority by retaining `prior_slice_canonical_basis` from the lowered `BridgeSubscriptionSlice` proof. The prior-slice native target basis accessor is now test-only, and native target basis remains retained only as reconstruction/evidence, not continuity request identity. QA also fixed a split-successor continuity test that asserted incidental ordering instead of the successor set. Verified with `cargo fmt -p worth-runtime-bridge`, `cargo test -p worth-runtime-bridge continuity -- --nocapture` (49 passed), `cargo test -p worth-runtime-bridge routing::planning::plan -- --nocapture` (1 passed), `cargo check -p worth-runtime-bridge --tests`, targeted prior-slice residue scan, touched-file line-cap scan, and scoped `git diff --check` clean.

- worth-runtime-bridge Phase 1 efficiency plan tightening: rewrote the active Phase 1 Finish Operating Plan in `_docs/worth-runtime-bridge/aspect_native_refactor.md` into a compact closed-or-reopened-row protocol. The plan now has explicit modes, a P1-0 through P1-6 finish loop, a ready blocker row contract, classifications, and closeout proof commands. It states Phase 1 is currently closed with an empty strict queue and forbids search/cleanup/file-splitting as a third mode unless a later phase exposes a concrete Phase 1 blocker row. Verified with targeted section inspection and `git diff --check` on the spec; no cargo tests were run because this was planning-only.

- worth-runtime-bridge Phase 4 snapshot read coordinate proof consumption batch: hard-broke `BridgeSnapshotReadCoordinate` away from public caller construction and raw subscription target-basis text. Snapshot read coordinates are now crate-internally constructed; subscription-slice coordinates retain typed `SnapshotReadTargetIdentity`, and that identity is exported as the public read-only evidence surface. Delivery snapshot contract rejection tests now prove the retained coordinate carries the planned target identity, and `snapshot_read_coordinate_constructor_private` compile-fail coverage proves the old raw target-basis constructor path is gone. Verified with `cargo fmt -p worth-runtime-bridge`, `cargo test -p worth-runtime-bridge delivery::snapshot -- --nocapture` (9 passed), `cargo test -p worth-runtime-bridge snapshot -- --nocapture` (50 passed), `cargo check -p worth-runtime-bridge --tests`, `cargo test -p worth-runtime-bridge --test phase_boundaries_compile_fail -- --nocapture`, targeted residue scans, touched-file line-cap scan, and scoped `git diff --check` (only existing LF/CRLF warnings).

- worth-runtime-bridge Phase 1 structured efficiency card: tightened the active Phase 1 Finish Execution Board in `_docs/worth-runtime-bridge/aspect_native_refactor.md` with a top-level Finish Card. The current operating answer is explicit: Phase 1 is closed, the strict reopen queue is empty, and future work may only reopen Phase 1 for one complete blocker row discovered by active phase work. The card defines the decision tree, execution budget, and anti-search rules: no concrete concern means no scan; old words without authority path are classified; possible authority feed gets one bounded manifest; incomplete rows are not edited; complete rows are rewritten vertically and closed. Verified by inspecting the active section and `git diff --check` on the spec; no cargo tests were run because this was docs/planning only.

- worth-runtime-bridge Phase 4 route diagnostic target-carrier batch: moved `BridgeRouteRecordEntry` target evidence from string-only retained canonical basis fields to typed `BridgeCommittedPatchTarget` carriers for both normalized route target evidence and source patch target evidence. Existing `target_canonical_basis()` and `source_target_canonical_basis()` remain projection-only report accessors derived from the typed carriers; route diagnostics/explanations can now inspect native target locators, masks, and target kind directly. Coupled route planning and explanation tests assert typed target carriers plus projection-only basis text. Verified with `cargo fmt -p worth-runtime-bridge`, `cargo test -p worth-runtime-bridge routing::planning::plan -- --nocapture` (1 passed), `cargo test -p worth-runtime-bridge explanations -- --nocapture` (6 passed), `cargo check -p worth-runtime-bridge --tests`, route diagnostic target-field residue scan clean, touched-file line-cap scan clean, and scoped `git diff --check` with only existing LF/CRLF warnings.

- worth-runtime-bridge Phase 1 structured finish replanning: replaced the overlapping active Phase 1 finish card/operating plan text in `_docs/worth-runtime-bridge/aspect_native_refactor.md` with a single `Phase 1 Sprint Packet`. The active status remains closed with an empty strict reopen queue, but the execution protocol is now more efficient: P1-S0 status lock, P1-S1 concern packet, P1-S2 row readiness, P1-S3 vertical annihilation, P1-S4 row proof, and P1-S5 closeout lock. The plan explicitly forbids broad scans, source edits, cleanup-only splitting, old-word renames, and terminal JSON churn unless a complete blocker row exists. Verified with targeted section inspection and `git diff --check` on the spec; no cargo tests were run because this was docs/planning only.

- worth-runtime-bridge Phase 1 finish plan tightening: rewrote the active Phase 1 board into a stricter two-mode operating protocol. Phase 1 is either `closed` with no scan/edit work, or `reopened-row` with one complete blocker row found by later-phase work. The finish loop is now P1-F0 through P1-F6: status lock, concern capture, blocker-row readiness, batch plan, vertical annihilation, structural restraint, and proof/closeout. Added `current-test impact` to the blocker row contract so test shims are explicitly in scope, and removed stale Phase 4-specific next-action wording. Verified with targeted spec inspection and `git diff --check` on the spec; no cargo tests were run because this was docs/planning only.

- worth-runtime-bridge Phase 5 grouped binding projection batch: replaced the remaining grouped truth "first field by aspect key" lookup with exact whole-aspect projection matching over native `AspectKey`, no field locator, and whole-aspect `ProjectionMask`. Grouped truth now rejects ambiguous binding projections through typed `AmbiguousIdentityAspect`/`AmbiguousGroupingAspect` errors with row/aspect/count evidence instead of silently selecting a row field. Added hostile grouped truth coverage for ambiguous whole-aspect grouping bindings and updated row-set tests to use the whole-aspect selector. QA found and fixed an unnecessary per-member candidate `Vec` allocation by switching to a single-pass match classifier. Verified with `cargo fmt -p worth-runtime-bridge`, `cargo test -p worth-runtime-bridge source -- --nocapture` (65 passed), `cargo check -p worth-runtime-bridge --tests`, residue scans for the deleted aspect-key lookup/direct map lookup/candidate collection shim, touched-file line-cap scan, and scoped `git diff --check` (only existing LF/CRLF warnings).

- worth-runtime-bridge Phase 5 grouped lane native aspect-key batch: moved `GroupedProjectionContract.grouping_aspect` and `BridgeGroupedLaneValue.grouping_aspect` from stored text to foundational `AspectKey`, added `native_grouping_aspect_key()` read-only accessors, and added invalid grouping-aspect-key rejection before materialization. Grouped truth digest basis now consumes the native grouping aspect key. Focused source tests and package check still pass. Remaining Phase 5 blocker: `GroupedProjectionSource` itself still admits grouping/identity/grouping binding keys as `&str`; next batch should hard-break that trait and direct worth-query-facing implementors to native `AspectKey` without compatibility shims.

- worth-runtime-bridge Phase 4 patch/routing error-coordinate target-carrier batch: moved `BridgePatchTargetCoordinate` fully onto typed `BridgeCommittedPatchTarget` retention. Patch construction and missing mapping denials now pass the native target carrier into the coordinate; aspect key, aspect locator, field locator, surface kind, projection mask, and target canonical basis are derived from that carrier. QA found and fixed an initial duplicate-evidence cache by deleting `target_canonical_basis_projection`; target basis is now an owned projection from the retained target. The compile-fail fixture now uses a sealed native target placeholder rather than WORTHd target-basis strings. Verified with `cargo fmt -p worth-runtime-bridge`, `cargo test -p worth-runtime-bridge input::envelope -- --nocapture` (8 passed), `cargo test -p worth-runtime-bridge routing::eligibility -- --nocapture` (2 passed), `TRYBUILD=overwrite cargo test -p worth-runtime-bridge --test phase_boundaries_compile_fail -- --nocapture`, `cargo test -p worth-runtime-bridge --test phase_boundaries_compile_fail -- --nocapture`, `cargo check -p worth-runtime-bridge --tests`, residue scans, touched line-cap scan, and scoped `git diff --check` (only existing LF/CRLF warnings). Next Phase 4 target: continue mask/native-proof consumption toward remaining grouped truth/query-facing projection contract seams without reopening Phase 1 unless a complete blocker row appears.

- worth-runtime-bridge Phase 1 efficiency plan update: added a compact Phase 1 Finish Card to the active execution board in _docs/worth-runtime-bridge/aspect_native_refactor.md. The card states Phase 1 is closed with an empty reopen queue, forbids broad scans/cleanup-only splitting/renames/test polishing, and defines the exact reopened-row protocol: name the blocker from active later-phase work, fill old authority shape/native replacement/choke point/direct scope/allowed skeleton/row scan/focused proof, rewrite vertically with no shims or aliases, verify row-local proof, then return Phase 1 to closed. Verified with targeted spec inspection and git diff --check; no cargo tests were run because this was docs-only planning.

- worth-runtime-bridge Phase 4 final closeout readiness: ran the bounded target/mask authority residue audit over routing, subscription, snapshot, continuity, delivery, diagnostics, and error surfaces. Found and fixed one real duplicate authority-shaped residue: BridgeRouteRecordEntry cached 	arget_canonical_basis_projection and source_target_canonical_basis_projection beside typed BridgeCommittedPatchTarget carriers. The record now stores only typed target carriers and derives projection-only target basis accessors at report time. Remaining hits are classified as private one-way target/mask proof construction, retained diagnostic/evidence projection, terminal report/test assertions, or canonical basis rows consuming typed identities. Verified with cargo fmt -p worth-runtime-bridge, cargo test -p worth-runtime-bridge routing::planning::plan -- --nocapture (1 passed), cargo test -p worth-runtime-bridge explanations -- --nocapture (6 passed), cargo check -p worth-runtime-bridge --tests, duplicate-cache scan clean, touched line-cap scan clean, and scoped git diff --check with only existing LF/CRLF warning. Phase 4 is closed for the current manifest; next work is Phase 5 grouped truth/query-facing projection contracts.

- worth-runtime-bridge Phase 5 grouped projection-source hard break: changed `GroupedProjectionSource` to require foundational `AspectKey` carriers for grouping aspect, identity binding, and grouping binding keys. Deleted `GroupedProjectionContractError` and grouped-truth `InvalidProjectionContract` because invalid aspect-key text is now unrepresentable instead of runtime-rejected. Updated runtime-bridge grouped tests/support, direct worth-query projection fixture, relational grouped projection implementor, and downstream query/relational compile fallout to consume typed historical-lineage identities and validated row-set values rather than old key/read-value shims. Split the touched over-cap relational bridge source test into `bridge_source_tests/{mod,support,lineage,publication}.rs`. Verified with `cargo fmt -p worth-runtime-bridge -p worth-query -p worth-relational`, `cargo test -p worth-runtime-bridge source -- --nocapture` (65 passed), `cargo check -p worth-runtime-bridge --tests`, `cargo check -p worth-query --tests`, `cargo check -p worth-relational --tests`, targeted residue scans, touched-file line-cap scan (only pre-existing allowlisted `worth-query/src/correspondence/tests.rs` over cap), and scoped `git diff --check` (only existing LF/CRLF warnings). Next Phase 5 target: audit query grouped planning/live projection text carriers and hard-break only the authority-bearing ones.

- worth-runtime-bridge Phase 1 structured finish planning: replaced the active Phase 1 finish board in `_docs/worth-runtime-bridge/aspect_native_refactor.md` with a compact Finish Packet. The packet makes the current state explicit: Phase 1 is closed, the strict reopen queue is empty, and Phase 1 may reopen only for a complete row exposed by later-phase work. The new plan names the reopen trigger, required row packet, P1-0 through P1-6 execution loop, reopened-row batch template, active queue, classification table, efficiency rules, and proof commands. It forbids broad scans, cleanup-only splitting, old-word renames, and test polishing unless the row deletes a concrete executable non-native authority shape. Verified with targeted spec inspection and `git diff --check`; no cargo tests were run because this was docs-only planning.

- worth-runtime-bridge Phase 1 efficiency replanning: tightened `_docs/worth-runtime-bridge/aspect_native_refactor.md` again into one active `Phase 1 Finish Plan` with explicit modes (`closed`, `candidate-row`, `reopened-row`), a `What Is Left` table, row-packet requirements, execution loop, and closeout certification commands. Removed stale historical wording that still called older Phase 1 boards "current" or "working" plans, so future turns have one operational source and cannot accidentally restart broad Phase 1 archaeology. Phase 1 remains closed with an empty strict reopen queue; no source code or cargo tests were changed/run because this was planning-only.
- worth-runtime-bridge Phase 1 structured finish planning: added a concise `Phase 1 Efficient Finish Card` to `_docs/worth-runtime-bridge/aspect_native_refactor.md`. The card locks Phase 1 in `closed` mode with an empty strict reopen queue, defines the only valid modes (`closed`, `evidence-only`, `concern`, `reopened-row`), lists the evidence-only sprint order, and requires a complete blocker-row packet before any source edits. Planning sanity checks passed with targeted spec inspection and `git diff --check`; no cargo tests were run because this was docs-only.

- worth-runtime-bridge Phase 5 grouped projection-consumption closeout: hard-broke query projection-consumption grouped machine facts away from stored grouping-aspect text. `ConsumedMembershipFact` and grouped `ConsumedRelationEndpointFact` now retain foundational `AspectKey`; relational and bridge grouped extraction pass native grouping keys into consumed fact construction; fact-set digest and certification oracle digest paths consume native key accessors while `grouping_aspect()` remains projection/read-only. Strengthened grouped source extraction tests to assert native consumed grouping keys. Verified with `cargo test -p worth-query projection_consumption -- --nocapture` (76 passed plus projection-consumption compile-fail boundary), `cargo check -p worth-query --tests`, `cargo test -p worth-runtime-bridge source -- --nocapture` (65 passed), clean grouped-aspect string-authority residue scans, touched-file line-cap checks, and scoped `git diff --check` with only existing LF/CRLF warnings. Updated the runtime-bridge spec: Phase 5 is closed for the current grouped truth/query-facing projection manifest; next work is Phase 6/7 reconciliation from current code/spec.

- worth-runtime-bridge Phase 1 efficient finish board: added a compact active finish board to `_docs/worth-runtime-bridge/aspect_native_refactor.md`. The board defines `P1-Lock`, `P1-Concern`, `P1-Row`, `P1-Rewrite`, and `P1-Proof`, making the current operating rule explicit: Phase 1 remains closed with an empty reopen queue, and future Phase 1-shaped work can begin only from one concrete later-phase concern that becomes a complete blocker row. Broad scanning, cleanup-only splitting, naming polish, and test detox remain out of scope unless they delete the row's executable old authority shape in the same vertical batch. No cargo tests were run because this was planning-only.

- worth-runtime-bridge Phase 6 authority request/receipt native intent retention batch: hard-broke the adapter-bound writeback authority request and receipt away from digest-only effect evidence. `TruthWritebackRequest` now retains the native `BridgeWritebackEffectIntent` and exposes `effect_intent()` as the read-only authority carrier; `TruthWritebackReceipt` copies and retains the same typed intent from the producing request. Existing digest and patch canonical-basis fields remain projection/evidence accessors derived from the native carrier. Facade writeback authority-session and admitted-execution-contract tests now assert request/receipt artifacts retain the same foundational authoritative patch as the lowered effect. QA found a related receipt-only projection weakness and fixed it before verification. Focused writeback verification initially exposed an unrelated pricing fanout order assertion over unordered retained diagnostics; fixed the capture path to sort target projections before asserting first/last. Verified with `cargo fmt -p worth-runtime-bridge`, `cargo test -p worth-runtime-bridge writeback -- --nocapture` (86 passed; expected caught-panic output), `cargo check -p worth-runtime-bridge --tests`, `cargo test -p worth-runtime-bridge --test phase_boundaries_compile_fail -- --nocapture`, clean old-effect-digest residue scan, touched-file line-cap scan, and scoped `git diff --check` with only existing LF/CRLF warnings. Updated the Phase 6 spec evidence in `_docs/worth-runtime-bridge/aspect_native_refactor.md`.

- worth-runtime-bridge Phase 1 efficiency planning: replaced the active Phase 1 finish section with a tighter `Phase 1 Completion Runbook`. The runbook states Phase 1 is currently closed with an empty strict reopen queue, and turns future Phase 1 work into one decision tree plus row packet: no broad scan, split, rename, or test detox unless later-phase work exposes a concrete executable blocker with old shape, native replacement, choke point, direct scope, test impact, allowed skeleton, row scan, and focused proof filled in before editing. Historical sections now point to the new runbook heading. This was docs-only; verified the active section heading references and trailing-whitespace scan. No cargo tests were run.

- worth-runtime-bridge Phase 6/7 duplicate-authority certification native retention batch: hard-broke duplicate writeback certification away from digest-only effect/request/receipt bundle truth. `WritebackDuplicateAuthorityMatrix` now retains `BridgeDerivedWritebackEffect`; `DuplicateReplayBundleReport` retains `BridgeWritebackReplayBundle`; and `DuplicateAuthorityBoundaryMatrix` retains both `TruthWritebackRequest` and `TruthWritebackReceipt` artifacts for first/repeated attempts. Digest and patch canonical-basis accessors now derive from those typed carriers for terminal projection only. Split over-cap `writeback/typed_certification_tests.rs` into `typed_certification_tests/{mod,duplicate_authority,other_certifications}.rs`; the duplicate proof now asserts the retained effect intent is identical across matrix effect evidence, both authority requests, both receipts, and retained replay bundle projection. Verified with `cargo fmt -p worth-runtime-bridge`, `cargo test -p worth-runtime-bridge duplicate_certification_retains_typed_authority_matrix -- --nocapture`, `cargo test -p worth-runtime-bridge writeback_certification -- --nocapture`, `cargo check -p worth-runtime-bridge --tests`, duplicate-authority digest-only residue scan, and touched-file line-cap scan. Next Phase 6/7 target: repeat this native-retention pattern for authority-denial and feedback/replay certification families that still store optional request/receipt/effect evidence as digest fields.

- worth-runtime-bridge Phase 1 efficiency planning: tightened the active Phase 1 Completion Runbook in `_docs/worth-runtime-bridge/aspect_native_refactor.md` into three modes only: `closed`, `concern`, and `reopened-row`. Added a required concern packet before any Phase 1 inspection, kept a single row packet before any source edits, made vertical rewrite/proof/lockback the only implementation loop, and removed the duplicate reopened-row template so future turns do not restart broad archaeology. This was docs-only; verified the active section shape and clean `git diff --check` on the spec.

- worth-runtime-bridge Phase 6/7 authority-denial certification native retention batch: hard-broke authority-denial writeback certification away from digest-only request/receipt/effect retention. `AuthorityDenialBoundaryFailure` now stores optional `BridgeDerivedWritebackEffect`, `TruthWritebackRequest`, and `TruthWritebackReceipt` carriers, with digest accessors deriving terminal projections from those carriers. `RejectingTruthWritebackAuthority` records a typed rejected request/receipt attempt instead of last request/receipt digest strings. The authority-denial typed certification test now proves merge rejection retains effect/request/receipt carriers with matching effect intent and that pre-authority feedback denials retain effect evidence while request/receipt remain absent. Verified with `cargo fmt -p worth-runtime-bridge`, `cargo test -p worth-runtime-bridge authority_denial_certification_retains_typed_zero_residue_proof -- --nocapture`, `cargo test -p worth-runtime-bridge writeback_certification -- --nocapture`, `cargo check -p worth-runtime-bridge --tests`, scoped digest-only residue scan, touched line-cap scan, and scoped diff hygiene.

- worth-runtime-bridge Phase 1 efficiency plan closeout: added a concrete `Phase 1 Closeout Certification Sprint` to `_docs/worth-runtime-bridge/aspect_native_refactor.md`. The sprint converts "are we really done?" into proof gates: status lock, no-shim scan/classification, JSON/import wall, public/test constructor wall, line-cap/topology restraint, closeout proof commands, and lockback. It explicitly forbids edits during scan/classification gates and permits source work only after a complete blocker row exists, keeping Phase 1 from becoming broad cleanup or archaeology.
- worth-runtime-bridge Phase 6/7 feedback-loop certification native-retention closeout: finished the feedback-loop writeback certification matrix so convergence proof retains typed `BridgeDerivedWritebackEffect`, `BridgeWritebackReplayBundle`, admitted contract, idempotence, loop-prevention, strategy-coherence, optional candidate/request/receipt, changed-effect idempotence/failure, and rebuilt restart-replay carriers. QA found and fixed a duplicate loop-prevention digest/disposition cache by deriving those projections from the retained `BridgeWritebackLoopPreventionReport`. Added dedicated `typed_certification_tests/feedback_loop.rs` proof that walks typed carriers directly instead of terminal JSON. Verified with `cargo fmt -p worth-runtime-bridge`, `cargo test -p worth-runtime-bridge feedback_certification_retains_typed_convergence_proof -- --nocapture`, `cargo test -p worth-runtime-bridge writeback_certification -- --nocapture`, `cargo check -p worth-runtime-bridge --tests`, targeted digest-only/no-shim residue scans, touched-file line-cap scan, and scoped `git diff --check`.
- worth-runtime-bridge Phase 1 efficiency planning: tightened the active Phase 1 Finish Packet in `_docs/worth-runtime-bridge/aspect_native_refactor.md` so Phase 1 is explicitly closed unless a complete blocker row exists. The plan now uses a concise `P1-F0` through `P1-F5` loop: status/concern decision, optional closeout sprint, bounded concern inspection, row-packet readiness, one vertical annihilation rewrite, and proof/lockback. It also records the default answer: nothing executable is open in Phase 1; what remains is closeout proof on demand plus a strict reopen protocol if later-phase work exposes a concrete authority blocker. Verified with targeted section inspection and `git diff --check` on the spec; no cargo tests were run because this was planning-only.
- worth-runtime-bridge Phase 6/7 admission-boundary certification native-retention batch: hard-broke the multi-family admission-boundary certification row away from digest-only family/authority proof after typed artifacts were already available. `AdmissionBoundaryFamilyEvidence` now retains `AdmittedBridgeWritebackContract`, `BridgeDerivedWritebackEffect`, `BridgeWritebackIdempotenceBasis`, and `BridgeWritebackReplayBundle`; `AdmissionBoundaryFamilyAdmissionProof` retains projected/aspect admitted contracts; and `AdmissionBoundaryAuthorityProof` retains projected/aspect `BridgeWritebackAuthorityOutcome` carriers. Digest/report accessors now derive terminal projections from retained typed carriers. The focused typed certification test now proves contract, effect-intent, patch-basis, idempotence, replay, and authority commit projections are derived from retained carriers. Verified with `cargo fmt -p worth-runtime-bridge`, `cargo test -p worth-runtime-bridge admission_boundary_certification_retains_typed_family_admission_proof -- --nocapture`, `cargo test -p worth-runtime-bridge writeback_certification -- --nocapture`, `cargo check -p worth-runtime-bridge --tests`, targeted digest-only field residue scan, touched line-cap scan, and scoped `git diff --check`.
- worth-runtime-bridge Phase 1 structured finish planning: added a lane-based `Structured Finish Plan` to the active Phase 1 Completion Runbook. Phase 1 now has exactly three efficient lanes: evidence-only closeout when the team wants confidence, named concern triage when a later phase names one suspicious seam, and reopened blocker rewrite only after a complete row packet exists. The update explicitly says Phase 1 has no cleanup backlog: file splitting, naming cleanup, and broad helper reshaping count only when they delete a proven legacy authority path, import-wall violation, fake current-test shim, or touched-file line-cap failure. Verified with `git diff --check` on the spec; no cargo tests were run because this was planning-only.
- worth-runtime-bridge Phase 6/7 mapper-parity certification native-retention batch: hard-broke mapper-parity family evidence away from digest-only effect/replay truth. `MapperParityFamilyEvidence` now retains `BridgeDerivedWritebackEffect` and `BridgeWritebackReplayBundle` carriers; `WritebackMapperParityMatrixEvidence` passes typed projected/aspect replay bundles instead of replay digest strings; effect-intent, patch canonical-basis, causality, and replay digest accessors derive terminal projections from retained carriers. Split the mapper-parity typed proof into `typed_certification_tests/mapper_parity.rs`, restoring `other_certifications.rs` under the line cap, and strengthened the test to walk retained effect/replay carriers directly.
- worth-runtime-bridge Phase 6/7 replay-loop isolation native-retention batch: hard-broke replay-loop family and isolation evidence away from stored digest/equality/projection strings. Replay-loop family rows now retain `BridgeDerivedWritebackEffect`, `BridgeWritebackIdempotenceBasis`, and `BridgeWritebackReplayBundle`; cross-family replay retains projected/aspect replay bundles plus typed error/replay record; same-family equivalence retains original/rebuilt effects/bundles plus execution record; changed-causality retains original/changed bundles plus typed error/replay record; feedback isolation retains feedback context plus loop-prevention report. Split `replay_loop_isolation/mod.rs` into proof-responsibility modules to restore the production line cap, and split the focused proof into `typed_certification_tests/replay_loop.rs`.

- worth-runtime-bridge Phase 1 structured finish plan refresh: inserted a concrete `Phase 1 Finish Execution Board` into `_docs/worth-runtime-bridge/aspect_native_refactor.md`. The board turns Phase 1 closeout into six fixed gates: authority residue classification, JSON/import wall, public/current-test constructor wall, current-test/UI fixture proof, topology restraint, and lockback. It names `BridgeDiagnostics::explain_route(&str)` as the only current concern candidate, with a bounded triage radius and blocker threshold instead of immediate source edits. Verified the inserted section and `git diff --check` on the spec; no cargo tests were run because this was planning-only.

- worth-runtime-bridge Phase 1 efficiency plan correction: updated the active Phase 1 Completion Runbook to reflect the real current state, not the older "closed queue" story. Phase 1 is now `reopened-row` with exactly one live row: `P1-R1 diagnostics public identity lookup`. The plan says to finish typed diagnostics lookup identities, run compile fallout, then triage only adjacent writeback public lookup strings before the constructor/current-test wall, JSON/import wall, touched line-cap proof, and lockback commands. It explicitly forbids broad Phase 1 search, cleanup-only splitting, test shims, and second-row work while R1 is unresolved. This was docs/memory only; `git diff --check` on the spec was clean and no cargo tests were run.
- worth-runtime-bridge Phase 1 structured finish replanning: collapsed the active Phase 1 runbook into a single `Phase 1 Finish Packet` in `_docs/worth-runtime-bridge/aspect_native_refactor.md`. The packet records that `P1-R1 diagnostics public identity lookup` is mostly implemented with closeout gates pending, folds adjacent writeback retained-record identity lookups into the row, names the exact row packet, lays out `P1-F1` through `P1-F6`, includes exact residue scans and proof commands, and demotes the older structured finish notes so they are guardrails rather than a competing execution plan. No source code or cargo tests were run; this was planning-only, with `git diff --check` clean on the spec.
- worth-runtime-bridge Phase 1 R1 lockback closeout: ran the `Phase 1 Finish Packet` gates for diagnostics public identity lookup and returned Phase 1 to `closed`. Refined row scans found no raw string replay calls, no caller `.as_str()` retained-identity lookups, no stale public `&str` signatures, and no runtime-bridge UI `todo!`/`unimplemented!` placeholders; the only `&str` row hit is the intentional `writeback_diagnostics_lookup_requires_record_identity` trybuild fixture. JSON/import wall classified cleanly: no `serde_json::Value` or `json!` in native diagnostics/facade/source/routing/writeback/speculation authority directories, with remaining production JSON under terminal report/export projection topology. Row-touched files are under the 400-line cap. Verified with `cargo fmt -p worth-runtime-bridge -p worth-query`, `cargo check -p worth-runtime-bridge --tests`, `cargo check -p worth-query --tests`, `cargo test -p worth-runtime-bridge --test phase_boundaries_compile_fail -- --nocapture`, `cargo test -p worth-runtime-bridge speculation -- --nocapture`, `cargo test -p worth-runtime-bridge diagnostics_explanations -- --nocapture`, `cargo test -p worth-runtime-bridge writeback -- --nocapture`, and `cargo test -p worth-runtime-bridge causal_envelope -- --nocapture`, all with 10 minute command timeouts. Updated the spec: strict Phase 1 reopen queue empty, no current concern, R1 closed.
- worth-runtime-bridge Phase 1 efficiency plan refinement: added a structured finish board to `_docs/worth-runtime-bridge/aspect_native_refactor.md` with exactly three lanes: `P1-A confidence closeout`, `P1-B named concern triage`, and `P1-C reopened blocker rewrite`. The plan explicitly forbids cleanup-only splitting, vocabulary polish, broad search work, and test prettification under Phase 1; large files matter only when the row touches them and the split deletes an authority blocker, quarantines JSON/foreign I/O, or restores the touched-file line cap. Verified the inserted section and `git diff --check`; no cargo tests were run because this was planning-only.
- worth-runtime-bridge Phase 6/7 execution/provenance native-retention batch: hard-broke writeback execution records, mutation provenance bundles, batch mutation authority counts, and admitted execution receipts away from digest-only request/receipt truth. `BridgeWritebackExecutionRecord` and `BridgeMutationProvenanceBundle` now retain typed `TruthWritebackRequest`/`TruthWritebackReceipt` carriers and derive digest accessors as projections; `BridgeBatchMutationAuthorityBundle` counts typed retained request/receipt carriers; `BridgeAdmittedWritebackExecutionReceipt` now retains the typed admitted execution request plus typed authority receipt, and `BridgeAdmittedWritebackExecution` no longer duplicates the receipt beside the sealed execution receipt. QA found and fixed the admitted execution receipt edge plus the duplicate parent retention. Verified with `cargo fmt -p worth-runtime-bridge -p worth-query`, `cargo check -p worth-runtime-bridge --tests`, compile-fail trybuild overwrite/rerun for the private constructor signature, `cargo test -p worth-runtime-bridge writeback -- --nocapture` (86 passed; expected caught-panic output), `cargo test -p worth-runtime-bridge writeback_certification -- --nocapture` (8 passed), residue scans for old digest-only fields/counts, touched-file line-cap scan, and scoped `git diff --check` with only existing LF/CRLF warnings.
- worth-runtime-bridge Phase 1 efficiency planning: tightened the active Phase 1 Completion Runbook with an efficient closeout board (`P1-0` through `P1-4`) so source edits cannot start from broad scans or cleanup instincts. Phase 1 is still closed with no source-edit backlog; permitted Phase 1 work is either a confidence scan on request or one named concern triage from later-phase work. Any source edit now requires a complete blocker packet naming the old authority shape, native replacement carrier, choke point, direct scope, test/compile-fail shim, required topology, deletion list, residue scan, and focused proof commands. No cargo tests were run because this was docs/planning only.
- worth-runtime-bridge Phase 6/7 authority-denial proof-carrier retention batch: replaced authority-denial certification boundary storage of contract/idempotence/strategy digest strings with typed `AdmittedBridgeWritebackContract`, `BridgeWritebackIdempotenceBasis`, `BridgeWritebackStrategyBasis`, and `BridgeWritebackStrategyCoherenceReport` carriers. Digest and disposition accessors now derive terminal projections from retained typed carriers; terminal JSON remains projection-only. Split the touched over-cap `authority_denial.rs` into `authority_denial/{mod,boundary,loop_prevention,zero_residue}.rs` so matrix orchestration, boundary proof retention, loop-prevention evidence, and zero-residue proof have separate structural fate. Strengthened the typed authority-denial certification proof to walk retained contract/idempotence/strategy carriers directly.
- worth-runtime-bridge Phase 1 structured finish planning: tightened the active Phase 1 Finish Packet into three explicit modes only: `P1-A closed confidence`, `P1-B named concern`, and `P1-C reopened row`. Added a concrete closeout sprint order, concern triage packet, blocker row packet, and a hard no-preservation rule for source and tests. Phase 1 remains closed with no source-edit backlog; broad scans are classification gates only, and source edits require a complete blocker row proving executable old authority. No cargo tests were run because this was docs/planning only.
- worth-runtime-bridge Phase 6/7 loop-prevention feedback proof retention batch: replaced production `BridgeWritebackLoopPreventionReport` cached feedback/causality/idempotence digest fields with retained `BridgeWritebackFeedbackProvenance`, optional `BridgeWritebackFeedbackContext`, and `BridgeWritebackIdempotenceBasis` carriers. Diagnostics safety-gate explanations now retain typed loop-prevention and strategy-coherence reports, authority-denial loop-prevention/boundary evidence retains typed reports and incoming feedback context, and duplicate-authority loop-prevention/attempt reports retain typed loop-prevention, idempotence, outcome, replay-bundle, and receipt carriers. Verified with `cargo fmt -p worth-runtime-bridge`, `cargo check -p worth-runtime-bridge --tests`, `cargo test -p worth-runtime-bridge writeback_certification -- --nocapture`, `cargo test -p worth-runtime-bridge authority_feedback -- --nocapture`, focused typed proof tests for duplicate authority, authority denial, and feedback loop, row residue scans, touched line-cap scan, and scoped diff hygiene with only existing LF/CRLF warnings.

- worth-runtime-bridge Phase 1 efficiency planning: tightened the active Phase 1 Finish Packet into a fixed six-gate bounded evidence sprint. Phase 1 remains closed by default; source edits are forbidden until a complete blocker row proves current executable old authority. The new plan orders status lock, authority residue wall, JSON/import wall, public/current-test constructor wall, topology restraint, focused proof, and lockback, with exact stop conditions and an allowed split skeleton limited to native authority/proof/diagnostic/terminal-report responsibilities. Verified with targeted section inspection and git diff --check on the spec; no cargo tests were run because this was docs-only planning.
- worth-runtime-bridge Phase 1 finish-plan efficiency refresh: rewrote the active Phase 1 Finish Packet into four explicit modes (`P1-closed`, `P1-confidence`, `P1-concern`, `P1-row`) plus fixed confidence gates `P1-G0` through `P1-G6`. The plan now makes Phase 1 a closed gate with a restart protocol instead of a cleanup backlog: no scans unless confidence proof is requested or a named later-phase seam appears; no source edits from scan hits; row packet required before any edit; one vertical annihilation row at a time; splitting only when the row deletes old authority, quarantines external I/O, or restores touched-file line cap. No cargo tests were run because this was docs-only planning.
- worth-runtime-bridge Phase 1 structured finish efficiency update: tightened the active Phase 1 Finish Packet with a `P1-D0` through `P1-D4` Fast Decision Board and explicit reopened-row batch order. Phase 1 remains closed by default; source-changing work now requires a complete blocker row proving executable old authority, native replacement carrier, choke point, current-test seam, deletion list, optional authority/line-cap skeleton, focused proof commands, and lockback. The plan explicitly forbids cleanup-only file splitting, helper movement, naming polish, broad archaeology, or test prettifying. Verified the active spec section and `git diff --check`; no cargo tests were run because this was planning-only.
- worth-runtime-bridge Phase 1 efficiency plan refinement: added a concise `Phase 1 Execution Cockpit` to `_docs/worth-runtime-bridge/aspect_native_refactor.md` so future Phase 1-shaped turns start from a fast decision ladder instead of historical-board archaeology. The cockpit records current state as `closed`, strict reopen queue empty, no live concern, and default action to continue later phases. It defines the only finish stop condition (`mode=closed`, empty queue, no live concern, zero unclassified confidence hits), an ordered confidence pass, three edit permissions only, and a required reopened-row skeleton that forbids `helpers`, `common`, `legacy`, `compat`, `shim`, `raw`, `old`, `new`, and cleanup-only folders even in tests. This was docs/planning only; no cargo tests were run.
- worth-runtime-bridge Phase 8 typed harness target-selection closeout: finished the lockback for `BridgeHarnessTargetId` as the harness execution target authority. The target-id surface is split into `target_id/{mod.rs,constructors.rs,terminal_projection.rs}`: `mod.rs` owns the native enum, `constructors.rs` exposes typed constructors requiring `TruthCommitIdentity`, `TruthBranchIdentity`, `SourceDeclarationIdentity`, `MergeHistoryDeclarationIdentity`, and `StructuralIdentityDeclarationIdentity`, and `terminal_projection.rs` is the only string projection boundary for display/report names. QA found and fixed wildcard imports in the new target-id modules. Updated the Phase 8 closeout evidence in `_docs/worth-runtime-bridge/aspect_native_refactor.md`. Verified with `cargo fmt -p worth-runtime-bridge`, `cargo check -p worth-runtime-bridge --tests`, focused mixed-offline certification test, target constructor/no-string residue scans, wildcard-import scan, touched line-cap checks, and whitespace/diff hygiene; remaining `target: String` hits are pricing presentation/support fields, not execution authority.
- worth-runtime-bridge Phase 8 writeback certification terminal-label detox: closed the current-test leak where writeback certification tests called `BridgeHarnessTargetId::as_external_target()` and named scenario/run strings as `external_target`. `writeback_certification.rs` now keeps `BridgeHarnessTargetId` as the execution authority for every `ExecutionRequest::target` call and derives fixture/request strings only through a local `WritebackCertificationTerminalLabels` packet. Updated Phase 8 closeout evidence. Verified with `cargo fmt -p worth-runtime-bridge`, `cargo test -p worth-runtime-bridge writeback_certification -- --nocapture`, `cargo check -p worth-runtime-bridge --tests`, and a targeted residue scan proving `as_external_target` remains only in `harness/adapter/target_id/terminal_projection.rs`.
- worth-runtime-bridge Phase 1 efficiency planning: added a concise `Phase 1 Efficient Finish Plan` to `_docs/worth-runtime-bridge/aspect_native_refactor.md`. The active answer is that Phase 1 is closed by default; remaining Phase 1 work is only one of four lanes: closed lock, bounded confidence proof, one named concern from later-phase evidence, or one complete blocker-row rewrite. The plan explicitly forbids cleanup-only splitting, helper prettifying, broad search work, and source edits before a complete row packet exists. If confidence proof opens no blocker row, Phase 1 is done for the current manifest; if it opens one, finish exactly that row vertically before any other Phase 1 work. Verified with section inspection and `git diff --check` on the spec; no cargo tests were run because this was planning-only.
- worth-runtime-bridge Phase 8 merge declaration fixture authority detox: hard-broke the merge facade/harness/adapter certification test helpers that accepted raw merge declaration ID strings and then minted `MergeHistoryDeclarationIdentity` plus `merge-artifact:{id}` internally. `registered_merge`, shared harness `merge_declaration`, harness merge `many_to_one_mapping_declaration`, adapter merge typed-certification `merge_declaration`, and retained causal-envelope `registered_causal_merge` now require `MergeHistoryDeclarationIdentity` at the call boundary and derive authority artifact basis text only from the typed identity. Updated direct merge, harness certification, mixed-family certification, adapter typed certification, and causal retained mapping callers. Verified with `cargo fmt -p worth-runtime-bridge`, `cargo test -p worth-runtime-bridge merge -- --nocapture` (50 passed), `cargo test -p worth-runtime-bridge causal_envelope_maps_retained_source_structural_stream_continuity_and_merge_records -- --nocapture` (1 passed), `cargo check -p worth-runtime-bridge --tests`, residue scans for raw merge helper signatures/string calls, and touched-file line-cap checks.
- worth-runtime-bridge Phase 1 structured efficiency plan: collapsed the active Phase 1 Finish Packet in `_docs/worth-runtime-bridge/aspect_native_refactor.md` into a single mode cockpit: `P1-closed`, `P1-confidence`, `P1-concern`, and `P1-row`. The plan now has one confidence gate sequence (`P1-G0` through `P1-G6`), one concern packet, one row packet, one row execution order, and explicit blocker thresholds. It reaffirms there is no cleanup mode: source edits require executable old authority, public/test shim, import-wall, or touched-file line-cap proof. Verified with targeted section inspection and `git diff --check` on the spec; no cargo tests were run because this was planning-only.
- worth-runtime-bridge Phase 1 finish efficiency update: added a compact `Phase 1 Efficient Finish Board` to `_docs/worth-runtime-bridge/aspect_native_refactor.md`. The board turns the remaining Phase 1 question into one bounded closeout sprint: status lock, one proof scan, one blocker-row decision, one vertical annihilation row only if real executable old authority appears, earned structure only for authority/import-wall/line-cap reasons, then proof and lockback. It explicitly forbids broad scan loops, cleanup-only splitting, rename-only work, compatibility/test shims, second rows, and cargo tests for planning-only edits.
- worth-runtime-bridge Phase 8 adapter typed-certification fixture boundary batch: hard-broke policy, speculation, and stream adapter typed-certification fixture helpers away from raw commit/patch/snapshot identity text. The local committed-patch helpers now require `TruthCommitIdentity`, `TruthPatchIdentity`, and `TruthSnapshotIdentity`; the policy helper also requires foundational `FieldKey`; snapshot helpers require `TruthSnapshotIdentity`. QA found no meaningful blocker in the touched adapter files. Verified with `cargo fmt -p worth-runtime-bridge`, `cargo test -p worth-runtime-bridge policy_certification -- --nocapture`, `cargo test -p worth-runtime-bridge speculation -- --nocapture`, `cargo test -p worth-runtime-bridge stream -- --nocapture`, `cargo check -p worth-runtime-bridge --tests`, adapter residue scans, and touched-file line-cap checks.

- worth-runtime-bridge Phase 1 efficiency planning: replaced the loose "concrete remaining work" section of the active Phase 1 Finish Packet with a structured closeout sprint table. The new plan has exact steps for tuple freeze, one no-shim authority scan, one JSON/import-wall scan, one public/current-test constructor wall, one blocker-row decision, one vertical annihilation row if needed, earned topology only, and lockback. It also defines the blocker threshold so Phase 1 only reopens for executable production/public/harness/current-test authority from JSON, bytes, labels, arbitrary digest strings, or string collections, not for cleanup-only splitting or presentation/canonical-mechanics noise. No cargo tests were run because this was docs/planning only.

- worth-runtime-bridge Phase 8 shared/pricing snapshot fixture identity closeout: completed the partial shared snapshot helper hard break by making `snapshot` and `field_slice_snapshot` require `TruthSnapshotIdentity`, then extended the same rule to pricing-shock `pricing_snapshot` and `pricing_aspect_snapshot` helpers so they no longer mint snapshot identity from raw text internally. Direct harness and pricing callers now construct `TruthSnapshotIdentity` before fixture admission. The row-touched over-cap test files were split only where earned by the typed helper rewrite: history assertions, diagnostics continuity source support, and delivery sink-rejection slice diagnostics moved into named test modules. QA found and fixed a broad wildcard import in the new delivery diagnostics module plus an unused import warning in `delivery/mod.rs`. Verified with `cargo fmt -p worth-runtime-bridge`, `cargo test -p worth-runtime-bridge delivery -- --nocapture`, `cargo test -p worth-runtime-bridge history -- --nocapture`, `cargo test -p worth-runtime-bridge diagnostics -- --nocapture`, `cargo test -p worth-runtime-bridge pricing_shock -- --nocapture`, `cargo check -p worth-runtime-bridge --tests`, raw snapshot-helper residue scans, line-cap scans, and scoped diff hygiene.

- worth-runtime-bridge Phase 1 efficiency planning: replaced the active Phase 1 finish-board text in `_docs/worth-runtime-bridge/aspect_native_refactor.md` with a compact `Phase 1 Closeout Sprint Packet`. Phase 1 is now explicitly one of four modes only: closed, confidence, concern, or row. The plan permits exactly one bounded proof sprint and then either stops or opens one complete executable-old-authority blocker row. Source edits require that row packet first; structure is allowed only when the row hides native authority, mixes terminal I/O with authority, or touches a file over the 400-line cap. Verified with section inspection and `git diff --check`; no cargo tests were run because this was docs-only planning.

- worth-runtime-bridge Phase 8 pricing-domain snapshot export identity closeout: hard-broke `PricingDomainWorld::snapshot_fixture` and `snapshot_fixture_with_overrides` away from raw snapshot identity text. The helpers now require `TruthSnapshotIdentity`, and pricing-domain plus pricing-shock scenario callers construct native snapshot identities before fixture admission. QA found no need for topology split because touched files stayed under the 400-line cap and the row only changed the identity boundary. Verified with `cargo fmt -p worth-runtime-bridge`, `cargo test -p worth-runtime-bridge pricing_domain -- --nocapture`, `cargo test -p worth-runtime-bridge pricing_shock -- --nocapture`, `cargo check -p worth-runtime-bridge --tests`, fixed-string raw-call/signature residue scans, line-cap scan, and scoped `git diff --check`.

- worth-runtime-bridge Phase 8 pricing-shock patch-envelope fixture identity closeout: hard-broke `pricing_patch` and `pricing_patch_items` away from branch/commit/patch/snapshot `&str` inputs. The helpers now require `BridgeCommittedPatchEnvelopeIdentity`, with callers constructing that identity from `TruthBranchIdentity`, `TruthCommitIdentity`, `TruthPatchIdentity`, and `TruthSnapshotIdentity` before committed-patch fixture admission. Added a narrow `pricing_patch_envelope_identity` helper that accepts only native identity carriers and lives beside the patch helper. QA found no remaining structural issue and no earned topology split because touched pricing-shock files remain under the 400-line cap. Verified with `cargo fmt -p worth-runtime-bridge`, `cargo test -p worth-runtime-bridge pricing_shock -- --nocapture`, `cargo check -p worth-runtime-bridge --tests`, raw-call/signature residue scans, line-cap scan, and scoped `git diff --check`.

- worth-runtime-bridge Phase 8 continuity authority fixture identity closeout: hard-broke certification and parity continuity authority fixtures away from branch/snapshot `&str` inputs. `continuity_authority`, `continuity_authority_with_successor`, `ambiguous_continuity_authority`, and the parity-local continuity fixture now require `TruthBranchIdentity` and `TruthSnapshotIdentity` before building `BridgeContinuityAuthorityBasis`. Updated certification and parity callers to construct native identities explicitly. QA found the touched `parity.rs` crossed the 400-line cap after the explicit callsite rewrite, so the repeated foundational `FieldKey` construction was extracted to a narrow `name_field_key` helper and the file returned to 397 lines without a topology split. Verified with `cargo fmt -p worth-runtime-bridge`, `cargo test -p worth-runtime-bridge continuity -- --nocapture`, `cargo test -p worth-runtime-bridge parity -- --nocapture`, `cargo check -p worth-runtime-bridge --tests`, raw-call/signature residue scans, line-cap scan, and scoped `git diff --check` (only existing LF/CRLF warning on `parity.rs`).

- worth-runtime-bridge Phase 1 efficiency planning: added a compact `Phase 1 Operator Card` to `_docs/worth-runtime-bridge/aspect_native_refactor.md`. It records the current executable Phase 1 backlog as none, strict reopen row as none, and live concern as none, then restricts future Phase 1-shaped work to exactly three paths: confidence proof, named concern, or blocker row. It includes a short blocker-row packet and lockback rules so future turns do not start with broad scans or cleanup-only splits. No cargo tests were run because this was docs/planning only.

- worth-runtime-bridge Phase 8 pricing-shock provenance snapshot identity closeout: hard-broke `snapshot_with_identity` away from raw snapshot identity text. The helper requires `TruthSnapshotIdentity`, and conflicting snapshot identity callers construct the native identity before rewriting fixture/read-result identity. QA found no remaining architectural blocker and no earned topology split because touched files stay under the line cap. Verified with `cargo fmt -p worth-runtime-bridge`, `cargo test -p worth-runtime-bridge pricing_shock -- --nocapture` (41 passed), `cargo check -p worth-runtime-bridge --tests`, row residue scans, touched-file line-cap checks, and scoped `git diff --check`.

- worth-runtime-bridge Phase 8 writeback feedback patch identity closeout: hard-broke `bridge_feedback_patch` away from branch/commit/patch/snapshot `&str` inputs. The helper now requires `BridgeCommittedPatchEnvelopeIdentity`, and feedback-loop certification constructs that identity from typed `TruthCommitIdentity`, `TruthPatchIdentity`, `TruthSnapshotIdentity`, and `TruthBranchIdentity` before feedback metadata is attached. Also removed the adjacent snapshot/branch `as_str()` round-trip in the ordinary comparison commit. QA found no remaining architecture or topology issue. Verified with `cargo fmt -p worth-runtime-bridge`, `cargo test -p worth-runtime-bridge writeback -- --nocapture` (86 passed, expected caught-panic output), `cargo check -p worth-runtime-bridge --tests`, row residue scans, touched-file line-cap checks, and scoped `git diff --check`.

- worth-runtime-bridge Phase 8 adapter speculation shared identity closeout: hard-broke `preview_declaration` and `authoritative_routing_digest` in `harness/adapter/adapter_impl/speculation/shared.rs` away from raw preview declaration, binding, truth-branch, signal-branch, snapshot, and commit identity text. Discard, promotion, and churn certification callers now construct typed `BridgePreviewSessionDeclarationIdentity`, `BridgeSpeculativeBranchBindingIdentity`, `TruthBranchIdentity`, `BridgeSignalBranchIdentity`, `TruthSnapshotIdentity`, and `TruthCommitIdentity` before entering shared helper code. Also removed the first-commit route digest string round-trip by passing cloned `TruthCommitIdentity` directly. QA found no remaining row issue and no earned topology split. Verified with `cargo fmt -p worth-runtime-bridge`, `cargo test -p worth-runtime-bridge speculation -- --nocapture` (29 passed), `cargo check -p worth-runtime-bridge --tests`, exact row residue scans, touched-file line-cap checks, and scoped `git diff --check` (only LF/CRLF warnings).

- worth-runtime-bridge Phase 1 planning efficiency update: replaced the overlapping active Phase 1 runbook fragments in `_docs/worth-runtime-bridge/aspect_native_refactor.md` with one structured `Phase 1 Finish Execution Plan`. The plan now has a single closed tuple, four operating modes, one bounded confidence sprint (`P1-G0` through `P1-G6`), one concern packet, one blocker threshold, one blocker-row packet, and one row execution order. It explicitly states that Phase 1 has no cleanup mode: no broad scan work queues, no cleanup-only splitting, no test shims, and no source edits without a complete executable-old-authority row. No cargo tests were run because this was docs/planning only; verified with spec inspection and scoped `git diff --check`.

- worth-runtime-bridge Phase 8 facade stream fixture identity closeout: hard-broke `facade/tests/support/native_patch_fixture.rs::canonical_envelope` away from branch/commit/patch/snapshot `&str` inputs. The fixture now requires `TruthBranchIdentity`, `TruthCommitIdentity`, `TruthPatchIdentity`, and `TruthSnapshotIdentity`, and direct facade stream plus causal-retained mapping callers construct those native identities before stream-window committed-patch fixture admission. Also locked back the prior writeback route commit identity row in the Phase 8 evidence: `route_digest_for_commit` and `authority_denial_causality` now require `TruthCommitIdentity`. Verified with `cargo fmt -p worth-runtime-bridge`, `cargo test -p worth-runtime-bridge stream -- --nocapture` (45 passed), `cargo test -p worth-runtime-bridge causal_envelope -- --nocapture` (30 passed), `cargo check -p worth-runtime-bridge --tests`, raw canonical-envelope residue scans, touched line-cap scans, and scoped diff hygiene.

- worth-runtime-bridge Phase 1 finish planning: added a stricter `Finish Protocol` and `Phase 1 Finish Board` to `_docs/worth-runtime-bridge/aspect_native_refactor.md`. The plan now starts every attempted Phase 1 turn from the closed tuple, forbids Phase 1 source edits without either a requested confidence sprint or one complete executable-old-authority blocker row, and makes row execution/lockback explicit so future work does not devolve into broad scans or cleanup-only splitting. No source changes or cargo tests were run because this was planning-only.

- worth-runtime-bridge Phase 1 efficiency refinement: compressed the active Phase 1 finish plan in `_docs/worth-runtime-bridge/aspect_native_refactor.md` into a smaller operator cockpit: `P1-closed`, `P1-confidence`, `P1-concern`, and `P1-row`; one confidence gate sequence; one concern packet; one blocker threshold; one blocker-row template; and explicit forbidden Phase 1 work. The important decision is that Phase 1 is closed by default and has no cleanup lane: no source edits, broad searches, helper moves, naming polish, or file splitting unless a concrete executable-old-authority row opens. Verified with section inspection and `git diff --check`; no cargo tests were run because this was docs/planning only.

- worth-runtime-bridge Phase 8 causal-envelope evidence-reference identity closeout: hard-broke `BridgeCausalEvidenceReference::new` and current worth-query causal materialization/certification helpers away from raw reference identity text. Public reference assembly now requires `BridgeCausalEvidenceReferenceIdentity`; owner-specific constructors bind query/runtime-bridge/relational/signal family before reference assembly, and the generic owner/family constructor remains private. Runtime-bridge causal-envelope/writeback mapping tests plus worth-query causal materialization/certification tests now construct typed reference identities before assembling references. QA found the explicit native lower-runtime-slot certification references pushed `slot_support.rs` over the line cap, so that request assembly moved into `slot_support/lower_runtime_slot_references.rs`. Verified with `cargo fmt -p worth-runtime-bridge -p worth-query`, `cargo check -p worth-runtime-bridge --tests`, `cargo test -p worth-runtime-bridge causal_envelope -- --nocapture`, `cargo test -p worth-runtime-bridge --test phase_boundaries_compile_fail -- --nocapture`, exact raw-helper/call residue scans, line-cap scan, and whitespace scan. `cargo check -p worth-query --tests` still fails only on the pre-existing mutation-evidence batch API drift: missing `request_digest_count` and `receipt_digest_count` on `BridgeBatchMutationAuthorityBundle`.

- worth-runtime-bridge Phase 1 finish planning: rewrote the active Phase 1 Finish Execution Plan in _docs/worth-runtime-bridge/aspect_native_refactor.md into a structured sprint (P1-S0 through P1-S7) so closeout is finite and efficient: tuple lock, authority wall, JSON/import wall, constructor wall, one-row decision, one-row annihilation if needed, proof, and lockback. The plan now explicitly states Phase 1 is closed by default, has no cleanup/broad-search/source-edit lane, and source edits require a complete blocker row packet first. Verified with git diff --check on the spec; no cargo tests run because this was planning-only.

- worth-runtime-bridge Phase 8/query downstream detox: fixed the remaining worth-query mutation-evidence API drift without reintroducing runtime-bridge digest-count shims. WORTHQueryBatchMutationEvidence now exposes uthority_request_count and uthority_receipt_count, consuming BridgeBatchMutationAuthorityBundle::authority_request_count / uthority_receipt_count; old equest_digest_count / eceipt_digest_count vocabulary is gone from the touched query runtime surfaces. The focused query bridge test support now registers an exact native TruthPatchScope::for_entity_field matching the scalar snapshot read contract instead of broad AspectKeySelector::any / TruthPatchTargetSelector::any, so the test fixture respects runtime-bridge mapping validation. Updated Phase 8 evidence in _docs/worth-runtime-bridge/aspect_native_refactor.md. Verified with cargo fmt -p worth-query -p worth-runtime-bridge, cargo check -p worth-query --tests, cargo test -p worth-query workspace_batch_aggregates_touched_surfaces_and_remains_inspectable -- --nocapture, cargo check -p worth-runtime-bridge --tests, scoped residue scans, touched line-cap checks, and scoped git diff --check (only existing LF/CRLF warnings).
- worth-runtime-bridge Phase 1 P1-R2 lockback: closed the facade/current-test authority helper identity constructor row. Writeback declaration/causality helpers, causal-envelope preview helpers, subscription preview helpers, retained writeback-chain helpers, and local mutation-bundle helpers now require typed identity carriers or typed identity packets at the call boundary; effect and idempotence identities are no longer minted from helper string parameters. Split row-touched test topology only where earned by the row and line cap: causal-envelope mapping scale remains in mapping_scale.rs, and mutation count assertions live in authority_session_mutation_counts.rs while authority_session_mutation_evidence.rs is back under cap. Verified with cargo fmt -p worth-runtime-bridge, cargo check -p worth-runtime-bridge --tests, cargo test -p worth-runtime-bridge writeback -- --nocapture, cargo test -p worth-runtime-bridge causal_envelope -- --nocapture, cargo test -p worth-runtime-bridge bridge_harness_subscription_suite_34_preview_zero_residue_and_promotion_are_explicit -- --nocapture, exact row residue scans, row line-cap scan, and scoped git diff --check with only existing LF/CRLF warnings. Phase 1 tuple is locked back to closed; continue later phases without reopening broad Phase 1 scans unless a new executable old-authority row appears.
- worth-runtime-bridge Phase 1 efficiency planning: replaced the active finish cockpit with a structured sprint in `_docs/worth-runtime-bridge/aspect_native_refactor.md`. Phase 1 now finishes through `P1-S0` through `P1-S7`: status lock, one no-shim authority scan, one JSON/import-wall scan, one public/current-test constructor wall, blocker decision, optional one-row annihilation, proof, and lockback. The sprint permits zero source edits before the blocker decision, one blocker row at a time, and zero cargo tests for no-row planning/classification. This was docs/memory only; verified with spec inspection and `git diff --check`.
- worth-runtime-bridge Phase 1 efficiency tightening: replaced the active Phase 1 structured sprint with a stricter `Phase 1 Finish Packet` using `P1-0` through `P1-7`: tuple lock, one authority residue proof, one JSON/import-wall proof, one constructor-wall proof, one decision, one optional row annihilation, row proof, and lockback. The packet explicitly stops when there are zero blockers, forbids broad scans as work queues, forbids cleanup-only splits/rename-only passes, and allows source edits only after a complete executable-old-authority blocker row exists. This was docs/memory only; verified with `git diff --check` on the spec and a reference scan for stale `P1-S*` active-plan names.
- worth-runtime-bridge Phase 6/7 writeback diagnostic explanation typed-retention batch: removed the remaining copied projection-string storage from `diagnostics/writeback` explanation structs. Admission, candidate, outcome, execution, mapper record, mapper envelope, mapped family input, replay bundle, and replay record explanations now retain the native artifact they explain and derive digest/canonical-basis/class/counter accessors from the retained carrier. Direct writeback diagnostics tests now assert retained explanation artifacts match runtime retained records/bundles before checking projections. Focused diagnostics/writeback tests and `cargo check -p worth-runtime-bridge --tests` passed with 10-minute command timeouts; residue scan found no copied effect-intent/string projection fields in `diagnostics/writeback`.
- worth-runtime-bridge Phase 1 efficiency planning: rewrote the active Phase 1 structured finish section in `_docs/worth-runtime-bridge/aspect_native_refactor.md` into a tighter finish sprint: `P1-A` status lock, `P1-B` authority vocabulary gate, `P1-C` JSON/import-wall gate, `P1-D` constructor/test-shim gate, `P1-E` single-row decision, optional `P1-F/P1-G` row rewrite/proof, and `P1-H` lockback. The new plan adds a small work ledger, explicit classification values, hard legal/illegal sprint endings, and keeps source edits illegal until a complete executable blocker row exists. No source code or tests changed; verified the edited spec with section inspection and `git diff --check`.
- worth-runtime-bridge Phase 1 efficiency refinement: updated the active completion runbook status after both closed rows (`P1-R1` diagnostics public identity lookup and `P1-R2` facade/current-test authority helper identity constructors), then added an explicit `P1-0` through `P1-7` Finish Board. The board is the fast path for finishing Phase 1: lock tuple, run each proof gate once, decide `0 blockers` or one complete blocker row, rewrite/prove only that row if present, and lock back. It forbids cleanup-only source edits, repeated broad search loops, cargo tests for planning-only classification, and multiple-row backlogs.
- worth-runtime-bridge Phase 1 no-row closeout proof: executed the active `P1-0` through `P1-4` finish board. Authority vocabulary hits classify as canonical/foundational digest mechanics, JSON hits are terminal report/export/capture only, the native authority import-wall scan is clean, constructor/test-shim hits classify as typed snapshot value fixtures, expected canonical-basis helpers, terminal policy labels, static private writeback certification digest domains, sealed UI raw-identity rejection proofs, or digest-prefix assertions. Decision: `0 Phase 1 blockers`; tuple locked back to `mode=P1-closed`, queue empty, concern none, row none. One non-Phase1 structural residual remains: `facade/tests/causal_envelope/retained_mapping.rs` is 401 lines and should be fixed only in a coupled later aspect/topology batch or dedicated line-cap pass, not as Phase 1 legacy work.
- worth-runtime-bridge Phase 1 efficiency planning: added a compact `Phase 1 Closeout Cockpit` to `_docs/worth-runtime-bridge/aspect_native_refactor.md`. The cockpit makes the current state explicit (`P1-closed`, empty reopen queue, no open blocker row), gives a fast path for future "what is left?" requests, limits confidence work to three bounded gates, and requires a single complete executable-old-authority blocker row before any Phase 1 source edit. It also records the known current non-blockers: terminal JSON/export topology, non-authority `&str` helper hits, and the 401-line causal-envelope retained-mapping test as structural hygiene rather than Phase 1 legacy work. No source code or cargo tests were changed because this was planning-only.
- worth-runtime-bridge Phase 7 feedback-loop certification typed-retention closeout: fixed the final proof-step split visibility issue by making origin/publication/replay/restart proof packets visible only to the feedback-loop certification orchestrator and re-exporting only orchestration functions. The feedback-loop matrix now retains typed causality, feedback provenance/context, route identity, and truth-commit identity carriers; terminal route/causality/provenance/commit digest strings are derived projections only. Updated Phase 7 closeout evidence in `_docs/worth-runtime-bridge/aspect_native_refactor.md`. Verified with `cargo fmt -p worth-runtime-bridge`, `cargo test -p worth-runtime-bridge writeback_certification -- --nocapture` (8 passed), `cargo test -p worth-runtime-bridge writeback -- --nocapture` (86 passed, expected caught-panic output), `cargo check -p worth-runtime-bridge --tests`, feedback-loop cached-string residue scans, touched-file line-cap scan, proof-step visibility scan, and scoped `git diff --check` (only existing LF/CRLF warning on `writeback.rs`).
- worth-runtime-bridge Phase 1 efficiency planning: collapsed the overlapping active Phase 1 finish sprint/cockpit/board language in `_docs/worth-runtime-bridge/aspect_native_refactor.md` into one `Phase 1 Structured Finish Packet`. The active plan now has a single operator flow (`P1-0` through `P1-7`), one work ledger, one blocker threshold, one concern packet, one blocker-row packet, and one stop condition: stop at `0 blockers` or annihilate exactly one executable old-authority row end-to-end and lock back. No source code or cargo tests changed because this was planning-only.
- worth-runtime-bridge Phase 8 speculation replay-retention closeout: replaced churn certification's replay lifecycle digest retention with typed `BridgePreviewReplayBundle` retention via `SpeculationPreviewReplayBundleSet`, changed branch isolation rows to retain typed preview session, truth branch, execution-record, and discard-record identities, and changed terminal replay digest projection to derive from replay bundles rather than `&str` digest parameters. QA found and fixed the terminal helper signature that still taught raw replay digest pairs. Verified with `cargo fmt -p worth-runtime-bridge`, `cargo test -p worth-runtime-bridge speculation -- --nocapture` (29 passed), `cargo check -p worth-runtime-bridge --tests`, row residue scans, touched-file line-cap scan, and scoped `git diff --check` (only existing LF/CRLF warnings).
- worth-runtime-bridge Phase 1 efficiency planning: tightened the active runbook in `_docs/worth-runtime-bridge/aspect_native_refactor.md` into a single `Phase 1 Efficient Finish Plan`. The plan now says efficiency means bounded proof, not softer standards: prove the closed tuple, run the authority/JSON/constructor walls once, decide `0 blockers` or exactly one executable blocker row, then stop or delete that row end to end. It also makes `P1-0` through `P1-4` read-only, forbids source edits before a complete row packet, orders the proof scans, treats ambiguous hits as the one row instead of another scan loop, and skips cargo for docs-only planning. Verified with scoped `git diff --check` on the spec.
- worth-runtime-bridge Phase 8 facade/harness preview-basis helper detox: hard-broke facade speculation and harness counter preview-basis helpers away from raw suffix text. `preview_session_basis` and `structural_basis` now consume typed test-local packets carrying `TruthBranchIdentity`, `TruthSnapshotIdentity`, `StructuralSchemaIdentity`, and `StructuralIdentityDeclarationIdentity`; current tests assemble those native carriers inline at the scenario boundary instead of using suffix factories or helper mini-constructors. QA found and fixed the first-pass hidden `StructuralBasisInput::drift` / `CounterPreviewSessionBasisInput::*` convenience constructors so tests do not preserve shim-like identity factories. Verified with `cargo fmt -p worth-runtime-bridge`, `cargo test -p worth-runtime-bridge speculation -- --nocapture`, `cargo test -p worth-runtime-bridge counters -- --nocapture`, and `cargo check -p worth-runtime-bridge --tests`.
- worth-runtime-bridge Phase 1 efficiency planning: replaced the noisy active Phase 1 finish runbook in `_docs/worth-runtime-bridge/aspect_native_refactor.md` with one `Phase 1 Finish Cockpit`. The cockpit records the current closed tuple, R1/R2 lockback, and latest `0 Phase 1 blockers` decision; defines a single `P1-0` through `P1-7` completion board; separates read-only proof gates from source-edit row annihilation; keeps the classification ledger and exact gate commands; and forbids backlogs, cleanup-only splits, rename-only passes, aliases, and test shims. No Rust source changed and no cargo tests were run because this was a docs/planning-only turn; scoped spec diff hygiene was clean.
- worth-runtime-bridge Phase 8 field-target expected-basis helper detox: removed the remaining `expected_field_target_basis(&str)` helper shape from runtime-bridge current tests. Harness explanation/routing tests, input envelope construction tests, and routing surface tests now construct foundational `FieldKey` at the scenario boundary, use the same typed key for committed-patch target construction, and pass that `FieldKey` into expected canonical target-basis assertions. QA found and fixed adjacent copies of the helper outside the initial harness radius. Verified with `cargo fmt -p worth-runtime-bridge`, focused construction/fine-grained/explanations/input-envelope/routing-surface tests, `cargo check -p worth-runtime-bridge --tests`, exact raw expected-basis residue scan, touched-file line-cap scan, and scoped diff hygiene with only existing LF/CRLF warnings.

- worth-runtime-bridge Phase 8 signal-scope and writeback denial helper detox: hard-broke the remaining facade/harness helper seams that still minted native invalidation/digest carriers from caller text. Bulk-planning identity tests, fine-grained routing tests, and pricing-shock runtime support now require `SignalInvalidationScope` at the mapping-registration helper boundary instead of accepting raw signal-scope strings. In harness adapter writeback certification, authority-denial failure digest derivation now uses a closed private `WritebackHarnessErrorDigestDomain` enum instead of caller-supplied domain strings. Verified with `cargo fmt -p worth-runtime-bridge`, `cargo test -p worth-runtime-bridge fine_grained_precision -- --nocapture`, `cargo test -p worth-runtime-bridge bridge_bulk_planning -- --nocapture`, `cargo test -p worth-runtime-bridge pricing_shock -- --nocapture`, `cargo check -p worth-runtime-bridge --tests`, touched-file line-cap scan, and helper residue checks.
- worth-runtime-bridge Phase 8 causal-envelope retained-reference helper detox: hard-broke the retained-mapping current-test family away from rebuilding runtime-bridge evidence references at each callsite with `BridgeCausalEvidenceFamily` plus `record_identity().as_str()`. `retained_mapping_support.rs` now exposes typed retained-reference helpers for route, bulk-planning, source materialization/failure, structural remap/branch comparison, stream replay/checkpoint, continuity, merge, and historical failure carriers; only the sealed support helper keeps the `runtime_bridge(family, identity)` assembly point, and only missing-record denial rows still spell family plus synthetic identity explicitly. QA found the support file crossed the 400-line cap after the helper expansion, so digest-basis mirror logic moved into `retained_mapping_digest_support.rs`. Verified with `cargo fmt -p worth-runtime-bridge`, `cargo check -p worth-runtime-bridge --tests`, `cargo test -p worth-runtime-bridge retained_mapping -- --nocapture`, retained-mapping residue scan, touched-file line-cap scan, and scoped `git diff --check` (only existing LF/CRLF warnings).
- worth-runtime-bridge Phase 8 causal-envelope preview/writeback retained-reference closeout: extended the same hard break through the remaining preview and writeback mapping suites plus the shared causal-envelope request tests. `mapping_support.rs`, `causal_envelope_mapping_support.rs`, and `causal_envelope/mod.rs` now own the sealed `runtime_bridge(family, identity)` assembly points with typed helpers for route, historical evaluation, preview execution/discard/promotion, and writeback admission/mapper-envelope/mapped-input/mapper/execution/replay artifacts. `mapping.rs`, `mapping_scale.rs`, `admission_summary.rs`, `external_authority.rs`, `receipt.rs`, `causal_envelope_mapping.rs`, and `causal_envelope_mapping_scale.rs` no longer rebuild retained bridge references directly from family enums plus identity strings at ordinary callsites. QA found and fixed one real issue: the writeback scale harness changed to typed retained artifacts but initially missed the necessary writeback record imports. The writeback scale chain now retains native admission, mapper-envelope, mapped-input, mapper-record, execution, and replay artifacts instead of six raw identity strings. Verified with `cargo fmt -p worth-runtime-bridge`, `cargo check -p worth-runtime-bridge --tests`, `cargo test -p worth-runtime-bridge causal_envelope -- --nocapture`, `cargo test -p worth-runtime-bridge writeback -- --nocapture`, residue scans for direct `BridgeCausalEvidenceReferenceIdentity::runtime_bridge(...)` calls, touched-file line-cap scan, and scoped `git diff --check` with only existing LF/CRLF warnings. Remaining direct calls in the touched area are the sealed helpers plus intentional synthetic denial/owner-mismatch rows.
- worth-runtime-bridge Phase 8 merge/structural certification bundle hard-break: removed the remaining string-retained declaration/contract/record identity authority from merge and structural harness certification bundles. Merge certification now stores native merge declaration, contract, and record identities in `merge/certification_bundle.rs`; structural summary, identity-separation, and diff reports now store native structural declaration, contract, and branch-comparison record identities in `structural/certification_bundle.rs`. Terminal report JSON projection is now the only place this seam calls `.as_str()` for those identities. Deliberately left structural retained candidate identities alone because production structural reduction still exposes them as `Arc<str>` retained candidates; wrapping them locally would be a fake shim. Verified with `cargo fmt -p worth-runtime-bridge`, `cargo check -p worth-runtime-bridge --tests`, `cargo test -p worth-runtime-bridge merge -- --nocapture`, `cargo test -p worth-runtime-bridge structural -- --nocapture`, touched-file line-cap scan, and scoped `git diff --check`.
- worth-runtime-bridge Phase 8 policy certification matrix hard-break: removed policy declaration/lowered-policy/truth-view-resolution string retention from the harness adapter matrix layer. `policy/matrices.rs` now stores native `BridgePolicyDeclarationIdentity`, native lowered-policy identity carriers, and full `BridgeTruthViewPolicyResolution` results instead of flattening them to strings before retention; `shared_artifacts.rs` and `certification_execution.rs` pass native carriers through; `terminal_report_export/json_projection.rs` is now the only place that renders policy identities or `"Admitted"` / `"Rejected"` labels. Tightened typed certification tests so admitted and rejection rows prove exact declaration identities and route rows prove real lowered-policy identity retention. Verified with `cargo fmt -p worth-runtime-bridge`, `cargo check -p worth-runtime-bridge --tests`, `cargo test -p worth-runtime-bridge policy -- --nocapture`, touched-file line-cap scan, and scoped `git diff --check`.
- worth-runtime-bridge Phase 8 pricing workload identity-retention hard-break: removed real runtime identity string retention from pricing support and pricing-shock harness evidence. `pricing_support.rs`, `pricing_domain/attribution.rs`, `pricing_shock/support/{capture_reference,capture_lifecycle,capture_failures,scenario,writeback_merge_capture}.rs`, and the coupled pricing showcase/report tests now retain native `Truth{Branch,Commit,Snapshot}Identity`, `BridgeRouteIdentity`, `BridgeInvalidationIdentity`, `BridgeTruthViewSelectorIdentity`, `BridgeHistoricalEvaluationRecordIdentity`, `BridgeAspectRegistrationId`, and `BridgePreviewSessionIdentity` carriers until certification/showcase JSON or markdown projection. QA surfaced the remaining residue in pricing terminal/report export surfaces and diagnostics-entrypoint evidence; fixed those edges so `.as_str()` happens only at presentation/report boundaries. Deliberately left simulation-only branch labels as strings because `simulation_capture.rs` synthesizes scenario labels rather than retaining runtime authority. Verified with `cargo fmt -p worth-runtime-bridge`, `cargo check -p worth-runtime-bridge --tests`, `cargo test -p worth-runtime-bridge pricing_shock -- --nocapture`, and `git diff --check` (only existing LF/CRLF warnings outside the touched row).
- worth-runtime-bridge Phase 8 pricing merge/provenance retained-authority closeout: removed the remaining real runtime authority string retention from `PricingMergeBundle` and `PricingHistoricalProvenanceBundle`. `pricing_support.rs`, `pricing_shock/support/{writeback_merge_capture,historical_portfolio}.rs`, pricing certification evidence, pricing support terminal export, pricing showcase lineage/markdown/JSON/ML export helpers, and the coupled pricing-shock proof suites now retain native `TruthCommitIdentity`, `TruthSnapshotIdentity`, and `BridgeAspectRegistrationId` carriers through the merge and historical-provenance rows. Terminal/report/showcase seams are now the only places that project those carriers to strings. QA found one real issue in the first pass: `pricing_support/certification_evidence.rs` compared against the new typed shock commit without importing `TruthCommitIdentity`; fixed that and reran proof. Verified with `cargo fmt -p worth-runtime-bridge`, `cargo check -p worth-runtime-bridge --tests`, `cargo test -p worth-runtime-bridge pricing_shock -- --nocapture`, touched-file line-cap checks (all touched files <= 395 lines), and `git diff --check` (only existing LF/CRLF warnings elsewhere in the dirty worktree). Next honest Phase 8 seam is the remaining pricing authority rows that still retain real runtime meaning as text, especially any historical/lineage/export bundles still depending on synthetic or partially projected commit/snapshot nodes outside terminal-only surfaces.
- worth-runtime-bridge Phase 8 pricing classification-status hard-break: removed the remaining early `String` flattening for real runtime classifications in pricing workload support. `PricingAspectBundle` now retains native `TruthDeltaSurfaceKind`, `FineGrainedMatchStatus`, and `SubscriptionSliceKind`; `PricingWritebackBundle` now retains native `BridgeWritebackFamilyKind` and `BridgeWritebackStrategyClass`; `PricingMergeBundle` now retains native `BridgeMergeConsumptionClass`, `BridgeMergeRoutingOutcomeClass`, optional `BridgeMergePrecedenceStage`, optional `BridgeMergeDenialClass`, and native merged-route `FineGrainedMatchStatus`; `PricingTrustAttackBundle` now retains native `BridgePolicyRejectionKind`, `BridgePolicyFieldKind`, `BridgeRouteErrorKind`, `BridgeMergePrecedenceStage`, and `BridgeMergeDenialClass`. Updated `pricing_shock/support/{capture_reference,writeback_merge_capture,capture_failures}.rs`, pricing certification digest-basis/evidence helpers, suite/certification/showcase/ML JSON projection, lineage edges, and the coupled pricing-shock proof suites so debug-string rendering happens only at terminal presentation boundaries. QA found one real issue: `capture_failures.rs` needed explicit imports for the newly returned public enum carriers after the hard break. Verified with `cargo fmt -p worth-runtime-bridge`, `cargo check -p worth-runtime-bridge --tests`, `cargo test -p worth-runtime-bridge pricing_shock -- --nocapture`, touched-file line-cap checks (largest touched file 398 lines), and `git diff --check` with only the existing worktree-wide LF/CRLF warnings.
- worth-runtime-bridge Phase 8 writeback-certification classification hard-break: removed retained string classification storage from feedback-loop, authority-denial, and mapper-parity certification rows. Feedback replay reports and boundedness proofs now retain native `BridgeWritebackStrategyClass`, `BridgeWritebackRetryDisposition`, `BridgeWritebackOutcomeClass`, and `BridgeWritebackErrorKind`; authority-denial rows retain native `BridgeWritebackErrorKind` plus a closed `AuthorityDenialBoundaryClass`; mapper-parity shadow-protocol rejection retains native `BridgeWritebackErrorKind`. Export/report JSON seams are now the only places that stringify those carriers. QA also split interleaved-truth projection accessors into `feedback_loop/terminal_projection_access/truth_interleaving_projection_access.rs` so all touched files stayed under the line cap. Verified with `cargo fmt -p worth-runtime-bridge`, `cargo check -p worth-runtime-bridge --tests`, `cargo test -p worth-runtime-bridge writeback -- --nocapture`, touched-file line-cap checks, and `git diff --check` (only existing LF/CRLF warnings).
- worth-runtime-bridge Phase 8 duplicate-authority causality hard-break: removed stored truth-trigger/route/causality strings from the duplicate-authority certification matrix. The matrix now retains native `BridgeWritebackCausalityBasis` and derives `causality_digest`, `truth_trigger_digest`, and `route_digest` from that carrier; the duplicate certification executor also now keeps typed `TruthCommitIdentity`, derives the typed route identity first, and builds causality from those native carriers instead of flattening the first patch identity to a `String`. Updated duplicate typed-certification tests to prove the projected digests come from the retained causality basis. Verified with `cargo fmt -p worth-runtime-bridge`, `cargo check -p worth-runtime-bridge --tests`, `cargo test -p worth-runtime-bridge writeback -- --nocapture`, touched-file line-cap checks (`duplicate_authority/mod.rs` 231, `writeback/duplicate_certification.rs` 235, `typed_certification_tests/duplicate_authority.rs` 210), residue scans, and `git diff --check` (only existing LF/CRLF warnings).
- worth-runtime-bridge Phase 8 replay-loop disposition hard-break: removed `BridgeWritebackLoopDisposition` stringification from non-export certification accessors. `ReplayLoopFeedbackIsolation` and `FamilyExtensionLoopIsolation` now return native `BridgeWritebackLoopDisposition`, while replay-loop and family-extension terminal JSON projection is the only place that formats that disposition to text. Updated typed replay-loop and family-extension certification tests to assert the native enum directly. Verified with `cargo fmt -p worth-runtime-bridge`, `cargo check -p worth-runtime-bridge --tests`, `cargo test -p worth-runtime-bridge writeback -- --nocapture`, touched-file line-cap checks (`replay_loop_isolation/feedback_isolation.rs` 54, `family_extension/replay_isolation/loop_isolation.rs` 54, `typed_certification_tests/replay_loop.rs` 140, `typed_certification_tests/family_extension.rs` 187), and `git diff --check` (only existing LF/CRLF warnings).
- worth-runtime-bridge Phase 8 replay/family-extension isolation digest-lane hard-break: removed internal failure/decision-trace digest synthesis from replay-loop and family-extension replay-isolation certification rows. `cross_family_replay.rs`, `same_family_equivalence.rs`, and `changed_causality.rs` under both `replay_loop_isolation` and `family_extension/replay_isolation` now retain native error, replay-record, projected-bundle, changed-bundle, rebuilt-bundle, and execution-record carriers only; terminal JSON projection in `terminal_report_export/{replay_loop_isolation_json_projection,family_extension_json_projection}.rs` is now the only seam that derives failure and decision-trace digests from those native carriers. The proof pass caught one honest residue in replay-loop certification-evidence export still calling the deleted `failure_digest()` method; fixed that export seam and reran proof. Verified with `cargo fmt -p worth-runtime-bridge`, `cargo check -p worth-runtime-bridge --tests`, `cargo test -p worth-runtime-bridge writeback -- --nocapture`, touched-file line-cap checks (`replay_loop_isolation/cross_family_replay.rs` 65, `same_family_equivalence.rs` 80, `changed_causality.rs` 69, `family_extension/replay_isolation/cross_family_replay.rs` 65, `same_family_equivalence.rs` 80, `changed_causality.rs` 69, `terminal_report_export/replay_loop_isolation_json_projection.rs` 177, `family_extension_json_projection.rs` 210), scoped row diff review, and `git diff --check` (only existing worktree-wide LF/CRLF warnings).
- worth-runtime-bridge Phase 8 family-extension mapper/shadow digest hard-break: removed retained admission-record digest strings and internal mapper/shadow failure/decision-trace digest synthesis from the family-extension certification row. `WritebackFamilyExtensionMatrixEvidence` now carries native `BridgeWritebackFamilyAdmissionRecord` values into `writeback_certification/family_extension/{mod.rs,mapper_and_shadow.rs}`, family evidence retains the native admission record instead of a copied digest string, and mapper/shadow proof rows now retain native admission records, execution records, and `BridgeWritebackError` only. `terminal_report_export/family_extension_json_projection.rs` is now the only seam that derives mapper-parity decision traces plus shadow-protocol failure/decision digests. The proof pass caught one honest residue in top-level family-extension certification-evidence export still calling the deleted `failure_digest()` method; fixed that export seam, removed one dead accessor that warning-clean `cargo check` exposed, and reran proof. Verified with `cargo fmt -p worth-runtime-bridge`, `cargo check -p worth-runtime-bridge --tests`, `cargo test -p worth-runtime-bridge writeback -- --nocapture`, touched-file line-cap checks (`writeback_certification/family_extension/mod.rs` 279, `mapper_and_shadow.rs` 156, `writeback/family_certification/extension_execution.rs` 396, `terminal_report_export/family_extension_json_projection.rs` 253), and `git diff --check` (only existing worktree-wide LF/CRLF warnings).
