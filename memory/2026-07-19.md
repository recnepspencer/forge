# 2026-07-19

- Implemented WORTH Query 9.14 Phases 1-6 on `codex/worth-query-9-14`.
- The slice covers portable installed operation meaning, exact graph providers and commit authority, atomic operating-world binding, Query-owned consumer support admission, move-only execution/publication/consumption/settlement, and installed workflow DAG execution with indexed progression and Query-owned traces.
- Hostile QA corrections included stable installed read plans, capability-scoped executor workspaces, exact primary-read evidence, runtime-affine commit witnesses, basis-safe publication, compiler-visible publishing-only contracts, stage-local read roles, typed denials, exact counters, and workflow settlement.
- Verification: full `cargo test -p worth-query` passed (2,695 unit tests plus all integration/doc suites); the focused installed-world suite passed 54 tests; the installation suite passed 17 tests; boundary and agent-context checks passed; strict Clippy found no issues in code introduced or semantically changed by Phases 1-6.
- Every changed Rust file complies with the 400-line cap, including the existing explicit allowlist entry for `runtime.rs`; the Bash-only workspace guard could not run in the Windows shell, so its changed-file and allowlist logic was reproduced in PowerShell across all 137 changed Rust files.
- A later skeptical Phase 1-6 audit corrected stale-generation checks throughout direct/workflow progression, preserved projection `Deferred` and `Unavailable` as distinct checked outcomes, replaced new execution-chain panic assumptions with typed internal-authority failures, added the missing workflow settlement facade export, adopted `worth-proof` for graph participation and both progression chains, and added a deterministic descriptive Foundational support projection plus a separate downstream consumer boundary.
- The updated conditional spec is phase-honest: Phases 1-6 explicitly deny conditional binding until Phases 7-11 install correspondence, lowering, and Signal decision authority; Phase 11 owns the retrofit and no operation may silently default to `Always`.
- Final evidence: installed-world suite now has 83 passing tests; full Query, Query installation/host, Foundational, Proof, Signal (1,013 tests), and Runtime Bridge (879 tests) suites are green; boundary-check and agent-context are green. Relational was green earlier, while two later reruns were killed by environment timeouts without failure output. The official workspace line-cap guard remains red on broad pre-existing debt, but all 316 touched non-allowlisted Rust files pass the 400-line cap.
- Updated Worth Store C.4 Phases 1-3 to the revised `worth-proof` and
  `worth-foundational` architecture.
- Added canonical namespace-identity lowering, Foundational trust-boundary
  weakening/readmission, and compiler denials for raw/proposed/runtime/path/
  digest/bridged identity substitution.
- Extended the existing backend capability claim lane to preserve all six
  checked progression categories without creating a parallel witness family or
  collapsing deferred/failed states through the legacy immediate adapter.
- Strengthened short-transfer and canonical mismatch proofs. Default and
  maximal-feature UI lanes, strict Clippy, boundary-check, and agent-context
  checks passed.
- The worktree still contains unrelated pre-existing Worth Query changes; they
  were preserved.
- Completed Worth Store C.4 Phases 4-7 and corrected their spec sequencing:
  final ACL qualification stays in Phase 8, injected fault accounting in Phase
  11, and mutants in Phase 15 rather than being falsely required before their
  substrates exist.
- The filesystem media owner now uses capability-relative, no-follow namespace
  access; root-bound handles; consumed-handle deletion; exact positioned I/O;
  explicit file/namespace/root/parent durability barriers; immutable
  publication summaries; and a live OS-enforced process mutation lease.
- Real evidence includes a hostile path/link corpus, cross-root denial,
  range/EOF/append/listing scenarios, an eight-process contention/death/
  re-admission scenario, and a handle-inheritance scenario. The 134-test
  backend suite, strict Clippy, boundary-check, and agent-context checks passed.
- Closed the remaining C.4 QA gaps without claiming impossible portable
  hostile-process semantics. Namespace rename/delete now carry one owner-bound
  coordinated authority through the durability barrier; lease invalidation is
  atomically ordered and later mutations are inert; naturally inducible Phase
  6 failures are explicit and tested, while sync-fault proof remains assigned
  to the real-backend Phase 11 interposer and Phase 15 observer. A hostile pass
  also removed a recursive `RwLock` acquisition that could deadlock a durable
  publication behind queued invalidation. The 147-test backend suite, strict
  Clippy, boundary-check, and agent-context checks passed.
- Completed Worth Store C.4 Phases 8-12: live root/profile qualification,
  stable store identity, consuming C.3-to-C.4 runtime progression, process OS
  mutation ownership, lifecycle propagation, real-backend fault interposition,
  exact structural counters, Foundational evidence lowering, and mechanical
  raw-writer/public-surface gates.
- Hostile QA found and fixed post-effect authority return, admission barrier
  recovery, cleanup short-circuiting, unclassified confinement/stale/
  unsupported counters, parallel-test global-counter coupling, and oversized
  cold failure enums. Backend tests (164), compile-fail doctests (28), eleven
  real process/filesystem journeys, compiler authority suites, strict default
  and all-feature Clippy, boundary-check, and agent-context checks pass.
- A final semantic trace reopened Phase 8: the caller-declared coordinated
  service-account posture no longer masquerades as probe evidence. It is now
  an explicit versioned deployment contract bound to the live OS lease and
  admitted store identity; capability claims carry the distinct
  `EstablishedByFilesystemAdmission` evidence class, and the qualification
  contract version was bumped so older reports rebind.
- The skeptical C.4 Phase 8-12 correction loop moved the sealed physical
  runtime owner into `worth-store-physical-backend`, leaving `worth-store` as
  the product re-export. The raw media qualifier is now crate-private, fault
  schedules require authority issued from a filesystem admission, namespace
  classification precedes mutation, failures retain exact counters, profile
  bindings include backend build/config identity, and the independent observer
  validates the complete identity grammar. Focused backend, process journey,
  compiler-sealing, runner-gate, boundary, and context checks all pass.
