Resynced the post-`master`/`forge-2` carry with downstream packages and fixed the real aspect-native fallout in `worth-schema` and `worth-topo`.

Key repair:
- `worth-schema` query test now uses `AspectKey` for `grouped_by(...)`.
- CRLF-sensitive closeout/boundary scans in `worth-topo` now normalize line endings before string assertions.
- The real bridge regression was an overloaded authoritative aspect: naming relations were publishing `naming.persistent_name` as an endpoint-backed `EntityRef` while naming entities published the same key as a string field.
- Fixed that by keeping `naming.persistent_name` for the naming entity lane and introducing dedicated naming relation endpoint carriers in schema bootstrap:
  - `naming.source_identity`
  - `naming.target_identity`
- `worth-topo` milestone-two invalidation declarations now target those dedicated naming relation carriers, and the topo bridge snapshot reader projects them from relation source/target identities.
- Blessed the one changed `worth-schema` trybuild stderr snapshot caused by updated compiler import suggestions.

Verification that passed after the fix:
- `cargo test -p worth-schema -- --nocapture`
- `cargo test -p worth-topo -- --nocapture`
- `cargo test -p worth-spatial -- --nocapture`
- `cargo test -p worth-kernel -- --nocapture`
- `cargo check -p forge-runtime-bridge --tests`
- `cargo check -p forge-query --tests`

`git diff --check` stayed clean except for the existing LF/CRLF warnings.

Broad follow-up on the same branch:
- A post-merge audit over `forge-runtime-bridge` / `forge-query` found a reopened public writeback causality seam. `forge-query` had drifted back to minting `BridgeWritebackCausalityBasis` from raw strings, and runtime-bridge had re-exposed the generic causality constructors on the public surface.
- Re-sealed that lane by introducing `BridgeWritebackNativeCausalityInputs` as the typed public packet, routing the admitted writeback request and query writeback-preparation bindings through it, and keeping `BridgeWritebackCausalityEvidence` crate-private again.
- Updated bridge/query production callsites plus the relevant test and harness support so public lowering and mutation-evidence assembly now use the native causality packet instead of raw evidence constructors.
- The only compile-fail drift from that change was the blessed mapper-envelope stderr fixture naming the old causality argument type; updated it.

Verification that passed after the causality reseal:
- `cargo fmt -p forge-runtime-bridge -p forge-query`
- `cargo check -p forge-runtime-bridge --tests`
- `cargo check -p forge-query --tests`
- `cargo test -p forge-runtime-bridge writeback -- --nocapture`
- `cargo test -p forge-runtime-bridge pricing_shock -- --nocapture`
- `cargo test -p forge-runtime-bridge --test phase_boundaries_compile_fail -- --nocapture`
- `cargo test -p forge-query lowering -- --nocapture`
- `cargo test -p forge-query causal_inspection -- --nocapture`
- `cargo test -p worth-schema -- --nocapture`
- `cargo test -p worth-topo -- --nocapture`
- `cargo test -p worth-spatial -- --nocapture`
- `cargo test -p worth-kernel -- --nocapture`

Broad residue scan notes:
- `serde_json` remains confined to terminal report/export paths in `forge-runtime-bridge`.
- The only `as_external_target()` survivor is still the sanctioned terminal projection helper in `crates/forge-runtime-bridge/src/harness/adapter/target_id/terminal_projection.rs`.
- The audit did not surface a new `forge-relational` blocker from this refactor pass.
