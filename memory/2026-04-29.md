## QA - Phase 6 terminal visibility accounting

- Ran a strict `qa-loop` pass against the Phase 6 terminal visibility slice.
- Found a real boundary-honesty bug: timeout, cancellation, revalidation coalescing, and supersession paths were charging `output_continuity_classification_width` and incrementing output-continuity decision telemetry even when a node had no prior output, so no preserve/hide decision had actually occurred.
- Fixed `resource.rs` so terminal visibility classification returns both continuity and whether a real visibility choice was made; no-prior-output paths now report zero terminal classification work and do not increment decision counters.
- Tightened cancellation footprint accounting so propagated dependent cancellations only contribute output-continuity classification width when their lifecycle transitions actually preserve or hide prior output.
- Added hostile regression tests in `resource_runtime.rs` proving:
  - timeout with no prior output reports zero terminal visibility classification width
  - cancellation with no prior output reports zero terminal visibility classification width
  - supersession with no prior output counts only the new pending classification and not a fake supersession visibility decision
- Verification passed:
  - `cargo fmt -p forge-signal`
  - `cargo test -p forge-signal visibility -- --nocapture`
  - `cargo test -p forge-signal resource_policy -- --nocapture`
  - `cargo test -p forge-signal resource_compile_fail_boundaries_hold -- --nocapture`
  - `cargo check -p forge-signal --tests`
  - `cargo test -p forge-signal`

## Implementation - Phase 6 rejection lifecycle and visibility

- Landed the remaining honest Phase 6 gap around rejection visibility by adding a real resource rejection lifecycle path instead of decorative policy-only support.
- Added `rejection.rs` resource artifacts:
  - `RejectedResourceRequest`
  - `DeniedResourceRejection`
  - `ResourceRejectionReason`
  - `ResourceRejectionDenialClass`
- Extended request/lifecycle/runtime vocab with:
  - `ResourceRejectionOrdinal`
  - `ResourceLifecycleTransitionKind::RequestRejected`
  - `ResourceInFlightStatus::Rejected`
  - `CompletionDenialClass::Rejected`
- Added output continuity policy support for `HideAfterRejection`, including built-in registry lowering and terminal visibility classification.
- Added `ResourceBoundaryKind::RejectionAdmission`, `ResourceRejectionReport`, and rejection admission performance envelope support.
- Wired runtime rejection admission through `resource.rs` and `runtime_state.rs`:
  - validates active pending handle ownership
  - retires active timeout wake before rejection
  - records rejection digest / ordinal / lifecycle transition
  - clears active request state and stale denied-completion observation
  - denies late completion after rejection explicitly as `CompletionDenialClass::Rejected`
- Updated telemetry with explicit rejection admission / denial / reason counters and rejected-completion denial counting.
- Patched `facade.rs` so `DeniedResourceRejection` is exported consistently across public surfaces; trybuild caught that omission before the full verification pass.
- Added hostile/runtime coverage for:
  - rejection visibility hiding prior output
  - no-prior-output rejection not charging fake terminal visibility work
  - late success completion denying after rejection
- Added compile-fail privacy fixtures and accepted stderr for:
  - `rejected_resource_request_fields_are_private`
  - `denied_resource_rejection_fields_are_private`
  - `resource_rejection_report_fields_are_private`
- Verification passed:
  - `cargo fmt -p forge-signal`
  - `cargo check -p forge-signal --tests`
  - `cargo test -p forge-signal resource_rejection -- --nocapture`
  - `cargo test -p forge-signal resource_completion_admission_denies_late_success_after_rejection -- --nocapture`
  - `cargo test -p forge-signal visibility -- --nocapture`
  - `cargo test -p forge-signal resource_compile_fail_boundaries_hold -- --nocapture`
  - `cargo test -p forge-signal`

## Implementation - Phase 7A retention decision plans and typed unavailable-history artifacts

- Landed the first real Phase 7 retention slice so resource retention is now a lowered policy decision instead of a decorative declaration knob.
- Added `policy/retention.rs` with sealed:
  - `ResourceRetentionDecisionClass`
  - `ResourceRetentionDecisionPlan`
- Expanded `ResourceRetentionPolicyDeclaration` to cover:
  - `RetainAllTransitions`
  - `TerminalSummariesOnly`
  - `CompactSuperseded`
  - `CompactCancelled`
  - `CompactTimedOut`
  - `RetainOperationalLifecycleSummary` as a compatibility alias lowered to `TerminalSummariesOnly`
- Added `retention.rs` resource artifacts:
  - `ResourceRetainedHistoryAvailability`
  - `ResourceRetainedHistoryAvailabilityClass`
- Threaded lowered retention plans through `descriptor.rs`, `policy_registry.rs`, `mod.rs`, and `facade.rs`, including new built-in registry entries and public exports.
- Reworked runtime retention behavior in `resource.rs` / `runtime_state.rs` so compaction is policy-driven:
  - `RetainAllTransitions` keeps rich retained history
  - summary/targeted compact policies emit typed unavailable-history artifacts instead of silent absence
  - retained-history pruning by explicit limit now produces `PrunedByRetainedHistoryLimit` evidence
- Expanded `ResourceLifecycleRetentionCompactionReport` with explicit unavailable-history and per-class compaction counters instead of one opaque total.
- Updated replay reconstruction digest inputs so retained-history availability evidence participates directly in replay truth.
- Kept late completion denial honest after compaction: typed retained-history-unavailable evidence still preserves canonical denial class when lifecycle truth remains provable.
- Added hostile/runtime coverage for:
  - typed unavailable artifact emission under `TerminalSummariesOnly`
  - targeted lifecycle compaction only reclaiming matching policy classes
  - retained-history limit pruning producing explicit unavailable-history evidence
  - branch restore / replay width accounting including retained-history availability artifacts
- Added compile-fail privacy coverage for:
  - `resource_retention_decision_plan_fields_are_private`
  - `resource_retained_history_availability_fields_are_private`
  - updated `resource_lifecycle_retention_compaction_report_fields_are_private`
- Verification passed:
  - `cargo fmt -p forge-signal`
  - `cargo check -p forge-signal --tests`
  - `cargo test -p forge-signal resource_lifecycle_retention_compaction -- --nocapture`
  - `cargo test -p forge-signal resource_terminal_summaries_only_compaction_emits_typed_unavailable_history_artifact -- --nocapture`
  - `cargo test -p forge-signal resource_targeted_retention_compaction_only_reclaims_matching_lifecycle_policy -- --nocapture`
  - `cargo test -p forge-signal resource_branch_restore_accounts_for_retained_lifecycle_history_width -- --nocapture`
  - `cargo test -p forge-signal resource_policy -- --nocapture`
  - `cargo test -p forge-signal resource_compile_fail_boundaries_hold -- --nocapture`
  - `cargo test -p forge-signal`

## QA - Phase 7A retention policy integrity

- Ran a strict `qa-loop` pass against the Phase 7A retention slice.
- Found a real semantic-contract bug in `ResourceRetainedHistoryAvailability`: the field named `retention_descriptor_id` was carrying the resource descriptor id instead of the retention policy descriptor id. That made the artifact name dishonest and weakened replay/compatibility evidence because the retained-history record could not point back to the actual policy descriptor that caused compaction.
- Fixed `retention.rs` and `resource.rs` so retained-history availability artifacts now store the real `ResourcePolicyDescriptorId` from the lowered retention decision plan.
- Tightened hostile coverage in `resource_runtime.rs` so terminal-summary compaction now asserts the retained-history artifact points at the exact lowered retention descriptor.
- Found a proof gap: the new `CompactTimedOut` built-in existed in lowering and registry code but had no direct runtime certification row proving it only compacts timed-out lifecycle and preserves timed-out late-completion denial.
- Added `resource_timed_out_retention_compaction_only_reclaims_matching_timeout_policy`, proving:
  - `CompactTimedOut` reclaims timed-out lifecycle
  - it does not reclaim cancelled lifecycle under the same descriptor shape
  - it emits `CompactTimedOut` retained-history availability evidence
  - late completion still denies as `CompletionDenialClass::TimedOut`
- Verification passed:
  - `cargo fmt -p forge-signal`
  - `cargo test -p forge-signal resource_terminal_summaries_only_compaction_emits_typed_unavailable_history_artifact -- --nocapture`
  - `cargo test -p forge-signal resource_timed_out_retention_compaction_only_reclaims_matching_timeout_policy -- --nocapture`
  - `cargo test -p forge-signal resource_lifecycle_retention_compaction -- --nocapture`
  - `cargo test -p forge-signal resource_compile_fail_boundaries_hold -- --nocapture`
  - `cargo test -p forge-signal`

## Implementation - Phase 7B diagnostics policy lowering and policy-gated cold reconstruction

- Implemented diagnostics policy as a first-class resource policy family instead of leaving cold reconstruction gated only by caller-supplied expansion budget.
- Added `ResourceDiagnosticsPolicyDeclaration` variants:
  - `RetainedOnly`
  - `BudgetedExpansion { max_replay_reconstruction_width }`
  - `DenyColdExpansion`
  - `Named { name }`
- Added lowered `ResourceDiagnosticsDecisionPlan` / `ResourceDiagnosticsDecisionClass` and threaded diagnostics through:
  - validated declarations
  - frozen descriptor sets
  - lowered policy bundles
  - descriptor lowering
  - compatibility classification
- Added built-in diagnostics policy descriptors:
  - `signal.resource.diagnostics.retained-only`
  - `signal.resource.diagnostics.budgeted-expansion`
  - `signal.resource.diagnostics.deny-cold-expansion`
- `try_resource_diagnostics_summary(...)` now computes an effective descriptor-backed diagnostics posture across participating nodes, increments explicit diagnostics-policy telemetry, and denies cold reconstruction as:
  - `PolicyRetainedOnly`
  - `PolicyColdReconstructionDisabled`
  - `PolicyReplayReconstructionBudgetExceeded`
  before falling back to caller-budget denials.
- `ResourceDiagnosticsExpansionDenial` and `ResourceDiagnosticsSummary` now retain policy decision class + digest so diagnostics expansion evidence explains which descriptor-backed posture admitted or denied reconstruction.
- Tightened hostile/runtime coverage for:
  - retained-only policy denying cold reconstruction even when caller budget allows it
  - descriptor-backed diagnostics budget denying expansion above policy width
  - mixed-node diagnostics policy collapsing to hard denial over permissive nodes
  - diagnostics policy parameter changes mutating frozen descriptor digest
- Updated compile-fail privacy coverage for:
  - `resource_diagnostics_decision_plan_fields_are_private`
  - updated diagnostics summary / denial private-field fixtures
  - updated validated/lowered policy bundle private-field fixtures with diagnostics family
- Honest verification hiccups fixed during the batch:
  - `ResourceDiagnosticsDecisionClass` needed `Deserialize` because diagnostics artifacts serialize/deserialize it
  - effective diagnostics policy helper needed accessor methods rather than direct field access from `runtime_state.rs`
  - older policy compatibility assertions had to be updated from 9 -> 10 family rows after adding diagnostics
- Verification passed:
  - `cargo fmt -p forge-signal`
  - `cargo check -p forge-signal --tests`
  - `cargo test -p forge-signal resource_diagnostics_summary -- --nocapture`
  - `cargo test -p forge-signal resource_diagnostics_policy -- --nocapture`
  - `cargo test -p forge-signal resource_policy -- --nocapture`
  - `cargo test -p forge-signal resource_compile_fail_boundaries_hold -- --nocapture`
  - `cargo test -p forge-signal`

## Implementation - Phase 7C exact-only replay compatibility proof and typed restore denials

- Implemented the first honest replay-compatibility admission lane on top of existing policy classification instead of leaving compatibility as a report-only surface.
- Added sealed replay/restore compatibility artifacts:
  - `ResourcePolicyRestoreCompatibilityProof`
  - `DeniedResourcePolicyRestoreCompatibility`
  - `ResourcePolicyRestoreCompatibilityDenialClass`
- Current admission is intentionally exact-only:
  - exact descriptor match admits proof
  - missing descriptor denies
  - incompatible version denies
  - parameter digest drift denies
  - decision semantics drift denies
  - mixed drift classes collapse to `MultipleIncompatibilities`
- Added runtime API `admit_resource_policy_restore_compatibility(...)` so restore/replay callers can request a proof-bearing decision instead of hand-interpreting `ResourcePolicyCompatibilityReport`.
- Proof and denial artifacts wrap the canonical compatibility report rather than recomputing policy truth, so this lane still consumes deterministic descriptor/frozen digest evidence and does not execute current policy family code on historical records.
- Added hostile/runtime coverage for:
  - exact restore compatibility proof on identical declarations
  - parameter-drift denial with zero diagnostics policy/cold-work counters touched
  - missing historical descriptor denial
  - incompatible-version denial
- Added compile-fail privacy coverage for:
  - `resource_policy_restore_compatibility_proof_fields_are_private`
  - `denied_resource_policy_restore_compatibility_fields_are_private`
  - updated `resource_policy_proof_artifacts_cannot_be_deserialized`
- Verification passed:
  - `cargo check -p forge-signal --tests`
  - `cargo fmt -p forge-signal`
  - `cargo test -p forge-signal resource_policy_restore_compatibility -- --nocapture`
  - `cargo test -p forge-signal resource_policy -- --nocapture`
  - `cargo test -p forge-signal resource_compile_fail_boundaries_hold -- --nocapture`
  - `cargo test -p forge-signal`

## QA - Phase 7C replay compatibility boundary honesty

- Ran a strict `qa-loop` pass against the new replay/restore compatibility lane.
- Found one real production-grade weakness: restore compatibility admission was piggybacking entirely on the generic policy-compatibility counters. That meant the authoritative replay/restore gate had no dedicated measurement boundary of its own, even though the spec explicitly requires replay compatibility decision accounting distinct from generic inspection/classification work.
- Fixed `telemetry.rs` and `runtime/state/resource.rs` so restore admission now records:
  - `resource_replay_compatibility_decision_count`
  - `resource_replay_compatible_count`
  - `resource_replay_incompatible_count`
  - `resource_replay_missing_policy_count`
- Kept the distinction honest: plain `classify_resource_policy_compatibility(...)` still increments only generic compatibility counters, while `admit_resource_policy_restore_compatibility(...)` increments both the underlying comparison counters and the replay-gate counters because it is the actual restore/replay authority boundary.
- Tightened `DeniedResourcePolicyRestoreCompatibility::from_compatibility(...)` so a compatible report can no longer silently fall through to a nonsense denial class; misuse now trips a debug assertion and `unreachable!` instead of inventing semantics.
- Strengthened hostile/runtime coverage in `resource_runtime.rs` so tests now prove:
  - exact-match restore proof increments replay decision + compatible counters
  - parameter-drift restore denial increments replay decision + incompatible counters
  - plain compatibility classification does **not** increment replay decision counters
- Verification passed:
  - `cargo fmt -p forge-signal`
  - `cargo test -p forge-signal resource_policy_restore_compatibility -- --nocapture`
  - `cargo test -p forge-signal resource_policy -- --nocapture`
  - `cargo test -p forge-signal resource_compile_fail_boundaries_hold -- --nocapture`
  - `cargo test -p forge-signal`

## Implementation - Phase 7D retention budgets for denied completion history and retry lineage

- Added `ResourceRetentionCompactionBudget` so lifecycle-history pruning, denied-completion pruning, and retry-lineage pruning can be budgeted independently instead of piggybacking on one retained-history limit.
- Added typed unavailable-history artifacts:
  - `ResourceRetainedDeniedCompletionAvailability`
  - `RetainedResourceRetryLineage`
  - `ResourceRetainedRetryLineageAvailability`
- Runtime retention state now keeps retained retry lineage separately from pruned retry lineage and pruned denied-completion availability, instead of silently dropping those histories.
- Replay/reconstruction summaries now expose:
  - retained retry lineage width
  - denied-completion unavailable count
  - retry-lineage unavailable count
  - retry-lineage digest
- Runtime summary and lifecycle-retention compaction reports now surface retained denied-completion / retry-lineage counts and pruning widths explicitly.
- Retention telemetry now tracks:
  - `resource_retained_denied_completion_count`
  - `resource_retained_retry_lineage_count`
- Tightened one artifact shape while writing hostile tests: `ResourceRetainedRetryLineageAvailability` now preserves `next_attempt` and `scheduled_delay` instead of throwing them away when lineage becomes unavailable.
- Added hostile runtime coverage proving:
  - denied-completion budget pruning emits typed unavailable-history evidence while replay summary distinguishes retained vs unavailable denial history
  - retry-lineage budget pruning emits typed unavailable-history evidence while replay summary distinguishes retained vs unavailable retry lineage
- Added compile-fail privacy fixtures for:
  - `resource_retention_compaction_budget_fields_are_private`
  - `resource_retained_denied_completion_availability_fields_are_private`
  - `retained_resource_retry_lineage_fields_are_private`
  - `resource_retained_retry_lineage_availability_fields_are_private`
- Refreshed existing trybuild stderr fixtures whose struct shapes changed because replay summary / runtime summary / compaction report now carry the new retention fields.
- Verification passed:
  - `cargo fmt -p forge-signal`
  - `cargo check -p forge-signal --tests`
  - `cargo test -p forge-signal resource_lifecycle_retention_compaction -- --nocapture`
  - `cargo test -p forge-signal resource_retention_budget -- --nocapture`
  - `cargo test -p forge-signal resource_compile_fail_boundaries_hold -- --nocapture`
  - `cargo test -p forge-signal`
  - `git diff --check` clean aside from existing LF/CRLF warnings

## Implementation - Phase 7E compatible restore approvals for retention and diagnostics

- Added explicit compatible replay classifications:
  - `CompatibleRetentionNarrowing`
  - `CompatibleDiagnosticsRichnessChange`
- Compatibility family reports now retain whether canonical runtime truth stays preserved and whether retained-history or diagnostics details became unavailable.
- Restore compatibility proofs now expose aggregate widths for:
  - canonical truth preserved
  - retained history unavailable
  - diagnostics details unavailable
- Kept the lane conservative:
  - only retention narrowing from `RetainAllTransitions` to a narrower retention policy admits
  - diagnostics richness changes admit only inside the diagnostics family under `CompatibleVersion`
  - retention widening still denies as `DecisionSemanticsDrift` even if the current descriptor advertises a compatible posture
- Added runtime-builder support for injecting a frozen resource policy registry so restore compatibility can be certified through the real runtime boundary instead of only raw classifier tests.
- Added hostile/runtime coverage proving:
  - compatible retention narrowing admits restore proof and marks rich history unavailable
  - compatible diagnostics richness change admits restore proof and marks diagnostics availability explicitly
  - retention widening still denies even under `CompatibleVersion`
- Verification passed:
  - `cargo check -p forge-signal --tests`
  - `cargo test -p forge-signal resource_policy_restore_compatibility_admits_retention_narrowing_with_unavailable_rich_history -- --nocapture`
  - `cargo test -p forge-signal resource_policy_restore_compatibility_admits_diagnostics_richness_change_with_explicit_availability_posture -- --nocapture`
  - `cargo test -p forge-signal resource_policy_restore_compatibility_denies_retention_widening_even_under_compatible_posture -- --nocapture`
  - `cargo test -p forge-signal resource_policy -- --nocapture`
  - `cargo test -p forge-signal resource_compile_fail_boundaries_hold -- --nocapture`
  - `cargo fmt -p forge-signal`
  - `cargo test -p forge-signal`
- QA pass on Phase 7E found the restore-compatibility proof/report surface was still too compressed: compatible retention and diagnostics drift were exposing only booleans for availability loss, which made `RetainedOnly` vs `DenyColdExpansion` and targeted retention compactors too implicit for replay-honest consumers.
- Fixed by extending `ResourcePolicyCompatibilityFamilyReport` with typed historical/current retention and diagnostics classes, binding those postures into the compatibility digest, and adding hostile tests proving the artifact distinguishes `CompactCancelled` retention from other narrowing postures and `RetainedOnly` diagnostics from `DenyColdExpansion`.
- Verification after the QA correction: `cargo fmt -p forge-signal`, `cargo test -p forge-signal resource_policy_restore_compatibility -- --nocapture`, `cargo test -p forge-signal resource_policy -- --nocapture`, `cargo test -p forge-signal resource_compile_fail_boundaries_hold -- --nocapture`, full `cargo test -p forge-signal` (817 passed, 23 ignored), and `git diff --check` clean aside from existing LF/CRLF warnings.

## Implementation - Phase 7F typed replay availability outcomes

- Added a new replay availability surface so Phase 7 can distinguish:
  - `Retained`
  - `Reconstructed`
  - `Omitted`
  - `Unavailable`
  - `Denied`
- Added `ResourceReplayAvailabilityReport` and `ResourceReplayAvailabilityClass` in `data/resource/replay_availability.rs`.
- Extended `ResourceRuntimeSummary` with explicit unavailable-history counters for:
  - retained lifecycle history
  - retained denied completions
  - retained retry lineage
- Added runtime APIs:
  - `resource_replay_availability(...)`
  - `resource_replay_availability_with_cold_reconstruction_budget(...)`
- Replay availability now composes three honest boundaries instead of blurring them:
  - runtime summary read
  - restore compatibility admission
  - optional diagnostics/cold reconstruction
- Added dedicated replay availability telemetry counters for decisions and per-outcome counts.
- Bound replay availability into its own performance envelope with `ResourceBoundaryKind::ReplayAvailability`.
- Added hostile tests proving:
  - exact-compatible restore with full retained history classifies as `Retained`
  - compatible restore with unavailable retained richness and no budget classifies as `Omitted`
  - compatible restore with unavailable retained richness and cold reconstruction denied by policy classifies as `Unavailable`
  - compatible restore with unavailable retained richness and admitted cold reconstruction classifies as `Reconstructed`
  - restore incompatibility classifies as `Denied` and does not attempt cold reconstruction
- Added compile-fail privacy coverage for `ResourceReplayAvailabilityReport`.
- Honest cleanup/fixes during verification:
  - removed `Deserialize` from the replay availability report because it contains sealed proof artifacts
  - switched replay availability diagnostics digesting to `ResourceDiagnosticsSummary::provenance_digest()`
  - updated the runtime summary trybuild fixture to include the new unavailable-history fields
  - corrected the new replay availability trybuild fixture to target `forge_signal::facade::ResourceReplayAvailabilityReport`
- Verification passed:
  - `cargo fmt -p forge-signal`
  - `cargo check -p forge-signal --tests`
  - `cargo test -p forge-signal resource_replay_availability -- --nocapture`
  - `cargo test -p forge-signal resource_policy_restore_compatibility -- --nocapture`
  - `cargo test -p forge-signal resource_compile_fail_boundaries_hold -- --nocapture`
  - `cargo test -p forge-signal`
  - `git diff --check` clean aside from existing LF/CRLF warnings
- Full crate after this batch: `822 passed, 23 ignored`.

## Implementation - Phase 7G replay policy family and restore-admission gating

- Added a first-class replay policy family so Phase 7 restore admission is no longer "all compatible drifts are always admitted." New declarations now cover:
  - `IdenticalOnly`
  - `CompatibleRetentionNarrowing`
  - `CompatibleDiagnosticsRichnessChange`
  - `CompatibleRetentionNarrowingAndDiagnosticsRichnessChange`
  - `DenyOnUnknownOrMissing`
  - `Named { name }`
- Added `data/resource/policy/replay.rs` with sealed:
  - `ResourceReplayDecisionClass`
  - `ResourceReplayDecisionPlan`
  - semantic-name built-ins for each replay posture
- Threaded replay policy through the full declaration/freeze/lowering path:
  - `ResourceNodeDeclaration::replay_policy`
  - `ValidatedResourcePolicyDeclaration`
  - `FrozenResourcePolicyDescriptorSet`
  - `LoweredResourcePolicyBundle`
  - `LoweredResourceDescriptor`
- Added replay built-in descriptor registrations in the policy registry and exposed the new family through `data/resource/mod.rs` and `facade.rs`.
- Tightened the restore boundary so `admit_resource_policy_restore_compatibility(...)` now:
  - classifies compatibility from canonical descriptor evidence
  - lowers the current replay decision plan from the validated declaration
  - admits only when the compatible family rows are explicitly allowed by that replay decision plan
  - denies otherwise as `ReplayPolicyDisallowsCompatibleDrift`
- Enriched replay restore proof/denial artifacts so both `ResourcePolicyRestoreCompatibilityProof` and `DeniedResourcePolicyRestoreCompatibility` now retain:
  - replay decision class
  - replay decision descriptor id
  - replay decision digest
- Added hostile runtime coverage proving:
  - replay policy can deny retention narrowing even when compatibility classification is otherwise compatible
  - replay policy can deny diagnostics richness change independently
  - replay-availability denial through the replay gate stays zero-cold and does not trigger diagnostics expansion work
- Added compile-fail privacy coverage for `ResourceReplayDecisionPlan` and refreshed the affected trybuild stderr fixtures for:
  - replay restore proof
  - replay restore denial
  - validated policy declarations
  - lowered policy bundles
- Verification passed:
  - `cargo fmt -p forge-signal`
  - `cargo check -p forge-signal --tests`
  - `cargo test -p forge-signal resource_policy_restore_compatibility -- --nocapture`
  - `cargo test -p forge-signal resource_replay_availability -- --nocapture`
  - `cargo test -p forge-signal resource_policy -- --nocapture`
  - `cargo test -p forge-signal resource_compile_fail_boundaries_hold -- --nocapture`
  - `cargo test -p forge-signal`
  - `git diff --check` clean aside from existing LF/CRLF warnings
- Full crate after this batch: `825 passed, 23 ignored`.

## QA - Phase 7G replay-policy restore gate integrity

- Ran a strict `qa-loop` pass against the replay-policy restore-admission slice.
- Found a real proof-integrity weakness: `ResourcePolicyRestoreCompatibilityProof::from_compatibility(...)` only checked whether the compatibility report was globally compatible, not whether the supplied replay decision plan actually admitted every compatible drift family in that report. That meant internal code could manufacture a restore proof for a replay-gated compatible drift by calling the constructor directly with an `IdenticalOnly` replay plan. Fixed `policy/compatibility.rs` so the proof constructor now also enforces replay-plan admission over all family rows before constructing the proof.
- Added hostile runtime coverage in `resource_runtime.rs` proving the proof constructor rejects a retention-narrowing compatibility report when paired with an `IdenticalOnly` replay plan, even though the compatibility report itself is nominally compatible.
- Found a replay-artifact digest honesty issue: `ResourceReplayAvailabilityReport::availability_digest()` was only binding restore compatibility digest and diagnostics provenance, not the replay decision provenance. Two replay-availability reports admitted under different replay policies but the same compatibility report could therefore collapse to the same availability digest, which is wrong for a replay-facing artifact. Fixed `replay_availability.rs` so availability digest now incorporates replay decision digest for both admitted and denied restore paths.
- Added hostile runtime coverage proving two retained replay-availability reports with identical compatibility truth but different admitted replay decision policies now produce different availability digests.
- Reverification after the QA corrections passed:
  - `cargo fmt -p forge-signal`
  - `cargo test -p forge-signal resource_policy_restore_compatibility -- --nocapture`
  - `cargo test -p forge-signal resource_replay_availability -- --nocapture`
  - `cargo test -p forge-signal resource_compile_fail_boundaries_hold -- --nocapture`
  - `cargo test -p forge-signal`
  - `git diff --check` clean aside from existing LF/CRLF warnings
- Full crate after the QA pass: `827 passed, 23 ignored`.

## Phase 7H - Compatible parameter expansion + forensic diagnostics budget

- Implemented the missing diagnostics built-in `ForensicExpansionBudget` and threaded it through:
  - policy declarations
  - built-in registry lowering
  - diagnostics decision plans
  - effective diagnostics policy aggregation
  - diagnostics denial artifacts
  - runtime diagnostics summary admission
- Added a second diagnostics budget dimension:
  - caller and policy can now distinguish replay reconstruction width from forensic reconstruction width
  - new denial classes:
    - `ForensicReconstructionBudgetExceeded`
    - `PolicyForensicReconstructionBudgetExceeded`
- Added replay-policy vocabulary for parameter-expansion-compatible restore:
  - `CompatibleParameterExpansion`
  - `CompatibleParameterExpansionAndRetentionNarrowingAndDiagnosticsRichnessChange`
- Updated the default replay policy declaration to the combined class that includes parameter expansion.
- Implemented `CompatibleParameterExpansion` compatibility classification for the concrete first-ship diagnostics drift:
  - historical: `signal.resource.diagnostics.budgeted-expansion`
  - current: `signal.resource.diagnostics.forensic-expansion-budget`
  - admission only when current replay width matches the historical replay width and the new forensic width defaults to that same value
- Compatibility family artifacts now retain `defaulted_parameter_names`, and compatibility digests bind that list directly so defaulted-parameter-compatible drift cannot collapse into a less explicit report.
- Added hostile runtime coverage proving:
  - descriptor-backed forensic diagnostics budgets deny above-forensic expansion even when replay width would admit
  - caller forensic budgets can deny independently of replay width
  - compatible parameter expansion admits restore proof and names `max_forensic_reconstruction_width` explicitly
  - replay policy can deny parameter expansion independently of diagnostics richness change
- Refreshed trybuild privacy fixtures for:
  - `ResourceDiagnosticsExpansionBudget`
  - `ResourceDiagnosticsExpansionDenial`
  - `ResourceDiagnosticsDecisionPlan`
  after the forensic-width field expanded the private surface.
- Verification passed:
  - `cargo check -p forge-signal --tests`
  - `cargo fmt -p forge-signal`
  - `cargo test -p forge-signal resource_diagnostics -- --nocapture`
  - `cargo test -p forge-signal resource_policy_restore_compatibility -- --nocapture`
  - `cargo test -p forge-signal resource_replay_availability -- --nocapture`
  - `cargo test -p forge-signal resource_policy -- --nocapture`
  - `cargo test -p forge-signal resource_compile_fail_boundaries_hold -- --nocapture`
  - `cargo test -p forge-signal`
- Full crate after this batch: `831 passed, 23 ignored`.

## Phase 7I - Replay access-lane denial for pruned budget history

- Implemented the first real runtime meaning for replay policy `DenyOnUnknownOrMissing`.
- Added typed replay-availability denial posture:
  - `ResourceReplayAvailabilityDenialClass::RestoreCompatibilityDenied`
  - `ResourceReplayAvailabilityDenialClass::BudgetHistoryUnavailable`
- `ResourceReplayAvailabilityReport` now carries optional denial class directly, so replay-facing callers can distinguish:
  - restore compatibility denied before replay
  - replay-compatible restore that still refuses access because retained budget history was pruned
- Runtime replay availability admission now:
  - preserves restore compatibility proof when replay is otherwise compatible
  - denies immediately with `BudgetHistoryUnavailable` when replay policy is `DenyOnUnknownOrMissing` and any retained history / denied completion / retry lineage is unavailable
  - stays zero-cold in that path and does not attempt diagnostics reconstruction
- Added replay telemetry split:
  - `resource_replay_budget_history_unavailable_count`
- Added hostile runtime coverage proving:
  - strict replay policy denies pruned retained denied-completion history before cold reconstruction
  - default replay lane still emits `Omitted` where strict replay lane denies
  - budget-history denial and restore-compatibility denial produce distinct replay-availability digests
- Refreshed compile-fail fixtures for the replay availability report plus a handful of nearby type-mismatch fixtures whose rustc note formatting drifted during the trybuild rerun.
- Verification passed:
  - `cargo check -p forge-signal --tests`
  - `cargo fmt -p forge-signal`
  - `cargo test -p forge-signal resource_replay_availability -- --nocapture`
  - `cargo test -p forge-signal resource_policy_restore_compatibility -- --nocapture`
  - `cargo test -p forge-signal resource_policy -- --nocapture`
  - `cargo test -p forge-signal resource_compile_fail_boundaries_hold -- --nocapture`
  - `cargo test -p forge-signal`
- Full crate after this batch: `834 passed, 23 ignored`.

## QA - test surface adversariality audit

- Ran a strict QA pass focused on whether the tests themselves were proving hostile conditions instead of just artifact shape.
- Found two real test-surface weaknesses in the replay availability lane:
  - strict replay denial for pruned budget history only exercised denied-completion unavailability, leaving lifecycle-history and retry-lineage unavailability under-proven
  - zero-cold denial tests asserted outer diagnostics expansion counts but did not pin the actual cold-reconstruction counter
- Strengthened `crates/forge-signal/src/tests/resource_runtime.rs` to:
  - assert `resource_diagnostics_cold_reconstruction_count == 0` on replay-denial lanes
  - add hostile strict-replay tests for pruned lifecycle-history denial before cold work
  - add hostile strict-replay tests for pruned retry-lineage denial before cold work
- The new tests briefly exposed two mistakes in the tests themselves during the QA pass:
  - one case accidentally drifted the current declaration and hit restore-compatibility denial instead of budget-history denial
  - one case assumed retry-report accessors that do not exist and had to be rewritten to drive the real retry lineage path explicitly
- Final verification passed:
  - `cargo fmt -p forge-signal`
  - `cargo test -p forge-signal resource_replay_availability -- --nocapture`
  - `cargo test -p forge-signal resource_compile_fail_boundaries_hold -- --nocapture`
  - `cargo test -p forge-signal`
- Full crate after the QA hardening pass: `836 passed, 23 ignored`.

- Second test-QA pass focused on observation/reporting lanes rather than replay.
  - Found that two observation tests were still mostly proving report shape:
    - denied-completion observation did not prove that real completion authority remained spendable afterward
    - retry-schedule observation did not prove that the scheduled retry wake and admission authority remained intact after observation materialization
  - Strengthened `crates/forge-signal/src/tests/resource_runtime.rs` so:
    - denied-completion observation now proves a valid completion for the same active handle still admits afterward
    - retry-schedule observation now proves the scheduled retry wake can still promote and admit after observation materialization
  - Verification passed:
    - `cargo fmt -p forge-signal`
    - `cargo test -p forge-signal observation_batch_report -- --nocapture`
    - `cargo test -p forge-signal resource_completion_transaction_rollback_suppresses_observation_and_restores_state -- --nocapture`
    - `cargo test -p forge-signal resource_compile_fail_boundaries_hold -- --nocapture`
    - `cargo test -p forge-signal`
  - Full crate remains `836 passed, 23 ignored`.

## Phase 7J - Optional deeper replay adapter lane

- Implemented the two missing mixed replay-policy adapters:
  - `CompatibleParameterExpansionAndRetentionNarrowing`
  - `CompatibleParameterExpansionAndDiagnosticsRichnessChange`
- Extended replay lowering / built-in registry / decision-class matching so those adapters admit exactly the compatible drift families they claim, no more.
- Added test helpers in `crates/forge-signal/src/tests/resource_runtime.rs` for:
  - parameter+retention replay declarations
  - parameter+diagnostics replay declarations
  - multi-family compatible registry setup
- Added hostile replay-compatibility coverage proving:
  - parameter+retention replay policy admits mixed retention-narrowing + parameter-expansion drift
  - parameter+retention replay policy still denies diagnostics-richness drift
  - parameter+diagnostics replay policy admits parameter expansion
  - parameter+diagnostics replay policy admits diagnostics-richness drift
  - parameter+diagnostics replay policy still denies retention narrowing
- Added replay-availability provenance coverage proving:
  - pair replay adapters remain digest-distinct even when they both admit the same compatible parameter-expansion drift
- Important design note captured during implementation:
  - `CompatibleParameterExpansion` and `CompatibleDiagnosticsRichnessChange` are both diagnostics-family compatibility classes, so they cannot co-occur in one compatibility report
  - the parameter+diagnostics adapter is therefore a **union adapter**, not a simultaneous-two-family adapter
- Verification passed:
  - `cargo fmt -p forge-signal`
  - `cargo check -p forge-signal --tests`
  - `cargo test -p forge-signal resource_policy_restore_compatibility -- --nocapture`
  - `cargo test -p forge-signal resource_replay_availability -- --nocapture`
  - `cargo test -p forge-signal resource_policy -- --nocapture`
  - `cargo test -p forge-signal resource_compile_fail_boundaries_hold -- --nocapture`
  - `cargo test -p forge-signal`
  - `git diff --check` clean aside from the existing LF/CRLF warnings
- Full crate after this batch: `842 passed, 23 ignored`.

## QA - Phase 7 completion pass

- Ran a strict end-of-phase QA pass specifically to decide whether Phase 7 is genuinely done rather than merely “green after the last replay batch.”
- Re-read the Phase 7 replay/retention/diagnostics obligations and checked the implementation against:
  - replay compatibility matrix rows
  - typed availability outcomes
  - zero-cold denial requirements
  - retention/diagnostics/replay separation requirements
  - the optional deeper replay-adapter lane just added
- Focused hostile verification passed with no new meaningful findings:
  - `cargo test -p forge-signal resource_lifecycle_retention_compaction -- --nocapture`
  - `cargo test -p forge-signal resource_diagnostics -- --nocapture`
  - `cargo test -p forge-signal resource_policy_restore_compatibility -- --nocapture`
  - `cargo test -p forge-signal resource_replay_availability -- --nocapture`
- Current judgment after hostile review:
  - Phase 7 is complete for the implemented Milestone C scope
  - no real in-scope Phase 7 runtime gap remains after the deeper replay-adapter lane
  - the next honest bucket is Phase 8 certification/closeout, not more substrate expansion

## Implementation - Phase 8A Milestone C policy certification family bundle and first scenario matrix

- Started Milestone C Phase 8 with the first real closeout slice instead of jumping straight to a decorative certification run.
- Added Phase 8A certification/reporting types in `crates/forge-signal/src/data/resource/certification.rs`:
  - `ResourceMilestoneCPolicyCertificationFamily`
  - `ResourceMilestoneCPolicyCertificationRecord`
  - `ResourceMilestoneCPolicyCertificationSummary`
  - `ResourceMilestoneCPolicyCertificationBundle`
  - `ResourceMilestoneCPolicyCertificationBuilder`
  - `ResourceMilestoneCPolicyScenarioId`
  - `ResourceMilestoneCPolicyScenarioEvidenceKind`
  - `ResourceMilestoneCPolicyScenarioRow`
  - `ResourceMilestoneCPolicyScenarioMatrixSummary`
  - `ResourceMilestoneCPolicyScenarioMatrix`
- Added the first required Phase 8 family/row constants:
  - `REQUIRED_RESOURCE_MILESTONE_C_POLICY_CERTIFICATION_FAMILIES`
  - `REQUIRED_RESOURCE_MILESTONE_C_POLICY_SCENARIOS`
  - `RESOURCE_MILESTONE_C_POLICY_CERTIFICATION_BUNDLE_SCHEMA_VERSION`
  - `RESOURCE_MILESTONE_C_POLICY_SCENARIO_MATRIX_SCHEMA_VERSION`
- Implemented builder-backed certification families for:
  - policy registry freeze
  - retry budget/backoff
  - timeout/deadline
  - cancellation/supersession
  - revalidation/freshness
  - observation/output continuity
  - retention/replay
- Implemented `resource_milestone_c_policy_scenario_matrix(...)` so the first shipped Phase 8 matrix now certifies real runtime evidence for:
  - registry order canonicalization
  - retry budget exhaustion rejection
  - terminal heartbeat extension denial
  - retention compaction unavailable-history evidence
  - diagnostics expansion budget denial
  - compatible restore admission
  - incompatible restore denial
  - missing descriptor restore denial
- Tightened the Phase 8A API surface while finishing it:
  - `ResourceMilestoneCPolicyScenarioMatrixSummary` accessors are now borrow-based instead of moving out of a shared summary reference
  - Milestone C certification constants are re-exported through `facade.rs` so the proof surface is consistent with the runtime tests
- Added hostile runtime coverage in `crates/forge-signal/src/tests/resource_runtime.rs`:
  - `resource_milestone_c_policy_certification_bundle_and_scenario_matrix_use_production_reports`
  - `resource_milestone_c_policy_scenario_matrix_rejects_wrong_restore_denial_class`
- Important Phase 8A test correction captured during implementation:
  - the initial missing-descriptor restore setup was self-contradictory because it tried to declare historical timeout policy under a registry that was already missing that descriptor
  - fixed by building the historical lowered bundle against the built-in registry, then classifying against a reduced current registry and deriving a real `MissingDescriptor` denial artifact from the canonical compatibility report
  - also fixed the incompatible-restore scenario so it now uses a true `VersionIncompatible` artifact instead of parameter drift, matching the Phase 8 scenario contract honestly
- Added compile-fail privacy coverage for the new proof objects:
  - `resource_milestone_c_policy_scenario_row_fields_are_private`
  - `resource_milestone_c_policy_scenario_matrix_fields_are_private`
- Verification passed:
  - `cargo fmt -p forge-signal`
  - `cargo check -p forge-signal --tests`
  - `cargo test -p forge-signal resource_milestone_c_policy -- --nocapture`
  - `cargo test -p forge-signal resource_compile_fail_boundaries_hold -- --nocapture`
  - `cargo test -p forge-signal`
  - `git diff --check` clean aside from existing LF/CRLF warnings
- Full crate after this batch: `844 passed, 23 ignored`.
## Implementation - Phase 8B Milestone C policy performance closeout

- Landed the next honest Phase 8 slice by adding `ResourceMilestoneCPolicyPerformanceCloseout`, `ResourceMilestoneCPolicyPerformanceCloseoutRow`, `ResourceMilestoneCPolicyPerformanceCloseoutSummary`, and `ResourceMilestoneCPolicyPerformanceClaimId` in `crates/forge-signal/src/data/resource/certification.rs`.
- Added required claim constants and schema version for the first shipped Milestone C performance closeout surface:
  - `RegistryFreezeOrderBounded`
  - `RetryBudgetDenialZeroWake`
  - `RetentionCompactionAvailabilityBounded`
  - `DiagnosticsBudgetDenialZeroCold`
  - `ReplayCompatibilityDescriptorBounded`
- Implemented `resource_milestone_c_policy_performance_closeout(...)` so the closeout is built only from a passing Phase 8A scenario matrix and refuses incomplete claim coverage.
- Bound performance evidence digests to either exact scenario-row evidence or the explicit replay-compatibility scenario trio, so the closeout proves where each claim came from instead of flattening everything into one opaque summary.
- Added validation rules per claim that force the expected boundary kind, cost contract, posture, and key envelope invariants before a closeout row can exist.
- Exposed the new Phase 8B closeout surface through `data/resource/mod.rs` and `facade.rs`, including the non-test root facade exports.
- Added compile-fail privacy coverage for `ResourceMilestoneCPolicyPerformanceCloseout` / row fields via `resource_milestone_c_policy_performance_closeout_fields_are_private` and wired it into `resource_compile_fail_boundaries_hold`.
- Extended `resource_closeout_assertions.rs` with Milestone C closeout row helpers and adversarial shape assertions instead of only checking row presence.
- Tightened the existing Phase 8A runtime certification test so it now also builds the Phase 8B performance closeout and proves:
  - exact required-claim coverage
  - unique rows per claim
  - scenario-matrix digest continuity
  - non-empty closeout/evidence digests
  - per-claim boundary/cost-envelope shape
- Honest verification snags fixed during the batch:
  - imported `ResourceCostContractId` into `certification.rs` for the new claim validators
  - corrected the Milestone C closeout test helper expectations to match the real policy-compatibility and retry-schedule envelope density/allocation semantics
  - blessed the new trybuild stderr fixture for closeout privacy
- Verification passed:
  - `cargo check -p forge-signal --tests`
  - `cargo fmt -p forge-signal`
  - `cargo test -p forge-signal resource_milestone_c_policy -- --nocapture`
  - `cargo test -p forge-signal resource_compile_fail_boundaries_hold -- --nocapture`
  - `cargo test -p forge-signal`
  - `git diff --check` clean aside from existing LF/CRLF warnings
- Full crate after this batch: `844 passed, 23 ignored`.
## QA - Phase 8B Milestone C performance closeout provenance

- Ran a hostile QA pass on the new Phase 8B performance closeout surface.
- Found one real artifact weakness: Milestone C performance closeout rows were compressing policy authority into opaque scenario digests instead of carrying explicit policy provenance at the closeout boundary.
- Corrected that by:
  - adding `policy_provenance_digest` to `ResourceMilestoneCPolicyScenarioRow` and `ResourceMilestoneCPolicyPerformanceCloseoutRow`
  - binding that provenance into closeout evidence digests
  - extending `DeniedResourceRetry` to retain `policy_decision_digest` so retry-budget denial certification can name real authority instead of inferred family posture
  - adding retention-compaction provenance digesting via `ResourceLifecycleRetentionCompactionReport`
  - shifting retention-compaction and denied-retry accessors to borrow-based APIs so the proof objects stay honest without relying on `Copy`
  - fixing an accidental Milestone B helper assertion bleed into `resource_closeout_assertions.rs`
  - refreshing the affected Milestone C trybuild stderr fixtures for scenario-row, closeout-row, and retention-compaction privacy
- Verification passed after the correction:
  - `cargo check -p forge-signal --tests`
  - `cargo fmt -p forge-signal`
  - `cargo test -p forge-signal resource_milestone_c_policy -- --nocapture`
  - `cargo test -p forge-signal resource_compile_fail_boundaries_hold -- --nocapture`
  - `cargo test -p forge-signal`
  - `git diff --check` clean aside from existing LF/CRLF warnings
- Full crate after this QA correction: `844 passed, 23 ignored`.
## Implementation - Phase 8C final Milestone C certification run

- Landed the final Milestone C certification-run surface in `crates/forge-signal/src/data/resource/certification.rs`:
  - `ResourceMilestoneCCertificationRun`
  - `ResourceMilestoneCCertificationRunSummary`
  - `RESOURCE_MILESTONE_C_CERTIFICATION_RUN_SCHEMA_VERSION`
  - `resource_milestone_c_certification_run(...)`
- The final run now binds and validates:
  - passing certification bundle
  - passing scenario matrix
  - passing performance closeout
  - bundle -> matrix -> closeout digest continuity
  - complete required family coverage
  - complete required scenario coverage
  - complete required performance-claim coverage
  - canonical per-record / per-scenario / per-claim digest participation in the final run digest
- Exported the new final certification-run surface through `crates/forge-signal/src/data/resource/mod.rs` and `crates/forge-signal/src/facade.rs`.
- Extended hostile runtime certification coverage in `crates/forge-signal/src/tests/resource_runtime.rs` by strengthening the existing Phase 8 production-evidence test so it now:
  - builds the final Milestone C certification run from real bundle + matrix + closeout artifacts
  - proves summary counts and digest continuity
  - proves the final run passes
  - proves incomplete bundle coverage is denied at the final certification gate
- Tightened compile-fail proof coverage:
  - added `resource_milestone_c_certification_run_fields_are_private`
  - extended `resource_certification_artifacts_cannot_be_deserialized` to include the Milestone C scenario row/matrix, performance closeout row/closeout, and final certification run artifacts
- Honest corrections during finish:
  - fixed the new negative runtime assertion to match the actual final-gate failure ordering (`failed completeness checks`)
  - refreshed trybuild stderr snapshots for the extended certification artifact deserialize boundary and the new final-run privacy fixture
- Verification passed:
  - `cargo test -p forge-signal resource_milestone_c_policy -- --nocapture`
  - `cargo test -p forge-signal resource_compile_fail_boundaries_hold -- --nocapture`
  - `cargo fmt -p forge-signal`
  - `cargo test -p forge-signal`
  - `git diff --check` clean aside from existing LF/CRLF warnings
- Full crate after this batch: `844 passed, 23 ignored`.
## QA - Phase 8 / Milestone C test-requirements audit

- Ran a strict test-audit pass against:
  - `_docs/forge_signal/test-requirements.md`
  - `_docs/engineering/forge_signal_adversarial_testing_matrix.md`
  - the Milestone C spec
  - the coding-guideline laws
- Found two real test-surface weaknesses in the final Phase 8 certification layer:
  1. The final Milestone C certification tests proved happy path and one denial path, but did not yet prove canonical final-run identity under equivalent evidence ordering.
  2. The compile-fail privacy fixture for `ResourceMilestoneCCertificationRun` was pulling UB-style zero-init warnings into the expected stderr, which made the test noisier and more brittle than the boundary it was actually supposed to certify.
- Corrected both:
  - strengthened `resource_milestone_c_policy_certification_bundle_and_scenario_matrix_use_production_reports` so it now rebuilds the bundle, scenario matrix, performance closeout, and final certification run from reversed-but-equivalent certification-family evidence and proves all canonical digests stay identical
  - rewrote `tests/ui/resource_milestone_c_certification_run_fields_are_private.rs` to use panic-based typed placeholders instead of zero-initialized `MaybeUninit` values, then refreshed the expected trybuild stderr so the fixture now asserts only the intended private-field boundary
- Resulting judgment:
  - Phase 8 tests now better match the test-requirements spirit for canonicality, adversarial certification artifacts, and clean machine-enforced proof boundaries
  - no further meaningful findings remained in the current Milestone C Phase 8 test surface after the correction loop
- Verification passed:
  - `cargo test -p forge-signal resource_milestone_c_policy -- --nocapture`
  - `TRYBUILD=overwrite cargo test -p forge-signal resource_compile_fail_boundaries_hold -- --nocapture`
  - `cargo fmt -p forge-signal`
  - `cargo test -p forge-signal`
  - `git diff --check` clean aside from existing LF/CRLF warnings
- Full crate after this QA correction: `844 passed, 23 ignored`.
## Implementation - Phase 9 first async nightmare grammar lane

- Landed the first real Phase 9 slice in `crates/forge-signal/src/tests/resource_runtime.rs`.
- Added `resource_async_nightmare_grammar_preserves_canonical_truth_across_restore_and_replay`, explicitly naming coverage for requirements `15`, `16`, `17`, `18`, `19A`, and `19B`.
- Reworked `resource_certification_fixture_artifacts(...)` so the Milestone B certification fixture is now fed by one mixed async hostile workload instead of separate polite helper reports. The combined workload now exercises:
  - retained unknown-request denial evidence
  - overlapping admissions with stale/superseded completion denial
  - transactional completion rollback with rollback-suppressed observation
  - cancellation racing late completion
  - timeout racing late completion
  - malformed completion denial
  - snapshot restore and replay reconstruction after mixed async churn
- The new lane exposed a real replay-honesty bug: `reconstruct_resource_replay_summary()` was hashing raw restored in-flight handles, so restore-epoch churn could perturb `in_flight_digest` even when the semantic in-flight story was equivalent.
- Fixed that in `crates/forge-signal/src/logic/transaction/runtime/state/resource.rs` by digesting canonical in-flight projections that preserve request/generation/branch identity and lifecycle truth while excluding restore-epoch-only noise.
- Also tightened the nightmare-lane assertions so the Milestone B certification run and hostile performance closeout are both proven from the same mixed workload.
- Verification passed:
  - `cargo test -p forge-signal resource_async_nightmare_grammar_preserves_canonical_truth_across_restore_and_replay -- --nocapture`
  - `cargo test -p forge-signal resource_milestone_b_certification_run_requires_complete_passing_bundle -- --nocapture`
  - `cargo test -p forge-signal resource_compile_fail_boundaries_hold -- --nocapture`
  - `cargo test -p forge-signal resource_milestone_b -- --nocapture`
  - `cargo fmt -p forge-signal`
  - `cargo test -p forge-signal`
  - `git diff --check` clean aside from existing LF/CRLF warnings
- Full crate after this batch: `845 passed, 23 ignored`.
## Implementation - Phase 9 async branch restore / replay equivalence lane

- Landed the next real Phase 9 slice in `crates/forge-signal/src/tests/resource_runtime.rs`.
- Added a new branch-local hostile workload helper:
  - `exercise_resource_async_hostile_suffix_on_active_branch(...)`
  - `resource_branch_replay_workload(...)`
- Added `resource_async_branch_restore_replay_equivalence_converges_for_equivalent_hostile_suffixes`, which now proves:
  - equivalent hostile async suffixes on sibling branches drift after checkpoint mutation
  - inactive branch restore does not mutate sibling branch replay truth
  - restored branch replay truth converges exactly back to the checkpointed branch truth
  - equivalent restored sibling branches converge to identical descriptor, lifecycle, denial, inflight, retry-lineage, replay, and diagnostics provenance digests
- Strengthened `resource_certification_fixture_artifacts(...)` so Milestone B certification no longer sources branch/replay family evidence from the simpler active snapshot-restore path; it now uses the stronger branch-local replay workload.
- The new lane exposed two real replay-canonicality bugs in `crates/forge-signal/src/logic/transaction/runtime/state/resource.rs`:
  1. denied-completion replay digests were still branch-identity sensitive because the digest basis serialized raw denied completion artifacts and pruned denied-history artifacts directly, including branch-local noise like denial ids and branch ids
  2. in-flight replay digests were still branch-identity sensitive because canonical handle digest basis still included `branch_id`
- Fixed both by lowering replay digest projection to canonical semantic entries:
  - denied-completion replay digests now preserve class, node, request/generation, restore epoch, attempt, and payload size while omitting denial-id and branch-id noise
  - pruned denied-completion availability now digests semantic availability posture instead of denial ids
  - in-flight replay handle digest basis now keeps request/generation identity but drops branch-id noise
- Also corrected one test-surface issue while wiring the workload:
  - inactive `restore_branch_snapshot(...)` updates stored branch truth but does not emit `ResourceBranchRestoreReport` until an active `restore_snapshot(...)` boundary occurs, so the helper now uses inactive restore for branch-isolation proof and an immediate active same-snapshot restore to emit certification-grade branch-restore evidence without changing replay truth
- Verification passed:
  - `cargo test -p forge-signal resource_async_branch_restore_replay_equivalence_converges_for_equivalent_hostile_suffixes -- --nocapture`
  - `cargo test -p forge-signal resource_milestone_b_certification_run_requires_complete_passing_bundle -- --nocapture`
  - `cargo fmt -p forge-signal`
  - `cargo test -p forge-signal resource_compile_fail_boundaries_hold -- --nocapture`
  - `cargo test -p forge-signal resource_milestone_b -- --nocapture`
  - `cargo test -p forge-signal`
  - `git diff --check` clean aside from existing LF/CRLF warnings
- Full crate after this batch: `846 passed, 23 ignored`.
## QA - Phase 9 async branch restore / replay equivalence

- Ran a hostile QA pass against:
  - `Phase 9` in `_docs/forge_signal/milestone-c-plan.md`
  - requirement `18` in `_docs/forge_signal/test-requirements.md`
  - `MENTALITY.md`, `arch_laws.md`, `domain_laws.md`, and `perf_laws.md`
- Found one real weakness:
  - the new branch-restore / replay lane was proving replay reconstruction convergence, but it was not directly certifying branch-local replay stream or branch-head snapshot honesty
  - that meant the test could still pass while restore preserved summary digests but silently drifted branch replay history or checkpoint-head truth, which under-served the requirement `18` promise around replay from checkpoint plus completion stream and branch-local explanation history
- Corrected it in `crates/forge-signal/src/tests/resource_runtime.rs` by expanding the branch workload state to retain:
  - branch id
  - branch head snapshot before/after restore
  - branch replay slice before restore
  - branch replay slice after restore
- Strengthened `resource_async_branch_restore_replay_equivalence_converges_for_equivalent_hostile_suffixes` so it now proves:
  - restore preserves the captured branch head snapshot
  - restore may append replay evidence but does not erase prior branch replay history
  - replay history remains branch-local after restore
  - restore does not invent or erase committed async replay history
  - equivalent restored sibling branches preserve identical restore replay causality counts
- Verification passed:
  - `cargo test -p forge-signal resource_async_branch_restore_replay_equivalence_converges_for_equivalent_hostile_suffixes -- --nocapture`
  - `cargo test -p forge-signal resource_milestone_b_certification_run_requires_complete_passing_bundle -- --nocapture`
  - `cargo fmt -p forge-signal`
  - `cargo test -p forge-signal resource_milestone_b -- --nocapture`
  - `cargo test -p forge-signal`
- Result:
  - no further meaningful findings remained in this Phase 9 branch-restore / replay slice after the correction loop
  - full crate after QA: `846 passed, 23 ignored`

## Implementation - Phase 9 mixed temporal parity and boundedness torture lane

- Landed the next real Phase 9 slice in `crates/forge-signal/src/tests/temporal_runtime.rs`.
- Added a reusable mixed temporal hostile workload:
  - `TemporalPhase9MixedBranchState`
  - `TemporalPhase9MixedWorkloadOutcome`
  - `exercise_temporal_phase9_hostile_suffix_on_active_branch(...)`
  - `temporal_phase9_mixed_workload()`
- Added `temporal_phase9_mixed_workload_preserves_parity_and_boundedness_across_branch_restore`, which now proves in one lane:
  - equivalent sibling branches share the same checkpoint-honest clock, scheduled-wake, ready-wake, eligibility, and previous-value digests
  - hostile branch-local churn perturbs ready-frontier truth before restore
  - restore reinstates the captured branch head snapshot
  - restore may append replay evidence but does not erase prior branch-local replay history
  - replay history remains branch-local after restore
  - equivalent restored sibling branches converge exactly on temporal reconstructability digests
  - large interval jumps charge bounded missed-tick policy outcomes instead of hiding elapsed-time work
  - branch-local restore work and rebuild-denial work stay attributable on the restored branches rather than smearing into main-branch telemetry
- Rewired `milestone_a_closeout_bundle_covers_hostile_temporal_certification_paths` to consume the stronger mixed workload evidence instead of rebuilding a smaller stitched set of examples inline.
- One useful correction fell out while wiring assertions:
  - `branch_local_temporal_restore_count` and restore parity counters are branch-local telemetry, so reading them back from `main` after branch switching under-claims the work; the workload now captures restored branch telemetry at the right boundary and the main-branch closeout assertion explicitly proves the counters do *not* smear across branches.
- Verification passed:
  - `cargo fmt -p forge-signal`
  - `cargo test -p forge-signal temporal_phase9_mixed_workload_preserves_parity_and_boundedness_across_branch_restore -- --nocapture`
  - `cargo test -p forge-signal milestone_a_closeout_bundle_covers_hostile_temporal_certification_paths -- --nocapture`
  - `cargo test -p forge-signal temporal_certification_builder_requires_distinct_family_evidence_lanes -- --nocapture`
  - `cargo test -p forge-signal`
  - `git diff --check` clean aside from existing LF/CRLF warnings
- Full crate after this batch: `847 passed, 23 ignored`.

## Implementation - Phase 9 async lifecycle parity and rollback/observation evidence hardening

- Landed the next real Phase 9 slice in:
  - `crates/forge-signal/src/data/resource/certification.rs`
  - `crates/forge-signal/src/tests/resource_runtime.rs`
- Strengthened the Milestone B async certification surface so it better reflects the spirit of requirements `15` and `17` instead of certifying through compressed report shards.

- Expanded `ResourceCertificationBuilder` and `ResourceCertificationEvidence`:
  - `with_async_resource_lifecycle_parity(...)` now consumes both replay reconstruction and diagnostics summary
  - `with_async_rollback_observation_equivalence(...)` now consumes:
    - rollback report
    - rollback-suppressed observation batch
    - pre-rollback replay summary
    - post-rollback replay summary
    - diagnostics summary
- Lifecycle parity evidence now binds:
  - descriptor digest
  - lifecycle digest
  - output continuity digest
  - denied-completion digest
  - retry-lineage digest
  - in-flight digest
  - replay digest
  - retained/unavailable counters
  - diagnostics provenance digest
- Rollback/observation equivalence evidence now rejects dishonest inputs unless:
  - rollback-suppressed observation evidence is present
  - every observation event is `RollbackSuppressed`
  - pre/post rollback replay, lifecycle, in-flight, denied-completion, and retry-lineage digests match exactly

- Added a direct hostile workload helper and proof test in `resource_runtime.rs`:
  - `ResourceAsyncLifecycleRollbackWorkloadOutcome`
  - `resource_async_lifecycle_rollback_workload()`
  - `resource_async_lifecycle_and_rollback_workload_preserves_committed_truth_and_suppresses_observation`
- That direct test now proves:
  - rollback preserves descriptor, lifecycle, output continuity, in-flight, retry-lineage, and replay truth exactly
  - rollback-suppressed observation remains visible as proof but does not deliver observer packets
  - the same completion still commits and delivers normally on the no-failure transactional control path
  - diagnostics provenance remains available for the rollback lane

- Rewired `resource_certification_fixture_artifacts(...)` so Milestone B certification no longer sources lifecycle parity and rollback/observation evidence from the weaker inline mini-paths:
  - lifecycle parity now uses branch-replay evidence plus diagnostics provenance
  - rollback/observation now uses the new hostile workload helper

- One useful correction fell out during implementation:
  - the no-failure control path had to use a transactional completion boundary, not direct runtime commit, otherwise the “one delivered observer packet” assertion was certifying the wrong boundary shape

- Verification passed:
  - `cargo fmt -p forge-signal`
  - `cargo test -p forge-signal resource_async_lifecycle_and_rollback_workload_preserves_committed_truth_and_suppresses_observation -- --nocapture`
  - `cargo test -p forge-signal resource_milestone_b_certification_run_requires_complete_passing_bundle -- --nocapture`
  - `cargo test -p forge-signal resource_async_nightmare_grammar_preserves_canonical_truth_across_restore_and_replay -- --nocapture`
  - `cargo test -p forge-signal resource_milestone_b -- --nocapture`
  - `cargo test -p forge-signal`
  - `git diff --check` clean aside from existing LF/CRLF warnings
- Full crate after this batch: `848 passed, 23 ignored`.

## QA - Phase 9 async lifecycle parity and rollback observation hardening

- Ran a hostile QA pass on the just-finished Phase 9 async lifecycle / rollback slice against:
  - `MENTALITY.md`
  - `arch_laws.md`
  - `domain_laws.md`
  - `perf_laws.md`
  - `_docs/forge_signal/test-requirements.md` requirements `15`, `17`, `18`, `19A`, `19B`

- Found and fixed two real weaknesses:
  1. **Lifecycle parity certification was still certifying a single replay report instead of equivalence across canonical async executions.**
     - Fixed in `crates/forge-signal/src/data/resource/certification.rs` by making `with_async_resource_lifecycle_parity(...)` require two replay reconstructions plus two diagnostics summaries and reject drift in:
       - descriptor
       - lifecycle
       - output continuity
       - denied completion
       - retry lineage
       - in-flight
       - replay digest
       - unavailable-history posture
       - diagnostics provenance
     - Added hostile regression in `crates/forge-signal/src/tests/resource_runtime.rs`:
       - `resource_lifecycle_certification_rejects_non_equivalent_replay_truth`

  2. **Rollback observation certification was proving “a suppressed packet existed” without proving packet-shape parity against the no-failure control boundary.**
     - Fixed by:
       - enriching `ResourceObservationEvent` in `crates/forge-signal/src/data/resource/observation.rs` with:
         - `touched`
         - `recomputed`
         - `meaningful_change`
         - `trigger_matched`
       - threading those fields through `crates/forge-signal/src/logic/transaction/runtime/state/resource_observation.rs`
       - expanding rollback certification in `crates/forge-signal/src/data/resource/certification.rs` so it now requires a delivered control observation report and rejects mismatched packet shape
       - extending `ResourceAsyncLifecycleRollbackWorkloadOutcome` and the direct hostile test in `crates/forge-signal/src/tests/resource_runtime.rs` to capture and compare the delivered control observation boundary honestly
     - Added hostile regression:
       - `resource_rollback_certification_rejects_control_observation_mismatch`

- Important honesty note from the QA pass:
  - the rollback-suppressed resource observation report materializes current post-rollback node state, so the right parity proof is **boundary packet shape parity plus rollback state integrity**, not naive equality of post-materialized matched node state between rollback and commit lanes.
  - The tests were updated to reflect that truth instead of overclaiming.

- Verification passed:
  - `cargo test -p forge-signal resource_async_lifecycle_and_rollback_workload_preserves_committed_truth_and_suppresses_observation -- --nocapture`
  - `cargo test -p forge-signal resource_lifecycle_certification_rejects_non_equivalent_replay_truth -- --nocapture`
  - `cargo test -p forge-signal resource_rollback_certification_rejects_control_observation_mismatch -- --nocapture`
  - `cargo test -p forge-signal resource_milestone_b -- --nocapture`
  - `cargo test -p forge-signal resource_compile_fail_boundaries_hold -- --nocapture`
  - `cargo fmt -p forge-signal`
  - `cargo test -p forge-signal`
  - `git diff --check` clean aside from existing LF/CRLF warnings
- Full crate after QA corrections: `850 passed, 23 ignored`.

## Implementation - Phase 9 async inflight pressure and boundedness hardening

- Landed the next real Phase 9 slice by replacing the old polite inflight-boundedness certification evidence with a hostile async-pressure workload.
- Added `resource_async_inflight_pressure_workload()` and `ResourceAsyncInflightPressureWorkloadOutcome` in `crates/forge-signal/src/tests/resource_runtime.rs`. The workload now mixes:
  - timeout -> retry scheduling -> duplicate retry denial -> scheduled retry admission
  - supersession with late stale completion denial
  - completion-batch admission containing contradictory, accepted, duplicate, and unknown-request completions
  - cancellation churn
  - branch snapshot restore with live inflight state
- Tightened `ResourceCertificationBuilder::with_async_inflight_boundedness(...)` and `ResourceCertificationEvidence::inflight_boundedness(...)` in `crates/forge-signal/src/data/resource/certification.rs` so inflight certification now requires:
  - runtime summary / replay agreement on inflight width
  - hostile retry / restore / supersession / duplicate / contradictory / unknown-request completion evidence
  - explicit telemetry-backed boundedness posture instead of one admitted request
- Important honesty fix during closeout: the Milestone B inflight-boundedness performance claim had been modeled like a single request admission. The real hostile boundary is `CompletionBatchAdmission` with:
  - `input_width = 4`
  - `admitted_count = 1`
  - `denied_count = 3`
  - `lifecycle_transition_count = 1`
  - `operational_allocation_count = 3`
  - `retained_history_allocation_count = 0`
  - `diagnostics_allocation_count = 4`
  - `facade_report_allocation_count = 1`
  - `density_strategy = BurstySortedDeduplicated`
- Updated both the certification validator in `certification.rs` and the shared Milestone B assertion helper in `crates/forge-signal/src/tests/resource_closeout_assertions.rs` to certify that real hostile envelope instead of the earlier guessed one.
- Added focused hostile tests:
  - `resource_async_inflight_pressure_workload_keeps_matching_local_and_bounded`
  - `resource_inflight_certification_rejects_non_hostile_pressure_evidence`
- Rewired `resource_certification_fixture_artifacts(...)` so Milestone B certification now consumes the new inflight-pressure workload instead of a trivial request-admission path.
- Verification passed:
  - `cargo test -p forge-signal resource_async_inflight_pressure_workload_keeps_matching_local_and_bounded -- --nocapture`
  - `cargo test -p forge-signal resource_milestone_b_certification_run_requires_complete_passing_bundle -- --nocapture`
  - `cargo test -p forge-signal resource_milestone_b -- --nocapture`
  - `cargo test -p forge-signal resource_compile_fail_boundaries_hold -- --nocapture`
  - `cargo fmt -p forge-signal`
  - `cargo test -p forge-signal`
- Full crate after this batch: `852 passed, 23 ignored`.

## Implementation - Phase 9 nightmare grammar completion-integrity and request-identity certification expansion

- Expanded Milestone B hostile scenario certification so the async nightmare grammar now machine-checks more of requirement `19A` directly instead of leaving it implied by telemetry.
- Added new hostile scenario rows in `crates/forge-signal/src/data/resource/certification.rs`:
  - `DuplicateCompletionRejected`
  - `ContradictoryCompletionRejected`
  - `UnknownRequestCompletionRejected`
- Expanded `REQUIRED_RESOURCE_MILESTONE_B_SCENARIOS` and hostile-scenario requirements so the scenario matrix now requires those completion-integrity and request-identity denial lanes alongside the older superseded / cancelled / timed-out / malformed rows.
- Added `ResourceMilestoneBHostileScenarioEvidenceRow::from_completion_batch_denial_report(...)`, so hostile evidence can now be built from a real `CompletionBatchAdmission` boundary instead of pretending every nightmare denial must come from a scalar completion admission.
- Kept the performance story honest:
  - the three new hostile rows are intentionally certified from the same mixed batch boundary used by the inflight-pressure workload
  - the existing `HostileCompletionDenialsScalarBounded` performance claim still covers only the four scalar hostile rows
  - the batch denial pressure is already certified separately through the `InflightBoundedness` performance lane, so we did not invent a fake scalar closeout for duplicate/contradictory/unknown-request denials
- Extended `ResourceAsyncInflightPressureWorkloadOutcome` to retain the real `ResourceCompletionBatchAdmissionReport`, and tightened the direct workload test to assert that the mixed batch actually contains all three required denial classes:
  - `Duplicate`
  - `Contradictory`
  - `UnknownRequest`
- Rewired `resource_certification_fixture_artifacts(...)` so Milestone B hostile evidence now consumes that real batch report when constructing the three new scenario rows.
- Strengthened the direct Phase 9 nightmare proof in `crates/forge-signal/src/tests/resource_runtime.rs`:
  - it now asserts all seven hostile denial rows exist
  - it proves the three new rows carry the right denial classes
  - it proves their evidence digests stay distinct from each other and from the older lifecycle-oriented hostile denials
- Updated `crates/forge-signal/src/tests/resource_closeout_assertions.rs` so hostile row assertions now distinguish:
  - scalar `CompletionAdmission` hostile evidence
  - bursty `CompletionBatchAdmission` hostile evidence from the mixed inflight-pressure lane
- Verification passed:
  - `cargo test -p forge-signal resource_async_inflight_pressure_workload_keeps_matching_local_and_bounded -- --nocapture`
  - `cargo test -p forge-signal resource_milestone_b_certification_run_requires_complete_passing_bundle -- --nocapture`
  - `cargo test -p forge-signal resource_async_nightmare_grammar_preserves_canonical_truth_across_restore_and_replay -- --nocapture`
  - `cargo test -p forge-signal resource_milestone_b -- --nocapture`
  - `cargo test -p forge-signal resource_compile_fail_boundaries_hold -- --nocapture`
  - `cargo fmt -p forge-signal`
  - `cargo test -p forge-signal`
- Full crate after this batch: `852 passed, 23 ignored`.

## QA - Phase 9 nightmare grammar hostile batch proof strength

- Ran a hostile QA pass on the new Phase 9 nightmare-grammar expansion against:
  - `MENTALITY.md`
  - `arch_laws.md`
  - `domain_laws.md`
  - `perf_laws.md`
  - `_docs/forge_signal/milestone-c-plan.md` Phase 9
  - `_docs/forge_signal/test-requirements.md` sections `19A` / `19B`

- Found one real proof weakness:
  - the new `DuplicateCompletionRejected`, `ContradictoryCompletionRejected`, and `UnknownRequestCompletionRejected` scenario rows only required that the denial class appeared somewhere inside a completion batch
  - they did **not** require that the batch was the actual hostile mixed batch shape the nightmare grammar claims
  - that meant a tiny or otherwise convenient completion batch could satisfy those new rows even though the milestone language says the runtime must survive one mixed hostile grammar, not a collection of polite micro-batches

- Fixed it in `crates/forge-signal/src/data/resource/certification.rs` by tightening `ResourceMilestoneBHostileScenarioEvidenceRow::from_completion_batch_denial_report(...)` so batch-backed hostile rows now require the full hostile batch envelope:
  - `CompletionBatchAdmission`
  - `input_width = 4`
  - `admitted_count = 1`
  - `denied_count = 3`
  - `lifecycle_transition_count = 1`
  - `operational_allocation_count = 3`
  - `retained_history_allocation_count = 0`
  - `diagnostics_allocation_count = 4`
  - `facade_report_allocation_count = 1`
  - `density_strategy = BurstySortedDeduplicated`

- Added focused regression coverage in `crates/forge-signal/src/tests/resource_runtime.rs`:
  - `resource_milestone_b_hostile_scenario_evidence_rejects_non_hostile_batch_denials`
  - it constructs an oversized completion batch that still contains duplicate / contradictory / unknown-request denial classes and proves the hostile scenario builder rejects it because it is not the canonical nightmare batch

- Also added a small helper `resource_late_superseded_completion_report()` so the new regression can assemble a complete hostile evidence set without duplicating scaffolding.

- Result after correction:
  - no remaining meaningful findings on this Phase 9 nightmare-grammar completion-integrity / request-identity slice
  - the new hostile rows now certify *mixed hostile workload proof*, not just denial-class presence

- Verification passed:
  - `cargo test -p forge-signal resource_milestone_b_hostile_scenario_evidence_rejects_non_hostile_batch_denials -- --nocapture`
  - `cargo test -p forge-signal resource_async_nightmare_grammar_preserves_canonical_truth_across_restore_and_replay -- --nocapture`
  - `cargo test -p forge-signal resource_milestone_b -- --nocapture`
  - `cargo fmt -p forge-signal`
  - `cargo test -p forge-signal`
- Full crate after QA correction: `853 passed, 23 ignored`.

- QA loop on Phase 9 liveness-pressure coverage found a real test-surface weakness:
  - the inflight-pressure / nightmare lane claimed `19A` liveness coverage, but it only implied lost-completion / ghost-inflight / zombie-completion behavior through boundedness and replay agreement
  - there was no direct adversarial assertion for the actual restore-era contract
- I initially pushed on "pre-restore completion should still admit after restore," and the runtime rejected that. That turned out to be the right failure: existing Milestone B branch-epoch semantics explicitly rekey restore epochs, so pre-restore completion authority must die even when restore reconstructs the same live inflight story.
- I corrected the hostile tests in `crates/forge-signal/src/tests/resource_runtime.rs` to assert the real production contract instead:
  - restored-away post-snapshot drift does not survive as ghost inflight state
  - zombie completion for that restored-away request denies as `UnknownRequest`
  - pre-restore completion denies as `Stale` after restore rekeys branch epoch
  - runtime summary and replay reconstruction still agree on retained inflight width after those denials
- Added direct hostile test:
  - `resource_async_liveness_failures_preserve_inflight_truth_and_reject_zombie_completion`
- Reverified with:
  - `cargo test -p forge-signal resource_async_liveness_failures_preserve_inflight_truth_and_reject_zombie_completion -- --nocapture`
  - `cargo test -p forge-signal resource_async_inflight_pressure_workload_keeps_matching_local_and_bounded -- --nocapture`
  - `cargo test -p forge-signal resource_completion_admission_denies_pre_restore_epoch_as_stale -- --nocapture`
  - `cargo test -p forge-signal resource_async_nightmare_grammar_preserves_canonical_truth_across_restore_and_replay -- --nocapture`
  - `cargo test -p forge-signal resource_milestone_b -- --nocapture`
  - `cargo fmt -p forge-signal`
  - full `cargo test -p forge-signal` -> `854 passed, 23 ignored`

## Spec - Milestone D capability-first async direction

- Added `_docs/forge_signal/milestone-d-plan.md` to capture the next explicit
  follow-on after Milestone C.
- The core decision is: async should become a first-class capability attachable
  to ordinary nodes, not a separate conceptual node species.
- The spec locks one crucial distinction:
  - ordinary node dirtiness remains `Clean | MaybeStale | Dirty`
  - async lifecycle remains orthogonal runtime truth
- Milestone D is framed as a broadening of the already-closed Milestone B/C
  substrate, not a rewrite of lifecycle or policy law.
- Updated `_docs/forge_signal/forge_signal_temporal_async_roadmap.md` to add
  `Phase 4: Async Capability On Arbitrary Nodes` and to stop implying the
  roadmap ends at resource-shaped policy families.

## Closeout - Milestone C acceptance map

- Added `_docs/forge_signal/milestone-c-closeout.md` as the missing closeout
  artifact for Milestone C.
- The closeout doc now maps:
  - must-ship obligations
  - required certification families
  - scenario matrix and performance closeout rows
  - direct vs combined-suite coverage for `test-requirements.md`
  - compile-fail proof boundaries
  - final verification commands and observed results
- Updated `_docs/forge_signal/milestone-c-plan.md` status to `Closed` and added
  a closeout link.
- Updated `_docs/forge_signal/forge_signal_temporal_async_roadmap.md` so Phase
  3 now points at the Milestone C closeout the same way earlier phases do.
- Reverified the core closeout claims with:
  - `cargo test -p forge-signal resource_milestone_c_policy -- --nocapture`
  - `cargo test -p forge-signal resource_async_nightmare_grammar_preserves_canonical_truth_across_restore_and_replay -- --nocapture`
  - `cargo test -p forge-signal temporal_phase9_mixed_workload_preserves_parity_and_boundedness_across_branch_restore -- --nocapture`
  - `cargo test -p forge-signal resource_compile_fail_boundaries_hold -- --nocapture`
