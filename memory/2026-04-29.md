2026-04-29

- User clarified a workspace-wide rule: code files and test files are capped at
  400 lines unless they are explicitly listed as exemptions.
- This cap applies to production code, tests, helpers, fixtures, and support
  modules.
- Specs and roadmap docs are not automatically under the 400-line cap unless a
  spec says so explicitly.
- Future work should treat splitting touched over-limit code/test files as part
  of the task rather than optional cleanup.
- Added mechanical enforcement for tracked Rust files under `crates/**` via
  `scripts/ci/check_workspace_rust_line_caps.sh`, its explicit debt allowlist,
  GitHub CI, and `make check`.
