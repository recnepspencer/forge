Finished the bigger `worth-kernel` corpus-root removal batch.

- deleted the remaining `construction/certification/corpus` root and its direct
  replay / compound owner files
- rehomed the last direct owners into
  `construction/tests/support/corpus_replay_row.rs` and
  `construction/tests/support/compound_runtime/...`
- retargeted the stale legacy deletion proofs away from dead certification
  paths onto the new support owners
- broad verification passed:
  - `cargo fmt -p worth-kernel`
  - `cargo test -p worth-kernel construction::tests::corpus_replay_siege -- --nocapture`
  - `cargo test -p worth-kernel construction::tests::compound_reports -- --nocapture`
  - `cargo test -p worth-kernel construction::tests::compound_parity_reports -- --nocapture`
  - `cargo test -p worth-kernel construction::tests::compound_closeout_reports -- --nocapture`
  - `cargo test -p worth-kernel construction::tests::legacy_deletion -- --nocapture`
  - `cargo test -p worth-kernel construction::tests::boundary -- --nocapture`
  - `cargo check -p worth-kernel -p worth-spatial -p worth-topo`
- new blocker truth: the old `construction/certification/corpus` tree is gone;
  next seam is smaller replay/compound hostile-support residue or the next
  stronger production query-native leak elsewhere

Worked the rebinding signal/continuation seam in `worth-spatial`.

Later in the same hard-break run, removed the replay corpus catalog and simplex
ladder registry from the `worth-kernel` certification tree:

- deleted
  `crates/worth-kernel/src/construction/certification/corpus/cases.rs`
- deleted
  `crates/worth-kernel/src/construction/certification/corpus/simplex_registry.rs`
- rehomed both into plain construction test support:
  - `crates/worth-kernel/src/construction/tests/support/corpus_cases.rs`
  - `crates/worth-kernel/src/construction/tests/support/corpus_simplex_registry.rs`
- rewired:
  - `crates/worth-kernel/src/construction/tests/support/corpus_ordering.rs`
  - `crates/worth-kernel/src/construction/tests/support/corpus_replay_generation.rs`
  - `crates/worth-kernel/src/construction/tests/support/corpus_replay_digest.rs`
  - `crates/worth-kernel/src/construction/tests/support/corpus_replay_view.rs`
  - `crates/worth-kernel/src/construction/tests/corpus_simplex_ladder.rs`
- tightened deletion proofs in:
  - `crates/worth-kernel/src/construction/tests/legacy_deletion_replay.rs`
  - `crates/worth-kernel/src/construction/tests/legacy_deletion_query_and_corpus.rs`

Verification stayed green with:
- `cargo fmt -p worth-kernel`
- `cargo test -p worth-kernel construction::tests::corpus_replay_siege -- --nocapture`
- `cargo test -p worth-kernel construction::tests::corpus_simplex_ladder -- --nocapture`
- `cargo test -p worth-kernel construction::tests::legacy_deletion -- --nocapture`
- `cargo check -p worth-kernel -p worth-spatial -p worth-topo`

New blocker truth: the remaining `construction/certification/corpus` seam is
now the direct root and direct owners like `replay_siege_row.rs` plus the still
live `compound/*` band, not the replay catalog or simplex inventory anymore.

- public `PrimitiveRebinding` now exports typed signal-compatibility and
  bridge-continuation surfaces
- geometry applicability no longer marks rebinding signal/continuation as
  denied
- rebinding route contract now honestly admits bridge participation
- public contract suite, UI suite, and targeted kernel rebinding signal /
  continuation tests passed after the fix
- removed `worth_spatial::facade::policy` as an ordinary public runtime
  namespace
- rehomed old arbitration/profile exports under
  `worth_spatial::certification::policy_support`
- added compile-fail proof that `worth_spatial::facade::policy` cannot return
- moved the arbitration/profile implementation files themselves out of
  `src/spatial_domain` and under `src/certification/policy_support`
- deleted the `src/spatial_domain` shell entirely by rehoming refs, witness
  resolution, and placement vocabulary into direct owner modules
- deleted `worth_spatial::facade::witness_catalog`; witness catalog vocabulary
  now lives with the rest of authored reference vocabulary under
  `worth_spatial::facade::refs`
- collapsed `worth-spatial` birth-family ownership onto
  `worth_primitives::PrimitiveConstructionFamilyKey` instead of keeping a
  duplicate spatial enum
- moved `PrimitiveConstructionBirthPlacementFacts` out of
  `worth_spatial::facade::bindings` and into `worth_spatial::facade::placement`
- removed the remaining live spatial `PrimitiveConstructionBirthFamily` alias
  too; spatial birth, tolerance, and certification code now names
  `PrimitiveConstructionFamilyKey` directly, and kernel construction proof
  callers no longer import a spatial birth-family wrapper
- verified the birth-owner-richness cut with `cargo check -p worth-spatial -p worth-kernel`,
  `cargo test -p worth-spatial --test public_api_contract -- --nocapture`,
  `cargo test -p worth-spatial --test ui -- --nocapture`, and
  `cargo test -p worth-kernel construction::tests::geometry_cleanup_integration -- --nocapture`
- collapsed kernel's duplicate `PrimitiveConstructionFamily` enum into a
  `PrimitiveConstructionFamilyKey` alias plus kernel-only helper lowering:
  `PRIMITIVE_CONSTRUCTION_FAMILIES` and
  `primitive_construction_topology_birth_class(...)`
- rewired the remaining kernel construction family `.topology_birth_class()`
  and `::ALL` fallout to that helper lane, including rejection-locality and
  birth-scaffold proof support
- verified the kernel family-owner collapse with `cargo fmt -p worth-spatial -p worth-kernel`,
  `cargo check -p worth-spatial -p worth-kernel`,
  `cargo test -p worth-spatial --test public_api_contract -- --nocapture`,
  `cargo test -p worth-spatial --test ui -- --nocapture`,
  `cargo test -p worth-kernel construction::tests::geometry_cleanup_integration -- --nocapture`,
  `cargo test -p worth-kernel construction::tests::phase_eight_minimization -- --nocapture`, and
  `cargo test -p worth-kernel construction::tests::construction -- --nocapture`
- collapsed the construction certification replay/query parity split:
  `construction/certification/parity.rs` no longer rebuilds runtime truth from
  direct local preparation helpers and now consumes the shared
  `construction/certification/runtime_truth.rs` collector
- rehomed the old `query/runtime_surface_facts.rs` logic into
  `construction/certification/runtime_truth.rs` so admitted and rejected
  construction certification lanes share one typed fact collector instead of
  each rebuilding their own truth subset
- added `construction/tests/legacy_deletion.rs` to lock out the old parity-band
  bypasses (`prepare_primitive_construction_topology_query_admitted_handoff_from_request`,
  `prepare_primitive_construction_birth_placement_facts`,
  `prepare_primitive_construction_rejected_facts`, and
  `prepare_realization_snapshot`) from construction parity/query certification
  files outside the shared collector
- verified the construction certification consolidation with
  `cargo fmt -p worth-kernel -p worth-spatial`,
  `cargo check -p worth-kernel -p worth-spatial`,
  `cargo test -p worth-kernel construction::tests::legacy_deletion -- --nocapture`,
  `cargo test -p worth-spatial --test public_api_contract -- --nocapture`,
  `cargo test -p worth-spatial --test ui -- --nocapture`,
  `cargo test -p worth-kernel construction::tests::construction -- --nocapture`, and
  `cargo test -p worth-kernel construction::tests::phase_eight_minimization -- --nocapture`
- finished the next construction runtime-truth cut:
  - deleted `construction/certification/query/runtime_surface_facts.rs`
  - rewired `inspection_parity.rs`, `projection_consumption_receipt.rs`,
    `graph_composition_parity.rs`, and `parity.rs` onto
    `construction/certification/runtime_truth.rs`
  - rewired `rejection_locality.rs` onto shared certification runtime truth
    instead of local admitted/rejected helper calls
  - deleted the redundant `construction/certification/corpus/row_support.rs`
    seam
  - rewired corpus replay and compound row builders to read breadth and
    rejection posture directly from shared execution/runtime truth
  - updated construction boundary/deletion proofs to point at the new collector
- reverified after the row-support deletion with:
  `cargo check -p worth-kernel -p worth-spatial`,
  `cargo test -p worth-kernel construction::tests::legacy_deletion -- --nocapture`,
  `cargo test -p worth-kernel construction::tests::boundary -- --nocapture`,
  `cargo test -p worth-kernel construction::tests::construction -- --nocapture`,
  `cargo test -p worth-kernel construction::tests::phase_eight_minimization -- --nocapture`,
  `cargo test -p worth-spatial --test public_api_contract -- --nocapture`, and
  `cargo test -p worth-spatial --test ui -- --nocapture`
- removed the next fake corpus seam:
  - deleted `construction/certification/corpus/rejection_witnesses.rs`
  - first rehomed the synthetic major-boundary proof away from corpus replay
  - then deleted that standalone certification report too
  - corpus replay-siege reports now speak only for real corpus scenarios
  - major boundary coverage now lives with the lower-layer construction outcome
    tests, which is the honest owner for synthetic rejection mapping
- verified the corpus/lower-layer boundary ownership with:
  `cargo check -p worth-kernel -p worth-spatial`,
  `cargo test -p worth-kernel rejected_outcome_covers_every_major_failure_boundary_with_typed_locality -- --nocapture`,
  `cargo test -p worth-kernel construction::certification::corpus::tests::replay_siege -- --nocapture`,
  `cargo test -p worth-kernel construction::certification::corpus::tests::replay_siege_rejections -- --nocapture`,
  `cargo test -p worth-kernel construction::tests::construction -- --nocapture`,
  `cargo test -p worth-kernel construction::tests::boundary -- --nocapture`, and
  `cargo test -p worth-spatial --test public_api_contract -- --nocapture`
- removed the next redundant construction corpus adapter:
  - deleted `construction/certification/corpus/simplex_exhaustion_witness_report.rs`
  - removed its module wiring from `construction/certification/corpus/mod.rs`
  - rewired the simplex corpus test to read
    `prepare_primitive_construction_realization_exhaustion_witness_report()`
    directly from the lower realization owner
  - renamed the test to reflect lower-layer ownership instead of the deleted
    wrapper
- verified the simplex adapter deletion with:
  `cargo check -p worth-kernel -p worth-spatial`,
  `cargo test -p worth-kernel construction::certification::corpus::tests::simplex_ladder -- --nocapture`,
  `cargo test -p worth-kernel construction::tests::construction -- --nocapture`, and
  `cargo test -p worth-spatial --test public_api_contract -- --nocapture`
- removed the next fake construction corpus shelf:
  - deleted `construction/certification/corpus/family_boundary_drift_report.rs`
  - rewired the simplex drift test to call
    `prepare_primitive_construction_family_boundary_report(...)` directly
  - collapsed `PrimitiveConstructionFamilyBoundaryReport` onto the lower
    realization witness row type instead of repackaging it into
    `PrimitiveConstructionFamilyBoundaryLowerLayerWitnessSummary`
  - added a deletion proof in `construction/tests/legacy_deletion.rs` locking
    out both the deleted drift alias and the deleted lower-layer summary shelf
- verified the family-boundary shelf deletion with:
  `cargo check -p worth-kernel -p worth-spatial`,
  `cargo test -p worth-kernel construction::certification::corpus::tests::family_boundaries -- --nocapture`,
  `cargo test -p worth-kernel construction::certification::corpus::tests::simplex_ladder -- --nocapture`,
  `cargo test -p worth-kernel construction::tests::legacy_deletion -- --nocapture`,
  `cargo test -p worth-kernel construction::tests::construction -- --nocapture`,
  `cargo test -p worth-kernel construction::tests::boundary -- --nocapture`, and
  `cargo test -p worth-spatial --test public_api_contract -- --nocapture`
- removed the next synthetic simplex report shelf:
  - deleted `construction/certification/corpus/simplex_ladder_report.rs`
  - deleted `construction/certification/corpus/simplex_ladder_support.rs`
  - rewired the simplex corpus certification tests to read direct owners:
    `prepare_primitive_construction_corpus_replay_siege(...)`,
    `prepare_primitive_construction_realization_strategy_report(...)`,
    `prepare_primitive_construction_realization_exhaustion_report(...)`,
    `prepare_primitive_construction_query_inspection_parity_report(...)`, and
    `prepare_primitive_construction_query_projection_consumption_parity_report(...)`
  - removed the dead `branch_local_digest()` / `replay_digest()` row accessors
    and the dead `PrimitiveConstructionRealizationExhaustionReport::conditioning_witness()`
    accessor that the deleted ladder shelf had left behind
  - added a deletion proof in `construction/tests/legacy_deletion.rs` locking
    out the deleted simplex ladder shelf
- verified the simplex ladder shelf deletion with:
  `cargo check -p worth-kernel -p worth-spatial`,
  `cargo test -p worth-kernel construction::tests::legacy_deletion -- --nocapture`,
  `cargo test -p worth-kernel construction::certification::corpus::tests::simplex_ladder -- --nocapture`,
  `cargo test -p worth-kernel construction::tests::construction -- --nocapture`,
  `cargo test -p worth-kernel construction::tests::boundary -- --nocapture`, and
  `cargo test -p worth-spatial --test public_api_contract -- --nocapture`
- removed the next synthetic threshold-boundary shelf:
  - deleted `construction/certification/corpus/family_boundary_report.rs`
  - rewired `family_boundaries.rs` and the simplex drift check to read direct
    owners instead:
    `prepare_primitive_construction_corpus_replay_siege(...)` and
    `prepare_primitive_construction_realization_exhaustion_witness_report()`
  - added a deletion proof that the corpus root no longer teaches the deleted
    `family_boundary_report` shelf
- verified the family-boundary shelf deletion with:
  `cargo check -p worth-kernel -p worth-spatial`,
  `cargo test -p worth-kernel construction::tests::legacy_deletion -- --nocapture`,
  `cargo test -p worth-kernel construction::certification::corpus::tests::family_boundaries -- --nocapture`,
  `cargo test -p worth-kernel construction::certification::corpus::tests::simplex_ladder -- --nocapture`,
  `cargo test -p worth-kernel construction::tests::construction -- --nocapture`, and
  `cargo test -p worth-spatial --test public_api_contract -- --nocapture`
- removed the next synthetic compound closeout shelf:
  - deleted `construction/certification/corpus/compound/milestone_closeout/report.rs`
  - deleted `construction/certification/corpus/compound/milestone_closeout/mod.rs`
  - rewired `compound/tests/closeout_reports.rs` to read
    `prepare_primitive_construction_compound_parity_report(...)` plus
    `compound_parity_registry().required_scenario_inventory()` directly
  - deleted the dead `construction/certification/corpus/closeout/gate.rs`
    shelf because the removed milestone wrapper was its only real user
  - added a deletion proof in `construction/tests/legacy_deletion.rs` locking
    out the deleted milestone-closeout shelf
- verified the compound closeout shelf deletion with:
  `cargo fmt -p worth-kernel`,
  `cargo test -p worth-kernel construction::certification::corpus::compound::tests::closeout_reports -- --nocapture`,
  `cargo test -p worth-kernel construction::tests::legacy_deletion -- --nocapture`,
  `cargo test -p worth-kernel construction::tests::construction -- --nocapture`,
  `cargo test -p worth-spatial --test public_api_contract -- --nocapture`, and
  `cargo check -p worth-kernel -p worth-spatial`
- removed the next synthetic compound transport shelf:
  - deleted `construction/certification/corpus/compound/lane_report.rs`
  - replaced it with a raw `authoring_lane.rs` owner that carries actual lane
    rows plus derived lane helpers instead of report-of-report metadata
  - rewired compound siege, ordering parity, parity truth, and compound tests
    from `lane_reports()` / `PrimitiveConstructionCompoundOrderLaneReport`
    onto raw `lanes()`
  - deleted the dead summary-row layer
    `PrimitiveConstructionCompoundAuthoringOrderRow`
  - added deletion-proof patterns in `construction/tests/legacy_deletion.rs`
    so the lane-report shelf cannot quietly return
- verified the compound lane-report shelf deletion with:
  `cargo fmt -p worth-kernel`,
  `cargo test -p worth-kernel construction::certification::corpus::compound::tests::reports -- --nocapture`,
  `cargo test -p worth-kernel construction::certification::corpus::compound::tests::parity_reports -- --nocapture`,
  `cargo test -p worth-kernel construction::tests::legacy_deletion -- --nocapture`,
  `cargo test -p worth-kernel construction::tests::construction -- --nocapture`,
  `cargo check -p worth-kernel -p worth-spatial`, and
  `cargo test -p worth-spatial --test public_api_contract -- --nocapture`
- removed the next duplicate compound parity carrier:
  - deleted stored `ordering` from
    `PrimitiveConstructionCompoundParityReportBundle`
  - deleted stored `ordering` from
    `PrimitiveConstructionCompoundParityVerificationFailure`
  - switched ordering access to a derived view from siege lanes instead of a
    second stored artifact copy
  - removed the dead compatibility `ordering()` accessors that became unused
    after the storage cut
- verified the compound ordering-storage collapse with:
  `cargo fmt -p worth-kernel`,
  `cargo test -p worth-kernel construction::certification::corpus::compound::tests::parity_reports -- --nocapture`,
  `cargo test -p worth-kernel construction::certification::corpus::compound::tests::closeout_reports -- --nocapture`,
  `cargo test -p worth-kernel construction::tests::construction -- --nocapture`, and
  `cargo check -p worth-kernel -p worth-spatial`
- removed the next standalone compound wrapper entrypoint:
  - deleted `prepare_primitive_construction_compound_ordering_parity_report(...)`
  - rewired ordering tests onto `prepare_primitive_construction_compound_adversarial_siege_report(...).derived_ordering()`
  - made the ordering surface explicit as a siege-derived view rather than a
    separate prepared report lane
- verified the ordering-entrypoint removal with:
  `cargo fmt -p worth-kernel`,
  `cargo test -p worth-kernel construction::certification::corpus::compound::tests::parity_reports -- --nocapture`,
  `cargo test -p worth-kernel construction::tests::construction -- --nocapture`,
  `cargo check -p worth-kernel -p worth-spatial`, and
  hostile grep showing no remaining `prepare_primitive_construction_compound_ordering_parity_report`
- removed the next standalone compound ordering artifact:
  - deleted `construction/certification/corpus/compound/ordering_report.rs`
  - moved ordering-view semantics onto
    `PrimitiveConstructionCompoundAdversarialSiegeReport`
  - added `ordering_scenario.rs` as a lean scenario-stability helper instead
    of a second report artifact layer
  - rewired specialized compound parity reports to consume siege-owned
    scenario stability directly
  - removed `OrderingProjectionDrift` as a separate mismatch class because
    ordering is no longer an independently supplied artifact that can drift
- verified the ordering-report deletion with:
  `cargo fmt -p worth-kernel`,
  `cargo test -p worth-kernel construction::certification::corpus::compound::tests::parity_reports -- --nocapture`,
  `cargo test -p worth-kernel construction::certification::corpus::compound::tests::closeout_reports -- --nocapture`,
  `cargo test -p worth-kernel construction::tests::construction -- --nocapture`,
  `cargo test -p worth-kernel construction::tests::legacy_deletion -- --nocapture`,
  `cargo check -p worth-kernel -p worth-spatial`, and
  hostile grep showing no live `ordering_report` or
  `prepare_primitive_construction_compound_ordering_parity_report`
- split the stale duplicate specialized parity shelf out of
  `construction/certification/corpus/compound/report.rs`
  - `PrimitiveConstructionCompoundAdversarialSiegeReport` now lives alone in
    `report.rs`
  - motion, grazing, and exhaustion parity reports stay in their dedicated
    owner files instead of being duplicated in `report.rs`
  - `report.rs` dropped to 148 lines, back under the workspace line cap
  - rewired `builder.rs`, `parity/bundle_verified.rs`, and `parity/truth.rs`
    to import the specialized parity owners from their real modules
  - removed stale compound test reexports that had become warning-only residue
- reverified the compound shelf split with:
  `cargo fmt -p worth-kernel`,
  `cargo test -p worth-kernel construction::certification::corpus::compound::tests::reports -- --nocapture`,
  `cargo test -p worth-kernel construction::certification::corpus::compound::tests::parity_reports -- --nocapture`,
  `cargo test -p worth-kernel construction::certification::corpus::compound::tests::closeout_reports -- --nocapture`,
  `cargo test -p worth-kernel construction::tests::legacy_deletion -- --nocapture`,
  `cargo test -p worth-kernel construction::tests::construction -- --nocapture`, and
  `cargo check -p worth-kernel -p worth-spatial`
- removed the next standalone compound parity report shelf:
  - deleted `construction/certification/corpus/compound/motion_report.rs`
  - deleted `construction/certification/corpus/compound/grazing_report.rs`
  - deleted `construction/certification/corpus/compound/exhaustion_report.rs`
  - deleted the old top-level `prepare_primitive_construction_compound_motion_parity_report(...)`,
    `prepare_primitive_construction_compound_grazing_boundary_report(...)`, and
    `prepare_primitive_construction_compound_exhaustion_witness_parity_report(...)`
    entrypoints
  - rewired compound parity and closeout tests to read specialized motion /
    grazing / exhaustion truth directly from `PrimitiveConstructionCompoundParityReport`
    instead of separate specialized report artifacts
  - flattened the verified parity bundle/failure to own raw specialized rows
    directly, with derived inventory/parity/report-digest semantics
  - split specialized-row digest/verification helpers into
    `construction/certification/corpus/compound/parity/specialized_rows.rs`
    so `parity/bundle_verified.rs` dropped back under the workspace line cap
    at 306 lines
  - added a deletion proof in `construction/tests/legacy_deletion.rs` locking
    out reintroduction of the deleted specialized report shelf
- reverified the specialized-report shelf deletion with:
  `cargo fmt -p worth-kernel`,
  `cargo test -p worth-kernel construction::certification::corpus::compound::tests::reports -- --nocapture`,
  `cargo test -p worth-kernel construction::certification::corpus::compound::tests::parity_reports -- --nocapture`,
  `cargo test -p worth-kernel construction::certification::corpus::compound::tests::closeout_reports -- --nocapture`,
  `cargo test -p worth-kernel construction::tests::legacy_deletion -- --nocapture`,
  `cargo test -p worth-kernel construction::tests::construction -- --nocapture`, and
  `cargo check -p worth-kernel -p worth-spatial`
- narrowed the verified compound parity success artifact further:
  - `PrimitiveConstructionCompoundParityReport` no longer stores canonical
    motion/grazing/exhaustion specialized rows after verification succeeds
  - success-side specialized rows are now rederived from `siege + truth` on
    demand
  - only `PrimitiveConstructionCompoundParityVerificationFailure` still keeps
    raw specialized rows, which is the honest place for drift context
  - `bundle_verified.rs` stayed under the line cap at 325 lines after the cut
- reverified the success-side parity storage collapse with:
  `cargo fmt -p worth-kernel`,
  `cargo test -p worth-kernel construction::certification::corpus::compound::tests::parity_reports -- --nocapture`,
  `cargo test -p worth-kernel construction::certification::corpus::compound::tests::closeout_reports -- --nocapture`,
  `cargo test -p worth-kernel construction::tests::legacy_deletion -- --nocapture`,
  `cargo test -p worth-kernel construction::tests::construction -- --nocapture`, and
  `cargo check -p worth-kernel -p worth-spatial`
- removed the duplicate canonical siege state from verified compound parity:
  - `PrimitiveConstructionVerifiedCompoundParityReportPayload` no longer stores
    a second owned `siege`; success-side `siege()` now reads through `truth`
  - `PrimitiveConstructionCompoundParityVerificationFailure` no longer stores a
    second owned `siege`; failure-side `siege()` now also reads through `truth`
  - the only remaining owned `siege` in this band is the transient
    `PrimitiveConstructionCompoundParityReportBundle` input carrier
  - `bundle_verified.rs` dropped again to 317 lines after the duplicate-state cut
- reverified the siege-duplication removal with:
  `cargo fmt -p worth-kernel`,
  `cargo test -p worth-kernel construction::certification::corpus::compound::tests::parity_reports -- --nocapture`,
  `cargo test -p worth-kernel construction::certification::corpus::compound::tests::closeout_reports -- --nocapture`,
  `cargo test -p worth-kernel construction::tests::legacy_deletion -- --nocapture`,
  `cargo test -p worth-kernel construction::tests::construction -- --nocapture`, and
  `cargo check -p worth-kernel -p worth-spatial`
- removed the last dead parity-digest residue in the compound parity lane:
  - `PrimitiveConstructionCompoundParityVerificationFailure` no longer stores
    an unused `report_digest`
  - `PrimitiveConstructionCompoundParityCanonicalTruth` no longer stores an
    unused `truth_digest`
  - `PrimitiveConstructionCompoundParityRegistry` no longer stores an unused
    `registry_digest`
  - the compound parity pocket is warning-clean again after those removals
- reverified the dead-digest cleanup with:
  `cargo fmt -p worth-kernel`,
  `cargo test -p worth-kernel construction::certification::corpus::compound::tests::parity_reports -- --nocapture`,
  `cargo test -p worth-kernel construction::tests::legacy_deletion -- --nocapture`, and
  `cargo check -p worth-kernel -p worth-spatial`
- narrowed the remaining compound parity failure carrier:
  - `PrimitiveConstructionCompoundParityVerificationFailure` now keeps raw
    specialized rows only for mismatched lanes
  - matched lanes no longer carry copied row payloads through the failure path
    and instead read through canonical truth
  - `motion_scenario_ids()` falls back to truth-owned scenario ids when motion
    did not drift, and the grazing/exhaustion coverage checks fall back to
    `true` when those lanes matched
- reverified the failure-payload narrowing with:
  `cargo test -p worth-kernel construction::certification::corpus::compound::tests::parity_reports -- --nocapture`,
  `cargo test -p worth-kernel construction::tests::construction -- --nocapture`, and
  `cargo check -p worth-kernel -p worth-spatial`
- removed the synthetic construction proof closeout shelf:
  - deleted `construction/proof/substrate_closeout_report.rs`
  - deleted `construction/proof/proof_grade.rs`
  - rewired `construction/certification/proof_substrate_closeout_tests.rs` to
    assert directly against the lower proof owners:
    digest protocol, geometry digest sensitivity, canonical witness parity,
    shell-with-hole hostility, simplex ratio, verified artifact surface,
    truth projection, and proof-boundary compile-fail inventory
  - retargeted the compile-fail denial proof from the deleted closeout bundle
    to `PrimitiveConstructionPhaseFiveBoundaryCloseoutReport`
  - added a deletion guard in `construction/tests/legacy_deletion.rs` so the
    deleted proof-substrate closeout shelf cannot quietly return
- removed the dead digest baggage that only existed for that deleted wrapper:
  - canonical witness parity report no longer stores `report_digest`
  - proof-boundary compile-fail inventory no longer stores `report_digest`
  - digest protocol report no longer stores `report_digest`
  - geometry digest sensitivity report no longer stores `report_digest`
  - shell-with-hole hostility suite no longer stores `report_digest`
  - simplex canonical ratio report no longer stores `report_digest`
- reverified the proof-shelf deletion with:
  `cargo fmt -p worth-kernel`,
  `cargo test -p worth-kernel construction::certification::proof_substrate_closeout_tests -- --nocapture`,
  `cargo test -p worth-kernel construction::tests::legacy_deletion -- --nocapture`,
  `cargo test -p worth-kernel construction::certification::proof_metadata_surface_tests -- --nocapture`, and
  `cargo check -p worth-kernel -p worth-spatial`
- deleted the synthetic construction realization bundle shelf:
  - removed `construction/certification/realization/bundle.rs`
  - rewired `construction/certification/realization/tests/reports.rs` so the
    shared-view proof builds the strategy, witness, stability, and exhaustion
    reports directly from one `prepare_realization_snapshot(...)`
  - added a deletion guard in `construction/tests/legacy_deletion.rs` so the
    bundle shelf cannot quietly return
- deleted the fake branch-local parity wrapper shelf and the kernel-owned
  branch-preview basis report shelf:
  - removed `PrimitiveConstructionBranchLocalParityReport` and
    `prepare_primitive_construction_branch_local_parity_report(...)`
  - removed `construction/certification/branch_preview_basis_report.rs`
  - rewired the surviving branch-preview lane onto direct Forge Query
    admissions inside `query/basis_preview_parity.rs`,
    `corpus/execution/proof_ingredients.rs`, and the construction tests
  - renamed propagated row fields from `branch_local_digest` to
    `branch_basis_digest`
  - removed the obsolete branch-local compile-fail fixture and refreshed the
    hostile audit files that had been reading the deleted report shelves
- deleted the query scan-report shelves:
  - removed `construction/certification/query/existing_truth_binding.rs`
  - removed `construction/certification/query/no_local_runtime_workaround_audit.rs`
  - moved the actual scan invariants into the hostile proof owners in
    `phase_five_boundary_closeout_tests.rs` and `legacy_deletion.rs`
  - retargeted the public certification-bucket compile-fail proof onto a
    surviving query certification type instead of the deleted audit report
- deleted the `construction/certification/rejection_locality.rs` report shelf
  and rewired the last construction test caller onto direct
  `prepare_primitive_construction_certification_runtime_truth(...)`
- kept only the honest reusable boundary mapping by rehoming
  `PrimitiveConstructionBlockingBoundary` plus `blocking_boundary_for(...)`
  into the smaller owner file
  `construction/certification/blocking_boundary.rs`
- rewired corpus execution/rows/tests onto that smaller boundary owner and
  added a deletion proof so the rejection-locality report shelf cannot return
- collapsed `construction/certification/parity.rs` onto canonical
  `PrimitiveConstructionCertificationRuntimeTruth` instead of keeping a second
  bespoke `PrimitiveConstructionParityPreparedFacts` schema
- added a deletion proof so that bespoke replay-parity prepared-facts carrier
  cannot quietly return
- collapsed `construction/certification/query/basis_preview_parity.rs` so it
  carries replay parity as a canonical sub-artifact instead of cloning the
  replay/runtime-truth fields into another report bag
- collapsed `construction/certification/query/inspection_parity.rs` and
  `construction/certification/query/projection_consumption_receipt.rs` so they
  carry canonical admitted runtime truth directly instead of duplicating the
  admitted runtime-truth field bag
- added a deletion proof so those query parity shelves cannot quietly
  reintroduce copied realized/runtime-truth field storage
- collapsed `construction/certification/query/graph_composition_parity.rs` so
  it also reads mutation surface and required family posture through canonical
  admitted runtime truth instead of storing copied owner slices
- added a deletion proof so graph-composition parity cannot quietly reintroduce
  copied admitted-runtime owner slices
- collapsed `construction/certification/corpus/execution/proof_ingredients.rs`
  onto canonical replay parity plus a branch-basis digest instead of a second
  bespoke corpus execution runtime-truth bag
- collapsed `construction/certification/corpus/replay_siege_row.rs` so
  replay-siege rows now carry canonical execution proof ingredients instead of
  another copied runtime-truth field bag
- rewired `construction/certification/corpus/replay_siege_builder.rs` onto the
  nested execution carrier and tightened `construction/tests/legacy_deletion.rs`
  so both deleted copied-field bags cannot quietly return
- verified the corpus execution + replay-siege row collapse with:
  `cargo fmt -p worth-kernel`,
  `cargo test -p worth-kernel construction::certification::corpus::tests::replay_siege -- --nocapture`,
  `cargo test -p worth-kernel construction::certification::corpus::tests::realization_classes -- --nocapture`,
  `cargo test -p worth-kernel construction::tests::legacy_deletion -- --nocapture`,
  and `cargo check -p worth-kernel -p worth-spatial`
- collapsed `construction/certification/corpus/compound/rows.rs` so compound
  rows now keep only row-owned digests plus an optional canonical execution
  carrier instead of another copied realization/rejection field bag
- rewired `construction/certification/corpus/compound/row_builder.rs` so real
  scenario rows carry `Some(execution)` and the synthetic mixed-topology batch
  row stays explicitly `None`
- relaxed the compound lane/report/parity container derives from `Eq` to
  `PartialEq` where they now honestly contain canonical runtime artifacts
- tightened `construction/tests/legacy_deletion.rs` so the deleted compound row
  copied field bag cannot quietly return
- corrected the compound closeout proof so it asserts honest branch-basis
  sharing instead of pretending branch-basis digests are scenario-unique
- verified the compound row collapse with:
  `cargo fmt -p worth-kernel`,
  `cargo test -p worth-kernel construction::certification::corpus::compound::tests::reports -- --nocapture`,
  `cargo test -p worth-kernel construction::certification::corpus::compound::tests::parity_reports -- --nocapture`,
  `cargo test -p worth-kernel construction::certification::corpus::compound::tests::closeout_reports -- --nocapture`,
  `cargo test -p worth-kernel construction::tests::legacy_deletion -- --nocapture`,
  and `cargo check -p worth-kernel -p worth-spatial`
- deleted the replay-side `PrimitiveConstructionCorpusAuthoringOrderRow` wrapper
  shelf and replaced it with real corpus authoring lanes in
  `construction/certification/corpus/ordering/corpus_authoring_lane.rs`
- rewired `construction/certification/corpus/replay_siege.rs` and
  `construction/certification/corpus/replay_siege_report.rs` to derive lane
  digests and matrix stability directly from those lanes instead of storing
  another summary row layer
- tightened `construction/tests/legacy_deletion.rs` so the deleted replay-side
  authoring-order row shelf cannot quietly return
- verified the replay authoring-lane cut with:
  `cargo fmt -p worth-kernel`,
  `cargo test -p worth-kernel construction::certification::corpus::tests::replay_siege -- --nocapture`,
  `cargo test -p worth-kernel construction::tests::legacy_deletion -- --nocapture`,
  and `cargo check -p worth-kernel -p worth-spatial`
- deleted `construction/certification/corpus/compound/ordering_scenario.rs`
  instead of keeping a second scenario-stability wrapper shelf over canonical
  compound lanes
- folded ordering-stability checks back into
  `construction/certification/corpus/compound/report.rs` as direct
  canonical-lane comparisons
- rewired `construction/certification/corpus/compound/parity/specialized_rows.rs`
  and `construction/certification/corpus/compound/tests/parity_reports.rs` to
  prove motion/grazing/exhaustion ordering stability straight from canonical
  lanes and rows
- added a deletion proof in `construction/tests/legacy_deletion.rs` so the
  deleted `ordering_scenario` shelf and its helper accessors cannot quietly
  return
- verified the compound ordering-scenario deletion with:
  `cargo fmt -p worth-kernel`,
  `cargo test -p worth-kernel construction::certification::corpus::compound::tests::parity_reports -- --nocapture`,
  `cargo test -p worth-kernel construction::tests::legacy_deletion -- --nocapture`,
  `cargo test -p worth-kernel construction::tests::construction -- --nocapture`,
  `cargo test -p worth-kernel --test public_api_contract -- --nocapture`,
  and `cargo check -p worth-kernel -p worth-spatial`
- collapsed `construction/certification/corpus/replay_siege_report.rs` so it
  no longer stores accepted/rejected counts, stored verification booleans, or
  a cached report digest
- rewired that replay siege report to derive coverage, digest uniqueness,
  authoring-order parity, counts, and digest directly from canonical rows plus
  corpus authoring lanes on demand
- simplified `construction/certification/corpus/replay_siege.rs` by deleting
  the dead canonical-digest compare path in replay authoring-lane construction
- added a deletion proof in `construction/tests/legacy_deletion.rs` so the
  replay-siege report cannot quietly regrow the deleted stored summary-field bag
- verified the replay-siege report storage collapse with:
  `cargo fmt -p worth-kernel`,
  `cargo test -p worth-kernel construction::certification::corpus::tests::replay_siege -- --nocapture`,
  `cargo test -p worth-kernel construction::tests::construction -- --nocapture`,
  `cargo test -p worth-kernel construction::tests::legacy_deletion -- --nocapture`,
  and `cargo check -p worth-kernel -p worth-spatial`
- deleted `construction/certification/corpus/ordering/corpus_authoring_lane.rs`
  instead of keeping a replay-side wrapper shelf around `lane enum + replay rows`
- collapsed the replay-side authoring-lane carrier down to the honest owner
  shape directly inside `construction/certification/corpus/replay_siege.rs` and
  `construction/certification/corpus/replay_siege_report.rs`
- kept only the shared digest helpers in
  `construction/certification/corpus/ordering/mod.rs`, since compound still
  legitimately consumes that logic too
- added a deletion proof in `construction/tests/legacy_deletion.rs` so the
  deleted replay-side authoring-lane wrapper shelf cannot quietly return
- verified the replay-side authoring-lane wrapper deletion with:
  `cargo fmt -p worth-kernel`,
  `cargo test -p worth-kernel construction::certification::corpus::tests::replay_siege -- --nocapture`,
  `cargo test -p worth-kernel construction::tests::construction -- --nocapture`,
  `cargo test -p worth-kernel construction::tests::legacy_deletion -- --nocapture`,
  and `cargo check -p worth-kernel -p worth-spatial`
- deleted `construction/certification/corpus/compound/authoring_lane.rs`
  instead of keeping a compound-side wrapper shelf around `lane enum + row vec`
- collapsed the compound-side lane carrier down to the honest owner shape in
  `construction/certification/corpus/compound/builder.rs` and
  `construction/certification/corpus/compound/report.rs`:
  `PrimitiveConstructionAdversarialAuthoringOrderLane` plus canonical
  `PrimitiveConstructionCompoundRow` vectors
- rewired `construction/certification/corpus/compound/parity/specialized_rows.rs`
  and the compound report/parity tests onto tuple-owned lanes instead of
  deleted wrapper methods
- removed the now-dead shared lane/matrix digest helpers from
  `construction/certification/corpus/ordering/mod.rs`
- added a deletion proof in `construction/tests/legacy_deletion.rs` so the
  deleted compound-side authoring-lane wrapper shelf cannot quietly return
- verified the compound authoring-lane wrapper deletion with:
  `cargo fmt -p worth-kernel`,
  `cargo test -p worth-kernel construction::certification::corpus::compound::tests::reports -- --nocapture`,
  `cargo test -p worth-kernel construction::certification::corpus::compound::tests::parity_reports -- --nocapture`,
  `cargo test -p worth-kernel construction::tests::construction -- --nocapture`,
  `cargo test -p worth-kernel construction::tests::legacy_deletion -- --nocapture`,
  `cargo test -p worth-kernel --test public_api_contract -- --nocapture`,
  and `cargo check -p worth-kernel -p worth-spatial`
- collapsed `construction/certification/corpus/replay_siege_report.rs` onto one
  canonical row owner instead of storing canonical replay rows twice
- deleted the duplicate replay-side `rows` storage and now derive canonical
  rows from the `PrimitiveConstructionCorpusAuthoringOrderLane::Canonical` lane
  inside `authoring_order_lanes`
- rewired `construction/certification/corpus/replay_siege.rs` so replay report
  construction no longer builds and stores a second canonical row bag alongside
  the lanes
- tightened `construction/tests/legacy_deletion.rs` so the replay report cannot
  quietly regain a duplicated
  `rows: Vec<PrimitiveConstructionCorpusReplaySiegeRow>` carrier or the old
  `new(rows, lanes)` construction shape
- verified the replay canonical-row-owner collapse with:
  `cargo fmt -p worth-kernel`,
  `cargo test -p worth-kernel construction::certification::corpus::tests::replay_siege -- --nocapture`,
  `cargo test -p worth-kernel construction::tests::legacy_deletion -- --nocapture`,
  `cargo test -p worth-kernel construction::tests::construction -- --nocapture`,
  and `cargo check -p worth-kernel -p worth-spatial`
- fixed `construction/certification/corpus/replay_siege_report.rs` so
  `authoring_order_matrix_stability_verified()` anchors on the named canonical
  lane instead of silently assuming vector position
- added an adversarial replay certification in
  `construction/certification/corpus/tests/replay_siege.rs` proving the replay
  report keeps the same canonical row meaning after lane reordering
- reverified the named-canonical replay cut with:
  `cargo fmt -p worth-kernel`,
  `cargo test -p worth-kernel construction::certification::corpus::tests::replay_siege -- --nocapture`,
  `cargo test -p worth-kernel construction::tests::legacy_deletion -- --nocapture`,
  `cargo test -p worth-kernel construction::tests::construction -- --nocapture`,
  and `cargo check -p worth-kernel -p worth-spatial`
- collapsed replay-side noncanonical lane storage again:
  `PrimitiveConstructionCorpusReplaySiegeReport` now stores canonical rows plus
  `PrimitiveConstructionCorpusAuthoringOrderLaneSummary` values instead of
  retaining full row vectors for every authoring-order lane
- rewired `construction/certification/corpus/replay_siege.rs` so authoring-order
  preparation lowers each lane to a summary artifact and reuses canonical rows
  for the canonical lane
- kept the named-canonical replay reorder proof green after the storage collapse
- verified the lean replay lane-summary cut with:
  `cargo fmt -p worth-kernel`,
  `cargo test -p worth-kernel construction::certification::corpus::tests::replay_siege -- --nocapture`,
  `cargo test -p worth-kernel construction::tests::legacy_deletion -- --nocapture`,
  `cargo test -p worth-kernel construction::tests::construction -- --nocapture`,
  `cargo test -p worth-kernel --test public_api_contract -- --nocapture`,
  and `cargo check -p worth-kernel -p worth-spatial`
- collapsed `construction/certification/query/basis_preview_parity.rs` so it
  no longer stores replay-owned `family` or a duplicated final
  `parity_verified` verdict
- kept only basis-preview-specific evidence there:
  branch/preview contract + admission digests, a basis-alignment bit, and the
  canonical `PrimitiveConstructionReplayParityReport`
- replay-owned family and final parity verdict now forward through the replay
  artifact instead of being stored a second time
- verified the basis-preview parity collapse with:
  `cargo fmt -p worth-kernel`,
  `cargo test -p worth-kernel construction::certification::query::basis_preview_parity::tests -- --nocapture`,
  `cargo test -p worth-kernel construction::tests::construction -- --nocapture`,
  `cargo test -p worth-kernel --test public_api_contract -- --nocapture`,
  and `cargo check -p worth-kernel -p worth-spatial`
- removed the dead `PrimitiveConstructionIntent::request_digest()` wrapper from
  `construction/phase_chain/intent.rs`
- removed the unused realization type reexports from
  `construction/certification/realization/mod.rs`; those warnings were residue,
  not a live owner seam
- reverified the cleanup with:
  `cargo fmt -p worth-kernel`,
  `cargo test -p worth-kernel construction::tests::legacy_deletion -- --nocapture`,
  `cargo test -p worth-kernel construction::tests::construction -- --nocapture`,
  and `cargo check -p worth-kernel -p worth-spatial`
- deleted `construction/certification/query/inspection_parity.rs`; that shelf
  was duplicating the same sanctioned query-read proof already carried by
  `projection_consumption_receipt.rs`
- rewired construction tests, simplex certification, and compound row building
  onto the surviving projection-consumption parity lane
- collapsed compound rows from separate `inspection_digest` +
  `projection_consumption_digest` carriers down to one `query_surface_digest`
- tightened `construction/tests/legacy_deletion.rs` so the deleted
  inspection-parity shelf cannot quietly return
- verified the shelf deletion and carrier collapse with:
  `cargo fmt -p worth-kernel`,
  `cargo test -p worth-kernel construction::certification::corpus::compound::tests::reports -- --nocapture`,
  `cargo test -p worth-kernel construction::certification::corpus::tests::simplex_ladder -- --nocapture`,
  `cargo test -p worth-kernel construction::tests::legacy_deletion -- --nocapture`,
  `cargo test -p worth-kernel construction::tests::construction -- --nocapture`,
  `cargo test -p worth-kernel --test public_api_contract -- --nocapture`,
  and `cargo check -p worth-kernel -p worth-spatial`
- deleted `construction/certification/query/graph_composition_parity.rs`; that shelf had become another copied admitted-runtime owner slice instead of a distinct proof owner
- rewired boundary registries and phase-five closeout coverage so both deleted query shelves stay out of the hostile audited file sets
- cleaned the two honest fallouts from the inspection/graph shelf deletions:
  - removed the dead `PrimitiveConstructionAdmittedRuntimeTruth::mutation_surface()` accessor from `construction/certification/runtime_truth.rs`
  - restored the missing `#[test]` on `phase_nine_construction_corpus_execution_no_longer_teaches_bespoke_runtime_truth_bag` in `construction/tests/legacy_deletion.rs`
- reverified the batch with:
  `cargo fmt -p worth-kernel`,
  `cargo test -p worth-kernel construction::tests::boundary -- --nocapture`,
  `cargo test -p worth-kernel construction::tests::legacy_deletion -- --nocapture`,
  `cargo test -p worth-kernel construction::tests::construction -- --nocapture`,
  `cargo test -p worth-kernel --test public_api_contract -- --nocapture`,
  and `cargo check -p worth-kernel -p worth-spatial`
- next honest seam moved deeper again: surviving replay/query/report owners around `construction/certification/parity.rs`, `construction/certification/query/projection_consumption_receipt.rs`, `construction/certification/corpus/replay_siege.rs`, and `construction/certification/corpus/replay_siege_report.rs`
- deleted the standalone replay-parity shelf:
  - removed `construction/certification/parity.rs`
  - removed `construction/certification/parity_tests.rs`
- rewired `construction/certification/corpus/execution/proof_ingredients.rs` onto canonical `PrimitiveConstructionCertificationRuntimeTruth` plus a direct replay-drift check instead of carrying `PrimitiveConstructionReplayParityReport`
- rewired `construction/certification/query/basis_preview_parity.rs` onto canonical runtime truth plus a direct replay-drift failure path instead of forwarding through a replay-parity report wrapper
- split the touched over-cap test owners so the batch stayed honest on workspace line-cap rules:
  - moved basis-preview tests to `construction/certification/query/basis_preview_parity_tests.rs`
  - moved query/report construction tests to `construction/tests/construction_query_reports.rs`
  - split `construction/tests/legacy_deletion.rs` into smaller `legacy_deletion*.rs` modules and updated the hostile proof surface to deny the deleted replay-parity shelf directly
- hostile grep after the cut only finds `PrimitiveConstructionReplayParityReport`, `prepare_primitive_construction_replay_parity_report`, and `mod parity;` inside the intentional deletion-proof strings, not live runtime code
- verified the replay-parity shelf deletion with:
  - `cargo fmt -p worth-kernel`
  - `cargo test -p worth-kernel construction::tests::legacy_deletion -- --nocapture`
  - `cargo test -p worth-kernel construction::tests::construction -- --nocapture`
  - `cargo test -p worth-kernel construction::certification::query::basis_preview_parity::tests -- --nocapture`
  - `cargo test -p worth-kernel construction::tests::boundary -- --nocapture`
  - `cargo test -p worth-kernel --test public_api_contract -- --nocapture`
  - `cargo test -p worth-spatial --test public_api_contract -- --nocapture`
  - `cargo check -p worth-kernel -p worth-spatial`
- next honest seam is no longer `construction/certification/parity.rs`; it moved to the remaining replay/query/report owners around `construction/certification/query/projection_consumption_receipt.rs`, `construction/certification/corpus/execution/proof_ingredients.rs`, `construction/certification/corpus/replay_siege.rs`, and `construction/certification/corpus/replay_siege_report.rs`
- deleted the projection-consumption report wrapper from
  `crates/worth-kernel/src/construction/certification/query/projection_consumption_receipt.rs`
  - removed `PrimitiveConstructionQueryProjectionConsumptionParityReport`
  - removed `prepare_primitive_construction_query_projection_consumption_parity_report(...)`
  - kept only direct runtime-truth validation plus
    `prepare_primitive_construction_query_projection_consumption_surface_digest(...)`
- rewired the remaining live consumers onto the digest/helper lane:
  - `crates/worth-kernel/src/construction/certification/corpus/compound/row_builder.rs`
  - `crates/worth-kernel/src/construction/certification/corpus/tests/simplex_ladder.rs`
  - `crates/worth-kernel/src/construction/tests/construction_query_reports.rs`
- tightened `crates/worth-kernel/src/construction/tests/legacy_deletion_query_shelves.rs`
  so the deleted projection-consumption report wrapper cannot quietly return
- broad verification passed after the cut:
  - `cargo fmt -p worth-kernel`
  - `cargo test -p worth-kernel construction::tests::legacy_deletion -- --nocapture`
  - `cargo test -p worth-kernel construction::tests::construction -- --nocapture`
  - `cargo test -p worth-kernel construction::certification::corpus::tests::simplex_ladder -- --nocapture`
  - `cargo test -p worth-kernel construction::certification::corpus::compound::tests::reports -- --nocapture`
  - `cargo test -p worth-kernel construction::certification::query::projection_consumption_receipt::tests -- --nocapture`
  - `cargo test -p worth-kernel construction::tests::boundary -- --nocapture`
  - `cargo test -p worth-kernel --test public_api_contract -- --nocapture`
  - `cargo test -p worth-spatial --test public_api_contract -- --nocapture`
  - `cargo check -p worth-kernel -p worth-spatial`
- hostile grep is clean for
  `PrimitiveConstructionQueryProjectionConsumptionParityReport` and
  `prepare_primitive_construction_query_projection_consumption_parity_report`;
  only the intentional deletion-proof strings still mention them
- next honest seam moved deeper again:
  - `crates/worth-kernel/src/construction/certification/corpus/execution/proof_ingredients.rs`
  - `crates/worth-kernel/src/construction/certification/corpus/replay_siege.rs`
  - `crates/worth-kernel/src/construction/certification/corpus/replay_siege_report.rs`
- deleted the corpus execution wrapper shelf:
  - removed `PrimitiveConstructionCorpusExecutionProofIngredients`
  - removed `prepare_corpus_execution_proof_ingredients(...)`
  - deleted `crates/worth-kernel/src/construction/certification/corpus/execution/proof_ingredients.rs`
- kept only the reusable branch-basis helper in
  `crates/worth-kernel/src/construction/certification/corpus/execution/mod.rs`
  as `prepare_branch_basis_digest(...)`
- rewired replay certification directly onto canonical runtime truth:
  - `crates/worth-kernel/src/construction/certification/corpus/replay_siege_builder.rs`
  - `crates/worth-kernel/src/construction/certification/corpus/replay_siege_row.rs`
- rewired compound certification directly onto canonical runtime truth:
  - `crates/worth-kernel/src/construction/certification/corpus/compound/row_builder.rs`
  - `crates/worth-kernel/src/construction/certification/corpus/compound/rows.rs`
- updated hostile proofs and boundary closeout for the new execution owner path:
  - `crates/worth-kernel/src/construction/tests/legacy_deletion_runtime_truth.rs`
  - `crates/worth-kernel/src/construction/tests/legacy_deletion_proof_and_query_shelves.rs`
  - `crates/worth-kernel/src/construction/certification/phase_five_boundary_closeout_tests.rs`
- verification passed after the cut:
  - `cargo fmt -p worth-kernel`
  - `cargo test -p worth-kernel construction::certification::corpus::tests::replay_siege -- --nocapture`
  - `cargo test -p worth-kernel construction::certification::corpus::compound::tests::reports -- --nocapture`
  - `cargo test -p worth-kernel construction::tests::legacy_deletion -- --nocapture`
  - `cargo test -p worth-kernel construction::tests::construction -- --nocapture`
  - `cargo test -p worth-kernel construction::tests::boundary -- --nocapture`
  - `cargo test -p worth-kernel --test public_api_contract -- --nocapture`
  - `cargo test -p worth-spatial --test public_api_contract -- --nocapture`
  - `cargo check -p worth-kernel -p worth-spatial`
- hostile grep is clean for:
  - `PrimitiveConstructionCorpusExecutionProofIngredients`
  - `prepare_corpus_execution_proof_ingredients`
  - `/construction/certification/corpus/execution/proof_ingredients.rs`
- next honest seam is narrower again:
  - `crates/worth-kernel/src/construction/certification/corpus/replay_siege.rs`
  - `crates/worth-kernel/src/construction/certification/corpus/replay_siege_report.rs`

- deleted the live replay report owner from
  `crates/worth-kernel/src/construction/certification/corpus/replay_siege.rs`
  by removing:
  - `PrimitiveConstructionCorpusReplaySiegeReport`
  - `prepare_primitive_construction_corpus_replay_siege(...)`
- replaced that shelf with direct owner preparation:
  - `prepare_primitive_construction_corpus_replay_rows(...)`
  - `prepare_primitive_construction_corpus_authoring_order_lane_digest_rows(...)`
- moved replay aggregation and derived convenience methods into
  `crates/worth-kernel/src/construction/certification/corpus/tests/support.rs`
  as the local `PrimitiveConstructionCorpusReplaySiegeView`
- then moved the remaining replay-derived coverage/count/report helpers out of
  live certification code too, so `replay_siege.rs` is now just row/lane
  preparation plus digest helpers
- tightened
  `crates/worth-kernel/src/construction/tests/legacy_deletion_replay_and_compound.rs`
  so the deleted replay report type and entrypoint cannot quietly return
- verification passed:
  - `cargo fmt -p worth-kernel`
  - `cargo test -p worth-kernel construction::certification::corpus::tests::replay_siege -- --nocapture`
  - `cargo test -p worth-kernel construction::certification::corpus::compound::tests::reports -- --nocapture`
  - `cargo test -p worth-kernel construction::tests::legacy_deletion -- --nocapture`
  - `cargo test -p worth-kernel construction::tests::construction -- --nocapture`
  - `cargo test -p worth-kernel construction::tests::boundary -- --nocapture`
  - `cargo test -p worth-kernel --test public_api_contract -- --nocapture`
  - `cargo test -p worth-spatial --test public_api_contract -- --nocapture`
  - `cargo check -p worth-kernel -p worth-spatial`
- hostile grep is clean for:
  - `PrimitiveConstructionCorpusReplaySiegeReport`
  - `prepare_primitive_construction_corpus_replay_siege`
  - `replay_siege_report`
- next honest seam moved deeper again:
  - `crates/worth-kernel/src/construction/certification/corpus/replay_siege_builder.rs`
  - `crates/worth-kernel/src/construction/certification/corpus/replay_siege_row.rs`
  - `crates/worth-kernel/src/construction/certification/corpus/compound/report.rs`
  - `crates/worth-kernel/src/construction/certification/corpus/compound/parity/bundle_verified.rs`

- deleted the live compound siege report owner:
  - removed `PrimitiveConstructionCompoundAdversarialSiegeReport`
  - removed `prepare_primitive_construction_compound_adversarial_siege_report(...)`
  - deleted `crates/worth-kernel/src/construction/certification/corpus/compound/report.rs`
- replaced that shelf with direct lane ownership in
  `crates/worth-kernel/src/construction/certification/corpus/compound/lanes.rs`
  via:
  - `PrimitiveConstructionCompoundAdversarialLanes`
  - free helpers for canonical-row lookup, coverage, parity, digest, and
    scenario stability
- rewired the live certification band onto lanes:
  - `construction/certification/corpus/compound/builder.rs`
  - `construction/certification/corpus/compound/parity/truth.rs`
  - `construction/certification/corpus/compound/parity/bundle_verified.rs`
  - `construction/certification/corpus/compound/parity/specialized_rows.rs`
- rewired the compound support/tests onto direct lanes:
  - `construction/certification/corpus/compound/tests/support.rs`
  - `construction/certification/corpus/compound/tests/reports.rs`
  - `construction/certification/corpus/compound/tests/parity_reports.rs`
  - `construction/certification/corpus/compound/tests/closeout_reports.rs`
- updated deletion proofs so the removed report shelf cannot quietly return:
  - `construction/tests/legacy_deletion_query_and_corpus.rs`
  - `construction/tests/legacy_deletion_replay_and_compound.rs`
- verification passed:
  - `cargo fmt -p worth-kernel`
  - `cargo test -p worth-kernel construction::certification::corpus::compound::tests::reports -- --nocapture`
  - `cargo test -p worth-kernel construction::certification::corpus::compound::tests::parity_reports -- --nocapture`
  - `cargo test -p worth-kernel construction::certification::corpus::compound::tests::closeout_reports -- --nocapture`
  - `cargo test -p worth-kernel construction::tests::legacy_deletion -- --nocapture`
  - `cargo test -p worth-kernel construction::tests::construction -- --nocapture`
  - `cargo test -p worth-kernel construction::tests::boundary -- --nocapture`
  - `cargo test -p worth-kernel --test public_api_contract -- --nocapture`
  - `cargo test -p worth-spatial --test public_api_contract -- --nocapture`
  - `cargo check -p worth-kernel -p worth-spatial`
- hostile grep is clean for:
  - `PrimitiveConstructionCompoundAdversarialSiegeReport`
  - `prepare_primitive_construction_compound_adversarial_siege_report`
  - `mod report;`
- next honest seam moved deeper again:
  - `crates/worth-kernel/src/construction/certification/corpus/replay_siege_builder.rs`
  - `crates/worth-kernel/src/construction/certification/corpus/replay_siege_row.rs`
  - `crates/worth-kernel/src/construction/certification/corpus/compound/row_builder.rs`
  - `crates/worth-kernel/src/construction/certification/corpus/compound/parity/bundle_verified.rs`

- took the next deeper compound parity cut too:
  - `PrimitiveConstructionCompoundParityReportBundle` no longer stores a cached
    top-level `report_digest`
  - verified `PrimitiveConstructionCompoundParityReport` no longer stores a
    `report_digest` payload field either; it now rederives the parity report
    digest from canonical siege + specialized rows on demand
  - deleted the dead bundle `report_digest()` method afterward so no warning-only
    residue remained
- reverified with:
  - `cargo check -p worth-kernel -p worth-spatial`
  - `cargo test -p worth-kernel construction::certification::corpus::compound::tests::parity_reports -- --nocapture`

- removed the replay-side cached breadth field bag:
  - `PrimitiveConstructionCorpusReplaySiegeRow` no longer stores
    `construction_breadth`, `birth_attachment_breadth`, or
    `certification_breadth`
  - replay breadth now derives directly from canonical runtime truth in
    `construction/certification/corpus/replay_siege_row.rs`
  - `construction/certification/corpus/replay_siege_builder.rs` no longer
    pushes the old triple breadth arguments into replay rows
  - tightened the hostile deletion proof in
    `construction/tests/legacy_deletion_replay_and_compound.rs` so the cached
    replay breadth bag cannot quietly return
- reverified the replay breadth collapse with:
  - `cargo fmt -p worth-kernel`
  - `cargo test -p worth-kernel construction::certification::corpus::tests::replay_siege -- --nocapture`
  - `cargo test -p worth-kernel construction::tests::legacy_deletion -- --nocapture`
  - `cargo check -p worth-kernel -p worth-spatial`
- next honest seam stayed deeper:
  - `crates/worth-kernel/src/construction/certification/corpus/replay_siege_builder.rs`
  - `crates/worth-kernel/src/construction/certification/corpus/compound/row_builder.rs`
  - `crates/worth-kernel/src/construction/certification/corpus/compound/parity/bundle_verified.rs`

- removed the duplicate compound-row digest bag:
  - `PrimitiveConstructionCompoundRow` no longer stores both `direct_digest`
    and `replay_digest`
  - compound rows now keep one canonical `outcome_digest`
  - rewired the mixed-topology aggregate row constructor in
    `construction/certification/corpus/compound/row_builder.rs` to the new
    owner shape
  - split the old 451-line `construction/certification/corpus/compound/rows.rs`
    shelf into:
    - `rows/siege_row.rs`
    - `rows/specialized_rows.rs`
    - `rows/mod.rs`
    so the line-cap debt is no longer being carried by that owner band
  - tightened `construction/tests/legacy_deletion_runtime_truth.rs` so the
    deleted duplicate digest bag cannot quietly return
- broader verification passed after the cut:
  - `cargo fmt -p worth-kernel`
  - `cargo test -p worth-kernel construction::certification::corpus::compound::tests::reports -- --nocapture`
  - `cargo test -p worth-kernel construction::certification::corpus::compound::tests::closeout_reports -- --nocapture`
  - `cargo test -p worth-kernel construction::tests::legacy_deletion -- --nocapture`
  - `cargo test -p worth-kernel construction::tests::construction -- --nocapture`
  - `cargo test -p worth-kernel construction::tests::boundary -- --nocapture`
  - `cargo test -p worth-kernel --test public_api_contract -- --nocapture`
  - `cargo test -p worth-spatial --test public_api_contract -- --nocapture`
  - `cargo check -p worth-kernel -p worth-spatial`
- next honest seam is still deeper in the same band:
  - `crates/worth-kernel/src/construction/certification/corpus/replay_siege_builder.rs`
  - `crates/worth-kernel/src/construction/certification/corpus/replay_siege_row.rs`
  - `crates/worth-kernel/src/construction/certification/corpus/compound/row_builder.rs`
  - `crates/worth-kernel/src/construction/certification/corpus/compound/rows.rs`
  - `crates/worth-kernel/src/construction/certification/corpus/compound/parity/bundle_verified.rs`

- removed the fake replay-row breadth taxonomy and stale digest alias:
  - `PrimitiveConstructionCorpusReplaySiegeRow` no longer exposes
    `birth_attachment_breadth()` or `certification_breadth()`
  - replay rows now expose only the honest canonical breadth surface:
    `construction_breadth()`
  - removed `direct_construction_digest()` in favor of `outcome_digest()`
  - updated replay certification tests and tightened
    `construction/tests/legacy_deletion_runtime_truth.rs` so the deleted fake
    breadth surfaces and alias cannot quietly return
- broader verification passed after the replay cut:
  - `cargo fmt -p worth-kernel`
  - `cargo test -p worth-kernel construction::certification::corpus::tests::replay_siege -- --nocapture`
  - `cargo test -p worth-kernel construction::tests::legacy_deletion -- --nocapture`
  - `cargo test -p worth-kernel construction::tests::construction -- --nocapture`
  - `cargo test -p worth-kernel construction::tests::boundary -- --nocapture`
  - `cargo test -p worth-kernel --test public_api_contract -- --nocapture`
  - `cargo test -p worth-spatial --test public_api_contract -- --nocapture`
  - `cargo check -p worth-kernel -p worth-spatial`
- next honest seam is deeper again:
  - `crates/worth-kernel/src/construction/certification/corpus/replay_siege_builder.rs`
  - `crates/worth-kernel/src/construction/certification/corpus/replay_siege_row.rs`
  - `crates/worth-kernel/src/construction/certification/corpus/compound/row_builder.rs`
  - `crates/worth-kernel/src/construction/certification/corpus/compound/rows/siege_row.rs`
  - `crates/worth-kernel/src/construction/certification/corpus/compound/parity/bundle_verified.rs`

- removed the fake compound-row three-breadth taxonomy:
  - `PrimitiveConstructionCompoundRow` no longer stores
    `birth_attachment_breadth` or `certification_breadth`
  - compound rows now expose only the honest canonical breadth owner:
    `construction_breadth()`
  - `construction/certification/corpus/compound/row_builder.rs` no longer
    pushes or aggregates the fake triple breadth bag
  - mixed-topology aggregate rows now sum only canonical breadth
  - tightened hostile deletion proofs in:
    - `construction/tests/legacy_deletion_replay_and_compound.rs`
    - `construction/tests/legacy_deletion_runtime_truth.rs`
- verification passed after the compound breadth-taxonomy cut:
  - `cargo fmt -p worth-kernel`
  - `cargo test -p worth-kernel construction::certification::corpus::compound::tests::reports -- --nocapture`
  - `cargo test -p worth-kernel construction::tests::legacy_deletion -- --nocapture`
  - `cargo check -p worth-kernel -p worth-spatial`
  - `cargo test -p worth-kernel construction::tests::construction -- --nocapture`
  - `cargo test -p worth-kernel construction::tests::boundary -- --nocapture`
  - `cargo test -p worth-kernel --test public_api_contract -- --nocapture`
  - `cargo test -p worth-spatial --test public_api_contract -- --nocapture`
- next honest seam narrowed again:
  - `crates/worth-kernel/src/construction/certification/corpus/replay_siege_builder.rs`
  - `crates/worth-kernel/src/construction/certification/corpus/replay_siege_row.rs`
  - `crates/worth-kernel/src/construction/certification/corpus/compound/parity/bundle_verified.rs`

- removed the replay-row semantic convenience facade from live certification code:
  - `PrimitiveConstructionCorpusReplaySiegeRow` no longer exports
    `PrimitiveConstructionCorpusOutcomeDisposition`
  - deleted replay-row pass-through helpers like:
    `birth_digest()`, `realization_strategy()`,
    `attempted_realization_strategies()`, `stability_class()`,
    `feature_conditioning_class()`, `support_normal_class()`,
    `normalization_disposition()`, `exhaustion_reason()`,
    `rejection_class()`, `rejection_locality()`, `blocking_boundary()`, and
    `construction_breadth()`
  - live replay rows now expose row identity plus canonical `runtime_truth()`
    instead of a certification-local semantic facade
  - rehomed the deleted convenience layer into test-only support helpers in
    `construction/certification/corpus/tests/support.rs`
  - tightened `construction/tests/legacy_deletion_runtime_truth.rs` so the
    replay-row facade cannot quietly return
- verification passed after the replay-row facade collapse:
  - `cargo fmt -p worth-kernel`
  - `cargo test -p worth-kernel construction::certification::corpus::tests::replay_siege -- --nocapture`
  - `cargo test -p worth-kernel construction::certification::corpus::tests::realization_classes -- --nocapture`
  - `cargo test -p worth-kernel construction::certification::corpus::tests::family_boundaries -- --nocapture`
  - `cargo test -p worth-kernel construction::certification::corpus::tests::simplex_ladder -- --nocapture`
  - `cargo test -p worth-kernel construction::tests::legacy_deletion -- --nocapture`
  - `cargo test -p worth-kernel construction::tests::construction -- --nocapture`
  - `cargo test -p worth-kernel --test public_api_contract -- --nocapture`
  - `cargo check -p worth-kernel -p worth-spatial`
- next honest seam narrowed again:
  - `crates/worth-kernel/src/construction/certification/corpus/replay_siege_builder.rs`
  - `crates/worth-kernel/src/construction/certification/corpus/compound/parity/bundle_verified.rs`

- removed the compound parity bundle wrapper shelf:
  - deleted `PrimitiveConstructionCompoundParityReportBundle`
  - `verify_bundle(...)` now takes `siege`, `motion_rows`, `grazing_rows`, and
    `exhaustion_rows` directly instead of a wrapper noun
  - rewired:
    - `construction/certification/corpus/compound/builder.rs`
    - `construction/certification/corpus/compound/parity/mod.rs`
    - `construction/certification/corpus/compound/parity/bundle_verified.rs`
    - `construction/certification/corpus/compound/tests/parity_reports.rs`
  - tightened `construction/tests/legacy_deletion_replay_and_compound.rs` so
    the bundle wrapper shelf and old `verify_bundle(bundle)` shape cannot
    quietly return
- verification passed after the parity-bundle deletion:
  - `cargo fmt -p worth-kernel`
  - `cargo test -p worth-kernel construction::certification::corpus::compound::tests::parity_reports -- --nocapture`
  - `cargo test -p worth-kernel construction::certification::corpus::compound::tests::reports -- --nocapture`
  - `cargo test -p worth-kernel construction::tests::legacy_deletion -- --nocapture`
  - `cargo test -p worth-kernel construction::tests::construction -- --nocapture`
  - `cargo test -p worth-kernel construction::tests::boundary -- --nocapture`
  - `cargo test -p worth-kernel --test public_api_contract -- --nocapture`
  - `cargo test -p worth-spatial --test public_api_contract -- --nocapture`
  - `cargo check -p worth-kernel -p worth-spatial`
- next honest seam narrowed again:
  - `crates/worth-kernel/src/construction/certification/corpus/replay_siege_builder.rs`
  - remaining surviving payload/report ownership in
    `crates/worth-kernel/src/construction/certification/corpus/compound/parity/bundle_verified.rs`

- removed the production replay self-check runtime-truth lane:
  - `construction/certification/corpus/replay_siege_builder.rs` no longer
    computes canonical runtime truth twice or emits
    `PrimitiveConstructionCorpusReplaySiegeError::ReplayParityDrift`
  - `construction/certification/corpus/compound/row_builder.rs` no longer
    computes canonical runtime truth twice or emits
    `PrimitiveConstructionCompoundAdversarialSiegeError::ReplayParityDrift`
  - `construction/certification/query/basis_preview_parity.rs` no longer
    computes canonical runtime truth twice or emits
    `PrimitiveConstructionQueryBasisPreviewParityError::ReplayDrift`
  - tightened hostile deletion proofs in:
    - `construction/tests/legacy_deletion_query_and_corpus.rs`
    - `construction/tests/legacy_deletion_replay_and_compound.rs`
- verification passed after the self-check deletion:
  - `cargo fmt -p worth-kernel`
  - `cargo test -p worth-kernel construction::certification::corpus::tests::replay_siege -- --nocapture`
  - `cargo test -p worth-kernel construction::certification::corpus::compound::tests::reports -- --nocapture`
  - `cargo test -p worth-kernel construction::certification::corpus::compound::tests::parity_reports -- --nocapture`
  - `cargo test -p worth-kernel construction::tests::legacy_deletion -- --nocapture`
  - `cargo test -p worth-kernel construction::tests::construction -- --nocapture`
  - `cargo test -p worth-kernel construction::tests::boundary -- --nocapture`
  - `cargo test -p worth-kernel --test public_api_contract -- --nocapture`
  - `cargo test -p worth-spatial --test public_api_contract -- --nocapture`
  - `cargo check -p worth-kernel -p worth-spatial`
- hostile grep is clean for the deleted production self-check seam; the only
  remaining hits for `ReplayParityDrift`, `ReplayDrift`, and the cloned
  `replay_truth` computation are the intentional deletion-proof strings
- next honest seam moved again:
  - `crates/worth-kernel/src/construction/certification/query/basis_preview_parity.rs`
  - `crates/worth-kernel/src/construction/certification/corpus/compound/parity/bundle_verified.rs`

- deleted the stale test-owned branch-preview parity shelf from the production
  certification/query tree:
  - removed `construction/certification/query/basis_preview_parity.rs`
  - removed `construction/certification/query/basis_preview_parity_tests.rs`
  - removed `mod basis_preview_parity;` from
    `construction/certification/query/mod.rs`
- rehomed the surviving branch-preview basis proof behavior into construction
  test support:
  - `construction/tests/support/branch_preview_basis.rs`
  - `construction/tests/branch_preview_basis.rs`
  - `construction/tests/construction_query_reports.rs`
- retargeted the hostile boundary/deletion owners so production no longer
  path-depends on the deleted certification/query shelf:
  - `construction/certification/phase_five_boundary_closeout_tests.rs`
  - `construction/tests/boundary_phase_five/sources.rs`
  - `construction/tests/legacy_deletion_runtime_truth.rs`
  - `construction/tests/legacy_deletion_query_and_corpus.rs`
  - `construction/tests/legacy_deletion_proof_and_query_shelves.rs`
- verification passed after the branch-preview shelf deletion:
  - `cargo fmt -p worth-kernel`
  - `cargo test -p worth-kernel construction::tests::branch_preview_basis -- --nocapture`
  - `cargo test -p worth-kernel construction::tests::construction -- --nocapture`
  - `cargo test -p worth-kernel construction::tests::legacy_deletion -- --nocapture`
  - `cargo test -p worth-kernel construction::tests::boundary -- --nocapture`
  - `cargo test -p worth-kernel --test public_api_contract -- --nocapture`
  - `cargo test -p worth-spatial --test public_api_contract -- --nocapture`
  - `cargo check -p worth-kernel -p worth-spatial`
- hostile grep is clean for the deleted production shelf:
  - no live `prepare_primitive_construction_query_basis_preview_parity_report`
  - no live `PrimitiveConstructionQueryBasisPreviewParityReport`
  - no live `basis_preview_parity.rs` module outside intentional negative
    fixtures and deletion-proof strings
- next honest seam moved again:
  - `crates/worth-kernel/src/construction/certification/corpus/compound/mod.rs`
  - `crates/worth-kernel/src/construction/certification/corpus/compound/builder.rs`
  - `crates/worth-kernel/src/construction/certification/corpus/compound/parity/bundle_verified.rs`
  - `crates/worth-kernel/src/construction/certification/corpus/compound/parity/truth.rs`
- new blocker truth:
  the compound parity/report band now looks like test-only behavior whose
  entrypoints are already `#[cfg(test)]`, but the production certification tree
  still compiles the machinery. That is the next honest hard-break seam.

- pushed the compound certification/parity band behind test-only module gates at
  the corpus roots:
  - `construction/certification/corpus/mod.rs`
  - `construction/certification/corpus/ordering/mod.rs`
  - `construction/certification/corpus/parity/mod.rs`
- ordinary `cargo check` no longer compiles:
  - `corpus::compound`
  - compound-specific corpus ordering lane helpers
  - compound-specific specialized-row parity helpers
- the hostile compound proof band still runs through the same test-owned
  surfaces, so this cut removed production ballast without deleting the proof
  machinery yet
- verification passed after the compound module-root quarantine:
  - `cargo fmt -p worth-kernel`
  - `cargo test -p worth-kernel construction::certification::corpus::compound::tests::reports -- --nocapture`
  - `cargo test -p worth-kernel construction::certification::corpus::compound::tests::parity_reports -- --nocapture`
  - `cargo test -p worth-kernel construction::certification::corpus::compound::tests::closeout_reports -- --nocapture`
  - `cargo test -p worth-kernel construction::tests::legacy_deletion -- --nocapture`
  - `cargo check -p worth-kernel -p worth-spatial`
- next honest seam moved deeper again:
  - `crates/worth-kernel/src/construction/certification/corpus/compound/builder.rs`
  - `crates/worth-kernel/src/construction/certification/corpus/compound/parity/bundle_verified.rs`
  - `crates/worth-kernel/src/construction/certification/corpus/compound/parity/truth.rs`
- updated blocker truth:
  the compound certification band is no longer production-compiled by default,
  but the remaining test/certification carriers still look heavier than the
  direct lane/truth owners they sit on top of. That is the next honest
  hard-break seam.

- removed the synthetic compound parity canonical-truth shelf:
  - deleted `construction/certification/corpus/compound/parity/truth.rs`
  - deleted `PrimitiveConstructionCompoundParityCanonicalTruth`
  - `PrimitiveConstructionCompoundParityReport` now stores only `siege`
  - `PrimitiveConstructionCompoundParityVerificationFailure` now stores only
    `siege`
  - `verify_bundle(...)` now compares motion/grazing/exhaustion rows directly
    against rederived canonical rows from siege instead of going through a
    second truth wrapper noun
  - rewired compound test support and compound parity/closeout tests to read
    expected motion/grazing/exhaustion inventory directly from
    `compound_parity_registry()`
- verification passed after the canonical-truth deletion:
  - `cargo fmt -p worth-kernel`
  - `cargo test -p worth-kernel construction::certification::corpus::compound::tests::reports -- --nocapture`
  - `cargo test -p worth-kernel construction::certification::corpus::compound::tests::parity_reports -- --nocapture`
  - `cargo test -p worth-kernel construction::certification::corpus::compound::tests::closeout_reports -- --nocapture`
  - `cargo test -p worth-kernel construction::tests::legacy_deletion -- --nocapture`
  - `cargo test -p worth-kernel construction::tests::construction -- --nocapture`
  - `cargo test -p worth-kernel construction::tests::boundary -- --nocapture`
  - `cargo test -p worth-kernel --test public_api_contract -- --nocapture`
  - `cargo test -p worth-spatial --test public_api_contract -- --nocapture`
  - `cargo check -p worth-kernel -p worth-spatial`
- next honest seam narrowed again:
  - `crates/worth-kernel/src/construction/certification/corpus/compound/builder.rs`
  - `crates/worth-kernel/src/construction/certification/corpus/compound/parity/bundle_verified.rs`
- updated blocker truth:
  the compound certification band is still test-only at the module roots, and
  the synthetic canonical-truth shelf is gone too. The remaining owner-richness
  question is now the surviving report/failure carrier logic in
  `bundle_verified.rs` plus any deeper builder-side duplication on top of the
  direct lane owners.

- deleted the remaining production-shaped compound parity wrapper shelf:
  - removed `construction/certification/corpus/compound/parity/bundle_verified.rs`
  - removed `prepare_primitive_construction_compound_parity_report(...)`
  - removed `PrimitiveConstructionCompoundParityReport`
  - removed `PrimitiveConstructionCompoundParityVerificationFailure`
  - removed `PrimitiveConstructionCompoundParityVerificationMismatch`
- live compound certification now owns only direct lane preparation and
  specialized-row derivation; the parity view and drift/failure helpers moved
  into explicit test support at
  `construction/certification/corpus/compound/tests/parity_view.rs`
- tightened
  `construction/tests/legacy_deletion_replay_and_compound.rs`
  so the deleted production-shaped parity wrapper shelf cannot quietly return
- verification passed after the compound parity wrapper deletion:
  - `cargo fmt -p worth-kernel`
  - `cargo test -p worth-kernel construction::certification::corpus::compound::tests::reports -- --nocapture`
  - `cargo test -p worth-kernel construction::certification::corpus::compound::tests::parity_reports -- --nocapture`
  - `cargo test -p worth-kernel construction::certification::corpus::compound::tests::closeout_reports -- --nocapture`
  - `cargo test -p worth-kernel construction::tests::legacy_deletion -- --nocapture`
  - `cargo test -p worth-kernel construction::tests::construction -- --nocapture`
  - `cargo test -p worth-kernel construction::tests::boundary -- --nocapture`
  - `cargo test -p worth-kernel --test public_api_contract -- --nocapture`
  - `cargo test -p worth-spatial --test public_api_contract -- --nocapture`
  - `cargo check -p worth-kernel -p worth-spatial`
- hostile grep is clean for the deleted parity wrapper shelf outside intentional
  deletion-proof strings and the new explicit test-support parity view
- next honest seam narrowed again:
  - `crates/worth-kernel/src/construction/certification/corpus/compound/builder.rs`
  - `crates/worth-kernel/src/construction/certification/corpus/compound/lanes.rs`
  - `crates/worth-kernel/src/construction/certification/corpus/compound/row_builder.rs`
  - `crates/worth-kernel/src/construction/certification/corpus/compound/parity/specialized_rows.rs`
- updated blocker truth:
  the compound certification band no longer has a production-shaped parity
  report/failure API at all. The next owner-richness question is the remaining
  direct lane and specialized-row helper surfaces, not another wrapper noun.

- deleted the remaining live compound specialized-row helper shelf:
  - removed `construction/certification/corpus/compound/parity/specialized_rows.rs`
  - removed live `build_motion_parity_rows_from_siege(...)`,
    `build_grazing_boundary_rows_from_siege(...)`, and
    `build_exhaustion_witness_parity_rows_from_siege(...)`
  - removed live `motion_*`, `grazing_*`, and `exhaustion_*` digest/parity
    helper exports from `construction/certification/corpus/compound/parity/mod.rs`
  - rebuilt those helpers in explicit test support:
    - `construction/certification/corpus/compound/tests/parity_support.rs`
    - `construction/certification/corpus/compound/tests/parity_view.rs`
- split the oversized replay/compound hostile proof owner to get back under the
  workspace line cap:
  - deleted `construction/tests/legacy_deletion_replay_and_compound.rs`
  - added `construction/tests/legacy_deletion_replay.rs`
  - added `construction/tests/legacy_deletion_compound.rs`
  - new compound deletion proof explicitly forbids the old live
    specialized-row helper shelf from returning
- verification passed after the helper-shelf deletion and hostile-proof split:
  - `cargo fmt -p worth-kernel`
  - `cargo test -p worth-kernel construction::tests::legacy_deletion -- --nocapture`
  - `cargo test -p worth-kernel construction::tests::construction -- --nocapture`
  - `cargo test -p worth-kernel construction::tests::boundary -- --nocapture`
  - `cargo test -p worth-kernel --test public_api_contract -- --nocapture`
  - `cargo test -p worth-spatial --test public_api_contract -- --nocapture`
  - `cargo check -p worth-kernel -p worth-spatial`
- blocker truth moved again:
  the old live specialized-row helper shelf is closed. The next honest seam is
  deeper in the remaining compound direct-lane and row builders, not the old
  builder/parity helper exports.

- deleted the remaining live compound lane report/coverage helper shelf:
  - removed `construction/certification/corpus/compound/lanes.rs`
  - moved `PrimitiveConstructionCompoundAdversarialLanes` into
    `construction/certification/corpus/compound/builder.rs`
  - rehomed canonical-lane, coverage, digest, and parity helpers into explicit
    test support at
    `construction/certification/corpus/compound/tests/lane_support.rs`
  - rewired compound report/parity/closeout test surfaces and parity support to
    that new owner
- corrected the hostile deletion proofs to match the new ownership truth:
  - `construction/tests/legacy_deletion_compound.rs`
  - `construction/tests/legacy_deletion_query_and_corpus.rs`
  - no stale `include_str!` references to deleted `compound/lanes.rs` remain
- verification passed after the lane-helper deletion:
  - `cargo fmt -p worth-kernel`
  - `cargo test -p worth-kernel construction::certification::corpus::compound::tests::reports -- --nocapture`
  - `cargo test -p worth-kernel construction::certification::corpus::compound::tests::parity_reports -- --nocapture`
  - `cargo test -p worth-kernel construction::certification::corpus::compound::tests::closeout_reports -- --nocapture`
  - `cargo test -p worth-kernel construction::tests::legacy_deletion -- --nocapture`
  - `cargo test -p worth-kernel construction::tests::construction -- --nocapture`
  - `cargo test -p worth-kernel construction::tests::boundary -- --nocapture`
  - `cargo test -p worth-kernel --test public_api_contract -- --nocapture`
  - `cargo test -p worth-spatial --test public_api_contract -- --nocapture`
  - `cargo check -p worth-kernel -p worth-spatial`
- blocker truth moved again:
  the old live lane report/coverage helper shelf is closed too. The next
  honest seam is deeper in the remaining direct lane preparation and row
  builders, not the deleted `lanes.rs` owner band.

- deleted the synthetic mixed-topology batch row from the live compound path:
  - removed `mixed_topology_batch_row(...)` from
    `construction/certification/corpus/compound/row_builder.rs`
  - stopped `build_rows_for_lane(...)` from fabricating a summary row on top of
    direct scenario runtime truth
  - removed `MixedTopologyClassBatch`, `MixedBatch`, and `MixedTopologyBatch`
    from `construction/certification/corpus/compound/schema.rs`
  - removed the synthetic required-scenario inventory entry from
    `construction/certification/corpus/compound/parity/registry.rs`
  - updated the closeout/report expectations and added a hostile deletion proof
    in `construction/tests/legacy_deletion_compound.rs`
- cleaned the immediate fallout:
  - adjusted the closeout proof to the real six-family branch-basis uniqueness
    count after the synthetic row removal
  - removed the now-dead compound `construction_breadth()` accessor from
    `construction/certification/corpus/compound/rows/siege_row.rs`
- verification passed after the synthetic batch-row deletion:
  - `cargo fmt -p worth-kernel`
  - `cargo test -p worth-kernel construction::certification::corpus::compound::tests::reports -- --nocapture`
  - `cargo test -p worth-kernel construction::certification::corpus::compound::tests::parity_reports -- --nocapture`
  - `cargo test -p worth-kernel construction::certification::corpus::compound::tests::closeout_reports -- --nocapture`
  - `cargo test -p worth-kernel construction::tests::legacy_deletion -- --nocapture`
  - `cargo test -p worth-kernel construction::tests::construction -- --nocapture`
  - `cargo test -p worth-kernel construction::tests::boundary -- --nocapture`
  - `cargo test -p worth-kernel --test public_api_contract -- --nocapture`
  - `cargo test -p worth-spatial --test public_api_contract -- --nocapture`
  - `cargo check -p worth-kernel -p worth-spatial`
- blocker truth moved again:
  the synthetic mixed-topology batch row is gone too. The next honest seam is
  deeper in the remaining direct row/runtime carrier in the compound band, not
  another helper or synthetic summary shelf.

- deleted the remaining row-level semantic facade from the live compound direct
  carrier:
  - `construction/certification/corpus/compound/rows/siege_row.rs` no longer
    exposes `realization_strategy()`, `attempted_realization_strategies()`,
    `stability_class()`, `exhaustion_reason()`, `rejection_class()`,
    `rejection_locality()`, or `blocking_boundary()`
  - the live row now only exposes canonical `runtime_truth()` for that truth
    seam
- rehomed the deleted semantic decoding into explicit test support at
  `construction/certification/corpus/compound/tests/row_support.rs`
- rewired compound report/parity/closeout/support owners onto that explicit
  row-support shim:
  - `compound/tests/reports.rs`
  - `compound/tests/closeout_reports.rs`
  - `compound/tests/parity_reports.rs`
  - `compound/tests/parity_support.rs`
  - `compound/tests/lane_support.rs`
- tightened `construction/tests/legacy_deletion_runtime_truth.rs` so the
  deleted row-level semantic facade cannot quietly return
- focused verification passed for the row-facade cut:
  - `cargo fmt -p worth-kernel`
  - `cargo test -p worth-kernel construction::certification::corpus::compound::tests::reports -- --nocapture`
  - `cargo test -p worth-kernel construction::certification::corpus::compound::tests::parity_reports -- --nocapture`
  - `cargo test -p worth-kernel construction::certification::corpus::compound::tests::closeout_reports -- --nocapture`
  - `cargo test -p worth-kernel construction::tests::legacy_deletion -- --nocapture`
- broader verification passed too:
  - `cargo test -p worth-kernel construction::tests::construction -- --nocapture`
  - `cargo test -p worth-kernel construction::tests::boundary -- --nocapture`
  - `cargo test -p worth-kernel --test public_api_contract -- --nocapture`
  - `cargo test -p worth-spatial --test public_api_contract -- --nocapture`
  - `cargo check -p worth-kernel -p worth-spatial`
- blocker truth moved again:
  the row-level semantic facade is closed. The next honest seam is the
  remaining direct row/runtime carrier storage inside
  `construction/certification/corpus/compound/row_builder.rs` and
  `construction/certification/corpus/compound/rows/siege_row.rs`, not another
  helper shelf.

- took the deeper direct-carrier storage cut immediately after that:
  - `construction/certification/corpus/compound/rows/siege_row.rs` no longer
    stores `motion_kind`, `motion_digest`, `grazing_kind`, `grazing_digest`,
    `construction_breadth`, or `outcome_digest`
  - the live row no longer wraps canonical runtime truth in `Option`; it now
    stores `PrimitiveConstructionCertificationRuntimeTruth` directly
  - row-level motion/grazing/outcome decoding now lives only in explicit test
    support at `construction/certification/corpus/compound/tests/row_support.rs`
  - row-digest shaping moved out of `rows/siege_row.rs` and into
    `construction/certification/corpus/compound/row_builder.rs`, so the row
    owner file no longer even names the deleted copied payload bag
- rewired the live hostile compound test band to the new ownership truth:
  - `compound/tests/reports.rs`
  - `compound/tests/closeout_reports.rs`
  - `compound/tests/parity_support.rs`
  - `compound/tests/parity_reports.rs`
  - `compound/tests/lane_support.rs`
  - `construction/tests/legacy_deletion_runtime_truth.rs`
- focused verification passed after the deeper carrier cut:
  - `cargo fmt -p worth-kernel`
  - `cargo test -p worth-kernel construction::certification::corpus::compound::tests::reports -- --nocapture`
  - `cargo test -p worth-kernel construction::certification::corpus::compound::tests::parity_reports -- --nocapture`
  - `cargo test -p worth-kernel construction::certification::corpus::compound::tests::closeout_reports -- --nocapture`
  - `cargo test -p worth-kernel construction::tests::legacy_deletion -- --nocapture`
- broader verification passed again after that cut:
  - `cargo test -p worth-kernel construction::tests::construction -- --nocapture`
  - `cargo test -p worth-kernel construction::tests::boundary -- --nocapture`
  - `cargo test -p worth-kernel --test public_api_contract -- --nocapture`
  - `cargo test -p worth-spatial --test public_api_contract -- --nocapture`
  - `cargo check -p worth-kernel -p worth-spatial`
- blocker truth moved again:
  the remaining direct compound carrier seam is now narrower: mostly the stored
  `branch_basis_digest`, `query_surface_digest`, and any still-live digest
  shaping support in `construction/certification/corpus/compound/row_builder.rs`
  and `construction/certification/corpus/compound/rows/siege_row.rs`.

- removed the last stored digest sidecars from the live compound row:
  - `construction/certification/corpus/compound/rows/siege_row.rs` no longer
    stores `branch_basis_digest` or `query_surface_digest`
  - those digests now rederive only in explicit test support at
    `construction/certification/corpus/compound/tests/row_support.rs`
  - the live row is now down to identity fields, canonical runtime truth, and
    `row_digest`
- rewired the remaining hostile compound test band that consumed those sidecars:
  - `compound/tests/reports.rs`
  - `compound/tests/closeout_reports.rs`
  - `construction/tests/legacy_deletion_runtime_truth.rs`
- focused verification passed after removing the stored branch/query sidecars:
  - `cargo fmt -p worth-kernel`
  - `cargo test -p worth-kernel construction::certification::corpus::compound::tests::reports -- --nocapture`
  - `cargo test -p worth-kernel construction::certification::corpus::compound::tests::parity_reports -- --nocapture`
  - `cargo test -p worth-kernel construction::certification::corpus::compound::tests::closeout_reports -- --nocapture`
  - `cargo test -p worth-kernel construction::tests::legacy_deletion -- --nocapture`
- broader verification passed again after that cut:
  - `cargo test -p worth-kernel construction::tests::construction -- --nocapture`
  - `cargo test -p worth-kernel construction::tests::boundary -- --nocapture`
  - `cargo test -p worth-kernel --test public_api_contract -- --nocapture`
  - `cargo test -p worth-spatial --test public_api_contract -- --nocapture`
  - `cargo check -p worth-kernel -p worth-spatial`
- blocker truth moved again:
  the remaining direct compound carrier seam is now mostly the row-digest
  shaping and rederivation support still living in
  `construction/certification/corpus/compound/row_builder.rs`, not stored row
  sidecars anymore.

- finished that direct row-digest cut:
  - `construction/certification/corpus/compound/rows/siege_row.rs` no longer
    stores `row_digest`
  - `construction/certification/corpus/compound/tests/row_support.rs` now
    rederives row digest only in hostile test support
  - `prepare_primitive_construction_compound_adversarial_lanes()` and
    `prepare_compound_parity_view()` are both now no-arg, with the stale
    workspace thread removed from the compound hostile parity/report band
  - `construction/tests/legacy_deletion_runtime_truth.rs` now forbids stored
    live `row_digest` from returning
  - removed the dead `Projection` variant from
    `construction/certification/corpus/compound/builder.rs`, so the band is
    warning-clean again
- verification passed after the row-digest/workspace cleanup:
  - `cargo fmt -p worth-kernel`
  - `cargo test -p worth-kernel construction::certification::corpus::compound::tests::reports -- --nocapture`
  - `cargo test -p worth-kernel construction::certification::corpus::compound::tests::parity_reports -- --nocapture`
  - `cargo test -p worth-kernel construction::certification::corpus::compound::tests::closeout_reports -- --nocapture`
  - `cargo test -p worth-kernel construction::tests::legacy_deletion -- --nocapture`
  - `cargo test -p worth-kernel construction::tests::construction -- --nocapture`
  - `cargo test -p worth-kernel construction::tests::boundary -- --nocapture`
  - `cargo test -p worth-kernel --test public_api_contract -- --nocapture`
  - `cargo test -p worth-spatial --test public_api_contract -- --nocapture`
  - `cargo check -p worth-kernel -p worth-spatial`
- blocker truth moved again:
  the remaining live compound seam is no longer stored row payload or stale
  workspace plumbing. It is the still-live motion/grazing digest derivation in
  `construction/certification/corpus/compound/row_builder.rs` and any other
  logic there that still deserves to collapse into explicit hostile test
  support.

- finished that live digest-helper cut too:
  - `construction/certification/corpus/compound/row_builder.rs` no longer owns
    `requested_motion_digest(...)`, `grazing_digest(...)`, or the supporting
    frame/math helpers
  - live compound row building now only lowers scenario -> canonical runtime
    truth -> direct row
  - those motion/grazing digest helpers now live only in explicit hostile test
    support at `construction/certification/corpus/compound/tests/row_support.rs`
  - `construction/tests/legacy_deletion_compound.rs` now forbids that live
    helper shelf from returning
- verification passed after the live digest-helper removal:
  - `cargo fmt -p worth-kernel`
  - `cargo test -p worth-kernel construction::certification::corpus::compound::tests::reports -- --nocapture`
  - `cargo test -p worth-kernel construction::certification::corpus::compound::tests::parity_reports -- --nocapture`
  - `cargo test -p worth-kernel construction::certification::corpus::compound::tests::closeout_reports -- --nocapture`
  - `cargo test -p worth-kernel construction::tests::legacy_deletion -- --nocapture`
  - `cargo test -p worth-kernel construction::tests::construction -- --nocapture`
  - `cargo test -p worth-kernel construction::tests::boundary -- --nocapture`
  - `cargo test -p worth-kernel --test public_api_contract -- --nocapture`
  - `cargo test -p worth-spatial --test public_api_contract -- --nocapture`
  - `cargo check -p worth-kernel -p worth-spatial`
- blocker truth moved again:
  the live compound row-builder path is no longer the blocker. The next honest
  question is whether the remaining compound hostile test-support rederivation
  is the right final certification shape, or whether the next meaningful
  query-native cut now lives elsewhere in the remaining production geometry
  runtime story.

- took the next real replay-side hard-break cut:
  - `construction/certification/corpus/replay_siege_row.rs` no longer stores
    live `row_digest`
  - `construction/certification/corpus/replay_siege.rs` no longer exports the
    live lane-digest matrix helper or
    `PrimitiveConstructionCorpusAuthoringOrderLaneDigestRow`
  - `construction/certification/corpus/replay_siege_builder.rs` no longer
    computes branch-basis digest just to feed a stored replay row digest
- rehomed that replay digest/lane-matrix logic into explicit hostile test
  support at:
  - `construction/certification/corpus/tests/replay_digest_support.rs`
  - `construction/certification/corpus/tests/support.rs`
- rewired replay/family/simplex hostile tests onto the test-owned replay digest
  surface:
  - `construction/certification/corpus/tests/replay_siege.rs`
  - `construction/certification/corpus/tests/replay_siege_rejections.rs`
  - `construction/certification/corpus/tests/family_boundaries.rs`
  - `construction/certification/corpus/tests/simplex_ladder.rs`
- tightened `construction/tests/legacy_deletion_replay.rs` so live replay code
  cannot quietly reintroduce stored `row_digest` or live lane-digest helpers
- verification passed after the replay cut:
  - `cargo fmt -p worth-kernel`
  - `cargo test -p worth-kernel construction::certification::corpus::tests::replay_siege -- --nocapture`
  - `cargo test -p worth-kernel construction::certification::corpus::tests::replay_siege_rejections -- --nocapture`
  - `cargo test -p worth-kernel construction::tests::legacy_deletion -- --nocapture`
  - `cargo test -p worth-kernel construction::tests::construction -- --nocapture`
  - `cargo test -p worth-kernel construction::tests::boundary -- --nocapture`
  - `cargo test -p worth-kernel --test public_api_contract -- --nocapture`
  - `cargo test -p worth-spatial --test public_api_contract -- --nocapture`
  - `cargo check -p worth-kernel -p worth-spatial`
- blocker truth moved again:
  the live replay cached-digest band is no longer the blocker either. The next
  honest question is whether the remaining hostile replay/compound test-support
  rederivation is the right final certification shape, or whether the next
  meaningful query-native cut now lives in a different surviving production
  seam elsewhere in the geometry runtime story.

- took the next broader realization-side removal:
  - deleted:
    - `construction/certification/realization/strategy_report.rs`
    - `construction/certification/realization/stability_class_report.rs`
    - `construction/certification/realization/conditioning_witness_report.rs`
    - `construction/certification/realization/exhaustion_report.rs`
  - `construction/certification/realization/mod.rs` no longer teaches those
    report files as live realization modules
- rehomed the surviving realization report behavior into explicit hostile test
  support at:
  - `construction/certification/realization/tests/report_support.rs`
  - with one shared `PrimitiveConstructionRealizationReportView` over
    `prepare_realization_snapshot(...)`
- split the oversized realization report test file so the touched test band is
  back under the workspace cap:
  - `construction/certification/realization/tests/reports.rs`
  - `construction/certification/realization/tests/reports_witness_inventory.rs`
- tightened `construction/tests/legacy_deletion_proof_and_query_shelves.rs` so
  the deleted realization report file shelves cannot quietly return
- verification passed after the realization cut:
  - `cargo fmt -p worth-kernel`
  - `cargo test -p worth-kernel construction::certification::realization::tests::reports -- --nocapture`
  - `cargo test -p worth-kernel construction::certification::corpus::tests::simplex_ladder -- --nocapture`
  - `cargo test -p worth-kernel construction::tests::legacy_deletion -- --nocapture`
  - `cargo test -p worth-kernel construction::tests::construction -- --nocapture`
  - `cargo test -p worth-kernel construction::tests::boundary -- --nocapture`
  - `cargo test -p worth-kernel --test public_api_contract -- --nocapture`
  - `cargo test -p worth-spatial --test public_api_contract -- --nocapture`
  - `cargo check -p worth-kernel -p worth-spatial`
- blocker truth moved again:
  the live realization report shelf is no longer the blocker either. The next
  honest production seams are the smaller certification carriers like
  `construction/certification/family_coverage.rs` and
  `construction/certification/query/projection_consumption_receipt.rs`, before
  spending another pass polishing test-only replay/realization/compound
  rederivation support.

- finished that smaller production-shelf cut too:
  - deleted `construction/certification/family_coverage.rs`
  - deleted `construction/certification/query/projection_consumption_receipt.rs`
  - deleted `construction/certification/query/mod.rs`
  - removed `mod family_coverage;` and `mod query;` from
    `construction/certification/mod.rs`
- rehomed the surviving hostile proof behavior into:
  - `construction/tests/support/family_coverage.rs`
  - `construction/tests/support/projection_consumption.rs`
- rewired the remaining consumers onto those honest test-support owners:
  - `construction/tests/construction.rs`
  - `construction/tests/construction_query_reports.rs`
  - `construction/certification/corpus/tests/simplex_ladder.rs`
  - `construction/certification/corpus/compound/tests/row_support.rs`
- retargeted the hostile boundary/deletion owners to the new truth:
  - `construction/tests/legacy_deletion_query_shelves.rs`
  - `construction/tests/legacy_deletion_query_and_corpus.rs`
  - `construction/tests/legacy_deletion_runtime_truth.rs`
  - `construction/tests/legacy_deletion_proof_and_query_shelves.rs`
  - `construction/tests/boundary_phase_five/sources.rs`
  - `construction/certification/phase_five_boundary_closeout_tests.rs`
- verification passed after the shelf deletion:
  - `cargo fmt -p worth-kernel`
  - `cargo test -p worth-kernel construction::tests::construction -- --nocapture`
  - `cargo test -p worth-kernel construction::tests::construction_query_reports -- --nocapture`
  - `cargo test -p worth-kernel construction::certification::corpus::tests::simplex_ladder -- --nocapture`
  - `cargo test -p worth-kernel construction::certification::corpus::compound::tests::reports -- --nocapture`
  - `cargo test -p worth-kernel construction::tests::legacy_deletion -- --nocapture`
  - `cargo test -p worth-kernel construction::tests::boundary -- --nocapture`
  - `cargo test -p worth-kernel --test public_api_contract -- --nocapture`
  - `cargo test -p worth-spatial --test public_api_contract -- --nocapture`
  - `cargo check -p worth-kernel -p worth-spatial`
- blocker truth moved again:
  the last small production certification shelves in this band are gone too.
  The next honest seam is no longer `construction/certification/query` or
  `family_coverage`; it is either the remaining hostile replay/realization/
  compound rederivation support or a different surviving production query-native
  seam elsewhere in the geometry runtime story.

- took the next fake-production-topology cut immediately after that:
  - deleted `construction/certification/runtime_truth.rs`
  - deleted `construction/certification/blocking_boundary.rs`
  - removed `mod runtime_truth;` and `mod blocking_boundary;` from
    `construction/certification/mod.rs`
- rehomed their surviving test-owned truth vocabulary into:
  - `construction/tests/support/runtime_truth.rs`
  - `construction/tests/support/blocking_boundary.rs`
  - updated `construction/tests/support/mod.rs`
- rewired the remaining hostile consumers onto the honest owners:
  - `construction/tests/support/branch_preview_basis.rs`
  - `construction/tests/support/projection_consumption.rs`
  - `construction/tests/construction_query_reports.rs`
  - `construction/certification/corpus/replay_siege_builder.rs`
  - `construction/certification/corpus/replay_siege_row.rs`
  - `construction/certification/corpus/tests/support.rs`
  - `construction/certification/corpus/tests/replay_digest_support.rs`
  - `construction/certification/corpus/tests/replay_siege.rs`
  - `construction/certification/corpus/tests/replay_siege_rejections.rs`
  - `construction/certification/corpus/tests/family_boundaries.rs`
  - `construction/certification/corpus/tests/simplex_ladder.rs`
  - `construction/certification/corpus/compound/row_builder.rs`
  - `construction/certification/corpus/compound/rows/siege_row.rs`
  - `construction/certification/corpus/compound/tests/row_support.rs`
- retargeted the hostile proof and boundary owners:
  - `construction/tests/legacy_deletion_query_and_corpus.rs`
  - `construction/tests/boundary.rs`
- verification passed after the topology-truth move:
  - `cargo fmt -p worth-kernel`
  - `cargo test -p worth-kernel construction::tests::construction_query_reports -- --nocapture`
  - `cargo test -p worth-kernel construction::certification::corpus::tests::replay_siege -- --nocapture`
  - `cargo test -p worth-kernel construction::certification::corpus::tests::simplex_ladder -- --nocapture`
  - `cargo test -p worth-kernel construction::certification::corpus::compound::tests::reports -- --nocapture`
  - `cargo test -p worth-kernel construction::tests::legacy_deletion -- --nocapture`
  - `cargo test -p worth-kernel construction::tests::boundary -- --nocapture`
  - `cargo test -p worth-kernel --test public_api_contract -- --nocapture`
  - `cargo test -p worth-spatial --test public_api_contract -- --nocapture`
  - `cargo check -p worth-kernel -p worth-spatial`
- blocker truth moved again:
  the remaining live question is no longer whether `runtime_truth` or
  `blocking_boundary` still pretend to be production certification owners.
  The next honest seam is the broader remaining test-only replay/realization/
  compound rederivation topology, or a different surviving production query-native
  seam elsewhere if a fresh hostile audit finds one.

- took the next whole-shelf realization cut:
  - deleted the remaining `construction/certification/realization` wrapper root:
    - `construction/certification/realization/mod.rs`
    - `construction/certification/realization/snapshot.rs`
    - `construction/certification/realization/exhaustion_witness_report.rs`
    - `construction/certification/realization/tests/mod.rs`
    - `construction/certification/realization/tests/report_support.rs`
    - `construction/certification/realization/tests/reports.rs`
    - `construction/certification/realization/tests/reports_witness_inventory.rs`
  - removed `mod realization;` from `construction/certification/mod.rs`
- rehomed the surviving realization behavior into explicit hostile support and
  construction-owned tests:
  - added `construction/tests/support/realization/mod.rs`
  - added `construction/tests/support/realization/report_support.rs`
  - added `construction/tests/support/realization/exhaustion_witness.rs`
  - added `construction/tests/realization_reports.rs`
  - added `construction/tests/realization_reports_witness_inventory.rs`
  - rewired corpus family/simplex/compound parity support to the new support
    owner
  - retargeted `legacy_deletion_proof_and_query_shelves.rs` so it now proves
    the realization certification root stays deleted instead of reading the
    deleted files
- verification passed after the realization-root deletion:
  - `cargo fmt -p worth-kernel`
  - `cargo test -p worth-kernel construction::tests::realization_reports -- --nocapture`
  - `cargo test -p worth-kernel construction::tests::realization_reports_witness_inventory -- --nocapture`
  - `cargo test -p worth-kernel construction::certification::corpus::tests::simplex_ladder -- --nocapture`
  - `cargo test -p worth-kernel construction::certification::corpus::tests::family_boundaries -- --nocapture`
  - `cargo test -p worth-kernel construction::certification::corpus::compound::tests::parity_reports -- --nocapture`
  - `cargo test -p worth-kernel construction::tests::legacy_deletion -- --nocapture`
  - `cargo test -p worth-kernel construction::tests::boundary -- --nocapture`
  - `cargo test -p worth-kernel --test public_api_contract -- --nocapture`
  - `cargo test -p worth-spatial --test public_api_contract -- --nocapture`
  - `cargo check -p worth-kernel -p worth-spatial`
- blocker truth moved again:
  the entire `construction/certification/realization` wrapper root is closed.
  The next honest seam is no longer replay/realization/compound as one bundle;
  it is the thinner remaining hostile replay/compound rederivation support, or
  a different surviving production query-native seam elsewhere if a fresh audit
  turns one up.

- found and closed a stronger production seam in `worth-topo`:
  - `crates/worth-topo/src/facade.rs` had been exporting a huge proof /
    closeout / parity surface directly from `crate::certification`
  - that ordinary public facade leak is now gone
  - `crates/worth-topo/src/lib.rs` now exposes `pub mod certification;`
    instead, and the proof-heavy vocabulary moved behind
    `topology::certification`
- rewired the topology public contract suite and internal support users onto
  the new boundary:
  - `public_api.rs`
  - `topology_operator_closeout.rs`
  - the mint / not-forgeable compile-fail fixtures
  - historical snapshot support
  - declared query surface retained-artifact / diagnostics / naming helpers
  - branch-local parity acceptance rows
- topology verification passed after the quarantine:
  - `cargo fmt -p worth-topo -p worth-kernel`
  - `cargo check -p worth-topo`
  - `cargo test -p worth-topo --test public_api_contract -- --nocapture`
  - `cargo test -p worth-topo --test public_topology_operator_closeout_contract -- --nocapture`
  - `cargo test -p worth-topo --test ui -- --nocapture`
  - `cargo check -p worth-kernel -p worth-spatial -p worth-topo`
- new blocker truth:
  the old `worth-topo::facade` certification leak is not the blocker anymore.
  The next honest seam is either another production query-native boundary in
  `worth-topo`, or the remaining replay/compound hostile rederivation support
  in `worth-kernel` if no stronger production seam appears first.

- found and closed the next stronger production seam in `worth-spatial`:
  - `crates/worth-spatial/src/facade/bindings.rs` had still been exporting a
    synthetic geometry support/admission posture shelf
  - deleted `crates/worth-spatial/src/bindings/query_native_geometry_support.rs`
  - split the honest shared runtime inventory into
    `crates/worth-spatial/src/bindings/query_native_geometry_inventory.rs`
    so `GeometryPublicSurface` remains public without keeping the proof shelf
    in the runtime facade
  - moved the support/admission matrix into the explicit certification
    namespace at
    `crates/worth-spatial/src/certification/geometry_support_posture.rs`
  - updated `crates/worth-spatial/src/certification.rs` to expose
    `pub mod geometry_support_posture;`
- rewired the public contract band to the new split:
  - `public_api_anchor_selection.rs`
  - `public_api_geometry_support_posture.rs`
  - `public_api_bindings_boundary.rs`
  - `bindings/query_native_geometry_applicability.rs`
- verification passed after the spatial quarantine:
  - `cargo fmt -p worth-spatial`
  - `cargo check -p worth-spatial`
  - `cargo test -p worth-spatial --test public_api_contract -- --nocapture`
  - `cargo test -p worth-spatial --test ui -- --nocapture`
  - `cargo check -p worth-kernel -p worth-spatial -p worth-topo`
- blocker truth moved again:
  the old `worth_spatial::facade::bindings` geometry support posture shelf is
  closed. The next honest seam is either another production query-native
  boundary in `worth-topo`, or the remaining replay/compound hostile
  rederivation topology in `worth-kernel` if no stronger production seam shows
  up first.

- found and closed the next stronger production seam in `worth-topo`:
  - `crates/worth-topo/src/facade.rs` had still been exporting the runtime
    support / posture / closeout report band directly from the ordinary facade
  - expanded `crates/worth-topo/src/runtime_support.rs` so those types now live
    behind `topology::runtime_support`
  - removed the support-report vocabulary from the facade while keeping the
    honest runtime entry surface there:
    - `topology_runtime(...)`
    - `TopologyRuntimeAdapters`
    - `TopologyRuntimeFailure`
- rewired the affected proof boundary:
  - `crates/worth-topo/src/certification/public_facade_contracts/contracts/public_api.rs`
  - `crates/worth-topo/src/certification/query_boundary_cleanup_closeout/rows/public_facade.rs`
  - the `public_runtime_*` trybuild fixtures under
    `crates/worth-topo/src/certification/public_facade_contracts/compile_fail/`
- verification passed after the runtime-support quarantine:
  - `cargo fmt -p worth-topo`
  - `cargo check -p worth-topo`
  - `cargo test -p worth-topo --test public_api_contract -- --nocapture`
  - `cargo test -p worth-topo --test public_topology_operator_closeout_contract -- --nocapture`
  - `cargo test -p worth-topo --test ui -- --nocapture`
  - `cargo check -p worth-kernel -p worth-spatial -p worth-topo`
- blocker truth moved again:
  the old `worth-topo::facade` runtime support / posture / closeout shelf is
  closed. The next honest seam is either the remaining production proof-style
  report vocabulary still exported from `worth-topo::facade` or nearby public
  runtime surfaces, or the surviving replay/compound hostile rederivation
  topology in `worth-kernel` if no stronger production seam shows up first.

- found and closed the next stronger production seam in `worth-topo`:
  - `crates/worth-topo/src/facade.rs` had still been exporting the proof-style
    materialization / interpretation / diagnostics / validation band directly
    from the ordinary facade
  - removed those exports from the facade entirely:
    - `MaterializedTopologyView`
    - `TopologyMaterializer`
    - materialization report / fallback vocabulary
    - `InterpretedTopologyView`
    - `build_topology_read_artifact(...)`
    - `certify_topology_view(...)`
    - `DerivedReadDiagnostics` and the derived invalidation / rebuild /
      fallback report types
    - topology validation report / phase / row / validator vocabulary
- rewired the remaining internal callers onto real owner modules instead of the
  facade shim:
  - `derived_topology/materialized_graph`
  - `derived_topology/traversal_views`
  - `projection/diagnostic_surfaces`
  - `validation`
  - plus the affected historical snapshot / query diagnostics / boundary proof
    files
- updated the public boundary proof and trybuild goldens so the new truth is
  “not exported from the ordinary facade at all,” not merely “still exported
  but unforgeable”
- verification passed after the facade cut:
  - `cargo fmt -p worth-topo`
  - `cargo check -p worth-topo`
  - `cargo test -p worth-topo --test public_api_contract -- --nocapture`
  - `cargo test -p worth-topo --test public_topology_operator_closeout_contract -- --nocapture`
  - `cargo test -p worth-topo --test ui -- --nocapture`
  - `cargo check -p worth-kernel -p worth-spatial -p worth-topo`
- blocker truth moved again:
  the old `worth-topo::facade` materialization / diagnostics / validation
  shelf is closed. The next honest seam is either the remaining proof-style
  read/runtime vocabulary still exported from public `worth-topo` surfaces
  outside the ordinary facade, or the surviving replay/compound hostile
  rederivation topology in `worth-kernel` if no stronger production seam shows
  up first.

- closed the next real `worth-topo` facade seam too:
  - `crates/worth-topo/src/facade.rs` no longer exports:
    - `build_milestone_one_runtime`
    - `configure_milestone_one_runtime_builder`
    - `milestone_one_runtime_builder`
    - `MilestoneOneRuntimeSetupError`
    - `TopologyValidationError`
  - the surviving setup surface now lives under explicit
    `topology::certification` via `crates/worth-topo/src/certification/mod.rs`
  - rewired the remaining callers and boundary proofs:
    - `crates/worth-topo/src/certification/public_facade_contracts/contracts/public_api.rs`
    - `crates/worth-topo/src/certification/query_boundary_cleanup_closeout/rows/public_facade.rs`
    - `crates/worth-kernel/src/construction/tests/branch_preview_basis.rs`
    - `crates/worth-kernel/src/construction/tests/construction_query_reports.rs`
    - `crates/worth-kernel/src/construction/certification/corpus/tests/support.rs`
    - `crates/worth-kernel/src/construction/certification/corpus/compound/tests/support.rs`
    - the affected `worth-kernel` compile-fail fixtures
  - the only weirdness was trybuild bookkeeping: one stderr file still
    mismatched after the text-level edit, so I had to bless it once with
    `TRYBUILD=overwrite` and then rerun `cargo test -p worth-topo --test ui`
    cleanly
  - verification passed:
    - `cargo fmt -p worth-topo -p worth-kernel`
    - `cargo test -p worth-topo --test ui -- --nocapture`
    - `cargo test -p worth-kernel construction::tests::branch_preview_basis -- --nocapture`
    - `cargo test -p worth-kernel construction::certification::corpus::compound::tests::reports -- --nocapture`
    - `cargo check -p worth-kernel -p worth-spatial -p worth-topo`
  - blocker truth moved again:
    the milestone-one runtime builder / validation-setup band is closed in the
    ordinary topology facade. The next honest seam is now the warning-only
    proof/setup residue in `worth-topo` reexport modules
    (`derived_topology/traversal_views/mod.rs`,
    `projection/diagnostic_surfaces/mod.rs`, `validation/mod.rs`) unless a
    stronger production query-native leak shows up first.

- closed that warning-only `worth-topo` residue in a real way instead of just
  accepting it:
  - collapsed the private mini-facades in:
    - `derived_topology/traversal_views/mod.rs`
    - `projection/diagnostic_surfaces/mod.rs`
    - `validation/mod.rs`
  - rewired the only remaining callers that still depended on those broad
    internal reexports:
    - `certification/topology_operator_closeout/acceptance_rows/fallout_classification_tests.rs`
    - `validation/tests.rs`
  - then took the deeper cut immediately after that:
    the whole-view / truth-materialization helpers that only hostile tests use
    are now `#[cfg(test)]` in:
    - `derived_topology/materialized_graph/mod.rs`
    - `derived_topology/materialized_graph/types.rs`
    - `derived_topology/materialized_graph/input_rows.rs`
    - `derived_topology/materialized_graph/entity_labels.rs`
    - `validation/facade.rs`
  - result:
    `cargo check -p worth-topo` is warning-clean again, so the next warning or
    failure should mean something real instead of “test-only helper compiled in
    ordinary lib build”
  - verification passed:
    - `cargo check -p worth-topo`
    - `cargo test -p worth-topo --test public_api_contract -- --nocapture`
    - `cargo test -p worth-topo --test ui -- --nocapture`
    - `cargo check -p worth-kernel -p worth-spatial -p worth-topo`
  - blocker truth moved again:
    the next seam is no longer `worth-topo` warning-only private reexports or
    ordinary-build compilation of test-only materialization/validation helpers.
    The remaining honest targets are deeper replay/compound hostile
    rederivation support in `worth-kernel`, or the next stronger surviving
    production query-native leak elsewhere if audit finds one first.

- removed the fake `worth-kernel` corpus-execution owner too:
  - deleted
    `crates/worth-kernel/src/construction/certification/corpus/execution/mod.rs`
  - that file had shrunk to one `prepare_branch_basis_digest(...)` helper that
    only replay / compound hostile support still used
  - moved that helper into explicit test support at
    `crates/worth-kernel/src/construction/tests/support/branch_basis_digest.rs`
  - rewired:
    - `construction/certification/corpus/tests/replay_digest_support.rs`
    - `construction/certification/corpus/compound/tests/row_support.rs`
    - `construction/certification/phase_five_boundary_closeout_tests.rs`
    - the relevant legacy deletion proofs
  - removed the dead `mod execution;` wire from
    `construction/certification/corpus/mod.rs`
  - verification passed:
    - `cargo fmt -p worth-kernel`
    - `cargo test -p worth-kernel construction::tests::legacy_deletion -- --nocapture`
    - `cargo test -p worth-kernel construction::certification::corpus::tests::replay_siege -- --nocapture`
    - `cargo test -p worth-kernel construction::certification::corpus::compound::tests::reports -- --nocapture`
    - `cargo test -p worth-kernel construction::tests::boundary -- --nocapture`
    - `cargo test -p worth-kernel --test public_api_contract -- --nocapture`
    - `cargo test -p worth-spatial --test public_api_contract -- --nocapture`
    - `cargo check -p worth-kernel -p worth-spatial -p worth-topo`
  - blocker truth moved again:
    the old `construction/certification/corpus/execution` shelf is closed. The
    next honest seam is the remaining replay / compound hostile rederivation
    topology still living under `construction/certification/corpus/...`, unless
    a stronger surviving production query-native leak shows up first.

- then took the adjacent replay hostile-support shelf too:
  - deleted
    `construction/certification/corpus/tests/support.rs`
  - deleted
    `construction/certification/corpus/tests/replay_digest_support.rs`
  - moved that replay rederivation band into plain test support at:
    - `construction/tests/support/corpus_replay_digest.rs`
    - `construction/tests/support/corpus_replay_view.rs`
  - rewired:
    - `construction/certification/corpus/tests/replay_siege.rs`
    - `construction/certification/corpus/tests/replay_siege_rejections.rs`
    - `construction/certification/corpus/tests/realization_classes.rs`
    - `construction/certification/corpus/tests/family_boundaries.rs`
    - `construction/certification/corpus/tests/simplex_ladder.rs`
  - removed the dead module wires from
    `construction/certification/corpus/tests/mod.rs`
  - tightened the replay deletion proof in
    `construction/tests/legacy_deletion_replay.rs`
  - had to widen only the minimum canonical corpus owners to crate-visible so
    the new test-support location could read them honestly:
    - `construction/certification/corpus/cases.rs`
    - `construction/certification/corpus/ordering/mod.rs`
    - `construction/certification/corpus/replay_siege_builder.rs`
    - plus reexports in `construction/certification/corpus/mod.rs`
  - verification passed:
    - `cargo fmt -p worth-kernel`
    - `cargo test -p worth-kernel construction::certification::corpus::tests::replay_siege -- --nocapture`
    - `cargo test -p worth-kernel construction::certification::corpus::tests::simplex_ladder -- --nocapture`
    - `cargo test -p worth-kernel construction::tests::legacy_deletion -- --nocapture`
    - `cargo test -p worth-kernel construction::tests::boundary -- --nocapture`
    - `cargo test -p worth-kernel construction::certification::corpus::compound::tests::reports -- --nocapture`
    - `cargo check -p worth-kernel -p worth-spatial -p worth-topo`
  - blocker truth moved again:
    replay hostile support no longer lives under
    `construction/certification/corpus/tests`. The next honest seam is the
    adjacent compound support / test-support band still living under
    `construction/certification/corpus/compound/...`, unless a stronger
    production query-native leak shows up first.

- then took the adjacent compound hostile-support shelf too:
  - deleted
    `construction/certification/corpus/compound/tests/support.rs`
  - moved the surviving compound inventory/workspace helpers into plain test
    support at:
    - `construction/tests/support/compound_corpus.rs`
  - rewired:
    - `construction/certification/corpus/compound/tests/reports.rs`
    - `construction/certification/corpus/compound/tests/parity_reports.rs`
    - `construction/certification/corpus/compound/tests/closeout_reports.rs`
    - `construction/certification/corpus/compound/tests/row_support.rs`
  - removed the dead test-tree module wire from
    `construction/certification/corpus/compound/tests/mod.rs`
  - tightened the compound deletion proof in
    `construction/tests/legacy_deletion_compound.rs`
  - also cleaned the dead `worth_spatial::certification::policy_support`
    reexport slab out of
    `construction/certification/corpus/compound/support/mod.rs`
  - verification passed:
    - `cargo test -p worth-kernel construction::certification::corpus::compound::tests::reports -- --nocapture`
    - `cargo test -p worth-kernel construction::certification::corpus::compound::tests::parity_reports -- --nocapture`
    - `cargo test -p worth-kernel construction::certification::corpus::compound::tests::closeout_reports -- --nocapture`
    - `cargo test -p worth-kernel construction::tests::legacy_deletion -- --nocapture`
    - `cargo test -p worth-kernel construction::tests::boundary -- --nocapture`
    - `cargo check -p worth-kernel -p worth-spatial -p worth-topo`
  - blocker truth moved again:
    the old compound hostile-support shelf no longer lives under
    `construction/certification/corpus/compound/tests`. The next honest seam is
    no longer this local support shelf; it is the remaining replay/compound
    hostile rederivation topology still compiled under
    `construction/certification/corpus/...`, unless a stronger production
    query-native leak shows up first.

- then took the deeper compound row/lane/parity helper band too:
  - deleted
    `construction/certification/corpus/compound/tests/row_support.rs`
  - deleted
    `construction/certification/corpus/compound/tests/lane_support.rs`
  - deleted
    `construction/certification/corpus/compound/tests/parity_support.rs`
  - deleted
    `construction/certification/corpus/compound/tests/parity_view.rs`
  - moved that whole hostile rederivation band into plain test support at:
    - `construction/tests/support/compound_row_support.rs`
    - `construction/tests/support/compound_lane_support.rs`
    - `construction/tests/support/compound_parity_support.rs`
    - `construction/tests/support/compound_parity_view.rs`
  - rewired:
    - `construction/certification/corpus/compound/tests/reports.rs`
    - `construction/certification/corpus/compound/tests/parity_reports.rs`
    - `construction/certification/corpus/compound/tests/closeout_reports.rs`
    - `construction/certification/corpus/compound/tests/mod.rs`
  - widened only the minimum canonical corpus owners to crate-visible so the
    new support location could read them honestly:
    - `construction/certification/corpus/compound/cases.rs`
    - `construction/certification/corpus/compound/mod.rs`
    - `construction/certification/corpus/ordering/mod.rs`
    - `construction/certification/corpus/parity/mod.rs`
    - plus corresponding reexports in `construction/certification/corpus/mod.rs`
  - tightened the hostile deletion proofs in:
    - `construction/tests/legacy_deletion_compound.rs`
    - `construction/tests/legacy_deletion_query_and_corpus.rs`
  - verification passed:
    - `cargo fmt -p worth-kernel`
    - `cargo test -p worth-kernel construction::certification::corpus::compound::tests::reports -- --nocapture`
    - `cargo test -p worth-kernel construction::certification::corpus::compound::tests::parity_reports -- --nocapture`
    - `cargo test -p worth-kernel construction::certification::corpus::compound::tests::closeout_reports -- --nocapture`
    - `cargo test -p worth-kernel construction::tests::legacy_deletion -- --nocapture`
    - `cargo test -p worth-kernel construction::tests::boundary -- --nocapture`
    - `cargo check -p worth-kernel -p worth-spatial -p worth-topo`
  - blocker truth moved again:
    the old compound row/lane/parity helper shelf no longer lives under
    `construction/certification/corpus/compound/tests`. The next honest seam is
    no longer compound-local helper ownership; it is the thinner replay /
    compound hostile topology still sitting under the certification corpus test
    tree, unless a stronger production query-native leak shows up first.

- finished that replay/compound hostile test-owner relocation too:
  - moved the remaining replay hostile owners from
    `construction/certification/corpus/tests/` into `construction/tests/`:
    - `replay_siege.rs` -> `corpus_replay_siege.rs`
    - `replay_siege_rejections.rs` -> `corpus_replay_siege_rejections.rs`
    - `realization_classes.rs` -> `corpus_realization_classes.rs`
    - `family_boundaries.rs` -> `corpus_family_boundaries.rs`
    - `simplex_ladder.rs` -> `corpus_simplex_ladder.rs`
  - moved the remaining compound hostile owners from
    `construction/certification/corpus/compound/tests/` into
    `construction/tests/`:
    - `reports.rs` -> `compound_reports.rs`
    - `parity_reports.rs` -> `compound_parity_reports.rs`
    - `closeout_reports.rs` -> `compound_closeout_reports.rs`
  - deleted the old certification-side test roots:
    - `construction/certification/corpus/tests/mod.rs`
    - `construction/certification/corpus/compound/tests/mod.rs`
    - removed `mod tests;` from
      `construction/certification/corpus/mod.rs` and
      `construction/certification/corpus/compound/mod.rs`
  - widened only the minimum crate-visible corpus exports needed by the moved
    tests, including the missing simplex helper reexports in
    `construction/certification/corpus/mod.rs`
  - tightened the deletion proofs so the dead certification-side test roots and
    paths cannot quietly return:
    - `legacy_deletion_replay.rs`
    - `legacy_deletion_compound.rs`
    - `legacy_deletion_query_and_corpus.rs`
    - `legacy_deletion_query_shelves.rs`
  - verification passed:
    - `cargo test -p worth-kernel construction::tests::corpus_replay_siege -- --nocapture`
    - `cargo test -p worth-kernel construction::tests::corpus_replay_siege_rejections -- --nocapture`
    - `cargo test -p worth-kernel construction::tests::corpus_realization_classes -- --nocapture`
    - `cargo test -p worth-kernel construction::tests::corpus_family_boundaries -- --nocapture`
    - `cargo test -p worth-kernel construction::tests::corpus_simplex_ladder -- --nocapture`
    - `cargo test -p worth-kernel construction::tests::compound_reports -- --nocapture`
    - `cargo test -p worth-kernel construction::tests::compound_parity_reports -- --nocapture`
    - `cargo test -p worth-kernel construction::tests::compound_closeout_reports -- --nocapture`
    - `cargo test -p worth-kernel construction::tests::legacy_deletion -- --nocapture`
    - `cargo test -p worth-kernel construction::tests::boundary -- --nocapture`
    - `cargo check -p worth-kernel -p worth-spatial -p worth-topo`
  - blocker truth moved again:
    replay and compound hostile test owners no longer live under the
    certification corpus test tree. The next honest seam is whatever
    certification/corpus topology still survives only to serve hostile test
    support, unless a bigger production query-native leak shows up first.

- then took the next adjacent certification-only seam too:
  - deleted the compound lowering support shelf from the certification corpus
    tree by removing `mod support;` from
    `construction/certification/corpus/compound/mod.rs` and deleting the old
    `construction/certification/corpus/compound/support/mod.rs`
  - rehomed the whole lowering band into plain construction test support:
    - `construction/tests/support/compound_lowering/mod.rs`
    - `construction/tests/support/compound_lowering/motion.rs`
    - `construction/tests/support/compound_lowering/relations.rs`
    - `construction/tests/support/compound_lowering/spatial_fixture_witness_catalog.rs`
  - moved the hostile lowering tests out of the certification tree too:
    - `construction/tests/compound_lowering.rs`
    - `construction/tests/compound_lowering_catalog_failures.rs`
    - `construction/tests/compound_lowering_directional.rs`
  - rewired `construction/certification/corpus/compound/cases.rs` and
    `construction/certification/corpus/compound/builder.rs` onto the honest
    `construction/tests/support/compound_lowering` owner
  - tightened `legacy_deletion_compound.rs` so that dead certification-side
    support shelf cannot quietly return
  - verification passed:
    - `cargo test -p worth-kernel construction::tests::compound_lowering -- --nocapture`
    - `cargo test -p worth-kernel construction::tests::compound_lowering_catalog_failures -- --nocapture`
    - `cargo test -p worth-kernel construction::tests::compound_lowering_directional -- --nocapture`
    - `cargo test -p worth-kernel construction::tests::compound_reports -- --nocapture`
    - `cargo test -p worth-kernel construction::tests::compound_parity_reports -- --nocapture`
    - `cargo test -p worth-kernel construction::tests::compound_closeout_reports -- --nocapture`
    - `cargo test -p worth-kernel construction::tests::legacy_deletion -- --nocapture`
    - `cargo test -p worth-kernel construction::tests::boundary -- --nocapture`
    - `cargo check -p worth-kernel -p worth-spatial -p worth-topo`
  - blocker truth moved again:
    the old `construction/certification/corpus/compound/support` shelf is
    gone. The next honest seam is the remaining certification/corpus helper
    topology that still exists only to feed hostile support, especially the
    replay/build-order/parity/closeout band, unless a stronger production
    query-native leak shows up first.

- then removed that replay/build-order/parity/closeout helper band too:
  - deleted:
    - `construction/certification/corpus/closeout/mod.rs`
    - `construction/certification/corpus/parity/mod.rs`
    - `construction/certification/corpus/replay_siege.rs`
    - `construction/certification/corpus/replay_siege_builder.rs`
    - `construction/certification/corpus/ordering/corpus_lane_application.rs`
    - `construction/certification/corpus/ordering/corpus_lanes.rs`
  - rehomed the surviving helper behavior into plain construction test support:
    - `construction/tests/support/compound_required_inventory.rs`
    - `construction/tests/support/compound_specialized_rows.rs`
    - `construction/tests/support/corpus_ordering.rs`
    - `construction/tests/support/corpus_replay_generation.rs`
  - rewired:
    - `construction/certification/corpus/compound/parity/registry.rs`
    - `construction/tests/support/compound_parity_support.rs`
    - `construction/tests/support/corpus_replay_digest.rs`
    - `construction/tests/support/corpus_replay_view.rs`
  - thinned the certification/corpus roots:
    - `construction/certification/corpus/mod.rs`
    - `construction/certification/corpus/ordering/mod.rs`
  - widened only the canonical replay row constructor in
    `construction/certification/corpus/replay_siege_row.rs` to `pub(crate)` so
    the new plain test-support owner could build canonical rows without another
    wrapper
  - tightened the proof owners:
    - `legacy_deletion_replay.rs`
    - `legacy_deletion_compound.rs`
  - verification passed:
    - `cargo test -p worth-kernel construction::tests::compound_parity_reports -- --nocapture`
    - `cargo test -p worth-kernel construction::tests::compound_closeout_reports -- --nocapture`
    - `cargo test -p worth-kernel construction::tests::corpus_replay_siege -- --nocapture`
    - `cargo test -p worth-kernel construction::tests::corpus_replay_siege_rejections -- --nocapture`
    - `cargo test -p worth-kernel construction::tests::legacy_deletion -- --nocapture`
    - `cargo check -p worth-kernel -p worth-spatial -p worth-topo`
  - blocker truth moved again:
    the old `construction/certification/corpus/closeout`, `corpus/parity`, and
    replay-ordering/replay-generation helper shelves are gone. The next honest
    seam is the remaining direct certification/corpus owner files that may now
    be thin enough to collapse into plain construction test support too,
    unless a stronger production query-native leak shows up first.

- took the next stronger production seam in `worth-spatial`:
  - `crates/worth-spatial/src/facade/bindings.rs` no longer exports the
    support/applicability, retained inspection/replay, recovery, projection
    consumption, or tolerance-certification families
  - added dedicated facade owners:
    - `facade/support.rs`
    - `facade/inspection.rs`
    - `facade/recovery.rs`
    - `facade/projection.rs`
    - `facade/tolerance.rs`
  - rewired the `worth-spatial` public-contract band and public docs to that
    split
  - found the honest downstream fallout immediately after: `worth-kernel`
    rebinding tests were still importing those moved APIs through
    `worth_spatial::facade::bindings`
  - fixed the fallout directly in `crates/worth-kernel/src/binding/tests/**`
    by retargeting inspection/recovery/projection imports to the new facade
    modules instead of restoring compatibility reexports
  - verification passed:
    - `cargo fmt -p worth-kernel -p worth-spatial`
    - `cargo check -p worth-spatial`
    - `cargo test -p worth-spatial --test public_api_contract -- --nocapture`
    - `cargo test -p worth-spatial --test ui -- --nocapture`
    - `cargo test -p worth-topo --test public_api_contract -- --nocapture`
    - `cargo test -p worth-topo --test ui -- --nocapture`
    - `cargo test -p worth-kernel --test public_api_contract -- --nocapture`
    - `cargo test -p worth-kernel binding::tests::inspection::historical_inspection -- --nocapture`
    - `cargo test -p worth-kernel binding::tests::inspection::replay_parity -- --nocapture`
    - `cargo test -p worth-kernel binding::tests::recovery_action -- --nocapture`
    - `cargo test -p worth-kernel binding::tests::rebinding_projection_consumption_receipt -- --nocapture`
    - `cargo test -p worth-kernel binding::tests::rebinding_signal_continuation -- --nocapture`
    - `cargo check -p worth-kernel -p worth-spatial -p worth-topo`
  - blocker truth moved again:
    the old `worth_spatial::facade::bindings` non-binding god-facade seam is
    closed for support posture, retained inspection/replay, recovery,
    projection consumption, and tolerance posture. The next honest seam is the
    remaining live binding/rebinding slab still exported from
    `crates/worth-spatial/src/facade/bindings.rs`, unless a stronger production
    query-native leak elsewhere turns up first.

- took the next live production cut inside `worth-spatial::facade`:
  - added:
    - `facade/neighborhood.rs`
    - `facade/continuation.rs`
  - `facade/bindings.rs` no longer exports grouped rebinding neighborhood
    helpers, grouped contribution helpers, topology-neighborhood replacement
    family/facts, or rebinding signal/continuation aliases/builders
  - rewired the `worth-spatial` public-contract proofs and the affected
    `worth-kernel` rebinding tests onto those narrower owners
  - verification passed:
    - `cargo fmt -p worth-spatial -p worth-kernel`
    - `cargo test -p worth-spatial --test public_api_contract -- --nocapture`
    - `cargo test -p worth-kernel binding::tests::rebinding::grouped_workflow -- --nocapture`
    - `cargo test -p worth-kernel binding::tests::rebinding_neighborhood_replacement -- --nocapture`
    - `cargo test -p worth-kernel binding::tests::rebinding_signal_continuation -- --nocapture`
    - `cargo check -p worth-kernel -p worth-spatial -p worth-topo`
  - hostile QA for the batch found no deeper structural miss in scope:
    the old paths are gone, the downstream proof bands use the new owners, and
    the split reduces real public-surface coupling instead of just moving names
  - blocker truth moved again:
    the old `worth_spatial::facade::bindings` workflow god-facade seam is
    closed for grouped neighborhood/replacement and signal/continuation. The
    next honest seam is the remaining combined declaration/evidence/projection
    slab still exported from `crates/worth-spatial/src/facade/bindings.rs`,
    especially whether anchor, binding, and rebinding still deserve one facade
    owner.

- fixed the Pass F closeout proof gap in `worth-kernel`:
  - the last real issue was not a live kernel runtime shelf; it was one missing
    compile-fail fixture registration
  - added
    `src/certification/public_facade_contracts/compile_fail/phases/public_authoring_input_traits_are_sealed.rs`
    to `compile_fail_contracts.rs`
  - reran the focused Pass F bundle:
    - `cargo test -p worth-kernel --test public_api_contract -- --nocapture`
    - `cargo test -p worth-kernel --test ui -- --nocapture`
    - `cargo test -p worth-kernel binding::tests::legacy_deletion -- --nocapture`
    - `cargo check -p worth-spatial -p worth-kernel -p worth-topo`
  - rewrote `_docs/worth/worth-geometry-query-native-ACTIVE.md` into a tight
    freeze board:
    - Passes C, D, E, and F closed on current evidence
    - reopen only on a concrete regression or another proof-net gap

- closed the explicit Phase 9 hostile proof row:
  - added
    `crates/worth-kernel/src/binding/tests/geometry_hard_break_closeout.rs`
  - the first version failed for a good reason: I compared projection
    consumption against the old kernel `retained_digest_for_receipt(...)`
    helper, but production projection consumption uses the retained-source
    identity digest instead
  - fixed the proof to compare projection consumption against neighborhood
    replacement's canonical retained-source digest, which gives a real
    cross-family check instead of forcing the wrong local digest contract
  - reran the closeout bundle successfully:
    - `cargo test -p worth-kernel geometry_hard_break_closeout -- --nocapture`
    - `cargo test -p worth-kernel --test public_api_contract -- --nocapture`
    - `cargo test -p worth-kernel --test ui -- --nocapture`
    - `cargo test -p worth-kernel binding::tests::legacy_deletion -- --nocapture`
    - `cargo test -p worth-spatial --test public_api_contract -- --nocapture`
    - `cargo test -p worth-spatial --test ui -- --nocapture`
    - `cargo test -p worth-topo --test public_api_contract -- --nocapture`
    - `cargo test -p worth-topo --test ui -- --nocapture`
    - `cargo check -p worth-spatial -p worth-kernel -p worth-topo`
  - rewrote ACTIVE again so it now reflects the broader truth:
    the query-native geometry hard break is closed on current evidence, with
    explicit reopen conditions instead of a fake live seam
