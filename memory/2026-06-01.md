- worth-runtime-bridge aspect-native spec QA loop: found that the hardened spec still used `compatibility` vocabulary in target topology and normative implementation bullets, which could accidentally legitimize shim/coexistence lanes. Replaced production-facing language with foreign/non-native external I/O terminology, renamed the target skeleton to `external_io/foreign_ingress` and `external_io/terminal_report_export`, and tightened acceptance/batch-plan wording around deleting legacy or foreign-format shims. Verified phase headings, direct whitespace hygiene, and residue scans for permissive compatibility phrases.
- worth-runtime-bridge Phase 6 writeback authority handoff batch: removed the remaining `proposed_effect_digest`/`domain_payload_digest` authority vocabulary from the writeback adapter request, mapper witness/record, execution record, mutation provenance, retained causal mapping, diagnostics explanations, harness authority, facade tests, and UI compile-fail fixtures. The digest is now consistently named `effect_intent_digest`, reflecting that it is derived from typed `BridgeWritebackEffectIntent` backed by foundational authoritative patches. Verified with `cargo fmt -p worth-runtime-bridge`, `cargo check -p worth-runtime-bridge --tests`, `cargo test -p worth-runtime-bridge writeback -- --nocapture` (71 passed; expected caught-panic output from panic-mapping test), `cargo test -p worth-runtime-bridge --test phase_boundaries_compile_fail -- --nocapture`, residue scans for `proposed_effect_digest`/`domain_payload_digest`, and `git diff --check` on touched files (only CRLF warnings).
- worth-runtime-bridge Phase 6 replay/certification closeout batch: moved writeback replay bundles and replay records from digest-only effect evidence to native effect-intent evidence by retaining `effect_intent_digest` and `effect_intent_patch_canonical_basis` from `BridgeWritebackEffectIntent`. Updated replay mismatch diagnostics and harness certification export to project effect-intent evidence instead of `bridge_effect_digest`/`expected_effect_digest`/`replayed_effect_digest`. Verified with `cargo fmt -p worth-runtime-bridge`, `cargo check -p worth-runtime-bridge --tests`, `cargo test -p worth-runtime-bridge writeback_certification -- --nocapture`, `cargo test -p worth-runtime-bridge writeback -- --nocapture` (71 passed; expected caught-panic output from panic-mapping test), `cargo test -p worth-runtime-bridge --test phase_boundaries_compile_fail -- --nocapture`, residue scan over touched writeback/replay/certification seams, and `git diff --check` on touched files (only CRLF warnings).
- worth-runtime-bridge Phase 6 QA loop: found that replay record explanations and retained causal-envelope evidence still omitted the new effect-intent digest/patch-basis proof, leaving the native replay record stronger than its diagnostic/causal projections. Added effect-intent proof fields and accessors to `BridgeWritebackReplayRecordExplanation`, added explicit effect-intent replay proof inputs to retained causal evidence digests and their test support mirror, and strengthened diagnostics replay/explanation tests. Verified with `cargo fmt -p worth-runtime-bridge`, `cargo check -p worth-runtime-bridge --tests`, `cargo test -p worth-runtime-bridge diagnostics_replay -- --nocapture`, `cargo test -p worth-runtime-bridge diagnostics_explanations -- --nocapture`, `cargo test -p worth-runtime-bridge causal_envelope_mapping -- --nocapture`, `cargo test -p worth-runtime-bridge writeback -- --nocapture` (71 passed; expected caught-panic output from panic-mapping test), targeted residue scan, and `git diff --check` (only CRLF warnings).
- worth-runtime-bridge Phase 7 implementation batch: moved writeback replay-mismatch certification from a stored `serde_json::Value` matrix to typed `WritebackReplayMismatchMatrix` under `harness/adapter/adapter_impl/writeback_certification/`, with separate `replay_mismatch_terminal_report.rs` JSON projection for terminal harness export only. The typed artifact now retains expected/replayed replay digests, semantic digests, causality digests, effect-intent digests, foundational effect-intent patch canonical bases, typed `BridgeWritebackErrorKind`, and restart replay mismatch evidence before report rendering. Strengthened `writeback_replay_mismatch_is_typed_and_counted` to prove certification evidence is projected from the replay mismatch artifact. Verified with `cargo fmt -p worth-runtime-bridge`, `cargo check -p worth-runtime-bridge --tests`, `cargo test -p worth-runtime-bridge writeback_certification -- --nocapture`, `cargo test -p worth-runtime-bridge writeback -- --nocapture` (71 passed; expected caught-panic output from panic-mapping test), residue scans for replay-mismatch JSON storage/indexing, and `git diff --check` (only CRLF warnings).
- worth-runtime-bridge Phase 7 duplicate-certification batch: moved `DuplicateCertification` from stored `serde_json::Value` to typed `WritebackDuplicateAuthorityMatrix`. Split the typed artifact into `duplicate_authority/{mod.rs,replay_and_idempotence.rs,authority_boundary.rs,attempts.rs}` and kept terminal report rendering in `duplicate_authority_terminal_report.rs`. The artifact now retains effect-intent digest plus foundational patch canonical basis, replay-bundle report, idempotence proof, loop-prevention proof, authority-boundary request/receipt/candidate proof, attempt outcomes, and boundedness proof as typed fields before JSON export. Strengthened duplicate certification tests to compare certification evidence back to typed-projected effect, patch-basis, idempotence, loop-prevention, authority-boundary, and boundedness evidence. Verified with `cargo fmt -p worth-runtime-bridge`, `cargo check -p worth-runtime-bridge --tests`, `cargo test -p worth-runtime-bridge writeback_certification -- --nocapture`, `cargo test -p worth-runtime-bridge writeback -- --nocapture` (71 passed; expected caught-panic output from panic-mapping test), line-cap scans for the new certification modules, duplicate JSON-storage residue scans, and `git diff --check` (only CRLF warnings).
- worth-runtime-bridge Phase 7 duplicate-certification QA loop: found one real structural/proof-boundary issue after the typed matrix split. `WritebackDuplicateAuthorityMatrix::from_duplicate_attempts` and `DuplicateAuthorityBoundaryMatrix::from_authority_boundary_evidence` still accepted giant evidence argument lists and suppressed Clippy with `too_many_arguments`, making the native proof handoff easier to miswire. Replaced both with named evidence packets (`WritebackDuplicateAuthorityMatrixEvidence` and `DuplicateAuthorityBoundaryEvidence`) while preserving typed artifact storage and terminal-only JSON projection. Verified with `cargo fmt -p worth-runtime-bridge`, `cargo check -p worth-runtime-bridge --tests`, `cargo test -p worth-runtime-bridge writeback_certification -- --nocapture`, `cargo test -p worth-runtime-bridge writeback -- --nocapture` (71 passed; expected caught-panic output from panic-mapping test), duplicate authority JSON/argument-residue scans, line-cap scans, and `git diff --check` on touched files (only existing CRLF warning).
- worth-runtime-bridge Phase 1 restart batch: moved the first live legacy-vocabulary seam off `surface_label` in native production carriers that already had foundational locators and target basis. Replaced `surface_label` with `target_label` across routing truth surfaces, subscription slice lowering, snapshot read requests/targets, continuity prior/remapped slices, patch/snapshot error coordinates, route diagnostics records, mapping lookup keys, and harness route-support JSON. Deleted the native `surface_label()` accessors in those touched slices, renamed the route diagnostics provenance field from `raw_patch_surface_label` to `source_target_label`, and removed the `legacy-surface-constructor` test basis in favor of a native test-only target constructor. Updated compile-coupled route/snapshot/continuity/pricing tests and pricing showcase support so current tests teach target-based native vocabulary instead of the old surface-label authority shape. Verified with `cargo fmt -p worth-runtime-bridge`, `cargo check -p worth-runtime-bridge --tests`, `cargo test -p worth-runtime-bridge snapshot -- --nocapture`, `cargo test -p worth-runtime-bridge routing -- --nocapture`, `cargo test -p worth-runtime-bridge continuity -- --nocapture`, `cargo test -p worth-runtime-bridge pricing_shock -- --nocapture`, touched-file residue scan for `surface_label|compatibility_surface_label|legacy-surface-constructor|raw_patch_surface_label|EmptySurfaceLabel` (no matches), and `git diff --check` (only existing CRLF warnings on touched files).

- worth-runtime-bridge Phase 1 fallback/widening annihilation batch: replaced route/mapping fallback vocabulary with explicit admitted widening across mapping policy/classes, lookup classification, route eligibility/status/counters, diagnostics records, facade exports, bulk packets/reductions, harness JSON projection, and coupled routing/planning/mapping tests. Deleted fallback-named test modules by moving them to widening/serial-reduction topology. Extended Phase 1 cleanup to remove all remaining Rust fallback vocabulary in worth-runtime-bridge src: causal envelope scan fallback counters became unindexed-scan counters, source adapter fallback counters became non-native escape counters, policy fallback success became authority-bypass success, and subscription latest fallback certification became latest-unretained-basis reconstruction evidence. Verified with cargo fmt -p worth-runtime-bridge, cargo check -p worth-runtime-bridge --tests, focused routing/mapping/packet_reduction/bulk_workload/source/policy_certification/subscription::certification/causal_envelope/certification_historical_basis tests, fallback residue scans over src, file-name fallback scan, and git diff --check (only CRLF warnings).

- worth-runtime-bridge Phase 1 strict no-test-shims batch: deleted the remaining public/test authority convenience constructors BridgeCommittedPatchItem::new and SnapshotReadRecord::string, forcing tests/callers to use native target constructors and explicit foundational AspectValue carriers. Replaced subscription delivery payload terminology with typed delivery content evidence, including new BridgeSubscriptionDeliveryContentDigest, BridgeSubscriptionDeliveryContentOmissionReason, delivery_content_digest, omitted_content, and ReplayBlockedByOmittedContent. Cleaned pricing/subscription test vocabulary so read values are named as aspect-value texts/content, not payloads. Verified with cargo fmt -p worth-runtime-bridge, cargo check -p worth-runtime-bridge --tests, focused snapshot/subscription::delivery/source/routing/subscription_certification tests, residue scans for deleted shim names/payload terms in touched seams, and git diff --check (only existing CRLF warnings).

- worth-runtime-bridge Phase 1 test-shim annihilation batch: removed JSON proof matrices from source certification tests and moved terminal harness JSON inspection behind typed source certification proof helpers. Updated stale UI compile-fail fixtures away from fallback/raw authority names (`bridge_record_scan_fallback_count`, `latest_fallback_bundle_digest`, `latest_truth_fallback_count`, `RawCommittedPatchEnvelope`) to the current native field/API names. Verified with `cargo fmt -p worth-runtime-bridge`, `cargo test -p worth-runtime-bridge source -- --nocapture`, `cargo test -p worth-runtime-bridge --test phase_boundaries_compile_fail -- --nocapture`, touched-seam residue scans, line-cap scan for `harness/tests/source`, and `git diff --check` (only existing CRLF warnings).

- worth-runtime-bridge Phase 1 policy certification matrix detox: converted `harness/adapter/adapter_impl/policy.rs` from an 823-line mixed adapter/report file into `policy/{mod.rs,certification_execution.rs,shared_artifacts.rs,matrices.rs,terminal_report.rs}`. `PolicyHarnessExecution` now retains typed policy certification/request/route matrices instead of `serde_json::Value`; JSON is rendered only at the terminal harness report boundary. Removed matrix JSON builders and Clippy argument suppressions while keeping terminal export keys stable. Verified with `cargo fmt -p worth-runtime-bridge`, warning-clean `cargo check -p worth-runtime-bridge --tests`, `cargo test -p worth-runtime-bridge policy_certification -- --nocapture`, `cargo test -p worth-runtime-bridge policy -- --nocapture`, policy matrix residue scans, line-cap scans, and scoped `git diff --check` (only existing CRLF warning on `policy_certification.rs`).

- worth-runtime-bridge Phase 1 policy certification QA loop: found two proof-boundary issues after the typed policy matrix split. `policy/mod.rs` still rendered top-level harness JSON instead of delegating all report export to the terminal boundary, and typed matrix rows had visible fields instead of named evidence packets. Moved summary/extension rendering into `policy/terminal_report.rs`, made matrix row fields private, and added named row evidence packets for admitted/rejected/route matrix construction. Reverified with `cargo fmt -p worth-runtime-bridge`, warning-clean `cargo check -p worth-runtime-bridge --tests`, `cargo test -p worth-runtime-bridge policy_certification -- --nocapture`, `cargo test -p worth-runtime-bridge policy -- --nocapture`, residue scans proving JSON only remains in terminal export/signature surfaces for this policy split, line-cap scans, and scoped `git diff --check` (only existing CRLF warning on `policy_certification.rs`).

- worth-runtime-bridge Phase 1 subscription resume checkpoint detox batch: deleted the remaining raw-stream-checkpoint vocabulary from subscription resume admission, counters, canonical counter basis, and the compile-fail fixture. The rejection is now named as an unsealed stream checkpoint denial, and the UI fixture is `subscription_resume_rejects_unsealed_stream_checkpoint_type`. Split `subscription/counters` further so values, canonical digest-basis rendering, and constructor families live in named submodules instead of over-cap constructor/mod files. Verified with `cargo fmt -p worth-runtime-bridge`, warning-clean `cargo check -p worth-runtime-bridge --tests`, `cargo test -p worth-runtime-bridge subscription -- --nocapture`, `cargo test -p worth-runtime-bridge counters -- --nocapture`, `cargo test -p worth-runtime-bridge --test phase_boundaries_compile_fail -- --nocapture`, residue scans for old raw names and shim/fallback/legacy/payload/JSON terms in touched surfaces, line-cap scans, and scoped `git diff --check` (only existing CRLF warnings).

- worth-runtime-bridge Phase 1 standard-path diagnostics facade detox batch: deleted the public `BridgeDiagnostics::raw()` escape hatch and replaced it with `retained_artifacts()` vocabulary so the standard path no longer teaches callers a raw diagnostics lane. Added `standard_path_diagnostics_raw_lane_removed` trybuild coverage proving the old method is gone, not aliased. Structural QA split touched over-cap facade surfaces into `standard_path/diagnostics.rs`, `standard_path/routing.rs`, and `runtime/continuity_planning.rs`, bringing touched files under the 400-line cap while preserving the public facade exports. Verified with `cargo fmt -p worth-runtime-bridge`, warning-clean `cargo check -p worth-runtime-bridge --tests`, `cargo test -p worth-runtime-bridge facade -- --nocapture`, `cargo test -p worth-runtime-bridge --test phase_boundaries_compile_fail -- --nocapture`, production residue scans for raw diagnostics vocabulary, line-cap scans, and scoped `git diff --check` (only existing CRLF warnings).

- worth-runtime-bridge Phase 1 schema parity detox batch: removed the subscription certification schema compatibility/incompatibility authority seam across production, facade, harness, tests, counters, canonical digest basis, and UI compile-fail fixture vocabulary. BridgeSubscriptionCertificationSchemaCompatibilityReport became BridgeSubscriptionCertificationSchemaParityReport; bundle/checkpoint failure boundaries now use schema/digest divergence and checkpoint divergence; reference workload sharing rejection now uses divergent-sharing vocabulary. Split touched over-cap files into subscription/certification/counters/{accessors,canonical_basis}.rs, eference_workload/lane_planning.rs, workload_coverage/expected_lane_evidence.rs, acade/runtime/subscription/{certification,lifecycle,delivery_admission}.rs, facade certification test submodules, and harness suites 35-37 submodules. Verified with cargo fmt -p worth-runtime-bridge, warning-clean cargo check -p worth-runtime-bridge --tests, cargo test -p worth-runtime-bridge subscription_certification -- --nocapture, cargo test -p worth-runtime-bridge subscription::certification -- --nocapture, cargo test -p worth-runtime-bridge --test phase_boundaries_compile_fail -- --nocapture, targeted compatibility/incompatibility residue scans, line-cap scan over touched files, and scoped git diff --check (only existing CRLF warnings).
- worth-runtime-bridge Phase 1 writeback strategy coherence hard-break: deleted the writeback strategy compatibility authority seam and replaced it with strategy coherence across production writeback classification, candidate validation, execution records, truth-writeback request canonical basis, diagnostics/explanations, facade methods, harness certification matrices, terminal report keys, and writeback tests/UI fixtures. The happy disposition is now `Coherent`, not `Compatible`; tests no longer preserve the old strategy-compatibility vocabulary. Verified with `cargo fmt -p worth-runtime-bridge`, warning-clean `cargo check -p worth-runtime-bridge --tests`, `cargo test -p worth-runtime-bridge writeback -- --nocapture` (71 passed; expected caught-panic output from panic-mapping test), `cargo test -p worth-runtime-bridge writeback_certification -- --nocapture`, `cargo test -p worth-runtime-bridge --test phase_boundaries_compile_fail -- --nocapture`, targeted residue scans for the old strategy-compatibility seam, and `git diff --check` (only existing CRLF warnings). Structural QA notes that `diagnostics/writeback/mod.rs`, `facade/runtime/writeback.rs`, `harness/adapter/adapter_impl/writeback.rs`, and `harness/tests/writeback_certification.rs` remain over the line cap and should be the next dedicated writeback topology batch.
- worth-runtime-bridge writeback diagnostics topology batch: split over-cap `diagnostics/writeback.rs` into `diagnostics/writeback/{admission, safety_gates, authority_execution, mapper_evidence, replay_evidence}.rs` plus an aggregation-only `mod.rs`. The split preserves typed artifact semantics from the strategy-coherence hard break while making the derived diagnostics lifecycle explicit: admission/candidate, safety gates, authority execution, mapper evidence, and replay evidence. QA found and fixed an initially too-granular one-file-per-type split and wildcard imports. Verified with `cargo fmt -p worth-runtime-bridge`, warning-clean `cargo check -p worth-runtime-bridge --tests`, `cargo test -p worth-runtime-bridge diagnostics_explanations -- --nocapture`, `cargo test -p worth-runtime-bridge writeback -- --nocapture` (71 passed; expected caught-panic output from panic-mapping test), writeback diagnostics residue scan for old strategy compatibility terms, line-cap scan for the new diagnostics modules, and scoped `git diff --check`.

- worth-runtime-bridge Phase 1 writeback runtime facade topology batch: deleted the 970-line flat acade/runtime/writeback.rs production facade bucket and replaced it with acade/runtime/writeback/{mod.rs,admission_and_lowering.rs,safety_gates.rs,replay_validation.rs,authority_execution.rs,authority_execution_recording.rs,authority_failure_mapping.rs,execution_counters.rs}. The split preserves the existing aspect-native writeback intent/effect, candidate, authority request/receipt, replay bundle, and typed execution record semantics while making the facade authority chain spatially explicit: admission/lowering, safety gates, replay validation, authority execution, execution record assembly, failure mapping, and counters. QA found and corrected the first mechanical split because uthority_execution.rs remained over cap and hid the execution record decision table. Verified with cargo fmt -p worth-runtime-bridge, warning-clean cargo check -p worth-runtime-bridge --tests, cargo test -p worth-runtime-bridge writeback -- --nocapture (71 passed; expected caught panic output), cargo test -p worth-runtime-bridge diagnostics_explanations -- --nocapture, line-cap scan, residue scan for old strategy-compatibility/legacy/fallback/raw/payload/bytes terms in the new writeback facade directory (only digest s_bytes() hash feed), and scoped git diff --check (only existing CRLF warning on facade/runtime.rs).

- worth-runtime-bridge Phase 1 writeback bypass certification detox batch: moved BypassCertification retained harness evidence off serde_json::Value storage into typed WritebackBypassAuthorityMatrix and BypassZeroResidueProof under harness/adapter/adapter_impl/writeback_certification, with terminal-only JSON report rendering via bypass_authority_terminal_report. QA found and fixed an initial leak where certification evidence re-indexed terminal JSON; evidence export now delegates to terminal report rendering without JSON as semantic storage. Verified with cargo fmt -p worth-runtime-bridge, warning-clean cargo check -p worth-runtime-bridge --tests, cargo test -p worth-runtime-bridge writeback_certification -- --nocapture, cargo test -p worth-runtime-bridge writeback -- --nocapture, bypass JSON storage/indexing residue scans, line-cap scan for new certification modules, and scoped git diff --check (only existing CRLF warning). Remaining writeback harness JSON matrices for feedback/family/admission/replay-loop/mapper parity and the over-cap writeback adapter/test files remain next Phase 1 detox targets.

- worth-runtime-bridge Phase 1 writeback feedback-loop certification detox batch: moved FeedbackLoopCertification retained harness evidence off serde_json::Value into typed WritebackFeedbackLoopMatrix under harness/adapter/adapter_impl/writeback_certification, with terminal-only JSON rendering in feedback_loop_terminal_report and terminal projection access isolated under feedback_loop/terminal_projection_access.rs. QA found and fixed two structural/proof issues: the initial typed file exceeded the 400-line cap, and several feedback proof facts were terminal constants instead of retained typed fields. Verified with cargo fmt -p worth-runtime-bridge, warning-clean cargo check -p worth-runtime-bridge --tests, cargo test -p worth-runtime-bridge writeback_certification -- --nocapture, cargo test -p worth-runtime-bridge writeback -- --nocapture, feedback JSON storage/indexing residue scans, writeback certification line-cap scan, and scoped git diff --check (only existing CRLF warning). Remaining retained JSON matrices in writeback.rs: family_extension_matrix, admission_boundary_matrix, replay_loop_matrix, mapper_parity_matrix.

- worth-runtime-bridge Phase 1 host-mapper-parity certification detox batch: moved HostMapperParityCertification retained evidence off stored serde_json::Value into typed WritebackMapperParityMatrix under harness/adapter/adapter_impl/writeback_certification, with terminal-only JSON rendering in mapper_parity_terminal_report. Certification evidence now projects from typed projected/aspect family proof, retained mapper envelope/input facts, execution/admission digests, effect-intent patch canonical basis, and typed FamilyBindingMismatch shadow-protocol denial rather than indexing report JSON. QA fixed an initial constant-proof smell by deriving shadow-protocol no-mapper evidence from the typed denial kind. Verified with cargo fmt -p worth-runtime-bridge, warning-clean cargo check -p worth-runtime-bridge --tests, cargo test -p worth-runtime-bridge writeback_certification -- --nocapture, cargo test -p worth-runtime-bridge writeback -- --nocapture (71 passed; expected caught-panic output), mapper-parity JSON storage/indexing residue scans, writeback certification line-cap scan, and scoped git diff --check (only existing CRLF warning). Remaining retained JSON matrices in writeback.rs: family_extension_matrix, admission_boundary_matrix, replay_loop_matrix.

- worth-runtime-bridge Phase 1 extensible-family certification detox batch: moved ExtensibleFamilyCertification retained evidence off stored serde_json::Value into typed WritebackFamilyExtensionMatrix under harness/adapter/adapter_impl/writeback_certification/family_extension, with terminal-only JSON rendering in family_extension_terminal_report. The retained proof now carries projected/aspect admitted contract family evidence, effect-intent patch canonical bases, idempotence/replay/receipt digests, cross-family replay isolation, same-family rebuild equivalence, changed-causality replay isolation, loop-prevention evidence, mapper retention parity, and typed FamilyBindingMismatch shadow-protocol denial. Structural QA split the initial over-cap typed artifact into family_extension/{mod.rs,replay_isolation.rs,mapper_and_shadow.rs}. Verified with cargo fmt -p worth-runtime-bridge, warning-clean cargo check -p worth-runtime-bridge --tests, cargo test -p worth-runtime-bridge writeback_certification -- --nocapture, cargo test -p worth-runtime-bridge writeback -- --nocapture (71 passed; expected caught-panic output), family-extension JSON storage/indexing residue scans, writeback certification line-cap scan, and scoped git diff --check (only existing CRLF warning). Remaining retained JSON matrices in writeback.rs: admission_boundary_matrix and replay_loop_matrix.

- worth-runtime-bridge Phase 1 writeback certification final-matrix detox batch: removed the last retained JSON proof matrices from writeback harness certification. MultiFamilyAdmissionBoundaryCertification now stores typed WritebackAdmissionBoundaryMatrix proof under writeback_certification/admission_boundary, and CrossFamilyReplayLoopIsolationCertification now stores typed WritebackReplayLoopIsolationMatrix proof under writeback_certification/replay_loop_isolation. JSON remains only in terminal report renderers. Verified with cargo fmt -p worth-runtime-bridge, warning-clean cargo check -p worth-runtime-bridge --tests, cargo test -p worth-runtime-bridge writeback_certification -- --nocapture, cargo test -p worth-runtime-bridge writeback -- --nocapture (71 passed; expected caught-panic output), retained-matrix JSON residue scans, line-cap scans for writeback_certification, and scoped git diff --check (only existing CRLF warning).

- worth-runtime-bridge Phase 1 speculation churn certification detox batch: removed retained JSON proof storage from SpeculationHarnessExecution::Churn. Churn certification now stores typed SpeculationChurnCertification evidence with branch isolation rows, resource bounds, and counter snapshot in harness/adapter/adapter_impl/speculation/churn_certification.rs; terminal JSON rendering is isolated in churn_terminal_report.rs. The churn producer now constructs typed branch lifecycle evidence first and derives counters/resource bounds from that evidence instead of building JSON maps. Added an internal native proof test proving the churn counter snapshot is derived from typed resource evidence. Verified with cargo fmt -p worth-runtime-bridge, warning-clean cargo check -p worth-runtime-bridge --tests, cargo test -p worth-runtime-bridge speculation_certification -- --nocapture, cargo test -p worth-runtime-bridge speculation -- --nocapture, residue scans for retained churn JSON fields/builders, touched-file line-cap scans, and scoped git diff/trailing-whitespace checks (only existing CRLF warnings on tracked files).

- worth-runtime-bridge Phase 1 stream certification pressure-report detox batch: removed the stream harness certification bundle path that accepted `pressure_report: Option<serde_json::Value>` as semantic evidence. `adapter_impl/stream.rs` is now a 280-line orchestration module, with typed `StreamHarnessCertificationBundle` in `adapter_impl/stream/certification_bundle.rs` retaining stream/checkpoint/replay/routing digests, cloned typed counters, and the typed `BackpressureDecisionRecord`; terminal JSON rendering now lives in `adapter_impl/stream/terminal_report.rs`. QA found and fixed an initial leak where the typed certification module still owned a `json()` method, moving all bundle/checkpoint/replay rendering into the terminal report module. Verified with cargo fmt -p worth-runtime-bridge, warning-clean cargo check -p worth-runtime-bridge --tests, cargo test -p worth-runtime-bridge stream -- --nocapture, residue scans for old pressure-report JSON inputs/builders, touched-file line-cap scans, and scoped diff/trailing-whitespace checks (only existing CRLF warning on stream.rs).

- worth-runtime-bridge Phase 1 source harness certification detox batch: split over-cap `harness/adapter/adapter_impl/source.rs` into orchestration plus `source/certification_bundle.rs` and `source/terminal_report.rs`. Source certification now constructs typed `SourceHarnessCertificationBundle` and `SourceHarnessCounterSnapshot` from `AdmittedSourceContract`, `SourceMaterializationRecord`, and `SourceFailureRecord` before any terminal JSON projection. `source.rs` dropped inline certification JSON/counter JSON/report rendering and is now under the 400-line cap. QA fixed a parallel-derivation smell by making summary rendering read diagnostics/failure proof from the typed bundle. Verified with `cargo fmt -p worth-runtime-bridge`, warning-clean `cargo check -p worth-runtime-bridge --tests`, `cargo test -p worth-runtime-bridge source -- --nocapture`, source seam residue scans proving typed certification stays JSON-free, touched source/test line-cap scans, and scoped `git diff --check` (only existing CRLF warning on `source.rs`).
- worth-runtime-bridge Phase 1 writeback certification terminal-report/test-shim detox batch: moved writeback certification bundle summary/extension rendering out of `harness/adapter/adapter_impl/writeback.rs` into `writeback/terminal_report.rs`, removed inline counter JSON builders from the execution adapter, and corrected downstream writeback certification terminal renderers to accept typed `BridgeWritebackCounters` instead of pre-rendered JSON counter snapshots. QA found a test-shim violation in `harness/tests/writeback_certification.rs`: the tests were certifying terminal JSON matrices instead of typed certification artifacts. Rewrote that harness test as parity/export smoke coverage with no `serde_json`/`json!` inspection, and added `writeback/typed_certification_tests.rs` beside the execution artifacts to pattern-match `WritebackHarnessExecution` variants and assert typed duplicate, bypass, feedback, replay-mismatch, family-extension, admission-boundary, replay-loop, mapper-parity matrices plus typed counters directly. Verified with `cargo fmt -p worth-runtime-bridge`, warning-clean `cargo check -p worth-runtime-bridge --tests`, `cargo test -p worth-runtime-bridge writeback_certification -- --nocapture`, `cargo test -p worth-runtime-bridge writeback -- --nocapture` (79 passed; expected caught-panic output), residue scans proving touched writeback certification tests are JSON/legacy-shim free, and scoped `git diff --check` (only existing CRLF warnings). Remaining structural debt: `harness/adapter/adapter_impl/writeback.rs` is still over cap at ~2926 lines and should be the next writeback adapter topology split.
- worth-runtime-bridge Phase 1 writeback adapter topology batch: split the remaining over-cap `harness/adapter/adapter_impl/writeback.rs` authority bucket into `writeback/{counter_snapshot,effect_intent,rejecting_authority,duplicate_certification,bypass_certification,feedback_loop_certification,replay_mismatch_certification,family_certification,feedback_patch,runtime_building,terminal_report,typed_certification_tests}.rs`, with family lanes further split under `family_certification/{extension_execution,admission_boundary_execution,replay_loop_execution,mapper_parity_execution}.rs`. The parent adapter is now a target router/shared helper surface under the line cap; every new writeback adapter file is under 400 lines. QA found and fixed terminal projection imports leaking through the parent authority router; JSON report imports now live in `terminal_report.rs` only. Verified with `cargo fmt -p worth-runtime-bridge`, warning-clean `cargo check -p worth-runtime-bridge --tests`, `cargo test -p worth-runtime-bridge writeback_certification -- --nocapture`, `cargo test -p worth-runtime-bridge writeback -- --nocapture` (79 passed; expected caught-panic output), residue scans showing JSON only in summary/export signatures and terminal report rendering for this slice, line-cap scans, and scoped `git diff --check` (only existing CRLF warnings).

- worth-runtime-bridge Phase 1 no-test-shims residue batch: removed the remaining non-external `compatibility` vocabulary from snapshot replay policy by replacing `TruthViewReplayCompatibility`/`replay_compatibility` with `TruthViewReplayContinuity`/`replay_continuity` across production exports, historical policy lowering, source tests, and canonical basis text. Cleaned writeback facade tests away from compatibility-named strategy-coherence fixtures. Deleted the migration-era `standard_path_diagnostics_raw_lane_removed` UI fixture, renamed remaining UI function names away from raw vocabulary, and renamed the snapshot validation input from `raw_result` to `unvalidated_result`. Split touched over-cap `facade/tests/structural.rs` into structural submodules for admission/reduction, replay records, read-packet derivation, and read-order invariance, with all touched structural test files under 400 lines. Verified with `cargo fmt -p worth-runtime-bridge`, warning-clean `cargo check -p worth-runtime-bridge --tests`, `cargo test -p worth-runtime-bridge snapshot -- --nocapture`, `cargo test -p worth-runtime-bridge writeback -- --nocapture` (79 passed; expected caught-panic output), `cargo test -p worth-runtime-bridge structural -- --nocapture`, `cargo test -p worth-runtime-bridge --test phase_boundaries_compile_fail -- --nocapture`, residue scans for `aspect_bytes|surface_label|domain_payload|legacy|fallback|compatibility|raw`, payload scan showing only foundational `.payload()` accessors, touched structural line-cap scan, and scoped `git diff --check` (only existing CRLF warnings).
- worth-runtime-bridge Phase 1 structural harness certification detox batch: deleted the 1013-line `harness/adapter/adapter_impl/structural.rs` retained-JSON bucket and replaced it with `structural/{mod.rs,execution_flows.rs,certification_bundle.rs,counter_snapshot.rs,diagnostics_digest.rs,terminal_report.rs,typed_certification_tests.rs}`. Structural harness execution now derives typed `StructuralHarnessSummary`, `StructuralHarnessCertificationBundle`, typed ambiguity/identity-separation/diff reports, and typed counter snapshots before terminal JSON rendering. QA caught an initial evidence weakening where retained candidate identities had become counts; fixed by retaining candidate identity vectors in the typed artifacts. Structural harness tests no longer import `serde_json`/`json!` or index report fields as certification proof; detailed proof assertions moved beside the adapter typed artifacts. Verified with `cargo fmt -p worth-runtime-bridge`, warning-clean `cargo check -p worth-runtime-bridge --tests`, `cargo test -p worth-runtime-bridge structural -- --nocapture`, `cargo test -p worth-runtime-bridge --test phase_boundaries_compile_fail -- --nocapture`, structural JSON residue scans proving JSON remains only in `terminal_report.rs`/delegating signatures, old-vocabulary scans, touched structural line-cap scans, and scoped `git diff --check` (only existing CRLF warnings).
- worth-runtime-bridge Phase 1 merge harness certification detox batch: deleted the flat retained-JSON `harness/adapter/adapter_impl/merge.rs` and replaced it with `merge/{mod.rs,certification_bundle.rs,counter_snapshot.rs,diagnostics_digest.rs,terminal_report.rs,typed_certification_tests.rs}`. Merge harness execution now retains typed `MergeHarnessCertificationBundle`, ontology mapping/support/denial/record evidence, diagnostics digest, and typed counter snapshots before terminal JSON export. Split over-cap `harness/tests/merge.rs` into `merge/{direct_authority,harness_certification,support}.rs`; tests no longer import `serde_json`/`json!` or index terminal reports as merge proof. QA caught an incorrect success expectation about remap publication and fixed the typed proof to assert remap publication when structural advisory is consistent. Verified with `cargo fmt -p worth-runtime-bridge`, warning-clean `cargo check -p worth-runtime-bridge --tests`, `cargo test -p worth-runtime-bridge merge -- --nocapture`, merge slice JSON residue scans showing JSON only in terminal report/signature surfaces, old-vocabulary scans, line-cap scans, and scoped `git diff --check` (only existing CRLF warning on `merge.rs`).

- worth-runtime-bridge Phase 1 policy certification no-test-shims batch: rewrote `harness/tests/policy_certification.rs` so public harness tests no longer import `serde_json`/`json!` or index terminal report JSON as semantic proof. Added private adapter-level `policy/typed_certification_tests.rs` that pattern-matches `PolicyHarnessExecution` and asserts typed policy certification/request/route matrices plus typed counters directly. Hardened retained policy matrix rows so request kind, execution class, diagnostics tier, rejection kind/stage/field/source, and route policy class/tier are stored as typed facade enums rather than debug-formatted strings; terminal JSON formatting is isolated in `policy/terminal_report.rs`. QA found and fixed an incomplete typed provenance fixture that had not registered the native committed patch/snapshot on the runtime source, causing route planning to fail under the wider policy filter. Verified with `cargo fmt -p worth-runtime-bridge`, warning-clean `cargo check -p worth-runtime-bridge --tests`, `cargo test -p worth-runtime-bridge policy_certification -- --nocapture`, `cargo test -p worth-runtime-bridge policy -- --nocapture` (60 passed), policy JSON/legacy residue scans showing JSON only in terminal report/delegating signatures, line-cap scans, and scoped `git diff --check` (only existing CRLF warning).

- worth-runtime-bridge Phase 1 M13 certification test-shim annihilation batch: deleted the 1006-line `harness/tests/certification.rs` JSON-proof bucket and replaced it with responsibility modules under `harness/tests/certification/{matrix,continuity,bulk_workload,speculation,mixed_offline,execution_harness,continuity_authority,mixed_family_fixtures}.rs`. The M13 mixed offline test no longer imports `serde_json`/`json!`, builds a synthetic JSON diagnosis bundle, or indexes terminal report rows to prove typed source/structural/merge/policy/writeback failure classes; it now acts only as cross-family execution/export smoke while the typed adapter certification tests own semantic proof. Continuity harness tests were also moved off terminal-field indexing and now only prove terminal export divergence/presence. QA found and fixed an initial generic `support.rs` helper bucket by splitting execution harness, continuity authority, and mixed-family fixtures into named support modules. Verified with `cargo fmt -p worth-runtime-bridge`, warning-clean `cargo check -p worth-runtime-bridge --tests`, `cargo test -p worth-runtime-bridge certification -- --nocapture` (106 passed), scoped residue scans showing no JSON proof indexing or legacy/raw/payload/fallback terms in the new certification directory, line-cap scans, and scoped `git diff --check`.
- worth-runtime-bridge Phase 1 speculation certification no-test-shims batch: rewrote `harness/tests/speculation_certification.rs` so public speculation certification tests no longer import `serde_json`/`json!`, index terminal `summary`/`extensions`, or walk JSON arrays as semantic proof. Moved discard/promotion speculation harness report rendering out of `speculation/mod.rs` and `shared.rs` into `speculation/terminal_report.rs`, leaving non-terminal speculation modules with typed execution/digest helpers only. Added `speculation/typed_certification_tests.rs` to assert retained `SpeculationHarnessExecution` discard, promotion, replay, route, counter, churn branch isolation, and resource-bound evidence directly. QA found that churn branch isolation still stored lifecycle outcome as a string, so `SpeculationBranchIsolationRow` now retains typed `BridgePreviewLifecycleStateKind` and only terminal JSON stringifies it. Verified with `cargo fmt -p worth-runtime-bridge`, warning-clean `cargo check -p worth-runtime-bridge --tests`, `cargo test -p worth-runtime-bridge speculation_certification -- --nocapture` (3 passed), `cargo test -p worth-runtime-bridge speculation -- --nocapture` (27 passed), residue scans proving public tests have no JSON proof indexing and non-terminal touched speculation modules have no JSON builders, legacy/raw/payload/fallback terms, line-cap scans, and scoped `git diff --check` (only existing CRLF warnings).
- worth-runtime-bridge Phase 1 stream delivery/replay no-test-shims batch: moved stream delivery/replay semantic proof out of public terminal JSON assertions and into adapter-private typed certification tests. `harness/tests/stream/delivery.rs` and `harness/tests/stream/replay.rs` no longer index `RunRecord.summary`, nested counter JSON, or `serde_json::json!`; they now certify harness export/parity shape only. Added `harness/adapter/adapter_impl/stream/typed_certification_tests.rs` to assert typed `StreamHarnessExecution` routing/replay-audit windows, member-set digest parity, consumer-specific window digest divergence, checkpoint/replay records, pressure classification, and typed stream counters directly. QA found public test names were still overclaiming semantic proof after the JSON assertions were removed, so renamed them to export/parity wording while private typed tests own canonical stream meaning. Verified with `cargo fmt -p worth-runtime-bridge`, warning-clean `cargo check -p worth-runtime-bridge --tests`, `cargo test -p worth-runtime-bridge stream -- --nocapture` (44 passed), touched-file residue scans for JSON proof indexing and legacy/raw/payload/fallback vocabulary, line-cap scans, and scoped `git diff --check` (only existing CRLF warnings).
- worth-runtime-bridge Phase 1 source certification no-test-shims batch: rewrote `harness/tests/source/certification.rs` so public source harness tests no longer import `serde_json`, index `RunRecord.summary`/`extensions`, or decode terminal counter fields as semantic proof. Added adapter-private `harness/adapter/adapter_impl/source/typed_certification_tests.rs` to assert `SourceHarnessExecution`, `SourceHarnessCertificationBundle`, typed materialization/replay records, typed source failures, and typed counters directly before terminal report export. Deleted the terminal JSON proof helper module. Also removed the production replay error name `CanonicalArtifactCompatibilityFailure`, replacing it with `CanonicalArtifactCoherenceFailure` across diagnostics/replay tests so production vocabulary no longer preserves compatibility language. Verified with `cargo fmt -p worth-runtime-bridge`, warning-clean `cargo check -p worth-runtime-bridge --tests`, `cargo test -p worth-runtime-bridge source -- --nocapture` (58 passed), `cargo test -p worth-runtime-bridge replay -- --nocapture` (108 passed), source/replay residue scans, line-cap scans, and scoped `git diff --check` (only existing CRLF warnings).
- worth-runtime-bridge Phase 1 public route/replay terminal-proof cleanup batch: removed remaining `RunRecord.summary[...]` semantic assertions from `harness/tests/replay/route.rs`, `harness/tests/delivery/snapshot.rs`, and `harness/tests/planning/route_identity.rs`. Replay capture now checks terminal capture only for request echo while proving source commit/snapshot through typed canonical route replay. Snapshot stability and route identity tests now assert retained typed route records instead of terminal harness summaries. Removed a now-unused `HarnessRunner` import from delivery tests. Verified with `cargo fmt -p worth-runtime-bridge`, warning-clean `cargo check -p worth-runtime-bridge --tests`, `cargo test -p worth-runtime-bridge replay -- --nocapture` (108 passed), `cargo test -p worth-runtime-bridge snapshot -- --nocapture` (48 passed), `cargo test -p worth-runtime-bridge route_identity -- --nocapture` (4 passed), touched-file residue scans for terminal JSON proof and legacy vocabulary, line-cap scans, and scoped `git diff --check` (only existing CRLF warnings).
- worth-runtime-bridge Phase 1 history harness no-test-shims batch: rewrote `harness/tests/history.rs` so public history/replay tests no longer assert `RunRecord.summary`, `ReplayRecord.summary`, or `extensions` JSON maps as semantic proof. The tests now execute/capture through the harness but assert retained `BridgeCanonicalHistoricalEvaluationRecord` decision logs, typed materialization paths, counters, and `BridgeHistoricalEvaluationReplaySummary` directly. QA caught and removed my initial terminal-envelope request/replay-name assertions so the slice no longer teaches terminal harness fields as proof. Verified with `cargo fmt -p worth-runtime-bridge`, warning-clean `cargo check -p worth-runtime-bridge --tests`, `cargo test -p worth-runtime-bridge history -- --nocapture` (17 passed), `cargo test -p worth-runtime-bridge replay -- --nocapture` (108 passed), touched-file residue scans for JSON/report-map proof and legacy/raw/payload/shim vocabulary, line-cap scan at 388 lines, and scoped `git diff --check` (only existing CRLF warning). Remaining `summary[`/`extensions[` harness-test residue is concentrated in `harness/tests/pricing_shock.rs`.
- worth-runtime-bridge Phase 1 pricing source-adapter no-test-shims batch: extracted pricing source-adapter parity tests into `harness/tests/pricing_shock/source_adapter_parity.rs` and replaced semantic proof via `RunRecord.summary`/`extensions` with typed retained `SourceMaterializationRecord` evidence plus retained source failure counts. The public parity tests still compare terminal pricing showcase exports where that is the behavior under test, but source-adapter materialization parity no longer relies on terminal JSON maps. Verified with `cargo fmt -p worth-runtime-bridge`, warning-clean `cargo check -p worth-runtime-bridge --tests`, `cargo test -p worth-runtime-bridge pricing_shock_suite_25_through_27_parity -- --nocapture`, `cargo test -p worth-runtime-bridge source -- --nocapture` (58 passed), seam residue scans for `RunRecord`/probe summary-extension indexing/legacy-shim vocabulary, line count for the new module at 188 lines, and scoped `git diff --check` (only existing CRLF warning). Remaining pricing JSON proof residue is concentrated in `harness/tests/pricing_shock.rs` workload/showcase certification assertions around `summary[...]`.
- worth-runtime-bridge Phase 1 pricing certification no-test-shims batch: added typed pricing certification evidence in `harness/tests/pricing_support/certification_evidence.rs` for retained profile comparison, diagnostics entrypoints, bundle completeness, reference-workload comparison, and certification counters. Deleted the old JSON `comparison_against` helper and rewrote pricing workload/profile/suite/ML simulation tests to assert retained typed evidence instead of `summary[...]`, `comparison[...]`, `counter_snapshot[...]`, or suite-27 report-map fields. Source-adapter parity now compares typed diagnostics/counter evidence where it previously indexed suite-27 terminal report JSON. Verified with `cargo fmt -p worth-runtime-bridge`, warning-clean `cargo check -p worth-runtime-bridge --tests`, focused pricing workload/suite/ML/source-adapter tests, residue scans proving no `summary[` or `extensions[` remains anywhere under `worth-runtime-bridge/src/harness/tests`, typed evidence legacy/shim/JSON scans, line count for the new evidence module at 316 lines, and scoped `git diff --check` (only existing CRLF warnings).

- worth-runtime-bridge Phase 1 pricing terminal-report/test-shim cleanup: moved pricing suite digest authority onto typed digest evidence and native basis-line builders instead of deriving digests/counters/completeness/comparison by rendering and re-reading terminal JSON. Split pricing support into typed certification evidence, digest basis, digest evidence, terminal report, suite terminal artifacts, showcase lineage edges, and ML pipeline export modules, all under the 400-line cap. Rewrote remaining pricing test digest comparisons away from suite JSON indexing. Verified with cargo fmt -p worth-runtime-bridge, warning-clean cargo check -p worth-runtime-bridge --tests, focused pricing workload/suite/ML/source-adapter tests, residue scans for JSON proof indexing and deleted summary/core helpers, line-cap scans, and scoped git diff --check (only existing CRLF warnings).

- worth-runtime-bridge Phase 1 no-test-shims follow-up: removed the remaining `serde_json::json!` proof assertions from `harness/tests/pricing_shock.rs` showcase/ML/suite tests. Semantic proof now reads retained typed pricing bundles, typed digest evidence, typed counters, and typed lineage edges; terminal JSON is only smoke-checked for export shape/string fields. Added `PricingLineageProvenanceEdge` so ML lineage graph tests can validate retained typed edges instead of parsing terminal JSON as authority. Verified with `cargo fmt -p worth-runtime-bridge`, warning-clean `cargo check -p worth-runtime-bridge --tests`, focused pricing showcase/ML/suite tests, touched-file residue scans, and scoped `git diff --check` (only existing CRLF warning). Remaining structural debt: `harness/tests/pricing_shock.rs` is still over cap and should be split by responsibility in the next no-shim pricing topology batch.

- worth-runtime-bridge Phase 1 no-test-shim pricing shock topology batch: converted the giant pricing_shock test harness root into an orchestration module with aspect-native responsibility modules for standard paths, historical provenance/oracle failures, lifecycle/fanout/writeback/merge, routing/replay attacks, certification/aspect workload, repricing/suite/restart failures, source adapter parity, showcase/ML exports, and split support modules. Showcase/ML/suite tests now assert typed bundle/digest/counter/lineage evidence and use JSON only as terminal export shape. Verified with cargo fmt -p worth-runtime-bridge, warning-clean cargo check -p worth-runtime-bridge --tests, cargo test -p worth-runtime-bridge pricing_shock -- --nocapture (41 passed), line-cap scan (all pricing_shock files <=400 lines), residue scan for JSON proof/shim/legacy/fallback/aspect_bytes/surface_label/domain_payload/payload terms (no matches), and git diff --check (only existing CRLF warning).
- worth-runtime-bridge Phase 1/7 writeback counter terminal-export detox: removed the production `BridgeWritebackCounters::json()` method so writeback counters remain typed artifacts with digest/canonical-basis accessors only. Added shared terminal renderer `harness/adapter/adapter_impl/writeback_counter_terminal_report.rs` and routed writeback + writeback-certification terminal reports through it. Verified with `cargo fmt -p worth-runtime-bridge`, warning-clean `cargo check -p worth-runtime-bridge --tests`, `cargo test -p worth-runtime-bridge writeback -- --nocapture` (83 passed; expected caught-panic output from panic-mapping test), `cargo test -p worth-runtime-bridge writeback_certification -- --nocapture` (8 passed), production counter residue scan for serde_json/json/counter `.json()` (no matches), line-cap scan on touched writeback terminal surfaces, and `git diff --check` on tracked touched files (only existing CRLF warning).

- worth-runtime-bridge Phase 1 harness adapter terminal-report quarantine batch: deleted the generic adapter support JSON bucket, moved route/replay/diagnostics/snapshot capture JSON rendering into explicitly named terminal_report modules, removed JSON-returning methods from the typed HarnessExecution carrier, and split the over-cap adapter_impl root into runtime_loading and snapshot_capture responsibility modules. Runtime loading now owns profile lowering, fixture truth loading, source-adapter binding, and mutation batch application; adapter_impl is back to trait orchestration. Verified with cargo fmt -p worth-runtime-bridge, warning-clean cargo check -p worth-runtime-bridge --tests, cargo test -p worth-runtime-bridge replay -- --nocapture (112 passed), cargo test -p worth-runtime-bridge diagnostics -- --nocapture (33 passed), touched-slice JSON/legacy residue scans, line-cap scan, and scoped git diff --check (only existing CRLF warnings).

- worth-runtime-bridge Phase 1 no-test-shim batch: renamed the route-planning policy admission guard from compatibility vocabulary to coherence/divergence in the runtime facade and coupled policy/pricing failure assertions. Quarantined pricing showcase JSON test export out of generic support by reducing pricing_showcase_support.rs to module aggregation and moving showcase, commit-explorer, and lineage JSON projection into explicit terminal-report/export modules while keeping typed lineage edge construction JSON-free. Verified with cargo fmt -p worth-runtime-bridge, cargo check -p worth-runtime-bridge --tests, cargo test -p worth-runtime-bridge policy_phase2 -- --nocapture, cargo test -p worth-runtime-bridge pricing_shock::showcase_and_ml_exports -- --nocapture, route-policy compatibility residue scan, pricing showcase JSON topology scan, line-cap scan, and scoped git diff --check (only existing CRLF warnings).

- worth-runtime-bridge Phase 1 subscription certification divergence batch: removed subscription fanout/schema completeness compatibility vocabulary from typed production certification reports and coupled tests. BridgeSubscriptionCertificationFanoutReport now exposes divergent rejection proof accessors, completeness uses schema-divergent field accounting, and the mixed offline certification test no longer advertises proof shims. Split the touched over-cap harness suites_31_34.rs bucket into suite-specific fanout, continuation, resume/replay, and preview/promotion modules. Verified with cargo fmt -p worth-runtime-bridge, cargo check -p worth-runtime-bridge --tests, cargo test -p worth-runtime-bridge certification_fanout -- --nocapture, cargo test -p worth-runtime-bridge fanout -- --nocapture, cargo test -p worth-runtime-bridge bridge_harness_subscription_suite_31 -- --nocapture, cargo test -p worth-runtime-bridge mixed_offline -- --nocapture, cargo test -p worth-runtime-bridge suites_31_34 -- --nocapture, targeted residue scans for incompatible/schema-incompatible/shim terms, line-cap scan for touched files, and scoped git diff --check (only CRLF warnings).
- worth-runtime-bridge Phase 1 incompatible-vocabulary eradication batch: removed the remaining `incompatible|compatible|compatibility|compat` source/test residue from runtime-bridge. Subscription admission now names unsupported basis pairing as `BasisKindDivergence` with `from_basis_kind_divergence_rejection`; source replay failure vocabulary uses `ReplaySourceRequestCoherenceFailure`; stream protocol rejection prose now says declared consumer-shape divergence; writeback candidate errors reject divergent strategy coherence/idempotence basis; replay-version tests now reject unsupported canonical versions instead of "incompatible" records. Updated the stale subscription fanout UI compile-fail fixture to divergent field names and split the touched over-cap facade merge test by moving replay-version coverage into `facade/tests/merge/replay_version.rs`, leaving the root at 385 lines. Verified with `cargo fmt -p worth-runtime-bridge`, warning-clean `cargo check -p worth-runtime-bridge --tests`, `cargo test -p worth-runtime-bridge subscription -- --nocapture`, `cargo test -p worth-runtime-bridge stream -- --nocapture`, `cargo test -p worth-runtime-bridge replay -- --nocapture`, `cargo test -p worth-runtime-bridge merge -- --nocapture`, `cargo test -p worth-runtime-bridge writeback -- --nocapture` (expected caught-panic output from panic-mapping test), `cargo test -p worth-runtime-bridge --test phase_boundaries_compile_fail -- --nocapture`, strict compatibility-vocabulary residue scan (no matches), touched-file line-cap scan, and scoped `git diff --check` (only existing CRLF warnings). Remaining flagged `payload()` calls are foundational validated-value/readiness accessors and need no rename unless a later semantic audit proves non-native authority.

- worth-runtime-bridge Phase 1/6 writeback authority-request proof hardening batch: removed the ambiguous `BridgeDerivedWritebackEffect::effect_digest()` intent shortcut and replaced it with `effect_intent_digest()` with no alias. Truth writeback authority requests now carry `effect_intent_patch_canonical_basis` from the foundational `BridgeWritebackEffectIntent` patch basis, and execution records, diagnostics explanations, feedback provenance, mutation provenance, and aggregate mutation authority digests retain that basis instead of leaving authority handoff digest-only. Updated writeback tests, certification helpers, and trybuild fixtures so tests construct/assert native effect-intent proof rather than preserving digest-only request shape. Verified with `cargo fmt -p worth-runtime-bridge`, `cargo check -p worth-runtime-bridge --tests`, `cargo test -p worth-runtime-bridge writeback -- --nocapture` (83 passed; expected caught panic output from panic-mapping test), `cargo test -p worth-runtime-bridge writeback_certification -- --nocapture` (8 passed), `cargo test -p worth-runtime-bridge --test phase_boundaries_compile_fail -- --nocapture`, effect-digest/no-shim residue scans, touched-file line-cap scan, and scoped `git diff --check` (only existing CRLF warnings).
- worth-runtime-bridge Phase 1 terminal export boundary detox batch: renamed/moved harness adapter report rendering from generic `terminal_report.rs`/`*_terminal_report.rs` files into explicit `terminal_report_export/json_projection.rs` modules across the adapter root, adapter_impl root, lane reports, writeback certification renderers, and shared writeback counter renderer. Deleted JSON-returning `summary_json`/`extensions_json` methods from typed lane execution records so source/merge/policy/stream/speculation/structural/writeback execution carriers no longer expose terminal report JSON; the central terminal export projector now consumes typed execution variants directly. QA caught and removed an accidental churn export wrapper plus the lane-method JSON leak. Verified with `cargo fmt -p worth-runtime-bridge`, warning-clean `cargo check -p worth-runtime-bridge --tests`, `cargo test -p worth-runtime-bridge harness::adapter::adapter_impl -- --nocapture` (28 passed), residue scans showing no old `*_terminal_report.rs` files, no shim/legacy/fallback vocabulary in the adapter slice, no non-export serde_json/json use in lane execution modules, and no adapter files over 400 lines.

- worth-runtime-bridge Phase 1 pricing support terminal-export quarantine batch: moved pricing support/showcase test JSON projection files out of generic terminal-report/export filenames and into explicit `terminal_report_export` modules. `pricing_support.rs` and `pricing_showcase_support.rs` are now aggregation-only, while certification, suite, showcase, commit-explorer, lineage, and ML-pipeline JSON rendering lives under `terminal_report_export/*_json_projection.rs`. Typed pricing certification bundles, digest evidence, and lineage edges remain the semantic evidence; JSON exists only as terminal export shape. Verified with `cargo fmt -p worth-runtime-bridge`, warning-clean `cargo check -p worth-runtime-bridge --tests`, `cargo test -p worth-runtime-bridge pricing_shock -- --nocapture` (41 passed), residue scans proving no generic terminal-report names or shim/legacy/fallback/payload terms in the touched pricing support topology, JSON scans showing `serde_json` only under `terminal_report_export`, and line-cap scans for the touched pricing support/showcase files.

- worth-runtime-bridge Phase 1 builder/pricing no-test-shims batch: split over-cap `builder/tests.rs` into an aggregation module plus responsibility modules for runtime construction, source registry admission, policy sections, continuity/writeback, and named test support (`runtime_fakes`, `mapping_registrations`, `source_declarations`, `native_continuity_slice`). Removed old `surface_test` builder fixture vocabulary by constructing prior slices with explicit field-path fixture names. Moved pricing ML export pretty serialization behind the existing `pricing_showcase_support/terminal_report_export` JSON boundary so pricing tests no longer call `serde_json` outside terminal export. QA found and fixed the first split's generic `support.rs` bucket by replacing it with named support submodules. Verified with `cargo fmt -p worth-runtime-bridge`, warning-clean `cargo check -p worth-runtime-bridge --tests`, `cargo test -p worth-runtime-bridge build_ -- --nocapture` (12 passed), `cargo test -p worth-runtime-bridge pricing_shock -- --nocapture` (41 passed), targeted no-shim/no-legacy/JSON residue scans, line-cap scans for touched files, and scoped `git diff --check` (only existing CRLF warning).
- worth-runtime-bridge Phase 2 snapshot subscription read hardening batch: deleted public label-derived `SnapshotReadRequest::for_subscription_slice*` constructors and the `SnapshotReadTarget::requested_target_label`/`snapshot_projection_field_path` path. Subscription snapshot reads now require native `AspectLocator`, optional `AspectFieldLocator`, `AspectMask<ProjectionMask>`, and native target basis; routing lowers `BridgeSubscriptionSlice` directly into that native read target. Snapshot targets retain a projection contract for mask admission and derive a struct projection contract for native scalar field reads rather than treating labels as field authority. Updated touched snapshot/source/pricing fixtures so field-slice record keys are derived from native committed-patch target basis, not old `entity:aspect:signal-field:field` strings. QA split pricing snapshot fixture records out of over-cap `route_and_runtime.rs`. Verified with `cargo fmt -p worth-runtime-bridge`, warning-clean `cargo check -p worth-runtime-bridge --tests`, `cargo test -p worth-runtime-bridge snapshot -- --nocapture`, `cargo test -p worth-runtime-bridge routing -- --nocapture`, `cargo test -p worth-runtime-bridge source -- --nocapture`, `cargo test -p worth-runtime-bridge pricing_shock -- --nocapture`, targeted constructor/key/vocabulary residue scans, line-cap scans, and scoped `git diff --check` (only existing CRLF warnings).
- worth-runtime-bridge Phase 1/2 no-test-shim snapshot result admission batch: deleted the public/test arbitrary string-key `SnapshotReadRecord::new(request_key, value)` authority path and replaced current construction with `SnapshotReadRecord::for_request(&SnapshotReadRequest, value)`. Updated source/materialization readers, facade/subscription tests, structural/policy/stream/speculation/writeback typed certification fixtures, pricing-domain snapshots, pricing provenance fixtures, and harness support so records are minted from native `SnapshotReadRequest` targets instead of hardcoded keys like `user:profile`, `entity-1:profile`, or provenance record-key helpers. QA also removed exact request-key string assertions from packet tests and deleted the unused field-slice record-key helper. Verified with `cargo fmt -p worth-runtime-bridge`, warning-clean `cargo check -p worth-runtime-bridge --tests`, focused snapshot/source/structural/pricing_shock/policy/stream/speculation/writeback tests, residue scans for deleted constructors/key helpers/string-key fixture names and legacy/fallback/compatibility/aspect_bytes/surface_label/domain_payload vocabulary, line-cap scans, and scoped `git diff --check` (only existing CRLF warnings).
- worth-runtime-bridge Phase 1/4 correlation hard-break batch: removed the remaining `request_key` vocabulary from runtime-bridge source/tests and replaced snapshot read matching with typed `SnapshotReadCorrelationId` derived only from native snapshot request canonical basis. Continuity planning/lineage packets now use `BridgeContinuityCorrelationId` instead of request-key wording. Snapshot records, validated records, errors, delivery coordinates, routing digest basis, in-memory fixture lookup, structural fingerprints, pricing provenance, and tests now consume typed correlation ids rather than string formulas. QA found field-slice snapshot fixtures were still minting planned locators for authoritative committed-patch targets; corrected them to use `LocatorAuthority::Authoritative` rather than loosening correlation matching. Verified with `cargo fmt -p worth-runtime-bridge`, warning-clean `cargo check -p worth-runtime-bridge --tests`, focused snapshot/source/routing/continuity/structural/pricing_shock tests, residue scans for request-key and legacy/fallback/compatibility/aspect_bytes/surface_label/domain_payload/JSON authority terms in touched seams, line-cap scan (only existing allowlisted pricing_domain fixture), and scoped `git diff --check` (only existing CRLF warnings).
- worth-runtime-bridge Phase 3 committed-patch target constructor hard break: deleted the public/test label-based committed-patch constructors (`BridgeCommittedPatchItem::entity_field/region/partition/facet/relation_endpoint` and target equivalents) and forced current callers to build `BridgeCommittedPatchTarget` from foundational `AspectLocator`, `AspectFieldLocator`/`CanonicalFieldPath`, and explicit surface kind. Patch targets now retain mutation and projection mask basis, envelope construction rejects non-authoritative locators and mask/field mismatches, non-field aspect targets derive their identity from surface kind instead of arbitrary region/partition labels, and compile-fail coverage proves the old label constructor no longer exists. Split over-cap construction tests into `input/envelope/construction_tests.rs`. Verified with `cargo fmt -p worth-runtime-bridge`, warning-clean `cargo check -p worth-runtime-bridge --tests`, `cargo test -p worth-runtime-bridge construction -- --nocapture`, `routing`, `snapshot`, `source`, `subscription`, `pricing_shock`, and `--test phase_boundaries_compile_fail`; residue scans show only the intentional UI compile-fail old-constructor reference and unrelated snapshot field-name helper remain.

- worth-runtime-bridge Phase 1/4 native target-selector hard break: replaced TruthPatchScope target matching from surface_selector: MappingSelector to native TruthPatchTargetSelector plus foundational AspectKeySelector. Mapping lookup and fine-grained aspect registration matching now compare entity identity + AspectKey + TruthDeltaSurfaceKind/CanonicalFieldPath, not target-label strings. Tests and UI fixtures now construct mapping scopes with AspectKey and FieldKey/native surface-kind selectors; current docs examples use TruthPatchScope::for_entity_field. Split routing surface tests into outing/surfaces_tests.rs to keep touched files under 400 lines. Verified with cargo fmt -p worth-runtime-bridge, warning-clean cargo check -p worth-runtime-bridge --tests, cargo test -p worth-runtime-bridge mapping -- --nocapture, cargo test -p worth-runtime-bridge routing -- --nocapture, cargo test -p worth-runtime-bridge --test phase_boundaries_compile_fail -- --nocapture, target-selector residue scans, line-cap scans, and git diff --check (only CRLF warnings). A downstream cargo check -p worth-query --tests remains blocked by pre-existing relational bridge API drift (RawCommittedPatchEnvelope, deleted string-key snapshot/patch constructors), not by this selector batch.
- worth-runtime-bridge Phase 1/4 native target-basis annihilation batch: removed `target_label` authority vocabulary from committed patch targets, normalized truth surfaces, subscription slices, prior continuity slices, snapshot read coordinates, route diagnostics, terminal route JSON, and pricing aspect certification projections. Subscription declaration intents now construct from foundational `AspectKey`/`FieldKey` or native aspect-surface carriers; the old label-taking `NormalizedSubscriptionSliceIntent::try_new` path is gone. Snapshot read target canonical basis is now derived from `AspectLocator`/`AspectFieldLocator`/projection mask instead of caller-supplied strings. QA found and fixed the intermediate arbitrary `native_target_basis` admission in subscription and snapshot constructors. Verified with `cargo fmt -p worth-runtime-bridge`, warning-clean `cargo check -p worth-runtime-bridge --tests`, focused routing/snapshot/source::row_set/subscription::declaration/continuity/pricing aspect tests, trybuild phase-boundary tests, strict `target_label`/old-constructor residue scans, touched-file line-cap scan, and scoped `git diff --check` (only existing CRLF warnings).

- worth-runtime-bridge Phase 1 no-shim writeback effect artifact vocabulary batch: deleted the remaining derived_effect_digest/derived-effect seam across writeback production records, truth writeback requests, diagnostics, certification matrices, terminal report keys, UI fixtures, and coupled tests. The retained digest is now named writeback_effect_artifact_digest, and strategy-coherence, candidate, diagnostics, and admitted execution receipt records now also retain effect_intent_digest plus foundational effect_intent_patch_canonical_basis proof so the typed effect-intent authority does not collapse to artifact digest-only evidence. Verified with cargo fmt -p worth-runtime-bridge, warning-clean cargo check -p worth-runtime-bridge --tests, cargo test -p worth-runtime-bridge writeback -- --nocapture (85 passed; expected caught-panic output from panic-mapping test), writeback_certification, diagnostics_explanations, diagnostics_replay, causal_envelope_mapping, and --test phase_boundaries_compile_fail; residue scans for derived-effect/effect_digest and no-shim vocabulary passed except foundational InternedString::Raw value-carrier matches; line-cap scan passed; scoped git diff --check showed only existing CRLF warnings.

- worth-runtime-bridge Phase 1 continuity target-basis no-shim batch: removed the caller-supplied `native_target_basis` parameter from `BridgeSubscriptionSlice::from_continuity_parts`, so continuity successor slices now derive target basis from foundational locators, field locators, projection masks, surface kind, and the committed-patch target canonical basis carrier. Tightened the constructor to fail immediately on field/mask/kind mismatches, deleted test-minted `test-native-target` and `builder-test-native-target` basis strings, and changed `TruthDeltaSurface::for_test_target` to derive through `BridgeCommittedPatchTarget` rather than inventing a native-looking target string. Added continuity regression coverage proving successor slices retain the committed-patch target basis including mutation/projection mask proof. Verified with `cargo fmt -p worth-runtime-bridge`, warning-clean `cargo check -p worth-runtime-bridge --tests`, `cargo test -p worth-runtime-bridge continuity -- --nocapture` (48 passed), `cargo test -p worth-runtime-bridge routing -- --nocapture` (30 passed), `cargo test -p worth-runtime-bridge build_ -- --nocapture` (12 passed), no-shim residue scans on the touched routing/continuity/builder slice, line-cap scan, and scoped `git diff --check` (only existing CRLF warnings).

- worth-runtime-bridge Phase 1 subscription declaration target-basis hard-break: replaced the declaration-local subscription target basis formatter with `BridgeCommittedPatchTarget` canonical basis derivation, so declared subscription slices now retain the same mutation/projection mask proof as committed patch routing targets instead of a parallel projection-only string. Strengthened declaration coverage to assert the exact committed-patch target basis and removed the weak `contains("field=")` test shim. Verified with `cargo fmt -p worth-runtime-bridge`, warning-clean `cargo check -p worth-runtime-bridge --tests`, `cargo test -p worth-runtime-bridge subscription::declaration -- --nocapture` (15 passed), `cargo test -p worth-runtime-bridge subscription -- --nocapture` (131 passed), no-shim/residue scans for the declaration slice, touched-file line-cap scan, and scoped `git diff --check` (only existing CRLF warnings).

- worth-runtime-bridge Phase 1/2 snapshot read-target basis hard-break: deleted the literal coarse snapshot read target basis (`"whole-aspect"`) and replaced snapshot target identity with a native read-target canonical basis derived from foundational locator and projection-mask readiness artifacts. Split `snapshot/read_target/native_basis.rs` so read target construction and foundational basis rendering have separate responsibilities. Strengthened packet tests from weak `contains("name")`/literal whole-aspect assertions to exact native locator/mask canonical-basis assertions. QA narrowed the renderer so it only accepts locator and projection-mask entries, avoiding generic compatibility vocabulary in native authority code. Verified with `cargo fmt -p worth-runtime-bridge`, warning-clean `cargo check -p worth-runtime-bridge --tests`, `cargo test -p worth-runtime-bridge snapshot -- --nocapture` (48 passed), `cargo test -p worth-runtime-bridge routing -- --nocapture` (30 passed), no-shim/residue scans for touched snapshot files, line-cap scan, and scoped `git diff --check`.
- worth-runtime-bridge Phase 1 subscription-slice target-basis hard-break: deleted the remaining declaration/lowering/continuity-local field locator and projection-mask canonical basis renderers so subscription intent identity, lowered route-slice identity, and prior continuity slice identity now trust the committed-patch target canonical basis as the single native target proof. Strengthened routing surface tests from weak `contains(...)` checks to exact native committed-target and surface-identity assertions. Verified with `cargo fmt -p worth-runtime-bridge`, warning-clean `cargo check -p worth-runtime-bridge --tests`, `cargo test -p worth-runtime-bridge subscription -- --nocapture` (131 passed), `cargo test -p worth-runtime-bridge continuity -- --nocapture` (48 passed), `cargo test -p worth-runtime-bridge routing -- --nocapture` (30 passed), no-shim/residue scans for deleted local basis helpers and weak contains assertions, touched-file line-cap scan, and scoped `git diff --check` (only existing CRLF warnings).

- worth-runtime-bridge Phase 1 committed-patch target canonical-basis hardening batch: replaced the committed patch target's bridge-local locator/mask string renderer with foundational canonicalization readiness artifacts for `AspectLocator`, `AspectFieldLocator`, `AspectMask<MutationMask>`, and `AspectMask<ProjectionMask>` in `input/envelope/target/native_basis.rs`. Updated committed-target, construction, routing, subscription, continuity, explanation, and pricing tests away from old `aspect=...|field=...` basis strings and weak substring assertions to exact committed-target proof strings. Verification passed with `cargo fmt -p worth-runtime-bridge`, `cargo check -p worth-runtime-bridge --tests`, focused target/construction/routing/subscription/snapshot/pricing_shock/explanations/continuity tests, no-shim residue scans for old target-basis formulas and weak contains assertions, line-cap scan, and scoped `git diff --check` (only existing CRLF warnings).

- worth-runtime-bridge Phase 2/5 row-set projection-basis hard-break batch: replaced `source/row_set.rs` local materialized-field projection basis rendering (`authority:aspect`, `fields:`/`whole-aspect` mask text, and helper functions) with `source/row_set/native_projection_basis.rs`, which derives locator and projection-mask proof from foundational canonicalization readiness artifacts. Strengthened row-set projection tests to assert exact foundational basis text and tightened snapshot contract failure proof from human-message substring matching to `BridgeSnapshotReadErrorKind::RecordCountMismatch`. Verified with `cargo fmt -p worth-runtime-bridge`, `cargo test -p worth-runtime-bridge source -- --nocapture`, `cargo test -p worth-runtime-bridge grouped -- --nocapture`, `cargo check -p worth-runtime-bridge --tests`, row-set residue scans for old basis formulas/helper names/weak assertions, line-cap scan, and scoped `git diff --check` (only existing CRLF warning).
- worth-runtime-bridge Phase 1 source capability no-shim batch: renamed the production source replay capability from `ReplayCompatibleRead` to `ReplayContinuityRead` across source planning, facade tests, harness fixtures, and certification support so the source contract no longer exposes compatibility/coexistence vocabulary. Strengthened source declaration/validation canonical-basis tests from substring checks to exact native basis assertions, and split over-cap `facade/tests/policy_and_materialization.rs` into source declaration, source packet record, and source invariance submodules after touching that test surface. Verified with `cargo fmt -p worth-runtime-bridge`, `cargo test -p worth-runtime-bridge source -- --nocapture`, `cargo test -p worth-runtime-bridge policy_and_materialization -- --nocapture`, `cargo check -p worth-runtime-bridge --tests`, no-shim residue scans, touched-file line-cap scan, and scoped `git diff --check` (only existing CRLF warnings).
- worth-runtime-bridge Phase 1/2 current-test proof hardening batch: removed weak canonical-basis substring assertions from snapshot selector/authority/planned-packet tests, speculation preview-session lifecycle tests, and structural declaration tests, replacing them with exact canonical-basis assertions derived from typed proof carriers. Tightened snapshot read validation tests away from human-message substring matching for record-count, duplicate-record, and contract-validation failures, asserting typed error kind, correlation id, validation denial, and aspect key evidence instead. Verified with `cargo fmt -p worth-runtime-bridge`, `cargo test -p worth-runtime-bridge snapshot -- --nocapture`, `cargo test -p worth-runtime-bridge speculation -- --nocapture`, `cargo test -p worth-runtime-bridge structural -- --nocapture`, `cargo check -p worth-runtime-bridge --tests`, targeted no-shim residue scans, touched-file line-cap scan, and scoped `git diff --check` (only existing CRLF warnings).

- worth-runtime-bridge Phase 1 no-test-shim writeback/envelope proof batch: removed remaining prose-substring assertions from the touched writeback authority/admission/replay tests and committed-patch envelope construction tests. Replaced them with typed `BridgeWritebackErrorKind`, `BridgeWritebackFailureClass`, retained execution/replay/admission records, counters, request/receipt/candidate digest presence, and native patch-coordinate context assertions. This keeps current tests from teaching callers to parse human error strings for authority proof. Verified with `cargo fmt -p worth-runtime-bridge`, `cargo test -p worth-runtime-bridge writeback -- --nocapture` (85 passed; expected caught-panic output from panic-mapping test), `cargo test -p worth-runtime-bridge construction -- --nocapture`, `cargo check -p worth-runtime-bridge --tests`, targeted weak-assertion/no-shim residue scans, touched-file line-cap scan, and scoped `git diff --check` (only existing CRLF warnings).
- worth-runtime-bridge Phase 1 mapping/lineage no-test-shim proof batch: removed weak human-message assertions from mapping freeze and historical lineage authority tests by adding typed `BridgeMappingFreezeContext` proof and precise lineage-source canonicality error variants for duplicate/noncanonical lineage keys, record keys, and traversed event ids. Mapping freeze denials now retain typed primary/conflicting mapping ids or invalid-field proof; facade continuity mapping preserves prior external continuity error semantics while adapter tests assert exact lineage-source causes. Verified with `cargo fmt -p worth-runtime-bridge`, `cargo test -p worth-runtime-bridge historical_lineage_authority -- --nocapture` (7 passed), `cargo test -p worth-runtime-bridge mapping -- --nocapture` (42 passed), `cargo check -p worth-runtime-bridge --tests`, scoped weak-assertion/no-shim residue scans, touched-file line-cap scan, and scoped `git diff --check` (only existing CRLF warnings).

- worth-runtime-bridge Phase 1 delivery/replay no-test-shim proof batch: removed the remaining strict runtime-bridge `to_string().contains(...)`, `format!("{error}")`, and `failure_record.detail().contains(...)` proof assertions from delivery/replay harness tests. Replay tests now assert typed `BridgeReplayErrorKind`, canonical schema version/coherence failures, route/snapshot/subscription-slice context, and retained canonical route/bulk plan digests instead of parsing human messages. Delivery tests now assert typed `BridgeDeliveryErrorKind`, `BridgeFailureClass`, snapshot identity/read coordinates, selected preparation mode, and parallel admission class; snapshot stability proof now reads the retained typed route record instead of harness summary JSON. Verified with `cargo fmt -p worth-runtime-bridge`, `cargo test -p worth-runtime-bridge replay -- --nocapture` (113 passed), `cargo test -p worth-runtime-bridge delivery -- --nocapture` (50 passed), `cargo check -p worth-runtime-bridge --tests`, strict runtime-bridge weak-error scan clean, touched-file no-shim scans clean, touched-file line-cap scan clean, and scoped `git diff --check` (only existing CRLF warnings).

- worth-runtime-bridge Phase 1 preview-residue/history no-test-shim proof batch: split over-cap `subscription/residue.rs` into `residue.rs`, `residue/proof.rs`, and `residue/rejection.rs`, all under the line cap. Preview discard residue rejections now retain typed `BridgeSubscriptionPreviewDiscardResidueRejectionContext` with exact missing/duplicate/nonzero residue category evidence and counts instead of a context string as proof; facade preview-residue tests assert the typed context directly. Historical harness unavailable-view coverage no longer parses the adapter error string and instead asserts the retained typed historical failure record class, branch, and missing commit identity. Verified with `cargo fmt -p worth-runtime-bridge`, `cargo test -p worth-runtime-bridge preview_residue -- --nocapture` (6 passed), `cargo test -p worth-runtime-bridge history -- --nocapture` (17 passed), `cargo check -p worth-runtime-bridge --tests`, broad runtime-bridge weak-error/rejection-context contains scan clean, touched-file no-shim scans clean, touched-file line-cap scan clean, and scoped `git diff --check` (only existing CRLF warnings).

- worth-runtime-bridge Phase 1 snapshot policy no-test-shim micro-batch: removed the remaining snapshot policy unit-test substring proof assertions over canonical-basis/detail text. The tests now assert typed retention admission, source capability, replay continuity, rejection kind, and branch identity directly. Verified with `cargo fmt -p worth-runtime-bridge`, `cargo test -p worth-runtime-bridge snapshot::policy -- --nocapture` (2 passed), snapshot policy weak assertion scan clean, touched-file line cap clean, and scoped `git diff --check` (only existing CRLF warnings).

- worth-runtime-bridge Phase 1 authority-substring proof annihilation batch: removed the remaining non-terminal `.contains(...)` proof assertions from causal envelope receipts, structural publication/reduction, merge publication/declaration, continuity authority, structural validation, subscription preview basis, writeback typed certification, and writeback authority closeout tests. Preview-basis rejections now retain typed `BridgeSubscriptionPreviewBasisRejectionContext` in `subscription/preview/rejection.rs` instead of storing one formatted context string as proof. Split over-cap `harness/tests/planning/continuity/resolution.rs` into `resolution/{single_successor,successor_variants,rejection_outcomes,delivery_and_diagnostics}.rs`. Broad runtime-bridge `.contains("...")` scan now reports only pricing-shock markdown terminal export smoke checks. Verified with `cargo fmt -p worth-runtime-bridge`, `cargo test -p worth-runtime-bridge preview_basis -- --nocapture` (3 passed), `causal_envelope` (30 passed), `structural` (56 passed), `merge` (51 passed), `continuity` (49 passed), `writeback_certification` (8 passed), `authority_evidence_closeout` (2 passed), `cargo check -p worth-runtime-bridge --tests`, no-shim scans, touched-file line-cap scan, and scoped `git diff --check` (only existing CRLF warnings).

- worth-runtime-bridge Phase 1 canonical-basis residue hardening batch: replaced duplicated bridge-local foundational canonical-basis text renderers in source row projection and snapshot read-target basis with one private `canonical_basis` rendering boundary, and moved writeback effect-intent patch basis away from `Debug` entry rendering to the same explicit foundational entry/locus/value renderer. This keeps source, snapshot, and writeback effect-intent digests derived from one native `CanonicalBasisReadyArtifact` projection instead of local mini-serializers. The strict old-authority scan is clean outside terminal export paths for `aspect_bytes`, `surface_label`, `domain_payload`, `legacy`, `fallback`, `compatibility`, `serde_json::Value`, and `json!`; remaining `payload()` occurrences are foundational artifact accessors only. Verified with `cargo fmt -p worth-runtime-bridge`, `cargo check -p worth-runtime-bridge --tests`, `cargo test -p worth-runtime-bridge source -- --nocapture` (60 passed), `cargo test -p worth-runtime-bridge snapshot -- --nocapture` (48 passed), `cargo test -p worth-runtime-bridge writeback -- --nocapture` (85 passed; expected caught-panic output from panic-mapping test), residue scans, touched-file line-cap scan, and scoped `git diff --check` (only existing CRLF warning on `lib.rs`).
- worth-runtime-bridge Phase 1 no-shim writeback mapper evidence hard break: deleted the caller-supplied `domain_evidence_digest` seam from writeback mapper envelopes, mapped family inputs, derived effects, diagnostics explanations, causal retained mapping digests, facade lowering APIs, and writeback/causal-envelope tests. Mapper evidence is now derived only from the admitted writeback contract, typed causality basis, and `BridgeWritebackEffectIntent` backed by foundational authoritative patch proof; tests no longer pass arbitrary evidence digest strings. Verified with `cargo fmt -p worth-runtime-bridge`, warning-clean `cargo check -p worth-runtime-bridge --tests`, `cargo test -p worth-runtime-bridge writeback -- --nocapture` (85 passed; expected caught-panic output from panic-mapping test), `cargo test -p worth-runtime-bridge causal_envelope -- --nocapture` (30 passed), residue scans for domain-evidence/domain-payload/old authority terms, touched-file line-cap scan, structural QA, and scoped `git diff --check` (only existing CRLF warnings).
- worth-runtime-bridge Phase 1 no-test-shim snapshot read contract hard break: deleted `SnapshotReadContract::string` and the redundant `SnapshotReadRequest::for_coarse_contract` alias so coarse and subscription reads no longer have a public/test path that mints read authority from an aspect key alone. Updated production route planning and coupled snapshot/source/routing/structural/facade/harness/pricing tests to pass explicit `SnapshotReadContract::scalar(..., ScalarAspectType::String)` or full struct contracts. Verification passed with `cargo fmt -p worth-runtime-bridge`, warning-clean `cargo check -p worth-runtime-bridge --tests`, focused `snapshot`, `source`, `routing`, `structural`, and `causal_envelope` tests, deletion/residue scans for the removed constructor names and old snapshot authority terms, and scoped `git diff --check` (only existing CRLF warnings). QA note: the next deeper Phase 2 seam is production route/source registration carrying declared read contracts instead of route planning synthesizing string scalar contracts for coarse reads.
- worth-runtime-bridge Phase 1/2 route registration read-contract hard break: added declared `SnapshotReadContract` authority to coarse `BridgeMappingRegistration` and fine-grained `BridgeAspectRegistration`, propagated it through frozen registries, fine-grained match outcomes, subscription slices, prior continuity slices, and canonical read-packet planning. Route planning no longer synthesizes string scalar contracts from aspect keys for coarse or subscription reads; registrations with wildcard aspect selectors now fail validation because they cannot prove one foundational read contract. Updated tests and docs examples to declare real contracts at registration sites rather than using compatibility helpers. Verification passed with `cargo fmt -p worth-runtime-bridge`, `cargo check -p worth-runtime-bridge --tests`, `cargo test -p worth-runtime-bridge routing -- --nocapture`, `mapping`, `continuity`, and `snapshot`; planning/lowering residue scans for synthesized scalar contracts were clean; line-cap scan showed only existing allowlisted over-cap files; scoped `git diff --check` reported only existing CRLF warnings.
- worth-runtime-bridge Phase 1 routing no-test-shim cleanup: removed the production `TruthDeltaSurface::for_test_target` constructor and its `test_committed_patch_target_basis` helper from `routing/surfaces.rs`. Routing matching tests now construct a real `BridgeCommittedPatchEnvelope` with a native `BridgeCommittedPatchTarget` and derive the `TruthDeltaSurface` through `derive_normalized_truth_delta_surface_set`, so tests no longer manufacture native-looking route surfaces outside the committed-patch normalization path. Verified with `cargo fmt -p worth-runtime-bridge`, `cargo check -p worth-runtime-bridge --tests`, `cargo test -p worth-runtime-bridge routing::matching -- --nocapture`, `cargo test -p worth-runtime-bridge routing -- --nocapture`, touched-file no-shim scans, touched-file line-cap scan, and scoped `git diff --check` (only existing CRLF warnings).
- worth-runtime-bridge Phase 1 retained-delivery no-test-shim batch: removed `BridgeSubscriptionRetainedDeliveryWindowSeed::with_canonical_member_count_for_test`, deleted the impossible replay-plan `RetainedWindowExceedsCostProfile` branch that only the shim could reach, and removed the unused member-limit subscription fixture. Native over-budget proof now remains at delivery-window sealing, where the cost profile rejects oversized member sets before retained replay seeds can exist. Verified with `cargo fmt -p worth-runtime-bridge`, `cargo check -p worth-runtime-bridge --tests`, `cargo test -p worth-runtime-bridge resume_replay -- --nocapture`, `cargo test -p worth-runtime-bridge subscription::delivery::cost_profiles -- --nocapture`, no-shim residue scans, touched-file line-cap scan, and scoped `git diff --check` (only existing CRLF warnings).
- worth-runtime-bridge Phase 1 fanout projection no-test-shim batch: removed `BridgeSubscriptionFanoutDeliveryProjectionSet` digest-basis tamper hooks (`with_canonical_member_digest_basis_for_test`, `with_consumer_binding_digest_basis_for_test`) and deleted shim-only projection validation rejection variants/fields (`ConsumerBindingOrderMismatch`, `ProjectionWidthMismatch`, `ProjectionDescriptorMismatch`, `CanonicalMemberDigestMismatch`, `rejected_projection_index`). Fanout projection validation now checks constructible cross-artifact drift between a sealed projection set and the supplied layout, while private-field self-corruption is no longer modeled as a production/test lane. Verified with `cargo fmt -p worth-runtime-bridge`, `cargo check -p worth-runtime-bridge --tests`, `cargo test -p worth-runtime-bridge fanout_projection -- --nocapture`, `cargo test -p worth-runtime-bridge subscription::fanout -- --nocapture`, no-shim residue scans, touched-file line-cap scan, and scoped `git diff --check` (only existing CRLF warnings).
- worth-runtime-bridge Phase 1 canonical replay schema-version no-test-shim batch: removed all remaining `with_schema_version_for_test` private-field mutation hooks from route, continuity, merge, historical, structural, and bulk canonical replay records. Deleted the shim-only unsupported-version tests that fabricated impossible canonical artifacts; kept native replay/drift/coherence coverage on constructible records. Verification passed with `cargo fmt -p worth-runtime-bridge`, `cargo check -p worth-runtime-bridge --tests`, `cargo test -p worth-runtime-bridge replay -- --nocapture`, `cargo test -p worth-runtime-bridge merge -- --nocapture`, `cargo test -p worth-runtime-bridge structural -- --nocapture`, no-test-shim/residue scans, touched-file line-cap scan, and scoped `git diff --check` (only existing CRLF warnings).
- worth-runtime-bridge Phase 1 preview work/residue no-shim batch: deleted the public/test path that admitted preview work and preview residue evidence from arbitrary digest strings. Split over-cap `subscription/preview_work.rs` into `preview_work/{evidence,rejection,records}.rs` plus an aggregation module. `BridgeSubscriptionPreviewWorkInput` now derives typed evidence from `BridgePreviewActiveSubscription`; trace recording rejects evidence from another preview-active proof. `BridgeSubscriptionPreviewResidueArtifactInput` now derives evidence from `BridgeSubscriptionPreviewWorkTrace`, so facade and harness tests no longer pass `preview-routing:*`, `preview-delivery:*`, `preview-diagnostics:*`, `preview-continuation:*`, or `preview-residue-evidence:*` strings. Verified with `cargo fmt -p worth-runtime-bridge`, warning-clean `cargo check -p worth-runtime-bridge --tests`, `cargo test -p worth-runtime-bridge preview_promotion -- --nocapture`, `cargo test -p worth-runtime-bridge preview_residue -- --nocapture`, `cargo test -p worth-runtime-bridge subscription_certification -- --nocapture`, residue scans, touched-file line-cap scan, and scoped `git diff --check` (only existing CRLF warnings).
- worth-runtime-bridge Phase 1 acknowledgement-frontier no-shim batch: deleted the facade/internal path that admitted subscription acknowledgement frontier proof from caller-supplied member identity plus arbitrary digest string. `admit_subscription_acknowledgement_frontier` now consumes the sealed `BridgeSubscriptionDeliveryMemberRecord`; digest and identity evidence are derived from the sealed delivery window/member proof. Removed the shim-only `AcknowledgedMemberDigestMismatch` rejection and wrong-digest test, replacing it with a native cross-window member rejection. Split over-cap `harness/tests/subscription_certification/support.rs` into `support/{mod,source_fixtures,runtime_fixtures,delivery_checkpoint,preview_sessions}.rs` after touching that helper surface. Verified with `cargo fmt -p worth-runtime-bridge`, warning-clean `cargo check -p worth-runtime-bridge --tests`, `cargo test -p worth-runtime-bridge checkpoint -- --nocapture`, `cargo test -p worth-runtime-bridge subscription_certification -- --nocapture`, residue scans, line-cap scan, and scoped `git diff --check` (only existing CRLF warnings).
- worth-runtime-bridge Phase 1 source-artifact evidence no-shim batch: deleted `BridgeSubscriptionSourceArtifactInput::new` so subscription certification source artifacts can no longer be admitted from caller-supplied identity/digest strings. Added typed source-artifact evidence roles/scenarios plus reference-workload family/lane evidence, updated production certification report builders and facade/offline-audit certification tests to use typed evidence, and added a trybuild proof that the old arbitrary digest constructor is gone. QA found and fixed two fossils: stale UI fixtures still using old pre-native fields/signatures, and test helpers carrying a no-op active subscription parameter after evidence stopped deriving from it. Also removed provenance-shaped `Facade*` scenario names from the public evidence enum. Verified with `cargo fmt -p worth-runtime-bridge`, warning-clean `cargo check -p worth-runtime-bridge --tests`, `cargo test -p worth-runtime-bridge subscription_certification -- --nocapture`, `cargo test -p worth-runtime-bridge subscription::certification -- --nocapture`, `cargo test -p worth-runtime-bridge --test phase_boundaries_compile_fail -- --nocapture`, residue scans for arbitrary source-artifact digest admission, touched-file line-cap checks, and scoped `git diff --check` (only existing CRLF warnings).
- worth-runtime-bridge Phase 1/2 snapshot value canonical-basis hard break: deleted the bridge-local raw `SnapshotReadValue`/`AspectValue` serializer (`snapshot/value_canonicalization.rs`) and replaced row-set, grouped-truth, and structural fingerprint digest evidence with canonical basis derived from `ContractValidatedAspectArtifact` admitted into foundational authoritative state. `BridgeMaterializedFieldValue` now carries both the typed read value and its validated-value canonical basis, grouped truth digests consume the retained row-field basis, and structural fingerprints hash the validated canonical basis rather than raw value text. Extended the shared canonical-basis renderer for all current foundational value variants needed by state basis. Verified with `cargo fmt -p worth-runtime-bridge`, warning-clean `cargo check -p worth-runtime-bridge --tests`, `cargo test -p worth-runtime-bridge snapshot -- --nocapture`, `source`, `grouped`, and `structural`, residue scans for the deleted canonicalizer/raw aspect bytes seam, touched-file line-cap scan, and scoped `git diff --check` (only existing CRLF warnings).
- worth-runtime-bridge Phase 1 speculation declaration no-shim hard break: replaced `BridgePreviewSessionDeclaration::new` caller-supplied truth/source/request/schema digest strings with typed `BridgePreviewSessionBasis` built from `BridgeTruthViewSelector`, `BridgeSourceCapabilitySet`, and `BridgePreviewRetainedArtifactSchema`. Deleted `with_structural_basis_digest`/merge-history digest mutation and replaced structural drift with `BridgePreviewStructuralBasis::from_admitted_contract(&AdmittedStructuralComparisonContract)`. Promotion/reuse/execution/subscription preview parent bases now consume derived typed-basis digests only. Updated facade, harness, pricing, causal-envelope, subscription-certification, and compile-fail tests so tests no longer construct preview authority from arbitrary digest strings except UI fixtures proving the old APIs are removed. Verified with warning-clean `cargo check -p worth-runtime-bridge --tests`, `cargo test -p worth-runtime-bridge speculation -- --nocapture`, `preview_basis`, `causal_envelope`, `subscription_certification`, `pricing_shock`, `cargo test -p worth-runtime-bridge --test phase_boundaries_compile_fail -- --nocapture`, residue scans, line-cap scan, and scoped `git diff --check` (only existing CRLF warnings).

## 2026-06-01 - Runtime bridge promotion authority hard break

- Removed `BridgeSpeculativePromotionRequest` and caller-supplied preview promotion authority digests from the standard speculation path.
- Added `BridgePreviewPromotionAuthorityBasis`, deriving promotion commit-boundary/artifact digests from the active preview session, execution record, declaration basis, and admissibility proof.
- Updated production callers and tests/harnesses so preview promotion no longer mints `commit-boundary:*` / `authoritative-artifact:*` literals; added a trybuild fixture proving the removed request API stays dead.
- Verified with `cargo check -p worth-runtime-bridge --tests`, `cargo test -p worth-runtime-bridge speculation -- --nocapture`, `cargo test -p worth-runtime-bridge pricing_shock -- --nocapture`, `cargo test -p worth-runtime-bridge subscription_certification -- --nocapture`, and `cargo test -p worth-runtime-bridge --test phase_boundaries_compile_fail -- --nocapture`.

- worth-runtime-bridge Phase 1 downstream no-shim detox batch: updated worth-query and worth-relational callers/tests after the strict runtime-bridge API break so downstream code no longer uses deleted bridge shims (`BridgeCommittedPatchItem::new`, `SnapshotReadRecord::new`, request-key joins, old mapping selector triples, empty committed-patch envelopes, digest-string preview declarations, string writeback effects, fallback scan counters, proposed/derived effect digest names). Query and relational fixtures now construct native committed patch targets with foundational `AspectLocator`/`CanonicalFieldPath`, explicit `SnapshotReadContract::scalar` read contracts, `BridgePreviewSessionBasis`, `BridgeWritebackEffectIntent` backed by foundational scalar patch proof, and current unindexed-scan/effect-intent evidence names. Updated UI compile-fail fixtures so they fail against current private native fields, not old field names. Verification passed with `cargo fmt -p worth-query -p worth-relational`, `cargo check -p worth-query --tests`, `cargo check -p worth-relational --tests`, `cargo check -p worth-runtime-bridge --tests`, and a narrow deleted-API residue scan across worth-query/worth-relational. Structural QA found no new production topology blocker in the touched query production files; broader query effect-lifecycle still has RawEffectIntent/external JSON authoring surfaces as future aspect-native work, outside this bridge-constructor detox batch.

- worth-runtime-bridge Phase 1 no-test-shim compile-fail detox batch: deleted old UI fixtures that preserved migration-era digest/string authority examples (`preview_declaration_digest_constructor_removed`, `preview_structural_digest_mutator_removed`, `preview_promotion_request_removed`, `subscription_source_artifact_digest_constructor_removed`, and `writeback_receipt_authoritative_artifact_digest_removed`). Replaced them with current native sealed-construction proofs over `BridgePreviewSessionDeclaration`, `BridgePreviewStructuralBasis`, `BridgeSubscriptionSourceArtifactInput`, and `TruthWritebackReceipt` private fields, so compile-fail coverage now teaches native proof packets rather than old digest constructors or `*:legacy` literals. Verified with `cargo fmt -p worth-runtime-bridge`, `cargo test -p worth-runtime-bridge --test phase_boundaries_compile_fail -- --nocapture`, `cargo check -p worth-runtime-bridge --tests`, targeted residue scans for the deleted fixture names/literals/APIs, touched fixture line-cap sanity, and scoped `git diff --check`.
- worth-runtime-bridge Phase 1 no-test-shim committed-patch compile-fail detox: removed the remaining committed-patch UI fixture that preserved the old label-constructor migration story (`committed_patch_item_rejects_label_constructor`). Replaced it with native sealed-construction fixtures proving `BridgeCommittedPatchTarget` and `BridgeCommittedPatchItem` cannot be synthesized by struct literals even when callers use foundational `AspectLocator`, `CanonicalFieldPath`, and mutation/projection masks. Verified with `cargo fmt -p worth-runtime-bridge`, `cargo test -p worth-runtime-bridge --test phase_boundaries_compile_fail -- --nocapture`, `cargo check -p worth-runtime-bridge --tests`, targeted residue scans for the deleted label-constructor seam and old digest/payload constructor names, touched fixture line-cap sanity, WIP cleanliness, and scoped `git diff --check`.
- worth-runtime-bridge Phase 1 subscription replay no-test-shim cleanup: removed the duplicate retained declaration authority field from `BridgeRetainedSubscriptionBundle`, making `declaration()` project from the admitted subscription and deleting the impossible `AdmittedDeclarationMismatch` replay branch that only current tests could fabricate by mutating private fields. Kept the constructible lifecycle/admitted mismatch proof, moved replay tests out of production `subscription/replay.rs` into `subscription/replay_tests.rs`, and removed the stale line-cap allowlist entry for `subscription/replay.rs`. Verified with `cargo fmt -p worth-runtime-bridge`, `cargo test -p worth-runtime-bridge subscription::replay -- --nocapture`, `cargo test -p worth-runtime-bridge replay -- --nocapture`, `cargo check -p worth-runtime-bridge --tests`, touched seam residue scans, line-cap checks, and scoped `git diff --check`.

- worth-runtime-bridge Phase 1 subscription certification report-bundle no-shim batch: deleted the mutable BridgeSubscriptionCertificationReportBundleInput role bag from certification report assembly and replaced it with named BridgeSubscriptionCertificationReportBundleScenario variants. Denied-continuation, historical-basis, fanout, strategy-lowering, and bundle-insufficiency reports now request sealed scenario bundles instead of mutating role/digest fields, so current production/tests no longer teach arbitrary certification source-artifact matrix construction. Verified with cargo fmt -p worth-runtime-bridge, cargo check -p worth-runtime-bridge --tests, cargo test -p worth-runtime-bridge subscription::certification -- --nocapture, mutable-input residue scans, line-cap checks, and scoped git diff --check (only existing CRLF warnings).
- worth-runtime-bridge Phase 1 subscription certification sealed-bundle no-shim closeout: removed the remaining post-seal certification mutation hooks (`with_required_field_count_for_certification` and `with_schema_digest_identity_for_certification`) and deleted the compile-fail fixture that preserved the old schema-rewrite API. Bundle field-count expectations and schema identity now live in typed assembly planning (`BridgeSubscriptionCertificationFieldExpectation` and `BridgeSubscriptionCertificationBundleSchemaIdentity`), so bundle insufficiency, reference workload, report assembly, and schema parity lanes request native planned scenarios before draft assembly instead of corrupting sealed bundles after the fact. Verified with `cargo check -p worth-runtime-bridge --tests`, `cargo test -p worth-runtime-bridge subscription::certification -- --nocapture`, `cargo test -p worth-runtime-bridge subscription_certification -- --nocapture`, `cargo test -p worth-runtime-bridge --test phase_boundaries_compile_fail -- --nocapture`, no-shim residue scans, line-cap checks, and scoped `git diff --check` (only existing CRLF warnings).
- worth-runtime-bridge Phase 1/6 writeback authority request no-shim hard break: removed public digest-only `TruthWritebackRequest::new` and replaced request assembly with crate-private `TruthWritebackRequestEvidence` derived from typed admitted contract, validated candidate, derived writeback effect, mapper witness, feedback provenance, loop-prevention, strategy-coherence, and idempotence proof artifacts. Runtime authority execution and writeback certification harnesses now assemble requests only from those typed artifacts. Mismatched-receipt facade tests no longer fabricate a request from arbitrary digest strings; they establish a native prior request through a real first authority call and then reuse that prior receipt against a second request. QA tightened the compile-fail proof by deleting the migration-smelling digest-constructor fixture and replacing it with `truth_writeback_request_literal_private`, which proves sealed construction without teaching WORTHd digest authority. Verified with `cargo fmt -p worth-runtime-bridge`, `cargo check -p worth-runtime-bridge --tests`, `cargo test -p worth-runtime-bridge --test phase_boundaries_compile_fail -- --nocapture`, `cargo test -p worth-runtime-bridge writeback -- --nocapture` (85 passed; expected caught-panic output from panic-mapping test), `cargo test -p worth-runtime-bridge writeback_certification -- --nocapture`, old constructor/WORTHd-digest residue scans, touched-file line-cap checks, WIP cleanliness, and scoped `git diff --check` (only existing CRLF warnings).

- worth-runtime-bridge Phase 1 writeback feedback-context no-shim hard break: deleted the two-string writeback feedback context admission path from producer metadata, runtime loop-prevention APIs, authority execution, facade tests, and writeback certification harnesses. Added sealed `BridgeWritebackFeedbackContext`, derived only from `BridgeWritebackFeedbackProvenance`; producer metadata now stores the typed context and exposes digest projections only as read/report output. Loop prevention no longer has a partial-feedback branch because partial context is unrepresentable. Harness feedback patches and replay/family certification matrices now pass typed feedback context; hostile tests use constructible native hazards such as different-effect/same-causality feedback or repeated-attempt idempotence instead of WORTHd digest tuples. Deleted the empty-digest construction test shim and added `writeback_feedback_context_literal_private` trybuild coverage proving sealed native construction. Verified with `cargo fmt -p worth-runtime-bridge`, `cargo check -p worth-runtime-bridge --tests`, `cargo test -p worth-runtime-bridge --test phase_boundaries_compile_fail -- --nocapture`, `cargo test -p worth-runtime-bridge writeback -- --nocapture` (84 passed; expected caught-panic output from panic-mapping test), residue scans for the deleted constructor/partial tuple shape, touched-file line-cap checks, and scoped `git diff --check` (only existing CRLF warnings).
- worth-runtime-bridge Phase 1 aspect-label authority detox batch: replaced `BridgePatchCoordinate` and `BridgeSnapshotReadCoordinate` string aspect-label storage with foundational `AspectKey`, switched patch/snapshot coordinate construction to pass native keys, and deleted adjacent `aspect_label()` convenience accessors from committed patch items, snapshot read requests, normalized truth-delta surfaces, subscription slices, subscription slice intents, and continuity prior slices. Route ordering, route diagnostic entries, terminal route JSON export, source/grouped tests, delivery tests, and subscription declaration tests now use `AspectKey` or derived `aspect_key` presentation only. Scoped residue scans for `aspect_label`, `surface_label`, `domain_payload`, `legacy`, `fallback`, `compatibility`, and `shim` are clean across the touched seams. Verified with `cargo fmt -p worth-runtime-bridge`, `cargo check -p worth-runtime-bridge --tests`, `cargo test -p worth-runtime-bridge --lib -- --nocapture` (686 passed; expected caught-panic output from panic-mapping test), `cargo test -p worth-runtime-bridge --test phase_boundaries_compile_fail -- --nocapture`, touched-file line-cap checks, structural QA, and scoped `git diff --check` (only existing CRLF warnings).

- worth-runtime-bridge Phase 1 no-test-shim UI proof detox: removed fake literal digest/proof/basis construction from compile-fail fixtures by replacing arbitrary string/Arc/Some/todo authority examples with sealed-construction placeholders that cannot execute and do not teach a legacy path. Reworked `writeback_family_match_non_exhaustive` so expected compiler output no longer includes `todo!()` help text, and updated trybuild stderr outputs. Verified with `cargo fmt -p worth-runtime-bridge`, `TRYBUILD=overwrite cargo test -p worth-runtime-bridge --test phase_boundaries_compile_fail -- --nocapture`, `cargo test -p worth-runtime-bridge --test phase_boundaries_compile_fail -- --nocapture`, `cargo check -p worth-runtime-bridge --tests`, no-test-shim residue scans over UI fixtures for legacy/fallback/compat/shim/raw/bytes/payload/surface_label/aspect_bytes/domain_payload and fake proof/digest constructors, UI line-cap scan, and scoped `git diff --check` (only existing CRLF warnings).

- worth-runtime-bridge Phase 1 diagnostics writeback state topology batch: split over-cap `diagnostics/state/writeback.rs` into `diagnostics/state/writeback/{mod.rs,retention.rs,accessors.rs}`. The retained writeback authority path now separates typed proof retention/index mutation from read-side accessors, keeping writeback admission, mapper envelope/input, execution, mapper record, and replay artifacts native and free of terminal JSON/export vocabulary. Verified with `cargo fmt -p worth-runtime-bridge`, `cargo check -p worth-runtime-bridge --tests`, `cargo test -p worth-runtime-bridge diagnostics_explanations -- --nocapture`, `cargo test -p worth-runtime-bridge writeback -- --nocapture` (84 passed; expected caught-panic output from panic-mapping test), touched-slice no-shim residue scans, touched-slice line-cap scan, and scoped `git diff --check`.

- worth-runtime-bridge Phase 1 diagnostics facade typed-lookup batch: deleted the over-cap flat `diagnostics/facade/query.rs` and replaced it with `query/{configuration,route_and_failure,source_and_stream,speculation,writeback,merge_and_history,structural,replay_projection}.rs`. Hard-broke the public diagnostics source-commit route lookup from `&str` to `TruthCommitIdentity`, and updated pricing shock support callers to pass the typed commit identity derived from route proof records instead of literal commit strings. Verified with `cargo fmt -p worth-runtime-bridge`, `cargo check -p worth-runtime-bridge --tests`, `cargo test -p worth-runtime-bridge diagnostics_explanations -- --nocapture`, `cargo test -p worth-runtime-bridge pricing_shock -- --nocapture`, residue scans for string source-commit lookup and forbidden shim vocabulary in touched seams, line-cap scans, and scoped `git diff --check` (only existing CRLF warning).

- worth-runtime-bridge Phase 1 facade test-support no-shim topology batch: split the over-cap root `facade/tests.rs` fixture blob into `facade/tests/support/{native_patch_fixture,source_adapter_fixture,snapshot_reader_fixture,runtime_fixture,source_registration_fixture,structural_registration_fixture,writeback_authority_fixture,signal_sink_fixture}.rs`. The root facade test module is now an index plus common imports; native committed-patch fixtures are built through foundational `AspectLocator`, `CanonicalFieldPath`, `AspectKey`, and `FieldKey`; snapshot fixtures return explicit `AspectValue` records; runtime fixtures use native `TruthPatchScope::for_entity_field` and `SnapshotReadContract::scalar`. Support exports are narrowed to `crate::facade::tests` so they cannot become crate-wide convenience shims. Verified with `cargo fmt -p worth-runtime-bridge`, warning-clean `cargo check -p worth-runtime-bridge --tests`, `cargo test -p worth-runtime-bridge facade -- --nocapture` (293 passed; expected caught-panic output from panic-mapping test), touched-tree line-cap scan, no-shim residue scans, and scoped `git diff --check` (only existing CRLF warning).
- worth-runtime-bridge Phase 1 pricing-domain harness no-shim topology batch: deleted the over-cap flat `harness/tests/pricing_domain.rs` and replaced it with `pricing_domain/{mod,material_catalog,attribution,feed_profiles,world_state,stream_simulation,snapshot_export,pricing_math,tests}.rs`. The split makes the native snapshot fixture boundary explicit: material catalog owns `SnapshotReadRequest` construction through `SnapshotReadContract::scalar` and foundational `AspectKey`/`ScalarAspectType`; snapshot export emits `SnapshotReadRecord::for_request` with `AspectValue::String` and `TruthSnapshotIdentity`. Pricing-shock consumes the pricing-domain evidence through narrow `crate::harness::tests` visibility rather than a crate-wide helper shim. Verified with `cargo fmt -p worth-runtime-bridge`, warning-clean `cargo check -p worth-runtime-bridge --tests`, `cargo test -p worth-runtime-bridge pricing_domain -- --nocapture` (5 passed), `cargo test -p worth-runtime-bridge pricing_shock -- --nocapture` (41 passed), line-cap scans for the new topology, no-shim residue scans for old snapshot/raw/payload/bytes/legacy/fallback/compat/shim APIs, and scoped `git diff --check`.
- worth-runtime-bridge Phase 1 facade stream no-shim topology batch: deleted the over-cap flat `facade/tests/stream.rs` and replaced it with `stream/{mod,contract_resolution,window_canonicalization,checkpoint_publication,replay_records,checkpoint_resume,delivery,diagnostics_retention}.rs`. The split preserves the existing native `canonical_envelope` fixture path built from foundational `AspectLocator`, `CanonicalFieldPath`, `AspectKey`, and `FieldKey`, while making the admitted stream proof chain spatially explicit: declaration resolution, canonical window planning, checkpoint publication, replay validation, checkpoint resume, delivery, and diagnostics retention. QA found and fixed the first-pass wildcard imports so each module now imports only `runtime`, `canonical_envelope`, and `BridgeRuntimePolicy` as needed. Verified with `cargo fmt -p worth-runtime-bridge`, warning-clean `cargo check -p worth-runtime-bridge --tests`, `cargo test -p worth-runtime-bridge stream -- --nocapture` (44 passed), line-cap scan for the new stream topology, no-shim residue scan for raw/payload/bytes/legacy/fallback/compat/shim/old-constructor APIs, and scoped `git diff --check`.
- worth-runtime-bridge Phase 1 facade policy/speculation no-shim topology closeout: deleted the final two over-cap facade test blobs, `facade/tests/policy_phase2.rs` and `facade/tests/speculation.rs`, replacing them with proof-family directories. Policy Phase 2 tests now live under `policy_phase2/{mod,admission,provenance,route_policy}.rs`; speculation tests now live under `speculation/{mod,lifecycle,reuse,promotion,replay_and_diagnostics}.rs`. The split keeps shared helpers narrow and native: policy uses admitted/lowered policy contracts plus provenance/replay bundles, and speculation uses typed preview session basis, source capability set, retained artifact schema, admitted structural registry, and promotion admissibility proof. Removed the stale CI line-cap allowlist entries for the deleted flat files. Verified with `cargo fmt -p worth-runtime-bridge`, warning-clean `cargo check -p worth-runtime-bridge --tests`, `cargo test -p worth-runtime-bridge policy_phase2 -- --nocapture` (17 passed), `cargo test -p worth-runtime-bridge speculation -- --nocapture` (27 passed), no-shim/wildcard/stale-path residue scans, crate-wide runtime-bridge Rust line-cap scan, and scoped `git diff --check` (only existing CRLF warning).
- worth-runtime-bridge Phase 1 policy replay bundle digest-constructor hard break: deleted public `BridgePolicyReplayBundle::from_digests`, which could mint replay bundle authority from caller-supplied contract/lowered/provenance digest strings. `BridgePolicyReplayBundle::from_canonical_records` now derives retained digests and canonical basis directly from `AdmittedBridgePolicyContract`, `LoweredBridgeExecutionPolicy`, and `BridgePolicyProvenanceRecord`. Added `policy_replay_bundle_digest_constructor_removed` compile-fail coverage using sealed placeholders instead of fake digest literals, so current tests prove the old arbitrary digest API is gone without teaching the old shape. Verified with `cargo fmt -p worth-runtime-bridge`, warning-clean `cargo check -p worth-runtime-bridge --tests`, `cargo test -p worth-runtime-bridge policy_phase2 -- --nocapture` (17 passed), `cargo test -p worth-runtime-bridge policy_certification -- --nocapture` (3 passed), `cargo test -p worth-runtime-bridge --test phase_boundaries_compile_fail -- --nocapture`, production residue scan for `from_digests`, fixture fake-digest-literal scan, scoped line-cap scan, and scoped `git diff --check` (only existing CRLF warning).

- worth-runtime-bridge Phase 1 no-test-shim UI proof closeout: removed the remaining fake authority placeholders in compile-fail fixtures that still used empty grouped-truth digest construction, `todo!`, `unimplemented!`, and arbitrary digest/proof literals. `grouped_truth_view_artifact_fields_private` now proves private artifact fields with sealed placeholders instead of `BridgeGroupedTruthViewDigest::new(&[])`; causal-envelope and subscription reference-workload UI fixtures now use sealed placeholders for private proof fields and nested proof carriers. Verified with `cargo fmt -p worth-runtime-bridge`, `TRYBUILD=overwrite cargo test -p worth-runtime-bridge --test phase_boundaries_compile_fail -- --nocapture`, `cargo test -p worth-runtime-bridge --test phase_boundaries_compile_fail -- --nocapture`, `cargo check -p worth-runtime-bridge --tests`, fake-constructor/no-shim residue scans, touched-file line-cap checks, and scoped `git diff --check` (only existing CRLF warnings).
- worth-runtime-bridge Phase 1 writeback authority-denial/bypass annihilation batch: hard-broke the remaining writeback `bypass` vocabulary out of production and tests. `AuthorityBypassRejected` is now `AuthorityDenied`; writeback counters export `writeback_authority_denial_count`; policy zero-authority proof uses `authority_escape_count`; `writeback-bypass-certify` is gone in favor of `writeback-authority-denial-certify`; `bypass_authority`/`bypass_certification` modules became `authority_denial`/`authority_denial_certification`; terminal JSON now emits `authority-denial-certification` and `denial_class`, with no old key aliases. Also detoxed writeback certification harnesses so duplicate, feedback, replay-mismatch, and family certification no longer use fake `strategy:sha256:writeback*`, `effect:sha256:writeback*`, `route:sha256:writeback*`, `truth-view:sha256:writeback*`, or `truth-state:sha256:writeback*` literals; those values now derive through harness digest helpers or native contract/effect/idempotence/route artifacts. Verified with `cargo fmt -p worth-runtime-bridge`, `cargo check -p worth-runtime-bridge --tests`, `cargo test -p worth-runtime-bridge writeback_certification -- --nocapture`, `cargo test -p worth-runtime-bridge policy_certification -- --nocapture`, `cargo test -p worth-runtime-bridge authority_feedback -- --nocapture`, no-bypass/no-writeback-cert-fake-digest residue scans, and touched line-cap checks. Remaining Phase 1 no-test-shim gap: facade writeback tests and pricing-shock support still contain broad digest-shaped literals and need the next detox batch.
- worth-runtime-bridge Phase 1 facade/pricing writeback no-test-shim detox batch: removed fake `strategy:sha256:*`, `effect:sha256:*`, `trigger:sha256:*`, `truth-state:sha256:*`, `route:sha256:*`, and `truth-view:sha256:*` construction from facade writeback tests and pricing-shock writeback support. Facade writeback support now derives strategy descriptor, causality route/evaluation/truth-view, trigger, and authoritative-state evidence from semantic labels, with guardrails rejecting digest-shaped labels; effect intent still goes through foundational `BridgeWritebackEffectIntent::validated_scalar_patch`. Pricing support gained a dedicated `support/writeback_authority.rs` so writeback declaration/policy/causality/evidence derivation is separated from route/runtime fixture construction and touched files stay under line cap. Verified with `cargo fmt -p worth-runtime-bridge`, warning-clean `cargo check -p worth-runtime-bridge --tests`, `cargo test -p worth-runtime-bridge writeback -- --nocapture` (84 passed; expected caught-panic output from panic-mapping test), `cargo test -p worth-runtime-bridge pricing_shock -- --nocapture` (41 passed), targeted fake-digest residue scans, and touched line-cap scans.
- worth-runtime-bridge Phase 1 writeback continuity mutation evidence hard-break: deleted caller-supplied `basis_binding_digest`, `lineage_digest`, and `continuity_resolution_digest` inputs from `BridgeContinuityMutationBundle` constructors. Continuity mutation bundles now derive binding-basis, lineage, and resolution digests from semantic continuity evidence, reject digest-shaped semantic inputs such as `*:sha256:*`, and retain private fields plus compile-fail coverage. Split the production surface into `writeback/continuity_mutation/{mod.rs,digest_basis.rs,semantic_input.rs}` so construction/accessors, digest-basis derivation, and semantic input rejection are spatially separated. Updated facade writeback tests to vary real successor/target-binding evidence instead of fake continuity digest strings and added hostile rejection coverage for digest-shaped successor evidence. Verified with `cargo fmt -p worth-runtime-bridge`, `cargo check -p worth-runtime-bridge --tests`, `cargo test -p worth-runtime-bridge writeback -- --nocapture` (86 passed; expected caught-panic output from panic-mapping test), `cargo test -p worth-runtime-bridge --test phase_boundaries_compile_fail -- --nocapture`, targeted continuity fake-digest residue scans, touched line-cap scans, and scoped `git diff --check` (only existing CRLF warnings).
- worth-runtime-bridge Phase 1 digest-label authority detox batch: tightened the remaining causal-envelope and mutation-evidence digest seams so production code no longer exposes caller-labelled generic digest helpers in the touched authority path. `diagnostics/causal_envelope` now derives through closed `BridgeCausalEnvelopeDigestArtifact` variants via `causal_envelope_digest`; retained mapping and writeback expected digest mirrors use artifact labels internally rather than caller labels; writeback mutation evidence exposes named artifact-family digest functions instead of `aggregate_digest(label, ...)`. Facade/pricing writeback support now names semantic evidence text instead of labels while retaining guards that reject digest-shaped authority input. QA found no further blocker in the scoped seam; remaining Phase 1 closeout target is broader semantic-evidence test digest helpers such as `truth_state_digest("...")`, which are not fake `*:sha256:*` literals but still derive digest evidence from semantic text. Verified with `cargo fmt -p worth-runtime-bridge`, warning-clean `cargo check -p worth-runtime-bridge --tests`, `cargo test -p worth-runtime-bridge writeback -- --nocapture` (86 passed; expected caught-panic output), `cargo test -p worth-runtime-bridge causal_envelope -- --nocapture` (30 passed), `cargo test -p worth-runtime-bridge pricing_shock -- --nocapture` (41 passed), scoped residue/line-cap scans, and scoped `git diff --check` (only existing CRLF warnings).
- worth-runtime-bridge Phase 1 writeback authoritative-state no-test-shim closeout: removed the `#[cfg(test)]` production constructor `BridgeWritebackAuthoritativeStateBasis::from_effect_and_test_basis` and deleted the facade writeback `WritebackTruthStateBasis` enum/helper path. Facade writeback tests now derive authoritative-state basis through the same production-native `BridgeWritebackAuthoritativeStateBasis::from_effect` path used by harness certification and pricing shock support; the idempotence drift test now changes real causality/effect-intent evidence instead of arbitrary state labels. Touched support stayed under the 400-line cap. Verified with `cargo fmt -p worth-runtime-bridge`, warning-clean `cargo check -p worth-runtime-bridge --tests`, `cargo test -p worth-runtime-bridge writeback -- --nocapture` (86 passed; expected caught-panic output), `cargo test -p worth-runtime-bridge writeback_certification -- --nocapture` (8 passed), `cargo test -p worth-runtime-bridge pricing_shock -- --nocapture` (41 passed), `cargo test -p worth-runtime-bridge --test phase_boundaries_compile_fail -- --nocapture`, no-test-shim/fake-digest residue scans, line-cap checks, and scoped `git diff --check` (only existing CRLF warnings).
