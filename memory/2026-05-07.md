# 2026-05-07

- Continued `_docs/worth/worth-query-domain-substrate.md` from late Phase 2 into Phase 3.
- Added Worth topology no-N-plus-one closeout contract rows for lowering breadth, fallback posture, view parity, and relationship proof posture.
- Verified the new contract surface through focused domain/runtime tests, public API contract coverage, trybuild forgeability denial, and the full `cargo test -p worth-topo` suite.
- Tightened `phase_three_ready()` so it requires every no-N-plus-one contract row to be satisfied, not just clear blocker rows. Added a regression proving query-native execution without parity evidence remains blocked.
- Ran a QA loop on Phase 3 no-N-plus-one closure. Fixed two proof-surface weaknesses: contract labels now use the spec's exact `worth_topology_read_*` names, and `ViewParity` now requires both replay and branch-local decoded-view parity before `phase_three_ready()` can become true.
- Ran a test-QA pass on the Phase 3 domain-query tests. Lifted duplicated parity artifact setup into a narrow domain-query `parity_harness`, added the branch-local-only readiness denial case, and hardened assertions so no-N-plus-one row digests carry the stable contract names.
- Ran a composition-focused code-quality QA pass. Extracted the no-N-plus-one view-parity decision into a named production status function and split readiness test setup from parity recording/assertion. Noted remaining structural debt: `crates/worth-topo/src/query/domain` is still a broad flat module and should be reshaped by responsibility in a dedicated cleanup.
- Implemented the Milestone 3 side-quest closeout gate. The hostile-suite certification report now carries a serializable no-N-plus-one side-quest closeout report with domain-read request/parity counters, named contract rows, blocker rows, and `phase_three_ready`.
- Added the Milestone 3 aggregate return gate on top of the side-quest closeout. The hostile-suite report now exposes `side_quest_gate_ready`, `milestone_three_return_gate_blocker_rows`, and `milestone_three_return_gate_ready`, so the current suite can honestly say the side quest is unblocked while Milestone 3 remains blocked by `missing_required_scenario:SplitCollapseChurn`.
- Ran QA loop on the aggregate return gate. Found and fixed that raw string blockers were too weak for the Phase 3 proof-row standard; replaced them with public `WorthMilestoneThreeReturnGateBlockerRow` rows and made `milestone_three_return_gate_ready` derive from row absence.
- Implemented the missing Milestone 3 `SplitCollapseChurn` hostile scenario. It certifies a query-native connected wire split followed by owner-retiring collapse/rehome churn, exposes `WorthMilestoneThreeSplitCollapseChurnWitness`, adds a public scenario entrypoint, and turns the suite return gate green when the Phase 3 side quest is ready.
- Added the Milestone 3 closeout requirements registry. `milestone_three_closeout_requirements()` and `milestone_three_closeout_suite_definition()` now declare the five hostile scenarios, replay rows, rejection rows, side-quest closeout output, and return-gate output, with tests proving the live hostile suite satisfies the declared contract.
- Ran QA loop on the Milestone 3 closeout registry. Fixed two findings: scenario/replay/rejection requirements now derive from one typed Milestone 3 source, and `certify_milestone_three_closeout()` now enforces the declared closeout report surfaces before returning. Verified with `cargo fmt --check` and full `cargo test -p worth-topo`.
- Closed `_docs/worth/worth-query-domain-substrate.md` after final QA. The doc now marks Phase 1-3 closed, treats Phase 4 as future domain-adoption guidance, records shipped evidence/docs, and states that Milestone 3 is unblocked by the side-quest closeout gate. Re-ran stale-wording grep, `cargo fmt --check`, and full `cargo test -p worth-topo`.
- Ran an additional query-surface QA pass after closeout. Confirmed the active `worth-topo` domain-read module has no direct `workspace.read`, `WorthTopologyQueryRowLookup`, or snapshot-index bypasses; raw row lookup remains only in certification/read-view/test scaffolding for seed and edit-id resolution. Updated Forge Query read-composition runtime/docs so the public phase gate says the topology Phase 3 aggregate proof and Milestone 3 resume gate are satisfied, then fixed the one stale trybuild stderr path caused by the execution preflight split. Verified with `cargo fmt --check`, focused read-composition tests, full `cargo test -p worth-topo --quiet`, and full `cargo test -p forge-query --quiet`.
- Fixed the first four query-code quality issues before Milestone 3 continuation: split `query/domain` by responsibility, deduplicated read-family mechanics, enriched domain views so hostile witnesses no longer use `WorthTopologyQueryRowLookup`, and split over-cap write lowering command orchestration. Full `cargo test -p worth-topo --quiet`, `cargo check -p worth-topo --quiet`, `cargo fmt --check`, and targeted line-cap scans passed.
- Ran the topology/schema naming purge requested by the user. Removed `Worth`/`worth_` from source, test, docs, runtime labels, compile-fail fixtures, and public Rust library imports inside `crates/worth-schema` and `crates/worth-topo`; retained only Cargo package dependency names (`worth-schema`, `worth-topo`, `worth-math`) as manifest/package identity. Verified with literal scans plus `cargo test -p worth-schema --quiet`, `cargo test -p worth-topo --quiet`, and `cargo fmt --check`.
- Implemented Forge Query basis-aware reusable read-family execution. `execute_read_family_in_basis_context` now runs a `ForgeQueryReadFamily` against an admitted current, branch, historical, or preview-derived query basis context, preserves the context query/basis digests on the receipt, and denies mismatched-context substitution before materialization. Added kernel coverage for all four basis families plus substitution denial, updated support/docs evidence, and verified with focused read-composition tests, query-context tests, trybuild phase-boundary tests, full `cargo test -p forge-query --quiet`, and `git diff --check`.
- Wired topology snapshot read-only domain reads through the existing historical/query-context basis surface. Added a sealed Forge Query runtime snapshot basis resolver, routed snapshot topology read families through admitted historical contexts, recorded historical engine/basis/snapshot receipt evidence on domain reports, flipped snapshot read support rows only after proof tests, and updated runtime/domain/Forge Query docs. Verified with full `cargo test -p forge-query --quiet`, full `cargo test -p worth-topo --quiet`, `cargo test -p worth-topo --test ui --quiet`, and `git diff --check`.
- Cleaned stale query-read documentation artifacts after snapshot historical execution landed. Updated the side-quest closeout doc from old `WorthTopology...` and fallback-first language to the current `TopologyDomainQuery`/`topology_read_*` surfaces, changed Forge Query read-composition closeout evidence to point snapshot read-only topology at the admitted historical basis-aware path, and added a topology doc freshness guard preventing regressions to deferred historical-read wording. Verified with `cargo fmt --package forge-query --package worth-topo --check`, full `cargo test -p forge-query --quiet`, full `cargo test -p worth-topo --quiet`, and `git diff --check`.
- Ran QA loop across the last three batches. Found and fixed that generic basis-context read-family execution was preserving receipt basis while still materializing current-head rows for non-current contexts; it now uses runtime rows only for current/same-snapshot contexts and context-derived rows otherwise. Also removed remaining certification direct-row-lookup bypasses, added stale naming/doc freshness guards, polished post-purge topology runtime error strings, and verified with full `forge-query`, `worth-topo`, and worth-topo UI suites plus `cargo fmt --check` and `git diff --check`.
- Ran test-quality QA on the same surface. Strengthened Forge Query basis-context tests so they prove row materialization as well as receipt labeling, including the same-snapshot historical case that must use runtime rows. Also fixed tests and production messages that were still certifying post-purge leading-space topology strings. Verified focused basis/domain/certification tests, full `cargo test -p forge-query --quiet`, full `cargo test -p worth-topo --quiet`, `cargo fmt --package forge-query --package worth-topo --check`, line/directory caps, stale-string scans, and `git diff --check`.
- Ran code-quality QA on the query/runtime cleanup. Replaced snapshot historical-domain-read detection that scraped BranchPreview denial prose with explicit public-support evidence (`topology-snapshot-historical-basis`) carried on unsupported support rows, and decomposed topology write-command lowering into named insert/delete/update command-family lowerers. Verified with focused snapshot/edit tests, full `cargo test -p forge-query --quiet`, full `cargo test -p worth-topo --quiet`, `cargo fmt --package forge-query --package worth-topo --check`, and `git diff --check`.
