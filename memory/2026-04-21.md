2026-04-21

- Drafted Forge Store Milestone 12 spec for artifact format evolution and rolling compatibility. Core constraint: mixed old/new readers, writers, stores, replicas, backups, restores, derived families, maintenance, and tier records must either preserve semantic truth through explicit compatibility witnesses or fail typed before partial truth acceptance. Added roadmap link to `_docs/forge-store/milestone-12.md`.
- QA-hardened Forge Store Milestone 12 to make it more tangible: added a concrete first-ship artifact family catalog, practical compatibility subsystem layout, explicit compatibility relations instead of numeric version inference, decode quarantine stages, registry exhaustiveness, manifest publication/recovery rules, first-ship policy limits, failure topology, stronger compile-fail obligations, expanded counters, and concrete certification matrix lanes.
- Added a focused performance hardening pass to Milestone 12: compatibility admission receipts for batch-local proof reuse, manifest-backed compatibility indexes, adapter cost classes with hot-read rejection, scoped restore breadth budgets, exact counters for relation rechecks/index artifact scans/adapter widths, and certification lanes for receipt reuse, manifest-bounded index rebuild, adapter hot-path rejection, and restore scope bounding.
- Revised Forge Query Milestone 9 spec to make policy-aware narrowing, tenant truth/schema basis, relationship-proof denial, and delivery-shape parity explicitly runtime-backed while `forge-store` remains unfinished. Added a store-gated handoff phase so Milestone 10 owns store-backed execution parity and Milestone 11 owns durable query/artifact/cursor reload.
- QA-hardened Forge Query Milestone 9 spec for practical implementation traps: masked fields influencing predicates/order/grouping/live relevance, explicit authority input artifacts, phase-typed facade transitions, concrete module topology, tri-state policy admission, and a concrete EmployeeRecord certification fixture.
- Performance-hardened Forge Query Milestone 9 spec with policy cost/locality/density postures, `PolicyWorkBudget`, allocation lifecycle contracts, unknown-cost denial, cross-tenant/schema fanout denial, live sparse-to-burst posture, and scale-slope certification across fixture sizes.
- Clarified Forge Query Milestone 9's hardest seam: aspect masking and Zanzibar-style relationship proofs must apply identically before truth touch across current one-shot reads, branch reads, historical reads, historical diffs, live subscriptions, delivery, and saved-query reuse. Added a hard execution-seam matrix and policy-aware optimizer-input requirements so policy cannot be bolted on after ordinary plan lowering.
- Created `_docs/forge-query/milestone-9-phase-1-implementation-plan.md` for Policy And Tenant Context Admission. It scopes Phase 1 to authority input artifacts, policy basis admission, tenant truth/schema basis admission, combined `AdmittedPolicyTenantContext`, saved-query policy/tenant reuse classification, support metadata, compile-fail boundaries, and certification, with explicit no-truth-touch/no-masking/no-execution gates.
- Implemented Forge Store Milestone 12 Phase 1 vocabulary foundation: added `crates/forge-store/src/compatibility/` with first-ship registry/catalog, manifest digest identities, decode quarantine/admission type stages, authoritative/derived/rolling/restore shells, adapter cost/parity vocabulary, M12 evidence shells/counter names, StoreErrorKind compatibility topology, unit tests, and trybuild compile-fail boundaries for witness constructors, quarantined semantic access, and complete family declaration posture.
- Implemented Forge Store Milestone 12 next batch (Phase 2A admission core): added in-memory manifest index reconstruction from registry snapshots, declared compatibility edge registry, reader/write admission planning internals, batch-local receipt reuse counters, quarantined-to-checked artifact transition behind crate-private receipt validation, M12 admission report shape, unit tests for typed rejection/admission/boundedness, and trybuild coverage for receipt and checked-artifact constructor privacy. No persistence, SQLite schema, facade read/write, restore, rolling execution, adapter execution, or derived rebuild behavior was added.
- Continued Forge Store Milestone 12 Phase 2A by replacing string-only admission failures with `CompatibilityRejectionKind`, mapping those kinds to `StoreErrorKind`, tracking accepted/rejected admission counts, and adding `Milestone12AdmissionReport::from_admission_counters`. Tests now assert typed undeclared-family, unsupported-format, unsupported-semantic, and missing-edge rejection surfaces plus exact report projection.
- Implemented Forge Store Milestone 12 Phase 2B decode quarantine/admission envelope hardening: added raw frame headers, pre-quarantine malformed/truncated rejection counters, structural digest quarantine construction, read/write admission outcome report shells, declared adapter edges with hot-read/batch/maintenance cost gating, admitted native vs forward/backward counters, M12 report projection fields, and trybuild guards preventing external construction of quarantined artifacts, adapter parity witnesses, and admission outcomes. Verified with focused compatibility, milestone_12, and phase-boundary compile-fail gates.
- Continued Forge Store Milestone 12 with derived reuse planning: exact derived reuse now requires a matching admitted read receipt, native relation produces a crate-private reuse proof, non-native admitted relations produce a rebuild-required plan instead of silent reuse, mismatched receipts reject with typed `CompatibilityDerivedReuseIncompatible`, and M12 counters/reporting include derived reuse incompatibility and rebuild-required counts. Added unit coverage and trybuild guards preventing external reuse witness construction or extraction of the plan's private receipt.
- Ran a `$qa-loop` hostile pass over Forge Store Milestone 12 work so far. Corrected proof-chain gaps: admission receipts now bind observed and target semantic versions, derived reuse planning requires a `DerivedFamilyDeclaration` so authoritative native receipts cannot masquerade as derived reuse, derived invalidation/rebuild/debt/stale constructors are crate-private with trybuild guards, M12 counter vocabulary names every implemented admission/quarantine/derived counter, and read/write admission outcomes expose read-only posture/counters while keeping constructors sealed. Focused compatibility, milestone_12, and phase-boundary compile-fail gates passed.
- Ran a second production-grade `$qa-loop` pass over Forge Store Milestone 12 compatibility admission. Fixed a critical admission leak where an explicit `CompatibilityRelation::Incompatible` edge could produce a receipt, made overlong frames reject before quarantine, and added visible counters/reporting for out-of-scope adapter rejections and admitted adapter relations. Focused compatibility, milestone_12, and phase-boundary compile-fail gates passed.
- Created `_docs/forge-store/milestone-12-phase-2c-implementation-plan.md` after reviewing the M12 spec against implementation state. Next honest batch is manifest publication/recovery plus authoritative partial-truth gates, not rolling/restore execution yet, because later derived/restore/replica work needs published/recovered manifest proof and explicit authoritative meaning admission.
- Implemented Forge Query Milestone 9 Phase 1 policy/tenant admission foundation: added `policy_basis` and `tenant_basis` modules, public facade exports, admitted context/bundle artifacts, policy/branch/tenant/schema authority snapshots, typed denial counters, saved-query policy/tenant reuse classification, support profile metadata, Milestone 9 certification harness rows, and compile-fail boundaries preventing admitted-context fabrication, raw query admission, bool shortcuts, and tenant truth identity minting. Verified with `cargo test -p forge-query`.
- Continued Forge Query Milestone 9 Phase 1 hardening: moved tenant-basis admission into `tenant_basis::admission`, encoded `PolicyCostPosture` and `PolicyWorkBudget` into policy snapshots/bases/admission bundles, denied unknown policy cost before tenant truth admission, expanded support metadata to mark Phase 2+ surfaces as deferred, added work-budget certification rows, and added raw authority snapshot struct-literal compile-fail guards. Verified with `cargo test -p forge-query`.
- Created `_docs/forge-query/milestone-9-phase-2-implementation-plan.md` for Pre-Execution Narrowing And Validation. It scopes the next batch to `AuthorizedProjectionArtifact`, masked/hidden influence validation, one bounded relationship-proof descriptor family, `PolicyAwareValidationReport`, immutable `NarrowedPolicyQueryArtifact`, Phase 2 work budgets/counters, saved-query/scope/template/view-shape integration, support honesty, compile-fail guards, and certification rows, with explicit no-truth-touch/no-execution/no-live/no-diff/no-delivery boundaries.
- Implemented Forge Query Milestone 9 Phase 2 foundation: added `authorized_projection`, `relationship_proof`, and `policy_narrowing` modules; `narrow_policy_query` now requires a canonical bundle plus `AdmittedPolicyTenantContext`, derives immutable authorized projection/masked projection identities, denies masked predicate/order/result emission before narrowing, admits bounded direct/ancestor/tenant relationship-proof descriptors without truth touch, emits `PolicyAwareValidationReport` and `NarrowedPolicyQueryArtifact`, extends Milestone 9 certification rows with Phase 2 evidence, and adds compile-fail guards for mask mutation, narrowed-artifact construction, raw canonical query bypass, and bool proof shortcuts. Verified with `cargo test -p forge-query`.
- Continued Forge Query Milestone 9 Phase 2: added `PolicyAwareOptimizerInput` derived only from `NarrowedPolicyQueryArtifact`, saved-policy-narrowing reuse descriptors/classification so exact saved reuse cannot skip projection/proof drift, tenant-membership and bounded-ancestor relationship-proof tests, extra compile-fail guards for optimizer raw-query bypass and `PolicyAwareValidationReport` fabrication, and certification rows for tenant-membership proof admission plus saved exact reuse/drift. Verified with `cargo fmt -p forge-query; cargo test -p forge-query` (469 tests plus trybuild).
- Ran a strict `$qa-loop` over Forge Query Milestone 9 Phase 2 against the domain/architecture/performance laws. Fixed the biggest proof-chain gap by introducing `PolicyMaskSnapshot`, so narrowing now requires a policy-digest-bound mask matching `AdmittedPolicyTenantContext` instead of accepting a raw host mask. Also enforced digest-part budgets, bound validation-report digests to authorized projection/proof identities, added relationship proof query-shape mismatch denial, expanded Phase 2 certification rows for non-disclosing use/query conflict/template-hidden influence, and corrected the Phase 2 plan's compile-fail verification command. Verified with focused policy narrowing, relationship proof, milestone certification tests, and full `cargo test -p forge-query` (473 tests plus trybuild).
- Ran a second production-grade `$qa-loop` over Forge Query Milestone 9 Phase 2. Found that grouped/template/derived hidden influence was still too implicit: certification covered nearby predicate/order behavior but no first-class influence artifact existed. Added `authorized_projection::PolicyInfluenceSet` plus purpose-tagged influence entries for grouping, derived result fields, and template predicates; `narrow_policy_query` now requires this influence set and budget validation counts it. Authorized projection now denies masked/non-disclosing grouping and derived influence with typed counters, and template predicate influence routes through the same predicate leakage rule. Updated Phase 2 plan and verified with `cargo test -p forge-query` (475 tests plus trybuild).
- Continued the Phase 2 QA loop by closing compile-fail guard holes from the plan: external crates can no longer call the host-callback relationship-proof test constructor, and trybuild now proves `MaskedProjectionArtifact` cannot be passed where `AuthorizedProjectionArtifact` is required. Full `cargo test -p forge-query` passed with 480 tests plus trybuild.
- Reviewed Forge Query Milestone 9 after Phase 1/2 implementation and created `_docs/forge-query/milestone-9-phase-3-implementation-plan.md`. The next honest batch is policy-aware execution seam lowering: current reads, branch reads, runtime-backed historical reads, runtime-backed diffs, live admission, optimizer input, and delivery shape must all consume `NarrowedPolicyQueryArtifact` before truth can be touched. Store-backed execution parity remains Milestone 10 debt; durable saved-query/cursor/reload remains Milestone 11 debt.
- Implemented Forge Store Milestone 12 Phase 2C manifest/authority hardening: added in-memory compatibility manifest publication ledger, recovered manifest index/frontier proof, manifest gap/digest/window drift typed rejections, receipt registry/frontier basis binding, authoritative meaning declarations, partial-truth rejection gate, counters/evidence projection, and compile-fail guards for new proof-bearing publication/recovery/authoritative types. Verified with `cargo fmt -p forge-store`, `cargo test -p forge-store compatibility --lib`, `cargo test -p forge-store milestone_12 --lib`, and `cargo test -p forge-store --test phase_boundaries_compile_fail -- --test-threads=1`.
- Continued Forge Store Milestone 12 with the first derived drift/rebuild planning slice: added derived basis compatibility plans, explicit invalidation reasons, retained-authority and maintenance-admission proof witnesses with sealed constructors, rebuild-debt and maintenance-admission counters, typed stale/basis/admission rejections, evidence projection, and compile-fail guards for the new witnesses. This remains planning/proof vocabulary only: no runtime rebuild execution, scheduler insertion, rolling upgrade, restore behavior, or SQLite schema was added. Verified with `cargo fmt -p forge-store`, `cargo test -p forge-store compatibility --lib`, `cargo test -p forge-store milestone_12 --lib`, and `cargo test -p forge-store --test phase_boundaries_compile_fail -- --test-threads=1`.
- Created `_docs/forge-store/milestone-12-phase-3b-implementation-plan.md` for the next Forge Store M12 batch. The plan finishes Phase 3 by adding first-ship derived/support compatibility lanes, lane-specific basis planning, bulk resume skew rejection, Milestone 11 maintenance-lane admission binding, Milestone 13 tier non-authority preservation, exact counters, unit tests, and trybuild boundaries. It explicitly keeps rebuild execution, maintenance queue insertion, rolling upgrade, restore publication, bulk execution, tier mutation, facade APIs, and SQLite schema out of scope.
- Implemented Forge Store Milestone 12 Phase 3B derived/support lane foundation: added first-ship `DerivedCompatibilityLaneKind` mapping/snapshot/registry, lane-specific derived planning, bulk resume changed-interpretation rejection, tier manifest placement-only non-authority posture, compatibility maintenance-lane admission requirements, lane/reuse/rejection/tier/maintenance counters, Milestone 12 report projection, unit tests, and compile-fail guards proving lane snapshots/admissions/rebuild requirements stay sealed and tier/bulk plans cannot masquerade as authority or execution proofs. Verified with `cargo fmt -p forge-store`, `cargo test -p forge-store compatibility --lib`, `cargo test -p forge-store milestone_12 --lib`, and `cargo test -p forge-store --test phase_boundaries_compile_fail -- --test-threads=1`.
- Implemented Forge Query Milestone 9 Phase 3 policy-aware execution seam lowering: added typed seam identities/counters/errors, policy-aware current/branch/runtime-historical/runtime-diff plan lowering, live admission relevance gates, delivery-shape narrowing, optimizer input derivation, Phase 3 certification rows/rejections, and trybuild guards proving raw canonical/planned/validated artifacts cannot enter policy-aware execution surfaces. Store-backed execution and durable cursor/artifact reload remain explicit deferred debt. Verified with focused policy tests, milestone certification, phase-boundary trybuild, and full `cargo test -p forge-query`.
- Continued Forge Query Milestone 9 closeout hardening by making store/durable handoff honesty executable instead of prose-only: added `PolicyExecutionSeamHandoffReport`, public durable overclaim denial functions for policy cursor resume, policy artifact reload, and delivery metadata reload, exact durable overclaim counters, certification rows for handoff honesty and each durable deferred denial, and facade exports. Verified with `cargo fmt -p forge-query`, focused `policy_execution_seam` and `milestone_nine_certification` tests, and full `cargo test -p forge-query` (491 tests plus trybuild).
- Created `_docs/forge-query/milestone-9-phase-4-implementation-plan.md` for the next Forge Query M9 batch. The plan targets runtime-backed certification closure: concrete EmployeeRecord fixture, exhaustive hidden influence coverage for aggregation/cursor/view membership, placeholder masking denial, live drift/density evidence, delivery-width class honesty, policy scale-slope evidence, direct/scope/template/saved/view-shaped parity, compile-fail boundaries, and support diagnostics. Store-backed execution remains M10 debt and durable saved/cursor/reload remains M11 debt.
- Continued Forge Store Milestone 12 into Phase 4A rolling compatibility: added first-ship two-capability rolling admission over reader/writer capability sets, rolling capability windows, mixed-version store/replica posture, typed multi-writer and skew rejections, rolling counters and Milestone 12 report projection, unit tests, and compile-fail guards preventing external construction of rolling admission plans or extraction of the upgrade witness. This remains admission/planning only: no restore publication, rolling execution, writer facade, replica transfer, or persistence was added. Verified with `cargo fmt -p forge-store`, `cargo test -p forge-store compatibility --lib`, `cargo test -p forge-store milestone_12 --lib`, and `cargo test -p forge-store --test phase_boundaries_compile_fail -- --test-threads=1`.
- Ran the next production-grade `$qa-loop` over Forge Store Milestone 12 Phase 4A rolling admission. Fixed the main semantic leak: rolling windows no longer admit based on numeric semantic-version proximity alone, and `plan_first_ship_rolling_upgrade` now requires a declared compatibility edge from the edge registry. Also blocked single reader/writer capability sets from hiding multiple semantic versions, added hostile tests for missing-edge numeric proximity and single-set multi-version rejection, and added an exact `edge_missing_rejection_count` counter projected into Milestone 12 evidence. Verified again with `cargo fmt -p forge-store`, `cargo test -p forge-store compatibility --lib`, `cargo test -p forge-store milestone_12 --lib`, and `cargo test -p forge-store --test phase_boundaries_compile_fail -- --test-threads=1`.
- Re-ran the Forge Store M12 Phase 4A `$qa-loop` as a double-check. Found and fixed one remaining proof-envelope weakness: rolling admission plans checked declared edges but discarded the selected relation, so admitted posture could not distinguish native from forward/backward compatibility later. `RollingUpgradeAdmissionPlan` now carries a read-only `CompatibilityRelation`, rolling tests assert native vs forward-read relation preservation, and the private-constructor trybuild fixture was refreshed. Verified with `cargo fmt -p forge-store`, `cargo test -p forge-store compatibility --lib`, `cargo test -p forge-store milestone_12 --lib`, and `cargo test -p forge-store --test phase_boundaries_compile_fail -- --test-threads=1`.
- Continued Forge Store Milestone 12 Phase 4 with restore/backup admission planning. Added scoped backup manifests and backup-scope membership, restore target compatibility planning through declared edges, restore publication conflict units/sets, sealed restore publication plans carrying relation plus witness, disaster-recovery window classification for authoritative truth vs derived acceleration, exact restore/disaster counters and M12 report projection, unit tests for scoped restore admission/out-of-scope rejection/publication-conflict rejection/DR classification, and a compile-fail guard preventing external restore plan construction. No restore execution, visibility publication, facade API, SQLite schema, or durable backup persistence was added. Verified with `cargo fmt -p forge-store`, `cargo test -p forge-store compatibility --lib`, `cargo test -p forge-store milestone_12 --lib`, and `cargo test -p forge-store --test phase_boundaries_compile_fail -- --test-threads=1`.
- Ran the cleanup/hardening batch before planning Forge Store M12 Phase 5. Added hostile lanes for rolling adapter-edge rejection without execution, restore missing-edge numeric-proximity rejection, and restore incompatible-edge rejection before publication witness construction; expanded M12 matrix row vocabulary for those restore/rolling lanes. Verified with `cargo fmt -p forge-store`, `cargo test -p forge-store compatibility --lib` (68 tests), `cargo test -p forge-store milestone_12 --lib`, and `cargo test -p forge-store --test phase_boundaries_compile_fail -- --test-threads=1`. Wrote `_docs/forge-store/milestone-12-phase-5a-implementation-plan.md` for the next certification-foundation batch.
- Refined `_docs/forge-store/milestone-12-phase-5a-implementation-plan.md` with an explicit implementation order: certification vocabulary, mandatory lane enumeration, matrix assembly, evidence bundle shell, counter-contract validation, conversion helpers from existing typed outcomes, compile-fail guards, then focused verification gates.
- Implemented Forge Query Milestone 9 Phase 4 closeout batch: added the concrete EmployeeRecord policy certification fixture, policy scale-slope/parity reports, exhaustive hidden influence purposes for aggregation/cursor/view membership, distinct placeholder-masking seam denial, live epoch drift and density evidence reports, Phase 4 certification rows for fixture/schema/delivery/live/scale/parity, and a trybuild guard requiring live drift epoch evidence instead of raw enum shortcuts. Verified with focused policy tests, milestone certification, phase-boundary trybuild, and full `cargo test -p forge-query` (503 tests plus trybuild).
- Continued Forge Query Milestone 9 Phase 4 closeout: added masked-versus-unmasked policy parity and identity-aware inspector parity reports/rows, typed seam denials and exact counters for per-row policy allocation, cross-tenant fanout, saved-query policy bypass, and unsupported policy/workflow composition, row-derived Phase 4 support diagnostics that keep store-backed execution and durable artifacts deferred, and compile-fail guards preventing external fabrication of EmployeeRecord bundles, placeholder masking denials, live drift reports, and scale-slope reports. Verified with `cargo fmt -p forge-query`, focused seam/certification tests, phase-boundary trybuild, and full `cargo test -p forge-query` (506 tests plus trybuild).
- Continued Forge Query Milestone 9 Phase 4 performance hardening: live policy drift/density posture is now encoded in seam counters for policy epoch drift readmission, tenant basis drift readmission, sparse-to-burst readmission, and dense restart debt. Live admission can annotate admitted plan counters, dense restart debt denies with a distinct counter, the Phase 4 certification matrix carries real drift/burst seam counter digests, and full `cargo test -p forge-query` passed (509 tests plus trybuild).
- Closed the remaining Forge Query M9 Phase 4 compile-time guard holes from the plan: hidden aggregation/cursor/view-membership influence entries cannot be fabricated outside the query-owned `PolicyInfluenceSet` path, and `SavedPolicyNarrowingReuseDescriptor::new` is now crate-private so exact saved reuse cannot be minted from matching strings without query-owned narrowing evidence. Added trybuild fixtures and verified with full `cargo test -p forge-query` (509 tests plus trybuild).
- Ran another `$qa-loop` over Forge Query M9 Phase 4. Fixed three production-grade gaps: live drift evidence now must bind its current policy and tenant basis to the admitted live plan; live density evidence must bind its authorized relevance width to the admitted live relevance contract; and policy/tenant/burst readmission counters moved from coarse admission disposition to certified drift/density evidence so policy-only and tenant-only drift are counted exactly. Also hardened Phase 4 support diagnostics so verified surfaces require executable evidence fields and typed rejection bundles, not just row names. Verified with full `cargo test -p forge-query` (513 tests plus trybuild).
- Re-ran the Forge Query M9 Phase 4 `$qa-loop` as a double-check. No new meaningful in-scope findings remained after reviewing live drift/density evidence binding, support diagnostic evidence requirements, compile-fail proof boundaries, and store/durable deferred honesty. Verified with focused `policy_live` and `milestone_nine_certification` tests, `cargo test -p forge-query --test phase_boundaries_compile_fail -- --test-threads=1`, and full `cargo test -p forge-query` (513 tests plus trybuild).
- Implemented Forge Store Milestone 12 Phase 5A certification foundation: added mandatory certification lane vocabulary, stable lane IDs, matrix assembly with duplicate/missing-lane rejection, certification outcome conversion helpers from read/write/derived/rolling/restore/DR plans, counter-contract report validation, and a sealed certification evidence bundle shell for rolling/restore/derived evidence. Added unit tests for lane completeness/determinism, matrix/evidence mismatch, counterless lane rejection, relation preservation, and missing-edge rejection projection, plus trybuild guards proving external code cannot mint certification lane outcomes or evidence bundles or substitute lane outcomes for restore/rolling/retained-authority witnesses. Verified with `cargo fmt -p forge-store`, `cargo test -p forge-store compatibility --lib`, `cargo test -p forge-store milestone_12 --lib`, and `cargo test -p forge-store --test phase_boundaries_compile_fail -- --test-threads=1`.
- Created `_docs/forge-store/milestone-12-phase-5b-implementation-plan.md` for the next Forge Store M12 batch. The plan adds a real Artifact Format Evolution and Rolling Compatibility certification runner that assembles every mandatory Phase 5A lane from actual compatibility operations, emits machine-checkable artifact/failure/diagnostics/counter/matrix evidence, and keeps persistence, facade integration, restore execution, rolling publication, adapter execution, and rebuild execution explicitly out of scope until the runner exposes the true blockers.
- Ran a hardcore QA pass over the Forge Query M9 tests themselves. Hardened the certification bundle to require `result_digest`, replaced Phase 4 label-like plan/seam/delivery evidence with machine digests from actual lowering, forced declared hostile/parity lane semantics, fixed weak tenant and masked/unmasked controls, required Phase 4 verified evidence fields to be SHA-256 digests, and added adversarial tests for delivery-width recomputation, EmployeeRecord query-family distinctness, and policy scale-slope digest drift. Verified with `cargo fmt -p forge-query`, focused M9/policy certification tests, phase-boundary trybuild, and full `cargo test -p forge-query` (518 tests plus trybuild).
- Closed Forge Query Milestone 9 for the admitted runtime-backed scope by adding `_docs/forge-query/milestone-9-closeout.md`, updating `milestone-9.md` to closed status with a shipped closeout link, and marking M9 closed in `forge_query_roadmap.md`. The closeout explicitly preserves store-backed policy execution as M10 debt and durable policy cursors/artifact reload/delivery metadata/tenant-query portability as M11/store-gated debt.
- Implemented Forge Store Milestone 12 Phase 5B certification runner: added `Milestone12CertificationRunner` and result/digest/diagnostics surfaces that assemble every mandatory Phase 5A lane from actual manifest/read/derived/rolling/restore/DR operations, aggregate real counter evidence, emit deterministic artifact/failure/matrix/skew/diagnostics/counter digests, and expose explicit runtime-gap labels for persistence/facade/restore-publication/rolling-writer/adapter/rebuild execution debt. Added compile-fail coverage proving external code cannot fabricate `Milestone12CertificationDigestSet`. Verified with `cargo fmt -p forge-store`, `cargo test -p forge-store compatibility --lib`, `cargo test -p forge-store milestone_12 --lib`, and `cargo test -p forge-store --test phase_boundaries_compile_fail -- --test-threads=1`.
- Continued Forge Store Milestone 12 production closeout with real shipped-path compatibility execution. Added a public rolling commit publication path through `ForgeStore`, kept the production lowering in `backend/engine/compatibility_production/rolling_publication.rs` to stay aligned with the domain laws, fixed facade/runtime integration issues, and verified hostile rolling-path tests. Also removed the stale `rolling_writer_publication_deferred` certification runtime-gap label now that the public path exists and is covered by focused execution tests. Verified with `cargo fmt -p forge-store`, `cargo test -p forge-store compatibility_rolling_execution --lib`, `cargo test -p forge-store compatibility_rebuild_execution --lib`, `cargo test -p forge-store artifact_format_evolution --lib`, and `cargo test -p forge-store --test phase_boundaries_compile_fail -- --test-threads=1`.
- Ran the workspace `qa-loop` skill against the new Forge Store Milestone 12 production-path work. The hostile pass found two real weaknesses: rolling certification evidence was not explicitly tied back to the shipped public path, and the rolling production path duplicated first-ship compatibility edges locally instead of consuming a shared compatibility declaration source. Corrected both by centralizing the first-ship rolling edge registry in `compatibility/rolling.rs`, adding admission-report evidence to the public rebuild/rolling outcomes, and adding a certification-alignment test proving the public rolling path matches the runner lane and justifies dropping the rolling deferred-gap label. Re-verified with `cargo fmt -p forge-store`, `cargo test -p forge-store compatibility_rolling_execution --lib`, `cargo test -p forge-store compatibility_rebuild_execution --lib`, `cargo test -p forge-store artifact_format_evolution --lib`, and `cargo test -p forge-store --test phase_boundaries_compile_fail -- --test-threads=1`.
- Continued Forge Store Milestone 12 closeout by promoting restore execution evidence onto a real public facade path. Added `CompatibilityRestoreExecutionOutcome`, wired `StoreBackend::from_export_bundle_with_compatibility(...)` and `ForgeStore::restore_from_authoritative_export_with_compatibility(...)`, updated the restore execution test to prove the production path instead of a private helper on the success lane, and removed an obsolete restore-admission helper from `compatibility_runtime.rs`. Verified with `cargo fmt -p forge-store`, `cargo test -p forge-store compatibility_restore_execution --lib`, and `cargo test -p forge-store --test phase_boundaries_compile_fail -- --test-threads=1`.
- Implemented the bounded parity-proven adapter path for Forge Store Milestone 12 instead of narrowing scope: added declared first-ship adapter metadata plus parity execution helpers, exposed `CompatibilityAuthoritativeAdapterRequest` / `CompatibilityAuthoritativeAdapterOutcome` through the public `ForgeStore` support facade, added a domain-aligned production module for authoritative adapter execution, tracked adapter parity failures in Milestone 12 evidence, and extended the certification runner with admitted/rejected adapter parity lanes so `adapter_execution_deferred` is gone honestly. Verified with `cargo fmt -p forge-store`, `cargo test -p forge-store compatibility_adapter_execution --lib`, `cargo test -p forge-store artifact_format_evolution --lib`, `cargo test -p forge-store compatibility_rolling_execution --lib`, `cargo test -p forge-store compatibility_rebuild_execution --lib`, `cargo test -p forge-store compatibility_restore_execution --lib`, and `cargo test -p forge-store --test phase_boundaries_compile_fail -- --test-threads=1`.
- Closed the last Milestone 12 runtime-gap claim by removing the stale durable-manifest persistence deferral after verifying persisted compatibility manifest reopen through local-file and SQLite paths. Added a certification-alignment persistence test, wrote `_docs/forge-store/milestone-12-closeout.md`, flipped `_docs/forge-store/milestone-12.md` to closed status, and updated `_docs/forge-store/forge_store_roadmap.md` to mark Milestone 12 closed. Focused verification passed for `cargo test -p forge-store compatibility_persistence --lib`, `cargo test -p forge-store artifact_format_evolution --lib`, and the previously passing production-path/compile-fail gates already named in the Milestone 12 closeout doc.
