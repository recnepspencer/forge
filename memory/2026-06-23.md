## Hadwiger-Nelson W607 weighted-rank cuts

- Retired the static half-turn/orbit completion hypothesis for replayed W607
  weighted local-rank cuts. Diagnostic artifact:
  `crates/hadwiger-research/docs/w607-weighted-rank-pattern-preflight.json`.
- Result: `6` mirror-completion candidates, `0` accepted new cuts, post-16-cut
  LP marginal drop `0.0`. Dense completions duplicate retained supports; the
  only non-duplicate two-hop completions (`twohop80_384`, `twohop80_385`) have
  violation numerator `-7787`.
- Next funded direction: adaptive weighted-rank separation from the post-16-cut
  LP solution, using high remaining fractional mass rather than mirroring
  root-discovered pockets.
- Ran that adaptive separation pass. It reproduced post-16 LP objective
  `641090.9615275878`, generated `92` unique <=120 supports, and found only one
  currently violated support (`twohop100_304`). Because it overlaps retained
  structure heavily (`0.833` Jaccard) and drops the LP by only about `265.57`,
  the adaptive local-pocket heuristic is retired. Next theory should move to
  stronger certificate/proof structure or lifted/disjunctive cuts from the
  retained 16-cut phenomenon.
- Ran a branch-local weighted-rank diagnostic after second-agent critique. Best
  split is vertex `304`: include child `604127.0`, exclude child improves from
  `638603.488` to `632232.400` after child-local cuts. This is real movement
  but misses the `625000` funding bar and the `631000` follow-up bar. Retire
  depth-1 local rank-cut branching; next test should target compact replayable
  LP duals or stronger lifted/disjunctive certificate structure.
- Root LP dual certificate path succeeded as proof plumbing. The post-16 W607
  root dual has only `597` positive rows: `595` triangles and `2` weighted-rank
  rows (`top_weight_120`, `dense80_304`), no singleton repairs. Denominator
  `1024` upward rounding gives an exact certificate
  `656787579/1024 = 641394.1201171875` with min coverage slack `0`.
  Added an in-crate replay checker and example; release replay verifies graph
  binding, triangle validity, deterministic rank supports, exact MWIS alpha
  recomputation, coverage, and objective equality. This is exact authority for
  the root relaxation, not the `512933` W607 target. Next: compact branch-local
  dual certificates or a lifted/disjunctive certificate using the same row
  language.
- Branch-local dual certificate also succeeded for the best child. For exclude
  vertex `304`, deleted-vertex and explicit fixed models agree at
  `632232.3996589432`; denominator `1024` upward rounding gives exact artifact
  `647496725/1024 = 632321.0205078125` with `599` parent-triangle rows, one
  child-rank row (`dense120_303`), no repairs, and min active slack `0`. Added
  in-crate replay checker and example. This proves branch-local triangle/rank
  dual covers are viable proof-plumbing artifacts, but still not target
  authority. Next theory: lift the `304` include/exclude logic into a stronger
  root-level disjunction, or test whether deeper compact branch duals compound.
- The paired include child for vertex `304` now also replays exactly. Fixing
  `x_304 = 1`, deleting the closed neighborhood, and replaying the residual
  cover gives denominator-`1024` certificate
  `618626223/1024 = 604127.1708984375`, with `573` residual clique rows and
  min active slack `0`; the residual model agrees with the explicit fixed LP.
  Combining include and exclude bounds yields a valid objective split cut
  `w*x <= U_exclude - (U_exclude - U_include) x_304`. At the post-16 root LP it
  is violated by `18746.12890625`, lowers the root diagnostic from
  `641090.9615275878` to `632321.0205078127`, and drives `x_304` to `0`. This
  is useful compressed one-node branch-bound evidence, not a new native
  root-cut theory or W607 target authority.
- Depth-2 branch compounding inside the `x_304 = 0` child was retired by a
  strict second-agent raw gate. The best second split among eight fractional
  candidates has raw grandchildren `613194.8024` and `631039.9884`, missing the
  `631000` pre-separation gate by about `39.99`, so no bespoke grandchild
  weighted-rank separation was run. Artifact:
  `crates/hadwiger-research/docs/w607-v304-exclude-depth2-split-diagnostic.json`.
- The single-row non-objective lift of the positive exclude-child rank row
  `dense120_303` was also retired. Recomputing the active `119`-vertex support
  gives `alpha0 = 287232`; the include residual has `beta = 240974`, so the
  lift coefficient is `M = 0` and the row is a parent-valid weighted-rank cut.
  It violates the post-16 root by `2554.4249`, but drops the LP only
  `598.6405`; with the v304 objective split it improves only `38.8043`. Next
  credible lifting theory should aggregate the whole positive child dual, not a
  single rank pocket.
- Aggregate child-dual lifting produced the first real non-objective lifted
  branch-certificate cut. Aggregating the exact v304-exclude certificate gives
  coverage vector `c_v`, objective numerator `647496725`, and min active slack
  `0`. Exact coverage-weight `gamma` on the `x_304=1` residual did not close in
  `180s`, but the MILP dual bound proves `gamma <= 546085806`, giving a valid
  conservative lift coefficient `L = 101410919`, stronger than the residual
  objective-split coefficient `65934182`. This lifted aggregate row drops the
  post-16 root LP by `8858.5619` to `632232.3997` and forces `x_304 = 0`.
  Combined with the v304 objective split it improves only `88.6208`, below the
  `100` beyond-split bar. This is funded as a root-cut mechanism but not yet
  publishable target authority. Next: close `gamma` exactly or test symmetric
  include-aggregate lifting.
- Symmetric include-aggregate lifting was retired by the cheap second-agent
  precheck. The include certificate covers vertex `304` by
  `37063680 = 1024*36195`, covers residual active vertices with min slack `0`,
  and covers no neighbors of `304`. At the post-16 root its aggregate LHS is
  only `570375351`, making the root-violation lift gate
  `M <= -76258321.9`; no nonnegative lift can clear the gate, so no MWIS was
  run. Next credible aggregate-lift hypothesis should optimize a nonnegative
  combination of include and exclude coverage vectors before lifting.
- Projected aggregate mixing found the strongest W607 movement so far, after a
  second-agent correction: test separation on the actual `x_304 = 0` bottleneck
  face first, not a branch-lift grid. The best grid row is pure exclude
  aggregate projection (`lambda = 1`). A `180s` capped coverage-weight MWIS did
  not close, but its dual upper bound `61337239200` certifies a projected RHS;
  the current face solution has LHS `64749672500`, giving certified violation
  `33324.5439`. Adding this face-valid projected row lowers the `x_304 = 0`
  LP from `632232.3997` to `598919.8441`. With the include child at
  `604127.1709`, the diagnostic one-node max becomes `604127.1709`. This is
  not root theorem authority yet: the projected row needs parent lifting or a
  replayed branch proof.
- The projected exclude-face row now has a valid parent lift. Using the same
  exclude aggregate coverage vector `c0`, the projected face upper bound is
  `gamma0 <= 613372392` and the include-branch coverage upper bound is
  `gamma1 <= 546085806`, so the conservative lifted row is
  `c0*x + 67286586*x_304 <= 613372392`. Second-agent critique confirmed upper
  bounds are safe if they are on the correct branch domains. The row dominates
  the older aggregate lift for all `x_304 < 1` and drops the post-16 root LP
  directly from `641090.9615` to `598919.8441`, improving the previous best
  split/aggregate bound by `33312.5556`. This is a real certificate-mechanism
  advance, but still above the W607 target and still needs exact replay of the
  gamma upper-bound artifacts before publication-grade authority.
- Flat gamma hardening was tested and retired after second-agent critique. A
  compatibility probe tried edge/triangle cover LPs for the exact `c0` weights
  on both branch domains. The `x_304=0` domain needs `gamma0 <= 613372392`, but
  the best flat cover is `670405293.0` (`9.30%` high). The `x_304=1` residual
  needs `gamma1 <= 546085806`, but the best flat cover is `581636664.67`
  (`6.51%` high). Both miss the `0.5%` gate, so replaying the parent lift will
  need a richer branch/tree or solver-native certificate language, not compact
  flat triangle covers.
- Post-parent-lift local separation was tested and retired. At the
  `598919.8441` parent-lift root solution, a bounded separator generated `78`
  supports and tested `234` W/c0/hybrid local MWIS rows. Two low-overlap `c0`
  rows were accepted, but the LP moved only `0.7876` to `598919.0564`. The huge
  c0 local violations are redundant with the parent-lift row. Future progress
  should come from another lifted branch/aggregate certificate or branch/tree
  gamma proof, not local post-lift pocket cuts.
- Post-parent-lift second-branch screening was also retired after second-agent
  gating. Under the `16` root rank rows plus the v304 projected parent-lift row,
  the top `8` fractional vertices were screened one at a time. Best split:
  vertex `152`, exclude `598919.4160`, include `598914.2639`, raw max
  `598919.4160`, objective-split movement only `0.4281`. This misses the
  `590000` raw max gate and `5000` split-movement gate, so no second aggregate
  gamma calculation is funded from high-weight single-vertex splits.
- Gamma0 branch-tree preflight produced a strong positive proof-language lead.
  For the projected parent-lift row's harder `x304=0` coverage-weight MWIS
  bound, root edge/triangle/rank LP upper is `654024375.5893` (`6.63%` high),
  but a best-bound branch tree closes `gamma0 <= 613372392` with only `17`
  expanded nodes and `18` closed leaves. Worst closed leaf upper:
  `608954093.6667`. This is not exact authority yet, but it funds denominator-
  rounded leaf dual export/replay for a compact gamma0 branch certificate.
- Gamma0 exact leaf-dual export succeeded. All `18` branch-tree leaves round at
  denominator `1024` with exact integer coverage and objective below
  `gamma0 <= 613372392`; worst exact rounded leaf objective is
  `608954093.8311`, and the artifact has `10135` positive rows total. This is
  now a Rust replay-checker candidate for hardening the projected parent-lift
  gamma0 bound from solver diagnostic into proof artifact.
- Gamma0 Rust replay checker now passes. It reconstructs W607, recomputes `c0`
  from the v304-exclude certificate, verifies semantic partition coverage of
  the `x304=0` branch, regenerates deterministic rank supports, and checks all
  `10135` rows with integer arithmetic. Example output reports `18` leaves,
  worst objective numerator `623568992083` (`608954093.8311`) and min slack
  `0`. This gives replay authority for the projected gamma0 face bound, but
  the full parent lift still needs gamma1 authority.
- Gamma1 and the composed parent lift are now replayed too. Flat
  edge/triangle/rank cover for gamma1 still misses by `6.51%`, but a branch
  tree closes the `x304=1` residual gamma1 target in `15` expanded nodes and
  `16` leaves. Exact leaf-dual export passes at denominator `1024` with
  `8555` rows; Rust replay reports worst objective numerator `559085319025`
  (`545981756.8604`) and min slack `0`. The parent-lift replay composes
  gamma0/gamma1 and verifies `613372392 - 546085806 = 67286586`, proving
  `c0*x + 67286586*x_304 <= 613372392`. This is proof-carrying authority for
  the strongest lifted aggregate mechanism, not yet the W607 target.
- Branch-slack lifted row now replays exactly. Conservative gamma0 leaf-slack
  charging over branch variables `{152,222,225,305,383,384,386,456}` gives
  positive coefficients on `{152,222,225,383,386,456}` and RHS reduction
  `4100471.0879`, moving the `x304=0` face by `4005.49`. Reusing the existing
  gamma1 leaves for `c0+p` succeeds at denominator `1024`, yielding the
  replayed canonical parent row
  `1024*(c0+p)*x + 64809127989*x_304 <= 623894447014`. The Rust checker
  verifies gamma0 slack charging, all `8555` modified gamma1 rows, semantic
  branch partitioning, and parent-row composition. Diagnostic parent LP drops
  from `598919.8441` to `594914.3515`. This is proof-carrying mechanism
  progress, still above the W607 target.
- Residual branch-slack iteration is retired. After second-agent critique,
  tested only a cheap sensitivity probe that freezes replayed `p` and `R1`
  and spends only residual gamma0 leaf slack outside the old eight branch
  variables. Artifact:
  `crates/hadwiger-research/docs/w607-branch-slack-residual-sensitivity.json`.
  Result: base objective with replayed `p` is `594914.3515`, but the residual
  LP finds `q_support_size = 0`, current violation `0`, face drop `~0`, and
  top non-old singleton capacities are already `0`. Do not fund same-substrate
  p/q iteration without a new proof object or branch-leaf substrate.
- Global W-rank separation on the post-branch-slack `1/3` plateau is also
  retired. After second-agent approval for exactly one bounded falsification
  pass, artifact
  `crates/hadwiger-research/docs/w607-branch-slack-global-rank-separation.json`
  tested `127` deduped low-overlap candidates and ran the capped `80` exact
  MWIS calls over support sizes `120..240`. Accepted rows: `0`; total LP drop:
  `0`; heavy `1/3` plateau count stays `15`. The largest raw violated rows
  were redundant: best single-row drop only `147.3249`, second `69.7629`.
  Do not re-fund larger local/global W-rank separation on this same two-parent
  row solution without a genuinely new support-generation principle.
- Plateau branch-tree preflight is retired too. After second-agent approval
  for exactly one bounded tree over the current `1/3` plateau, artifact
  `crates/hadwiger-research/docs/w607-branch-slack-plateau-branch-tree.json`
  branched on Tier A `{305,224,385,303,223,384}` under the current `16` root
  rows plus both replayed parent rows. The full Tier A tree closes in `16`
  leaves, but worst leaf is still `594597.0511`, only `317.3004` below root
  `594914.3515`, far above the `590000` continuation gate and export gates.
  The all-excluded Tier A leaf is the worst. Do not fund plateau branch/leaf
  dual export or Tier B expansion from this evidence.
- Mod-3 triangle CG cuts are retired for the bounded sparse pass. After
  second-agent approval, artifact
  `crates/hadwiger-research/docs/w607-branch-slack-mod3-triangle-cg.json`
  searched rank-1 Chvatal-Gomory cuts formed from strict-tight triangle rows
  with multipliers `1/3` or `2/3`. There are `1261` strict-tight triangles,
  but across `26` local pools of `120` triangles each, every sampled GF(3)
  kernel vector had `sum y == 0 mod 3`, so the rounded RHS was integral and
  no violated cut existed. Generated/tested/accepted cuts all `0`; objective
  stays `594914.3515`; heavy `1/3` plateau count stays `15`. Do not fund
  sparse local mod-3 triangle CG on this solution without a new pool principle.
- Native weighted Lovasz-theta preflight is retired under current tooling.
  Clarabel `0.11.1` cannot solve the `607x607` PSD cone here, attempting a
  `272 GB` allocation in both primal and dual formulations. The fallback
  sparse spectral dual probe is diagnostic only and lands at `614434.7845`,
  above the `590000` hard-kill gate. Artifact:
  `crates/hadwiger-research/docs/w607-weighted-theta-preflight.json`.
- Branch-leaf dual-ray perturbation over the existing replayed gamma0/gamma1
  leaf row dictionaries is retired. The sound common-`d` LP found a
  non-duplicate vector with raw violation `22466674.8668`, but adding the row
  moved the root LP only `19.6918` (`594914.3515` -> `594894.6597`) versus a
  `2000` diagnostic gate. The lift is `0`, so the row is redundant rather
  than a new parent-lift mechanism. Artifact:
  `crates/hadwiger-research/docs/w607-branch-leaf-dual-ray-perturbation.json`.
- Mixed tight-row rank-1 CG is retired after Bohr's second-agent review funded
  one exact small-denominator falsification pass. New script/artifact:
  `crates/hadwiger-research/docs/run_w607_mixed_tight_row_cg.py` and
  `crates/hadwiger-research/docs/w607-mixed-tight-row-cg.json`. Baseline
  reproduced exactly at `594914.351525072`; tight rows were stable under
  `1e-10` (`1262/1262`). Across `34` pools and moduli `{3,5}`, the search
  generated no cut clearing the exact mixed violation gate: sampled kernels
  rejected as `not_mixed=17197` or `rhs_integral=115`. Objective and heavy
  `1/3` plateau stayed unchanged. Do not re-fund local mixed CG without a
  genuinely different row-generation principle.
- All-excluded Tier-A residual-face rank diagnostic is retired. Laplace
  approved one leaf-only test on fixed exclusions `{305,224,385,303,223,384}`.
  New script/artifact:
  `crates/hadwiger-research/docs/run_w607_all_excluded_leaf_rank_diagnostic.py`
  and `crates/hadwiger-research/docs/w607-all-excluded-leaf-rank-diagnostic.json`.
  The fixed-leaf baseline reproduced exactly at `594597.0511219536`. The pass
  generated `64` supports and ran `40` exact MWIS calls, finding one real
  conditional row (`wx_dense220_152`, alpha `362026`) with single/total drop
  `2194.8934`, leaving the leaf at `592402.1577`. Because this misses the
  `3000` funding gate and remains above the `592000` kill line, do not fund
  leaf-rank export/follow-up from this evidence. It is evidence of a real
  conditional pocket, but not a branch-certificate breakthrough.
- Conditional rank-row propagation is retired as bookkeeping closure.
  Heisenberg allowed adding only the regenerated `wx_dense220_152` row globally
  and rerunning root plus the same Tier-A tree. New script/artifact:
  `crates/hadwiger-research/docs/run_w607_conditional_rank_row_propagation.py`
  and `crates/hadwiger-research/docs/w607-conditional-rank-row-propagation.json`.
  Support regeneration passed (`size=220`, alpha `362026`, leaf replay
  `592402.1577`). Root drop was numerical zero, and the Tier-A max improved
  only `237.5947`; the worst leaf shifted to a neighboring high leaf
  `{223 included; 224,303,385 excluded}` at `594359.4564`. This matches the
  kill condition. Do not reopen general rank separation from this row.
- Odd-cycle parity cuts are retired on the current two-parent W607 LP. Faraday
  approved one bounded separator for simple odd cycles of length `5..101`.
  New script/artifact:
  `crates/hadwiger-research/docs/run_w607_odd_cycle_diagnostic.py` and
  `crates/hadwiger-research/docs/w607-odd-cycle-diagnostic.json`. Baseline
  reproduced at `594914.351525072`; there are no high-weight near-`1/2`
  vertices, round 1 generated `0` violated odd-cycle cuts, and objective /
  heavy `1/3` plateau are unchanged. Do not fund odd-cycle rows on this
  substrate without evidence of a new near-half parity structure.
- Two-variable non-objective aggregate lift is retired. Arendt approved one
  bounded diagnostic only if each face gamma maximized the same vector `d`.
  New script/artifact:
  `crates/hadwiger-research/docs/run_w607_two_variable_lift_diagnostic.py`
  and `crates/hadwiger-research/docs/w607-two-variable-lift-diagnostic.json`.
  Tested `7` Tier-A interaction pairs and vectors `{c0,c0+p}` with `56` LP
  face solves. Best row (`c0`, pair `305,224`) has huge raw root violation
  `287016.2194`, but adding it drops the root only `20.9058`
  (`594914.3515 -> 594893.4457`), far below the `1000` gate and below the
  `250` kill threshold. Top rows have cosine about `0.956` with weights,
  consistent with objective/projection redundancy. Do not fund two-variable
  affine lifting on these Tier-A pairs without a new non-objective vector.
- Web template inequalities are retired. Noether allowed only a tiny web-only
  structural falsifier and recommended skipping clique-family rows as too close
  to the CG graveyard. New script/artifact:
  `crates/hadwiger-research/docs/run_w607_web_template_diagnostic.py` and
  `crates/hadwiger-research/docs/w607-web-template-diagnostic.json`. The pass
  used geometric angular order only to propose candidates, then required graph
  edge-containment for web parameters `3..8` and exact alpha validation before
  adding any row. Baseline reproduced at `594914.351525072`; the first
  high-priority pool consumed the `500` candidate cap, found `0` structural
  webs and `0` accepted cuts. Do not fund web/antiweb template rows on this
  substrate without a new support-ordering principle.
- Branch-slack symmetry-image parent rows are retired. Mill approved one cheap
  diagnostic after requiring the artifact to prove that half-turn transport
  fixes branch vertex `304`. New script/artifact:
  `crates/hadwiger-research/docs/run_w607_branch_slack_symmetry_image_diagnostic.py`
  and
  `crates/hadwiger-research/docs/w607-branch-slack-symmetry-image-diagnostic.json`.
  The half-turn preserves all `3390` edges and all `607` weights, with fixed
  vertex set `{304}`. The projected and branch-slack image rows are
  non-duplicate/non-dominating, and the current root solution is not symmetric.
  Even so, projected image drop is numerical zero and branch-slack image drop is
  only `0.454651` (`594914.3515 -> 594913.8969`) despite a denominator-`1024`
  raw violation about `1.34e6`. Do not fund symmetry-image parent rows on this
  substrate without a new asymmetric proof object.
- Tailored `gamma0(c0+p)` branch-tree improvement is retired as a parent-row
  path. Rawls approved one bounded diagnostic with no export unless root
  movement cleared the gate. New script/artifact:
  `crates/hadwiger-research/docs/run_w607_tailored_gamma0_branch_tree.py` and
  `crates/hadwiger-research/docs/w607-tailored-gamma0-branch-tree.json`.
  Reoptimizing the old gamma0 leaves for `d=c0+p` found only `168` numerator
  units of spare gain, but a fresh best-bound tree closed in `23` expanded
  nodes and `24` leaves with diagnostic `gamma0_d = 609058840.6162`, improving
  the conservative charged bound by `213080.2959` objective units. The composed
  hypothetical parent row still drops the current root only `208.0114`
  (`594914.3515 -> 594706.3402`), below the `250` cheap root gate. Do not
  export tailored gamma0 leaves from this evidence; stronger gamma0 alone is
  geometrically redundant at the current root.
- Tailored gamma0 slack-face charging is also retired. Wegener approved one
  face-only funding diagnostic and warned not to treat it as parent-valid
  without gamma1. New script/artifact:
  `crates/hadwiger-research/docs/run_w607_tailored_gamma0_slack_face_diagnostic.py`
  and
  `crates/hadwiger-research/docs/w607-tailored-gamma0-slack-face-diagnostic.json`.
  Charging nonnegative `q` over the fresh tree's branch variables
  `{456,383,222,152,386,225,305,384,223}` gives `q_support_size=0`,
  `rhs_reduction=0`, and current face violation `0`; the only face drop is the
  already-retired `208.0114` from stronger gamma0. Do not fund gamma1 for
  `d+q`.
- Plateau affine-disjunction aggregation is retired. Ampere approved one cheap
  diagnostic using a conservative nonnegative upper envelope over the Tier-A
  plateau branch leaves. New script/artifact:
  `crates/hadwiger-research/docs/run_w607_plateau_affine_disjunction.py` and
  `crates/hadwiger-research/docs/w607-plateau-affine-disjunction.json`.
  The fitted row has coefficients around `2084..2086` on Tier-A
  `{305,224,385,303,223,384}` and raw violation `2640.4843`, but LP drop is
  only `139.1847`, weaker than the scalar max-leaf cap drop `317.3004` and
  below the `250` kill gate. Do not fund affine plateau leaf replay.
- Plateau signed-CGLP disjunctive hull is retired. Euler approved one bounded
  diagnostic as disjunctive-hull evidence only, not native current-LP authority.
  New script/artifact:
  `crates/hadwiger-research/docs/run_w607_plateau_signed_cglp.py` and
  `crates/hadwiger-research/docs/w607-plateau-signed-cglp.json`. For each
  signed coefficient vector the script recomputed all leaf suprema
  `sup_L(w*x+a_T*x_T)` under the same row system. Best result is
  boundary-driven in the `10000` box with all six Tier-A coefficients
  essentially `+10000`; raw violation remains `2640.4843`, but root drop is
  only `0.2457`, far below the `250` kill gate and weaker than scalar max-leaf.
  Do not reopen Tier-A disjunctive aggregation without a larger or different
  branch family.
- Multileaf conditional-rank bundle found a real but not-yet-fundable pattern.
  Banach approved one bounded branch-leaf proof-object precheck over the top
  six Tier-A leaves. New script/artifact:
  `crates/hadwiger-research/docs/run_w607_multileaf_conditional_rank_bundle.py`
  and `crates/hadwiger-research/docs/w607-multileaf-conditional-rank-bundle.json`.
  All six leaves get meaningful drops (`1161..2195`), and five neighboring
  leaves show a repeated conditional pattern: a `wx_dense220_*` row with alpha
  about `358075` plus a `wx_dense180_*` row with alpha `258701`; the
  all-excluded leaf keeps the old `wx_dense220_152` alpha `362026` row. Tested
  max improves from `594597.0511` to `593196.5447` (`1400.5064`), above the
  `592000` funding gate and below the `3000` max-movement gate. This is a
  strong structural signal, not replay/export authority.
- Multileaf residual-rank iteration is negative but partial. Zeno approved one
  bounded residual pass after the first leaf-local rank family. New
  script/artifact:
  `crates/hadwiger-research/docs/run_w607_multileaf_residual_rank_bundle.py`
  and `crates/hadwiger-research/docs/w607-multileaf-residual-rank-bundle.json`.
  The original `20`-call plan and then a `10`-call rescue timed out, so the
  script now checkpoints after each leaf and uses only `3` MWIS calls per leaf
  with a nominal `5s` cap. Five of six leaves completed; all five accepted `0`
  residual rows and had `0` additional meaningful drops. Additional tested-max
  movement is only `45.9824` (`593196.5447 -> 593150.5623`). Sixth leaf did not
  finish before the outer timeout. Do not fund residual iteration without a
  better MWIS backend or sharper prefilter.
- Full-tree template-driven rank-family diagnostic is retired. Volta approved
  one full-tree schema check of the repeated `dense220`/`dense180` first-family
  pattern, provided it stayed deterministic and used at most `24` new MWIS
  calls. New script/artifact:
  `crates/hadwiger-research/docs/run_w607_full_tree_rank_family.py` and
  `crates/hadwiger-research/docs/w607-full-tree-rank-family.json`. The run
  covered all `16` Tier-A leaves, spent `20/24` new MWIS calls, accepted rows
  in `8` leaves, and kept the average template count at exactly `2.0` per
  leaf. But the full-tree max only moved from `594597.0511` to `593196.8907`
  (`1400.1605`), missing the `592000` max gate and `3000` movement gate. The
  compact family is real but stalls at a new `~593196` ceiling, so do not fund
  first-family replay/export without a new template that changes that ceiling.
- Conditional-rank disjunctive lifting is retired by dominance. Meitner
  approved one cheap falsifier for Tier-A literal lifting of the accepted
  first-family rows, but the implementation found a stronger precheck: each
  accepted `alpha_w` is an ordinary support MWIS value, making the accepted row
  globally valid before any branch charges. New script/artifact:
  `crates/hadwiger-research/docs/run_w607_conditional_rank_global_dominance.py`
  and `crates/hadwiger-research/docs/w607-conditional-rank-global-dominance.json`.
  Adding all `8` accepted rows globally to the current root system moves only
  numerical noise (`594914.351525072 -> 594914.351525073`); best single-row drop
  is `0.0`, and the best row is under-saturated at root by `35617`. Since any
  nonnegative branch-literal lift would be weaker than these global rows, do
  not fund Tier-A literal lifting of the first-family rank rows. Future lifting
  needs genuinely conditional bounds, not support MWIS rows that are merely
  branch-violated.
- Conditional-alpha lifting is also retired. Gibbs approved one bounded test
  of the remaining loophole after global dominance: compute face-specific
  support MWIS values for the accepted first-family supports and lift only if a
  source face has `beta_source < alpha_w`. New script/artifact:
  `crates/hadwiger-research/docs/run_w607_conditional_alpha_lift.py` and
  `crates/hadwiger-research/docs/w607-conditional-alpha-lift.json`. The first
  attempt timed out before artifact creation; it was stopped by exact PID and
  the script was tightened with a source-beta precheck. Final run used the `8`
  accepted supports, charged only the six Tier-A branch variables, and found
  `0` source-face beta reductions, hence `0` admissible lifted rows and root
  objective unchanged at `594914.351525072`. The first-family supports do not
  hide a lower branch-specific independence number; their effect is contextual
  LP violation only.
- Pair-seeded second-family rank rows are retired under the strict shape gate.
  Erdos approved one bounded falsifier only if pair-seeded supports were
  genuinely two-center and not decorative single-center dense rows. New
  script/artifact:
  `crates/hadwiger-research/docs/run_w607_pair_seeded_second_family.py` and
  `crates/hadwiger-research/docs/w607-pair-seeded-second-family.json`. The
  initial run timed out before artifact creation; a checkpointed rerun showed
  no MWIS calls because the pre-MWIS pair-shape filter rejected every generated
  pair on the first three completed worst leaves (`1`, `6`, `12`): each had
  `50` `single_like` and `10` `unbalanced` rejections, `0` candidates. This
  retires strict pair-seeded dense supports under the current backend; relaxing
  the shape gate would just reopen the already-retired single-center residual
  separator.
- Post-first-family common-`d` CGLP is retired. Turing approved a tiny
  common-vector falsifier using enriched first-family leaf row systems, with no
  Tier-A literal coefficients and all `16` leaf suprema recomputed before root
  LP measurement. New script/artifact:
  `crates/hadwiger-research/docs/run_w607_post_family_common_d_cglp.py` and
  `crates/hadwiger-research/docs/w607-post-family-common-d-cglp.json`. The
  cutting-plane learned only the uniform Tier-A average (`d_v = 1/6` on the six
  Tier-A variables), all-16 `B = 1/3`, root LHS `1/3`, raw violation
  `-1.7e-16`, and root drop numerical zero. The scalar post-family max-leaf cap
  still drops `1717.46`, but the enriched leaf dictionaries do not expose a new
  non-objective common-`d` direction.
- Post-first-family micro-branch substrate is funded for follow-up. Bacon
  approved one LP-only branch-substrate falsifier over the top six enriched
  post-first-family leaves. New script/artifact:
  `crates/hadwiger-research/docs/run_w607_post_family_micro_branch.py` and
  `crates/hadwiger-research/docs/w607-post-family-micro-branch.json`. With no
  new MWIS/supports, each leaf used its accepted first-family row and branched
  on the top six remaining heavy fractional variables. The same compact pool
  appears in every leaf: `{152,222,225,383,386,456}`. Depth-3 trees solve in
  `22` nodes per leaf without hitting node caps, and the top-six max falls
  `593196.8907 -> 586224.2383`, movement `6972.6524`; every leaf moves by
  about `6973`. This clears the continuation gate and is the first strong
  post-branch-slack evidence of a compact new branch substrate. It is not
  parent authority yet; next work should test export/lift feasibility over all
  relevant Tier-A leaves.
- Full-16 fixed-pool micro-branch stress test is not export-ready, but it
  sharply localizes the obstruction. Lorentz approved one all-leaf stress test
  using fixed pool `{152,222,225,383,386,456}`, depth `3`, node cap `24`, no
  new rows/supports/MWIS. New script/artifact:
  `crates/hadwiger-research/docs/run_w607_full16_micro_branch_stress.py` and
  `crates/hadwiger-research/docs/w607-full16-micro-branch-stress.json`. All
  `16` leaves ran with no cap hits and total `352` nodes. The full max moves
  `593196.8907 -> 589302.6440` (`3894.2466`), missing the `586500` export gate.
  The new worst leaf is leaf `0` (all-excluded Tier-A), which moves only
  `592402.1577 -> 589302.6440`; top-six leaves stay near `586224`, and many
  non-top-six leaves move much more. Next lead: inspect or repair leaf `0`
  under/near the same compact residual pool.
- Second-opinion critique of H6: worth exactly one bounded all-16 diagnostic
  because it tests whether the compact residual pool is a Tier-A semantic
  substrate rather than a top-six artifact. Scope all 16 leaves, but require
  first-family-enriched baselines, fixed ordered pool `{152,222,225,383,386,456}`,
  depth-3 best-bound trees, no new rows/MWIS/support selection, per-leaf branch
  traces, and aggregate full max. Stricter gates: no cap hits, total expanded
  nodes should match the prior closed shape unless explicitly explained, every
  previously high leaf must move materially, no outside-top-six leaf may become
  the new bottleneck above `586500`, and repeated branch pattern should be
  measured structurally, not asserted from the candidate pool alone.
- Leaf-0 depth-4 same-pool repair is retired. Halley approved one narrow
  leaf-0-only falsification pass over the same fixed pool
  `{152,222,225,383,386,456}`, with exact depth-3 reproduction required before
  comparing depth `4` and no new rows/supports/MWIS/variables. New
  script/artifact:
  `crates/hadwiger-research/docs/run_w607_leaf0_depth4_micro_branch.py` and
  `crates/hadwiger-research/docs/w607-leaf0-depth4-micro-branch.json`.
  Result: depth `3` reproduced `592402.1577 -> 589302.6440`, but depth `4`
  only reached `588378.8643`; extra movement `923.7797` is below the `1000`
  continuation gate, and leaf0 remains above both the `588000` kill max and
  the `586500` export gate. Do not keep funding deeper same-pool trees as the
  leaf0 repair; next leaf0 work needs a changed compact pool or conditional row
  language.
- Leaf-0 augmented-pool-304 repair is retired. Harvey approved one narrow
  comparison after the depth-4 bottleneck showed vertex `304` (one-based;
  internal solver index `303`) as the heaviest outside-pool fractional
  variable. New script/artifact:
  `crates/hadwiger-research/docs/run_w607_leaf0_augmented_pool304.py` and
  `crates/hadwiger-research/docs/w607-leaf0-augmented-pool304.json`. The run
  compared old pool `{152,222,225,383,386,456}` against augmented pool
  `{152,222,225,304,383,386,456}` on leaf `0`, depths `3` and `4`, node cap
  `80`, no new rows/supports/MWIS. Reproduction gates passed, but augmented and
  old trees are identical at both depths: depth `3` `589302.6440`, depth `4`
  `588378.8643`, extra movement `0.0`, and `304` is never selected in the
  augmented traces. Treat `304` as a terminal-specific decoy.
- Leaf-0 residual-pair closure is a strong positive lead. Schrodinger approved
  one bounded diagnostic over the original pool `{152,222,225,383,386,456}`:
  replay leaf0 depth `4`, then split residual pair `{222,456}` only at
  above-gate terminals where both remain unresolved and fractional. New
  script/artifact:
  `crates/hadwiger-research/docs/run_w607_leaf0_residual_pair_closure.py` and
  `crates/hadwiger-research/docs/w607-leaf0-residual-pair-closure.json`.
  Reproduction passed (`589302.6440` depth `3`, `588378.8643` depth `4`).
  Exactly one residual node triggered, assignment `{152=0,225=0,383=0,386=0}`,
  with `222` and `456` both fractional at `1/3`. Four child solves close it
  with max `581481.0000`; final closed leaf0 max `581481.0000`, no cap/timeout,
  no closed terminal above `586500`, status `strong_residual_pair_closure`.
  Fund a full-16 shape check next: fixed-pool depth `3` everywhere plus
  exceptional leaf0 depth `4` residual-pair closure, then inspect full max and
  exportability.
- Full-16 mixed residual closure funds export/lift design, with strict
  authority boundaries. Huygens approved one export-shape artifact and warned
  against authority contamination. New script/artifact:
  `crates/hadwiger-research/docs/run_w607_full16_mixed_residual_closure.py` and
  `crates/hadwiger-research/docs/w607-full16-mixed-residual-closure.json`. It
  combines full-16 fixed-pool depth `3` stress with the leaf0 depth `4`
  residual-pair closure, verifies source reproduction gates, records digests,
  and labels fixed-pool trees plus residual closure as diagnostic branch
  authority only. Result: old full-16 max `589302.6440`; mixed full-16 max
  `586224.2383`, argmax leaf `1`, margin `275.7617` below the `586500` gate,
  total nodes plus extra solves `356`, no cap/timeout, no new
  rows/supports/MWIS/variables. Status `fund_export_lift_design`; next work is
  export/lift design, not proof-claiming.
- Fresh mixed-branch replay independently validates the export-shaped
  diagnostic. Avicenna approved the replay as a stale-artifact contamination
  guard. New script/artifact:
  `crates/hadwiger-research/docs/run_w607_fresh_mixed_branch_replay.py` and
  `crates/hadwiger-research/docs/w607-fresh-mixed-branch-replay.json`. The
  final strict run used `--no-checkpoint`, loaded no prior stress/mixed/leaf0
  closure artifacts and no own replay checkpoint, reconstructed all `16`
  first-family-enriched leaf row systems from `w607-full-tree-rank-family.json`,
  reran depth `3` fixed-pool enumeration over `{152,222,225,383,386,456}` for
  every leaf, and applied the exceptional leaf0 depth `4` residual-pair closure
  `{222,456}` only on leaf `0`. Result: final mixed max `586224.2383`, argmax
  leaf `1`, margin `275.7617` below `586500`, no cap/timeout, no new
  rows/supports/MWIS/branch variables, no failure reasons. Still diagnostic
  branch authority only, but now replayed from source rather than assembled
  from stale artifacts.
- Pool-affine terminal-mask lift is retired as scalar-cap equivalent. Hume
  approved one bounded bridge test with conservative terminal masks over
  `{152,222,225,383,386,456}` (`fixed_0 -> 0`, `fixed_1 -> 1`,
  `free/unresolved -> 1`) and nonnegative coefficients. New script/artifact:
  `crates/hadwiger-research/docs/run_w607_pool_affine_lift_probe.py` and
  `crates/hadwiger-research/docs/w607-pool-affine-lift-probe.json`. The LP
  learned all-zero coefficients and `B = 586224.2383`; the induced root row is
  exactly the scalar max-bound cap, with post-row objective `586224.2383`, drop
  `8690.1133`, and `drop_minus_scalar_drop = 0.0`. Failure reasons:
  `zero_coefficients`, `raw_violation_below_gate`,
  `scalar_cap_equivalent_or_weaker`. Conservative six-pool affine masks do not
  export the branch certificate; any real lift must exploit richer terminal or
  dual structure.
- Terminal-dual provenance is inconclusive but rules out simple fixed-literal
  domination. Kant approved a bounded probe over all terminals within `1000`
  of the mixed max plus leaf0 residual children, warning that HiGHS duals are
  nonunique. New script/artifact:
  `crates/hadwiger-research/docs/run_w607_terminal_dual_provenance.py` and
  `crates/hadwiger-research/docs/w607-terminal-dual-provenance.json`. It
  re-solved `10` selected terminal LPs and reproduced objectives within
  `5.9e-10`. Aggregate fixed-bound/literal dual mass ratio is `0.0`, so the
  high-terminal certificate is not powered by fixed-bound marginals. Positive
  mass is structural: edges `837329.82`, triangles `3512047.22`, parent lifts
  `1830935.63`, first-family leaf rows `309516.43`. But common structural row
  overlap across selected terminals is empty, largely because top-six leaves
  use leaf-local first-family rows and different edge/triangle rows. Conclusion
  `dual_degeneracy_inconclusive`; next export direction should align/replay the
  leaf-local first-family family or build explicit disjunctive/literal lifting.
- First-family dense180 alignment is a positive family-export lead. Peirce
  approved a bounded falsifier to test whether the active
  `dense180_top_wx_center_2` rows are a real reusable family or merely matching
  labels. New script/artifact:
  `crates/hadwiger-research/docs/run_w607_first_family_alignment_probe.py` and
  `crates/hadwiger-research/docs/w607-first-family-alignment-probe.json`.
  Result: six active top-leaf rows (`alpha_w = 258701`, size `180`, centers
  `{152,152,225,383,383,456}` on leaves `{1,2,4,6,9,12}`) have common core
  size `109`, union size `217`, pairwise Jaccard `0.7734..0.8090`, average
  `0.7912`, and identical invariant signature across all six: weight sum
  `853635`, pool incidence `{152,222,225,383,386,456}`, empty Tier-A incidence,
  internal edges `785`, internal triangles `370`. Center-shell signatures vary
  slightly, but invariant group contains all six; dual contribution from the
  first-family row is stable around `32084`. Status `family_export_funded`.
- Dense180 common-core packaging retires the core row but funds a variant
  disjunction route. Dewey approved one bounded diagnostic with at most `8`
  MWIS solves. New script/artifact:
  `crates/hadwiger-research/docs/run_w607_dense180_core_packaging_probe.py` and
  `crates/hadwiger-research/docs/w607-dense180-core-packaging-probe.json`.
  Result: 109-vertex common core alpha `210431`, root lhs only `137372`, root
  drop numerical zero; 217-vertex union alpha `308031`, root lhs `268792`, root
  drop numerical zero. Core/union rows are root-inert. But all six 71-vertex
  variants have identical alpha `115942` (`variant_alpha_spread = 0`), with
  structured variant Jaccard `0.5106..0.5778`. Status
  `fund_variant_disjunction`; next export hypothesis should target explicit
  dense180 variant-disjunction/literal lifting.
- Dense180 variant literal preflight funds a singleton-Tier-A disjunction
  route. Singer approved a bounded preflight and warned that six active leaves
  are not automatically root-valid. New script/artifact:
  `crates/hadwiger-research/docs/run_w607_dense180_variant_literal_preflight.py`
  and
  `crates/hadwiger-research/docs/w607-dense180-variant-literal-preflight.json`.
  Result: active leaves `{1,9,2,12,6,4}` cover singleton included Tier-A
  vertices `{223,224,303,305,384,385}` one-to-one; there are no singleton
  Tier-A leaves outside the active set. All six variants have size `71`, alpha
  `115942`, invariant signature count `1`; all non-active leaves are below
  `586500`, worst non-active leaf `0` at `581481.0000`. Status
  `fund_singleton_literal_disjunction`; next target is an actual literal lift
  over singleton Tier-A faces plus existing mixed coverage for remaining
  faces.
- All-16 Tier-A affine literal aggregation is retired as scalar-equivalent.
  Aquinas approved one bounded diagnostic requiring semantic all-16 masks,
  independently sourced mixed leaf bounds, scalar comparison, and singleton
  control. New script/artifact:
  `crates/hadwiger-research/docs/run_w607_tier_a_mixed_affine_lift.py` and
  `crates/hadwiger-research/docs/w607-tier-a-mixed-affine-lift.json`. The fit
  learns nonzero nonuniform coefficients on Tier-A vertices
  `{224,303,305,385}`, but the induced root LP objective is exactly the scalar
  mixed cap within numerical noise: `586224.2383`, drop `8690.1133`, with
  `drop_minus_scalar_drop = 6.98e-10`. Tight leaves are singleton-only
  `{1,2,4,9,12}`; leaf `6` is near-tight but not active. Failure reasons:
  `scalar_cap_equivalent_or_weaker`, `singleton_only_active_control`. This
  confirms that naive all-16 nonnegative Tier-A affine lifting does not export
  the branch certificate; the remaining route needs a stronger proof object
  than a single scalar-dominated literal row.
- Selected-terminal exact-dual export preflight is retired in its row-only
  form. Popper approved a tightly scoped proof-plumbing test over the six
  near-max normal terminals plus leaf0's exceptional residual-pair shape. New
  script/artifact:
  `crates/hadwiger-research/docs/run_w607_selected_terminal_dual_export_preflight.py`
  and
  `crates/hadwiger-research/docs/w607-selected-terminal-dual-export-preflight.json`.
  The script reconstructs terminal row systems from graph/root/parent/
  first-family sources and selected assignments, then tries exact upward
  rounding over structural rows only. It reproduces all floating terminal
  objectives and fixed-branch bound duals are zero, but every required terminal
  fails export: nonfixed variable-bound dual mass is nonzero (`~100k..350k`
  over `8..16` bound rows per terminal). At coarse denominators the huge
  parent-row scaling creates enormous objective overhead; at high denominator
  structural row-only coverage goes negative. Status
  `retire_selected_terminal_export_preflight`. Next plausible bounded test is
  an explicit-bound-row formulation, if second-agent critique accepts
  singleton upper/lower bound rows as legitimate proof rows.
- Explicit-bound selected-terminal export is partially positive but still
  retired under the canonical denominator gate. Euclid approved one bounded
  preflight allowing explicit `0 <= x <= 1` and branch-fixing rows, with
  lower-bound rows reported but not upward-rounded in the canonical export.
  New script/artifact:
  `crates/hadwiger-research/docs/run_w607_explicit_bound_terminal_dual_export_preflight.py`
  and
  `crates/hadwiger-research/docs/w607-explicit-bound-terminal-dual-export-preflight.json`.
  After correcting the formulation so branch fixed assignments and global
  bounds are represented as explicit rows with no hidden solver bounds, all
  four leaf0 residual children export cleanly (`3` at denominator `1024`, one
  at `4096`; `592..597` positive rows, exact min slack `0`). The six near-max
  depth-3 terminals still fail only the objective gate: exact coverage is
  positive, hidden solver bound marginals are zero, and row counts are
  `596..602`, but rounding the tiny parent-lift multiplier
  (`~4.89e-7` on RHS `623894447014`) causes about `29.5k` overhead even at
  denominator `16777216`, far above the `275.76` margin to `586500`. Status
  `retire_explicit_bound_terminal_export_preflight`. Next bounded question:
  whether large-denominator/bigint terminal export is acceptable proof
  plumbing, or whether the parent row needs rational reconstruction/rescaling.
- Big-denominator selected-terminal export is also partially positive and
  sharply localizes the remaining compactness obstruction. Maxwell approved
  one bounded probe with denominator ladder `{2^24,2^28,2^32,2^36}` and bit
  caps. New script/artifact:
  `crates/hadwiger-research/docs/run_w607_bigdenom_terminal_export_probe.py`
  and `crates/hadwiger-research/docs/w607-bigdenom-terminal-export-probe.json`.
  Result: `8/10` required selected terminals export. All four leaf0 residual
  children still pass; four of the six near-max depth-3 terminals pass at
  `2^32` with objectives around `586329.878..586340.623`, exact positive
  slack, and `596..602` positive rows. Leaves `12` and `9` also clear
  objective/coverage at `2^32`, but fail the policy bit cap:
  `max_multiplier_numerator_bits = 49` versus gate `48`; at `2^36` they have
  tiny objective overhead (`~7`) but fail the same bit cap more strongly.
  Status `retire_bigdenom_terminal_export_probe` under Maxwell's strict gates.
  Interpretation: selected terminal export has no apparent row-semantics
  obstruction; the remaining issue is certificate compactness/presentation,
  likely requiring parent-row multiplier rational reconstruction or an
  explicitly relaxed bit cap after review.
- Per-row rational selected-terminal export fixes the near-max parent-rounding
  obstruction but exposes a tiny coverage-repair need. McClintock approved an
  exact replay filter where HiGHS duals are only candidate multipliers and
  authority comes from exact Fraction coverage/objective replay. New
  script/artifact:
  `crates/hadwiger-research/docs/run_w607_rational_terminal_export_probe.py`
  and `crates/hadwiger-research/docs/w607-rational-terminal-export-probe.json`.
  Result: all six near-max depth-3 terminals export with exact rational
  objective essentially equal to the floating objective (`~1e-5` or less
  overhead), positive exact slack, `596..602` rows, and compact parent
  multipliers (parent overhead `~1e-5` or less). Two leaf0 residual children
  also export at cap `1e6`. The two remaining leaf0 children (`child_0` and
  `child_3`) fail only because `Fraction(float).limit_denominator(...)` can
  approximate some positive multipliers from below, causing tiny exact
  coverage deficits despite objective being on target. Status
  `retire_rational_terminal_export_probe` under exact replay. Next plausible
  bounded test: rational support-preserving coverage repair/min-cost upward
  adjustment over the positive dual support.
- Uniform upward-rational selected-terminal export is retired as too blunt.
  Linnaeus approved one support-preserving repair preflight with uniform
  one-sided rationalization. New script/artifact:
  `crates/hadwiger-research/docs/run_w607_upward_rational_terminal_export_probe.py`
  and
  `crates/hadwiger-research/docs/w607-upward-rational-terminal-export-probe.json`.
  Result: the uniform upward grid policy repairs the leaf0 residual children
  but fails the six near-max depth-3 terminals under the tight repair-cost
  gate; rounding every positive multiplier upward adds too much objective.
  Status `retire_upward_rational_terminal_export_probe`. Next plausible test
  is targeted exact deficit repair starting from H21 candidates: adjust only
  rows needed to cover vertices with negative exact slack.
- Targeted rational deficit repair is retired as too denominator-expensive.
  Kepler approved one support-preserving exact feasibility repair with
  deterministic greedy increments over existing positive support only. New
  script/artifact:
  `crates/hadwiger-research/docs/run_w607_targeted_rational_terminal_repair.py`
  and
  `crates/hadwiger-research/docs/w607-targeted-rational-terminal-repair.json`.
  Result: the literal exact-increment repair explodes denominators/numerator
  bits and trips compactness gates (`denominator_gate_failed`,
  `numerator_bits_gate_failed`, often `too_many_repaired_rows`), while not
  improving the selected-terminal picture. Status
  `retire_targeted_rational_terminal_repair`. The evidence now favors a
  mechanism-class hybrid presentation: per-row rational certificates for the
  parent-lift-heavy near-max terminals, and compact upward integer certificates
  for the leaf0 residual children.
- Hybrid selected-terminal certificate policy succeeds as proof-plumbing
  preflight. Leibniz approved a mechanism-class policy rather than one
  universal multiplier style. New script/artifact:
  `crates/hadwiger-research/docs/run_w607_hybrid_terminal_export_preflight.py`
  and
  `crates/hadwiger-research/docs/w607-hybrid-terminal-export-preflight.json`.
  The predeclared class policy is:
  `top_six_depth3_parent_lift -> per_row_rational_reconstruction` and
  `leaf0_residual_child -> common_denominator_upward_integer`. The artifact
  regenerates row systems from source, uses explicit bound/fix rows, and
  verifies exact coverage/objective for all selected terminals. Result:
  `10/10` required selected terminals pass, total positive rows `5971`, worst
  selected objective `586224.2382592709`, no failure reasons, status
  `fund_full_mixed_tree_terminal_export_design`. Authority remains selected
  terminal preflight only, not root theorem or full mixed-tree export
  authority. This is the strongest validation milestone so far: the high
  branch-bound terminals are exact-certificate friendly under a non-ad-hoc
  mechanism-class policy.
- Full mixed-tree terminal export preflight succeeds. Tesla approved H25 with
  strict manifest, source-binding, exact-replay, row-budget, and authority
  gates. New script/artifact:
  `crates/hadwiger-research/docs/run_w607_full_terminal_export_preflight.py`
  and
  `crates/hadwiger-research/docs/w607-full-terminal-export-preflight.json`.
  The first run exposed only a helper-routing bug: all `135` manifest terminals
  were accidentally classified as audit-only by the selected-terminal H24
  helper. After patching H25 to apply the predeclared full-tree mechanism class
  directly, the strict run passed: manifest count `135/135`, no duplicates, one
  triggered leaf0 depth-4 terminal replaced by four residual children, `135/135`
  terminal certificates exported, total positive rows `80143` under the
  `100000` budget, worst exact terminal objective `586224.2382592709`, and no
  failure reasons. Class summaries: four leaf0 residual children pass by
  common-denominator upward integer; eleven ordinary leaf0 closed terminals
  pass by common-denominator upward integer; six high parent-lift depth-3
  terminals pass by per-row rational reconstruction; and `114` ordinary
  non-leaf0 depth-3 terminals pass by compact common-denominator integer.
  Status `fund_full_mixed_tree_terminal_replay_checker`. Authority is still
  full-terminal export preflight only: it validates the terminal certificates
  and export design, not yet a root theorem or Rust semantic partition checker.
- Full-terminal Rust replay checker now succeeds as H26 proof-plumbing
  authority. Franklin approved the hypothesis, with the warning that a checker
  over the H25 summary JSON alone would be too weak because H25 did not carry
  row-level payloads. H26 therefore first upgraded
  `crates/hadwiger-research/docs/run_w607_full_terminal_export_preflight.py`
  so `w607-full-terminal-export-preflight.json` includes every positive proof
  row with exact integer coefficients and exact `{num, den}` multipliers. New
  Rust files/example:
  `crates/hadwiger-research/src/frontier_seeds/g27_w_circles_full_terminal_export_replay.rs`,
  `crates/hadwiger-research/src/frontier_seeds/g27_w_circles_full_terminal_export_support.rs`,
  and
  `crates/hadwiger-research/examples/replay_g27_w_circles_full_terminal_export.rs`.
  The example replays the `39 MB` row-carrying artifact with exact rational
  arithmetic over all `135` terminals and `80143` positive rows, enforcing
  source digests, manifest counts/classes, duplicate-key absence, terminal
  strategies, row counts, exact nonnegative coverage slack, terminal objective
  gates, aggregate row budget, and argmax terminal. Command:
  `cargo run -p hadwiger-research --example replay_g27_w_circles_full_terminal_export`.
  Output summary:
  `terminals 135 rows 80143 worst_objective_floor 586224 min_slack_floor 0 status ReplayedFullTerminalExportPreflight`.
  Verification also passed:
  `cargo check -p hadwiger-research --example replay_g27_w_circles_full_terminal_export`,
  `cargo fmt -p hadwiger-research --check`, Python compile for the exporter,
  and touched code files remain below `400` lines. Authority remains
  terminal-replay checker authority for the H25 mixed diagnostic branch
  partition, not a root theorem; next hypothesis should address semantic
  partition/root composition or identify the remaining authority gap precisely.
- Semantic branch partition composition now succeeds as H27 preflight
  authority. Bernoulli approved the hypothesis as the real next authority gap:
  H26 proved each listed terminal certificate, but not that the listed
  terminals exactly partition the declared branch search. New Rust
  checker/example:
  `crates/hadwiger-research/src/frontier_seeds/g27_w_circles_semantic_partition_replay.rs`
  and
  `crates/hadwiger-research/examples/replay_g27_w_circles_semantic_partition.rs`.
  The checker raw-digest-binds the fresh mixed replay and row-carrying H26
  terminal artifact, then calls the H26 exact terminal replay. It verifies the
  `16` first-stage Tier-A clauses form a complete disjoint partition of the
  `64` Boolean assignments over `{223,224,303,305,384,385}`; each non-leaf0
  leaf's eight depth-3 fixed-pool clauses partition the `64` assignments over
  `{152,222,225,383,386,456}`; leaf0's depth-4/residual-closure clauses
  partition the same pool cube after replacing the one triggered terminal by
  four `{222,456}` children; and exported terminal pool assignments match the
  fresh replay exactly. Command:
  `cargo run -p hadwiger-research --example replay_g27_w_circles_semantic_partition`.
  Output:
  `tier_assignments 64 terminals 135 rows 80143 status SemanticPartitionTerminalCompositionPreflight`.
  Verification also passed:
  `cargo check -p hadwiger-research --example replay_g27_w_circles_semantic_partition`,
  `cargo fmt -p hadwiger-research --check`, and touched code files remain below
  `400` lines. Authority is now semantic branch partition plus H26 exact
  terminal replay for the declared mixed diagnostic partition, still not a
  final root theorem because full source-row reconstruction and final theorem
  packaging remain separate authority questions.
- Row-family semantics replay now succeeds as H28 preflight authority. Boole
  approved the hypothesis as the remaining obvious attack on H26/H27: the
  terminal rows covered weights exactly and partitioned semantically, but still
  needed proof that the exported rows were legitimate W607 row families rather
  than arbitrary coefficient vectors. New checker/example:
  `crates/hadwiger-research/src/frontier_seeds/g27_w_circles_row_family_semantics_replay.rs`
  and
  `crates/hadwiger-research/examples/replay_g27_w_circles_row_family_semantics.rs`.
  The checker composes with H27/H26, parses the row-carrying terminal artifact,
  and validates every one of the `80143` positive rows: edge rows against the
  retained exact W607 edge set and `edge_u_v` ids; triangle rows against all
  three retained edges; fixed-zero/fixed-one literal polarity against combined
  Tier-A plus pool terminal assignments; first-family rows against
  `w607-full-tree-rank-family.json` support digests, support size, alpha/RHS,
  and W607 integer weights as coefficients; and six parent-lift row
  occurrences against regenerated coefficient vectors from the existing
  digest-bound projected-parent and branch-slack lift artifacts. Command:
  `cargo run -p hadwiger-research --example replay_g27_w_circles_row_family_semantics`.
  Output:
  `checked_rows 80143 parent_lift_rows 6 status RowFamilySemanticsPreflight`.
  Verification passed:
  `cargo check -p hadwiger-research --example replay_g27_w_circles_row_family_semantics`,
  `cargo fmt -p hadwiger-research --check`, and touched files remain under
  `400` lines. Authority is now row-family semantics + semantic partition +
  exact terminal replay for the declared mixed diagnostic partition. Remaining
  gap is final theorem packaging/source reconstruction audit: parent vectors
  are matched to regenerated digest-bound lift artifacts, not newly reproved
  from first principles inside this H28 checker.
