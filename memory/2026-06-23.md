# 2026-06-23

## Query memory workspace native field path vocabulary

- Renamed memory-workspace retained aspect state from
  `external_projection_path` to `native_field_path`, including
  `ForgeQueryAspect::native_field_path()` and the crate-local
  `ForgeQueryAspect::from_native_field_path(...)`.
- Converted test-backend schema storage and duplicate-path validation to
  `native_field_path`; the public authoring helper still accepts projection
  field path text but immediately lowers it to `CanonicalFieldPath`.
- Refreshed compile-fail coverage so the removed
  `external_projection_path()` accessor fails directly, while
  `native_field_path()` and `schema.aspects()` paths still cannot be treated as
  `&str`.
- Verification passed: `cargo fmt -p forge-query`,
  `cargo check -p forge-query --tests`,
  `cargo test -p forge-query --test aspect_native_query_compile_fail`,
  `cargo test -p forge-query memory_workspace --lib`,
  `cargo test -p forge-query test_backend --lib`, and residue scans showing
  production no longer contains `external_projection_path`,
  `from_native_external_projection_path`, or `external projection path`.

## Worth touched graph milestone 6 spec draft

- Drafted `_docs/worth/touched-graph-milestone-6-graph-read-access-inventory-hard-break.md`
  with the aggressive Milestone 5-style rollover shape: build a parallel
  graph-read access inventory lane, migrate proof consumers, then delete or
  mechanically cap old `query_adoption/graph_read_access` authority.
- Updated `_docs/worth/touched-graph-roadmap.md` to link Milestone 6 to the
  spec and make old-lane deletion/capping an explicit done condition.

## Worth touched graph milestone 6 phase 1 implementation

- Added the initial `worth-kernel` graph-read access inventory lane at
  `crates/worth-kernel/src/graph_read_access_inventory/query_capabilities`.
- Phase 1 currently owns Query graph-read access capability vocabulary,
  receipt-field anchors, local Worth folklore rejection, and focused tests.
- `cargo fmt -p worth-kernel` and `git diff --check` passed. Focused
  `cargo test -p worth-kernel graph_read_capability --lib` is blocked before
  reaching `worth-kernel` because `worth-topo` is stale against the current
  `forge-query` API.
- QA loop found and fixed two Phase 1 gaps: arbitrary unknown Worth-local
  graph-read labels now fail unless they match Query-owned capability labels,
  and Query graph-read cost counter vocabulary is now represented explicitly
  instead of leaving the `CostCounter` row kind empty.
- QA-tests hardening sealed capability row construction to the inventory lane,
  added typed category-specific admission helpers, made exact parity tests
  compare against Query `ALL` surfaces and declared Worth receipt/counter
  inventories, rejected graph-touch obligation vocabulary as the wrong
  authority family, and source-qualified overlapping cost counter labels
  (`access_execution_counters.edge_scan_count` vs.
  `cost_estimate_counters.edge_scan_count`) so flat counter names cannot hide
  the Query-owned measurement boundary.
- Code-quality QA found `capability_map.rs` was structurally overloaded: it
  mixed report API, row catalog construction, vocabulary admission policy, and
  Query symbol anchoring. Split it into `capability_report.rs`,
  `capability_catalog.rs`, `vocabulary_admission.rs`, and the tiny
  `capability_snapshot.rs` orchestrator. Post-split line counts are all under
  cap; production tops out at `capability_catalog.rs` with 197 lines.
- Implemented Milestone 6 Phase 2 substrate under
  `crates/worth-kernel/src/graph_read_access_inventory/inventory_lane`.
  Added a Milestone 6 seed wrapper admitted only from the Milestone 5 graph
  read inventory seed, typed inventory owner/classification/cost/deletion/
  Milestone 7 disposition enums, validated inventory row builders, capped
  residue rows, closeout counters, and focused omission/counter tests. The
  lane does not execute graph reads, create access plans, fabricate receipts,
  or edit old `query_adoption/graph_read_access`; old-path mentions are only
  deletion/capped-residue fixtures.

## Query projection consumption aspect-key extraction

- Changed query-read result row extraction so aspect values reach projection
  fact field-path construction as `AspectKey` carriers instead of being
  weakened to local `aspect_path` strings first.
- Replaced private
  `projection_fact_field_path_from_aspect_label(&str)` with
  `projection_fact_field_path_from_aspect_key(&AspectKey)`.
- Verification passed: `cargo fmt -p forge-query`,
  `cargo check -p forge-query --tests`,
  `cargo test -p forge-query projection_consumption --lib`, and a targeted
  scan showing the removed helper/name residue is gone.

## Query mutation metadata admitted-key storage

- Changed `ForgeQueryMutationMetadata` from raw
  `BTreeMap<String, ForgeQueryMutationMetadataValue>` storage to
  `BTreeMap<ForgeQueryMutationMetadataKey, ForgeQueryMutationMetadataValue>`.
- Duplicate detection, typed lookup, and typed iteration now preserve the
  admitted metadata key proof internally, projecting key text only for denial
  messages.
- Verification passed: `cargo fmt -p forge-query`,
  `cargo check -p forge-query --tests`,
  `cargo test -p forge-query mutation_metadata --lib`,
  `cargo test -p forge-query write_receipt_inspection_retains_authored_mutation_metadata --lib`,
  `cargo test -p forge-query --test aspect_native_query_compile_fail`, and a
  scan showing the raw metadata map shape removed from production.

## Query public bridge existing-truth native key harness

- Replaced public bridge runtime test harness existing-truth map keys from raw
  `(String, String, String)` tuples to `PublicExistingTruthKey`, retaining the
  binding digest, target collection projection, and admitted
  `ForgeQueryAspectTouch`.
- Changed `seed_backend_authoritative_truth(...)` to accept
  `ForgeQueryAspectTouch` instead of raw aspect-path text; graph-composition
  and public bridge bootstrap tests now pass native `touch(...)` fixtures.
- Verification passed: `cargo fmt -p forge-query`,
  `cargo check -p forge-query --tests`,
  `cargo test -p forge-query --test public_bridge_runtime_bootstrap`,
  `cargo test -p forge-query --test graph_composition_public_bridge`,
  `cargo test -p forge-query --test graph_composition_public_bridge_existing`,
  and residue scans showing the old tuple-string existing-truth key and
  raw-string seed calls are gone.

## Query runtime test existing-truth native key adapter

- Replaced `TestExistingTruthVerificationAdapter` storage keys from raw
  `(String, String, String)` tuples to `TestExistingTruthKey`, retaining the
  binding digest, target collection projection, and admitted
  `ForgeQueryAspectTouch`.
- Kept `.with_value(binding, touch_fixture, value)` as fixture ergonomics, but
  storage now keeps the lowered touch proof instead of
  `admitted_touch_digest_part()` text.
- Verification passed: `cargo fmt -p forge-query`,
  `cargo check -p forge-query --tests`,
  `cargo test -p forge-query bridge_backed_verification_execution --lib`,
  `cargo test -p forge-query graph_composition_verified_existing --lib`,
  `cargo test -p forge-query graph_composition_edge_split --lib`, and scans
  showing the old tuple-key shape removed from the adapter.

## Query public bridge external-row native touch paths

- Removed the public bridge runtime external-row writer's
  `ForgeQueryAspectTouch -> terminal dotted string -> CanonicalFieldPath`
  round trip.
- `apply_aspects_to_external_row(...)` now sets and clears row values through
  admitted `ForgeQueryAspectTouch` carriers, deriving canonical external row
  fields from `native_aspect_key()` plus the optional native field path.
- Verification passed: `cargo fmt -p forge-query`,
  `cargo check -p forge-query --tests`,
  `cargo test -p forge-query --test public_bridge_runtime_bootstrap`,
  `cargo test -p forge-query --test graph_composition_public_bridge`,
  `cargo test -p forge-query --test graph_composition_public_bridge_existing`,
  `cargo test -p forge-query --test public_submission_lane_replacements`, and
  a targeted scan showing terminal path projection/string-path helpers gone
  from `tests/support/public_bridge_runtime/external_row.rs`.

## Query intent-admission certification probe native matching

- Removed semantic branching on `admitted_touch_digest_part()` from
  `src/intent_admission/certification/fixtures/runtime.rs`.
- The certification runtime existing-truth probe now matches by native aspect
  key and native field path, so the fixture uses admitted touch structure as
  authority instead of formatted `identity:id` / `title:value` strings.
- Verification passed: `cargo fmt -p forge-query`,
  `cargo check -p forge-query --tests`,
  `cargo test -p forge-query intent_admission --lib`,
  `cargo test -p forge-query --test intent_admission_public_dx`, and a scan
  showing `admitted_touch_digest_part()` gone from the fixture.

## Query graph-read predicate admission native lookup key

- Replaced boolean predicate admission's raw
  `BTreeMap<(String, String, String), ...>` lookup key with
  `AdmittedPredicateKey`, retaining `AspectKey`, `FieldKey`, and predicate
  family.
- Declarative predicate filters now admit into the same typed key before
  lookup instead of comparing anonymous aspect/field/family string triples.
- Verification passed: `cargo fmt -p forge-query`,
  `cargo check -p forge-query --tests`,
  `cargo test -p forge-query --test graph_read_access_phase_four_requirements`,
  `cargo test -p forge-query --test graph_read_access_phase_six_admission`,
  `cargo test -p forge-query --test graph_read_access_phase_two_selectivity`,
  `cargo test -p forge-query --test graph_read_access_phase_four_operator_matrix`,
  and a scan showing the raw tuple predicate lookup shape removed.

## Query effect intent input native aspect-touch value

- Added private `ForgeQueryIntentInputValue::AspectTouch` so effect payload
  intent inputs retain native `ForgeQueryAspectTouch` carriers in
  `changed_aspects`, `input_aspects`, and `output_aspects`.
- Digest material still serializes touches at the final digest boundary, but
  pre-digest intent input no longer stores those touches as generic strings.
- Verification passed: `cargo fmt -p forge-query`,
  `cargo check -p forge-query --tests`,
  `cargo test -p forge-query intent_admission --lib`,
  `cargo test -p forge-query effect --lib`, and a scan showing the old string
  touch-array helper removed.

## Query batch touched-aspect native dedupe

- Replaced digest-string keyed touched-aspect dedupe maps with native
  `BTreeSet<ForgeQueryAspectTouch>` in batch write execution, batch receipt
  aggregate derivation, and unified batch component inspection.
- Touched-aspect uniqueness now uses the admitted touch carrier directly
  instead of `admitted_touch_digest_part()` string projections.
- Verification passed: `cargo fmt -p forge-query`,
  `cargo check -p forge-query --tests`,
  `cargo test -p forge-query batch --lib`,
  `cargo test -p forge-query inspection --lib`,
  `cargo test -p forge-query --test public_submission_lane_replacements`, and
  a scan showing digest-keyed touched-aspect insertions removed from the
  converted runtime files.

## Query preview affected-target native routing maps

- Replaced preview session affected-aspect propagation maps from
  `BTreeMap<String, Vec<ForgeQueryAspectTouch>>` to maps keyed by
  `ForgeQueryLiveArtifactTarget` and `ForgeQueryDerivedMaterializationTarget`.
- Internal preview live/computed/effect routing now indexes changed aspect
  touches by typed Query artifact targets; public evidence still reports
  terminal handle names.
- Verification passed: `cargo fmt -p forge-query`,
  `cargo check -p forge-query --tests`,
  `cargo test -p forge-query preview --lib`,
  `cargo test -p forge-query effect --lib`,
  `cargo test -p forge-query --test public_submission_lane_replacements`, and
  a scan showing the old preview string-map routing shape removed.

## Query focused-inspector denial native aspect retention

- Changed `src/view_shape_live/execution.rs` so `focus_projection(...)`
  returns rejected widening aspects as `AspectKey` carriers instead of
  `Vec<String>`.
- The focused-inspector denial still renders terminal aspect text in the human
  message, but the denial decision now retains native aspect identity until
  that boundary.
- Verification passed: `cargo fmt -p forge-query`,
  `cargo check -p forge-query --tests`,
  `cargo test -p forge-query focused_inspector_widening_is_denied_and_counted --lib`,
  and a targeted scan showing
  `native_aspect_key().as_str().to_string()` removed from
  `src/view_shape_live/execution.rs`.

## Query retained evidence admitted-aspect-touch vocabulary

- Renamed production evidence identity tags that still called admitted touch
  material `aspect_path` / `aspect_paths`.
- Updated computed patch inspection, effect delivery inspection, preview
  execution evidence, intent-admission symbolic aspect seeds, unified
  write/batch inspection identities, graph-composition evidence, and batch
  mutation evidence to use `admitted_aspect_touch` or
  `admitted_aspect_touches`.
- Verification passed: `cargo fmt -p forge-query`,
  `cargo check -p forge-query --tests`,
  `cargo test -p forge-query batch --lib`,
  `cargo test -p forge-query effect --lib`,
  `cargo test -p forge-query preview --lib`,
  `cargo test -p forge-query intent_admission --lib`, and a production scan
  showing no `ForgeQueryEvidenceTag::new("aspect_path")` /
  `ForgeQueryEvidenceTag::new("aspect_paths")` under
  `crates/forge-query/src`.

## Query in-memory facade native row equivalence keys

- Changed `tests/in_memory_test_backend_facade.rs` so
  `selected_task_scalars(...)` returns
  `Vec<(CanonicalFieldPath, Option<AspectValue>)>` instead of
  `Vec<(String, Option<AspectValue>)>`.
- The equivalence assertion now compares retained row values by native
  field-path identity, with authoring text confined to fixture construction.
- Verification passed: `cargo fmt -p forge-query`,
  `cargo check -p forge-query --tests`,
  `cargo test -p forge-query --test in_memory_test_backend_facade`, and a scan
  showing the old string tuple shape removed.

## Query projection-policy native assertion keys

- Changed `authorized_projection`, `policy_live`, and `policy_narrowing` test
  assertion helpers to compare `(AspectKey, FieldKey)` tuples instead of
  `(String, String)` tuples.
- This keeps fixture authoring ergonomic while ensuring policy projection proof
  checks assert over native field identity.
- Verification passed: `cargo fmt -p forge-query`,
  `cargo check -p forge-query --tests`,
  `cargo test -p forge-query authorized_projection --lib`,
  `cargo test -p forge-query policy_live --lib`,
  `cargo test -p forge-query policy_narrowing --lib`, and a scan showing the
  native-key-to-string assertion helpers removed from those modules.

## Query integration fixture authored-touch ingress

- Added shared integration-test `support::aspect_touch(...)` as the root-level
  fixture ingress from authored touch text to `ForgeQueryAspectTouch`.
- Replaced duplicated local `fn touch(aspect_path: &str)` parsers across public
  bridge, graph read, intent DX, in-memory backend, public submission, hostile
  graph fixture, and public-bridge hostile support tests.
- Renamed the remaining graph-obligation local helper vocabulary to
  `authored_touch_text`; that module keeps a local parser because importing the
  full root support tree into its nested support module breaks crate-root
  paths.
- Verification passed: `cargo fmt -p forge-query`,
  `cargo check -p forge-query --tests`, and focused integration runs for
  `graph_composition_public_bridge`,
  `graph_composition_public_bridge_existing`,
  `runtime_backed_read_bootstrap`, `public_bridge_runtime_bootstrap`,
  `graph_read_access_phase_eight_ephemeral_provisioning`,
  `graph_read_access_phase_nine_streaming_frontier`,
  `graph_read_access_phase_thirteen_live_maintenance`,
  `in_memory_test_backend_facade`, `public_submission_lane_replacements`,
  `graph_obligation_consumer_kit_facade`,
  `graph_obligation_hostile_certification`, and `intent_admission_public_dx`.
- Scan clean for old `fn touch(aspect_path: &str)`,
  `fn set_operation(aspect_path: &str)`, old parser messages, and
  `AspectKey::new(segment.to_string())` / `FieldKey::new(segment.to_string())`
  in integration tests.

## Query backend affected live target carrier cleanup

- Replaced backend/source-adapter `affected_live_view_ids(...)` with
  `affected_live_view_targets(...)`, returning
  `Vec<ForgeQueryLiveArtifactTarget>`.
- Runtime batch receipt rows now use backend-returned target carriers directly
  instead of rebuilding affected live targets from terminal ids.
- Updated bridge-backed forwarding plus consumer-kit, runtime stabilization,
  intent-admission certification, stateful bridge, runtime source-adapter, and
  public bridge support implementations.
- Added the explicit
  `ForgeQueryLiveArtifactTarget::from_source_adapter_declared_view_name(...)`
  boundary for source-adapter declared view names.
- Verification passed: `cargo fmt -p forge-query`,
  `cargo check -p forge-query --tests`, `cargo test -p forge-query batch
  --lib`, `cargo test -p forge-query --test graph_composition_public_bridge`,
  and `cargo test -p forge-query --test aspect_native_query_compile_fail`.

## Query preview and adapter target-collection carrier cleanup

- Added `DeclarativeLiveQueryRequest::target_collection_identity()` and made
  `ForgeQueryMutationTargetCollectionIdentity::same_target_collection_as(...)`
  public while keeping the raw constructor sealed.
- Exported `ForgeQueryMutationTargetCollectionIdentity` through the facade so
  public runtime adapters can keep native collection identity tokens instead of
  storing live-view targets as raw strings.
- Changed preview live relevance and preview same-batch symbolic target
  resolution to compare native collection identity carriers instead of
  `request.target()` or `as_str()` projections.
- Changed live-view target maps in consumer-kit, transcript runtime,
  intent-admission certification, stateful bridge runtime support, runtime test
  source adapters, and public bridge runtime support from
  `BTreeMap<String, String>` to maps keyed by view name but valued by
  `ForgeQueryMutationTargetCollectionIdentity`.
- Verification passed: `cargo fmt -p forge-query`,
  `cargo check -p forge-query --tests`, `cargo test -p forge-query preview
  --lib`, `cargo test -p forge-query --test public_bridge_runtime_bootstrap`,
  `cargo test -p forge-query intent_admission --lib`, `cargo test -p
  forge-query --test graph_composition_public_bridge`, `cargo test -p
  forge-query --test graph_composition_public_bridge_existing`, `cargo test -p
  forge-query --test public_submission_lane_replacements`, and `cargo test -p
  forge-query --test aspect_native_query_compile_fail`.
- Residue scans stayed clean for production `serde_json` except the two
  allowlisted support terminal JSON codecs, and clean for the old affected-map
  string comparison/storage patterns in `src` and public test support.

## Query boundary reporting admitted-touch cleanup

- Changed test-backend schema duplicate-aspect diagnostics from
  `terminal_projection_from_aspect_touch(...)` to
  `reporting_projection_from_admitted_touch(...)`, backed by the stored
  `ForgeQueryAspectTouch`.
- Renamed public bridge existing-truth seed reporting from
  `terminal_aspect_path_projection(...)` to
  `admitted_aspect_touch_reporting_projection(...)`; the assertion now expects
  admitted-touch reporting text.
- Simplified public bridge and stateful bridge external-row boundary lowering
  so `FieldKey::new(...)` receives `native_aspect_key().as_str()` directly
  instead of explicitly allocating string projections.
- Verification passed: `cargo fmt -p forge-query`,
  `cargo check -p forge-query --tests`,
  `cargo test -p forge-query test_backend --lib`,
  `cargo test -p forge-query --test public_bridge_runtime_bootstrap`,
  `cargo test -p forge-query --test public_submission_lane_replacements`, and
  `cargo test -p forge-query bridge_backed_verification_execution --lib`.
- Scans are clean for direct `native_aspect_key().as_str().to_string()`,
  direct `native_field_key().as_str().to_string()`, and
  `terminal_aspect_path_projection(...)` outside compile-fail fixtures.

## Query crate-wide JSON residue enforcement

- Added crate-wide Rust source guards in
  `consumer_kit::support_snapshot::tests::runtime_boundary`.
- `rust_source_serde_json_residue_stays_terminal_or_compile_fail_only` now
  fails unless `serde_json` appears only in the two approved support terminal
  codecs, the guard file itself, or the deliberate compile-fail raw-JSON
  rejection fixtures.
- `query_sources_do_not_define_local_foundational_json_compatibility_bridge`
  now fails if Query grows `JsonCompatibility*`, `compatibility().json()`, or
  `lower_json_*` usage as an unapproved local JSON-as-aspect bridge.
- Verification passed: `cargo fmt -p forge-query`,
  `cargo test -p forge-query
  rust_source_serde_json_residue_stays_terminal_or_compile_fail_only --lib`,
  and `cargo test -p forge-query
  query_sources_do_not_define_local_foundational_json_compatibility_bridge
  --lib`.

## Query certification fixture aspect-path vocabulary cleanup

- Renamed runtime API stabilization transcript helper parameters from
  `aspect_path` to `authored_touch_text` / `authored_field_text`.
- Renamed the remaining computed-test update helper parameter from
  `aspect_path` to `authored_touch_text`.
- The helpers already lowered into Foundational field/aspect keys and
  `ForgeQueryAspectTouch`; this removes stale path-authority vocabulary from
  src-hosted certification/test helpers.
- Verification passed: `cargo fmt -p forge-query`,
  `cargo test -p forge-query runtime_api_stabilization --lib`,
  `cargo test -p forge-query computed --lib`, `cargo check -p forge-query
  --tests`, and a targeted scan showing no `fn .*aspect_path`,
  `aspect_path:`, `let aspect_path`, or old transcript aspect-path messages
  except the explicitly terminal computed projection helper.

## Query effect live-trigger target carrier cleanup

- Renamed runtime effect routing's live-view target map helper from
  `live_view_targets()` to `live_artifact_target_collections()`.
- Changed the map key from raw live view-name `String` to
  `ForgeQueryLiveArtifactTarget`; live-trigger routing now reconstructs the
  target carrier before reading the declared collection used for receipt-delta
  comparison.
- This keeps collection text at the live declaration / receipt boundary while
  removing another raw-string live artifact routing key.
- Verification passed: `cargo fmt -p forge-query`,
  `cargo test -p forge-query effect --lib`, `cargo check -p forge-query
  --tests`, and a targeted scan showing `live_view_targets(...)` gone from the
  converted runtime routing files.

## Query verified-assumption native touch breadth cleanup

- Changed `ForgeQueryVerificationReadSetBreadth::new(...)` so distinct
  asserted aspect touch counting dedupes `ForgeQueryAspectTouch` values
  directly instead of formatting each touch through
  `admitted_touch_digest_part()` first.
- Removed the graph-composition assumption summary helper named
  `native_asserted_aspect_touch_digest_parts(...)`; summary evidence now keeps
  native touches through aggregation and formats them only at the final digest
  boundary.
- Verification passed: `cargo fmt -p forge-query`, `cargo check -p
  forge-query --tests`, `cargo test -p forge-query graph_composition --lib`,
  `cargo test -p forge-query bridge_backed_verification_execution --lib`,
  `cargo test -p forge-query graph_composition_verified_existing --lib`, and a
  targeted scan showing the removed helper/string-dedupe pattern gone.

## Query effect payload terminal touch digest vocabulary cleanup

- Renamed effect delivery helper `native_touch_digest_sequence(...)` to
  `terminal_touch_digest_sequence(...)`.
- Effect payloads already retain `ForgeQueryAspectTouch` carriers; the helper
  only joins admitted touch digest parts for final digest material, so the new
  name makes the string projection boundary explicit.
- Verification passed: `cargo fmt -p forge-query`,
  `cargo test -p forge-query effect --lib`, `cargo check -p forge-query
  --tests`, and a targeted scan showing the old helper name gone from runtime
  effect sources.

## Query terminal aspect-touch digest helper vocabulary cleanup

- Renamed helper functions that projected admitted aspect touches into strings
  while calling the result `native`.
- `native_aspect_digest_parts(...)` became
  `terminal_aspect_touch_digest_parts(...)` in the consumer test-backend
  equivalence report and effect-triggered intent handoff binding identity.
- Graph obligation registration `native_touch_digest_parts(...)` became
  `terminal_touch_digest_parts(...)`.
- Verification passed: `cargo fmt -p forge-query`,
  `cargo test -p forge-query intent_admission --lib`,
  `cargo test -p forge-query graph_obligation --lib`,
  `cargo test -p forge-query test_backend --lib`, `cargo check -p
  forge-query --tests`, and a targeted scan showing the old helper names gone.

## Query projection consumption native visibility key

- Added crate-private `ProjectionFactAspectFieldKey`, which is produced only
  when a requested `ProjectionFactFieldPath` has the exact `aspect.field`
  shape.
- Changed projection-consumption visibility admission to compare authorized
  `AspectKey` / `FieldKey` carriers against that native request key instead of
  comparing `native_*().as_str()` to canonical path segment text.
- Added `visibility_admission_requires_exact_native_aspect_field_key`, proving
  an authorized `profile.display_name` does not admit
  `profile.display_name.extra` through loose field-path text matching.
- Verification passed: `cargo fmt -p forge-query`,
  `cargo test -p forge-query projection_consumption --lib`,
  `cargo check -p forge-query --tests`, and a targeted scan showing the
  projection-consumption native-key string comparison was removed.

## Query projection consumption extraction carrier cleanup

- Added private `ProjectionMaterializedField` so external relational/bridge
  field labels are lowered into `ProjectionFactFieldPath` at row-like
  extraction ingress instead of being passed through shared extraction as
  `(&str, AspectValue)` pairs.
- Added private `ProjectionGroupedMember` so grouped extraction consumes named
  row identity, member identity, and grouping value fields instead of
  `(&str, AspectValue, AspectValue)` tuples.
- Terminal bridge/relational field labels still exist at the source API
  boundary, but projection-consumption core now consumes typed projection
  field/member carriers.
- Verification passed: `cargo fmt -p forge-query`,
  `cargo test -p forge-query projection_consumption --lib`,
  `cargo check -p forge-query --tests`, and targeted scans showing the old
  row-like/grouped tuple iterator shapes removed from projection-consumption.

## Query read-composition native identity anchor key

- Replaced the read-composition scope classifier's direct
  `native_aspect_key().as_str() == "identity"` /
  `native_field_key().as_str() == "id"` comparisons with a local
  `NativeIdentityAnchorPredicateKey` carrying Foundational `AspectKey` and
  `FieldKey` values.
- The classifier still uses existing validated predicate-family/value-kind
  metadata, but aspect identity is now proven by native carriers when deciding
  whether a predicate is the identity anchor.
- Verification passed: `cargo fmt -p forge-query`,
  `cargo test -p forge-query read_composition --lib`,
  `cargo check -p forge-query --tests`, and a targeted scan showing the
  read-composition native-key string comparison was removed.

## Query native key string-equality residue cleanup

- Converted the intent-admission certification existing-truth probe fixture to
  compare requested probe fields against admitted `ForgeQueryAspectTouch`
  fixtures (`identity_id_touch()` / `title_value_touch()`) instead of
  decomposing touches into aspect/field text.
- Updated policy delivery and policy narrowing assertions to compare
  `AuthorizedProjectionFieldPath` or `(AspectKey, FieldKey)` carriers instead
  of literal `native_*().as_str()` equality.
- Verification passed: `cargo fmt -p forge-query`,
  `cargo test -p forge-query intent_admission --lib`,
  `cargo test -p forge-query policy_delivery --lib`,
  `cargo test -p forge-query policy_narrowing --lib`,
  `cargo check -p forge-query --tests`, and a targeted scan with no remaining
  direct native aspect/field key string equality sites under
  `crates/forge-query/src`.

## Query grouped row fixture native value carriers

- Replaced grouped live/view-shape and milestone-eight certification
  `GroupedRowFixture = (String, String, String)` aliases with named
  `GroupedRowFixture` structs.
- The member key remains terminal bridge fixture identity, while grouped row
  display/lane values are retained as Foundational `AspectValue` values
  produced through Query's native scalar helper instead of positional string
  tuple slots.
- Snapshot read helpers now ask the fixture row for values by terminal bridge
  read contract (`identity.id`, `profile.display_name`, `status.lane`) while
  keeping those terminal labels at the bridge boundary.
- Verification passed: `cargo fmt -p forge-query`,
  `cargo test -p forge-query view_shape_live --lib`,
  `cargo test -p forge-query milestone_eight_certification --lib`,
  `cargo check -p forge-query --tests`, and scans showing grouped row tuple
  aliases/destructures/string-tuple literals removed from the converted
  harnesses.

## Query same-batch symbolic target carrier cleanup

- Added internal `ForgeQuerySameBatchSymbolicTarget` in runtime helpers with
  private entity identity / optional collection fields and read-only accessors.
- Converted authoritative batch symbolic admission, same-batch resolution,
  symbolic aspect evidence rebuilding, and deferred backend atomic-batch target
  tracking away from `BTreeMap<String, (ForgeQueryEntityIdentity,
  Option<String>)>`.
- Converted preview batch symbolic preflight and staged symbolic target
  resolution to the same internal carrier, keeping preview and authoritative
  lanes aligned.
- Verification passed: `cargo fmt -p forge-query`,
  `cargo test -p forge-query batch --lib`,
  `cargo test -p forge-query preview --lib`,
  `cargo check -p forge-query --tests`, and the targeted runtime scan for
  `(ForgeQueryEntityIdentity, Option<String>)` symbolic target maps returned no
  matches.

## Query grouped execution lane identity carrier cleanup

- Changed `GroupedExecutionLaneValue` to store the existing native
  `GroupedLaneIdentity` instead of duplicating `AspectKey` plus lane-key text.
- Preserved public grouped execution accessors and added a native lane identity
  accessor, aligning grouped execution rows with grouped baseline desired-state
  and delta comparison carriers.
- Verification passed: `cargo fmt -p forge-query`,
  `cargo test -p forge-query view_shape_live --lib`, and
  `cargo check -p forge-query --tests`.

## Query collection ordering native key constructor cleanup

- Added `OrderingKeyPath::from_native_keys(...)` and changed validated
  collection ordering lowering to clone `AspectKey` / `FieldKey` carriers
  directly instead of projecting them to text and reparsing.
- The text constructor remains for authoring/default construction, but
  validated bundle planning no longer reconstructs proof from key strings.
- Verification passed: `cargo fmt -p forge-query`,
  `cargo test -p forge-query collection --lib`, and
  `cargo check -p forge-query --tests`.

## Query live relevance native field-key constructor cleanup

- Added `QueryFieldKey::from_native_keys(...)` and changed detail, ordered
  collection, and bounded materialization live relevance contracts to preserve
  validated `AspectKey` / `FieldKey` carriers instead of projecting native keys
  through strings and reparsing.
- Added `QueryFieldKey::terminal_digest_part(...)` for detail patch digest
  formatting so the remaining field string projection is visibly terminal
  evidence material.
- Verification passed: `cargo fmt -p forge-query`,
  `cargo test -p forge-query live --lib`, `cargo check -p forge-query
  --tests`, and a targeted scan showing the old live relevance native-key
  reparse pattern removed.

## Query effect aspect admission native-boundary cleanup

- Removed `validate_declared_effect_aspects(...)`, which projected native
  effect touches through `native_aspect_key().as_str().is_empty()` even though
  individual aspect identity is already guaranteed by `ForgeQueryAspectTouch`
  and Foundational `AspectKey` construction.
- Effect admission still rejects empty trigger/input/output touch lists; it no
  longer pretends a post-hoc string emptiness check is part of native
  authority.
- Verification passed: `cargo fmt -p forge-query`,
  `cargo test -p forge-query effect --lib`, `cargo check -p forge-query
  --tests`, and a targeted scan showing the removed validator/string guard is
  gone.

## Query aspect-field native lowering boundary cleanup

- Added crate-private `AspectFieldKey::native_aspect_key()` and
  `AspectFieldKey::native_field_key()` so admitted authoring field keys lower
  to Foundational carriers through one named boundary.
- Converted read obligation dispatch, preview aspect touch derivation, and live
  subscription delivery routing away from repeated
  `AspectKey::new(field.aspect().as_str())` /
  `FieldKey::new(field.field().as_str())` reconstruction.
- Verification passed: `cargo fmt -p forge-query`,
  `cargo test -p forge-query read_obligation_dispatch --lib`,
  `cargo test -p forge-query preview --lib`,
  `cargo test -p forge-query live --lib`, `cargo check -p forge-query
  --tests`, and a targeted scan showing the repeated runtime reparse pattern
  removed.

## Query graph composition terminal operation digest vocabulary cleanup

- Renamed graph composition's
  `native_declared_aspect_operation_digest_part(...)` helpers to
  `terminal_declared_aspect_operation_digest_part(...)` in obligation
  registration selector helpers, lookup-key material, and touch descriptor row
  digest construction.
- This is vocabulary cleanup, not behavior change: native
  `ForgeQueryAspectMutationOperation` remains the operation carrier, while the
  resulting string is terminal selector/digest/evidence material.
- Verification passed: `cargo fmt -p forge-query`,
  `cargo test -p forge-query graph_obligation --lib`,
  `cargo test -p forge-query graph_composition --lib`, `cargo check -p
  forge-query --tests`, and a targeted scan showing no
  `native_declared_aspect_operation_digest_part` residue.

## Query terminal digest material vocabulary root break

- Mechanically renamed the high-fanout `native_digest_material()` helper family
  to `terminal_digest_material()` across admitted aspect values, desired aspect
  values, effect payloads, mutation lowering, backend mutation authority,
  assertion construction, intent handoff bindings, and verification adapters.
- Renamed `aspect_value_native_digest_text(...)` to
  `terminal_aspect_value_digest_text(...)`, keeping `AspectValue` as the native
  carrier while marking the formatted string as terminal digest/evidence
  material.
- Renamed internal row and metadata formatter helpers from
  `native_digest_text`, `native_digest_parts`, and
  `native_result_digest_parts` to terminal vocabulary where they format
  evidence strings from already-native carriers.
- Verification passed: `cargo fmt -p forge-query`,
  `cargo test -p forge-query mutation --lib`,
  `cargo test -p forge-query effect --lib`,
  `cargo test -p forge-query computed --lib`,
  `cargo test -p forge-query --test aspect_native_query_compile_fail`,
  `cargo check -p forge-query --tests`, and residue scans showing the renamed
  helper names removed.

## Query existing-truth denial terminal value digest fence

- Renamed `ForgeQueryExistingTruthAssertionDenial`'s stored
  `expected_native_value_digest` and `found_native_value_digest` strings to
  `expected_terminal_value_digest` and `found_terminal_value_digest`.
- Renamed the public denial accessors to
  `expected_terminal_value_digest()` and `found_terminal_value_digest()`, so
  consumers cannot treat terminal digest strings as native value authority.
- Renamed existing-truth denial evidence tags from
  `expected_native_value` / `found_native_value` to
  `expected_terminal_value` / `found_terminal_value`, and renamed the mutation
  lowering row value tag from `native_value` to `terminal_value`.
- Added `assertion_denial_native_value_digest_alias_removed.rs` compile-fail
  coverage proving facade callers cannot use the old native-value digest
  accessors.
- Verification passed: `cargo fmt -p forge-query`,
  `cargo test -p forge-query verify_existing --lib`,
  `cargo test -p forge-query verified_update_existing --lib`,
  `cargo test -p forge-query verified_delete_existing --lib`,
  `cargo test -p forge-query --test aspect_native_query_compile_fail`,
  `cargo check -p forge-query --tests`, and residue scans showing the old
  native-value digest names removed from Query Rust sources.

## Query grouped binding native source-key constructor cleanup

- Added `AspectFieldKey::from_native_keys(...)` and changed grouped planning to
  build `QueryResultBindingProof` from native `AspectKey` / `FieldKey` source
  keys instead of terminal strings.
- Replaced grouped planning identity checks against `native_source_*().as_str()`
  with equality against native `AspectKey` / `FieldKey` values.
- Verified with green `cargo check -p forge-query --tests` and a targeted scan
  showing no remaining `QueryResultBindingProof::new` or grouped planning
  `native_source_*().as_str()` comparisons.

## Query projection-consumption native grouping-aspect oracle cleanup

- Changed grouped projection-consumption oracle digest helpers to accept native
  `AspectKey` / `Option<&AspectKey>` grouping aspect carriers instead of
  arbitrary grouping aspect text.
- Localized the terminal string projection to
  `terminal_grouping_aspect_digest_part(...)` inside the oracle digest boundary.
- Converted the structured-content validation fixture to compare native
  `AspectKey` / `FieldKey` values instead of `native_source_*().as_str()` text.
- Verified with green `cargo fmt -p forge-query`, green
  `cargo test -p forge-query projection_consumption --lib`, green
  `cargo test -p forge-query validation_cases --lib`, green
  `cargo check -p forge-query --tests`, and a targeted residue scan.

## Query grouped support native claim-admission cleanup

- Converted grouped shared-posture support admission from string comparisons to
  direct enum/native key comparisons.
- `SharedMaterialPreview` now checks
  `ForgeQueryGroupedIntent::Authoritative`, and `SharedContinuity` now checks
  `ForgeQueryGroupedContinuityAssumption::PreserveNeighborhood`.
- Replaced local `AspectFieldKey` matching through `aspect().as_str()` /
  `field().as_str()` with equality against an `AspectFieldKey` built from
  foundational `AspectKey` / `FieldKey` carriers.
- Verified with green `cargo fmt -p forge-query`, green
  `cargo test -p forge-query grouped_authoring --lib`, green
  `cargo check -p forge-query --tests`, and a targeted residue scan.

## Query memory workspace native declared-aspect alias cleanup

- Replaced memory workspace declared-aspect matching that reparsed native field
  paths through terminal dotted strings with direct construction of a native
  `ForgeQueryAspectTouch::aspect_field_path(...)` alias.
- Removed the local `parsed_aspect_touch(...)` helper and the now-dead
  `workspace.rs` terminal field-path projection helper.
- Left `memory_workspace/entity_row.rs` terminal field-path formatting alone
  because it only feeds `terminal_result_digest_parts()` evidence/report
  material.
- Verified with green `cargo fmt -p forge-query`, green
  `cargo test -p forge-query memory_workspace --lib`, green
  `cargo test -p forge-query test_backend --lib`, green
  `cargo check -p forge-query --tests`, and targeted scans.

## Query stateful bridge grouped baseline native projection selection cleanup

- Changed stateful bridge grouped baseline member extraction to select identity
  and grouping projection fields by native `AspectKey` values from
  `DeclarativeProjectionField::source_field_key()` instead of terminal
  `field.aspect()` / grouping-aspect text comparisons.
- Added `external_row_text_at_path(...)` for named external-row terminal lookup
  from `CanonicalFieldPath`, then removed the stale dotted-path
  `external_row_text(...)` and `native_external_field_path(...)` helpers.
- Kept the aspect-as-field conversion localized in
  `native_external_field_path_for_touch(...)`, the explicit stateful bridge
  external-row compatibility boundary.
- Verified with green `cargo fmt -p forge-query`, green
  `cargo test -p forge-query --test public_bridge_runtime_bootstrap`, green
  `cargo test -p forge-query --test runtime_backed_read_bootstrap`, green
  `cargo test -p forge-query live_grouped --lib`, green
  `cargo test -p forge-query runtime::tests::live --lib`, green
  `cargo check -p forge-query --tests`, and a targeted residue scan.

## Query same-batch symbolic collection carrier cleanup

- Changed `ForgeQuerySameBatchSymbolicTarget` to retain
  `ForgeQueryMutationTargetCollectionIdentity` instead of `Option<String>`.
- Converted authoritative batch execution, atomic-batch symbolic preflight,
  preview-batch symbolic preflight, preview receipt construction, and command
  declared-collection identity derivation to pass collection identity carriers
  through same-batch symbolic resolution.
- Kept terminal string projection at bridge bundle, memory-delta, receipt, and
  denial-message boundaries only. Collection mismatch checks compare labels
  from typed collection identities because the identities are role-scoped.
- Added
  `symbolic_target_reference_denies_collection_mismatch_with_native_target_identity`
  to cover authoritative collection-mismatch denial.
- Verification passed: `cargo fmt -p forge-query`,
  `cargo test -p forge-query symbolic_reference --lib`,
  `cargo test -p forge-query preview_branch_obligation_dispatch --lib`,
  `cargo test -p forge-query batch --lib`, and
  `cargo check -p forge-query --tests`.

## Query retained projection source target carrier cleanup

- Changed projection-consumption source constructors for retained derived
  artifact bindings and live artifact bindings to build source-reference
  identities from `ForgeQueryDerivedMaterializationTarget` /
  `ForgeQueryLiveArtifactTarget` carriers instead of terminal target-view-name
  iterators.
- This matches retained/live extraction, which already iterates
  `binding.targets()`, and leaves string view-name projection only at the
  explicit source-reference identity formatting boundary.
- Verification passed: `cargo fmt -p forge-query`,
  `cargo test -p forge-query retained_binding_declaration_preserves_binding_identity_and_target_refs --lib`,
  `cargo test -p forge-query retained_live --lib`,
  `cargo test -p forge-query projection_consumption --lib`, and
  `cargo check -p forge-query --tests`.

## Query graph composition collection carrier cleanup

- Changed `ForgeQueryGraphCompositionProgramStep` declared collection storage
  from raw `String` text to
  `Option<ForgeQueryMutationTargetCollectionIdentity>`.
- Builder symbolic declarations/followups/retirements now pass
  `target_collection_identity()` carriers into program steps. Existing-target
  graph lifecycle steps now clone `binding.target_collection_identity()`
  instead of projecting terminal collection text.
- Graph composition denials and `graph_composition_error(...)` now carry
  target collection identity carriers; workspace graph error mapping preserves
  identities from existing-truth bindings and symbolic references.
- Touch descriptor validation now compares command/program collection carriers
  by label, and touch-row derivation projects from identities only at the row
  output boundary.
- Converted graph obligation/touch descriptor fixtures and stop-class graph
  denial fixtures away from direct `Option<String>` collection construction.
- Verification passed: `cargo fmt -p forge-query`,
  `cargo check -p forge-query --tests`,
  `cargo test -p forge-query graph_composition --lib`,
  `cargo test -p forge-query graph_obligation --lib`, and a targeted scan
  showing the graph-composition collection projection residues removed.

## Query mutation receipt target evidence carrier cleanup

- Changed `ForgeQueryMutationTargetDescriptor::new(...)` to accept
  `Option<ForgeQueryMutationTargetCollectionIdentity>` rather than
  `Option<String>`.
- Write receipt target evidence now passes declared/resolved collection
  identity carriers directly instead of projecting labels and rebuilding target
  identities.
- Symbolic target evidence, symbolic aspect resolution evidence, and symbolic
  aspect lowering digest rows now clone/use `target_collection_identity()` from
  the Query symbolic reference rather than rebuilding from
  `target_collection()` terminal text.
- Preview symbolic entity identity now cites the target collection evidence
  identity instead of using target collection label text as a shape field.
- Verification passed: `cargo fmt -p forge-query`,
  `cargo check -p forge-query --tests`,
  `cargo test -p forge-query symbolic_reference --lib`,
  `cargo test -p forge-query aspect_preview --lib`, and
  `cargo test -p forge-query batch --lib`.

## Query naming and continuity target collection evidence cleanup

- Converted naming and continuity mutation evidence construction so write
  receipt helpers accept `ForgeQueryMutationTargetCollectionIdentity` carriers
  instead of optional collection string slices.
- Authoritative, batch, and preview write receipt construction now passes
  collection identity carriers into naming/continuity evidence rather than
  projecting collection labels and rebuilding weaker identities.
- Preview receipt construction now derives the target collection identity from
  `command.declared_collection_identity()` and no longer uses command terminal
  declared-collection projection.
- Removed the now-dead command terminal declared-collection projection accessor
  and updated the aspect-native compile-fail fixture to prove consumers cannot
  call it.
- Bridge bundle text remains a bridge-boundary compatibility input only; once
  converted to a target collection identity, native receipt construction stays
  carrier-based.
- Verification covered green `cargo check -p forge-query --tests`, green
  `cargo test -p forge-query --test aspect_native_query_compile_fail`, green
  focused `cargo test -p forge-query naming --lib`, and green focused
  `cargo test -p forge-query continuity --lib`.

## Query receipt-summary collection carrier cleanup

- Changed `classify_receipt_mutation_summary(...)` to return
  `Option<ForgeQueryMutationTargetCollectionIdentity>` for the single resolved
  receipt collection instead of `Option<String>`.
- Updated authoritative write routing, batch component receipt construction,
  intent/effect execution routing, and same-batch symbolic target recording to
  pass the carrier through rather than projecting labels and rebuilding target
  identities.
- Updated bridge naming/continuity lowering and symbolic-target bridge
  lowering to accept collection identity carriers; terminal projection now
  happens at the bridge facade boundary instead of in runtime execution.
- Updated preview symbolic write receipt attachment to retain collection
  identity carriers and removed the dead cloned terminal collection helper from
  same-batch symbolic targets.
- Verification covered green `cargo fmt -p forge-query`, green
  `cargo check -p forge-query --tests`, green focused `cargo test -p
  forge-query bridge_lowered_continuity --lib`, green focused `cargo test -p
  forge-query batch --lib`, and green focused `cargo test -p forge-query
  aspect_preview --lib`.

## Query read-composition relation materialization cleanup

- Converted runtime read-composition traversal materialization so relation
  lookup receives native `RelationName` carriers instead of terminal relation
  projection text.
- Localized the legacy relation row-field compatibility mapping in
  `relation_target_field_path(...)`, which accepts a `RelationName` and builds
  the corresponding `CanonicalFieldPath` from `FieldKey` segments.
- Converted identity anchor and identity ordering detection to compare
  `source_field_key()` through native `AspectFieldKey` lowering instead of
  terminal `aspect() == "identity"` / `field() == "id"` aliases.
- Verification covered green `cargo fmt -p forge-query`, green focused
  `cargo test -p forge-query read_composition --lib`, green
  `cargo check -p forge-query --tests`, and a targeted scan showing no old
  terminal relation/identity alias calls in `read_composition_materialization.rs`.

## Query bridge row extraction native projection cleanup

- Converted bridge truth-view row-set projection fact extraction so bridge
  fields derive `ProjectionFactFieldPath` from
  `BridgeMaterializedFieldValue::projection()` metadata instead of parsing
  `BridgeMaterializedFieldIdentity::as_str()` as a dotted field path.
- Added `ProjectionMaterializedField::from_bridge_field_value(...)` as the
  bridge-specific constructor. It uses the bridge field locator's native
  `CanonicalFieldPath` when present and falls back to the projection's native
  aspect key for whole-aspect projections.
- Bridge field identity text now appears only in diagnostic field labels for
  invalid bridge field shapes; it no longer feeds consumed fact lookup keys.
- Verification covered green `cargo fmt -p forge-query`, green
  `cargo check -p forge-query --tests`, green focused `cargo test -p
  forge-query projection_consumption --lib`, and a targeted scan confirming
  bridge row extraction no longer parses bridge field identity text as field
  authority.

## Query memory workspace aspect constructor proof check

- Changed public `ForgeQueryAspect::new(...)` to return
  `Result<ForgeQueryAspect, ForgeQueryWorkspaceError>` and reject mismatches
  between an admitted `ForgeQueryAspectTouch` and the declared
  `CanonicalFieldPath`.
- The constructor now requires declared native field paths to be rooted at the
  touch's admitted aspect key, and field-specific touches must match the full
  `aspect_key + touch field path` storage path.
- Added
  `memory_workspace_aspect_rejects_mismatched_native_field_path` and refreshed
  the aspect-native compile-fail fixture line snapshot after making
  `ForgeQueryAspect::new(...)` fallible.
- Verification passed: `cargo fmt -p forge-query`,
  `cargo test -p forge-query memory_workspace --lib`,
  `cargo check -p forge-query --tests`, and
  `cargo test -p forge-query --test aspect_native_query_compile_fail`.

## Query subagent remaining-root audit

- Used subagents to audit remaining aspect-native/json-removal roots while
  implementing the memory-workspace constructor slice locally.
- The highest-impact remaining roots found are:
  `authoring/names.rs` `AspectFieldKey` string storage, mutation command and
  backend-admissible collection strings, retained/live artifact routing keyed
  by view-name text, projection fact external-boundary path ingress, graph
  touch descriptor collection selectors, and predicate admission reparsing of
  `filter.aspect()` / `filter.field()` aliases.

## Query authoring aspect-field native key storage cleanup

- Changed `AspectName` and `FieldName` from raw `String` wrappers to
  foundational `AspectKey` and `FieldKey` wrappers.
- `AspectFieldKey` now retains foundational key proof internally while keeping
  the existing public ergonomic string authoring and display accessors.
- `AspectFieldKey::from_native_keys(...)`, `native_aspect_key()`, and
  `native_field_key()` now clone retained native carriers instead of projecting
  to text and reparsing.
- Added focused authoring tests for foundational admission and native-key
  preservation.
- Verification passed: `cargo fmt -p forge-query`,
  `cargo test -p forge-query authoring::names --lib`, and
  `cargo check -p forge-query --tests`; `authoring/names.rs` remains under
  the 400-line cap and the old native-key text reparse pattern is gone.

## Query write-command collection identity storage cleanup

- Converted `ForgeQueryWriteCommand::InsertAspects.collection` and
  `DeleteAspects.declared_collection` from raw collection text to
  `ForgeQueryMutationTargetCollectionIdentity` carriers.
- Converted `ForgeQueryBackendAdmissibleMutationShape::Insert.collection` and
  `DeleteDirect.declared_collection` to retain backend-admissible collection
  identity carriers, with one re-role transition from command proof state to
  backend-admissible proof state.
- Insert/delete builders still accept ergonomic text, but immediately lower to
  `"write-command-declared"` identities before command construction.
- Symbolic delete rebuilds now clone collection identity carriers; preview
  memory deltas project collection labels only at the terminal delta boundary.
- Verification passed: `cargo fmt -p forge-query`,
  `cargo check -p forge-query --tests`,
  `cargo test -p forge-query mutation --lib`,
  `cargo test -p forge-query batch --lib`, and
  `cargo test -p forge-query preview_branch_obligation_dispatch --lib`.

## Query graph touch selector collection carrier cleanup

- Converted graph touch selector collection variants from raw collection text
  to `ForgeQueryMutationTargetCollectionIdentity` carriers. Constructor text is
  now only ergonomic ingress and lowers immediately to selector-role carriers.
- Converted graph touch descriptor rows to retain declared collection identity
  carriers; row string projection remains only for terminal reporting.
- Added a private graph-obligation collection lookup identity built from target
  collection carriers so index keys no longer expose `Collection(String)`.
- Removed dead string collection matching/accessor paths.
- Verification passed: `cargo fmt -p forge-query`,
  `cargo check -p forge-query --tests`,
  `cargo test -p forge-query graph_obligation --lib`,
  `cargo test -p forge-query graph_touch_descriptor --lib`, and a targeted
  residue scan for the old graph selector/row collection string shapes.

## Query retained/live artifact bundle target carrier cleanup

- Converted `ForgeQueryLiveArtifactBundle` and
  `ForgeQueryDerivedMaterializationBundle` from view-name string keyed maps to
  maps keyed by `ForgeQueryLiveArtifactTarget` /
  `ForgeQueryDerivedMaterializationTarget` carriers.
- Converted live/derived bundle construction, published shared-read artifact
  binding, retained/live projection support, and retained scalar tests to
  insert target carriers instead of raw view-name strings.
- Replaced bundle by-name lookup helpers with `read_for_target(...)` /
  `materialization_for_target(...)`, and binding validation now checks
  carrier-keyed bundle membership.
- Compile-fail expectations now prove the by-name bundle helpers are removed
  entirely.
- Verification passed: `cargo fmt -p forge-query`,
  `cargo check -p forge-query --tests`,
  `cargo test -p forge-query retained_live --lib`,
  `cargo test -p forge-query projection_consumption --lib`,
  `cargo test -p forge-query runtime::tests::live_artifacts --lib`,
  `cargo test -p forge-query retained_scalar --lib`, and
  `cargo test -p forge-query --test aspect_native_query_compile_fail`.

## Query backend live lookup target carrier cleanup

- Converted `ForgeQueryRuntimeBackend` and `ForgeQueryRuntimeSourceAdapter`
  live lookup methods from raw `&str` view names to
  `ForgeQueryLiveArtifactTarget` carriers.
- Added `ForgeQueryLiveArtifactTarget::from_subscription_installation(...)`
  and changed runtime live reads, patch drains, preview program reads,
  retained live refreshes, and materialized read-composition lookups to pass
  live targets instead of anonymous view-name strings.
- Updated bridge-backed backend forwarding plus in-memory, stateful bridge,
  public bridge, certification, transcript, support-snapshot, and source
  adapter fixtures to the target-carrier contract. UI compile-fail fixtures
  were refreshed so they still fail on the intended removed `snapshot_token`
  method, not removed live lookup methods.
- Split `runtime/backend/snapshot_identity.rs` out of `contracts.rs` to bring
  the touched backend contract file back under the 400-line cap.
- Verification passed: `cargo fmt -p forge-query`,
  `cargo check -p forge-query --tests`,
  `cargo test -p forge-query runtime::tests::live --lib`,
  `cargo test -p forge-query retained_live --lib`,
  `cargo test -p forge-query read_composition --lib`, and
  `cargo test -p forge-query --test phase_boundaries_bridge_truth_identity_compile_fail`.

## Query relational projection fact path ingress cleanup

- Removed the generic
  `ProjectionMaterializedField::from_external_boundary(...)` relational
  extraction path and replaced it with
  `from_relational_projected_aspect_key(...)`, which accepts the
  `AspectKey` carrier retained by `RelationalProjectedAspectValueSet`.
- Renamed the internal dotted lowering helper to
  `projection_fact_field_path_from_relational_projected_aspect_key(...)`,
  fencing the remaining relational label split behind a named compatibility
  boundary instead of an arbitrary external-boundary parser.
- Bridge row-set extraction remains on bridge projection metadata, and
  query-owned read/live extraction remains on native entity field paths.
- Verification passed: `cargo fmt -p forge-query`,
  `cargo check -p forge-query --tests`, and
  `cargo test -p forge-query projection_consumption --lib`.

## Query projection fact field-path segment carrier cleanup

- Changed `projection_fact_field_path_from_segments(...)` to accept
  foundational `FieldKey` carriers instead of raw string segments.
- Mechanically converted projection-consumption, domain-capability,
  intent-admission, retained/live, shared-read, hostile certification, and
  certification fixture call sites to construct `FieldKey` segments before
  building `ProjectionFactFieldPath`.
- This closes the remaining projection field-path construction root: bridge
  extraction uses bridge projection metadata, query/read/live extraction uses
  native entity field paths, relational row extraction is fenced behind a named
  relational projected-aspect-key compatibility boundary, and segment assembly
  now requires foundational field-key proof.
- Verification passed: `cargo fmt -p forge-query`,
  `cargo check -p forge-query --tests`, and
  `cargo test -p forge-query projection_consumption --lib`.

## Query predicate admission native key cleanup

- Used subagents as sidecar auditors for the aspect-native Query refactor. Both audits agreed the immediate root was predicate/declarative graph-read string re-projection, while production JSON is now mostly contained to terminal support codecs.
- Added crate-internal authoring predicate constructors that accept `AspectFieldKey` carriers directly, then converted declarative predicate lowering, normalized predicate projection, boolean predicate admission/evidence, and schema-reference admission to use `source_field_key()` / native `AspectKey` / `FieldKey` instead of `filter.aspect()` / `filter.field()` reparsing.
- Removed the declarative predicate filter `aspect()` / `field()` aliases entirely, strengthening the compile-fail fixture from private-method errors to method-not-found errors.
- Verification passed: `cargo fmt -p forge-query`, `cargo check -p forge-query --tests`, `cargo test -p forge-query --test aspect_native_query_compile_fail`, `cargo test -p forge-query graph_read_access --lib`, and `cargo test -p forge-query read_composition --lib`.
- Next roots recorded in `_docs/forge-query/aspect_native_query_refactor.md`: native-first `AspectFieldKey`, native selector constructors for projection/result-shape/ordering, then target-carrier routing for affected live/computed/effect dependencies.

## Query AspectFieldKey/declarative carrier enforcement

- Used two subagents as sidecar auditors while keeping implementation on the
  main thread. Tesla found production `serde_json` is confined to the two
  support terminal JSON codecs, and called out digest-string authority for
  admitted aspect touches as the next meaningful production root. Zeno found
  the biggest safe mechanical test batch: hundreds of
  `AspectFieldKey::from_authoring_parts(...)` fixture calls that should move
  through native test helpers.
- Renamed the neutral `AspectFieldKey::new(...)` constructor to
  `from_authoring_parts(...)`, made the native foundational-key constructor
  public, added native selector constructors, and added a compile-fail fixture
  proving the neutral constructor is gone.
- Converted declarative canonicalization/read-composition lowering to carry
  `AspectFieldKey` via `source_field_key()` / `target_field_key()` instead of
  decomposing into aspect/field strings and reparsing.
- Removed now-dead crate-local declarative raw helper roots and string aliases
  for ordering, predicates, projection, branch-compare deltas, and writeback
  changes. Refreshed compile-fail stderrs so stronger removals now show
  method-not-found where applicable.
- Verification passed: `cargo check -p forge-query --tests` and
  `cargo test -p forge-query --test aspect_native_query_compile_fail`.
  Whole-crate rustfmt still intermittently hits Windows mapped-section locks;
  touched-file formatting should be retried before closeout when locks clear.

## Query mutation seed touch evidence cleanup

- Converted mutation lowering digest-row helpers so callers pass
  `ForgeQueryAspectTouch` carriers instead of pre-rendered admitted touch
  digest strings. Terminal projection now stays inside the final evidence row
  helper.
- Converted authoritative mutation seed operation identity from anonymous
  `kind:touch_digest` strings to structured evidence identities carrying the
  operation kind and terminal admitted-touch evidence.
- Verification passed: touched-file `rustfmt`, `cargo check -p forge-query
  --tests`, `cargo test -p forge-query intent_admission --lib`, and
  `cargo test -p forge-query mutation --lib`. Both touched source files are
  297 lines after formatting.

## Query production field-carrier cleanup

- Used subagents for two parallel slices, then integrated locally. Production
  workspace live declaration assembly now retains schema fields as
  `AspectFieldKey` carriers, uses `DeclarativeProjectionField::new(...)`
  instead of reparsing aspect/field labels, and removed the old
  `schema_field_view(...)` string helper.
- Converted canonical projection, predicate, ordering, and result-shape
  entries to retain `AspectFieldKey` carriers instead of split `AspectName` /
  `FieldName` fields. Authorized projection, validation, predicate
  normalization, ordering validation, result-shape validation, and
  validated-artifact construction now consume the carrier and project terminal
  text only for diagnostics/digests/events.
- The intentional `cargo check -p forge-query --tests` red exposed 65
  split-field consumers, all fixed in the slice.
- Verification passed: `cargo check -p forge-query --tests`,
  `cargo test -p forge-query canonicalization --lib`,
  `cargo test -p forge-query validation --lib`, and
  `cargo test -p forge-query authorized_projection --lib`. Prior worker
  verification also passed `cargo test -p forge-query workspace_declaration
  --lib`.
- Updated the refactor plan to focus remaining work on production authority
  roots and critical tests only: admitted-touch digest authority, mutation
  delta collection routing, and live/computed/effect target routing.

## Query mutation delta collection identity cleanup

- Replaced the raw `collection: String` field on `ForgeQueryMutationDelta`
  with `ForgeQueryMutationTargetCollectionIdentity`.
- Memory-workspace production receipt construction now creates mutation deltas
  from a collection identity. Existing `collection()` text is a terminal
  projection from the identity for compatibility with current string-keyed
  indexes.
- Live subscription maintenance evidence now uses
  `delta.target_collection_identity().evidence_identity()` directly instead of
  rebuilding collection evidence from text.
- The intentional `cargo check -p forge-query --tests` red exposed direct
  `delta.collection` consumers in routing, inspection, preview relevance,
  signal invalidation, and critical harness adapters. Those consumers now use
  the native identity where evidence is needed or `collection()` where terminal
  text/index compatibility is still the boundary.
- Verification passed: `cargo check -p forge-query --tests`,
  `cargo test -p forge-query memory_workspace --lib`,
  `cargo test -p forge-query mutation --lib`, and
  `cargo test -p forge-query preview --lib`. The attempted
  `cargo test -p forge-query live_subscription --lib` matched zero tests.

## Query live subscription index target carrier cleanup

- Converted `ForgeQueryRuntime::live_subscription_index` to store
  `ForgeQueryLiveArtifactTarget` values instead of raw view-name strings.
- Live subscription registration now stores the target carrier derived from the
  admitted subscription installation.
- Delivery routing now consumes and returns `ForgeQueryLiveArtifactTarget`
  carriers, projecting `view_name()` only when indexing
  `live_subscriptions` or reporting delivery errors.
- Authoritative mutation routing now keeps the returned affected live targets
  instead of rebuilding targets from strings for the write receipt/effect path.
  Computed candidate routing still projects live targets to view names at its
  dependency boundary; that remains a future routing-family cleanup.
- Verification passed: `cargo fmt -p forge-query`,
  `cargo check -p forge-query --tests`, and focused
  `cargo test -p forge-query aspect_crud --lib`.

## Query computed dependency target carrier cleanup

- Converted `ForgeQueryComputedDependencyIndex` so live dependency keys are
  `ForgeQueryLiveArtifactTarget` carriers and computed dependency keys/values
  are `ForgeQueryDerivedMaterializationTarget` carriers.
- `computed_candidate_live_views(...)` now returns live artifact targets from
  the live subscription index instead of candidate view-name strings.
- `route_derived_view_patches(...)` now consumes live target candidates and
  returns affected derived materialization targets. Authoritative mutation
  routing keeps those targets for write receipts and effect routing instead of
  reconstructing them from strings.
- Retained upstream row maps and `ForgeQueryDerivedView` declarations still
  project names at their existing boundaries; the computed dependency graph
  itself no longer uses names as its authority carrier.
- Verification passed: `cargo check -p forge-query --tests`,
  `cargo test -p forge-query computed --lib`, and
  `cargo test -p forge-query retained_live --lib`.

## Query effect trigger index target carrier cleanup

- Converted `ForgeQueryEffectIndex` so live trigger keys are
  `ForgeQueryLiveArtifactTarget` values and computed trigger keys are
  `ForgeQueryDerivedMaterializationTarget` values.
- Effect declaration admission now derives live/derived target sets from
  runtime state and checks declared trigger names through those target
  carriers.
- Effect candidate selection now consumes affected live and derived target
  slices directly instead of projecting the affected write-route targets to
  strings before consulting the effect index.
- `ForgeQueryEffectTrigger` remains a public declaration boundary with a source
  name; live trigger change collection projects that name into a live target
  locally. The effect index and candidate-selection authority no longer use raw
  names as their routing keys.
- Verification passed: `cargo check -p forge-query --tests` and
  `cargo test -p forge-query effect --lib`.

## Query graph descriptor collection identity cleanup

- `ForgeQueryGraphTouchDescriptorRow::touches_declared_collection(...)` now
  compares `ForgeQueryMutationTargetCollectionIdentity` carriers through an
  explicit target-collection semantic comparator instead of projecting both
  sides to text.
- `ForgeQueryGraphTouchDescriptorInventory` now retains declared collection
  identity carriers and dedupes with that native comparator instead of storing
  `BTreeSet<String>`.
- Added deterministic ordering support for
  `ForgeQueryMutationTargetCollectionIdentity` for native ordered collections.
- Focused graph tests first failed when full evidence-identity equality proved
  too strict for semantic target collection matching; fixed by distinguishing
  role/provenance identity from same target-collection semantics.
- Verification passed: `cargo check -p forge-query --tests`,
  `cargo test -p forge-query graph_touch_descriptor --lib`, and
  `cargo test -p forge-query graph_obligation --lib`.

## Query same-batch symbolic collection identity cleanup

- `record_same_batch_symbolic_target(...)` now matches created deltas against
  declared target collections with
  `ForgeQueryMutationTargetCollectionIdentity::same_target_collection_as(...)`
  instead of terminal collection text.
- `resolve_same_batch_symbolic_target(...)` now checks expected versus resolved
  collection with the same native semantic comparator.
- `classify_receipt_mutation_summary(...)` now collects mutation delta
  collection identities and returns the retained identity for single-target
  receipts instead of collecting strings and rebuilding a summary identity.
- Verification passed: `cargo check -p forge-query --tests`,
  `cargo test -p forge-query symbolic_reference --lib`, and
  `cargo test -p forge-query batch --lib`.

## Query live subscription target collection index cleanup

- Converted `ForgeQueryRuntime::live_subscription_index` away from a
  target-collection `String` key and into native
  `ForgeQueryMutationTargetCollectionIdentity` index entries.
- Registration stores `request.target_collection_identity()` and dedupes with
  `same_target_collection_as(...)`; unregistration prunes native entries when
  their target set becomes empty.
- Live delivery routing and computed candidate discovery now match mutation
  delta target collection identities against index entries instead of doing
  `.get(delta.collection())`.
- Effect live-trigger collection routing now carries
  `ForgeQueryMutationTargetCollectionIdentity` and compares deltas with the
  same native semantic comparator.
- Verification passed: `cargo fmt -p forge-query`,
  `cargo check -p forge-query --tests`, `cargo test -p forge-query live
  --lib`, `cargo test -p forge-query computed --lib`,
  `cargo test -p forge-query effect --lib`, and
  `cargo test -p forge-query --test aspect_native_query_compile_fail`.
- Focused scans confirmed the old runtime subscription-index string-key
  patterns are gone. Production `serde_json` under `crates/forge-query/src`
  remains confined to the two approved support terminal JSON codecs.

## Query published artifact target registry cleanup

- Converted `ForgeQueryPublishedArtifactRegistry` generation entries from
  `BTreeMap<String, ForgeQueryPublishedArtifactEntry>` to
  `BTreeMap<ForgeQueryDerivedMaterializationTarget, ...>`.
- Shared-read publication and consumption now resolve published artifacts by
  `ForgeQueryDerivedMaterializationTarget`; terminal view names remain for
  handle reporting, errors, and existing evidence labels.
- `ForgeQueryPublishedArtifactEntry` stores its target carrier and binding no
  longer rebuilds a target from raw view-name text.
- Verification passed: `cargo fmt -p forge-query`,
  `cargo check -p forge-query --tests`, `cargo test -p forge-query shared_read
  --lib`, `cargo test -p forge-query runtime_boundary --lib`, and
  `cargo test -p forge-query --test aspect_native_query_compile_fail`.

## Query live subscription state target key cleanup

- Converted `ForgeQueryRuntime::live_subscriptions` from
  `BTreeMap<String, ForgeQueryRuntimeLiveSubscriptionState>` to
  `BTreeMap<ForgeQueryLiveArtifactTarget, ForgeQueryRuntimeLiveSubscriptionState>`.
- Declaration inserts by installed live target; delivery routing, async
  projection, time-only and mixed-cause delivery, live reads, preview routing,
  inspection, state snapshots, downstream delivery, and critical test helpers
  now construct target carriers at lookup boundaries.
- `live_artifact_target_collections(...)` now uses the native subscription map
  key directly instead of reconstructing targets from string keys.
- Verification passed: `cargo fmt -p forge-query`,
  `cargo check -p forge-query --tests`, `cargo test -p forge-query live
  --lib`, `cargo test -p forge-query shared_read --lib`,
  `cargo test -p forge-query runtime_boundary --lib`, and
  `cargo test -p forge-query --test aspect_native_query_compile_fail`.
- Focused scans confirmed the old runtime live-subscription string map and
  published-artifact string map patterns are gone, while production
  `serde_json` remains confined to the two support terminal codecs.

## Query computed derived target authority cleanup

- Converted `ForgeQueryRuntime::derived_views` from a view-name `String` map to
  `BTreeMap<ForgeQueryDerivedMaterializationTarget, ForgeQueryDerivedViewRuntime>`.
- Updated computed admission, cycle detection, topological routing,
  retained-upstream discovery, patch routing, shared-read publication,
  effect-trigger lookup, preview binding lookup, runtime read drains, and
  inspection materialization lookup to flow through derived target carriers.
- Converted `ForgeQueryRetainedUpstreamInputs` internals to keep live rows under
  `ForgeQueryLiveArtifactTarget` and computed rows under
  `ForgeQueryDerivedMaterializationTarget`; authoritative mutation routing no
  longer rebuilds a raw-name retained live row map before computed refresh.
- Refreshed the retained-upstream compile-fail stderr after the private
  constructor signature became stricter and exposed typed carriers in the
  diagnostic.
- Verification passed: `cargo fmt -p forge-query`,
  `cargo check -p forge-query --tests`, focused `cargo test -p forge-query
  computed --lib`, focused `cargo test -p forge-query effect --lib`, focused
  `cargo test -p forge-query shared_read --lib`, focused `cargo test -p
  forge-query runtime_boundary --lib`, and `cargo test -p forge-query --test
  aspect_native_query_compile_fail`.
- Focused scans confirmed no remaining runtime
  `BTreeMap<String, ForgeQueryDerivedViewRuntime>` or raw-name retained
  upstream row maps; production `serde_json` remains confined to the two
  support terminal JSON codecs.

## Query effect runtime target authority cleanup

- Added runtime-local `ForgeQueryEffectTarget` and converted
  `ForgeQueryRuntime::effects` from `BTreeMap<String,
  ForgeQueryEffectRuntime>` to `BTreeMap<ForgeQueryEffectTarget,
  ForgeQueryEffectRuntime>`.
- Converted `ForgeQueryEffectIndex` live/computed trigger candidate sets from
  raw effect-name `String` entries to `ForgeQueryEffectTarget` entries.
- Updated effect delivery routing, delivery draining, inspection, pending
  write-intent admission, pending delivery removal, and effect-triggered intent
  execution to lift public/terminal effect names into target carriers at lookup
  boundaries.
- Kept public handles, declarations, deliveries, receipts, and errors as
  ordinary effect-name reporting surfaces; the runtime authority table no
  longer uses the raw name as its key.
- Verification passed: `cargo fmt -p forge-query`,
  `cargo check -p forge-query --tests`, focused `cargo test -p forge-query
  effect --lib`, focused `cargo test -p forge-query intent_admission --lib`,
  and `cargo test -p forge-query --test aspect_native_query_compile_fail`.
- Focused scans confirmed no remaining runtime `BTreeMap<String,
  ForgeQueryEffectRuntime>`, effect trigger indexes storing `BTreeSet<String>`,
  or unlifted `self.effects.get(...)` / `get_mut(...)` lookup boundaries.
  Production `serde_json` remains confined to the two support terminal codecs.

## Query same-batch symbolic target key cleanup

- Added runtime-local `ForgeQuerySameBatchSymbolicTargetKey` for same-batch
  symbolic target planning and resolution.
- Converted authoritative batch planned/resolved symbolic target maps from
  `BTreeMap<String, ForgeQuerySameBatchSymbolicTarget>` to
  `BTreeMap<ForgeQuerySameBatchSymbolicTargetKey,
  ForgeQuerySameBatchSymbolicTarget>`.
- Converted backend atomic-batch deferred replay and preview batch
  planned/resolved symbolic maps to the same typed key carrier.
- Same-batch target resolution now lifts `ForgeQuerySymbolicTargetReference`
  into the key carrier at lookup time; terminal symbol text remains for denial
  messages and public evidence.
- Verification passed: `cargo fmt -p forge-query`,
  `cargo check -p forge-query --tests`, focused `cargo test -p forge-query
  symbolic_reference --lib`, focused `cargo test -p forge-query preview
  --lib`, focused `cargo test -p forge-query batch --lib`, and
  `cargo test -p forge-query --test aspect_native_query_compile_fail`.
- Focused scans confirmed no remaining production
  `BTreeMap<String, ForgeQuerySameBatchSymbolicTarget>`,
  `BTreeMap::<String, ForgeQuerySameBatchSymbolicTarget>`, or
  `symbolic_targets.get(reference.symbol())`. Remaining raw symbol projection
  sites are graph-composition builder/reporting and test support, making graph
  symbolic builder the next likely authority root.

## Query graph-composition symbolic declaration cleanup

- Converted `ForgeQueryGraphCompositionBuilder::declared_symbols` from
  `BTreeSet<String>` to `BTreeSet<ForgeQueryMutationSymbolIdentity>`, so
  duplicate graph symbolic declarations use the admitted mutation symbol
  identity carrier rather than raw symbol text.
- Removed the raw
  `ForgeQueryAspectMutationBuilder::build_insert_symbolic(...)` method. Public
  batch authoring still accepts symbol text at
  `ForgeQueryMutationBatchBuilder::insert_symbolic(...)`, but that boundary now
  lowers into `ForgeQuerySymbolicTargetReference` before command construction.
- Graph symbolic entity/relation declarations now call
  `build_insert_symbolic_reference(...)` with the already-admitted
  `ForgeQuerySymbolicTargetReference` instead of projecting
  `reference.symbol().to_string()` and rebuilding the reference.
- Symbolic insert command construction now validates a reference's retained
  target collection identity against the declared insert collection identity.
- Added an aspect-native compile-fail fixture proving facade callers cannot use
  the removed raw symbolic insert builder method.
- Verification passed: `cargo fmt -p forge-query`,
  `cargo check -p forge-query --tests`, `cargo test -p forge-query
  graph_composition --lib`, `cargo test -p forge-query symbolic_reference
  --lib`, `cargo test -p forge-query batch --lib`, and `cargo test -p
  forge-query --test aspect_native_query_compile_fail`.
- Focused scans confirmed no remaining production
  `declared_symbols: BTreeSet<String>`, no raw symbolic insert builder method,
  and no `reference.symbol().to_string()` symbolic insert command
  construction. Production `serde_json` remains confined to the two support
  terminal JSON codecs.

## Query materialized read target registry cleanup

- Converted `ForgeQueryRuntime::materialized_read_views` from
  `BTreeMap<String, DeclarativeLiveQueryRequest>` to
  `BTreeMap<ForgeQueryLiveArtifactTarget, DeclarativeLiveQueryRequest>`.
- Runtime read materialization still derives a backend declaration name from
  the read graph digest, but immediately lifts it into
  `ForgeQueryLiveArtifactTarget` before cache admission, cache collision
  checking, runtime registration, and backend row reads.
- Renamed the old name-availability helper to
  `ensure_materialized_read_target_available(...)` so the internal vocabulary
  matches the new authority carrier.
- Verification passed: `cargo fmt -p forge-query`,
  `cargo check -p forge-query --tests`, `cargo test -p forge-query
  read_composition --lib`, and `cargo test -p forge-query materialized_read
  --lib`.
- Focused scans confirmed no remaining production
  `materialized_read_views: BTreeMap<String, ...>`, no materialized-read cache
  lookup by `view_name`, and no old materialized-read name-availability helper.
  Production `serde_json` remains confined to the two support terminal codecs.

## Query program registry identity cleanup

- Added sealed runtime surface carriers
  `ForgeQueryProgramInstallationIdentity` and `ForgeQueryProgramRunIdentity`.
- Converted `ForgeQueryRuntime::installed_programs` from
  `BTreeMap<String, ForgeQueryProgram>` to
  `BTreeMap<ForgeQueryProgramInstallationIdentity, ForgeQueryProgram>`.
- Converted `ForgeQueryRuntime::run_traces` from
  `BTreeMap<String, ForgeQueryProgramTrace>` to
  `BTreeMap<ForgeQueryProgramRunIdentity, ForgeQueryProgramTrace>`.
- Runtime execution and preview execution now retain the typed installation
  identity inside `ForgeQueryInstalledOperation`, allocate a typed run identity,
  store traces by that carrier, and expose raw program/run text only through
  terminal accessors and diagnostics.
- Added aspect-native compile-fail fixture
  `program_runtime_receipt_raw_construction_removed.rs` proving facade callers
  cannot construct installed operations or run receipts with raw `program_id`
  and `run_id` fields.
- Verification passed: `cargo fmt -p forge-query`,
  `cargo check -p forge-query --tests`, `cargo test -p forge-query program
  --lib`, `cargo test -p forge-query preview --lib`, and `cargo test -p
  forge-query --test aspect_native_query_compile_fail`.
- Focused scans confirmed no remaining installed-program/run-trace string-map
  declarations, no program lookup by `operation.program_id`, no trace lookup by
  `run.run_id()`, and no raw public struct-literal construction path for
  program/run registry authority.

## Query backend live-view target map cleanup

- Converted `ForgeQueryInMemoryTestBackend::live_views` from
  `BTreeMap<String, ForgeQueryMutationTargetCollectionIdentity>` to
  `BTreeMap<ForgeQueryLiveArtifactTarget,
  ForgeQueryMutationTargetCollectionIdentity>`.
- Backend live reads and affected-live-view routing now use retained
  `ForgeQueryLiveArtifactTarget` keys directly instead of looking up by
  terminal view-name projection.
- Converted the adjacent src-hosted runtime test source adapter and public
  bridge runtime support state to the same target-keyed map so critical adapter
  tests stop preserving the raw live-view-name authority pattern.
- Verification passed: `cargo fmt -p forge-query`,
  `cargo check -p forge-query --tests`, `cargo test -p forge-query
  test_backend --lib`, `cargo test -p forge-query --test
  public_bridge_runtime_bootstrap`, and `cargo test -p forge-query --test
  aspect_native_query_compile_fail`.
- Focused scans confirmed no remaining `live_views: BTreeMap<String, ...>` in
  the converted backend/support roots, no live entity lookup by
  `target.terminal_view_name_projection()`, and no affected-view construction
  from raw `(name, _)` live-view map keys.

## Query read materialized row identity index cleanup

- Added private `ForgeQueryReadMaterializedRowIdentity` inside
  `runtime/read_composition_materialization.rs`.
- Converted read materialization row indexing from
  `BTreeMap<String, ForgeQueryEntity>` to
  `BTreeMap<ForgeQueryReadMaterializedRowIdentity, ForgeQueryEntity>`.
- Anchor identity lookup, relation target traversal, selected-row maps, and
  identity ordering now carry the private row identity key instead of passing
  anonymous identity strings through read control flow.
- The private carrier has no string accessor, so once identity text is admitted
  from native row fields it cannot be casually re-projected inside the read
  materializer.
- Verification passed: `cargo fmt -p forge-query`,
  `cargo check -p forge-query --tests`, `cargo test -p forge-query
  read_composition --lib`, and `cargo test -p forge-query materialized_read
  --lib`.
- Focused scans confirmed the old read-composition row-index string map and
  traversal `next_identity.to_string()` handoff are gone.

## Query causal decision trace lookup cleanup

- Added private `CausalDecisionTraceLookupKey` in
  `runtime/inspection/causal/admission_trace.rs`.
- Converted `CausalDecisionTraceIndex::lookup` from `HashMap<String, usize>` to
  `HashMap<CausalDecisionTraceLookupKey, usize>`.
- Public `row_for_key(&str)` remains the ergonomic/reporting boundary, but the
  retained trace index now lifts key text into the private carrier before map
  lookup.
- Verification passed: `cargo fmt -p forge-query`,
  `cargo check -p forge-query --tests`, `cargo test -p forge-query causal
  --lib`, and `cargo test -p forge-query inspection --lib`.
- Focused scans confirmed no remaining causal admission trace
  `HashMap<String, ...>` lookup root.

## Query program operation lookup cleanup

- Added `ForgeQueryProgramOperationIdentity`.
- Converted `ForgeQueryProgram::operations` from
  `BTreeMap<String, ForgeQueryOperation>` to
  `BTreeMap<ForgeQueryProgramOperationIdentity, ForgeQueryOperation>`.
- Converted `ForgeQueryInstalledOperation` to retain
  `ForgeQueryProgramOperationIdentity` instead of raw `operation_id` text.
- Runtime execution and preview execution now use the operation identity for
  program lookup, trace construction, error text, and run-id display
  projection.
- Strengthened `program_runtime_receipt_raw_construction_removed.rs` so facade
  callers cannot construct installed operations with raw `program_id` or raw
  `operation_id`, nor run receipts with raw `run_id`.
- Verification passed: `cargo fmt -p forge-query`,
  `cargo check -p forge-query --tests`, `cargo test -p forge-query program
  --lib`, and `cargo test -p forge-query --test
  aspect_native_query_compile_fail`.
- Remaining production `BTreeMap<String, ...>` hits now look like program value
  object grammar, value-expression object grammar, bound-input maps, and intent
  input object grammar rather than runtime registry authority.

## Query source-hosted live-view target cleanup

- Converted the runtime API stabilization transcript source adapter,
  intent-admission certification runtime source adapter, and shared stateful
  bridge runtime test support from
  `BTreeMap<String, ForgeQueryMutationTargetCollectionIdentity>` to
  `BTreeMap<ForgeQueryLiveArtifactTarget,
  ForgeQueryMutationTargetCollectionIdentity>`.
- Live-view declarations still receive public view names and return public
  handles, but the retained maps now store typed live artifact targets
  immediately.
- Affected-live-view routing now returns cloned target carriers instead of
  reconstructing targets from raw map-key names, and the stateful bridge
  runtime live read path now looks up by target carrier directly instead of
  `target.terminal_view_name_projection()`.
- Verification passed: `cargo fmt -p forge-query`,
  `cargo check -p forge-query --tests`, `cargo test -p forge-query
  runtime_api_stabilization --lib`, `cargo test -p forge-query
  intent_admission --lib`, and `cargo test -p forge-query --test
  public_bridge_runtime_bootstrap`.
- Focused scans confirmed no remaining
  `live_views: BTreeMap<String, ForgeQueryMutationTargetCollectionIdentity>`,
  no stateful bridge lookup by terminal view-name projection, and no
  affected-view reconstruction from `(name, _)` live-view map keys in the
  converted roots.

## Query bridge readmission fixture identity cleanup

- Converted the phase-six lower-runtime readmission certification fixture from
  terminal-string bridge identity maps to runtime-bridge identity maps:
  committed patches now use `TruthCommitIdentity`, branch heads map
  `TruthBranchIdentity` to `TruthCommitIdentity`, and snapshots use
  `TruthSnapshotIdentity`.
- The fixture no longer projects bridge identities through
  `bridge_admission_evidence().terminal_projection_for_reporting()` for
  internal lookup state.
- Added
  `bridge_readmission_fixtures_do_not_key_authority_by_terminal_strings` as a
  mechanical residue guard in the support snapshot runtime-boundary tests.
- Verification passed: `cargo fmt -p forge-query`,
  `cargo check -p forge-query --tests`, `cargo test -p forge-query
  bridge_readmission_fixtures_do_not_key_authority_by_terminal_strings --lib`,
  and `cargo test -p forge-query lower_runtime_routing --lib`.
- Remaining production `BTreeMap<String, ...>` hits are now mostly program
  value/object grammar, intent input object grammar, evidence/report indexes,
  and template binding indexes. They need classification, not blind conversion.

## Query production string-map residue guard

- Added
  `production_string_map_residue_is_classified_grammar_or_reporting_only` to
  the support snapshot runtime-boundary tests.
- The guard scans production Query source for `BTreeMap<String` and
  `HashMap<String` and allows only:
  `composition/templates/instantiation.rs`,
  `consumer_kit/evidence_report/report.rs`,
  `consumer_kit/evidence_report_adoption/syntax.rs`, `program.rs`, and
  `runtime/intent/input.rs`.
- These allowed roots are classified as template grammar, report/adoption
  reporting indexes, program value/bound-input grammar, and intent input object
  grammar. They are not approved aspect authority storage.
- Verification passed: `cargo fmt -p forge-query`,
  `cargo check -p forge-query --tests`, `cargo test -p forge-query
  production_string_map_residue_is_classified_grammar_or_reporting_only
  --lib`, and `cargo test -p forge-query
  production_serde_json_is_confined_to_support_terminal_codecs --lib`.

## Query graph obligation digest-string projection guard

- Audited graph-obligation index/selection digest-string usage. Actual
  selection already uses native `ForgeQueryGraphObligationTouchLookupKey`
  buckets with `ForgeQueryAspectTouch` /
  `ForgeQueryAspectMutationOperation` carriers rather than rendered touch
  digest strings.
- Renamed the lookup-key string projection helper from `value()` to
  `terminal_value_projection()` and the public index-entry accessor from
  `touch_key_value()` to `terminal_touch_key_value_projection()`.
- Added
  `graph_obligation_index_does_not_expose_touch_digest_as_lookup_key_value`
  to mechanically guard against exposing rendered touch digest text as a
  lookup-key-shaped API.
- Verification passed: `cargo fmt -p forge-query`,
  `cargo check -p forge-query --tests`, `cargo test -p forge-query
  graph_obligation_index_does_not_expose_touch_digest_as_lookup_key_value
  --lib`, and `cargo test -p forge-query graph_obligation --lib`.

## Query write receipt touch digest projection guard

- Audited write-receipt replay outcome touch digest rendering. It feeds journal
  replay evidence construction only, not routing, matching, or mutation
  authority.
- Renamed `touched_aspect_digest_parts(...)` to
  `terminal_touched_aspect_digest_projections(...)`.
- Added
  `write_receipt_touch_digest_helpers_are_terminal_projections_only` to guard
  against returning to ambiguous authority-looking helper names.
- Verification passed: `cargo fmt -p forge-query`,
  `cargo check -p forge-query --tests`, `cargo test -p forge-query
  write_receipt_touch_digest_helpers_are_terminal_projections_only --lib`, and
  `cargo test -p forge-query write_receipt --lib`.

## Query batch receipt touch digest projection guard

- Audited batch receipt and unified batch write inspection digest helpers.
  They render admitted aspect touches only for batch receipt / inspection
  evidence identities, not routing, matching, or mutation authority.
- Renamed `evidence_touch_identities(...)` helpers to
  `terminal_touch_projection_identities(...)` in batch receipt identity,
  unified batch write digest, and unified batch write digest component helpers.
- Updated batch receipt construction to call the terminal-projection helper for
  admitted touched aspects.
- Added
  `batch_receipt_touch_digest_helpers_are_terminal_projections_only` as the
  mechanical guard.
- Verification passed: `cargo fmt -p forge-query`,
  `cargo check -p forge-query --tests`, `cargo test -p forge-query
  batch_receipt_touch_digest_helpers_are_terminal_projections_only --lib`, and
  `cargo test -p forge-query batch --lib`.

## Query computed/preview/verified/effect touch digest projection guard

- Audited remaining computed inspection, preview execution evidence,
  verified-assumption summaries, graph-composition assumption summaries, and
  effect delivery payload digest material.
- These roots retain native `ForgeQueryAspectTouch` values and render touch
  digest text only for evidence identity / terminal digest material.
- Renamed effect helper `terminal_touch_digest_sequence(...)` to
  `terminal_touch_digest_projection_sequence(...)`.
- Added
  `summary_and_effect_touch_digest_rendering_stays_terminal_evidence_only` as
  a mechanical guard against returning to ambiguous effect helper naming or
  rendering touch digests outside evidence identity construction in the guarded
  summary files.
- Verification passed: `cargo fmt -p forge-query`,
  `cargo check -p forge-query --tests`, `cargo test -p forge-query
  summary_and_effect_touch_digest_rendering_stays_terminal_evidence_only
  --lib`, `cargo test -p forge-query effect --lib`, and `cargo test -p
  forge-query computed --lib`.

## Query aspect-native closeout status checkpoint

- Full `cargo test -p forge-query` initially found one real concentrated
  fixture bug: declaration aspect test keys still split at the first dot, while
  foundational aspect keys may contain dotted namespaces and field keys may not
  contain dots. Fixed
  `crates/forge-query/src/application/declaration_aspect_test_support.rs` to
  split at the last dot.
- Focused verification for the formerly failing runtime clusters is green:
  `cargo test -p forge-query application::declaration_bridge_routing --lib`,
  `cargo test -p forge-query application::declaration_relational_routing
  --lib`, `cargo test -p forge-query
  application::declaration_signal_compatibility --lib`, `cargo test -p
  forge-query
  binding_pipeline::tests::continuation_binding_reports_authority_mismatch_when_contract_requires_hidden_bridge_slice
  --lib`, and `cargo test -p forge-query signal_compatibility_orchestration
  --lib`.
- Refreshed stale trybuild expectations where stronger native-boundary
  compiler errors surfaced:
  `phase_boundaries_bridge_truth_identity_compile_fail`,
  `phase_boundaries_compile_fail`, and
  `phase_boundaries_domain_capabilities_compile_fail`.
- Current green checks: `cargo check -p forge-query --tests`, `cargo test -p
  forge-query runtime_boundary --lib`, and `cargo test -p forge-query --test
  phase_boundaries_domain_capabilities_compile_fail`.
- Continued full-suite closeout exposed additional stale compile-fail stderr
  snapshots where compiler errors now prove stronger native carrier boundaries:
  graph-read admitted relation requires `RelationName`, runtime receipts reject
  removed mutation-delta `collection`, and prohibition-registry existing-truth
  probe now requires `ForgeQueryAspectTouch`.
- Refreshed and verified:
  `phase_boundaries_graph_read_access_compile_fail`,
  `phase_boundaries_runtime_receipts_compile_fail`, and
  `prohibition_registry_compile_fail`.
- Final full-suite verification passed: `cargo test -p forge-query`.
- Current residue scan evidence: JSON compatibility terms in Query are confined
  to runtime-boundary guard source and hostile aspect-native compile-fail
  fixtures; broad string-map residue is covered by the production string-map
  runtime-boundary classification guard.

## Worth touched graph milestone 6 phase 2 QA hardening

- QA loop found three real Phase 2 inventory-lane gaps: Milestone 5 seed
  digest families only proved non-empty instead of count parity, capped residue
  rows defaulted missing current count to zero, and inventory rows allowed
  contradictory classification/action/disposition combinations.
- Hardened the parallel inventory substrate so seed digest family lengths must
  match selected obligation/registration counts, capped residue current count
  is explicit evidence, and each row classification has exactly one legal
  deletion action plus Milestone 7 disposition contract.
- Added focused negative tests for seed count mismatches, missing residue
  current count, and classification contract mismatches. `cargo fmt -p
  worth-kernel`, `git diff --check`, and line-count checks passed. Focused
  `cargo test -p worth-kernel inventory_lane --lib` remains blocked before
  reaching `worth-kernel` because `worth-topo` is stale against the current
  `forge-query` API.

## Worth touched graph milestone 6 phase 2 test-QA correction

- Implemented the qa-tests hardening plan for the Phase 2 graph-read inventory
  lane. Public row builder exports and direct public closeout-from-rows
  construction were removed; closeout now flows through a lane-owned inventory
  collector that admits rows and rejects duplicate `(source_path, owner,
  current_caller)` identities.
- Added out-of-scope reason typing so out-of-scope rows must carry an explicit
  non-graph-read reason and `NoGraphTraversal` cost posture. Added explicit
  denial surfaces for fabricated graph-read receipt proof and local support
  row proof, plus a residue growth policy that rejects implicit cap growth.
- Added hostile hardening tests for duplicate row identities, fabricated
  receipt/support proof denial, out-of-scope reason/cost validation, and
  residue cap growth. Added public-facade compile-fail fixtures proving callers
  cannot access the row builder or direct old closeout constructor.
- Verification passed: `cargo fmt -p worth-kernel`, `git diff --check`
  (CRLF warnings only), and graph-read inventory line-count checks. Focused
  `cargo test -p worth-kernel inventory_lane --lib` remains blocked before
  reaching `worth-kernel` by the existing `worth-topo` / `forge-query` API
  drift.

## Worth touched graph milestone 6 phase 2 code-quality QA

- Code-quality QA found the new `inventory_lane` production folder was becoming
  a flat many-file directory: row contracts, residue policy, closeout
  admission, seed admission, and proof denial all lived as peers. Reorganized
  it into responsibility folders: `row/`, `residue/`, and `closeout/`, with
  lane-level `seed.rs`, `inventory_error.rs`, and `proof_input.rs`.
- Renamed moved files by their local responsibility rather than repeating the
  parent context (`row/classification.rs`, `row/cost_posture.rs`,
  `closeout/summary.rs`, `closeout/collector.rs`, `residue/growth_policy.rs`).
- Verification passed: `cargo fmt -p worth-kernel`, `git diff --check`
  (CRLF warnings only), inventory file line-cap scan, and stale-module-name
  scan. Focused `cargo test -p worth-kernel inventory_lane --lib` remains
  blocked before `worth-kernel` by the existing `worth-topo` / `forge-query`
  API drift.

## Worth touched graph milestone 6 phase 5 bypass audit implementation

- Added the Phase 5 Consumer Kit bypass audit lane under
  `graph_read_access_inventory/inventory_lane/bypass_audit`. The inventory
  closeout now attempts Query-owned graph-read bypass audit/adoption
  certification before it can close, and exposes a Worth projection of the
  Query adoption proof.
- Added a Query source-inventory bridge that derives required audit roots from
  admitted graph-read inventory rows, resolving catalog paths from the
  workspace root and deduplicating source roots before calling
  `query_boundary_source_inventory`.
- Added a Consumer Kit residue-manifest bridge with explicit Worth caps for the
  currently observed graph-read bypass classes. Current counts come from the
  Query audit report; any count above the cap fails with
  `ResidueGrowthRequiresCapUpdate`.
- Added Phase 5 tests for old-adoption parity, residue cap growth rejection,
  and Consumer Kit audit coverage of every inventory row root. `cargo fmt -p
  worth-kernel` and inventory line-cap scans passed. Focused worth-kernel tests
  remain blocked before reaching `worth-kernel` by the existing stale
  `worth-topo` / `forge-query` API drift.

## Worth touched graph milestone 6 phase 5 QA correction

- QA found the Phase 5 bypass residue rollover was too digest-heavy: closeout
  exposed the Consumer Kit residue manifest digest and certified finding count,
  but not the row-level owner/cap/blocker/removal-trigger ledger. Added
  structured `WorthGraphReadBypassResidueReportRow` projection from Query
  residue rows and strengthened tests to assert visible row fields, exact
  class coverage, current-count sum parity with Query finding count, and cap
  bounds.
- Verification passed: `cargo fmt -p worth-kernel`, graph-read inventory
  line-cap scan, and residue scans for local source-grep/empty-manifest
  shortcuts. Focused `cargo test -p worth-kernel graph_read_access_inventory
  --lib` remains blocked before reaching `worth-kernel` by stale
  `worth-topo` / current `forge-query` API drift.

## Worth touched graph milestone 6 phase 5 test-QA hardening

- Hardened the Phase 5 bypass tests and production report surfaces so root
  coverage is exposed as structured required-root coverage rows derived from
  Query's sealed source inventory instead of only `covered_roots` strings.
- Made the Worth graph-read bypass residue cap ledger an explicit
  inventory-lane surface with owner, introduced-in, cap, blocker, and removal
  trigger fields; bypass tests now assert residue rows against that ledger
  instead of hand-coded class strings.
- Replaced the bypass growth test's synthetic numeric policy proof with a real
  Query `graph_read_bypass_audit` report whose findings exceed the Worth cap,
  then verified the Worth manifest bridge rejects it with
  `ResidueGrowthRequiresCapUpdate`. The older numeric growth-policy test
  remains as a small unit check for the policy object.
- Worth inventory errors can now preserve Query error messages for boundary
  audit, residue manifest, and bypass adoption failures. Verification passed:
  `cargo fmt -p worth-kernel`, `git diff --check` (CRLF warnings only),
  graph-read inventory line-cap scan, and focused residue scans. Focused
  `cargo test -p worth-kernel
  graph_read_residue_manifest_cannot_grow_without_cap_update --lib` remains
  blocked before reaching `worth-kernel` by stale `worth-topo` /
  current `forge-query` API drift.
- Code-quality QA then split broad Phase 5 files by structural responsibility:
  `bypass_audit/report.rs` became adoption report, required-root coverage, and
  residue-row projection files; `residue/consumer_kit_manifest.rs` now only
  builds the Query manifest while the explicit bypass cap ledger lives in its
  own inventory file.

## Worth touched graph milestone 6 phase 6 ledger

- Implemented Phase 6 graph-read ledger surfaces in parallel folders:
  declaration candidates, Query access capability gaps, deletion-only items,
  and Phase 6 closeout/seed projection. The ledger rejects any keep-local
  graph-read disposition and requires every required inventory classification
  to become a candidate, gap, or deletion item before Milestone 7 can consume it.
- Public boundary hardening now routes graph-read inventory compile-fail
  fixtures through a dedicated fixture catalog. Verification passed:
  `cargo fmt -p worth-kernel`, `git diff --check` (CRLF warnings only), and
  touched-file line-cap scan. Focused Phase 6 tests remain blocked before
  reaching `worth-kernel` by stale `worth-topo` / current `forge-query` API
  drift.

## Worth touched graph milestone 6 phase 6 test-QA correction

- Replaced the Phase 6 manual fixture closeout proof with a production
  `WorthGraphReadAccessPhaseSixCloseout::from_inventory(...)` converter backed
  by a typed disposition planner. The converter lowers the real current
  inventory catalog into five declaration candidates, two Query capability
  gaps, one deletion item, and four certification-only exclusions.
- Added row classification to the Phase 6 row context so closeout/seed
  invariants can distinguish honest capped residue from uncapped old
  graph-read folklore pretending to be a declaration or capability gap.
- Added converter tests proving real row-context preservation, broad/frontier
  spatial reads become persistent-continuation Query gaps, and the old
  `query_adoption/graph_read_access` path stays deletion-only. Added
  compile-fail fixtures for private Phase 6 module access, private closeout and
  seed constructors, and private row-context fields.
- Verification passed: `cargo fmt -p worth-kernel`, `git diff --check` (CRLF
  warnings only), and Phase 6 line-cap scans. `cargo check -p worth-kernel
  --tests` and focused `cargo test -p worth-kernel phase_six_converter --lib`
  remain blocked before reaching `worth-kernel` by the known stale
  `worth-topo` / current `forge-query` API drift.

## Worth touched graph milestone 6 phase 7 hard delete

- Implemented the Phase 7 graph-read adoption hard rollover. The old
  `crates/worth-kernel/src/query_adoption/graph_read_access` module files were
  deleted and `query_adoption` no longer publicly exports
  `current_worth_kernel_construction_graph_read_access_adoption`,
  `WorthKernelGraphReadAccessAdoptionReport`, or
  `WorthKernelGraphReadAccessAdoptionError`.
- Reworked the new inventory bypass rollover test so it no longer uses the old
  adoption report as a parity witness. The new lane now checks Consumer
  Kit-backed bypass coverage, explicit residue rows, and cap enforcement
  directly.
- Added graph-read access public-firewall compile-fail fixtures proving public
  callers cannot import the old graph-read adoption module, function, report
  types, or a compatibility bridge from old adoption digests into the Phase 6
  closeout.
- Added a test-only production source firewall under
  `graph_read_access_inventory` that rejects reintroduced old graph-read
  adoption authority patterns while allowing explicit deletion-target source
  paths in the new inventory.
- Verification passed: `cargo fmt -p worth-kernel`, `git diff --check` (CRLF
  warnings only), source scans confirming the old module path is gone, and
  line-cap checks. Focused `cargo test -p worth-kernel ... --lib` commands
  still stop before reaching `worth-kernel` because `worth-topo` is stale
  against current `forge-query`.
- QA tightened Phase 7 after hostile review: the public firewall now asserts the
  deleted `query_adoption/graph_read_access` directory is absent, scans
  non-compile-fail test/support source for old authority names, bans the removed
  `OldGraphReadAdoption`/`old_graph_read_adoption` production vocabulary, and
  the compile-fail fixture catalog now uses deleted-source naming rather than
  preserving old-adoption language in production source.
