# 2026-06-23

## Worth UI hot-reload and query-graph work

- Worth UI Phase 32 cleanup: mounted interaction activation now carries a
  Query-backed graph execution receipt through `runtime/query_graph`, with
  Worth semantic labels mapped onto Query canonical obligation kinds and
  Consumer Kit adoption proof coverage. Added residue audit tests so local
  `WorthUiGraphObligationRegistry::select` rows cannot masquerade as final
  Phase 32 proof.
- Worth UI Phase 33 cleanup: primitive event dispatch now uses Query-backed
  graph execution receipts and typed dispatch outcomes/candidates instead of
  boolean event bags. Cursor arbitration has a distinct cursor-target
  candidate, disabled hits cannot emit, and validation rendering asks the event
  dispatch plan for appearance observation instead of doing local hit-test
  posture decisions.
- Phase 33 QA tightened the event proof path: removed public `cursor_at`/raw
  event `hit_test` bypasses from renderer-facing use, classified disabled hover
  as `DisabledHit` evidence rather than ordinary cursor target, made primitive
  event Query rows posture-aware instead of all-supported, and made pointer
  capture consume typed activation outcome proof before checking capture
  policy.
- Worth UI query graph cleanup: removed the Worth-side
  `WorthUiQueryGraphSupportPosture` decision table. Touch descriptors now carry
  declared aspect operation facts derived from already-proven Worth posture,
  Query registrations are static operation-selector rows, and execution
  receipts no longer accept basis/posture inputs to preselect support.
- Worth UI Phase 34.5 implementation: added a runtime `user_intent_target`
  lane with sealed target-binding receipts, target-binding graph facts/Query
  execution, semantic-slice mapping, visible page-slot binding for the centered
  primitive proof, target-bound mounted interaction planning, and target
  evidence rows. The exact `button.proof` disabled while `primitive.proof`
  mounted bug is now covered by focused validation tests. Remaining cleanup:
  raw public compatibility shims (`resolve_primitive_proof`, raw mounted
  interaction plan) still exist for older tests/helpers and should be
  quarantined or removed in the next hardening pass.
- Worth UI Phase 34.5 QA hardening: sealed raw primitive proof/projection, raw
  mounted-interaction planning, and raw event point cursor/dispatch behind
  target-bound APIs; added compile-fail fixtures for those bypasses; split
  user_intent_target, primitive resolver helpers, event dispatch helpers, and
  validation workbench interaction application to keep touched files under line
  cap. Verified focused worth-ui and validation-app checks/tests.
- Worth UI Phase 34.2 implementation: added live-view state binding as a
  runtime-owned, Query-backed lane for binding UI controls to runtime state
  facts without a form abstraction. The lane has live-view
  declaration/binding/value fact families, target-bound sealed receipts,
  batched admission denials, runtime-owned state mutation through sealed edit
  intents, classified durable state changes, Query graph adoption proof,
  validation-app text-control proof rendering, and compile-fail guards so app
  code cannot mint declaration receipts or edit intents directly. Also split
  the live-view contract, app frame update loop, native-boundary marker tables,
  and query-graph operation catalog to keep touched Rust files under the
  400-line cap. Verified `cargo check -p worth-ui`,
  `cargo check -p worth-ui-validation-app`,
  `cargo test -p worth-ui-validation-app --test live_view_state_binding_proof`,
  `cargo test -p worth-ui-validation-app --test native_boundary`,
  `cargo test -p worth-ui --test query_graph_adoption`, and
  `cargo test -p worth-ui --test runtime_authority_facade_compile`.
- Worth UI Phase 34.2 QA hardening: stale live-view target bindings now reject
  at declaration admission and edit-application time before state mutation,
  using one canonical live-view denial/report path. Added a sealed declaration
  rebind receipt that proves exact changed live-view binding facts for
  identity/fact edits, evidence rows for target-binding/state-fact/value-kind/
  binding digests, and compile-fail coverage that app code cannot mint rebind
  receipts. Strengthened the denial proof so stale target, invalid binding id,
  unsupported value kind, unsupported write posture, and duplicate binding all
  batch in canonical declaration order.
- Worth UI Phase 34.2 test-hardening cleanup: the first authored live-view
  source pass initially put data types, parsing, admission, lowering, and
  token classification in one `authoring.rs` file. Code-quality QA split it
  into `runtime/live_view/authoring/{document,parse,admission,lowering,tokens}.rs`
  so the authored boundary teaches the semantic steps instead of becoming
  another local folklore file.
- Worth UI Phase 34.3 implementation: added graph-backed live-view control
  projection and conditional projection lanes over Phase 34.2 runtime state
  bindings. Text input and select controls now admit as sealed projection
  receipts, conditional participation derives from runtime state facts while
  retaining hidden state, validation rendering consumes only admitted
  participating controls, unsupported option sources deny through the control
  admission report, and compile-fail fixtures prove app code cannot mint
  control/conditional projection receipts.
- Worth UI Phase 34.3 QA hardening: added a combined source-order-aware
  live-view projection admission receipt/report, preserved authored control/
  condition declaration order for diagnostics, replaced the always-preserved
  compatibility stub with per-control compatibility rows, sealed participation
  receipts with compile-fail coverage, added graph execution tests for
  control/conditional projection families, proved `yes` -> non-`yes` removes
  layout/event/a11y participation while retaining state, and moved live-view
  source parsing out of the validation frame path via a prepared document
  cache.
- Worth UI Phase 34.4 QA hardening: readiness receipts now carry the active
  user-intent target binding, live-view submissions reject stale target
  bindings before payload emission, payload projections consume only
  readiness-relevant state bindings, and interaction effect support now
  participates in Query graph operation rows with `QueryEffectPosture` facts
  instead of bypassing graph proof. Verified Phase 34.4 behavior, Query
  adoption, facade compile-fail sealing, and validation-app compile.
- Worth UI live-view control hardening: text/dropdown inputs now render through
  sealed `WorthUiLiveViewControlHostFrameReceipt`s with primitive subject
  evidence, style/frame fields, consumed primitive facts, and compile-fail
  sealing. Validation egui rendering now adapts the host frame instead of raw
  control receipts. Remaining honest gap before the full form scenario:
  authored control-level primitive recipe bindings still need to feed those
  host-frame style fields for arbitrary hot-reloadable input styling.
- Worth UI mounted product boundary: added sealed
  `WorthUiMountedProductViewReceipt` as the product-rendering authority over
  live-view mounted nodes. It carries semantic slice, consumed facts, graph
  obligation execution digests, zero source/renderer parse counters, and node
  count. Validation rendering now enters through `mounted_product_view()`,
  app-side guards reject raw `mount_live_view_projection`/
  `WorthUiMountedViewReceipt`, and compile-fail coverage proves app code cannot
  mint mounted product receipts.
- Worth UI mounted evidence migration: live-view projection/control/conditional/
  readiness/payload/interaction evidence rows now live on
  `WorthUiMountedEvidenceNodeReceipt` as sealed
  `WorthUiMountedEvidenceRowReceipt`s derived from runtime projection receipts.
  The validation renderer displays mounted evidence rows instead of locally
  formatting projection meaning. Remaining local evidence gap: last
  edit/submission observations and denial coloring are still adapter-side
  display work and should be moved into typed mounted observation/diagnostic
  nodes next.
- Worth UI mounted observation migration: live-view edit receipts, edit
  denials, submission receipts, activation denials, and projection/source
  denial rows now flow through `mount_live_view_observation_evidence` and
  `WorthUiMountedEvidenceNodeReceipt` instead of renderer-local
  `format!("{denial:?}")`/colored-label handling. Added validation guards so
  live-view edit/submission denials stay typed until Worth UI mounts
  presentation rows. Remaining migration islands: launch-error screen, header
  adapter, top-level central panel/page shell, and lower-fidelity source-denial
  strings need first-class mounted diagnostic/product receipts.

## Query memory workspace native field path vocabulary

- Renamed memory-workspace retained aspect state from
  `external_projection_path` to `native_field_path`, including
  `ForgeQueryAspect::native_field_path()` and the crate-local
  `ForgeQueryAspect::from_native_field_path(...)`.
- Converted test-backend schema storage and duplicate-path validation to
  `native_field_path`; the public authoring helper still accepts projection
  field path text but immediately lowers it to `CanonicalFieldPath`.
- Refreshed compile-fail coverage so the removed
  `external_projection_path()` accessor fails directly, while
  `native_field_path()` and `schema.aspects()` paths still cannot be treated as
  `&str`.
- Verification passed: `cargo fmt -p forge-query`,
  `cargo check -p forge-query --tests`,
  `cargo test -p forge-query --test aspect_native_query_compile_fail`,
  `cargo test -p forge-query memory_workspace --lib`,
  `cargo test -p forge-query test_backend --lib`, and residue scans showing
  production no longer contains `external_projection_path`,
  `from_native_external_projection_path`, or `external projection path`.

## Query projection consumption aspect-key extraction

- Changed query-read result row extraction so aspect values reach projection
  fact field-path construction as `AspectKey` carriers instead of being
  weakened to local `aspect_path` strings first.
- Replaced private
  `projection_fact_field_path_from_aspect_label(&str)` with
  `projection_fact_field_path_from_aspect_key(&AspectKey)`.
- Verification passed: `cargo fmt -p forge-query`,
  `cargo check -p forge-query --tests`,
  `cargo test -p forge-query projection_consumption --lib`, and a targeted
  scan showing the removed helper/name residue is gone.

## Query mutation metadata admitted-key storage

- Changed `ForgeQueryMutationMetadata` from raw
  `BTreeMap<String, ForgeQueryMutationMetadataValue>` storage to
  `BTreeMap<ForgeQueryMutationMetadataKey, ForgeQueryMutationMetadataValue>`.
- Duplicate detection, typed lookup, and typed iteration now preserve the
  admitted metadata key proof internally, projecting key text only for denial
  messages.
- Verification passed: `cargo fmt -p forge-query`,
  `cargo check -p forge-query --tests`,
  `cargo test -p forge-query mutation_metadata --lib`,
  `cargo test -p forge-query write_receipt_inspection_retains_authored_mutation_metadata --lib`,
  `cargo test -p forge-query --test aspect_native_query_compile_fail`, and a
  scan showing the raw metadata map shape removed from production.

## Query public bridge existing-truth native key harness

- Replaced public bridge runtime test harness existing-truth map keys from raw
  `(String, String, String)` tuples to `PublicExistingTruthKey`, retaining the
  binding digest, target collection projection, and admitted
  `ForgeQueryAspectTouch`.
- Changed `seed_backend_authoritative_truth(...)` to accept
  `ForgeQueryAspectTouch` instead of raw aspect-path text; graph-composition
  and public bridge bootstrap tests now pass native `touch(...)` fixtures.
- Verification passed: `cargo fmt -p forge-query`,
  `cargo check -p forge-query --tests`,
  `cargo test -p forge-query --test public_bridge_runtime_bootstrap`,
  `cargo test -p forge-query --test graph_composition_public_bridge`,
  `cargo test -p forge-query --test graph_composition_public_bridge_existing`,
  and residue scans showing the old tuple-string existing-truth key and raw-
  string seed calls are gone.

## Query runtime test existing-truth native key adapter

- Replaced `TestExistingTruthVerificationAdapter` storage keys from raw
  `(String, String, String)` tuples to `TestExistingTruthKey`, retaining the
  binding digest, target collection projection, and admitted
  `ForgeQueryAspectTouch`.
- Kept `.with_value(binding, touch_fixture, value)` as fixture ergonomics, but
  storage now keeps the lowered touch proof instead of
  `admitted_touch_digest_part()` text.
- Verification passed: `cargo fmt -p forge-query`,
  `cargo check -p forge-query --tests`,
  `cargo test -p forge-query bridge_backed_verification_execution --lib`,
  `cargo test -p forge-query graph_composition_verified_existing --lib`,
  `cargo test -p forge-query graph_composition_edge_split --lib`, and scans
  showing the old tuple-key shape removed from the adapter.

## Query public bridge external-row native touch paths

- Removed the public bridge runtime external-row writer's
  `ForgeQueryAspectTouch -> terminal dotted string -> CanonicalFieldPath`
  round trip.
- `apply_aspects_to_external_row(...)` now sets and clears row values through
  admitted `ForgeQueryAspectTouch` carriers, deriving canonical external row
  fields from `native_aspect_key()` plus the optional native field path.
- Verification passed: `cargo fmt -p forge-query`,
  `cargo check -p forge-query --tests`,
  `cargo test -p forge-query --test public_bridge_runtime_bootstrap`,
  `cargo test -p forge-query --test graph_composition_public_bridge`,
  `cargo test -p forge-query --test graph_composition_public_bridge_existing`,
  `cargo test -p forge-query --test public_submission_lane_replacements`, and a
  targeted scan showing terminal path projection/string-path helpers gone from
  `tests/support/public_bridge_runtime/external_row.rs`.

## Query intent-admission certification probe native matching

- Removed semantic branching on `admitted_touch_digest_part()` from
  `src/intent_admission/certification/fixtures/runtime.rs`.
- The certification runtime existing-truth probe now matches by native aspect
  key and native field path, so the fixture uses admitted touch structure as
  authority instead of formatted `identity:id` / `title:value` strings.
- Verification passed: `cargo fmt -p forge-query`,
  `cargo check -p forge-query --tests`,
  `cargo test -p forge-query intent_admission --lib`,
  `cargo test -p forge-query --test intent_admission_public_dx`, and a scan
  showing `admitted_touch_digest_part()` gone from the fixture.

## Query graph-read predicate admission native lookup key

- Replaced boolean predicate admission's raw
  `BTreeMap<(String, String, String), ...>` lookup key with
  `AdmittedPredicateKey`, retaining `AspectKey`, `FieldKey`, and predicate
  family.
- Declarative predicate filters now admit into the same typed key before
  lookup instead of comparing anonymous aspect/field/family string triples.
- Verification passed: `cargo fmt -p forge-query`,
  `cargo check -p forge-query --tests`,
  `cargo test -p forge-query --test graph_read_access_phase_four_requirements`,
  `cargo test -p forge-query --test graph_read_access_phase_six_admission`,
  `cargo test -p forge-query --test graph_read_access_phase_two_selectivity`,
  `cargo test -p forge-query --test graph_read_access_phase_four_operator_matrix`,
  and a scan showing the raw tuple predicate lookup shape removed.

## Query effect intent input native aspect-touch value

- Added private `ForgeQueryIntentInputValue::AspectTouch` so effect payload
  intent inputs retain native `ForgeQueryAspectTouch` carriers in
  `changed_aspects`, `input_aspects`, and `output_aspects`.
- Digest material still serializes touches at the final digest boundary, but
  pre-digest intent input no longer stores those touches as generic strings.
- Verification passed: `cargo fmt -p forge-query`,
  `cargo check -p forge-query --tests`,
  `cargo test -p forge-query intent_admission --lib`,
  `cargo test -p forge-query effect --lib`, and a scan showing the old string
  touch-array helper removed.

## Query batch touched-aspect native dedupe

- Replaced digest-string keyed touched-aspect dedupe maps with native
  `BTreeSet<ForgeQueryAspectTouch>` in batch write execution, batch receipt
  aggregate derivation, and unified batch component inspection.
- Touched-aspect uniqueness now uses the admitted touch carrier directly
  instead of `admitted_touch_digest_part()` string projections.
- Verification passed: `cargo fmt -p forge-query`,
  `cargo check -p forge-query --tests`,
  `cargo test -p forge-query batch --lib`,
  `cargo test -p forge-query inspection --lib`,
  `cargo test -p forge-query --test public_submission_lane_replacements`, and a
  scan showing digest-keyed touched-aspect insertions removed from the
  converted runtime files.

## Query preview affected-target native routing maps

- Replaced preview session affected-aspect propagation maps from
  `BTreeMap<String, Vec<ForgeQueryAspectTouch>>` to maps keyed by
  `ForgeQueryLiveArtifactTarget` and `ForgeQueryDerivedMaterializationTarget`.
- Internal preview live/computed/effect routing now indexes changed aspect
  touches by typed Query artifact targets; public evidence still reports
  terminal handle names.
- Verification passed: `cargo fmt -p forge-query`,
  `cargo check -p forge-query --tests`,
  `cargo test -p forge-query preview --lib`,
  `cargo test -p forge-query effect --lib`,
  `cargo test -p forge-query --test public_submission_lane_replacements`, and a
  scan showing the old preview string-map routing shape removed.

## Query focused-inspector denial native aspect retention

- Changed `src/view_shape_live/execution.rs` so `focus_projection(...)`
  returns rejected widening aspects as `AspectKey` carriers instead of
  `Vec<String>`.
- The focused-inspector denial still renders terminal aspect text in the human
  message, but the denial decision now retains native aspect identity until
  that boundary.
- Verification passed: `cargo fmt -p forge-query`,
  `cargo check -p forge-query --tests`,
  `cargo test -p forge-query focused_inspector_widening_is_denied_and_counted --lib`,
  and a targeted scan showing
  `native_aspect_key().as_str().to_string()` removed from
  `src/view_shape_live/execution.rs`.

## Query retained evidence admitted-aspect-touch vocabulary

- Renamed production evidence identity tags that still called admitted touch
  material `aspect_path` / `aspect_paths`.
