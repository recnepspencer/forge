## Worth Milestone 7.5 Phase 16 Closeout

- Manually drove Phase 16 to closure after the runner stalled.
- Closed the source-only overlap-region proof path so recovered overlap-chain
  lineage without loop/island participation can still lower through honest
  source-loop witnesses, source-only arrangement topology, shared-area
  admission, pre-region normalization, candidate promotion, identity/ledger,
  and summum-bonum closeout.
- Updated `_docs/worth/milestone-7.5.md` from Draft to Closed and added
  closeout evidence. Updated `_docs/worth/milestone-7-roadmap.md` so 7.5 is
  marked closed and 7.6+ must consume `PlanarBooleanOverlapRegionLedgerReceipt`
  without reopening touched graph, selected route, Query posture, loops,
  islands, cells, or scans.
- Verified closeout with:
  - `cargo check -p worth-spatial -p worth-kernel`
  - `cargo test -p worth-spatial region_candidate --lib -- --nocapture`
  - `cargo test -p worth-spatial pre_region_normalization --lib -- --nocapture`
  - `cargo test -p worth-kernel overlap_closeout -- --nocapture`
  - `cargo test -p worth-kernel summum_bonum -- --nocapture`
  - `cargo test -p worth-kernel planar_boolean_overlap_region_metaboss_is_canonical_replayable_area_honest_and_unWORTHable -- --nocapture`
- Broader `overlap_closeout` QA exposed a real source-only pre-region
  normalization gap: source-only full-span evidence carried subordinate
  start/end roles for the same source loop. Fixed normalization so full-span
  source-only evidence is the stronger semantic fact and collapses boundary
  roles to `FullOverlapSpan`, while island-backed local start/end ambiguity
  still denies.
- Removed scratch verification logs and confirmed changed Rust files are under
  the 400-line cap.

## Worth Milestone 7.6 Runner Setup

- Added `automation/phase_runner/config/worth-milestone-7-6-fragment-classification.json`
  as the 7.6 durable runner config, mirroring the 7.5 runner shape with 21
  operational phases from `_docs/worth/milestone-7.6.md`.
- Kept `gpt-5.4` with `reasoning_effort: medium`, but intentionally omitted
  the 7.5 fast-mode config keys (`service_tier: fast` and
  `features.fast_mode: true`) so the runner uses regular-speed 5.4 medium.
- Validated with `python automation/phase_runner/runner.py validate
  automation/phase_runner/config/worth-milestone-7-6-fragment-classification.json`
  and scanned the new config for fast-mode residue.

## Worth 7.5 / 7.6 Code Quality Cleanup

- Cleaned the 7.5 overlap-region substrate before 7.6 starts building on it:
  named the source-only full-span pre-region normalization case, split overlap
  ledger assembly into explicit sorted-row and row-building stages, split hostile
  arrangement fixtures out of the oversized general fixture file, and decomposed
  kernel replay authority into lifecycle-shaped helper stages.
- Added semantic topology overlap-readiness aliases while keeping the existing
  milestone 7.5 names as compatibility exports. Split the large topology facade
  validator exports into `facade_validator_invariant_exports.rs` so the touched
  facade stays under the 400-line cap.
- Added lifecycle-oriented spatial facade groupings for legacy compatibility,
  stage evidence, ledger authority, and closeout authority without breaking flat
  exports.
- Completed the follow-on structural cleanup in the "fully committed cleanup"
  spirit:
  - Split selected-route packet construction, accessors, conflict-family
    identities, proof-chain lowering, and test overrides into child modules.
  - Split ordinary-consumer cutover current loading, inventory lowering, model
    accessors, row behavior, witness behavior, and test support into child
    modules.
  - Split spatial rebinding certification tests by behavior so each hostile
    guarantee is named and local.
  - Split topo query workflow trait-method implementation into declaration/
    route, signal compatibility, continuation, and grouped/contribution macro
    modules, leaving `handle_impl.rs` as a table of contents.
- Important lesson: source-firewall/inventory scanners validate source paths,
  so moving selected-route compatibility accessors also requires moving the
  corresponding conflict-batch admission inventory row paths to
  `selected_route/packet/accessors.rs`.
- Verification after the split:
  - `cargo test -p worth-kernel selected_route --lib`
  - `cargo test -p worth-kernel ordinary_consumer_cutover --lib`
  - `cargo test -p worth-spatial rebinding --lib`
  - `cargo test -p worth-topo query_workflow --lib`
  - `cargo check -p worth-kernel -p worth-spatial -p worth-topo`

## Warning Cleanup Pass

- Took a principled warning-reduction pass across `worth-kernel`,
  `worth-spatial`, and `worth-topo` instead of suppressing lints:
  gated planner-owned wrapper surfaces that only existed for tests, removed
  stale convenience reexports, restored one live topo retained-artifact module
  that certification still consumes, and kept test-only helper exports only
  where the harness truly still needs them.
- Result: normal development builds are now warning-clean for the three target
  crates under `cargo check -p worth-kernel -p worth-spatial -p worth-topo`.
- Verified topo behavior still held with
  `cargo test -p worth-topo query_workflow --lib`.
- Remaining work is in the test-only layer rather than the runtime layer:
  `worth-topo` and `worth-spatial` still have lib-test warning clouds, and
  `worth-kernel` lib tests currently have broader harness compile errors that
  need a separate cleanup pass before test-warning counts can be trusted.
- Follow-up pass repaired that harness breakage:
  - restored internal-only reexports needed by kernel lib tests without
    reopening the public API,
  - restored analogous crate-private facade paths for spatial planner-route
    tests,
  - added a few explicit test imports/type annotations where the test module
    had become too implicit after the cleanup.
- Verification after the harness repair:
  - `cargo test -p worth-kernel --lib --no-run`
  - `cargo test -p worth-spatial --lib --no-run`
- Current state: dev builds are clean, lib-test harnesses compile again, but
  there is still a large remaining warning tail across kernel/topo/spatial test
  and internal helper surfaces that needs a separate reduction pass.
- Continued the topo warning pass by deleting dead planner-owned
  `query_backed_read_family` wrapper files, removing a dead invalidation route
  packet duplicate, adding a real retained-artifact historical snapshot test,
  and trimming dead fixture helpers; this brought `worth-topo` lib-test
  warnings down from 34 to 10.

## Worth 7.6 Hot/Cold Side Quest

- Paused the `worth76` runner after a representative Phase 1 repair test proved
  the timeout was architectural rather than incidental.
- Traced the representative `7.6` fragment-classification entry test and found
  it was spending about 223 seconds rebuilding the full `7.5` owner seam
  through touched-graph readiness, loop replay, and overlap closeout instead of
  consuming a compact carried authority artifact.
- Drafted `_docs/worth/worth-fragment-classification-hot-cold-test-architecture-side-quest.md`
  as a one-off side quest that introduces a hot carried-authority lane, keeps
  the owner seam as a cold certification lane, defines a representative
  `Phase75OverlapToPhase76ClassificationEntryBasis`, and sets the new test
  precedent without rewriting every legacy test or editing the `7.6` milestone
  spec yet.
