use forge_store_security::AuthenticityLayoutReport;

fn main() {
    let _ = AuthenticityLayoutReport {
        rule: todo!(),
        rebuild_posture: todo!(),
        security_boundary: todo!(),
        exact_counters: todo!(),
    };
}
