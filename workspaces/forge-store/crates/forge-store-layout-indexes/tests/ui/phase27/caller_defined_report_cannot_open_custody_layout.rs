use forge_store_security::CustodyLayoutReport;

fn main() {
    let _ = CustodyLayoutReport {
        rule: todo!(),
        rebuild_posture: todo!(),
        security_boundary: todo!(),
        exact_counters: todo!(),
    };
}
