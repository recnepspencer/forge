use forge_store_layout_indexes::evolution::migration::LayoutBindingWitness;

fn forge() -> LayoutBindingWitness {
    LayoutBindingWitness {
        family: todo!(),
        bound_version: todo!(),
        observed_version: todo!(),
        bound_authority: todo!(),
    }
}

fn main() {
    let _ = forge();
}
