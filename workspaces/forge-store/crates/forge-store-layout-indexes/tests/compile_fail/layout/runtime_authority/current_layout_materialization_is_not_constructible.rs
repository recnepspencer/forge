use forge_store_layout_indexes::CurrentLayoutMaterialization;

fn forge() -> CurrentLayoutMaterialization {
    CurrentLayoutMaterialization {
        materialization: panic!(),
    }
}

fn main() {}
