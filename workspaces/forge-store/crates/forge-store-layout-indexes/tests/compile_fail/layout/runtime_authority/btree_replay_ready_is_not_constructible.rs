use forge_store_layout_indexes::BTreeReplayReady;

fn forge() -> BTreeReplayReady {
    BTreeReplayReady {
        source: todo!(),
        current_materialization: todo!(),
    }
}

fn main() {
    let _ = forge();
}
