use forge_store_layout_indexes::AdmittedPhysicalKeyDomain;

fn forge() -> AdmittedPhysicalKeyDomain {
    AdmittedPhysicalKeyDomain {
        family: panic!(),
        domain: panic!(),
        encoding: panic!(),
        comparator: panic!(),
        prefix: panic!(),
        range: panic!(),
        hash_collision: panic!(),
        composite_ordering: panic!(),
        tenant_partition: panic!(),
    }
}

fn main() {}
