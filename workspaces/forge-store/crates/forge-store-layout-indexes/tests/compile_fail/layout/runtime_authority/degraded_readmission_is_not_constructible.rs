use forge_store_layout_indexes::DegradedScanReadmission;

fn forge() -> DegradedScanReadmission {
    DegradedScanReadmission {
        basis: panic!(),
        current: panic!(),
    }
}

fn main() {
    let _ = forge();
}
