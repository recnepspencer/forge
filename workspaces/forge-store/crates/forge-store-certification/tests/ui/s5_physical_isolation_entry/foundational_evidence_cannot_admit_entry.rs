use forge_store_physical_isolation::{
    admit_physical_isolation_entry, PhysicalIsolationEntryFoundationalEvidence,
};

fn main() {
    let evidence: PhysicalIsolationEntryFoundationalEvidence = todo!();
    let _ = admit_physical_isolation_entry(evidence);
}
