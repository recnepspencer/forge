use forge_store_certification::S51CertificationCloseoutInput;

fn main() {
    let _input = S51CertificationCloseoutInput {
        scenario_evidence: Vec::new(),
        replay_transcripts: Vec::new(),
        security_foundation_handoff: todo!(),
        policy: todo!(),
    };
}
