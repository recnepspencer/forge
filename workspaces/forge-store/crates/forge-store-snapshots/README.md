# forge-store-snapshots

Owns Roadmap 1 Milestone 4: immutable snapshots, point-in-time reads,
snapshot-plus-tail restore, snapshot basis records, and rebuild parity.

Snapshots are derived durable artifacts. They must never outrank canonical
commit authority.
