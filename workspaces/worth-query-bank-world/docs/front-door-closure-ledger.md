# Bank World Front-Door Closure Ledger

## Closure rule

`PROVED` requires a named production owner, a positive witness, a hostile twin
where the claim has a denial boundary, an independent oracle, a consequential
assertion, and the cheapest exact command. `OPEN` means the guarantee is
currently reachable but not yet proved. `DEFECT` means present source violates
the guarantee. `BLOCKED` names the earlier guarantee or later phase whose
unavailable behavior makes honest proof impossible. A green test alone does
not close a row. A high- or critical-impact finding reopens every row it can
invalidate.

## Current phase frontier

Runtime Hardening Phases 1-10 and Bank World Phases 1-5 are `PROVED`. Their
phase-owned closure ledgers remain the canonical detailed evidence. Runtime
Phase 9 closes through its host conditional-operation ledger and Runtime Phase
10 closes through its public-cutover ledger. Bank World Phase 6 is the next
open boundary; Closure Phase 1 remains causally downstream in the order fixed
by the milestone specification.

Historical rows below retain the claim proved by their owning phase, but their
consequence column describes the current frontier rather than preserving a
blocker that a later completed phase has already removed.

## Bank World Phase 1

| ID | Status | Claim and owner | Positive evidence | Independent or hostile evidence | Consequence and command |
| --- | --- | --- | --- | --- | --- |
| B1.1 | PROVED | `bank-domain` owns the exact entity, aspect, field, relationship, operation, installed operation-program, policy, currency, effect, and value inventory. | `schema/{entities,fields,relations,operations,program_manifest,governance,values,manifest}.rs`. | `schema_inventory::bank_manifest_matches_the_frozen_phase_one_world` compares every member family and each operation's exact action set to independent sets. | Adding, removing, renaming, or retargeting a member fails `cargo test --manifest-path workspaces/worth-query-bank-world/Cargo.toml -p bank-domain --test schema_inventory`. |
| B1.2 | PROVED | `bank-domain` owns customer and employee role meaning; graph relationships, never strings or token claims, are the future power source. | `model/roles.rs`; exact relationships in `banking-product-contract.md`. | The residue oracle denies raw descriptor construction; the courtroom requires role combinations and revocation. | Role/schema drift changes the manifest identity or inventory and fails the bank package command. |
| B1.3 | PROVED | `bank-domain` owns exact positive transaction `Money`, distinct signed accounting `SignedMoney`, typed USD, immutable-posting rules, operation purposes, and future invariant meaning. | `model/money.rs`, `schema/values.rs`, and the monetary-invariants section of `banking-product-contract.md`. | Zero and negative `Money` construction denies; a signed constructor does not exist on the operation-input type; `wrong_currency_value.rs` denies a USD marker on `Money<EUR>` and declaration tests deny an undeclared currency. | Sign, currency, or capability drift changes the Rust type or canonical schema identity rather than reinterpreting the same value. |
| B1.4 | PROVED | The frozen mutation inventory includes account creation, opening funding, deposit, withdrawal, stable-recipient transfer, business initiation, approve, reject, grant, revoke, and reversal. | `schema/operations.rs`; exact mutation list in `banking-product-contract.md`. | The manifest inventory is independent of the declaration chain; `SendMoney` accepts `BankPrincipalId`, not a caller-selected destination account. | Operation drift fails `schema_inventory`; wrong input types fail compiler certification. |
| B1.5 | PROVED | Read and mutation outcome families have one bank-domain meaning owner without transport vocabulary. | `model/outcomes.rs`; outcome section of `banking-product-contract.md`. | HTTP mapping is explicitly downstream and cannot alter the enums. | Runtime Phase 5 now consumes these meanings through typed ordinary outcomes. Wire translation remains owned by Bank Phase 5. |
| B1.6 | PROVED | Authentik OIDC code flow, issuer/subject identity, dynamic provisioning, independent user processes, and teardown posture are frozen as fixture responsibilities. | `async-identity-courtroom.md`. | The hostile inventory includes malformed/expired tokens, unknown or ambiguous mapping, crashes, response loss, and queue saturation; product source contains no participant identities. | Runtime Phase 2 and Bank Phase 2 resolved authenticated-principal and Authentik adapter admission. Bank Phase 5 now proves independent per-user node processes and TCP execution. |
| B1.7 | PROVED | Every required courtroom actor and combination has a dynamic real-process destination. | Dynamic-participant and process-topology inventories in `async-identity-courtroom.md`. | The fixture forbids locally minted identity, shared user runtimes, and privileged product mutation. | The real cold identity command proves dynamic Authentik identities/browser admission; Bank Phase 5 now proves independent user-node process execution through the same real identity world. |
| B1.8 | PROVED | Real read, traversal, mutation, effect, approval, grant, and revoke authoring use only audience facades. | `public_consumer_transcript.rs` and the exact Phase 1 blocks in `public-consumer-contract.md`. | The transcript binds an installed schema and asserts binding identity, graph changes, and denial kinds. | `cargo test --manifest-path workspaces/worth-query-bank-world/Cargo.toml -p bank-domain --test public_consumer_transcript`. |
| B1.9 | PROVED | Authentication, policy admission, execution, commit, and live delivery are not simulated in Phase 1. | Later-execution block and prohibitions in `public-consumer-contract.md`. | Phase 1 introduced no premature bank executor, HTTP adapter, authentication bypass, or courtroom substitute. | Runtime Phases 2-8 and Bank Phases 2-5 now own their completed runnable behavior, including canonical installed application queries and real transport. |
| B1.10 | PROVED | Every known workaround is classified by its causal owner and phase. | Gap table below. | The ledger records typed graph mutation and currency association as resolved Phase 1 gaps rather than hiding them downstream. | Any new generic Query gap must add/reopen a Runtime row and phase before bank-local code may proceed. |

## Runtime Hardening Phase 1

| ID | Status | Claim and owner | Positive evidence | Independent or hostile evidence | Consequence and command |
| --- | --- | --- | --- | --- | --- |
| R1.1 | PROVED | Declaration owns one portable manifest covering entity, aspect, field, relation, operation, installed operation-program, policy, currency, and effect members. | `application_schema/{declaration,operation_program}.rs` and schema macros. | Canonical-identity tests vary every member family; bank inventory independently lists every family and exact program edge. | Missing closure or duplicate members deny in `cargo test --manifest-path workspaces/worth-query/Cargo.toml -p worth-query-declaration`. |
| R1.2 | PROVED | Manifest identity is explicit, order-convergent, versioned, and includes field family/type/currency/capabilities, operation input, every create/delete/write/link/unlink/emit edge, and effect payload types. | `canonical_identity.rs`. | Identity tests vary every program action and field dimension; member order convergence is tested separately. | Capability-program drift changes package identity rather than sharing a digest. |
| R1.3 | PROVED | Installation alone binds a typed schema to exact runtime, generation, package identity, admission identity, owner, version, and schema meaning. | `worth-query-installation::{application_schema,installed_index/application_schema}.rs`. | Installation tests deny foreign runtime, stale generation, package drift, admission drift, owner/version mismatch, and schema drift. | `cargo test --manifest-path workspaces/worth-query/Cargo.toml -p worth-query-installation application_schema`. |
| R1.4 | PROVED | Reads expose typed root entity, direction-typed relation traversal, projection, and equality filtering only where the field posture permits it. | `read_authoring.rs`; bank account and owner-traversal transcript. | `cross_schema_field`, `wrong_relation_direction`, `wrong_field_value`, and `unsupported_equality_operator` compiler cases; forged endpoints deny installed admission. | Illegal next actions do not compile; forged names return a typed authoring denial. |
| R1.5 | PROVED | Application values carry exact scalar family and Rust value type; currency-bearing values additionally prove the domain currency represented by the schema marker. | `values.rs`, currency capability types, and currency field macro. | `wrong_currency_value.rs`, missing-currency declaration test, forged-value and forged-currency consumer attacks. | Wrong type, scalar family, or currency cannot silently bind to the installed field. |
| R1.6 | PROVED | Mutations expose typed create, delete, relation link/unlink, and field write actions gated by operation-specific compile capabilities and the same exact installed operation-program edge. | `mutation_authoring.rs`, `operation_program.rs`, and operation capability macros. | `illegal_write.rs`, `illegal_graph_mutation.rs`, forged field/relation attacks, `compile_capability_without_installed_operation_edge_is_denied`, and dangling-program closure denial. | Grant and revoke carry explicit graph changes; an undeclared or merely compile-local graph effect cannot author successfully. |
| R1.7 | PROVED | Operation and effect authoring bind the exact operation input and installed emit edge plus declared effect payload. | `effect_authoring.rs`; bank mutation/effect transcript. | Wrong-operation input compiler case; forged operation, input, effect, and payload denials; program omission denial. | `effects(operation).emit(effect, payload)` requires both `OperationEmits` and the identical installed program edge. |
| R1.8 | PROVED | Descriptors and copied binding identity are descriptive only; the opaque installed-schema handle remains the authority root. | `ApplicationSchemaBindingIdentity` documentation and non-`Clone` `WorthQueryInstalledApplicationSchema`. | `installed_handle_constructor_is_private.rs`; index validation consumes exact package authority fields. | A forged descriptor/binding can author no executable authority; later execution must validate against the opaque handle. |
| R1.9 | PROVED | Dynamic application keys are absent; generated bank code has no accidental dynamic lane. | No dynamic reference type is exported. | `schema_inventory::bank_schema_source_has_no_raw_query_descriptor_or_dynamic_key_lane` scans production source. | Dynamic extension requires an explicit future phase/spec change rather than a string escape hatch. |
| R1.10 | PROVED | Bank application source has no raw application descriptor constructors or non-audience Query dependency. | Macro-authored schema modules and `bank-domain/Cargo.toml`. | Residue/dependency oracle plus boundary checker. Hostile tests may construct raw descriptors only to prove denial. | Production residue or a dependency bypass fails the bank package or constitutional gate. |
| R1.11 | PROVED | Ordinary declaration, installation, and bank consumer loops are isolated from compiler certification. | Dedicated `application_schema_compile_fail` target contains ten orthogonal cases. | Final warm times: declaration 2.26s, installation 0.44s, bank workspace 0.52s, focused trybuild 1.70s. Cold trybuild rebuilds remain deliberate and isolated. | Ordinary edits do not invoke the historical compiler matrix; compiler certification runs only on its exact command. |
| R1.12 | PROVED | Dependency compilation distinguishes primary-logical-graph evidence from separate-authority provider calls. | `dependency_impact/compilation/graph_calls.rs` filters both read and touch provider receipts by graph participation; local mutation retains runtime effect plus primary-read evidence. | `workflow_effect_execution` proves the local success and partial-effect paths; `dependency_impact::workflow_closure` names the declared touch/read edges and realized primary read; the complete 319-test installed-world matrix preserves hostile remote call and commit checks. | Primary graph work cannot be forced to invent provider receipts, while missing, duplicate, contaminated, or wrong-authority remote receipts still deny. |

Runtime Phase 2 reopens R1.1, R1.2, R1.3, R1.8, R1.10, and R1.11 while
principal binding becomes a first-class schema member and the ordinary host
gains its first primary-graph composition surface. They return to `PROVED` only
after the Phase 1 independent inventories, hostile authority cases, dependency
boundaries, and warm commands cover the enlarged contract.

The Runtime Phase 2 rows below re-prove those reopened guarantees: the bank
inventory remains exact, every principal-binding dimension participates in
fixed-width canonical identity, installation remains runtime/generation
affine, proof and bootstrap constructors remain sealed, bank production source
retains only audience dependencies, and focused authentication/primary-graph
tests remain outside the compiler-certification lane.

## Runtime Hardening Phase 2

| ID | Status | Claim and owner | Positive evidence | Independent or hostile evidence | Consequence and command |
| --- | --- | --- | --- | --- | --- |
| R2.1 | PROVED | `worth-query-declaration::authentication` owns bounded typed `(issuer, subject)` identity and one collision-free length-delimited lookup value. | `external_identity.rs` validates each component and lowers the typed pair into one index value without a lossy digest. | Pair-alias, empty/whitespace, control-character, issuer-overlength, and subject-overlength cases fail at the typed boundary. | No raw string can substitute for the pair; `cargo test --manifest-path workspaces/worth-query/Cargo.toml -p worth-query-declaration authentication::external_identity`. |
| R2.2 | PROVED | Application-schema declaration, canonical identity, and installation own one exact principal-binding contract, including the typed identity field on the target principal. | `member_closure.rs`, `canonical_identity.rs`, and `installed_index/application_schema.rs` bind external identity/status fields, their exact postures, mapping direction, target-principal identity aspect/field/scalar/value type, schema meaning, runtime, and generation. | Wrong or mutable external fields, wrong or mutable target identity, immutable status, reversed relation, missing dependency, copied identifiers with a forged target identity type, foreign runtime, stale generation, package drift, admission drift, and schema drift deny. Every binding dimension changes fixed-width canonical identity. | Descriptors alone open no mapping authority; `cargo test --manifest-path workspaces/worth-query/Cargo.toml -p worth-query-declaration principal_binding` and `cargo test --manifest-path workspaces/worth-query/Cargo.toml -p worth-query-installation principal_binding`. |
| R2.2a | PROVED | Ordinary host domain-package composition carries application schemas into the exact execution-installed portable package index through the lawful `worth-query-host` audience facade. | `bank-server/tests/identity_admission.rs` installs the bank package and resolves its application schema and binding through `worth-query-host` only. | Package/schema owner mismatch and package/admission identity drift deny in the installation suite; `cargo tree -p bank-server --depth 1` shows no internal Query authority dependency. The source-complete domain-authority inventory classifies both domain and runtime-core audience exports on one semantic row. | A side-built index, unclassified audience export, or direct monolith import opens no lawful bank host authority. |
| R2.2b | PROVED | Domain-package and application-schema grammars share the same namespace-qualified canonical owner. | The public schema macro accepts literal `WORTH.tests.primary-graph`, which exactly matches the host package namespace/name. | Leading, trailing, repeated-dot, empty-segment, and dotted-member cases deny while the dotted owner succeeds. | Ordinary host installation is representable without weakening member identifiers; `cargo test --manifest-path workspaces/worth-query/Cargo.toml -p worth-query-declaration application_schema`. |
| R2.3 | PROVED | `worth-query-admission::authenticated_principal` owns request deadline/cancellation and the only authenticated-external-proof constructor. | An installed-schema-affine admitted adapter validates a candidate and mints the sealed, time-bounded external proof. | Wrong audience, expiry, cancellation, deadline, direct field construction, deserialization, and issuer/subject/attribute Debug disclosure fail. | An adapter candidate or host assertion is not authentication authority; `cargo test --manifest-path workspaces/worth-query/Cargo.toml -p worth-query-admission authenticated_principal` and `cargo test --manifest-path workspaces/worth-query/Cargo.toml -p worth-query-admission --doc`. |
| R2.4 | PROVED | `worth-query-execution` owns one primary Relational graph root and the lawful host audience composes it without importing the monolith or Runtime Bridge. | Ordinary builder composition preserves runtime name, schema, invariant catalog, shared writes, and synchronous index refresh; `bank-server` installs the same execution-owned graph authority through `worth-query-host`. Consumed bootstrap atomically reserves external identity, principal key, and canonical typed principal identity. | Mixed host schema bases, implicit custom backends, duplicate external identities, duplicate principal keys, and duplicate typed identities deny. A corrected row after a duplicate-identity denial still publishes, proving failure did not poison bootstrap state. | The host preserves rather than reconstructs authority; run the host surface test, the monolith `primary_graph_composition` suite, and the bank-server suite. |
| R2.4a | PROVED | `worth-query-execution::primary_graph` owns a schema-typed application terminal that consumes the raw execution root and installation authority at publication. | The host journey and bank server progress from admitted portable package through installed schema and move-only bootstrap into `WorthQueryPrimaryGraphApplicationRuntime`. | Raw Relational injection, broad graph publication, and integration-handle extraction exist only in the non-reexported execution integration facade; host compile-fail docs prove the audience runtime has neither bypass. | The lawful bank server depends directly only on `bank-domain` and `worth-query-host`; run host doctests, the host surface test, and `cargo tree -p bank-server --depth 1`. |
| R2.5 | PROVED | `worth-relational` owns capped exact-generation equality lookup and authoritative-storage parity; execution owns typed mapping resolution. | Ordinary resolution uses the derived identity index and records examined candidates; certification compares the same classification to authoritative storage. | Unbounded/nonexact requests, cap overflow, unknown, disabled, ambiguous, corrupt index parity, and stale index generation deny. The shared-root write witness proves refresh after the authoritative commit. | Ordinary work is one exact index generation and at most the declared candidate cap; `cargo test -p worth-relational bounded_entity_field` from the repository root and `cargo test --manifest-path workspaces/worth-query/Cargo.toml -p worth-query-execution primary_graph`. |
| R2.6 | PROVED | `worth-query-execution::primary_graph` owns the sealed application-principal proof bound to runtime, installation binding, admitted adapter proof, expiry, mapping entity, target relation, graph-derived typed principal identity, and fixed-width canonical freshness digest. | Successful resolution decodes the declared identity field from the exact mapped Principal row, returns its typed value in the sealed proof, and later freshness validation succeeds. | Cross-runtime, changed mapping target/status/typed principal identity, ambiguous mapping, expired authentication, cancellation, direct field construction, and deserialization fail; debug output omits external identity and target identity values. | Authentication grants identity only and becomes unusable when its exact graph or time basis is stale; `cargo test --manifest-path workspaces/worth-query/Cargo.toml -p worth-query-execution primary_graph` and `cargo test --manifest-path workspaces/worth-query/Cargo.toml -p worth-query-execution --doc`. |

## Bank World Phase 2

| ID | Status | Claim and owner | Positive evidence | Independent or hostile evidence | Consequence and command |
| --- | --- | --- | --- | --- | --- |
| B2.1 | PROVED | `bank-http-adapter` performs real Authentik Authorization Code with PKCE, state, and nonce. | The exact cold courtroom completes browser-driven identification and password stages, receives the callback, exchanges the code, and admits the resulting credential against one HTTPS issuer. | The same lane denies wrong state and nonce and proves callback response loss plus request cancellation; protocol-boundary tests deny malformed callbacks. | No password grant, locally minted token, or decode-and-trust substitute closes the row; run the exact cold courtroom command below. |
| B2.2 | PROVED | The adapter validates issuer, audience, signature, algorithm, time, ID/access pair binding, exact introspection client affinity, and online revocation posture. | The real credential admits, configuration admits only HTTPS issuer/control endpoints with issuer-origin affinity, and every credential requires active introspection with exact client ID, issuer, and subject affinity. | The real lane denies forged signature, wrong nonce, malformed token, wrong audience, issuer-only mismatch, crossed independently valid token halves, expiration, and post-revocation reuse. Protocol tests deny HTTP and cross-origin control endpoints; ordinary install rejects the cold certificate even when the cold feature is unified. | A locally valid ID token, active unrelated access token, introspection response missing its client/issuer/subject identity, secret-bearing cross-origin request, or feature-unified cold trust cannot authenticate through the ordinary constructor. |
| B2.3 | PROVED | Signing-key rotation causes one bounded JWKS refresh and revalidation. | Real Authentik signing-key rotation admits a newly signed credential after exactly one refresh. | A real displaced key absent after the one refresh denies with refresh count one; forged signature, nonce, audience, and issuer failures retain count zero. | An unbounded refresh loop or stale-key acceptance fails the exact cold courtroom. |
| B2.4 | PROVED | `bank-server` owns the installed primary identity graph through host facades only, with no parallel principal registry. | Dynamic principal/mapping installation writes a canonically unique typed `BankPrincipalId` onto the Principal row, then resolution returns that exact graph-derived ID in one sealed bank principal; the consumer proves a non-ordinal ID of 41 survives resolution. | Unknown, disabled, duplicate/ambiguous mappings, duplicate typed identities, and changed typed principal identity deny or stale in the bank and execution suites; source inspection finds no external-identity-to-bank-ID map. `cargo tree -p bank-server --depth 1` shows only `bank-domain` and `worth-query-host`. | Bank product code imports neither Relational nor Query authority packages and cannot collapse or relabel a resolved actor from side state; `cargo test --manifest-path workspaces/worth-query-bank-world/Cargo.toml -p bank-server`. |
| B2.5 | PROVED | `bank-courtroom` dynamically provisions and tears down Authentik, PostgreSQL, browser automation, users, clients, keys, and mappings. | The exact cold Docker courtroom passed against Authentik 2026.5.6, PostgreSQL, and Selenium after first executing its intentional-unwind world. | The hostile prelude requires absence of labeled containers, volumes, networks, and the fixture directory before the successful world starts; an independent post-run name-prefix and OS-temp audit found no residue. | The real external test fails rather than silently skipping when Docker is unavailable or cleanup escapes. |
| B2.6 | PROVED | Raw tokens and Authentik types stop at the adapter boundary. | The adapter returns `BankAuthenticatedPrincipal`, whose private representation retains the sealed `WorthQueryAuthenticatedPrincipal<BankSchema, Principal, BankPrincipalId>`; its typed bank identity is decoded from the exact installed primary-graph principal row rather than joined from host side state. It exposes identity and non-authoritative display attributes without exposing raw Query authority; bank domain/server source contains no OIDC or token type and `bank-server` depends only on `bank-domain` plus `worth-query-host`. | Credential, callback, pending-flow, configuration, external identity, authenticated-proof, and typed-principal Debug tests redact values. The real cold courtroom compiles and observes retained display attributes through the bank wrapper. The cold mutation/install surface is feature-gated and absent from ordinary builds. | Application execution receives neither tokens nor claims nor a raw Query constructor; source residue, cold-courtroom compilation, default adapter tests, and dependency-tree evidence fail vocabulary, provenance, or authority leakage. |

## Runtime Hardening Phase 3

These rows begin `OPEN`. A declaration or passing happy-path test cannot close
them; closure requires the exact lower owner and hostile authority evidence
named in each row.

| ID | Status | Claim and owner | Positive evidence required | Independent or hostile evidence required | Consequence and cheapest exact command |
| --- | --- | --- | --- | --- | --- |
| R3.1 | PROVED | Declaration owns schema-typed personal, business, and institution ability references plus typed operation requirements; installation alone promotes their descriptive identity into runtime authority. | An ordinary application declares abilities and operation requirements without semantic strings, and the `OperationRequiresAbility<Operation>` relationship is a compile-time requirement. | Cross-schema and wrong-scope typed composition is uncallable. Public doc-hidden constructors used by downstream macros remain descriptive: fabricated ability name or scope compiles but fails canonical installation with `AbilityNotInstalled` or `AbilityMeaningChanged`. | No marker, role enum, or copied identifier grants power; run the declaration ability tests, the focused installation forgery test, and the bank ability-inventory suite. |
| R3.2 | PROVED | Installation compiles application operations into the existing installed domain-operation graph-read, touch, effect, invariant, and conditional contracts. | One installed bank operation retains the exact compiled closure and current package/generation authority. | Missing dependency, meaning drift, foreign runtime, stale generation, and application/domain-operation divergence deny. | No parallel application executor exists; the five focused installation cases pass. |
| R3.3 | PROVED | Execution owns a move-only typed primary-graph policy-fact bootstrap with no retained product mutation bypass. | Typed entities, fields, and relations publish atomically into the same primary Relational graph as principal mapping. | Wrong type/schema/direction, duplicate key, dangling relation, failed batch, post-publication bootstrap reuse, and raw graph extraction fail. | The eight primary-graph tests and four execution doctests pass. |
| R3.4 | PROVED | Relational owns exact touched-graph evidence for the policy facts actually read, while Query binds the admission to the installed allowed touched-graph contract. | Evidence retains exact entity, relation, adjacency-list, aspect, snapshot, and realized-observation identities; admission retains the installed graph-read, touch, and effect contract. | Foreign snapshot, widened plan, omitted negative adjacency, and operation-contract drift deny. | Seven Relational authorization tests and three Query authorization tests pass without fabricated future effect IDs. |
| R3.5 | PROVED | Runtime Bridge lowers installed policy dependencies only through exact installed semantic correspondence. | Lowering retains installed correspondence authority, realized Relational observation identity, Signal graph authority, path order, and dependency cardinality. | Reordered paths, non-exhaustive facts, unknown correspondence, and foreign evidence cannot satisfy Query retention. | The two focused Bridge authorization tests pass. |
| R3.6 | PROVED | Signal alone mints allow/deny decision evidence from installed policy evaluation. | Positive, negative, membership, role-field, distinct-actor, and revocation inputs produce deny-precedent decisions. | A decision enum, token claim, caller role, or detached digest cannot construct retained evidence. | The two focused Signal tests and Query proof-construction doctest pass. |
| R3.7 | PROVED | Query composes authenticated principal, installed operation, Relational evidence, Bridge lowering, and Signal evidence into one non-cloneable admitted-operation proof that retains the exact monotonic authentication deadline and original live request scope. | Current installed membership admits the exact operation and typed scope; repeated observations mint the same operation-scope fingerprint for downstream idempotency, and current-authority validation succeeds before the carried bases expire. | Missing membership, crossed runtime scope, cancellation before or after admission, authentication expiry before or after admission, stale proof, and deserialization fail; the fingerprint excludes snapshot and request timing but includes runtime, operation, principal, and scope. | Four focused Query authorization tests, including post-admission expiry/cancellation, and four execution doctests pass. |
| R3.8 | PROVED | Authorization preserves the distinction between the installed allowed touched graph and later realized proposal effects. | The admitted operation retains the exact allowed graph contract and typed scope without claiming identities for records that do not exist yet. | Crossed scope, operation-contract drift, caller touch assertions, and widened authorization dependencies deny. | Runtime Phase 4 owns R4.1; Phase 3 exposes no caller touch-set parameter or fabricated scope-only effect evidence. |
| R3.9 | PROVED | Query owns one lane-independent admitted-scope proof family for later read, mutation, explanation, history, and live consumers. | The proof is schema-, operation-, principal-, scope-, snapshot-, installed-contract-, authentication-lifetime-, and request-interruption-bound without a lane-specific policy mirror. | A caller boolean, detached timestamp, replacement cancellation token, role enum, descriptive identity, crossed runtime, or alternate constructor opens no lane authority. | Runtime Phase 4/5 must consume the proof's Query-owned current-authority validation; the one Phase 3 proof is private-field, move-only, non-deserializable authority. |
| R3.10 | PROVED | Current policy re-evaluation observes membership and revocation dependencies rather than treating an earlier admission as timeless authority. | Fresh observation after relationship removal denies and retains the empty adjacency dependency; unrelated graph scale leaves work unchanged. | Omitted negative facts, stale correspondence, and cached decisions open no new operation admission. | The Relational revocation and scale tests pass; Runtime Phase 5 owns active-live R5.2. |
| R3.11 | PROVED | Ordinary authorization work is bounded by declared dependency and touched width. | Counters expose adjacency lists/edges, records, predicates, frontier width, Bridge paths, and Signal dependency families. | Scaling unrelated principals/accounts does not change current-path counters; historical reconstruction is separately counted. | The bounded-work and historical-revocation Relational tests pass. |

## Bank World Phase 3

| ID | Status | Claim and owner | Positive evidence required | Independent or hostile evidence required | Consequence and cheapest exact command |
| --- | --- | --- | --- | --- | --- |
| B3.1 | PROVED | `bank-domain::proposals` owns personal/business account creation and explicit one-time opening funding; `bank-server` binds each preparation to the exact installed operation proof. | A real teller-authenticated consumer authorizes and prepares both account families; creation emits no journal and opening funding emits one balanced journal. | Customer creation authority, duplicate owner/business identity, unknown institution, repeated opening funding, partial journal, and implicit funding all deny or remain absent. | No commit method exists; run `cargo test --manifest-path workspaces/worth-query-bank-world/Cargo.toml -p bank-domain --test accounting_proposals` and `cargo test --manifest-path workspaces/worth-query-bank-world/Cargo.toml -p bank-server --test authorized_proposals`. |
| B3.2 | PROVED | `accounting::{journal,balance,invariants}` and `proposals::{journal_proposal,proposal_completion}` own immutable `SignedMoney` postings, derived signed balances, exact effect replay, and typed account-activity emissions for deposit, withdrawal, transfer, and reversal. Operation requests retain distinct positive `Money`. | Independent sums recompute every journal and account balance; each movement carries the journal plus one typed activity per touched account, and exact opposite postings reverse once. | Zero/negative request amounts are unrepresentable, while overdraft, frozen/closed account, unknown recipient, malformed/unbalanced journal, omitted state effect, second reversal, and wrong typed currency fail. Private posting fields prevent mutation. | A sign-confused operation amount, shadow balance, or effect/state mismatch cannot obtain the invariant witness; run the bank-domain lib, doctests, and `accounting_proposals` tests. |
| B3.3 | PROVED | The installed bank authorization graph plus `business_payment` proposal semantics own initiation, distinct approval/rejection, and actor-bound workflow transitions. | A real authorized approver converts one pending intent into a balanced payment proposal while the authenticated bank actor is retained. | Self-approval deny precedence, actor relabeling, viewer plus employee role combination, unrelated-business approver role, revoked membership, and duplicate decision all deny. | Route state and role enums mint no proposal authority; run `cargo test --manifest-path workspaces/worth-query-bank-world/Cargo.toml -p bank-server --test business_authorization` and the bank-domain `workflow_proposals` test. |
| B3.4 | PROVED | `validate_proposed_snapshot` and exact effect replay execute topology, allocator, append-only journal, posting uniqueness, checked arithmetic, funds, account-status, and conservation invariants before minting `BankInvariantApprovedProposal`; the proposal also retains the exact private causal authority of its basis snapshot. | Independent accounting oracles match proposed signed balances and zero-sum journals across funding, deposit, withdrawal, transfer, approval, and reversal. An exact snapshot clone matches the proposal basis. | One-posting, unbalanced, omitted-effect, overflow frontier, overdraft, frozen/closed, repeated funding, repeated reversal, and an independently built identical same-version basis create no matching witness for the authoritative snapshot. USD and sign posture are compile-bound through `Money<USD>` inputs and `SignedMoney<USD>` accounting values. | Runtime Phase 4 must consume rather than recreate this private witness and must require exact `matches_basis`, not compare only `BankSnapshotVersion`; run the bank-domain lib and accounting suites. |
| B3.5 | PROVED | `BankIdempotencyIntent` length-delimited hashing binds the typed key, canonical operation payload, and Query-minted authenticated operation-scope fingerprint. | Equivalent input/key/admission retries produce the same intent; a real Query admission retry preserves its fingerprint. | Payload and raw binding drift change the intent, while real principal, installed operation, and typed scope changes independently change the Query binding. | Phase 3 contains no resolution map or `AlreadyCommitted`; run `workflow_proposals::idempotency_intent_is_stable_and_detects_binding_or_payload_drift` and the bank-server `idempotency_binding` test. |
| B3.6 | PROVED | `bank-server` publishes `BankWorldSeed` through `worth-query-host`, derives the typed authenticated bank actor from the sealed Query principal proof, and progresses Query admission into `BankAuthorizedProposal`, which retains both admission—including its authentication/request currentness basis—and an invariant proof bound to the exact causal bank snapshot. `bank-http-adapter` can install the same world with Authentik. | External consumer tests authenticate, resolve graph-owned typed principal identities and real graph scopes, authorize installed operations, and prepare account, positive-money transfer, and payment proposals while graph publication carries signed balances and postings. | Parallel principal registry, cross-account scope substitution, authenticated-actor substitution, missing permission, detached timing assertions, same-version alternate snapshot substitution, signed-value substitution into an operation input, private-field construction, raw Query runtime extraction, and non-audience dependencies fail or are absent. | There is no authoritative mutation or bank-local Query authority builder; Runtime Phase 4 must validate both retained Query currentness and exact bank basis before commit. Run the three bank-server consumer tests, bank-domain/bank-server doctests, strict bank-workspace Clippy, and `cargo tree -p bank-server --depth 1`. |

## Bank World Phase 3 QA Correction History

| Finding | Status | Invalidated guarantee | Root correction | Closure evidence |
| --- | --- | --- | --- | --- |
| Q3.1 A proposal could carry a state change not exactly represented by its effect list. An earlier correction also placed derived account-activity delivery in the atomic mutation list, incorrectly coupling publication to accounting truth. | CLOSED | B3.2, B3.4, B3.6, R4.1, future R5 | Proposal completion replays every authoritative typed effect from the basis snapshot and compares the exact resulting snapshot. Reversal retains the original journal identity. Account activity is no longer an atomic proposal effect; Runtime Phase 5 derives it from the committed journal artifact. | The omitted-effect unit test denies; accounting and consumer suites assert journal-only mutation cardinality and exact reversal. Later activity delivery cannot exist without a committed journal source. |
| Q3.2 Snapshot fixtures could not express frozen/closed hostile states, allocator frontiers were not validated, and opening funding could repeat. | CLOSED | B3.1, B3.2, B3.4 | The validated builder gained typed status-bearing fixture accounts, collision/frontier validation, and checked maximum-ID handling. Opening funding now requires a zero derived balance. | Frozen, closed, repeated funding, duplicate account, unknown institution, malformed journal, and maximum-frontier-invalid shapes deny. |
| Q3.3 Returning a naked invariant proposal would have dropped Query admission before compare-and-commit, and the Authentik composition installed principals without the bank policy world. | CLOSED | B3.3, B3.6, future R4.1 | `BankAuthorizedProposal` retains the concrete Query admission and invariant witness. `AuthentikBankIdentity::install_world` installs the same `BankWorldSeed` and exposes only the governed bank runtime. | Private-constructor doctests pass; real consumer tests traverse authentication, graph admission, and proposal preparation without raw authority. |
| Q3.4 Idempotency evidence varied arbitrary binding bytes but did not prove the real Query fingerprint separated each causal dimension. | CLOSED | B3.5 | A real two-principal, two-institution graph now compares retry, principal, operation, and scope fingerprints independently. | `bank-server --test idempotency_binding` proves retry equality and three independent drift inequalities; the bank-domain test proves binding drift changes the final intent. |
| Q3.5 The Phase 3 bank-principal wrapper omitted the Phase 2 display-attribute and redacted-diagnostic surfaces, leaving the feature-gated real Authentik courtroom uncompilable, while B2.6 still described the displaced raw Query-principal handoff. | CLOSED | B2.6, B3.6 | `BankAuthenticatedPrincipal` now carries non-authoritative attributes through its retained sealed Query proof and implements value-redacted `Debug` without exposing that proof; B2.6 describes the actual post-Phase-3 authority boundary. | The bank-server identity test proves diagnostic redaction; the cold courtroom compiles and observes a real Authentik email attribute through the bank wrapper; the full cold lane and ordinary bank suites pass. |
| Q3.6 R3.1 claimed fabricated references failed at compile time even though downstream declaration macros intentionally use public doc-hidden descriptive constructors, and no focused test proved installation rejected fabricated ability identity. | CLOSED | R3.1 | The ledger now separates type-enforced relationship shape from installation-owned semantic identity, and the installation suite challenges fabricated ability names and scopes directly. | Compile-time `OperationRequiresAbility` evidence remains; `fabricated_ability_identity_cannot_bind_installed_authority` proves copied text opens no installed authority. |
| Q3.7 `Money<USD>` represented both positive operation amounts and signed accounting values; its public signed constructor let a consumer create zero or negative movement input that could nullify or reverse the intended transfer direction. | CLOSED | B1.3, B3.2, B3.4, B3.6 | Positive `Money<Currency>` and signed `SignedMoney<Currency>` are separate types. Operations and payment intents accept only the former; balances, posting fields, journals, reversals, and accounting oracles use only the latter. The signed constructor was removed from `Money` with no compatibility alias. | Unit evidence denies zero/negative `Money`; a compile-fail public example proves the displaced signed constructor is absent; consumer and server suites prove signed accounting still publishes and conserves correctly. |
| Q3.8 Operation authorization checked authentication expiry and request interruption only while minting the admitted proof, then discarded both bases, leaving later governed phases unable to reject authority that expired or was cancelled after admission. | CLOSED | R3.7, R3.9, B3.6, future R4.1/R5.1 | `WorthQueryAdmittedApplicationOperation` now privately retains the exact monotonic authentication deadline and cloned live request scope and owns their current-authority validation. The operation-scope fingerprint remains timing-independent for equivalent authorized retries. | A focused adversarial test proves validation succeeds initially, then independently rejects post-admission authentication expiry and post-admission cancellation. Private fields and the absence of deserialization prevent detached timestamps or replacement tokens from restoring authority. |
| Q3.9 `BankInvariantApprovedProposal` retained only caller-constructible `BankSnapshotVersion`; an independently built alternate snapshot could reuse the same version and obtain an invariant witness that later compare-and-commit could not distinguish from the provider's actual basis. | CLOSED | B3.4, B3.6, future R4.1 | Every built bank snapshot now owns a private causal authority shared by exact clones. Proposal completion privately retains that authority beside the descriptive version, and `matches_basis` compares both without hashing or rescanning the world. | The consumer accounting suite proves an exact clone matches while an independently built value-equal, same-version snapshot does not. No public constructor exposes the authority, and Phase 4 is explicitly required to consume this exact match. |
| Q3.10 `BankIdentityRuntime` resolved Query's graph principal, then joined it to a caller-seeded parallel `ExternalIdentity -> BankPrincipalId` map. The map could diverge from the primary graph while the ledger claimed one authority. | CLOSED | R2.2, R2.6, B2.4, B2.6, B3.6 | Principal binding now includes a read-only, equality-capable typed target-principal identity field across declaration closure, canonical identity, installation, bootstrap, runtime observation, freshness, and the sealed principal proof. `bank-server` deleted the parallel registry and obtains `BankPrincipalId` only from that proof. | Declaration denies mutable and wrong-typed target fields; installation denies copied identifiers paired with a forged identity type; execution stales a proof when the exact Principal identity value changes; bank inventory and consumer suites prove the bound field and graph-derived actor. |
| Q3.11 The graph-owned identity correction still admitted two distinct Principal rows with the same typed application identity, allowing distinct Query principals to collapse to one bank actor ID. A failed multi-key bind also mutated earlier reservation sets before returning. | CLOSED | R2.4, R2.6, B2.4, B3.6 | Consumed bootstrap now computes Relational's canonical scalar comparison key and enforces target identity uniqueness within each installed principal binding. It preflights external identity, principal key, entity key, and typed identity before atomically reserving any of them. | A focused hostile test binds two distinct keys/external identities to `7`, receives `DuplicatePrincipalIdentity`, then successfully retries the denied external/key with `8` and publishes both rows. The bank consumer proves graph resolution returns seeded ID `41`, not an ordinal inference. |

## Runtime Hardening Phase 4 Handoff

| ID | Status | Claim and owner | Positive evidence required | Independent or hostile evidence required | Consequence and cheapest exact command |
| --- | --- | --- | --- | --- | --- |
| R4.1 | PROVED | Query and the provider prove that every realized proposal read and effect is inside the exact admission-bound touched-graph closure before provider commit. | The Bank send consumer projects its typed root and bounded accounting neighborhood, completes the exact dynamic read plan, authors installed effects, and commits. | Foreign-scope reads/effects, unprojected same-type entities, projection-occurrence substitution, undeclared fields, and installed-program drift deny before candidate formation. | `cargo test --manifest-path workspaces/worth-query/Cargo.toml -p worth-query-execution primary_graph::tests::application_attempt` and `cargo test --manifest-path workspaces/worth-query-bank-world/Cargo.toml -p bank-server --test authorized_proposals`. |
| R4.2 | PROVED | The primary provider owns typed idempotency and resolves one canonical intent binding to one authoritative terminal result. | Equivalent retries and concurrent equivalent attempts recover the original transaction receipt without duplicate effects. | Principal, operation, scope, or intent drift denies; response loss and index-publication failure reconstruct the provider result rather than guessing rollback. | The Query application-attempt suite proves retry, drift, concurrency, response loss, and reconstruction; the Bank authorized-proposals suite proves real journal cardinality. |
| R4.3 | PROVED | Query binds Runtime Phase 3 admission to compiler-visible projected-read, effect-program, provider-proposal, invariant, and compare-and-commit phases. | Only a completed admission-bound projected read set exposes installed effect authoring and the sealed provider candidate. | Raw admission, root-only reads, foreign projection occurrence, fabricated marker types, and independently assembled bank witnesses open no mutation path. | The application-attempt suite plus declaration compile certification prove both runtime and type-level closure. |
| R4.4 | PROVED | One installed primary-graph provider authority owns observed decision facts and atomic committed effects; `BankSnapshot` is isolated proposed state and an independent oracle only. | Bank projections are read at one provider version and their effects plus idempotency record commit in one Relational transaction. | Caller-built snapshots, same-version independently built snapshots, and bank-local mutation cannot enter Query's sealed commit candidate. | The Bank authorized-proposals consumer runs through `worth-query-host`; Query application-attempt tests exercise the provider transaction directly. |
| R4.5 | PROVED | Balance is derived from authoritative journal/posting topology under bounded, reconstructible provider projection; it is not a mutable counter or a finite-lifetime full-history scan. | Query's operation-typed exclusive incoming aggregate returns checked signed money plus exact contributing-posting count at one provider version. Warm maintenance examines only changed posting sources; cache-miss breadth is explicit. Bank requires the count to equal `AccountingRevision` before using the value. | Independent accounting oracles, concurrent overspend denial, full graph destruction/rebuild parity, arithmetic-eviction proof, duplicate posting-account denial, revision/count drift denial, and repeated public mutation beyond the former fact ceiling prove acceleration cannot change truth or become an account lifetime limit. | Run the bank projection hostile/rebuild tests, aggregate-projection unit tests, authorized concurrency suite, and the sustained-history live courtroom. |
| R4.6 | PROVED | Provider compare-and-commit owns typed terminal posture for denial, proved abort, indeterminate/response loss, and proved commit. | Every committed result carries the authoritative transaction receipt; retries resolve against provider truth. | Prepare rejection and pretransaction failure leave no effect or idempotency residue; response/index publication failures recover commit. `PartialEffect` remains explicit in the protocol but is structurally unrepresentable for this primary provider because effects and idempotency publish in one atomic transaction. | `cargo test --manifest-path workspaces/worth-query/Cargo.toml -p worth-query-execution primary_graph::tests::application_attempt::terminal_failures`. |
| R4.7 | PROVED | Basis lifecycle and managed-run admission retain the exact primary-branch snapshot and Runtime Bridge basis used by mutation preparation. | Unrelated head movement does not replace the pinned basis and relevant facts are rechecked at provider comparison. | Foreign runtime/binding, released snapshot, cancellation, and relevant drift deny; no caller can restamp a basis as current. | Query application-attempt lifecycle and same-fact race tests plus the complete Runtime Bridge suite. |
| R4.8 | PROVED | Concurrent requests use request-local Runtime Bridge execution lanes over one shared installed truth authority. | Equivalent concurrent attempts converge on one receipt; independent attempts both commit. | Thread-affine Signal state is not shared between requests and request-local lanes cannot mint provider truth. | Query application-attempt concurrency tests and Bank same-basis/independent transfer tests. |
| R4.9 | PROVED | Bank invariant projection and Query reads consume one move-only, admission-bound, pinned provider snapshot lease. | The projection that mints the Bank invariant witness is consumed by the matching admitted operation. | Reacquired same-version state, independent bank snapshots, another admission, foreign runtime, released leases, and inspection output cannot substitute. | Query invariant-projection and application-attempt suites plus Bank authorized proposals. |
| R4.10 | PROVED | Monetary truth is reconstructed from journal/posting topology; a typed per-account accounting revision supplies exact conflict detection without becoming a balance authority. | Every journal append advances touched-account revisions and post-commit balance matches the independent journal oracle. | Concurrent spending stales one candidate; unrelated accounts do not conflict; removal of derived presentation cannot alter accounting truth. | Bank accounting and authorized-proposals concurrency suites. |
| R4.11 | PROVED | Provider-to-Bank projection is a total, unique interpretation of the admitted operation's bounded reachable typed topology. | Every reached account, authorization, journal, posting, payment, and approval validates exact required live endpoints and projects once. | Duplicate semantic edges, orphan reachable records, missing endpoints, multiply attached postings, and multiple decisions deny; unrelated outside topology is neither scanned nor denied. | The complete `bank-server` unit and integration suites exercise each bounded projector and hostile topology family. |
| R4.12 | PROVED | Ordinary projection is provider-derived, version-bound, and proportional to admitted touched topology rather than installed-world size. | Structural counters report the exact bounded reads used by proposal preparation. | Growing unrelated accounts and topology leaves send projection work unchanged; no full-world scan or cloned authoritative snapshot enters the ordinary lane. | `authorized_proposals::concurrency_and_work::unrelated_world_growth_does_not_increase_send_projection_work` and the workflow projection suite. |
| R4.13 | PROVED | Created Bank identities are typed functions of admitted idempotency identity and within-operation role, not scanned allocation frontiers. | Equivalent retries derive identical IDs and independent bindings derive disjoint IDs without coordination. | Concurrent unrelated proposals from one version cannot collide; payload drift under one key cannot select fresh identities. | Bank authorized-proposals concurrency and Query idempotency-drift tests. |
| R4.14 | PROVED | Projection authority is operation-typed while mandatory commit dependencies remain a distinct exact installed manifest. | `OperationReads` constrains projector APIs; projection returns an operation-bound snapshot and exact dynamic dependency plan. | Reading outside capability fails compilation; omitted, extra, substituted, or multiplicity-collapsed decision facts deny completion. | Query declaration certification and application-attempt touched-graph tests. |
| R4.15 | PROVED | The application read set consumes the exact realized identity closure and installed distinct-fact budget for one admission occurrence. | Send carries the source, recipient, destination, and bounded accounting identities reached from its typed root. | Same-type foreign-scope entities, admission-unbound inspection, another occurrence, and over-budget facts deny before provider freshness work. | Query touched-graph closure tests, including the ordering oracle for budget denial before provider work. |
| R4.16 | PROVED | Admission-bound projection validates current authority before provider reads, separates inspection output from consumable projection authority, and releases pins on unwind. | A current exact admission yields one consumable snapshot; inspection returns data and work only. | Expiry, cancellation, foreign runtime/binding execute zero projection work; a panicking projector leaves no retained pin. | Query authorization and invariant-projection lifecycle tests. |
| R4.17 | PROVED | Compiler-visible phase types distinguish complete ordinary reads from complete projected mutation reads; only projected completion exposes effect authoring. | Exact admission plus exact projection advances to the mutation-only phase. | Root-only reads and fabricated/equivalent state cannot call `begin_effect_program`. | Query application-attempt touched-graph tests and compile-time API shape. |
| R4.18 | PROVED | Admission-bound invariant projection mints the exact dynamic dependency-key plan, which must equal the complete application read set. | Send requires both source and destination status/revision plus exact identity and ownership facts. | One repeated field-family instance cannot satisfy two entity dependencies; omitted, extra, or substituted keys deny. | Query touched-graph closure tests. |
| R4.19 | PROVED | Projection truth access uses the retained version, counts actual provider reads, enforces an installed projection-work limit, and keeps ordinary mutation replay-disabled. | Pinned adjacency remains stable and exact counters include endpoint reads. | Current-head substitution, endpoint undercount, budget exhaustion, unrelated widening, and replay-enabled ordinary preparation cannot mint authority. | Query invariant-projection tests, Runtime Bridge request-lane tests, and Bank projection-work tests. |
| R4.20 | PROVED | Admission-bound projection begins from the exact typed root identity already carried by authorization. | The projector receives a root bound to its private projection occurrence and incurs no redundant equality lookup. | Caller values, independently resolved same-type entities, inspection output, and foreign admissions cannot substitute. | `admitted_projection_supplies_its_exact_root_without_an_equality_lookup` and the Bank send work oracle. |
| R4.21 | PROVED | The exact Relational observation and sealed Runtime Bridge/Signal decision that minted admission are retained and provider-recompared under a separate installed cardinality budget. | Unchanged membership and negative-path bases remain fresh through commit, with exact evidence identity correspondence. | Revoking membership, removing the last allow path, or inserting a matching deny path prevents commit; unrelated relation kinds do not widen or stale authorization work. | Query authorization-causality tests, Relational authorization freshness tests, and Runtime Bridge sealed-correspondence tests. |

## Runtime Hardening Phase 4 QA Correction History

| Finding | Status | Invalidated guarantee | Root correction | Closure evidence |
| --- | --- | --- | --- | --- |
| Q4.1 The first application path could enter provider execution without a compiler-distinct projected mutation phase. | CLOSED | R4.1, R4.3, R4.17 | Ordinary and projected read completion became distinct phase types; only projected completion exposes installed effect authoring. | Root-only and foreign-projection hostile cases deny; the lawful Bank send transcript commits. |
| Q4.2 Projection was schema-typed but not bound to the exact operation, admission occurrence, or realized entity closure. | CLOSED | R4.9, R4.14-R4.16 | Operation-typed readers, private projection occurrence authority, a typed root, and the realized identity closure now travel together. | Cross-operation, cross-admission, same-type foreign-entity, and inspection-substitution cases deny. |
| Q4.3 Installed target-family presence collapsed repeated dynamic dependencies into one fact. | CLOSED | R4.1, R4.15, R4.18 | Projection mints an exact dependency-key plan and read completion requires set equality under the installed fact budget. | One source status cannot satisfy source and destination dependencies; omitted, extra, and over-budget facts deny. |
| Q4.4 Projection adjacency used current head, undercounted endpoint reads, had no hard work limit, and enabled replay on an ordinary lane. | CLOSED | R4.7, R4.12, R4.19 | Retained-version adjacency/endpoints, exact charging, installed projection budgets, and replay-disabled ordinary requests are explicit contracts. | Pinned-history, budget exhaustion, exact-counter, unrelated-growth, and request-lane tests pass. |
| Q4.5 Provider-to-Bank projection scanned total installed state and silently overwrote malformed duplicate topology. | CLOSED | R4.4, R4.11, R4.12 | Each operation now walks only its bounded reachable topology and validates unique required endpoints before producing domain state. | Hostile projector tests deny duplicate/orphan/multiply attached reachable topology while ignoring unrelated outside state. |
| Q4.6 Snapshot-global identity frontiers collided for concurrent proposals prepared from one version. | CLOSED | R4.2, R4.13 | Created IDs are typed derivations of admitted idempotency identity plus within-operation role. | Equivalent retries converge; independent same-version proposals derive disjoint IDs and both commit. |
| Q4.7 Balance risked becoming a mutable parallel fact and lacked exact conflict granularity. | CLOSED | R4.5, R4.10 | Journal/posting topology remains monetary truth; typed per-account accounting revisions carry conflict authority. | Independent reconstruction matches provider projection and concurrent overspend stales without unrelated-account conflicts. |
| Q4.8 Failure posture could have inferred rollback from a lost response or derived-index failure. | CLOSED | R4.2, R4.6 | Idempotency is committed atomically with effects and terminal recovery consults authoritative provider state. | Prepare/pretransaction failures prove absence; response/index publication failures recover the committed receipt. |
| Q4.9 Admission currentness was checked too late and a panicking domain projector could retain a snapshot pin. | CLOSED | R4.7, R4.16 | Current authority is checked before provider access and framework-owned leases unwind around contained projection execution. | Expired/cancelled admissions run zero projection work and the panic test leaves no pin or poisoned graph. |
| Q4.10 Authorization observations influenced admission but were absent from provider decision facts. | CLOSED | R4.1-R4.3, R4.6, R4.8, R4.21 | Provider facts now retain the exact Relational observation plus sealed Bridge/Signal evidence and revalidate their identity correspondence and freshness before commit. | Post-admission membership revocation and newly matching deny paths stale; unrelated relation-kind growth remains fresh. |
| Q4.11 The public operation-reads macro changed the governed audience facade without refreshing constitutional metadata. | CLOSED | R4.14 and repository boundary integrity | The facade snapshot was explicitly regenerated and the derived `worth-query-decl` agent context regenerated from it. | `boundary-check` and `agent-context check` both pass with the exact one-symbol facade delta. |
| Q4.12 Deposit preparation reconstructed every historical journal and posting into its decision snapshot. After modest history it exhausted the installed fact ceiling and made that ceiling an account lifetime limit. | CLOSED | R4.5 and downstream R5.4 | Query now owns a version-bound, reconstructible exclusive incoming aggregate. It carries checked signed money and exact source count, maintains warm state from authoritative changed postings, and leaves journal/posting topology as sole monetary truth. Bank decision snapshots carry only required derived starting values and one new journal. | Sixteen preexisting real deposits plus two live deposits commit with identical warm aggregate work and zero rebuild rows; destroying and reinstalling the graph rebuilds the same value; the next proposal contains one journal rather than copied history. |
| Q4.13 Decision transfer construction ran a legacy full-snapshot available-funds check before the provider-derived starting balance reached decision invariant validation, so every history-free decision snapshot appeared unfunded. | CLOSED | R4.5 and Bank invariant honesty | Journal construction now validates transfer shape and account status only. Full proposals and bounded decision proposals certify funds at their respective completion boundaries from complete history or provider-derived starting balance plus the appended delta. | Deposit-then-withdraw, send, approval, overdraft, and full bank-domain invariant suites pass; no second funds oracle remains in journal construction. |
| Q4.14 Journal commit lowering derived the next `AccountingRevision` from the partial proposed snapshot, rewriting it to one on every mutation and eventually disagreeing with posting topology. | CLOSED | R4.4, R4.5, R4.10 | Lowering reads the governed current revision through the exact projected application read attempt, advances it by the exact emitted posting count, and writes it atomically with those postings. The proposal snapshot supplies no revision authority. | The complete mutation family now performs deposit, withdrawal, send, approval, later history read, and reversal without revision drift; hostile revision/count mismatch still denies. |

## Runtime Hardening Phase 5 Handoff

| ID | Status | Claim and owner | Positive evidence required | Independent or hostile evidence required | Consequence and cheapest exact command |
| --- | --- | --- | --- | --- | --- |
| R5.1 | PROVED | Every ordinary read, mutation, explanation, history, and live facade consumes the Runtime Phase 3 admitted-scope proof family. | Each bank public builder accepts a typed operation, a sealed authenticated principal, and caller controls, then privately enters the operation-specific admission before projection or delivery. | Cross-account substitution, viewer-to-management widening, teller-to-audit widening, actor relabeling, copied continuation use, stale/foreign history cursors, and live revocation all deny through typed public outcomes; no surface exposes a read-set or provider bypass. | Run the complete `bank-server` integration suite: `cargo test --manifest-path workspaces/worth-query-bank-world/Cargo.toml -p bank-server`. |
| R5.2 | PROVED | Account discovery begins from the authenticated principal's typed graph identity and traverses declared visibility edges under a caller result bound and installed work bound. | An owner sees exactly the personal and business accounts reachable through current ownership or authorization, with typed result count, truncation, provider version, and exact work. | Thirty-two unrelated accounts leave output and work identical; 160 reachable authorizations exhaust the installed projection budget with a typed denial before unbounded delivery; discovery never enumerates and redacts foreign accounts. | `cargo test --manifest-path workspaces/worth-query-bank-world/Cargo.toml -p bank-server --test ordinary_reads` passes the exact membership, amplification, and budget courtrooms. |
| R5.3 | PROVED | Account summary/detail, authorized-user, activity, payment, pending-payment, and institution-audit reads return typed data with exact consistency and work metadata. | Every family consumes one admitted operation, pins one provider version, projects only operation-declared topology, reports exact work/result metadata, and returns bank-domain value types. | Owner-to-recipient cross-account substitution, stranger access, viewer-to-access-management widening, teller-to-auditor widening, over-budget work, cancellation, and deadline all deny through typed public outcomes; unrelated growth leaves scoped work unchanged and reconstructive scans remain zero. | The six-test public ordinary-read transcript, exact schema/ability inventories, read-only installation proofs, full Query/bank suites, and strict changed-crate Clippy pass. |
| R5.4 | PROVED | All twelve bank mutations use Runtime Phase 4's sealed projected-mutation progression and operation-specific compile capabilities, and repeated lawful use does not exhaust a history-proportional decision snapshot. | Account creation, opening funding, deposit, withdrawal, stable-recipient send, business initiation, approval, rejection, grant, revoke, and reversal reach provider commit through one typed public builder. Money movement uses constant warm aggregate work after initial reconstruction and proposals retain only their new journal. | The public surface exposes no generic effect bag, proposal constructor, read-set constructor, receipt constructor, or provider session; operation-specific lowerers require exact compile capability. Sixteen prior deposits cannot turn installed fact width into an account lifetime limit. | Run `ordinary_mutations`, `authorized_proposals`, and `live_activity::admitted_buffer_capacity_retains_multiple_matching_commit_causes`. |
| R5.4a | PROVED | Typed application emits publish in the same provider transaction and recoverable receipt causality as their graph changes, under the operation's installed retained-byte envelope. | A committed mutation returns an emitted-effect count; that count is stored in the atomic provider idempotency record. Every declared payload supplies compile-required retained-byte meaning, authoring rejects cumulative payload bytes before provider mutation, and provider lowering seals the revalidated batch as the only live-causality input. | Preparation, retained-byte excess, or pretransaction rejection publishes no emission; response loss recovers the original receipt without duplicate publication; after 64 later batches evict the original live payload, idempotency still recovers its exact emission count from provider truth. | Run Query declaration doctests and `application_attempt::emitted_effects`, including two individually admissible 140,000-capacity Strings whose sum is denied, plus the public bank mutation courtroom and fixed-width `ActivityEvent` test. |
| R5.4b | PROVED | Mutation projection seals the exact realized field, entity, relation, and bounded directional-adjacency predicates under an installed target-family ceiling. | Projected completion re-observes only its sealed dynamic keys; an observed adjacency carries exact relation membership usable for unlink. | An edge entering a sealed empty adjacency stales the attempt, unrelated-anchor growth remains fresh, compile capability cannot widen the installed relation manifest, callers cannot author keys, and an exact empty conditional dependency set is not forced to invent a fact. | Run Query `decision_adjacency`, `touched_graph_closure`, and `effect_authority`; the bank revoke courtroom proves consumer use of exact unlink authority. |
| R5.4c | PROVED | Primary-provider publication admits every installed bank mutation contract against a bounded per-attempt support envelope distinct from fixed concurrent-attempt capacity. | Every installed decision-fact and effect-program ceiling passes publication and all twelve operations reach provider admission. | An unsupported installed contract fails publication rather than installing a permanently unusable operation; concurrent capacity remains separately fixed at 64 rather than being widened with semantic width. | Run the installation/provider support suites and the complete public mutation matrix. |
| R5.5 | PROVED | Caller-owned request and idempotency controls remain causal through admission, projection, compare-and-commit, and typed outcome mapping. | Equivalent retries recover the original typed result; cancellation and deadline interruption remain typed; metadata retains bounded provider work and projection work. | Intent drift denies, cancellation after program preparation prevents provider commit, response loss duplicates neither graph changes nor emits, and principal/scope/operation authority cannot substitute. | Run `ordinary_mutations::public_mutation_controls_preserve_interruptions_permissions_and_intent_drift`, Query application-attempt authority/idempotency tests, and the full bank-server suite. |
| R5.6 | PROVED | Business initiation returns a descriptive typed continuation whose only next-action markers are approval or rejection under newly admitted principal authority and fresh request controls. | A committed or authoritatively recovered initiation returns the same payment continuation; a current pending-payment read can reconstruct it for a separate process; distinct authorized approvers and rejectors each commit through actorless decision markers. | The continuation deliberately carries no authority: explicit copied construction opens nothing, the deciding actor is derived from the fresh authenticated proof, an unauthorized initiator denies, cancelled initiation returns no continuation, a decided continuation cannot advance again, and marker fields reject caller actor injection at compile time. | `cargo test --manifest-path workspaces/worth-query-bank-world/Cargo.toml -p bank-server --test workflow_continuations` plus bank-server doctests. |
| R5.7 | PROVED | Mutation denial, invariant violation, stale attempt, abort, cancellation, deadline, partial-effect, indeterminate, committed, and recovered outcomes expose one total typed explanation view. | Public committed/recovered workflow outcomes retain the exact receipt and recovery posture; denied outcomes retain a typed stage and governed denial; invariant violations are first-class public status and explanation variants. | The mapping exhaustively matches every terminal enum variant, never parses `Debug`, `Display`, provider text, or diagnostic subjects, and a nested domain invariant is normalized before publication rather than relabelled by an adapter. | The internal terminal explanation matrix, public workflow explanation assertions, source residue review, strict bank-server Clippy, and full bank-server suite pass. |
| R5.8 | PROVED | Account activity history is a bounded ordinary projection of committed journal/posting topology, not replay. | Account/version-bound cursors and caller limits page stable typed activity ordered by an authoritative per-account posting sequence, with provider version and bounded work metadata. | A foreign-account cursor denies, a provider-version change returns an explicit stale-cursor denial, and twelve committed deposits prove chronology is independent of idempotency-derived journal identity. Bank production dependencies contain no replay crate. | `cargo test --manifest-path workspaces/worth-query-bank-world/Cargo.toml -p bank-server --test activity_history`. |
| R5.9 | PROVED | Live account/activity delivery observes byte- and batch-bounded provider commit causality and re-evaluates current Bridge/Signal permission evidence before every payload. | A current lease ignores unrelated commits, retains every matching emission in batch order, and binds each cause to one fresh authorized projection of its exact journal, account, and per-account sequence before reporting delivery. The source retains at most 64 installed-byte-admitted batches, tracks exact payload bytes through eviction, and keeps only O(1) ordering counters. | Revocation closes before the next payload; cancellation and deadline remain distinct; a caller cannot enlarge installed buffering; variable-width payloads cannot evade the retained-byte ceiling; a counterparty posting with the same sequence is skipped; two buffered causes deliver exactly once with history-independent work; transient release failure retains cause and occupancy; 65 constant-neighborhood commits produce exact overflow. | `cargo test --manifest-path workspaces/worth-query-bank-world/Cargo.toml -p bank-server --test live_activity` plus Query declaration, live-delivery, and emitted-effect tests. |
| R5.10 | PROVED | A normal consumer discovers and executes every Runtime Phase 5 family through bank-domain plus Query audience facades only. | The bank-server consumer suite reads every query family, executes all twelve mutations, advances and reconstructs workflow continuations, explains typed outcomes, pages history, and consumes live delivery. `public-consumer-contract.md` documents the same stable call shape. | `bank-server` directly depends only on `bank-domain` and `worth-query-host`; compile-fail docs reject caller-built admissions, proposals, continuations with actor fields, live leases, cursors, and effect programs. Boundary and generated-context checks reject audience drift. | Run the full bank-server suite, `cargo tree --manifest-path workspaces/worth-query-bank-world/Cargo.toml -p bank-server --depth 1`, boundary-check, and agent-context checks. |

## Runtime Hardening Phase 5 QA Correction History

| Finding | Status | Invalidated guarantee | Root correction | Closure evidence |
| --- | --- | --- | --- | --- |
| Q5.1 Application-operation contract compilation assigned mutation effects and touched-graph invariant execution to an installed operation even when its authored program was empty. | CLOSED | R5.1-R5.3 and the read-only authority boundary | Contract compilation now emits `NotRequired` for touches, effects, invariants, and invariant execution when decision reads exist but the mutation program is empty. An operation with neither reads nor a program still denies installation. | Focused installation tests prove a read-only operation installs with one declared read and no mutation authority contract, while an empty operation receives `MissingProgram`; the full Query and bank suites remain green. |
| Q5.2 Endpoint-pair relation evidence could not prove that a mutation had depended on an empty adjacency, so a new matching edge could enter before commit without invalidating the decision. | CLOSED | R5.4b and permission-sensitive mutation freshness | Projection now seals a private direction-typed bounded adjacency key with its exact sorted relation membership; provider compare-and-commit re-observes that same predicate before effects. | `an_edge_entering_an_observed_empty_adjacency_stales_the_attempt` and `growth_at_an_unrelated_anchor_does_not_stale_the_attempt` prove relevant phantom rejection without global false conflicts. |
| Q5.3 Treating the installed decision-read manifest as an exact realized fact list forced conditional mutations to invent one arbitrary fact from every allowed family. | CLOSED | R5.4b and exact dynamic dependency closure | The installed manifest is the allowed target-family ceiling; the admission-bound projector seals the exact realized key set, and projected completion requires equality with that set, including a legitimate empty set. Manual unprojected completion still requires its installed mandatory set. | The touched-graph closure suite proves exact empty completion, repeated-family occurrence identity, extra/omitted denial, and fact-budget precedence. |
| Q5.4 The primary provider advertised only 64 decision facts per attempt while installed bank mutation contracts legitimately required larger bounded ceilings, allowing publication of operations that provider admission could never run. | CLOSED | R5.4c and honest installation | Per-attempt semantic/resource support is 4,096 while retained bytes and fixed concurrent-attempt capacity remain independently bounded; publication validates installed contracts against this support. | All bank manifests publish and the twelve-family public mutation courtroom reaches real provider commit; provider-support mismatch tests still fail closed. |
| Q5.5 Business-payment approval projected partial counterparty accounts and then ran a whole-snapshot balance invariant, falsely rejecting a valid approval when omitted history affected a non-required partial account. | CLOSED | R5.4 and bank invariant honesty | Approval and rejection use `BankDecisionSnapshot` with an explicit required-account set; only the source balance required by the decision is validated while journal topology remains exact. | `payment_projection_preserves_the_source_account_balance` and the public approval/rejection courtroom pass against real projected topology. |
| Q5.6 Revocation carried authorization fields and edges but not the entity-existence fact required to bind deletion authority. | CLOSED | R5.4 and exact delete authority | The operation manifest declares the authorization entity read, projection explicitly requires that entity, and projected completion carries it into the effect target set before unlink/delete authoring. | The public consumer completes revoke, and foreign or undeclared effect targets remain denied by Query effect-authority tests. |
| Q5.7 A private-field continuation was initially described as non-forgeable even though the bank's read DTOs are intentionally descriptive and caller-constructible. That compile-fail claim confused representation privacy with authority. | CLOSED | R5.6 and authority honesty | Continuation construction is explicitly descriptive and may use a payment identity crossing a process boundary. Fresh authentication, request controls, installed admission, projection, and invariant decision remain the only authority; only actor-bearing decision-marker fields stay compiler-private. | A copied continuation fails under an unauthorized principal, lawful fresh principals approve or reject, and the compile-fail marker test rejects caller-selected actor injection. |
| Q5.8 The generic mutation outcome could report initiation success but had no typed place to carry the next workflow action, encouraging callers to rediscover payment identity or retain proposal authority. | CLOSED | R5.6 and typed phase progression | Initiation has a specialized outcome that exposes a continuation only for `Committed` or `AlreadyCommitted`; all other terminal postures suppress it. The continuation contains only `PaymentId` and can cross process boundaries without transferring authority. | Commit and retry recover one continuation, cancelled initiation returns none, and approval/rejection each require a newly authenticated principal and fresh controls. |
| Q5.9 Domain invariant failures were nested inside generic proposal denial, so adapters could not exhaustively distinguish invariant violation without reinterpreting internal error structure. | CLOSED | R5.7 and typed explanation honesty | Mutation outcome construction normalizes governed `BankProposalDenial` into a first-class `InvariantViolated` status; one total borrowed explanation mapping preserves all other distinct terminal postures and typed denial stages. | The public already-decided continuation returns the exact `PaymentAlreadyDecided` invariant, and the exhaustive terminal matrix proves stale, cancellation, deadline, denial, invariant, abort, partial-effect, and indeterminate remain distinct. |
| Q5.10 Activity history initially sorted by idempotency-derived journal identity, which is stable identity but not committed chronology. | CLOSED | R5.8 and history order honesty | Every posting now carries a typed per-account sequence assigned atomically with its account journal revision; ordinary history sorts that authoritative sequence. | Twelve sequential deposits prove monotonic account sequence even when derived journal identities contain an inversion. |
| Q5.11 A live lease could advance its source cursor after delivering only the first matching emission in a multi-emission commit batch, dropping later matching causes when caller buffering was narrow. | CLOSED | R5.9 and exact provider causality | The move-only lease retains the active batch plus emission index until every matching cause has been delivered in order; source advancement occurs only after batch exhaustion. | Query live-delivery tests use buffer capacity one against a multi-emission batch and prove every matching emission arrives exactly once and in order. |
| Q5.12 The first consumer overflow attempt used repeated deposits and hit the legitimate accounting projection-work ceiling before delivery retention saturated, coupling two unrelated bounds. | CLOSED | R5.9 and independent test evidence | The overflow courtroom now alternates grant/revoke commits over one constant-size authorization neighborhood; only the retained commit-source axis grows. | Sixty-five real committed batches against capacity sixty-four produce `Overflow` with exactly one missed batch in 0.36 seconds. |
| Q5.13 The bounded live source also retained an unbounded `commit_id -> emission_count` map solely for idempotent receipt recovery, so memory still grew with total runtime commits after payload eviction. | CLOSED | R5.4a, R5.9, and bounded-source honesty | Emitted-effect count is now a required `UInt64` in the provider idempotency entity created atomically with graph effects. Recovery reads it with committed-version metadata. The live source keeps only 64 batches and O(1) monotonic/count state. | The first emitted batch is demonstrably evicted after 64 later commits, yet retry recovers the identical receipt and emission count from provider truth; source tests prove exact overflow, closure, and monotonic publication. |
| Q5.14 Live polling removed a pending cause before Bridge/Signal occupancy release; a transient release failure returned `Unavailable` but dropped the committed cause and its move-only retry occupancy. | CLOSED | R5.9 and exact-once failure posture | Release failure now returns the occupancy into the same cause and restores it at the front of the bounded queue. Cursor and batch progression remain unchanged until successful delivery. | A buffer-one, two-emission batch injects one transient release failure, observes `Unavailable` with one buffered cause, then delivers both payloads exactly once and in order. |
| Q5.15 The live source bounded retained commit count but erased arbitrary typed payloads behind `Any` without measuring their recursively owned allocations, so one String or collection could make a nominally bounded batch retain unbounded memory. | CLOSED | R5.4a, R5.9, and retained-memory honesty | Effect declaration now requires `ApplicationEffectPayload`; authoring counts cumulative typed payload bytes against the installed `RetainedBytes` envelope, provider lowering revalidates and seals an admitted batch before mutation, and the live source accepts only that proof while tracking exact payload bytes through eviction. | An unmeasured payload fails declaration at compile time; one 140,000-capacity String is admitted but a second is denied on cumulative bytes before provider publication; a small String commits and its source byte counter equals its admitted measurement; the bank's real `ActivityEvent` proves its fixed-width count. |
| Q5.16 The bank live facade consumed a typed cause and then reran the oldest-first bounded activity query without using `ActivityEvent.journal_sequence`. Once history exceeded the caller's result limit, it labeled a stale prefix as the new commit's update; any post-cause projection failure also lost the cause. | CLOSED; PHASE 6 REPLACED THE TEMPORARY PATH | R5.9 and exact live result meaning | Phase 5 first introduced a crate-private exact-cause projection. Q6.23 later deleted that temporary operation/query authority: the installed `AccountActivityQuery` now binds each `ActivityEvent` to its globally typed posting identity, and Query retains the cause and occupancy until the canonical query-shaped projection succeeds or a typed terminal consumes it. | Two real buffered deposits after sixteen prior commits deliver distinct one-entry query results in commit and account-sequence order with identical target-index, adjacency, and fallback work. Source residue proves the temporary cause query and `ReadAccountActivityOperation` no longer exist. |
| Q5.17 Exact live-cause projection stopped at the first posting with the emitted per-account sequence. Both sides of a balanced journal may lawfully have that same sequence, so a counterparty posting caused false `Absent` before the target posting was examined. | CLOSED | R5.9 and exact cause identity | Cause matching is conjunctive over journal identity, account identity, and account sequence. A sequence match attached to another account continues the bounded journal-width search; duplicate matches for the target still deny as ambiguous. | After sixteen prior deposits, two buffered deposits deliver their distinct target postings in commit and sequence order with equal work, one result each, and no reconstructive scan. |

## Bank World Phase 4

| ID | Status | Claim and owner | Positive evidence required | Independent or hostile evidence required | Consequence and cheapest exact command |
| --- | --- | --- | --- | --- | --- |
| B4.1 | PROVED | `bank-domain` owns exact estate, death-notice, executor, beneficiary, joint-owner, authorized-signer, branch, employee-assignment, legal-authority, grant, emergency-access, and typed mandatory-review meaning without installing premature Query execution authority. | The typed world and application-schema manifest contain the exact entity, field, relationship, and policy-contribution inventory; Runtime Phase 7 remains the explicit installation handoff. | The global manifest inventory proves exactness; the focused presence and source-complete residue oracles reject missing required members, installed estate operations, local superuser/redaction/permission/undo lanes, and raw descriptor construction. | Run `schema_inventory` and `estate_schema_inventory`; the full all-feature bank workspace and constitutional gates pass. |
| B4.2 | PROVED | Capability authority is exact across estate, account, institution, branch, operation, purpose, field, amount, validity, workflow stage, delegation ancestry, and current grant status. | A causally lawful estate specialist can recognize authority, release an estate, and perform a balanced disbursement with exact grants. | Foreign targets, revoked/expired grants, principal mismatch, scope mismatch, missing parents, wrong grantors, and wider delegated authority deny through isolated causal seams. | Run `estate_capability_courtroom`, `estate_authority_seams`, and `estate_fail_closed_seams`. |
| B4.3 | PROVED | Ordinary employee role, beneficiary status, delegated capability, and emergency elevation remain separately attributable; no one fact silently substitutes for another. | Each lawful action requires both an operation-appropriate current employee assignment and its exact capability. | A same-branch teller with a matching grant still denies; the beneficiary branch manager's ordinary, delegated, and emergency facts remain independently visible and cannot combine into power. | The public integration courtrooms consume `bank_domain::estate` only and prove capability alone is insufficient. |
| B4.4 | PROVED | Emergency elevation requires an exact requester, distinct approver and reviewer, elevated grant, emergency-review identity, estate, reason, status, and current use; elevation bypasses no conflict rule. | One well-formed active elevation accompanies only its exact grant and requester. | Missing/self approver, reviewer conflict or record drift, missing/wrong-kind review, grant/requester mismatch, inactive/review-required state, and beneficiary conflict deny at their own causal boundaries. | Run `estate_authority_seams`, `estate_elevation_seams`, and `estate_fail_closed_seams`. |
| B4.5 | PROVED | Restricted customer, beneficiary, document, account, posting, audit, and governance-metadata disclosure is governed by domain-owned classifications, exact purpose law, and truthful typed result carriers. | The seven-row field/purpose partition is exact and the result shape distinguishes disclosed values from typed omissions. | Purpose-confused access denies before projection; concrete posting-history and audit-trail carriers replace placeholder estate identifiers, and no callback redaction or metadata bag exists. | Run `estate_disclosure_contract` and `estate_authority_seams`. |
| B4.6 | PROVED | Conflict, separation-of-duty, distinct-actor, and beneficiary-exclusion contributions fail closed without a superuser or emergency bypass. | Non-conflicted specialists execute the legal recognition, release, and disbursement path. | The beneficiary branch manager cannot read restricted estate data, approve their own elevation, or disburse for self; a non-beneficiary employee/executor cannot complete their own release review; emergency elevation cannot bypass either rule. | The courtrooms isolate conflict precedence from delegation, role, and structural denials. |
| B4.7 | PROVED | Estate disbursement uses positive typed money, two exact signed postings, distinct source/destination accounts, recognized executor graph authority, beneficiary ownership, sufficient funds, and an independent accounting oracle. | The lawful balanced transfer is accepted only with both legal-authority and executor facts plus the exact release-review kind. | Independent seams deny malformed posting shape, illegal destination ownership, missing executor conjunction, wrong review kind, and insufficient funds; the denial vocabulary contains no structurally unreachable accounting branch. | Run `estate_capability_courtroom` and `estate_fail_closed_seams`. |
| B4.8 | PROVED | Every Phase 4 action has a domain-owned aftermath classification distinguishing no mutation, reversible authorization, compensatable movement, reconcilable external work, and irreversible legal/audit decisions. | The exact thirteen-operation inventory maps exhaustively to a typed aftermath. | One independent expected table fails when an action is omitted or silently changes class. | Run `estate_aftermath_contract`. |

## Bank World Phase 4 QA Correction History

| Finding | Status | Invalidated guarantee | Root correction | Closure evidence |
| --- | --- | --- | --- | --- |
| QB4.1 Administrative commands carried a scoped capability but did not bind every target grant, access record, notice, or account to that estate. | CLOSED | B4.1, B4.2, B4.4 | One integrity progression now validates exact estate/institution/branch targets before role or capability contributions can allow the action. | Foreign revoke and freeze targets deny; emergency administration and use share the same target validator. |
| QB4.2 Active elevation allowed a missing approver and parallel command/use validation let reviewer or review-record drift pass on one lane. | CLOSED | B4.4, B4.6 | Emergency administration and consumption share exact requester/grant/review structure; approver and reviewer distinctness plus typed review kind/status are mandatory. | Lawful, missing, mismatched, inactive, review-required, self-approval, and reviewer-conflict seams pass. |
| QB4.3 Employee roles and disclosure classifications were stored but non-causal, while posting/audit results used `EstateCaseId` placeholders. | CLOSED | B4.3, B4.5 | Operation-appropriate role and field/purpose oracles now contribute independently; concrete posting-history and audit-trail result types own their payload meaning. | A teller with a matching grant and an invalid field purpose deny independently; the original six-row purpose partition and typed result contract pass. |
| QB4.9 Runtime Phase 7 exposed that the administration-governed estate projection had no truthful disclosure class of its own and could only be mislabeled as one of the six Phase 4 value classes. | CLOSED | B4.5 and Runtime Phase 7 R7.13-R7.14 | `GovernanceMetadata` is a distinct highly restricted class permitted only for estate administration; every declared governance-query result slot requires it. | The seven-row purpose oracle, installed composition contract, exact public grant, wrong-field twin, and complete governed disclosure receipt prove the new class without widening any earlier field. |
| QB4.4 Branch-manager self-disbursement originally failed on malformed destination authority rather than the intended beneficiary conflict. | CLOSED | B4.6, B4.7 | The hostile world now supplies lawful destination ownership before conflict evaluation, and deny precedence is explicit. Delegation widening uses a non-conflicted actor so its own oracle remains independent. | The manager denies with `BeneficiaryConflict`; the widened child denies with `DelegationWidensAuthority`. |
| QB4.5 Recognized legal authority did not require the executor graph fact, and an undifferentiated completed emergency review could release an estate. | CLOSED | B4.1, B4.6, B4.7 | Release/disbursement require recognized authority conjoined with `EstateExecutor`; mandatory reviews carry `EstateRelease` or `EmergencyAccess` kind. | Missing executor and wrong-kind completed review both deny despite otherwise valid facts. |
| QB4.6 Separation-of-duty policy was declared but did not stop an employee who was also executor from reviewing their own release. | CLOSED | B4.6 | Executor overlap is a causal deny contribution for mandatory review, release, and disbursement; self-recognition is also denied. | A non-beneficiary specialist/executor with exact review capability denies as `SeparationOfDutyConflict`. |
| QB4.7 Accounting and conflict denial enums contained branches structurally masked by stronger exact-shape or beneficiary-conflict rules. | CLOSED | B4.6, B4.7 | Unreachable `AccountingUnbalanced` and `SelfBenefitingDisbursement` branches were removed; the remaining denial vocabulary corresponds to representable causal failures. | Exact malformed-shape, beneficiary-conflict, and insufficient-funds tests reach distinct remaining denials. |
| QB4.8 Estate schema registration used one 135-line relation chain and the global inventory crossed the 400-line cap. | CLOSED | B4.1 and composition integrity | Member, capability, estate-relation, authority-relation, and policy installation now have named owners; field/governance inventory moved to a named test module. | Every one of 350 dirty Rust files is at most 400 lines; strict all-target/all-feature bank Clippy and rustfmt pass. |

## Runtime Hardening Phase 6

Runtime Phase 5's bank-local query implementations are candidate integration
evidence, not proof of Runtime Phase 6's canonical installed application-query
authority. No row becomes `PROVED` by relabeling an existing query or wrapping
a lane-specific projector. The current-result column describes the checked
worktree at the original Phase 6 handoff. Runtime Phase 6 is now closed; its
canonical final evidence and correction history live in
`_docs/WORTH-query/milestone-9.16-runtime-phase-6-closure-ledger.md`. The
historical narratives below are retained to explain the handoff, while their
statuses are synchronized to that final ledger.

| ID | Status | Guarantee and production owner | Positive evidence required | Independent or hostile evidence required | Current result, consequence, and cheapest exact command |
| --- | --- | --- | --- | --- | --- |
| R6.1 | PROVED | Domain declaration and package installation own one schema-bound application-query identity covering typed parameters, admitted root, typed authorization scope, structurally exact result shape, cardinality, predicate and ordering meaning, dependency ceiling, disclosure contract, basis support, lane eligibility, and compatibility generation. Nested path, sibling position, and scope meaning are identity-bearing; parameter values and request controls are not. | A query declared in the domain package resolves by typed reference to one fixed-width installed identity; equivalent package declarations converge. | Undeclared, host-authored, foreign-schema, stale-generation, package-drifted, or forged descriptive query meaning denies before execution. Independently varying every identity-bearing dimension changes package/query identity, while parameter-value and request-control changes do not. | Declaration prepares and retains one ready Foundational application-query basis under an explicit Query domain and rule version. Graph, planning, continuation, and installed-query artifacts derive from and retain that basis; compact identities derive only from ready basis material, while installed authority remains a separate nonce-bound seal. Structured Foundational comparison proves equivalence and mismatch, the identity/hostility and one-axis matrices remain green, and mutation-sensitive residue rejects direct SHA, `Debug` formatting, or a second identity grammar in every former semantic owner. |
| R6.2 | PROVED | Installation binds each application query to the existing canonical `WorthQueryReadFamily` / `WorthQueryReadGraph` meaning and the single Milestone 9.10 graph-read access-planning authority; no reduced planner, parallel requirement vocabulary, or lane-local query engine exists. | A bank query and the equivalent mature canonical graph lower through one explicit installed read-family binding to the same admitted schema references, normalized planning input, requirement derivation, runtime support inventory, budget, and plan-review semantics and produce equal semantic evidence. Each runtime boundary may seal that shared review into its own lower-layer execution authority without importing a higher-layer wrapper. | Missing, mismatched, or side-built read-family, graph, requirement, inventory, or plan evidence denies; source, type-identity, and dependency residue reject a second planner, copied vocabulary, name-only binding, or lane-local semantic executor. | Both real mature and installed application graphs prepare the same normalized Foundational planning semantics before admission. The execution runtime accepts only a prepared planning contract and consumes its carried artifact; no public reconstructive derivation remains. Real cross-source equivalence plus the one-axis direction, depth, predicate, ordering, cardinality, fanout, pressure, result-count, and lifecycle matrix pass while source schema and installed authority remain distinct. Runtime-owned support deletion denies plan review, and source/dependency residue contains no second planner or copied requirement vocabulary. |
| R6.3 | PROVED | Parameter binding, predicate normalization, selectivity, ordering, traversal, result-buffer, and lifecycle requirements resolve before execution into exact runtime-owned typed support or unsupported posture. | Supported parameter and ordering changes produce the exact `PredicateSupport`, `OrderingSupport`, traversal, buffer, and lifecycle requirements expected by an independent oracle and by the equivalent canonical graph. | One-axis changes to binding, predicate, order, traversal, cardinality, lane, or installed runtime support alter only the causally owned evidence. Unsupported search, ordering, traversal, Store, preview, or access-product posture cannot widen into a scan, host loop, local cache, or undeclared fallback. | Parameter and request controls feed one prepared planning artifact and exact runtime-owned support review before plan authority. Typed result selectors now compare stack-built structural slot keys against cold installed `Arc` identities without SHA, formatting, canonicalization, or heap work on the projection path; forged selectors deny. Every retained node, field, relation, nested-row vector, and owned scalar clone is charged through the move-only result reservation before allocation or clone. Temporary child buffers release as ownership moves, recursive retained measurement verifies the final tree, and independent observation distinguishes rejected capacity from actual peak. Real nested, 25 KB overflow, projection-denial, cancellation, continuation, and live-delivery courtrooms restore baseline; actual retained peak never exceeds the admitted limit. |
| R6.4 | PROVED | Domain-owned projection and derived-field meaning are package-declared and execute behind the installed query contract; hosts select installed queries and supply controls but cannot become query declaration, dispatch, or projection authority. | Bank account, payment, estate, and audit declarations supply package identity-bearing meaning while Query owns resolution, admission, execution, and result construction. | A host-authored definition, local marker, projection closure, mode switch, or internal Query import opens no executable lane; public compile-negative and dependency evidence reject the bypasses. | Query owns the path-keyed result tree and package projectors use query-bound field plus optional/one/many relation accessors. Account, payment, estate, and audit meaning is domain-declared; the former host projectors, markers, sorts, private cause query, and operation-admission live lease are deleted. The final compiler, dependency, facade, and source-residue courts prove no host authority lane survives. |
| R6.5 | PROVED | Query owns an opaque continuation bound to exact query identity, parameters, scope, basis, ordering, result shape, provider posture, and compatibility generation; it carries no authorization or runtime resource. | A lawful page resumes with identical canonical meaning and fresh scoped admission. | Copied, foreign-query, parameter-changed, cross-scope, stale-basis, wrong-order, wrong-shape, wrong-provider, or wrong-generation continuation denies before projection. Revocation between pages narrows or denies the next page. | Query issues a private-field, move-only continuation for one query-bound `Many` collection. It retains descriptive installed-query, ready Foundational parameter-basis, scope, continuation-contract, provider/index, exact basis-version, ordered-boundary, and generation affinity only. Resume consumes it, validates runtime/query affinity before runtime-support inspection, prepares one fresh parameter artifact, compares the two ready artifacts through Foundational before support review or basis acquisition, then reruns normal installation, principal, scope, interruption, authorization, planning, and exact-basis admission. Real bank tests prove lawful resume, cross-account/runtime denial, revocation denial, exact-version stability across a commit, generation continuity, page parity, and bounded seek work. Focused hostility proves changed parameter, stale installation, changed continuation meaning, wrong provider, stale/unavailable basis, stale principal/scope, authentication expiry, cancellation, deadline, page bound, and work bound; the Query basis observer proves every pre-basis denial leaves acquisition count unchanged. Compile-fail docs reject construction, cloning, cross-query, cross-scope, and cross-result substitution. Retained and disposed tokens leave runtime-basis, result-buffer, and provider-session observations at baseline, while source-complete token inspection rejects runtime-basis leases, provider sessions, buffers, and live resource types. |
| R6.6 | PROVED | Every application-query execution receives an execution-owned typed access context carrying the mapped authenticated principal and exact resolved scope required by the installed query, plus fresh installed scoped authorization and explicit caller controls for basis, consistency, supported freshness posture, deadline, cancellation, result count, and work. Runtime Phase 7 can strengthen this context without changing canonical query identity. | Current and pinned-basis requests carry the exact mapped principal, typed scope, installed scoped-authorization proof, and controls through admission and typed outcomes. | An external credential without mapped identity authority, wrong scope type or entity, unauthorized but valid scope, stale principal or scope, missing or weakened controls, ambient context, cursor-carried authority, expired authentication, cancellation, deadline, or over-budget work fails at its earliest owned boundary. | Current, pinned, first-page, and resume controls are coherent constructors owned by execution; callers cannot supply basis strings or independently combine basis, consistency, freshness, or lane posture. Access requires the sealed mapped principal and the exact query-typed resolved scope. The real bank owner succeeds and a mapped stranger denies before plan authority; current authorization still governs pinned old data. Foreign/expired/cancelled basis cases deny and release. Continuation-specific current-graph mutations prove stale principal and stale scope deny before basis acquisition, while authentication expiry, cancellation, deadline, page/result bound, and work exhaustion independently leave the basis acquisition counter unchanged and all observed resources at baseline. |
| R6.7 | PROVED | One-shot, continuation, historical, live, and admitted preview lanes share one canonical query meaning, lower execution kernel, and result shape, with an explicit support posture and fresh authorization on every execution or delivery. | One installed bank query produces parity across every supported lane; each non-live lane consumes one admitted graph-read plan through the shared lower kernel, while live consumes its separately admitted maintenance posture for that same read meaning. | A lane-specific selection, cardinality, ordering, projection, work, or receipt implementation, changed membership/disclosure meaning, silent unsupported fallback, or stale authorization makes the parity and revocation evidence fail. | Account activity proves current, pinned, continuation, historical, preview, and live execution from one installed meaning. Same-runtime historical and preview truth succeeds, foreign or discarded authority denies, every later lane reruns current authorization, and unsupported posture cannot fall back to current. The real estate preview and cross-query parity courtrooms close the broader Bank evidence. |
| R6.8 | PROVED | Expected-version and expected-fact mutation preconditions lower from the public application contract into exact provider-recompared decision facts rather than host-side check-then-act logic. | A matching precondition commits through the ordinary mutation facade and records the provider-recompared fact. | Relevant drift stales or denies atomically; unrelated drift remains admissible; deleting provider recompare or moving it into the host fails the evidence. | The public mutation contract carries exact expected facts into provider recompare-and-commit. Focused and ordinary Bank mutation courtrooms prove matching commit, relevant-drift stale denial, unrelated-drift admission, and no host-side check-then-act substitute. |
| R6.9 | PROVED | Governed read receipts carry the admitted Milestone 9.10 plan, requirement set, support, strategy, fallback, edge-scan, per-result-neighbor-lookup, basis, work, and typed omission evidence needed to interpret the result. | Covered nested bank views report exact plan and work evidence with zero fallback and zero per-result neighbor lookups. | Growing unrelated entities or result cardinality cannot introduce caller-owned N+1 work, undeclared sorting/filtering, hidden reconstruction, or missing omission posture. | The shared kernel receipt identifies query, parameter binding, graph authority, provider, basis/version, plan, index generations, exact component work, result/truncation counts, released buffer evidence, zero neighbor fallback, and typed omission. Scale courts cover unrelated growth and result growth; governed disclosure courts prove explicit Phase 7 omission decisions rather than silent absence. |
| R6.10 | PROVED | Every bank query family migrates to the installed application-query surface and removes application-local cursor identity, lane-specific query meaning, host pagination/filtering/sorting, repeated child queries, and duplicated projectors where Query owns the generic contract. | A fresh consumer discovers and executes every supported query lane through `bank-domain` plus Query audience facades and documentation. | Source-complete residue, dependency, facade, and documentation-contract checks fail if a workaround or internal import survives or returns. | Every Bank query family enters through domain declarations and Query audience facades. The bank-local cursor, lane operations, query markers, sorts, duplicate projectors, private cause query, and older live lease are deleted. Cargo-metadata dependency checks, facade snapshots, compiler negatives, documentation routing, and source-complete residue protect the final cutover. |
| R6.11 | PROVED | Basis leases, admitted execution reservations, result buffers, and live-maintenance leases are bounded and released on success, denial, cancellation, deadline, overflow, and disposal; opaque continuations retain identity only and hold no runtime resource. | Resource counters return to their pre-execution values after every terminal posture, while a retained continuation consumes no provider, basis, buffer, or live capacity. | Cancellation, timeout, projection denial, abandoned results, and live overflow cannot leak capacity or smuggle a resource lease into a continuation. | Runtime-minted pinned, historical, and preview bases wrap one real Relational lease without adding authority. Checked accounting, pre-allocation charging, continuation identity-only retention, and exact teardown are proved across success, unsupported posture, revocation, cancellation, deadline, projection denial, abandoned results, foreign session, source closure, live overflow, and disposal for every migrated Bank family. |
| R6.12 | PROVED | Runtime Phase 6 evidence preserves the ordinary edit-and-test path by consolidating related integration scenarios and avoiding a new compiled test target for every query family or lane. | One focused application-query integration target covers its scenario modules, and narrow declaration, installation, admission, execution, and bank-domain unit targets remain independently runnable. | Test-target proliferation, duplicate fixture worlds, or broad workspace-only feedback makes this row fail even when behavior is correct. | Framework scenarios remain consolidated in the execution library target; Bank history, preview, continuation, live, and mutation scenarios extend existing domain targets. The final target inventory, focused suites, strict local lint lanes, dirty line cap, and full Query/Bank certification are green without per-family target proliferation. |
| R6.13 | PROVED | Compiler-visible phase progression is the sole constructor of package-declared query, installed query, bound-parameter, admitted-plan, basis-lease, canonical lane-execution, continuation, and governed-receipt authority. Descriptors, digests, support rows, and copied evidence are descriptive only. | Each lawful transition consumes or borrows the exact prior sealed authority and preserves package, runtime, installation, generation, query, principal scope, basis, provider affinity, and canonical executor identity through typed outputs. | Public compile-negative cases prove undeclared query construction, direct installed construction, copied identity, foreign runtime, stale generation, side-built support, skipped admission, parallel executor substitution, and cross-lane proof substitution open no executable door. | Historical, preview, continuation, and live join the same sealed progression. Provider-affine receipts, same-runtime liveness, and move-only plan authority reject copied numbers, foreign evidence, side-built support, skipped phases, cloning, and cross-lane substitution. The complete compiler matrix and final bypass courtroom close the constructor authority. |

### Runtime Hardening Phase 6 QA Correction History

Every correction status below is synchronized to the final Runtime Phase 6
ledger. `CLOSED` means the root defect and all invalidated guarantees passed
their named final-source evidence.

| Finding | Severity | Status | Invalidated guarantee | Root correction required | Closure evidence required |
| --- | --- | --- | --- | --- | --- |
| Q6.1 Installed result-shape lowering discarded structural path identity, allowing distinct nested shapes with repeated entities, relations, or output names to approach the same flattened graph identity. | HIGH | CLOSED | R6.1 | Preserve canonical structural paths and ordered sibling position in the installed graph contract and identity. | `installed_identity_preserves_which_nested_branch_owns_each_field` proves formerly flatten-equivalent branch groupings now produce distinct graph and query identities; equivalent identity convergence remains green. |
| Q6.2 The first application-query admission draft copied a reduced requirement derivation beside the mature Milestone 9.10 planner. Shared enum names did not make it one authority. | HIGH | CLOSED | R6.2, R6.3, R6.9, R6.13 | Extract one normalized planning input and one requirement derivation used by both legacy canonical graphs and installed application queries; delete copied semantics. Seal the resulting review into execution authority only at the owning runtime boundary; do not import a monolith-owned wrapper into a lower crate. | One normalized input and requirement derivation serve both paths. Exact mature/application lowering equivalence, one-axis mutation cases, and runtime-owned binding of the shared review to basis and lane execution pass against final source. |
| Q6.3 The initial Phase 6 ledger represented only unproved work and did not distinguish known defects, causal blockers, or compiler-visible authority construction as an independently failable guarantee. | MEDIUM | CLOSED | Ledger completeness | Add current-result evidence, use `DEFECT` and `BLOCKED` truthfully, and add R6.13. | This revised ledger makes the known implementation defects and causal progression explicit before further code is accepted. |
| Q6.4 Application-query admission accepted caller-constructed support rows and inventory, so a consumer could label absent runtime support `Verified` and receive an admitted plan. | CRITICAL | CLOSED | R6.2, R6.3, R6.6, R6.9, R6.13 | Remove executable plan admission from the lower crate; runtime-owned support and plan admission belong in execution, where the real primary graph and installed runtime authority are available. | The lower executable-plan surface is deleted; only primary-graph execution reviews runtime-owned inventory and mints the private-field plan. Public compiler negatives prove caller-authored descriptive support cannot mint or substitute executable authority. |
| Q6.5 The installed query had no typed authorization-scope binding, while the draft access context carried only an external-principal proof from the lower admission crate. A caller could therefore choose no exact mapped principal/scope pair for Query to revalidate, and a later continuation had no canonical scope meaning to bind. | CRITICAL | CLOSED | R6.1, R6.5, R6.6, R6.7, R6.13 | Make authorization scope an identity-bearing typed part of declaration and installation. Move the access context to execution and require the sealed mapped application principal plus a typed resolved scope of exactly that installed scope type. Revalidate both against the current runtime and request scope before plan admission and every later lane transition. | Scope is a compile-time parameter and installed-identity dimension; execution access requires a mapped principal plus the same typed scope. Scope identity delta, lawful admission, foreign-runtime scope denial, post-admission principal revocation, and identity-field scope staleness all pass. Public compiler cases prove external authentication cannot substitute for mapped principal authority and an institution scope cannot substitute for the installed account scope. |
| Q6.6 The installed `WorthQueryInstalledApplicationReadFamily` was a parallel name-only wrapper, and primary-graph admission used the generic support inventory rather than evidence derived from that runtime. | HIGH | CLOSED | R6.2, R6.3, R6.7, R6.9, R6.13 | Delete the name-only wrapper, use the shared read-graph planning contract and canonical derivation, and derive support only from the installed runtime before minting executable authority. | The wrapper and its module have no source residue. Both graph representations use the shared planning contract, exact lowering equivalence passes, and hostile absent/mismatched runtime-support worlds deny before plan authority. |
| Q6.7 `WorthQueryInstalledApplicationSchema::install_query` accepted an arbitrary caller-built definition and minted installed query authority from the installed package nonce. A host could introduce an undeclared public query, choose scope/disclosure, and bypass domain-owned query meaning after package admission. | CRITICAL | CLOSED | R6.1, R6.4, R6.10, R6.13 | Make erased application-query definitions canonical members of the domain schema/package declaration and package identity. Replace post-installation definition admission with typed reference resolution against that retained declaration; only exact package-declared meaning may mint the installed query proof. | Query meaning enters schema canonical identity; schema closure validates its dependencies and typed projections. Installed resolution accepts the retained exact reference and rejects undeclared or same-name/different-query, parameter, result, or scope types. Exact rebuilt authority validates, while foreign-runtime and successor-generation authority deny before execution. The public host courtroom proves a descriptive definition cannot substitute for an installed query, and production Rust contains no `install_query` residue. |
| Q6.8 Runtime-derived support treated the existence of an order field as proof of `OrderingSupport`, although the primary graph had neither an installed ordering index nor a bounded application-query ordering executor. Runtime ownership of the inventory did not make unsupported capability truthful. | HIGH | CLOSED | R6.2, R6.3, R6.7, R6.9, R6.13 | Represent only ordering mechanisms the installed runtime can actually execute; unsupported ordering must deny before plan authority is minted. The eventual executor must consume the same typed mechanism that produced support evidence. | A field without an ordering mechanism denies plan authority. Supported ordering consumes the exact installed related-entity ordered-seek mechanism; exact order evidence plus deletion and mismatch hostile twins pass. |
| Q6.9 Pinned basis controls were caller-created strings. A pinned one-shot request checked only declared support and tuple coherence; it did not resolve, validate, acquire, or bind runtime-owned basis authority. Preview also exposed descriptive identity despite being unsupported. | CRITICAL | CLOSED | R6.5, R6.6, R6.7, R6.9, R6.11, R6.13 | Replace descriptive basis identities at the execution boundary with an execution-owned resolved basis authority or lease bound to runtime, provider, generation, and lifecycle. Unsupported or invented basis identities must deny before plan construction. | String basis controls and arbitrary tuples are deleted. Current and pinned constructors carry move-only runtime-owned basis authority; mutation-after-pin, foreign, stale-schema, wrong-provider, unsupported, expired, cancelled, release, drop, authorization, later-lane parity, compiler, and exact resource-accounting cases pass. |
| Q6.10 The first one-shot executor revalidated the principal and read the admitted basis under separate primary-graph lock acquisitions. A revocation could commit between those operations and allow one stale-authorized result. | CRITICAL | CLOSED | R6.6, R6.7, R6.11, R6.13 | Validate current principal truth, then basis-scoped entity truth and provider data, within one primary-graph critical section. Keep credential lifetime and request interruption checks on both sides of provider work. | One-shot, continuation, historical, preview, and live all execute provider reads through `execute_authorized_read`. Post-admission principal revocation denies historical execution as `StalePrincipal`; post-admission ownership revocation denies preview as `Authorization(PermissionDenied)`; the existing one-shot, continuation, and real live revocation courtrooms remain green. Every denial releases its exact basis. |
| Q6.11 Provider projection silently omitted a missing or scalar-incompatible declared field while the draft receipt still counted every declaration as projected and reported no omission. A permissive domain projector could therefore return a result under false evidence. | HIGH | CLOSED | R6.4, R6.7, R6.9, R6.13 | Require every non-omissible installed projection path to produce one authoritative value of its installed scalar family before domain projection or receipt construction. Governance-owned omission must later use an explicit typed omission posture rather than absence. | Projection fails at the exact result path on missing or incompatible truth and charges work only after complete observation. Corruption/deletion hostile twins and Phase 7 typed omission courtrooms pass. |
| Q6.12 Admitted parameter identity used Rust `Debug` formatting in an independently maintained pre-canonical string grammar, which is not a canonical semantic encoding and makes continuation/selectivity identity representation-sensitive. | HIGH | CLOSED | R6.1, R6.2, R6.3, R6.5, R6.13 | Prepare a named, versioned Foundational canonical basis from sorted parameter names, canonical scalar-family names, and typed canonical values. Retain that prepared artifact, derive the compact descriptor only afterward, and make continuation readmission compare ready artifacts through Foundational before acquiring a basis. | `WorthQueryApplicationParameterCanonicalArtifact` retains a ready basis under `worth-query.application-query-parameters` and rule `worth-query-application-query-parameters-v1`; entries contain binding count, sorted name, canonical scalar name, and typed canonical value. The continuation retains that ready artifact rather than a parameter digest string. Resume prepares one fresh artifact and calls its Foundational structured-equivalence boundary before plan completion; direct comparison proves convergence and one-value mismatch, source residue rejects digest-string affinity plus `Debug` and the old grammar, and the changed-parameter twin proves the Query basis acquisition count does not change. |
| Q6.13 The application-query implementation shared planning vocabulary but had no explicit installed read-family binding, while lookup, scope intersection, projection, cardinality, and work accounting were physically owned by the one-shot lane. Adding continuation, history, preview, or live beside it would invite separate semantic engines behind one canonical-looking admission proof. | CRITICAL | CLOSED | R6.2, R6.7, R6.9, R6.10, R6.13 | Install a substantive binding from application-query identity to the canonical read-graph meaning and plan chain. Extract provider execution semantics into one lower application-query kernel consumed by every supported non-live lane; keep lane lifecycle/admission separate and do not import a monolith-owned higher-layer executor downward. Preserve the already-correct fresh-authentication, typed projection, basis-release, and governed-receipt requirements around that kernel. | Q6.29/Q6.32 prove real mature/application planning equivalence. One-shot, continuation, historical, and preview consume one non-live kernel, authorization boundary, root selection, tree materializer, projection, work ledger, receipt builder, and result shape; live retains the same installed meaning with separately admitted maintenance. Source/dependency residue rejects a lane-local semantic executor. |
| Q6.14 Nested application-query result relations carried a nested shape but no cardinality. A declaration could not distinguish optional-one, exactly-one, and many-valued relation results, so structurally different result meanings could share identity and buffer/continuation planning could not be exact. | CRITICAL | CLOSED | R6.1, R6.3, R6.4, R6.5, R6.7, R6.9, R6.13 | Make every nested result relation declare typed cardinality; carry it through canonical declaration identity, installed graph identity, normalized planning identity, result-buffer requirements, execution, and domain projection. Remove cardinality-free authoring so compiler-visible construction cannot omit the choice. | Sealed optional-one, exactly-one, and many selectors carry through declaration, identity, planning, execution, projection, buffer, and continuation meaning. Compiler hostility, real Bank paths, and the complete one-axis identity matrix pass. |
| Q6.15 Installed result identity preserved nested paths, but `WorthQueryApplicationProjectionRow` flattened observed values to `(entity, aspect, field)`. Repeated sibling branches that projected the same typed field therefore collided, and domain projection had no typed relation/path accessor for optional, one, or many nested results. | CRITICAL | CLOSED | R6.1, R6.4, R6.5, R6.7, R6.9, R6.10, R6.13 | Introduce query-typed result field and relation selectors whose identities include the declared branch path and cardinality. Preserve a tree or path-keyed row set through provider execution, and expose cardinality-correct typed projection access without output-name parsing or host closures. Root-only projection must use the same mechanism. | The runtime preserves a query/slot-keyed tree and batch-traverses branch frontiers. Real Bank optional/one/many paths, slot identity deltas, duplicate/invented slot denial, compiler hostility, and source-wide flattened-path residue all pass. |
| Q6.16 Query-bound result relations encode only declared `From -> To` traversal, and the shared tree materializer can batch only outgoing adjacency. Real bank account activity requires `Account <- PostingAccount - Posting <- JournalPosting - Journal`; without a typed reverse direction, the bank must retain repeated reverse lookups or invent a parallel host projector. | CRITICAL | CLOSED | R6.1, R6.2, R6.3, R6.4, R6.7, R6.9, R6.10, R6.13 | Add a sealed traversal-direction marker to the query relation selector, make the result-shape parent/child types direction-correct at compile time, preserve direction in declaration/install/planning identity, and execute reverse branches through one bounded frontier batch owned by Relational/Query. Remove the forward-only API rather than retaining compatibility shims. | Sealed forward/reverse endpoint laws carry through identity, planning, projection, and the shared kernel. Real reverse Bank traversal, zero neighbor fallback, direction-only requirement delta, endpoint compiler denial, and final forward-only/host-lookup residue pass. |
| Q6.17 Application-query ordering is declared with a raw schema field rather than a query-bound result selector. Repeated branches can therefore make the intended ordered collection ambiguous, and no installed path/slot authority connects an eventual bounded ordering mechanism to the field that produced support evidence. | CRITICAL | CLOSED | R6.1, R6.2, R6.3, R6.5, R6.7, R6.9, R6.10, R6.13 | Bind every ordering term to an exact query result selector/path, carry that identity through installation and planning, and make admission support name the same bounded runtime mechanism consumed by execution. Remove raw-field ordering authoring. | Raw-field ordering is removed. The real bank query binds ordering to its posting-sequence slot; continuation installation lowers that exact collection path and ordering to a versioned related-entity index consumed by bounded page execution. Reversed fixture order, duplicate-key tie ordering, absent/forged slot denial, foreign-query compile denial, generation mismatch denial, direct-child-only continuation ordering, and deletion of the final host sort pass all pass. Removing the installed continuation contract emits no ordering support, deleting the ordering inventory row denies plan review, and a one-unit nested-query work limit returns no plan authority. |
| Q6.18 Installed application-query admission proved only that the principal and typed scope were genuine and current; the installed query carried no required ability or authorization-policy identity. `Public` disclosure therefore permitted any mapped principal to select any valid entity of the scope type, while non-public disclosure denied all execution. | CRITICAL | CLOSED | R6.1, R6.4, R6.5, R6.6, R6.7, R6.10, R6.13 | Make the exact scoped authorization requirement an identity-bearing part of domain query declaration and installation. Admission must evaluate the existing installed ability/policy graph for the mapped principal and exact scope before minting plan authority, preserve that authority into execution, and freshly re-evaluate it at every later lane boundary. Disclosure remains a separate Phase 7-strengthened decision and may not stand in for access authorization. | The real bank account-activity query consumes installed `ViewAccount`; owner/stranger, authorization identity delta, package closure, wrong-scope compiler denial, and host-authority residue pass. Continuation, historical, preview, and live each freshly rerun the installed ability. Post-admission revocation denies every lane at its owned boundary and returns resources to baseline. |
| Q6.19 The one-shot executor required every exact-scope root to repeat its identity as one equality predicate and then unconditionally indexed `predicates()[0]`. A lawful installed query whose exact authorized scope was already its root denied as `ExecutionShapeUnsupported`, pressuring domains to invent redundant parameters and `PredicateSupport`. | HIGH | CLOSED | R6.1, R6.3, R6.4, R6.6, R6.7, R6.9, R6.13 | Make root selection a generic bounded responsibility with explicit direct exact-scope and indexed-predicate strategies. Direct scope must consume the freshly validated typed scope, charge bounded work, and never widen into enumeration; the existing indexed path must remain unchanged. | `exact_scope_root_executes_without_an_invented_predicate` proves zero predicate-support requirements and one direct candidate; `a_different_root_cannot_widen_exact_scope_into_enumeration` denies before plan authority; the pre-existing indexed application-query family remains green. Real bank account activity executes through the direct strategy, while its stranger still denies before plan authority. |
| Q6.20 Basis-release tests reached private Relational snapshot handles through `#[cfg(test)]` accessors on production pinned proofs and admitted plans. The tests observed real teardown, but the harness changed the production surface under test and coupled Query evidence to lower-runtime representation. | HIGH | CLOSED | R6.11, R6.12, R6.13 and test-harness integrity | Delete both test-only accessors. Relational's existing snapshot authority must own a generally valid, read-only liveness observation keyed by the already-public opaque execution-basis identity, including runtime affinity; Query tests may clone descriptive identity but never snapshot authority. | `SnapshotAuthority::execution_basis_is_live` validates runtime identity and exact registry lease identity. Relational public-API tests prove owned identity is live until release or drop and a foreign runtime cannot observe it as live. All Query lifecycle tests now use `basis_identity()` or `identity()` plus that owner boundary; source residue contains no `test_snapshot_handle`, `test_basis_snapshot_handle`, or application-query production `cfg(test)` branch. |
| Q6.21 Application queries did not declare which `Many` result collection owns continuation, and the only admitted ordering mechanism heap-sorted an already materialized collection. The bank's host cursor therefore reread, sorted, and offset-skipped the complete activity history on every page; wrapping that path in a Query cursor would have preserved the same unbounded repeated work behind a stronger name. | CRITICAL | CLOSED | R6.1, R6.2, R6.3, R6.5, R6.7, R6.9, R6.10, R6.11, R6.12, R6.13 | Make one query-bound `Many` collection an identity-bearing continuation target. Install an exact related-entity ordered-seek mechanism whose versioned generation is consumed by page execution, and make continuation admission require that mechanism. A continuation carries only the canonical last-row boundary and identities; every page freshly reacquires its basis and authorization. Delete the bank cursor and offset page projector; the complete family migration owns deletion of any surviving non-page host sort. | Query-bound continuation declaration, installed contract, exact versioned Relational seek, fresh readmission, shared lower materializer, and the paged bank cutover are implemented. Lower tests prove duplicate-key ordering, parent-bound boundary rejection, and expected-generation denial. Seven real bank cases prove page/one-shot parity, mutation-between-pages basis stability, revocation denial, cross-scope and cross-runtime denial, bounded admission, index-only ordering, and released receipts. Compile-fail docs reject token clone/construction, and active source contains no bank cursor, offset skip, host page projector, or the ordinary/live duplicate later closed by Q6.23. The independent `retained_continuation_holds_identity_but_no_relational_basis` courtroom observes the exact page basis live before execution, released while the opaque continuation is retained, and still released after token disposal. |
| Q6.22 Resumed ordered pages used binary search to find the continuation boundary, but `examined_entry_count` and the public receipt counted returned rows only. The ordinary lane therefore hid logarithmic seek work behind an apparently constant page cost. | HIGH | CLOSED | R6.3, R6.9, R6.12 and performance honesty | Count every ordered-index comparison performed while locating a continuation boundary and add it to returned-row verification work before Query charges its shared work ledger. Preserve zero seek comparisons on an initial page. | The focused Relational test proves first-page seek comparisons are zero and resumed `examined_entry_count == returned rows + seek comparisons`; the real bank consumer proves a resumed receipt exceeds returned-row count. All seven activity-history cases pass. |
| Q6.23 The first pagination cutover deleted the host cursor and offset projector but left the public legacy `queries::account_activity` executor. That executor still materialized and host-sorted the complete activity collection, and its query marker also fronted the legacy live lane. Calling account activity fully migrated would therefore hide duplicate canonical meaning and an available unbounded workaround. | CRITICAL | CLOSED | R6.4, R6.7, R6.9, R6.10, R6.13 | Migrate ordinary, continuation, and live account activity to one installed `AccountActivityQuery`; make the domain bind committed causes to exact typed target identity; make Query own the bounded cause queue, fresh admission, targeted canonical projection, receipt, and teardown; delete the legacy operation, marker, projector, sort, and lane facade without aliases. | `ordinary_reads`, `activity_history`, and `live_activity` pass real owner/stranger, page parity, cross-scope/runtime, revocation, cancellation/deadline, unrelated commit, two-cause retention, constant-work, buffer ceiling, and overflow courtrooms. Active production, test, and public-documentation residue contains none of the deleted symbols; only this historical defect record retains their names. Q6.21 independently closes continuation-resource liveness for the same canonical path. |
| Q6.24 Installed live-contract validation compared the target identity field path directly with its containing continuation collection path, so every lawful nested target denied installation even though the exact projection was installed. | CRITICAL | CLOSED | R6.1, R6.5, R6.7, R6.10 | Require the target projection's structural parent path to equal the installed continuation collection path; nested and sibling paths must not alias it. | Focused installation tests prove exact-parent acceptance and nested/sibling rejection; all six real live account-activity courtrooms open through the installed target. |
| Q6.25 Account activity declared a 256-unit maximum live delivery budget while the shared canonical planner's conservative intrinsic minimum for its three-relation result graph is 1,537 units. The installed contract therefore authorized no executable control value. | HIGH | CLOSED | R6.3, R6.7, R6.9, R6.11 | Preserve the single planner and separate admitted maximum from actual work. Declare a bounded 2,048-unit envelope, reject caller widening, and continue charging exact targeted work in the receipt. | All seven real live cases pass. `caller_cannot_enlarge_the_installed_live_work_ceiling` varies only requested work from 2,048 to 2,049 and receives `WorkLimitExceedsInstalled`; the separate buffer-ceiling twin still receives `BufferCapacityExceedsInstalled`. Two deliveries after sixteen prior commits retain identical target-index and adjacency work with zero fallback/per-result lookup. |
| Q6.26 Runtime inventory verified the installed live contract and both exact equality indexes but emitted Milestone 9.10's default `RequiresAccessCapabilityRegistration` posture for `LiveMaintenanceSupport`, so plan review denied every Phase 6 live query. | CRITICAL | CLOSED | R6.2, R6.3, R6.7, R6.13 | Keep the generic default conservative. Only after the application runtime verifies the exact installed live contract and scope/target indexes may it emit Query-runtime-owned, runtime-maintained, verified/available maintenance support. | `live_support_requires_the_exact_contract_and_both_equality_indexes` proves the exact world emits Query-runtime/runtime-maintained/verified/available support, while independently removing the contract, scope index, or target index emits no live-support row. `application_query_live_target_requires_equality.rs` proves an unindexed typed target cannot declare live meaning. The real seven-case bank courtroom proves that the resulting support passes ordinary graph-read plan review and opens executable live authority. |
| Q6.27 The framework still publicly exported the pre-application-query `WorthQueryLiveEffectLease`. Any admitted operation could select an installed effect, supply a caller-authored payload filter, and open live execution without resolving the package-declared query, its typed live scope/target contract, query-bound projection, or canonical query authorization requirement. | CRITICAL | CLOSED | R6.4, R6.7, R6.10, R6.11, R6.13 | Delete the operation-admission live lease and its public exports without aliases or compatibility shims. Retain only non-authority source/queue machinery actually consumed behind the installed application-query lease, and make that ownership explicit. | The old lease, outcome, courtroom, facade exports, open methods, and caller projection filter are deleted. `legacy_live_effect_authority_is_unavailable.rs` proves the public type cannot be imported; active Rust residue is zero. The consolidated 28-case compiler target passes warm in 1.33 seconds, the seven-case bank live consumer passes in 0.20 seconds, and strict execution and bank-server Clippy pass. Shared source, bounded queue, and request controls remain private lower machinery consumed only by the installed application-query lease. |
| Q6.28 The application-query live lease reports source overflow or closure without immediately terminalizing its Relational basis, Bridge reservation, Signal request, or queue. Its disposal path pops every buffered occupancy while ignoring release failure and then discards Bridge finalization failure. Because opening forks and drops a request-local Bridge lane handle, Query also lacks an owner-valid independent observation that those resources returned to baseline. | CRITICAL | CLOSED | R6.7, R6.11, R6.12, R6.13 | Make live authority explicitly Signal-thread-affine at compile time; preserve queue occupancy and Bridge basis authority on recoverable teardown failure rather than discarding it; terminalize overflow, closure, authorization staleness, cancellation, and deadline at the outcome boundary; and add a generally valid read-only Runtime Bridge lifecycle observer minted by the owning basis, not a Query test accessor. | Runtime Bridge now mints a read-only owner observer that projects reservation activity, Signal terminal posture, and exact managed-queue pressure without exposing Signal authority. Query lifecycle courtrooms use that independent boundary plus Relational `SnapshotAuthority` to prove live state and baseline after explicit close, cancellation, deadline, source close, overflow, and abandoned drop; the real bank authorization-revocation case terminates immediately, drains its buffer, and reports `Closed` on the next poll. Queue teardown preserves failed occupancy at the front, and Query retains its lower basis when queue release or Bridge finalization fails. `application_query_live_lease_is_thread_affine.rs` proves the Signal-owning lease is not `Send`. The focused warm runs complete in 0.01 seconds for Bridge, 0.04 seconds for Query lifecycle, and 0.20 seconds for the bank consumer. |
| Q6.29 The only courtroom named as legacy/application planning equivalence constructed `LegacyEquivalentGraph` by wrapping the installed application graph and changing its source digest. It never constructed the real mature `WorthQueryReadGraph`. Production mature and application paths also lowered into `WorthQueryCanonicalGraphReadPlanningInput` through separate adapters, so schema basis, request cardinality, traversal operator, result pressure, and selectivity meaning could drift while the synthetic courtroom remained green. | CRITICAL | CLOSED | R6.2, R6.3, R6.9, R6.12, R6.13 | Use one descriptive canonical graph-planning semantics boundary that opens no executable door. Lower both the installed application graph and admitted mature graph-access shape into it, then derive authority-bound requirements through one implementation while keeping schema authority, caller controls, and runtime support mechanisms explicit. Delete or honestly rename synthetic wrapper evidence. | `LegacyEquivalentGraph` is gone; the retained source-digest test is honestly named and scoped. The exact real courtroom constructs one mature family and one installed application query independently, proves their graph digests and schema bases remain unequal, then proves equal canonical query meaning and structural requirement evidence. It exposed and closed missing application proof support and mature fanout undercounting. Source-local hostile twins cover source digest, relation presence/direction/depth, predicate presence, ordering direction, cardinality, result pressure, and foreign schema basis; Q6.32 adds the exact cross-source one-axis matrix. Truthful ordering mechanisms and maximum-cardinality evidence remain separately asserted rather than normalized away. Installation proves binding identity is the canonical planning identity and cardinality changes it. The application admission suite passes 7/7, execution application-query 29/29, and the real mature/application courtroom 3/3; R6.2's runtime inventory, budget, plan-review, and residue obligations are proved. |
| Q6.30 R6.1 cited public foreign-schema compiler denial, but the fixture's `ForeignSchema` did not implement `ApplicationSchema`; it failed before reaching query/schema type affinity and therefore could not prove the claimed boundary. | HIGH | CLOSED | R6.1 and compiler-evidence honesty | Make both schema markers lawful application schemas, then pass a `WorthQueryInstalledApplicationQuery<Schema, ...>` to `WorthQueryInstalledApplicationSchema<ForeignSchema>::validate_installed_query`. Accept only the diagnostic showing the exact expected `ForeignSchema` versus found `Schema` authority mismatch. | The repaired fixture reaches E0308 at `validate_installed_query`, with the expected and found installed-query schema parameters named explicitly. The consolidated 29-case `application_schema_compile_fail` courtroom passes warm in 1.33 seconds. |
| Q6.31 `WorthQueryGraphReadBudget` carried `max_inline_intermediate_set_size` in identity and public observations but `check_supported_cost` compared only index and result bytes. An oversized traversal workset could therefore pass the declared budget dimension, and the application path had no honest request-scoped replacement for the generic 16-unit default. | HIGH | CLOSED | R6.2, R6.3, R6.6, R6.9, R6.12, and budget honesty | Enforce every declared budget dimension in the one canonical checker. Preserve the runtime's byte ceilings while deriving the application intermediate ceiling privately from its typed `maximum_work` control; do not accept a caller-authored budget or inflate a global constant. Validate total estimated work before returning plan authority. | The mature cost courtroom proves intermediate pressure alone changes the budget class to `ExceedsInlineEphemeralBudget`. An admitted application plan reports the exact 10,000-unit request ceiling, while the same real nested query with a one-unit ceiling returns `WorkLimitExceeded` and no plan. All 26 application-query execution tests pass. |
| Q6.32 Q6.29's closure prose claimed hostile coverage for all planning dimensions, but the final real mature/application courtroom compared only one baseline while source-local wrappers carried the one-axis variations. That could not prove both independently constructed sources interpreted the same delta, and R6.2 correctly remained proof-open despite the broader wording. | HIGH | CLOSED | R6.2, R6.3, R6.9, R6.12, R6.13, and ledger evidence honesty | Keep the independently constructed real mature family and installed application query as the two roots, then compare their normalized planning deltas one axis at a time. Preserve truthful source-specific schema, mechanism, and request-bound evidence rather than forcing full digest equality. | `mature_and_application_sources_share_every_one_axis_planning_delta` covers direction, depth, predicate presence, ordering direction, root cardinality, fanout, relation-cardinality-driven result pressure, maximum result count, and live lifecycle. The three-test real equivalence courtroom passes; source-local production derivation twins remain as independent adapter evidence. |
| Q6.33 The mature cost-model memory courtroom still expected a 1,024-byte narrow result buffer for a two-relation frontier collection after Q6.29 correctly made mature fanout derive `CollectionWide`. The broad QA gate therefore contradicted the canonical planning rule and would have rewarded regressing the fanout fix. | MEDIUM | CLOSED | R6.2, R6.3, R6.9, R6.12, and test-oracle independence | Keep the fixture visibly multi-relation, assert its independently required `CollectionWide` row before estimating cost, and expect the canonical 2,048-byte wide buffer contribution. | The cost courtroom now establishes the result-pressure cause before checking every named memory bucket. `mature_fanout_alone_adds_proof_support_and_wide_result_pressure` remains an independent flat/fanout twin. |
| Q6.34 The application result-buffer plan used one fixed 2,048-byte allowance regardless of typed maximum result count, while execution copied the full installed field/relation contract into every result node and had no owner-visible buffer lifecycle. Exact accounting showed a lawful seven-node nested result retained 3,712 bytes, so either ordinary queries failed after admission or nested allocations remained unmeasured. | HIGH | CLOSED | R6.3, R6.9, R6.11, R6.13, and memory honesty | Preserve cardinality-scaled planning and runtime-owned lifecycle, but replace fixed-size per-selector hashes with compact installed slot indices and move capacity admission ahead of every owned result allocation or clone. | Planning scales result pressure with typed cardinality. Installed projections/relations retain one shared structural slot key behind `Arc`; result rows clone that pointer-sized identity rather than per-row contracts or hashes. The move-only reservation and checked allocation constructor cover every retained result vector and scalar clone. Real seven-node nested projection succeeds with exact receipt/observer peak agreement, while the 25 KB hostile scalar denies without crossing the admitted peak and returns baseline. |
| Q6.35 Application-query definition identity, installed graph identity, and installed query identity directly SHA-hash independently maintained strings, numeric formatting, and enum `Debug` output. This bypasses Foundational canonical basis/comparison, makes digest encoding the semantic truth, and leaves three grammars free to drift while the digest is also carried as installed identity. | CRITICAL | CLOSED | R6.1, R6.2, R6.3, R6.9, R6.13, and cross-crate identity honesty | Prepare one ready Foundational canonical basis under a named Query domain and explicit rule version for the complete portable query meaning. Derive compact digests only from that ready basis, preserve Query's stronger installed authority separately, and use Foundational structured comparison for convergence/mismatch evidence. | Declaration, graph, planning, continuation, and installed-query canonical artifacts now form one retained Foundational basis chain. Compact digests derive from ready basis material; the installed nonce remains isolated in `authority_seal.rs`. Structured Foundational comparison proves real mature/application equivalence and mismatches, including the cross-source one-axis matrix. `canonical_basis_residue` covers every former identity owner and rejects SHA, text hash helpers, `Debug` grammar, or reconstructive planning derivation outside the isolated authority seal; declaration, installation, admission, and execution estates pass. |
| Q6.36 The 6.3 selector correction derives SHA-256 identity and allocates `format!` strings every time a typed field or relation selector is constructed. Domain projectors construct those selectors on the ordinary projection path, so compiler-visible integrity added cryptographic and heap work to every warm result. | HIGH | CLOSED | R6.3, R6.9, R6.12, R6.13, Q6.34, and warm-path honesty | Keep selectors as zero-allocation typed/static references. Result nodes carry compact indices into the already installed projection/relation contract, and the Query-owned projection row checks every selector dimension against that borrowed installed contract without digest or canonical-basis work. Delete the hash identity types rather than retaining aliases. | Selector identity is a stack-built structural key containing the exact query/slot `TypeId` and field/relation contract dimensions. Installation prepares and shares the corresponding key once behind `Arc`; result rows carry only the shared identity plus value. `invented_selector_contract_denies_domain_projection` proves a forged field contract opens no projection, while source residue across read execution and projection contains no SHA, `format!`, canonical preparation/comparison, or digest work. |
| Q6.37 Result-buffer enforcement computes recursive retained bytes only after materialization cloned every scalar and allocated every nested vector. The 25 KB hostile case therefore proves typed denial and cleanup but not a bound: the forbidden allocation already occurred before `ResultBufferLimitExceeded`. | HIGH | CLOSED | R6.3, R6.9, R6.11, R6.12, R6.13, Q6.34, and peak-memory honesty | Move the move-only reservation into tree materialization. Charge node, field, relation, nested-row, and owned scalar-clone capacity before allocation or clone; separately observe rejected capacity versus actually retained peak; release transient redistribution buffers as ownership moves. Post-hoc recursive measurement may remain a test oracle but cannot enforce the bound. | All three tree-materialization callers must pass a move-only reservation. One checked constructor owns byte multiplication, claim, allocation, and exact capacity verification for root nodes, fields, relation slots, and final child rows; owned scalar capacity is claimed before clone. Child redistribution drops and releases its temporary combined buffer before returning, and final recursive measurement must exactly equal retained claims. The 25 KB test observes `peak_rejected_bytes` above the admitted limit while `peak_observed_bytes` stays at or below it; nested success, forged projection, post-admission cancellation, continuation, and a real committed live delivery all return active buffers and retained bytes to zero. |
| Q6.38 Normalized read-planning identity omitted the installed schema basis. Identical graph shape under a foreign schema therefore converged to the same planning digest even though requirement identity and authority must remain schema-affine. | HIGH | CLOSED | R6.1, R6.2, R6.3, R6.13, and schema-affine planning | Include `schema_basis_digest` as a named entry in the one Foundational normalized-planning basis; do not reintroduce source graph digest or selector-type noise that intentionally normalizes away. | Installation directly compares two independently compiled graphs with identical shape and distinct schema bases through Foundational and observes mismatch. Admission's one-axis planning courtroom proves foreign schema changes read-graph and access-shape identity while selector-slot-only source noise still converges. The full 127-case installation and 89-case admission estates pass. |
| Q6.39 Application-query basis and result-buffer lifecycle observations used wrapping atomic increments, while release underflow was debug-only. A sufficiently long-lived runtime or accounting defect could wrap to a false baseline in release builds and invalidate zero-residue evidence. | HIGH | CLOSED | R6.11, R6.12, R6.13, and lifecycle evidence honesty | Centralize checked atomic acquire/release transitions for authoritative live counts and retained bytes; use saturation only for lifetime diagnostic totals that cannot deny execution. Overflow or underflow must leave the observed count unchanged. | `resource_lifecycle/lifecycle_count.rs` owns the checked transitions. Owner tests prove rejected overflow and underflow preserve the prior value, lifetime acquisition count saturates, and global result-buffer overflow returns denial without local retention or a false successful peak. The 353-case execution estate and all continuation/live baseline courtrooms pass. |
| Q6.40 Historical index reconstruction and public version reads could fall back to a freshly built partial visibility state after old live metadata had been trimmed. A historical query could therefore return incomplete truth instead of typed unavailability, and published snapshot handles did not participate in the retention fence. | CRITICAL | CLOSED | R6.7, R6.11, R6.13, and historical truth honesty | Make published snapshots and execution bases bounded retention authorities; rebuild historical indexes only from retained/reconstructed truth; remove partial-state fallbacks; and keep raw version reads crate-internal so copied version numbers open no public read path. | Published and execution-basis versions now participate in the protected set. Pruned publication handles return unavailable and cannot mint a basis. Historical entity-field, relation-field, and related-order indexes execute exact retained generations after update/deletion, with parity against storage. Account activity reads a real prior bank commit with the same canonical result meaning. |
| Q6.41 Preview basis admission copied the Runtime Bridge session identity and then discarded session liveness while lowering into the generic Query lease. An explicitly discarded or abandoned preview session could leave an already admitted Query plan executable. | CRITICAL | CLOSED | R6.7, R6.11, R6.13, and preview authority progression | Runtime Bridge owns an opaque liveness owner/observer/guard. Query may carry the observer but cannot construct it or keep the session alive. Preview execution holds the Bridge read guard through the locked provider-read linearization point; discard, promotion, or drop takes the write side and invalidates subsequent execution. | Bridge tests prove explicit discard and abandoned handle drop invalidate observers. Query proves foreign sessions mint no basis, discarded sessions invalidate admitted plans as `StalePreviewSession`, cancellation releases the basis without closing the still-active session, and post-admission authorization revocation denies preview before projection. |
| Q6.42 The first bank historical adapter made `worth-query-host` depend on and re-export Runtime Bridge so it could construct branch/commit selectors from a flattened `u64` bank receipt. This violated the exact audience-facade contract and allowed cross-runtime commit-number collisions to select local history. | CRITICAL | CLOSED | R6.7, R6.10, R6.13, and audience-facade honesty | Keep `worth-query-host` as the exact configured Query leaf. Query commit receipts retain provider-runtime affinity; the internal provider receipt grammar and idempotent replay preserve it. Public historical selection consumes the typed Query receipt, while raw Bridge selectors remain in-crate test-only. Historical basis admission compares receipt affinity with the real Relational lease before authority escapes. | Boundary-check and agent-context pass with no host-to-Bridge dependency. A real same-runtime bank receipt reads prior activity. A hostile foreign receipt with an intentionally colliding commit ordinal denies as `ForeignHistoricalReceipt`. Query and bank strict local Clippy pass. |
| Q6.43 Tree materialization reused the caller's root `maximum_result_count` as a cumulative cap over root and nested dependency records. An exactly-one account summary therefore denied when two postings were needed to compute its balance, even though work and result-buffer limits already bounded that internal expansion. | HIGH | CLOSED | R6.3, R6.6, R6.9, R6.10, and result-budget honesty | Enforce result count at root selection and at the explicitly continued collection only. Charge every nested dependency record and field to the admitted work limit and pre-allocation result-buffer reservation, without relabelling dependency count as consumer result count. | `root_result_limit_does_not_cap_nested_dependency_records` returns one root while projecting seven charged records under a one-result limit. The real bank account-summary courtroom returns one balance derived from multiple postings, reports one result, zero fallback, and zero per-result neighbor lookups. All 56 application-query execution tests and all eight consolidated ordinary-read consumer tests pass. |
| Q6.44 The graph-read memory estimate added proof bytes to `index_bytes`, then compared that subtotal with `max_inline_index_bytes` even though 9.10 declares index, result, and proof bytes as separate cost dimensions. A lawful payment detail needed 4,928 index/workset bytes plus one bounded 256-byte proof artifact and was falsely denied against the 5,120-byte index ceiling. | HIGH | CLOSED | R6.2, R6.3, R6.6, R6.9, and cost-dimension honesty | Keep proof bytes independently estimated and included in total memory, but exclude them from the inline index subtotal. Preserve hostile denial when actual index bytes exceed the ceiling. | `proof_memory_is_counted_without_consuming_inline_index_capacity` proves exact index, proof, result, and total accounting; an exact index ceiling admits while a one-byte-smaller ceiling denies. The real bank payment-detail consumer returns its installed nested result with zero fallback and zero per-result neighbor lookups. All six mature cost-model tests, nine application-planning tests, 56 execution application-query tests, eight bank ordinary-read consumers, and strict Query/bank no-dependency Clippy pass. |
| Q6.45 Application-query root selection originally admitted only a scope entity as its result root, leaving account discovery to a bank-local union. | CRITICAL | CLOSED | R6.1, R6.2, R6.3, R6.6, R6.9, R6.10, R6.12, R6.13, and account-discovery authority | Query now owns typed, direction-checked root-path unions, canonical union/dedup/order meaning, bounded lower relation reads, and global result admission. The bank-local discovery authority is deleted. | Disconnected and wrong-direction compile denials, canonical one-axis identity twins, exact planning rows, unrelated-world invariance, duplicate convergence, exhaustion, zero-fallback work evidence, and source residue are recorded in the dedicated Runtime Phase 6 closure ledger. |

## Runtime And Bank Phase 2 QA Correction History

| Finding | Status | Invalidated guarantee | Root correction | Closure evidence |
| --- | --- | --- | --- | --- |
| Q2.1 Host could call doc-hidden inherent methods to inject/extract a raw Relational graph or publish while retaining broad execution authority. | CLOSED | R2.4, R2.4a, B2.4 | Raw preparation, publication, and handle retention moved to `worth-query-execution::facade::integration`; host publication consumes runtime and installation authority into the typed terminal. | Host compile-fail docs reject raw handle extraction and broad publication; ordinary shared-root composition still passes through the integration facade. |
| Q2.2 External-principal Debug derived issuer and subject through candidate/proof wrappers. | CLOSED | R2.1, R2.3, R2.6, B2.6 | `WorthQueryExternalPrincipalIdentity` now owns a value-redacted Debug implementation. | Owner and admitted-proof tests assert issuer, subject, and attribute values are absent. |
| Q2.3 ID-token access-token binding and introspection client affinity were optional. | CLOSED | B2.2, B2.6 | The adapter verifies `at_hash` when present and always requires exact introspection client ID, issuer, and subject affinity; cold certification can cross token halves without exposing them. | The real two-user browser courtroom proved both original credentials admit and the crossed pair fails with `BindingMismatch`. |
| Q2.4 Setup could leave a secret-bearing directory before the Docker cleanup guard existed, and teardown did not inspect networks. | CLOSED | B2.5 | Docker ownership now begins immediately after directory creation; teardown audits containers, volumes, networks, and the directory. | The exact lane passed its real intentional-unwind prelude and clean full-world startup; the independent post-run audit also found no resource or fixture-directory residue. |
| Q2.5 Wrong-issuer evidence also changed audience, so its oracle could pass while issuer checking was broken. | CLOSED | B2.2 | The alternate real issuer is now validated with the primary token's actual client ID and trusted shared signing key; issuer is the only hostile delta. | The exact real lane produced `CredentialRejected` with zero JWKS refresh. |
| Q2.6 Authentik configuration accepted six interchangeable positional strings at a security-sensitive boundary. | CLOSED | B2.1, B2.2, B2.6 | The adapter now exposes only a named-field configuration builder and validates every required field before construction. | Protocol-boundary tests prove missing and invalid security fields deny, client-secret Debug output is redacted, and no positional constructor call remains. |
| Q2.7 Authentik advertised `host.docker.internal` as its issuer while publishing only on host loopback, which could make the identical issuer unreachable from Selenium. | CLOSED | B2.1, B2.5 | The dynamic Authentik port now listens on all host interfaces while Selenium remains host-loopback-only; both the host adapter and browser use the same `host.docker.internal` issuer. | Real host discovery and Selenium browser authorization passed against the same issued token and exact HTTPS issuer. |
| Q2.8 The workspace Rust line-cap guard did not scan the new bank-world crates, and three touched Query/Relational files had crossed the cap. | CLOSED | R2.1, R2.2, R2.5, B2.1-B2.6 | The gate now includes `worth-query-bank-world`; principal-binding references, index facade exports, and schema authority snapshots were split into named owners without changing audience paths. | The tracked-plus-untracked dirty audit covers every changed Rust file and leaves no unallowlisted file over 400 lines. |
| Q2.9 The first Q2.3 correction required `at_hash` even though Authentik's valid Authorization Code token response omits it. | CLOSED | B2.2 | The adapter follows the provider and pinned OpenID Connect client contract: verify `at_hash` when present, then require online introspection to match the validated ID token's client, issuer, and subject. | A real Authentik credential admitted and crossed halves from two real credentials failed with `BindingMismatch`. |
| Q2.10 The cold fixture advertised an HTTP issuer, so the RFC 7009 client rejected revocation before making the real request; configuration also admitted insecure or cross-origin secret-bearing control endpoints. | CLOSED | B2.1, B2.2, B2.5 | The courtroom now publishes Authentik's real 9443 TLS listener and confines ephemeral self-signed trust to certification-gated clients. Ordinary configuration requires HTTPS and issuer-origin affinity for introspection and revocation. | Protocol-boundary tests deny HTTP and cross-origin endpoints; the exact cold Docker courtroom passed discovery, authorization, introspection, and revocation against one HTTPS issuer. |
| Q2.11 Enabling `cold-certification` weakened the normal adapter HTTP client, so Cargo feature unification could make ordinary identity installation trust invalid certificates. | CLOSED | B2.2, B2.6 | Ordinary discovery now always constructs a strict client. Invalid-certificate trust exists only behind `cold_certification::install_identity`, whose internal adapter construction is unavailable outside the feature-gated namespace. | Strict Clippy passed under the cold feature, and the exact real courtroom proved ordinary `AuthentikBankIdentity::install` rejects the same self-signed issuer immediately before the explicit cold install succeeds. |
| Q2.12 A credential could expire after skew-tolerant ID-token verification but before candidate construction, and the adapter collapsed Query's typed invalid time range into `ProtocolViolation`. | CLOSED | B2.2 | The adapter checks signed expiry before online work and again at one captured post-introspection validation time, then passes that same instant to candidate construction. Signed expiry takes precedence over sparse inactive introspection; opaque inactivity while signed evidence remains live retains `CredentialRevoked`. | The focused equality-boundary test maps `expires_at <= validated_at` to `CredentialExpired`, and the exact real courtroom proved the one-second Authentik credential expires with that denial. |
| Q2.13 The runtime-core facade re-exported five domain-package input authorities without classifying the second audience path, so the source-complete consumer-kit inventory rejected the facade. | CLOSED | R2.2a, R2.4 | One semantic authority row now carries every exporting facade. The five package inputs remain `PackageInput` and explicitly name both domain and runtime-core facades; no scanner exclusion, duplicate semantic row, or compatibility accessor exists. | The focused multi-facade classification test, complete source audit, 2,691-test monolith suite, and strict monolith Clippy all pass. |

### Repository-wide gate posture

The canonical Git-Bash workspace line-cap command does not complete within the
60-second Windows interaction ceiling. An exact PowerShell implementation of
the same tracked scopes, 400-line threshold, and allowlist passes; a second
execution-crate audit includes untracked additions and also passes.
`worth-query-execution` strict Clippy with `--no-deps` passes. Dependency-wide
strict Clippy remains red on pre-existing `worth-harness` and
`worth-foundational` diagnostics outside the Phase 6.3 diff. These tooling
limits and dependency diagnostics are not represented as Runtime proof,
waived, or silently called green. Boundary-check and generated agent-context
checks pass.

## Runtime Phases 7-8 handoff

Runtime Phase 7 is closed by
`_docs/WORTH-query/milestone-9.16-runtime-phase-7-closure-ledger.md` and its
closed Milestone 9.16.1 prerequisite. Runtime Phase 8 is closed by
`_docs/WORTH-query/milestone-9.16-runtime-phase-8-closure-ledger.md` against
the final C1-C8 source candidate. Those phase-owned ledgers are authoritative;
this Bank ledger does not duplicate their requirement and finding tables.

## Bank World Phase 5

No Bank Phase 5 row is proved by an in-process substitute. The phase closes
only when the real transport and independent-process courtroom prove the same
typed Bank semantics already established below the boundary.

| ID | Status | Guarantee and production owner | Positive evidence required | Independent or hostile evidence required | Current result and consequence |
| --- | --- | --- | --- | --- | --- |
| B5.1 | PROVED | `bank-http-adapter` owns one versioned HTTP/SSE protocol that translates typed Bank requests, outcomes, controls, and legal next actions without serializing Query authority. | An authenticated public account query and one live stream cross real TCP and preserve their typed semantic result. | Malformed versions, fields, credentials, and opaque tokens deny before Bank execution; source residue rejects authority serialization and route-local policy. | The versioned Axum protocol covers every declared family. Seventeen adapter tests and six protocol-boundary tests pass. Query publication is translated outside the descriptive protocol module. The real Docker court crosses the production HTTP and SSE binaries with typed publication. |
| B5.2 | PROVED | One authoritative `bank-server` process owns the installed Bank runtime; transport handlers submit bounded work and never construct policy, provider, branch, reconstruction, or execution authority. | Dynamic-port readiness admits lawful requests through the existing Bank facade. | Saturation, cancellation, deadline, disconnect, and shutdown return typed posture and leak no request, queue, stream, or Query resources. | Request queues, active live leases, per-stream queues, and opaque registries are independently bounded. Node deadlines cover the complete upstream response. Recovery capacity is reserved before effects. A held-authentication court proves full-queue denial before authentication/domain work; the real process court proves typed node-request and live saturation, disconnect release, deadlines, and orderly shutdown. |
| B5.3 | PROVED | One independently authenticated `bank-user-node` process exists per participant and owns only that user's credential/session and transport client. | Two dynamic users authenticate and issue isolated requests through distinct process and runtime identities. | Cross-user credential/token substitution, node crash, restart, and stale session state open no Bank authority. | `bank-user-node` owns one credential session, bounded request/live admission, deadline-bound forwarding, and no Bank authority. The real court proves distinct requester, approver, reviewer, peer, and replacement node PIDs; the restarted primary has no stale session and must authenticate again. |
| B5.4 | PROVED | Opaque continuation, recovery, and elevation tokens retain server-side move-only authority while exposing only bounded transport handles and typed lifecycle posture. | A lawful continuation and recovery journey survive response loss or node restart without duplicate effects. | Forged, copied, cross-user, expired, consumed, foreign-runtime, and stale-generation handles deny. | Direct TCP and real-process courts prove response-loss replay, cross-user denial, expiry, wrong-phase denial without phase loss, multi-actor elevation, and opaque recovery. The process journeys abandon the first mutation, notification, and disbursement response bodies and require typed `AlreadyCommitted` without duplicate effects. Existing linear undo/redo transport experiments remain provisional regression coverage only; they are neither a supported product contract nor a Bank Phase 5 closure obligation. |
| B5.5 | PROVED | Live delivery is SSE-backed, bounded by installed and request limits, freshly authorized before each payload, and closed on revocation, expiry, cancellation, deadline, overflow, or disconnect. | A lawful account activity stream delivers exact committed causes in order. | Narrow buffers, split or coalesced HTTP chunks, keepalive comments, slow consumers, token expiry, and live revocation preserve exact once-or-explicit-recovery semantics. | Local causal courts prove exact update publication, frame SSE events independently of arbitrary HTTP chunk boundaries, preserve multiple complete events from one chunk, keep valid comment frames nonterminal without counting them as missed commits, emit one explicit overflow only when a complete update cannot enter the bounded queue, and fuse both terminal streams after that event. The real process court proves update/publication, active-stream saturation, disconnect permit release, deadline termination, session-revision revocation cancellation, and authenticated-principal expiry while a stream is open. |
| B5.6 | PROVED | The courtroom owns dynamic ports, readiness, real Authentik browser login, distinct process IDs, failure injection at transport boundaries, deterministic teardown, and leak inspection. | The complete public journey runs through TCP using the production binaries. | User-node crash/restart, response loss, saturation, expiry, and revocation are independently asserted; no same-process fixture satisfies the row. | `transport_process_courtroom` passed against Docker Desktop on final source: seven distinct PIDs, real browser login, node restart, response-body abandonment/replay, opaque recovery and continuation journeys, request/live saturation, disconnect/deadline/revocation, live token expiry, and deterministic shutdown completed in 125.48 seconds. The independent real-browser identity courtroom also passed in 136.77 seconds. |
| B5.7 | PROVED | Public documentation and dependency/residue gates make the transport boundary discoverable without exposing Query internals or parallel compatibility paths. | A fresh consumer can start the server and node, authenticate, query, continue, stream, and interpret every typed outcome. | Manifests and source reject direct lower-runtime imports, raw authority fields, unbounded bodies/queues, ad hoc status parsing, and legacy same-process transport claims. | `process-transport.md` documents startup, session lifecycle, typed publication, live terminal postures, opaque handles, limits, and anti-patterns. Public exports, protocol authority-residue tests, strict Clippy, boundary-check, and agent-context verification form the mechanical evidence. |

## Gap classification

| Gap | Classification | Owning phase | Posture |
| --- | --- | --- | --- |
| Typed markers were self-authored and implied authority. | Query | Runtime Phase 1 | Resolved: markers are descriptive; exact installed handle owns binding authority. |
| Installation lacked a complete application-schema inventory. | Query | Runtime Phase 1 | Resolved by package/index manifest identity and binding validation. |
| Typed relations existed without direction-typed traversal. | Query | Runtime Phase 1 | Resolved with installed endpoint admission and compile-negative direction proof. |
| Currency markers were not tied to currency-bearing values. | Query | Runtime Phase 1 | Resolved with domain-currency-linked markers, manifest identity, closure, and hostile proof. |
| Mutation authoring could not express relationship grant/revoke or deletion. | Query | Runtime Phase 1 | Resolved with typed operation delete/link/unlink capabilities and installed admission. |
| Operation capability traits were absent from canonical installed schema identity. | Query | Runtime Phase 1 | Resolved with first-class operation-program members, closure validation, canonical identity, exact installed admission, and omission proof. |
| Dependency compilation treated primary graph touches as separate-provider calls. | Query | Runtime Phase 1 | Resolved by participation-symmetric receipt matching and explicit local mutation/read dependency evidence. |
| Ordinary execution of authored declarations was not composed. | Query | Runtime Phase 5 | Resolved by the typed read, mutation, workflow, history, explanation, and live facades in R5.1-R5.10. |
| Authentik proof admission did not exist. | Adapter | Bank Phase 2 | Resolved by the real HTTPS Authentik authorization-code courtroom, online introspection/revocation, key rotation, and hostile protocol evidence. |
| Primary logical graph ownership was not exposed through lawful host composition. | Query | Runtime Phase 2 | Resolved by an execution-owned, schema-typed terminal that consumes raw installation authority; the host facade and bank server import neither the monolith nor Runtime Bridge. |
| Scoped touched-graph authorization was not composed. | Query | Runtime Phase 3 | Resolved by the sealed installed-operation admission retaining Relational, Bridge, Signal, and exact allowed-graph evidence. |
| Exact accounting behavior and invariants were not installed. | Bank domain | Bank Phase 3 | Resolved through immutable proposal snapshots, exact effect replay, derived balances, workflow transitions, and a private invariant witness. Provider commit is separately resolved by Runtime Phase 4. |
| Application operations and Bank Phase 3 proposals lacked a provider-bound pre-commit progression. | Query | Runtime Phase 4 | Resolved by the admission-bound projected-read/effect/provider typestate and exact dynamic dependency closure. |
| Bank proposal snapshots and the primary graph could describe overlapping bank facts without one commit owner. | Query and bank adapter | Runtime Phase 4 | Resolved: the installed primary provider owns runtime truth and atomic commit; `BankSnapshot` is proposed state or an independent oracle only. |
| Available balance lacked reconstructible provider-derived acceleration tied to journal/posting truth; bounded full-history reconstruction eventually exhausted the installed fact ceiling. | Query provider | Runtime Phase 4 | Resolved by Q4.12/R4.5: exclusive typed aggregate summaries carry checked value plus exact source count, rebuild from truth, and have constant warm maintenance over changed postings. |
| Named bank reads required one installed canonical application-query contract across current, pinned, historical, preview, continuation, and live lanes. | Query | Runtime Phase 6 | **PROVED.** The Runtime Phase 6 ledger records canonical identity, typed access context, exact planning and seek, fresh authorization, bounded work, governed publication, resource release, complete bank-query migration, and final residue closure. |
| Bank Phase 4 capability, disclosure, delegation, conflict, and elevation meaning required generic Query authority across governed query and operation lanes. | Query | Runtime Phase 7 | **PROVED.** Runtime Phase 7 installs typed capability, purpose, disclosure, delegation, conflict, elevation, receipt, and current-authority admission over the Phase 6 canonical lane boundary; the Bank Phase 4 meaning remains independently proved. |
| Public outcomes do not yet expose framework-owned recovery, lawful compensation or inverse progression, and freshly authorized linear redo. | Query | Runtime Phase 8 | **PROVED** at Gate 8.6: typed host facade next actions (R8.47), publication noninterference (R8.48), HTTP descriptive boundary (R8.49), superseded path removal (R8.50), ordinary-commit zero aftermath cost (R8.51). |
| PB1 — `ApplicationFieldRef`'s eighth type parameter was named `Currency` (finance instance of a general unit-of-measure slot). Intake category 2. | Query declaration | Closed by Phase 8 gap-closure corrective (pre Phase 9 facade snapshot) | **CLOSED.** Platform slot is `Unit` / `ApplicationFieldUnit` / `NoApplicationUnit` / `ApplicationUnitRef` / `worth_query_unit!`. Bank finance `trait Currency` / `Money` / `UsdCurrency` retained. Consequence command now green: `rg ApplicationCurrencyMarker workspaces/worth-query/crates/worth-query-declaration` empty. |
| PB2 — `ApplicationCapabilityAmountDimension` / constraint `amount` field named finance magnitude bounds. Intake category 2. | Query declaration | Closed by Phase 8 gap-closure corrective | **CLOSED.** Platform name is `ApplicationCapabilityMagnitudeDimension` / `magnitude`. Bank money `.amount()` retained. Consequence: `rg ApplicationCapabilityAmountDimension workspaces/worth-query/crates/worth-query-declaration` empty. |
| PB4 — one production ordinary-branch literal duplicated `PRIMARY_APPLICATION_BRANCH`; test-local `BranchId("main")` counterfeits. Intake categories 1 and 5. | Query execution | Closed by Phase 8 gap-closure corrective | **CLOSED.** Production and execution-src sites use `primary_relational_branch_id` / `primary_truth_branch_identity`. Residue: no `BranchId("main")` outside `application_branch.rs`. |
| HTTP and independent user-node processes do not exist. | Adapter and fixture | Bank Phase 5 | **PROVED.** Production HTTP/SSE and independent user-node processes exist; the cold separate-process failure matrix passed and is bound by B5.1-B5.7. |
| Host-installed conditional operations, managed clocks, and reconstructible wakes are absent from the public host front door. | Query | Runtime Phase 9 | **PROVED.** The Phase 9 ledger closes the exact provider/clock/reconstruction contract, Signal-owned wake decision, fresh operation re-entry, idempotency, reconstruction, lifecycle, work, and host-only courtroom evidence. |
| The proven public front door has not yet completed documentation/facade cutover and workaround deletion across reference consumers. | Query and closure | Runtime Phase 10 | **PROVED.** The Phase 10 ledger contracts both audience facades, installs the canonical developer route, classifies support and consumer ownership, and enforces Query and Bank residue. |
| Complete bank consumer journeys do not yet compose every runtime authority through the public TCP-facing application. | Bank world | Bank Phase 6 | **OPEN.** Runtime Phase 10 now supplies the canonical public contract; Bank Phase 6 must assemble and prove every declared journey over the real process boundary. |

The purpose-scoped primary-graph host terminal is a newly discovered Runtime
Phase 2 guarantee rather than a separate phase: without it, the lawful bank
host must fabricate unrelated adapter ceremony before it can exercise identity
authority. Currency association, direction-typed traversal, typed graph
mutation, and primary-versus-provider dependency evidence were incomplete guarantees inside
Runtime Hardening Phase 1 and are closed there. Authentik protocol behavior is
not generic Query behavior and was resolved by Bank Phase 2. The current open
frontier is Bank World Phase 6; Closure Phase 1 remains downstream in the
causal order recorded above rather than being available for parallel
workaround work.

## Exact verification commands

```text
# Declaration
cargo test --manifest-path workspaces/worth-query/Cargo.toml -p worth-query-declaration

# Installation
cargo test --manifest-path workspaces/worth-query/Cargo.toml -p worth-query-installation

# Primary-graph authentication execution
cargo test --manifest-path workspaces/worth-query/Cargo.toml -p worth-query-execution

# Installed-world regression, including primary and separate graph evidence
cargo test --manifest-path workspaces/worth-query/Cargo.toml -p worth-query --test installed_operating_world

# Bank consumer and independent inventory/residue oracle
cargo test --manifest-path workspaces/worth-query-bank-world/Cargo.toml -p bank-domain

# Complete bank world, including the real Docker/Authentik browser courtroom
cargo test --manifest-path workspaces/worth-query-bank-world/Cargo.toml --workspace --all-features

# Runtime Phase 5 typed bounded public reads
cargo test --manifest-path workspaces/worth-query-bank-world/Cargo.toml -p bank-server --test ordinary_reads

# Consolidated hostile compiler certification
cargo test --manifest-path workspaces/worth-query/Cargo.toml -p worth-query-certification --test application_schema_compile_fail

# Cold real-system courtroom
cargo test --manifest-path workspaces/worth-query-bank-world/Cargo.toml -p bank-courtroom --features cold-docker-courtroom --test async_identity_courtroom

# Cold Bank server + independent user-node transport courtroom
cargo test --manifest-path workspaces/worth-query-bank-world/Cargo.toml -p bank-courtroom --features cold-docker-courtroom --test transport_process_courtroom
```
