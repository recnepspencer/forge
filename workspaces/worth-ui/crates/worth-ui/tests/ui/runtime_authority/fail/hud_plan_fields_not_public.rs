use worth_ui::facade::WorthUiHudPlan;

fn main() {
    let _plan = WorthUiHudPlan {
        handle_receipt: todo!(),
        support_digest: 1,
        hud_plan_digest: 2,
        frame_policy: todo!(),
        rows: Vec::new(),
        renderer_surface_admissions: Vec::new(),
        command_plan_indexes: Vec::new(),
        accessibility_plan_indexes: Vec::new(),
        diagnostics_plan_indexes: Vec::new(),
        overlay_hooks: Vec::new(),
        counters: todo!(),
    };
}
