use worth_ui::facade::WorthUiCanvasSpatialPlan;

fn main() {
    let _plan = WorthUiCanvasSpatialPlan {
        handle_receipt: todo!(),
        support_digest: 1,
        canvas_plan_digest: 2,
        rows: Vec::new(),
        command_plan_indexes: Vec::new(),
        diagnostics_plan_indexes: Vec::new(),
        selection_state_slot_handles: Vec::new(),
        draw_hooks: Vec::new(),
        hit_test_hooks: Vec::new(),
        tool_state_hooks: Vec::new(),
        counters: todo!(),
    };
}
