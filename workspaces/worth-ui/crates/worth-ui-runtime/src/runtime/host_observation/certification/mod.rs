//! Certification support authority — host observation replay scenarios.

mod identity_state_query;
mod lane_frame_cost;
mod lane_meaning_parity;

#[cfg(test)]
mod file_rust_replacement;
#[cfg(test)]
mod reload_storm;
