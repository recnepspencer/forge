use worth_store_physical_certification::ExecutedTranscriptParts;

fn main() {
    let _parts = ExecutedTranscriptParts {
        plan: todo!(),
        schedule: todo!(),
        fixture_manifest: todo!(),
        trace: todo!(),
        counter_receipt: todo!(),
        fault_events: Vec::new(),
        oracle_verdicts: Vec::new(),
    };
}
