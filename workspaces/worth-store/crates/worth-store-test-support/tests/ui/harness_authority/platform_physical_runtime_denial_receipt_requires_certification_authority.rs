use worth_store_physical_format::{PlatformPhysicalAppendReport, PlatformPhysicalModelReceipt};

fn main() {
    let report: PlatformPhysicalAppendReport = todo!();
    let _ = PlatformPhysicalModelReceipt::from_append_hidden_scan_denial(report);
}
