use worth_store_layout_indexes::DegradedScanReady;

fn worth() -> DegradedScanReady {
    DegradedScanReady {
        recipe: panic!(),
        current_materialization: panic!(),
    }
}

fn main() {
    let _ = worth();
}
