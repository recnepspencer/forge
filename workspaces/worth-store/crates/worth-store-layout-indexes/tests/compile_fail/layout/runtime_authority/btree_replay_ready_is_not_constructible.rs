use worth_store_layout_indexes::BTreeReplayReady;

fn worth() -> BTreeReplayReady {
    BTreeReplayReady {
        source: todo!(),
        current_materialization: todo!(),
    }
}

fn main() {
    let _ = worth();
}
