use worth_store_layout_indexes::BaselineBTreeReadSource;

fn worth() -> BaselineBTreeReadSource {
    BaselineBTreeReadSource {
        witness: panic!(),
        plan: panic!(),
        protected: panic!(),
    }
}

fn main() {
    let _ = worth();
}
