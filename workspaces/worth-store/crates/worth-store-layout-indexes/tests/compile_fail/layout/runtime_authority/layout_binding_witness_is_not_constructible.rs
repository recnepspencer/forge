use worth_store_layout_indexes::evolution::migration::LayoutBindingWitness;

fn worth() -> LayoutBindingWitness {
    LayoutBindingWitness {
        family: todo!(),
        bound_version: todo!(),
        observed_version: todo!(),
        bound_authority: todo!(),
    }
}

fn main() {
    let _ = worth();
}
