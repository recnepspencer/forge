use worth_store_layout_indexes::PreparedLsmCompaction;

fn worth() -> PreparedLsmCompaction {
    PreparedLsmCompaction {
        membership: panic!(),
        replay_tail: panic!(),
        output: panic!(),
        physical_intent: panic!(),
    }
}

fn main() {
    let _ = worth();
}
