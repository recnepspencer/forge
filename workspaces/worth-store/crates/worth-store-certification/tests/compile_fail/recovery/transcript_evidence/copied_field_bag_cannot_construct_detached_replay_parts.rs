use worth_store_physical_certification::DetachedSimulationReplayParts;

fn main() {
    let _parts = DetachedSimulationReplayParts {
        transcript_identity: todo!(),
        replay_basis_identity: todo!(),
        plan: todo!(),
        schedule: todo!(),
        fixture_manifest: todo!(),
        trace: todo!(),
        counter_receipt: todo!(),
        fault_events: Vec::new(),
        oracle_verdicts: Vec::new(),
    };
}
