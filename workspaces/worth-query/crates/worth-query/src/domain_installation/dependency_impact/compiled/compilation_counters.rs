#[derive(Clone, Copy, Debug, Default, Eq, PartialEq)]
pub struct WorthQuerySemanticAspectDependencyCompilationCounters {
    pub semantic_contract_checks: usize,
    pub execution_receipt_checks: usize,
    pub graph_receipt_checks: usize,
    pub conditional_authority_checks: usize,
    pub workflow_trace_checks: usize,
    pub installed_definition_visits: usize,
    pub graph_read_role_visits: usize,
    pub native_projection_edges: usize,
    pub collection_membership_edges: usize,
    pub collection_ordering_edges: usize,
    pub collection_grouping_edges: usize,
    pub collection_window_edges: usize,
    pub result_shape_edges: usize,
    pub touch_edges: usize,
    pub effect_contract_edges: usize,
    pub invariant_contract_edges: usize,
    pub replay_contract_edges: usize,
    pub lineage_contract_edges: usize,
    pub support_contract_edges: usize,
    pub workflow_stage_read_edges: usize,
    pub conditional_node_visits: usize,
    pub conditional_truth_edges: usize,
    pub realized_graph_call_edges: usize,
    pub realized_direct_output_edges: usize,
    pub realized_workflow_read_edges: usize,
    pub realized_conditional_outcome_edges: usize,
    pub realized_effect_edges: usize,
    pub realized_invariant_edges: usize,
    pub realized_lineage_edges: usize,
    pub realized_workflow_output_edges: usize,
    pub conditional_observations_retained: usize,
    pub canonical_traversal_edges: usize,
    pub uniqueness_hash_checks: usize,
    pub compiled_dependency_count: usize,
    pub closure_edges_traversed: usize,
    pub workflow_graph_edges_traversed: usize,
    pub impact_index_entries: usize,
    pub impact_index_dependency_visits: usize,
    pub impact_mask_propagation_edges: usize,
    pub unrelated_definition_scans: usize,
    pub unrelated_runtime_scans: usize,
    pub consumer_registry_scans: usize,
}
