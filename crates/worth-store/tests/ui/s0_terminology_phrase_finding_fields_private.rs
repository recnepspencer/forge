use worth_store::TerminologyPhraseFinding;

fn main() {
    let _finding = TerminologyPhraseFinding {
        row_id: todo!(),
        subject_kind: todo!(),
        subject_path_or_symbol: String::new(),
        classification: String::new(),
        evidence_refs: vec![],
        forbidden_claims: vec![],
        deferred_s_sequences: vec![],
        status: todo!(),
        notes: String::new(),
        phrase: String::new(),
        line_number: 0,
        line_excerpt: String::new(),
        allowed_use: todo!(),
        required_qualifier: todo!(),
    };
}
