use worth_query::facade::workflow::WorthQueryPromotionEligibility;

fn main() {
    let _eligibility = WorthQueryPromotionEligibility {
        session_label: todo!(),
        snapshot_identity: todo!(),
        eligibility_identity: todo!(),
    };
}
