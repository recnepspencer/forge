use worth_query::facade::policy::{execute_scoped_preview_live_session_plan, PreviewSessionPlanBinding};

fn main() {
    let binding: PreviewSessionPlanBinding = todo!();
    let _ = execute_scoped_preview_live_session_plan(&binding);
}
