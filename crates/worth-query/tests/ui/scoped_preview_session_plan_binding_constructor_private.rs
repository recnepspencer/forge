use worth_query::facade::policy::ScopedPreviewSessionPlanBinding;

fn main() {
    let _ = ScopedPreviewSessionPlanBinding {
        scoped_basis: panic!(),
        preview_binding: panic!(),
    };
}
