@@ -79,22 +79,28 @@
     let bound = arena.half_edge_count().max(1);
 
+    eprintln!("DEBUG: disk_component seed={} vertex={} out_set={:?}", seed.index(), vertex.index(), outgoing_set.iter().map(|h| h.index()).collect::<Vec<_>>());
+
     while let Some(curr) = queue.pop_front() {
         let curr_data = arena.get_half_edge(curr)?;
         if curr_data.origin() != vertex {
+            eprintln!("DEBUG:   skipping curr={} because origin={} != vertex={}", curr.index(), curr_data.origin().index(), vertex.index());
             continue;
         }
 
         let out_he = curr;
         let in_he = curr_data.prev();
+        eprintln!("DEBUG:   curr={} in_he={}", out_he.index(), in_he.index());
 
         // 1. Walk the entire radial ring of the outgoing edge
         let mut r = out_he;
         for step in 0..=bound {
             let r_data = arena.get_half_edge(r)?;
+            eprintln!("DEBUG:     out_ring r={} origin={}", r.index(), r_data.origin().index());
             
             if r_data.origin() == vertex {
                 if outgoing_set.contains(&r) && disk.insert(r) {
+                    eprintln!("DEBUG:       inserted r={}", r.index());
                     queue.push_back(r);
                 }
             }
             
