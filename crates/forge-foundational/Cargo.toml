[package]
name = "forge-foundational"
edition.workspace = true
version.workspace = true
license.workspace = true
description = "Shared Forge boundary vocabulary for canonical values, aspect state, identities, locators, and compatibility surfaces."

[dependencies]
forge-proof = { path = "../forge-proof" }
serde = { version = "1", features = ["derive", "rc"] }
serde_json = "1"

[dev-dependencies]
trybuild.workspace = true

[lints]
workspace = true
