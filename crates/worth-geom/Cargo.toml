[package]
name = "worth-geom"
edition = "2021"
version = "0.1.0"
license = "BUSL-1.1"
description = "Analytic surfaces, NURBS, and curves for the Worth geometry kernel"

[dependencies]
worth-math = { path = "../worth-math" }
serde = { version = "1", features = ["derive"] }
sha2 = "0.10"

[dev-dependencies]
serde_json = "1"
