#[allow(unused_imports)]
pub(crate) use crate::construction::certification::corpus::replay_siege_report::{
    prepare_primitive_construction_corpus_replay_siege_report as siege_report,
    row_attempted_realization_strategies, row_birth_digest, row_blocking_boundary,
    row_construction_breadth, row_exhaustion_reason, row_feature_conditioning_class,
    row_normalization_disposition, row_realization_strategy, row_rejection_class,
    row_rejection_locality, row_stability_class, row_support_normal_class, siege_workspace,
    PrimitiveConstructionCorpusReplaySiegeReport,
};

#[allow(dead_code)]
pub(crate) type PrimitiveConstructionCorpusReplaySiegeView =
    PrimitiveConstructionCorpusReplaySiegeReport;
