use worth_kernel::workload_composition::AdmittedSpatialConflictInput;

fn main() {
    let _ = AdmittedSpatialConflictInput {
        authority: todo!(),
        route: todo!(),
        routing_contract: todo!(),
        admission_digest: todo!(),
    };
}
