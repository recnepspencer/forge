use worth_kernel::workload_composition::WorthQuerySelectedGraphObligations;

fn main() {
    let _: fn(()) -> WorthQuerySelectedGraphObligations =
        WorthQuerySelectedGraphObligations::from_selected;
}
