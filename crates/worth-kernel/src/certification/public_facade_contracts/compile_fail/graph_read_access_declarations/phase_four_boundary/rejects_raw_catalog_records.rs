use worth_kernel::graph_read_access_declarations::current_worth_graph_read_requirement_derivation_closeout;

fn main() {
    let records: Vec<()> = Vec::new();
    let _ = current_worth_graph_read_requirement_derivation_closeout(&records);
}
