pub(crate) const fn missing_query_read_family_artifact_blocker() -> &'static str {
    "Worth Phase 3 carries Query family name and digest evidence, but not the real ForgeQueryReadFamily artifact required by Query plan execution."
}
