[package]
name = "forge-geom"
edition.workspace = true
version.workspace = true
license.workspace = true
description = "Analytic surfaces, NURBS, and curves for the Forge geometry kernel"

[dependencies]
forge-math.workspace = true
serde = { workspace = true, features = ["derive"] }

[lints]
workspace = true
