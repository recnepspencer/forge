use topology::facade::{
    certify_milestone_two_verified_topology_commit_traced,
    certify_verified_topology_commit_traced,
};

fn main() {
    let _ = certify_verified_topology_commit_traced;
    let _ = certify_milestone_two_verified_topology_commit_traced;
}
