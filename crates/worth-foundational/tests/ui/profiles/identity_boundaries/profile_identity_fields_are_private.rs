use worth_foundational::FoundationalProfileIdentity;

fn main() {
    let _ = FoundationalProfileIdentity {
        basis: panic!("type-check only"),
        digest: panic!("type-check only"),
    };
}
