use worth_spatial::facade::planar_boolean_common_plane::PlanarBooleanCommonPlaneSharedPlaneIdentityReceipt;

fn main() {
    let _receipt = PlanarBooleanCommonPlaneSharedPlaneIdentityReceipt {
        shared_plane_receipt_identity: String::new(),
        plane_agreement_identity: String::new(),
        shared_plane_identity: String::new(),
        left_surface_support_identity: String::new(),
        right_surface_support_identity: String::new(),
    };
}
