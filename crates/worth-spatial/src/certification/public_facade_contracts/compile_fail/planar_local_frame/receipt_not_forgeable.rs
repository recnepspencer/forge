use worth_spatial::facade::planar_local_frame::PlanarLocalFrameCertificateReceipt;

fn main() {
    let _ = PlanarLocalFrameCertificateReceipt::new(
        todo!(),
        String::new(),
        String::new(),
        String::new(),
        todo!(),
    );
}
