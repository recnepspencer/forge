use worth_spatial::facade::planar_boolean_events::PlanarBooleanPointEventExtractionReceipt;

fn main() {
    let _ = PlanarBooleanPointEventExtractionReceipt {
        predicate_binding_identity: String::new(),
        point_events: Vec::new(),
        counters: Default::default(),
        extraction_identity: String::new(),
    };
}
