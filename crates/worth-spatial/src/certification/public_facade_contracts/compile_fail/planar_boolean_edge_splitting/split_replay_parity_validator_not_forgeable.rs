use worth_spatial::facade::planar_boolean_edge_splitting::ValidatePlanarBooleanReplayParity;

fn main() {
    let _validator = ValidatePlanarBooleanReplayParity {
        receipt_identity: String::new(),
        parity_receipt_identity: String::new(),
        validated_rows: 0,
    };
}
