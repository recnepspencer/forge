use worth_spatial::facade::planar_boolean_edge_splitting::PlanarBooleanSplitSelectorResolutionRow;

fn main() {
    let _ = PlanarBooleanSplitSelectorResolutionRow {
        row_identity: "forged".to_string(),
        persistent_name_identity: "name".to_string(),
        artifact_identity: "artifact".to_string(),
        selector_basis_identity: "selector".to_string(),
    };
}
