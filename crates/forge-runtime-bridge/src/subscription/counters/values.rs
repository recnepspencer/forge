#[derive(Debug, Clone, Copy, Default, PartialEq, Eq)]
pub(super) struct BridgeSubscriptionCounterValues {
    pub(super) family_registry_freeze_count: usize,
    pub(super) family_count: usize,
    pub(super) family_supported_slice_kind_count: usize,
    pub(super) family_lookup_count: usize,
    pub(super) declaration_count: usize,
    pub(super) declaration_input_slice_intent_count: usize,
    pub(super) declaration_normalized_slice_intent_count: usize,
    pub(super) declaration_deduplicated_slice_intent_count: usize,
    pub(super) declaration_rejection_count: usize,
    pub(super) basis_request_count: usize,
    pub(super) basis_binding_count: usize,
    pub(super) basis_rejection_count: usize,
    pub(super) signal_strategy_selection_count: usize,
    pub(super) signal_strategy_rejection_count: usize,
    pub(super) admitted_subscription_count: usize,
    pub(super) lifecycle_record_count: usize,
    pub(super) replay_reconstruction_count: usize,
    pub(super) replay_mismatch_count: usize,
    pub(super) diagnostics_bundle_count: usize,
    pub(super) subscription_delivery_cost_profile_selection_count: usize,
    pub(super) subscription_delivery_cost_profile_rejection_count: usize,
    pub(super) subscription_delivery_over_budget_rejection_count: usize,
    pub(super) subscription_delivery_density_sparse_count: usize,
    pub(super) subscription_delivery_density_coalesced_count: usize,
    pub(super) subscription_delivery_density_dense_restart_count: usize,
    pub(super) subscription_consumer_contract_admission_count: usize,
    pub(super) subscription_consumer_contract_rejection_count: usize,
    pub(super) subscription_activation_count: usize,
    pub(super) subscription_delivery_record_count: usize,
    pub(super) subscription_delivery_member_count: usize,
    pub(super) subscription_delivery_family_selection_count: usize,
    pub(super) subscription_diagnostics_reference_emit_count: usize,
    pub(super) subscription_rich_diagnostics_hot_path_materialization_count: usize,
    pub(super) subscription_delivery_arena_reset_count: usize,
    pub(super) subscription_delivery_buffer_reuse_count: usize,
    pub(super) subscription_allocation_count: usize,
    pub(super) subscription_clone_count: usize,
    pub(super) subscription_callback_identity_scan_count: usize,
    pub(super) subscription_active_registry_scan_count: usize,
    pub(super) subscription_fanout_per_member_consumer_scan_count: usize,
    pub(super) subscription_preview_residue_nonzero_count: usize,
    pub(super) subscription_fanout_plan_admission_count: usize,
    pub(super) subscription_fanout_plan_rejection_count: usize,
    pub(super) subscription_fanout_layout_build_count: usize,
    pub(super) subscription_fanout_consumer_binding_count: usize,
    pub(super) subscription_fanout_delivery_projection_count: usize,
    pub(super) subscription_fanout_delivery_projection_rejection_count: usize,
    pub(super) subscription_delivery_window_seed_retention_count: usize,
    pub(super) subscription_delivery_replay_seed_retention_count: usize,
    pub(super) subscription_delivery_replay_readiness_inspection_count: usize,
    pub(super) subscription_delivery_replay_plan_count: usize,
    pub(super) subscription_delivery_replay_plan_rejection_count: usize,
    pub(super) subscription_delivery_replay_retained_window_count: usize,
    pub(super) subscription_delivery_replay_retained_member_count: usize,
    pub(super) subscription_fanout_projection_validation_count: usize,
    pub(super) subscription_fanout_projection_validation_rejection_count: usize,
    pub(super) subscription_acknowledgement_frontier_admission_count: usize,
    pub(super) subscription_acknowledgement_frontier_rejection_count: usize,
    pub(super) subscription_checkpoint_ready_count: usize,
    pub(super) subscription_checkpoint_publication_count: usize,
    pub(super) subscription_checkpoint_publication_rejection_count: usize,
    pub(super) subscription_duplicate_replay_policy_selection_count: usize,
    pub(super) subscription_resume_admission_count: usize,
    pub(super) subscription_resume_admission_rejection_count: usize,
    pub(super) subscription_resume_plan_count: usize,
    pub(super) subscription_resume_basis_capture_count: usize,
    pub(super) subscription_resume_temporal_basis_count: usize,
    pub(super) subscription_resume_inflight_async_basis_count: usize,
    pub(super) subscription_resume_delivery_basis_count: usize,
    pub(super) subscription_resume_basis_admission_count: usize,
    pub(super) subscription_resume_basis_rejection_count: usize,
    pub(super) subscription_resume_replay_readiness_count: usize,
    pub(super) subscription_unsealed_stream_checkpoint_rejection_count: usize,
    pub(super) subscription_checkpoint_truncation_rejection_count: usize,
    pub(super) subscription_resume_cross_branch_rejection_count: usize,
    pub(super) subscription_resume_delivery_mismatch_rejection_count: usize,
    pub(super) subscription_resume_inflight_async_generation_rejection_count: usize,
    pub(super) subscription_continuation_index_build_count: usize,
    pub(super) subscription_continuation_candidate_count: usize,
    pub(super) subscription_continuation_candidate_index_lookup_count: usize,
    pub(super) subscription_continuation_decision_count: usize,
    pub(super) subscription_continuation_rejection_count: usize,
    pub(super) subscription_branch_leak_rejection_count: usize,
    pub(super) subscription_continuation_child_record_count: usize,
    pub(super) subscription_continuation_full_registry_scan_count: usize,
    pub(super) subscription_preview_basis_admission_count: usize,
    pub(super) subscription_preview_basis_rejection_count: usize,
    pub(super) subscription_preview_activation_count: usize,
    pub(super) subscription_preview_discard_count: usize,
    pub(super) subscription_preview_discard_rejection_count: usize,
    pub(super) subscription_preview_residue_check_count: usize,
    pub(super) subscription_preview_residue_scope_index_lookup_count: usize,
    pub(super) subscription_preview_non_scope_registry_scan_count: usize,
    pub(super) subscription_preview_lifecycle_residue_envelope_count: usize,
    pub(super) subscription_preview_promotion_count: usize,
    pub(super) subscription_preview_promotion_rejection_count: usize,
    pub(super) subscription_preview_authoritative_readmission_count: usize,
    pub(super) subscription_preview_crossed_completion_rejection_count: usize,
    pub(super) subscription_preview_temporal_evidence_drift_rejection_count: usize,
    pub(super) subscription_temporal_admission_count: usize,
    pub(super) subscription_temporal_rejection_count: usize,
    pub(super) subscription_temporal_activation_ready_count: usize,
    pub(super) subscription_temporal_time_only_cause_count: usize,
    pub(super) subscription_temporal_truth_plus_time_cause_count: usize,
    pub(super) subscription_temporal_duplicate_clock_rejection_count: usize,
    pub(super) subscription_temporal_stale_clock_rejection_count: usize,
    pub(super) subscription_temporal_delivery_plan_count: usize,
    pub(super) subscription_mixed_cause_ordering_request_count: usize,
    pub(super) subscription_mixed_cause_ordering_count: usize,
    pub(super) subscription_mixed_cause_ordered_cause_count: usize,
    pub(super) subscription_mixed_cause_duplicate_suppression_count: usize,
    pub(super) subscription_mixed_cause_denied_cause_count: usize,
    pub(super) subscription_mixed_cause_authoritative_preview_rejection_count: usize,
    pub(super) subscription_mixed_cause_delivery_window_plan_count: usize,
    pub(super) subscription_shared_delivery_plan_count: usize,
    pub(super) subscription_shared_delivery_plan_rejection_count: usize,
    pub(super) subscription_shared_delivery_layout_count: usize,
    pub(super) subscription_shared_delivery_bundle_draft_count: usize,
    pub(super) subscription_shared_delivery_bundle_sealed_count: usize,
    pub(super) subscription_shared_delivery_projection_count: usize,
    pub(super) subscription_shared_delivery_projection_rejection_count: usize,
    pub(super) subscription_shared_delivery_acknowledgement_count: usize,
    pub(super) subscription_shared_delivery_acknowledgement_rejection_count: usize,
    pub(super) subscription_historical_truth_basis_admission_count: usize,
    pub(super) subscription_historical_previous_value_evidence_count: usize,
    pub(super) subscription_historical_temporal_replay_basis_admission_count: usize,
    pub(super) subscription_historical_temporal_replay_rejection_count: usize,
    pub(super) subscription_historical_temporal_readiness_count: usize,
}
