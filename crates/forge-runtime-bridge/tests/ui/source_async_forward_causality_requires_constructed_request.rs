use forge_runtime_bridge::facade::BridgeAsyncRetryLineageRequest;

fn main() {
    let _ = BridgeAsyncRetryLineageRequest {
        prior: todo!(),
        timeout_report: None,
        cancellation_report: None,
        retry_schedule_report: todo!(),
        retry_admission_report: todo!(),
    };
}
