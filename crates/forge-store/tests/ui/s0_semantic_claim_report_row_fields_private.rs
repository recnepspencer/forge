use forge_store::SemanticPhysicalClaimReportRow;

fn main() {
    let _row = SemanticPhysicalClaimReportRow {
        row_id: todo!(),
        subject_kind: todo!(),
        subject_path_or_symbol: String::new(),
        classification: String::new(),
        evidence_refs: vec![],
        forbidden_claims: vec![],
        deferred_s_sequences: vec![],
        status: todo!(),
        notes: String::new(),
        claim_family: todo!(),
        claim_status: todo!(),
        semantic_capability_proven: String::new(),
        closeout_or_planned_source: String::new(),
        named_suite: String::new(),
        evidence_lanes: vec![],
    };
}
