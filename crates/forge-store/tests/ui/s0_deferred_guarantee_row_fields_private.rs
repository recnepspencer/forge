use forge_store::DeferredPhysicalGuaranteeRow;

fn main() {
    let _row = DeferredPhysicalGuaranteeRow {
        row_id: todo!(),
        subject_kind: todo!(),
        subject_path_or_symbol: String::new(),
        classification: String::new(),
        evidence_refs: vec![],
        forbidden_claims: vec![],
        deferred_s_sequences: vec![],
        status: todo!(),
        notes: String::new(),
        guarantee_category: todo!(),
        current_evidence_status: todo!(),
        missing_proof: String::new(),
        dependent_named_suite: String::new(),
        dependent_evidence_lanes: vec![],
    };
}
