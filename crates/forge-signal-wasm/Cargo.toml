[package]
name = "forge-signal-wasm"
edition.workspace = true
version = "0.1.7"
description = "WebAssembly facade for Forge Signal"
repository = "https://github.com/recnepspencer/forge"
license = "UNLICENSED"
readme = "README.md"
publish = false

[lib]
crate-type = ["cdylib", "rlib"]

[features]
default = ["profile-extended"]
profile-compact = ["forge-signal/profile-compact"]
profile-standard = ["forge-signal/profile-standard"]
profile-extended = ["forge-signal/profile-extended"]

[dependencies]
forge-signal = { path = "../forge-signal", default-features = false }
serde = { workspace = true, features = ["derive"] }
serde_json.workspace = true
wasm-bindgen = "0.2"
js-sys = "0.3"
serde-wasm-bindgen = "0.6"
console_error_panic_hook = "0.1"
sha2 = "0.10"
base64 = "0.22"
rmp-serde = "1.3"

[lints]
workspace = true
