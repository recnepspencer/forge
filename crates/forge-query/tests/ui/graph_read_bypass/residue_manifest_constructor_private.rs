use forge_query::facade::consumer_kit::ForgeQueryGraphReadBypassResidueManifest;

fn main() {
    let _ = ForgeQueryGraphReadBypassResidueManifest {
        rows: Vec::new(),
        manifest_digest: String::new(),
    };
}
