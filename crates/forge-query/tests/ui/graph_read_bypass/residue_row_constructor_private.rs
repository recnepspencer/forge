use forge_query::facade::consumer_kit::ForgeQueryGraphReadBypassResidueRow;

fn main() {
    let _ = ForgeQueryGraphReadBypassResidueRow {
        class: todo!(),
        owner: String::new(),
        introduced_in: String::new(),
        current_count: 0,
        must_not_exceed_count: 0,
        blocker: String::new(),
        removal_trigger: String::new(),
        row_digest: String::new(),
    };
}
