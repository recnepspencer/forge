use forge_query::facade::QueryDeliveryBatchReceipt;

fn main() {
    let _receipt = QueryDeliveryBatchReceipt {
        attachment_digest: todo!(),
        sequence: todo!(),
        receipt_digest: String::new(),
    };
}
