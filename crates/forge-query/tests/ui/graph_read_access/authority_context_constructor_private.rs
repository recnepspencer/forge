use forge_query::facade::runtime::ForgeQueryGraphReadAccessAuthorityContext;

fn main() {
    let _ = ForgeQueryGraphReadAccessAuthorityContext {
        basis_scope: todo!(),
        policy_tenant: None,
        relationship_proof: None,
        receipt: todo!(),
    };
}
