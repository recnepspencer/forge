use forge_query::facade::ScopedPreviewLiveSessionPlanBinding;

fn main() {
    let _ = ScopedPreviewLiveSessionPlanBinding {
        scoped_binding: panic!(),
        preview_live: panic!(),
    };
}
