Milestone 13 Phase 1 internal compiler-mutation record

Production-visibility posture:
- no Signal phase type or executor was widened for this proof
- the mutation was applied to the actual internal owner-progression prototype

Command:
cargo test -p worth-signal progression_contract_tests --lib

Temporary source mutation in progression_contract_tests.rs:
- let executed = execute_ready(admit_ready(lower(resolved)));
+ let executed = execute_ready(PreparedDirectInvalidation::new(7));

Compiler result (rustc E0308):
progression_contract_tests.rs:205:38
expected `ExecutionReadyRecipe<PrototypeWork, FreshnessScopedBasis<...>>`
found struct `PreparedDirectInvalidation`
the argument to execute_ready must be ReadyPrototypeBatch

The mutation was reverted after recording this failure. The lawful prototype
still consumes resolved origin -> lowered batch -> readiness admission ->
ready-only execution.
