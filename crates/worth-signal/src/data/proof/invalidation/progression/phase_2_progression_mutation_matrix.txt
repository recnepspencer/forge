Milestone 13 Phase 2 actual-owner compiler mutation matrix

Command for each temporary mutation:
cargo test -p worth-signal actual_owner_forms_progress_source_work_through_ready_execution --lib --no-run

The mutations were applied one at a time to progression/tests.rs and reverted
after rustc rejected them. Production visibility was not widened.

1. Skip origin resolution: admitted source -> lower
   Mutation: pass AdmittedSourceRecompute directly to
   LoweredInvalidationBatch::lower.
   rustc E0308: expected `ResolvedInvalidationWork`, found
   `AdmittedSourceRecompute`.

2. Skip readiness admission: lowered -> execute
   Mutation: pass LoweredInvalidationBatch directly to
   ExecutedInvalidationBatch::execute.
   rustc E0308: expected `ReadyInvalidationBatch`, found
   `LoweredInvalidationBatch`.

3. Duplicate resolved authority
   Mutation: insert `let _forged_duplicate = resolved.clone();` before
   topology lowering.
   rustc E0599: no method named `clone` found for struct
   `ResolvedInvalidationWork`.

The lawful path remains admitted owner form -> resolved work -> lowered batch
-> ready batch -> executed batch.
