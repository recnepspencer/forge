[package]
name = "forge-harness"
edition.workspace = true
version.workspace = true
license.workspace = true
description = "Generic execution harness infrastructure for Forge runtimes"
readme = "README.md"
repository = "https://github.com/recnepspencer/forge"
keywords = ["harness", "testing", "certification", "parity", "workflows"]
categories = ["development-tools::testing", "science"]

[dependencies]
serde = { version = "1", features = ["derive"] }
serde_json = "1"
tracing = "0.1"

[lints]
workspace = true
