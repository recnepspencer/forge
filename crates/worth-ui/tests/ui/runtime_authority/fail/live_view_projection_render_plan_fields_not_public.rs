use worth_ui::facade::WorthUiLiveViewProjectionRenderPlan;

fn main() {
    let _forged = WorthUiLiveViewProjectionRenderPlan {
        controls: Vec::new(),
        interactions: Vec::new(),
        consumers: Vec::new(),
        render_plan_digest: 1,
    };
}
