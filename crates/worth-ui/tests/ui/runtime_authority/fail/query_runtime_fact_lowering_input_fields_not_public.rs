use worth_ui::facade::WorthUiQueryRuntimeFactLoweringInput;

fn main() {
    let _ = WorthUiQueryRuntimeFactLoweringInput {
        support_receipt: todo!(),
        binding_comparison: todo!(),
        live_rebind_plan: todo!(),
        projection_fact_receipts: Vec::new(),
        state_snapshot_receipts: Vec::new(),
        effect_posture_receipts: Vec::new(),
        virtualized_frame_targets: Vec::new(),
    };
}
