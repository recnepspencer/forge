use worth_ui::facade::WorthUiRealtimeFrameReceipt;

fn main() {
    let _receipt = WorthUiRealtimeFrameReceipt {
        target: todo!(),
        lane: todo!(),
        renderer_surface_admission: todo!(),
        touched_plan_indexes: Vec::new(),
        touched_runtime_handles: Vec::new(),
        command_plan_indexes: Vec::new(),
        accessibility_plan_indexes: Vec::new(),
        diagnostics_plan_indexes: Vec::new(),
        counters: todo!(),
        certification: todo!(),
    };
}
