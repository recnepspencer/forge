# Worth Milestone 7.5: Coplanar Overlap Region Extraction

> **Status:** Draft
>
> **Purpose:** freeze the canonical overlap-region products that consume the
> `7.4` loop reconstruction ledger and feed `7.6` fragment classification
> without reopening split, continuation, walk, loop, projection, plane, or
> workload-entry authority.

## Goal

Milestone `7.5` closes the gap between the `7.4` loop reconstruction ledger
and honest coplanar overlap-region extraction.

By the end of this milestone:

- a `7.4` loop reconstruction ledger can enter one and only one overlap-region
  extraction request boundary
- reconstructed loops, born loops, loop islands, overlap-chain lineage,
  persistent-name propagation, and loop decision-log identity are lowered into
  overlap-participation products instead of host-local pairwise geometry
  rediscovery
- overlap adjacency, overlap islands, shared-boundary contact, shared-area
  admission, opposite-sense coincidence, winding normalization, candidate
  promotion, and boundary-only outcomes each have typed, localized,
  replay-stable products or denials
- admitted overlap regions receive canonical, replay-stable identities plus
  persistent-name, lineage, and downstream-consumption identity strong enough
  for `7.6` fragment classification and `7.7` face assembly to inherit without
  reopening loop truth
- workload evidence, replay, diagnostics, operator registration, validator
  registration, and public-contract fences prove that overlap extraction
  consumed the real `7.4` loop ledger rather than raw loops, raw overlap
  chains, raw split fragments, or hand-filled overlap-region rows

Milestone `7.5` does **not** classify keep/discard fragment truth, assemble
faces, clean final topology, or certify final boolean legality. It freezes the
canonical overlap-region ledger those later phases must consume.

## Why This Milestone Exists

The tempting mistake after `7.4` is to treat coplanar overlap as a side helper:
scan loop pairs, notice coincident segments, infer whether they "kind of"
overlap, and feed ad hoc regions directly into classification.

That is not enough for Worth. Coplanar overlap is where loop truth first
becomes shared-area truth. If this milestone reopens raw loops, rediscovers
neighborhoods by repeated scans, collapses boundary contact into positive area,
lets opposite-sense coincidence choose winding by incidental ordering, or
allows later classification to inspect raw overlap-chain folklore instead of a
receipt-backed overlap ledger, the planar boolean lane becomes impossible to
trust.

`7.5` therefore makes overlap extraction a receipt-backed, phase-typed,
operator-named boundary. It still does not classify keep/discard semantics or
assemble result topology, but it must produce the exact canonical overlap
ledger that fragment classification and face assembly can consume without
rediscovering overlap meaning from loop geometry.

## Governing Summaries

- `MENTALITY.md`: protects adversarial-constraint-first design. `7.5` must
  solve the hostile overlap-storm, boundary-versus-area, and
  opposite-sense-normalization problem before fragment classification can claim
  progress.
- `arch_laws.md`: protects proof-bearing phase transitions and authority
  separation. Every overlap phase must consume the previous proof artifact and
  produce a stronger one; no phase may accept raw loops, raw chain bags, or
  generic region summaries when a receipt-backed artifact exists.
- `composition_laws.md`: protects semantic decomposition. Overlap request,
  participation recovery, adjacency indexing, island extraction,
  boundary-contact classification, shared-area admission, winding
  normalization, candidate promotion, identity, naming, diagnostics, and
  certification must remain separate named responsibilities.
- `domain_structure_laws.md`: protects visible ownership. Query remains the
  runtime entry and retained-artifact owner; `worth-kernel` owns workload
  composition and evidence pressure; `worth-spatial` owns coplanar overlap
  semantics and overlap-ledger artifacts; `worth-topo` remains topology truth
  and topology-operator authority.
- `perf_laws.md`: protects visible breadth and repeated-rediscovery denial.
  Overlap work must expose participation, adjacency, island, contact,
  admission, and winding counters rather than hiding broad pair scans behind
  cheap-looking APIs.
- `_docs/worth/milestone-7-roadmap.md`: protects `7.5` as coplanar overlap
  region extraction only. Fragment classification, face assembly, cleanup, and
  legality remain later `7.x` milestones.
- `_docs/worth/milestone-7.4.md`: protects loop-ledger authority. `7.5` must
  consume `PlanarBooleanLoopReconstructionLedgerReceipt` and must not rebuild
  split, continuation, walk, loop-role, or loop-island meaning from raw loop
  rows or raw split artifacts.
- `_docs/worth_topo/operators-list.md`: protects the operator vocabulary that
  makes overlap extraction explicit. `7.5` must name the overlap participation,
  island, boundary-only, shared-area, winding, and anti-theatre operators it
  adds instead of hiding them behind vague "extract overlap regions" prose.
- `_docs/worth_topo/validators.md`: protects validator-family closure. `7.5`
  must split validation work across enough phases that participation, adjacency,
  area honesty, winding normalization, naming, replay, and anti-theatre proof
  each get direct tests.
- `crates/forge-query/docs/AI_README.md`: protects the rule `declare intent
  once, lower it once, execute or inspect it through canonical runtime-owned
  artifacts`. `7.5` may add domain overlap artifacts, but it must not invent a
  caller-owned overlap route, pseudo-Query runtime lane, or local support
  posture.

## Adversarial Constraint

Given a real Query/workload-composed planar boolean operand pair that has
successfully produced a `7.4` loop reconstruction ledger, overlap-region
extraction must either:

- deny before overlap-ledger construction with a typed, localized,
  replay-stable reason

or:

- emit one canonical overlap-region ledger whose overlap request identity,
  participating loops, participating islands, overlap adjacency index, overlap
  island extraction, shared-boundary outcomes, shared-area admission outcomes,
  opposite-sense coincidence normalization, winding outcomes, admitted overlap
  regions, boundary-only overlap outcomes, identity, naming propagation,
  counters, decision log, and downstream-consumption identity remain stable
  across replay, benign loop-order variation, reversed source-edge sense,
  opposite-sense coincident boundary presentation, nested overlap islands,
  overlap-storm pressure, and shared-boundary-versus-shared-area pressure.

If `7.6` still has to ask raw loop, overlap-chain, or pairwise geometry
questions instead of consuming a `7.5` overlap-region ledger receipt, this
milestone has failed.

## Product Decision Lock

- `7.5` starts from `PlanarBooleanLoopReconstructionLedgerReceipt` and nowhere
  else.
- Query continues to own declaration, admission, support posture, runtime
  handles, retained artifact progression, receipts, envelopes, inspection, and
  ordinary outcomes.
- `worth-kernel` owns workload composition, stage requirements, evidence rows,
  catalog hostile recipes, public anti-theatre fences, and closeout pressure.
- `worth-spatial` owns overlap request semantics, participation recovery,
  adjacency indexing, island extraction, boundary-contact and shared-area
  outcomes, winding normalization, overlap identities, diagnostics, counters,
  and replay identity.
- `worth-topo` owns topology truth, topology operator public surfaces, and the
  eventual execution of authoritative face, shell, and body rewrites. `7.5`
  may prepare overlap-region topology intent and consume topology provenance,
  but it does not classify keep/discard fragments or finalize face truth.
- Every branch and outcome that affects overlap extraction must be typed and
  traceable. No boundary/contact choice, area-admission choice, or
  winding-normalization choice may survive only as inline syntax, string
  messages, or debug-only annotations.
- Persistent naming through overlap extraction is admitted as overlap-region
  lineage and subshape-signature propagation only; final face-level naming and
  merge/conflict semantics remain later roadmap work.
- `Milestone 8` remains EMBER. `7.5` stays in the B-rep planar lane.

## Artifact Ladder

`7.5` should be implemented as an explicit artifact ladder, not as one broad
"extract overlap regions" procedure.

Input:
- `PlanarBooleanLoopReconstructionLedgerReceipt`

Request:
- `PlanarBooleanOverlapRegionExtractionRequest`

Participation:
- `PlanarBooleanLoopOverlapParticipationMap`
- `PlanarBooleanLoopIslandOverlapParticipationMap`
- `PlanarBooleanOverlapChainRegionLineageMap`

Adjacency:
- `PlanarBooleanOverlapRegionAdjacencyIndex`
- `PlanarBooleanOverlapAdjacencyOrderingBasis`

Arrangement:
- `PlanarBooleanCoplanarOverlapArrangementGraph`
- `PlanarBooleanOverlapArrangementCellSet`
- `PlanarBooleanOverlapCellContainmentMap`
- `PlanarBooleanOverlapCellWindingField`

Islanding and components:
- `PlanarBooleanOverlapIslandCandidateSet`
- `PlanarBooleanOverlapIslandSet`
- `PlanarBooleanBoundaryContactComponentSet`
- `PlanarBooleanAreaOverlapComponentSet`

Contact and area:
- `PlanarBooleanSharedBoundaryContactOutcomeSet`
- `PlanarBooleanPureBoundaryOnlyOutcomeSet`
- `PlanarBooleanSharedAreaAdmissionOutcomeSet`
- `PlanarBooleanMixedBoundaryAreaOutcomeSet`

Normalization:
- `PlanarBooleanOppositeSenseOverlapNormalizationSet`
- `PlanarBooleanOverlapRegionCanonicalWindingSet`

Region candidates:
- `PlanarBooleanOverlapRegionCandidateSet`
- `PlanarBooleanDeniedOverlapRegionCandidateSet`

Region products:
- `PlanarBooleanAdmittedOverlapRegionSet`
- `PlanarBooleanBoundaryOnlyOverlapOutcomeSet`

Identity and naming:
- `PlanarBooleanOverlapRegionIdentityMap`
- `PlanarBooleanOverlapRegionPersistentNamePropagationMap`
- `PlanarBooleanOverlapRegionSubshapeSignatureMap`

Ledger:
- `PlanarBooleanOverlapRegionDecisionLog`
- `PlanarBooleanOverlapRegionLedger`
- `PlanarBooleanOverlapRegionLedgerReceipt`

## Overlap Ledger Exclusivity Law

After `7.5`, every `7.6+` planar boolean phase must consume
`PlanarBooleanOverlapRegionLedgerReceipt` and must not consume raw loops, raw
loop islands, raw overlap chains, raw pairwise-contact scans, or host-local
overlap summaries as substitutes for overlap-region truth.

## Existing Surface Inventory

Milestone `7.5` should widen live surfaces before inventing new ones:

- `crates/worth-spatial/src/facade/planar_boolean_loop_reconstruction.rs`
  - `PlanarBooleanLoopReconstructionLedgerReceipt`
  - reconstructed-loop, born-loop, island-partition, role, identity, naming,
    and replay surfaces from `7.4`
- `crates/worth-spatial/src/workload_platform/planar_boolean_loop_reconstruction/*`
  - request, provenance, continuation, walk, candidate, island, identity,
    replay, and proof-chain support produced by `7.4`
- `crates/worth-spatial/src/workload_platform/planar_boolean_edge_splitting/*`
  - overlap-chain lineage and split-ledger provenance that `7.4` already
    carried forward
- `crates/worth-spatial/src/workload_platform/evidence_ledger/*`
  - `BooleanEvidenceReceipt`
  - `BooleanEvidenceStageKind`
  - `CompleteWorkloadEvidenceLedger`
  - typed stage-index products from `7.4`
- `crates/worth-kernel/src/workload_composition/*`
  - stage requirements, workload evidence requirements, and hostile workload
    catalog recipes
- `crates/worth-topo/src/topology_operators/*`
  - loop reconstruction blueprint precedent for operator classification and
    validator registration
- `crates/forge-query/docs/AI_README.md`
  - ordinary domain work starts at Query
  - intent must be declared once, lowered once, and executed or inspected
    through canonical runtime-owned artifacts
  - graph/index views are proof boundaries, not late performance cleanup

New `7.5` surfaces are allowed where existing surfaces cannot honestly express:

- a proof-bearing overlap-region extraction request from a loop-ledger receipt
- loop and island participation recovered from loop-ledger products and
  overlap-chain lineage
- a first-class overlap adjacency index and canonical ordering basis
- a coplanar arrangement graph, arrangement cell set, containment map, and
  operand winding field
- typed shared-boundary, mixed-boundary-area, shared-area, and
  winding-normalization outcomes
- one canonical overlap-region ledger receipt consumed by `7.6`
- overlap evidence rows and public-contract fences against synthetic overlap
  proof

## Query, Graph, And Invariant Integration Contract

`7.5` must distinguish three different things that are easy to collapse:

- spatial overlap products
  - prepared artifacts owned by `worth-spatial`
  - examples: overlap request, participation maps, adjacency indexes, island
    sets, boundary-only outcomes, shared-area outcomes, winding outcomes,
    overlap-region products, overlap ledger
  - these do not classify final fragment keep/discard truth by themselves
- topology operator declarations
  - Query-declared topology mutation intent owned by `worth-topo`
  - examples: future consumed forms of face assembly, hole attachment, or
    shell restructuring
  - these must implement `ForgeQueryDeclarationInput<TopologyQueryDomain>` and
    travel through `TopologyOperatorWorkflowHandleExt` declaration, review,
    progression, route, receipt, envelope, grouped, contribution, and recovery
    surfaces where applicable
- validators and legality rules
  - domain invariant meaning owned by Worth/topology/spatial semantics
  - where they block graph-shaped overlap authoring, they must be registered or
    invoked through Query invariant-pack / domain-invariant denial posture
    rather than remembered by overlap executors as manual checks

This means:

- a new `7.5` operator name in the spec is not automatically a public mutation
  method
- overlap-region operators in this milestone should usually remain prepared
  overlap-ledger artifacts in `worth-spatial`, not premature face-mutation
  declarations
- graph-shaped same-batch authoring must use Query graph composition or the
  existing topology grouped/contribution workflow, not caller-owned batch
  choreography
- overlap neighborhood lookup must have a first-class index product before
  island extraction consumes participating loops; repeated pair scans are
  allowed only as a hostile baseline fixture, never as the
  production-confidence proof path
- validator families must be attached to declared graph shape, overlap-ledger
  consumption, or topology operator declaration families so the runtime can
  emit typed denials; "remember to run validator X" is not an admitted plan
- overlap evidence rows are not validator execution. Evidence proves the stage
  happened; registered/domain validators prove the stage is legal

## Required Denial Taxonomy

Milestone `7.5` must expose explicit typed denial families rather than relying
on one generic "overlap failed" posture.

At minimum, the overlap-region lane must preserve denial classes in this
neighborhood:

- `NonCoplanarLoopLedgerInputDenied`
- `ForeignLoopLedgerReceiptDenied`
- `DanglingLoopParticipationDenied`
- `ContradictoryIslandMembershipDenied`
- `UnindexedOverlapNeighborhoodDenied`
- `AmbiguousCoincidentSegmentOrderingDenied`
- `BoundaryOnlyAreaAdmissionDenied`
- `MixedBoundaryAreaRequiresCellDecompositionDenied`
- `ZeroAreaRegionDenied`
- `UnstableWindingTieBreakerDenied`
- `ConflictingPersistentNamePropagationDenied`
- `SyntheticOverlapLedgerDenied`

Implementations may refine this taxonomy, but they may not collapse these
distinct refusal classes into one generic denial bucket or one stringly stop.

## Minimal Honest 7.5 Path

The first honest implementation target for `7.5` is:

1. Build canonical participation rows from `7.4` loop-ledger identity.
2. Build a canonical overlap adjacency/index product from those participation
   rows and overlap-chain lineage.
3. Build a coplanar arrangement/cell decomposition for the participating loop
   neighborhood.
4. Classify arrangement cells by operand containment and winding membership.
5. Separate positive-area overlap cells from boundary-only contact components
   without collapsing mixed islands.
6. Canonicalize admitted region boundaries and winding.
7. Mint stable region identities and lineage.
8. Emit one overlap-region ledger receipt consumed by `7.6`.

## Phase Plan

### Phase 1: Overlap-Region Request Boundary

Freeze the only artifact that may enter `7.5`: a request built from the `7.4`
loop reconstruction ledger receipt.

**Consumes**
- `PlanarBooleanLoopReconstructionLedgerReceipt`

**Produces**
- `PlanarBooleanOverlapRegionExtractionRequest`

**Relevant subsystems**
- `worth-kernel` workload composition
- `worth-spatial` planar boolean loop reconstruction and overlap extraction
- Query retained artifact progression

**Relevant APIs**
- `PlanarBooleanLoopReconstructionLedgerReceipt`
- new `PlanarBooleanOverlapRegionExtractionRequest`

**Warnings**
- Do not accept raw reconstructed loops, raw born loops, raw overlap chains, or
  copied loop-ledger fields as substitutes for the request artifact.
- Do not add a second overlap extraction route in `worth-spatial` that the
  kernel cannot evidence.

**Test requirements**
- Adversarial parity test: the same loop ledger must produce the same
  overlap-region extraction request identity across replay.
- Adversarial rejection test: a synthetic overlap request built from raw loops
  or copied receipt fields must fail to enter the extraction boundary.

**Engineering decisions**
- The request boundary is a proof transition, not an ergonomic constructor.
- Preserve the `7.4` loop-ledger, decision-log, and naming receipt identities
  so later overlap evidence rows can point back to real loop truth.

**Open questions**
- None.

### Phase 2: Overlap Extraction Surface Contract And Registration

Freeze the operator classification matrix and validator registration plan before
overlap functionality spreads across crates.

**Consumes**
- milestone-scoped overlap operator inventory
- milestone-scoped overlap validator inventory

**Produces**
- `PlanarBooleanOverlapOperatorClassificationMatrix`
- `PlanarBooleanOverlapValidatorRegistrationPlan`

**Relevant subsystems**
- `worth-topo`
- `worth-spatial`
- Query declaration and graph-composition surfaces

**Relevant APIs**
- topology operator declaration families
- Query invariant registration
- grouped and contribution workflow precedent where naming propagation is needed

**Warnings**
- Do not let overlap operators remain unnamed helpers.
- Do not defer validator registration until closeout; the runtime denial surface
  must be designed with the operators, not added after them.
- Do not prematurely classify `7.5` overlap operators as face-assembly
  declarations when the milestone still owns prepared overlap products.

**Test requirements**
- Adversarial parity test: every new `7.5` operator must map to exactly one
  classification class in the operator matrix.
- Adversarial rejection test: an overlap-affecting operator missing Query or
  validator registration evidence must fail closeout certification.

**Engineering decisions**
- Treat operator growth and validator growth as first-class milestone outputs,
  not implementation notes.
- Keep topology declaration growth minimal in `7.5`; final face mutation
  authority remains later roadmap work.

**Open questions**
- None.

### Phase 3: Loop Participation And Overlap Lineage Recovery

Recover the loop, island, and overlap-chain lineage that overlap extraction is
allowed to consume.

**Consumes**
- `PlanarBooleanOverlapRegionExtractionRequest`
- `PlanarBooleanLoopReconstructionLedgerReceipt`

**Produces**
- `PlanarBooleanLoopOverlapParticipationMap`
- `PlanarBooleanLoopIslandOverlapParticipationMap`
- `PlanarBooleanOverlapChainRegionLineageMap`

**Relevant subsystems**
- `worth-spatial`
- `worth-kernel`

**Relevant APIs**
- loop-ledger reconstructed-loop, born-loop, island-partition, and naming
  surfaces
- split-ledger overlap-chain lineage surfaces inherited through `7.4`

**Warnings**
- Do not flatten source-loop role, island origin, or overlap-chain lineage into
  anonymous region candidates.
- Do not recover participation by re-walking raw split fragments or
  continuation maps.

**Test requirements**
- Adversarial parity test: the same loop ledger must produce the same overlap
  participation rows and lineage rows across replay and benign ordering
  variation.
- Adversarial rejection test: dangling loop references, contradictory island
  participation, or foreign overlap-chain lineage must deny before adjacency
  construction.

**Engineering decisions**
- Participation recovery is a first-class proof product, not a prelude hidden
  inside adjacency building.
- Preserve enough lineage here for later shared-area and winding outcomes to be
  justified without reopening loop truth.

**Open questions**
- None.

### Phase 4: Overlap Adjacency Index And Canonical Ordering

Freeze the first-class overlap neighborhood/index product and the canonical
ordering basis that island extraction must consume.

**Consumes**
- `PlanarBooleanLoopOverlapParticipationMap`
- `PlanarBooleanLoopIslandOverlapParticipationMap`
- `PlanarBooleanOverlapChainRegionLineageMap`

**Produces**
- `PlanarBooleanOverlapRegionAdjacencyIndex`
- `PlanarBooleanOverlapAdjacencyOrderingBasis`

**Relevant subsystems**
- `worth-spatial`

**Relevant APIs**
- overlap participation maps
- overlap-chain lineage maps
- canonical ordering and counter surfaces

**Warnings**
- Do not allow repeated pairwise loop scans to masquerade as production overlap
  extraction.
- Do not let incidental map iteration order choose adjacency order or
  normalization precedence.

**Test requirements**
- Adversarial parity test: the same participation product must produce the same
  adjacency index and ordering basis across replay and benign input-order
  variation.
- Adversarial rejection test: any extraction path that needs unindexed overlap
  discovery or iteration-order tie breaks must fail.

**Engineering decisions**
- The adjacency index is a proof boundary, not a late optimization.
- This phase is where `ValidateNoNPlusOneOverlapNeighborhoodDiscovery` earns
  its meaning.

**Open questions**
- None.

### Phase 5: Coplanar Arrangement Graph Construction

Build the canonical planar subdivision that turns overlap neighborhoods into
real area-bearing cells rather than rhetorical "shared area" posture.

**Consumes**
- `PlanarBooleanOverlapRegionAdjacencyIndex`
- `PlanarBooleanOverlapAdjacencyOrderingBasis`

**Produces**
- `PlanarBooleanCoplanarOverlapArrangementGraph`
- `PlanarBooleanOverlapArrangementCellSet`

**Relevant subsystems**
- `worth-spatial`

**Relevant APIs**
- overlap adjacency index
- arrangement graph and arrangement cell surfaces

**Warnings**
- Do not let "shared area" remain a yes/no gate without a concrete planar cell
  substrate.
- Do not let arrangement construction secretly rediscover raw loops or raw
  pairwise geometry outside the admitted adjacency/index product.

**Test requirements**
- Adversarial parity test: the same adjacency index must yield the same
  arrangement graph and arrangement cell set across replay, benign ordering
  variation, and overlap-storm pressure.
- Adversarial rejection test: disconnected or contradictory neighborhoods that
  cannot support one canonical planar subdivision must deny before containment
  or area admission.

**Engineering decisions**
- Arrangement construction is the geometric middle of the milestone. Shared
  area must be constructed through cells, not inferred from prose.
- Later phases may aggregate cells into islands or regions, but they may not
  skip this arrangement substrate.

**Open questions**
- None.

### Phase 6: Overlap Cell Containment And Operand Winding Field

Classify arrangement cells by operand containment and operand-local winding so
later phases can distinguish "inside both" from mere coincidence.

**Consumes**
- `PlanarBooleanCoplanarOverlapArrangementGraph`
- `PlanarBooleanOverlapArrangementCellSet`

**Produces**
- `PlanarBooleanOverlapCellContainmentMap`
- `PlanarBooleanOverlapCellWindingField`

**Relevant subsystems**
- `worth-spatial`

**Relevant APIs**
- arrangement graph and arrangement cell surfaces
- containment-map and winding-field surfaces

**Warnings**
- Do not let shared-area admission run on cells that do not yet have explicit
  containment and winding evidence.
- Do not assume post-admission canonical winding can substitute for the
  operand-local winding truth needed to build honest area membership.

**Test requirements**
- Adversarial parity test: the same arrangement cells must produce the same
  containment map and operand winding field across replay and benign ordering
  variation.
- Adversarial rejection test: any path that reaches shared-area admission
  without explicit containment or winding evidence must deny.

**Engineering decisions**
- This phase owns the "inside both operands" proof family that later
  classification must not recompute.
- Pre-admission orientation truth belongs here, not in the later canonical
  winding phase.

**Open questions**
- None.

### Phase 7: Overlap Island Extraction And Component Partition

Aggregate arrangement cells and contact structure into overlap islands and typed
component partitions without yet deciding final region admission.

**Consumes**
- `PlanarBooleanOverlapArrangementCellSet`
- `PlanarBooleanOverlapCellContainmentMap`
- `PlanarBooleanOverlapCellWindingField`

**Produces**
- `PlanarBooleanOverlapIslandCandidateSet`
- `PlanarBooleanOverlapIslandSet`
- `PlanarBooleanBoundaryContactComponentSet`
- `PlanarBooleanAreaOverlapComponentSet`

**Relevant subsystems**
- `worth-spatial`

**Relevant APIs**
- arrangement-cell, containment, and winding surfaces
- island and component partition surfaces

**Warnings**
- Do not treat boundary-only as an island-level fact before components prove
  whether the island also contains positive-area cells.
- Do not collapse mixed boundary-and-area islands into one blunt outcome.

**Test requirements**
- Adversarial parity test: the same arrangement, containment, and winding
  products must produce the same overlap islands and component partitions across
  replay and benign ordering variation.
- Adversarial rejection test: contradictory component membership or mixed
  islands that cannot yet be partitioned honestly must deny before
  boundary-only or area outcomes are emitted.

**Engineering decisions**
- Islanding and component partition must happen after cells exist and before
  boundary-only or area admission is finalized.
- Mixed boundary/area neighborhoods are first-class hostile cases, not strange
  edge cases to smooth over later.

**Open questions**
- None.

### Phase 8: Boundary Contact Component Classification

Classify pure boundary-contact components and keep them structurally separate
from any island that still carries positive-area overlap components.

**Consumes**
- `PlanarBooleanBoundaryContactComponentSet`
- `PlanarBooleanAreaOverlapComponentSet`
- `PlanarBooleanOverlapIslandSet`

**Produces**
- `PlanarBooleanSharedBoundaryContactOutcomeSet`
- `PlanarBooleanPureBoundaryOnlyOutcomeSet`

**Relevant subsystems**
- `worth-spatial`

**Relevant APIs**
- boundary-contact component surfaces
- pure-boundary-only outcome surfaces

**Warnings**
- Do not classify an entire island as boundary-only if it still contains an
  admitted area component nearby.
- Do not hide mixed islands by emitting only pure-boundary outcomes.

**Test requirements**
- Adversarial parity test: the same component partitions must produce the same
  shared-boundary and pure-boundary-only outcomes across replay and benign
  ordering variation.
- Adversarial rejection test: any path that turns a mixed island into a
  pure-boundary island without cell/component proof must deny.

**Engineering decisions**
- Boundary-only is component-scoped first and island-scoped only after the
  absence of area-bearing cells is proven.
- This phase exists to prevent the second most tempting naive collapse in the
  milestone.

**Open questions**
- None.

### Phase 9: Shared-Area Cell Admission And Mixed-Island Denial Boundary

Admit positive-area overlap components and deny mixed or contradictory islands
that still require more than the milestone admits.

**Consumes**
- `PlanarBooleanAreaOverlapComponentSet`
- `PlanarBooleanOverlapCellContainmentMap`
- `PlanarBooleanOverlapCellWindingField`
- `PlanarBooleanPureBoundaryOnlyOutcomeSet`

**Produces**
- `PlanarBooleanSharedAreaAdmissionOutcomeSet`
- `PlanarBooleanMixedBoundaryAreaOutcomeSet`

**Relevant subsystems**
- `worth-spatial`

**Relevant APIs**
- area-overlap component surfaces
- shared-area and mixed-boundary-area outcome surfaces

**Warnings**
- Do not admit area overlap merely because a coincident neighborhood exists.
- Do not let mixed boundary/area islands pass as admitted regions until the
  component-level proof says they are honest.

**Test requirements**
- Adversarial parity test: the same area-overlap components must produce the
  same shared-area admission results across replay and benign ordering
  variation.
- Adversarial rejection test: boundary-only components, zero-area cells, or
  mixed islands that still need deeper decomposition must deny with typed
  localization.

**Engineering decisions**
- Shared-area admission is cell-backed and component-backed, not island-rhetoric
  backed.
- `MixedBoundaryAreaRequiresCellDecompositionDenied` is load-bearing here.

**Open questions**
- None.

### Phase 10: Pre-Region Coincidence And Orientation Normalization

Normalize opposite-sense coincidence and source-orientation ambiguity enough
that admitted overlap components can become region candidates honestly.

**Consumes**
- `PlanarBooleanSharedAreaAdmissionOutcomeSet`
- `PlanarBooleanOverlapChainRegionLineageMap`

**Produces**
- `PlanarBooleanOppositeSenseOverlapNormalizationSet`

**Relevant subsystems**
- `worth-spatial`

**Relevant APIs**
- shared-area admission outcomes
- overlap-chain lineage surfaces
- opposite-sense normalization surfaces

**Warnings**
- Do not let operand order, edge sense, or coincident segment ordering choose
  region-candidate orientation by accident.
- Do not hide pre-region normalization decisions in later identity or ledger
  assembly phases.

**Test requirements**
- Adversarial parity test: the same admitted area components must produce the
  same opposite-sense normalization outcomes across replay, operand-order
  variation, and reversed coincident boundary sense.
- Adversarial rejection test: ambiguous coincident ordering or unstable
  orientation tie-breakers must deny before region candidate promotion.

**Engineering decisions**
- Pre-region normalization is distinct from the later canonical winding surface
  consumed by downstream phases.
- This phase carries only the normalization needed to promote honest region
  candidates.

**Open questions**
- None.

### Phase 11: Overlap-Region Candidate Promotion And Product Boundary

Promote admitted normalized overlap components into canonical overlap-region
products and fence denied candidates before post-admission canonical winding.

**Consumes**
- `PlanarBooleanSharedAreaAdmissionOutcomeSet`
- `PlanarBooleanMixedBoundaryAreaOutcomeSet`
- `PlanarBooleanOppositeSenseOverlapNormalizationSet`

**Produces**
- `PlanarBooleanOverlapRegionCandidateSet`
- `PlanarBooleanDeniedOverlapRegionCandidateSet`
- `PlanarBooleanAdmittedOverlapRegionSet`
- `PlanarBooleanBoundaryOnlyOverlapOutcomeSet`

**Relevant subsystems**
- `worth-spatial`

**Relevant APIs**
- overlap candidate, admitted-region, denied-candidate, and boundary-only
  outcome surfaces

**Warnings**
- Do not auto-promote every normalized component into an admitted overlap
  region.
- Do not erase denied-candidate locality just because an island already
  admitted some area-bearing components.

**Test requirements**
- Adversarial parity test: the same admitted shared-area and normalization
  outcomes must promote to the same overlap-region candidate and admitted
  product sets across replay.
- Adversarial rejection test: candidates with unresolved area, unresolved
  normalization, or contradictory promotion posture must deny before canonical
  winding and identity minting.

**Engineering decisions**
- Candidate promotion is a proof boundary distinct from both pre-region and
  post-admission winding.
- Boundary-only outcomes stay first-class products rather than "failed region"
  leftovers.

**Open questions**
- None.

### Phase 12: Post-Admission Canonical Winding And Boundary Normalization

Canonicalize the admitted overlap-region boundary and winding surface that
`7.6+` downstream consumers may treat as stable.

**Consumes**
- `PlanarBooleanAdmittedOverlapRegionSet`
- `PlanarBooleanBoundaryOnlyOverlapOutcomeSet`

**Produces**
- `PlanarBooleanOverlapRegionCanonicalWindingSet`

**Relevant subsystems**
- `worth-spatial`

**Relevant APIs**
- admitted overlap-region surfaces
- canonical winding surfaces

**Warnings**
- Do not let post-admission canonical winding rewrite area membership truth.
- Do not let downstream consumers use pre-region normalization as a substitute
  for the final canonical winding surface.

**Test requirements**
- Adversarial parity test: the same admitted overlap-region set must produce
  the same canonical winding set across replay and benign ordering variation.
- Adversarial rejection test: any path that yields multiple plausible final
  canonical windings without a typed denial must fail certification.

**Engineering decisions**
- Post-admission canonical winding is distinct from the pre-region orientation
  work needed for area truth.
- `7.6` should consume this final winding surface, not recompute its own.

**Open questions**
- None.

### Phase 13: Overlap-Region Identity, Persistent Naming, And Lineage Propagation

Mint canonical overlap-region identities and carry the limited naming and
lineage surfaces this milestone is allowed to own.

**Consumes**
- `PlanarBooleanAdmittedOverlapRegionSet`
- `PlanarBooleanBoundaryOnlyOverlapOutcomeSet`
- `PlanarBooleanOverlapRegionCanonicalWindingSet`

**Produces**
- `PlanarBooleanOverlapRegionIdentityMap`
- `PlanarBooleanOverlapRegionPersistentNamePropagationMap`
- `PlanarBooleanOverlapRegionSubshapeSignatureMap`

**Relevant subsystems**
- `worth-spatial`
- `worth-topo`
- Query contribution workflow surfaces

**Relevant APIs**
- identity minting surfaces
- persistent naming and lineage propagation surfaces
- topology contribution workflow precedent

**Warnings**
- Do not let persistent naming widen into final face identity, hole ownership,
  shell/body ownership, or final topology conflict resolution.
- Do not let identity minting reopen area, normalization, or canonical winding
  semantics.

**Test requirements**
- Adversarial parity test: the same admitted overlap-region set must produce
  the same region identities, persistent names, and subshape signatures across
  replay and benign ordering variation.
- Adversarial rejection test: conflicting persistent naming survival, duplicate
  region identity minting, or dangling name references must deny.

**Engineering decisions**
- `7.5` may produce names for overlap-region identity, source loop provenance,
  source island provenance, source overlap-chain lineage, boundary-contact
  component identity, area-cell/region identity, and subshape-signature seeds.
- `7.5` may not decide final face identity, merged face identity, post-boolean
  surviving face identity, hole ownership after assembly, shell/body ownership,
  or final topology name conflict resolution.

**Open questions**
- None.

### Phase 14: Overlap Decision Log And Overlap Ledger Assembly

Assemble the canonical overlap-region ledger and record every typed branch and
downstream-visible outcome that affected extraction.

**Consumes**
- `PlanarBooleanAdmittedOverlapRegionSet`
- `PlanarBooleanBoundaryOnlyOverlapOutcomeSet`
- `PlanarBooleanOverlapRegionCanonicalWindingSet`
- `PlanarBooleanOverlapRegionIdentityMap`
- `PlanarBooleanOverlapRegionPersistentNamePropagationMap`
- `PlanarBooleanOverlapRegionSubshapeSignatureMap`

**Produces**
- `PlanarBooleanOverlapRegionDecisionLog`
- `PlanarBooleanOverlapRegionLedger`
- `PlanarBooleanOverlapRegionLedgerReceipt`

**Relevant subsystems**
- `worth-spatial`
- `worth-kernel`
- Query graph-composition and artifact-envelope surfaces

**Relevant APIs**
- overlap decision-log and ledger receipt surfaces
- structured failure localization surfaces

**Warnings**
- Do not summarize area, winding, or boundary-only outcomes without retaining
  typed decision rows.
- Do not let overlap-ledger assembly change operational truth based on
  diagnostic richness.

**Test requirements**
- Adversarial parity test: the same admitted overlap path must produce the same
  overlap decision-log digest and overlap-ledger digest across replay.
- Adversarial rejection test: missing decision rows, missing naming receipts,
  or ledger rows that cannot be justified from prior proof products must deny.

**Engineering decisions**
- The overlap ledger is the only downstream product accepted by `7.6`.
- Every branch and outcome affecting downstream consumption must be represented
  in typed decision-log rows or typed ledger rows.
- The Overlap Ledger Exclusivity Law becomes operational here: after this
  phase, later milestones must consume
  `PlanarBooleanOverlapRegionLedgerReceipt` rather than reopening loops,
  islands, adjacency scans, arrangement cells, or overlap folklore.

**Open questions**
- None.

### Phase 15: Evidence, Replay, Determinism, And Anti-Theatre Fences

Make overlap extraction a real workload stage with typed evidence and prove that
the canonical overlap ledger is replay-safe, checkpoint-safe, and fenced
against synthetic products.

**Consumes**
- `PlanarBooleanOverlapRegionLedgerReceipt`
- `PlanarBooleanOverlapOperatorClassificationMatrix`
- `PlanarBooleanOverlapValidatorRegistrationPlan`

**Produces**
- `PlanarBooleanOverlapRegionEvidenceReceipt`
- `PlanarBooleanOverlapRegionStageRequirement`
- `PlanarBooleanOverlapRegionReplayParityReceipt`
- `PlanarBooleanOverlapRegionCheckpointParityReceipt`
- `PlanarBooleanOverlapRegionPublicContractFenceProof`
- `PlanarBooleanOverlapRegionAntiTheatreFenceProof`

**Relevant subsystems**
- `worth-kernel`
- `worth-spatial`
- `worth-topo`
- compile-fail public facade contract surfaces

**Relevant APIs**
- `BooleanEvidenceReceipt`
- `WorkloadStageRequirement`
- overlap-ledger receipts
- replay parity receipts
- retained / checkpoint parity surfaces

**Warnings**
- Do not let manual overlap ledgers or copied overlap rows satisfy workload
  requirements.
- Do not call replay success closeout if canonical region identities, area
  outcomes, canonical winding, or naming outcomes drift.
- Do not rely on raw loop, raw chain, or raw arrangement reconstruction inside
  replay proof.

**Test requirements**
- Adversarial parity test: real workload-backed overlap extraction should
  satisfy the same stage requirement and produce the same ledger across replay,
  retained consumption, and checkpoint consumption.
- Adversarial rejection test: synthetic overlap evidence, raw overlap rows, or
  replay/checkpoint paths that require reopening raw loops, raw scans, or
  bypassed arrangement/cell proofs must fail.

**Engineering decisions**
- Overlap extraction gets its own evidence boundary rather than piggy-backing on
  loop evidence.
- Determinism is part of overlap truth, not a later polishing step.
- Anti-theatre proof belongs before the summum bonum phase so the hostile
  closeout cannot accidentally lean on synthetic fixtures.

**Open questions**
- None.

### Phase 16: Summum Bonum Closeout Certification

Run one hostile certification program that proves `7.5` survives the exact
pressure that would break a naive overlap extractor.

**Consumes**
- `PlanarBooleanOverlapRegionLedgerReceipt`
- `PlanarBooleanOverlapRegionEvidenceReceipt`
- replay, checkpoint, and public-contract fence proof from `Phase 15`

**Produces**
- `PlanarBooleanOverlapRegionSummumBonumCloseout`

**Relevant subsystems**
- `worth-kernel`
- `worth-spatial`
- workload catalog hostile recipes

**Relevant APIs**
- overlap-region ledger receipt
- workload catalog hostile recipes
- overlap replay / checkpoint parity surfaces

**Warnings**
- Do not use synthetic overlap-region rows, hand-filled islands, hand-filled
  arrangement cells, or local helper fixtures as the primary closeout proof.
- Do not let the metaboss quietly reopen split, loop, arrangement,
  containment, or classification work.

**Test requirements**
- Adversarial parity test: the same admitted hostile workload must produce the
  same overlap-region ledger digest, region identities, shared-area outcomes,
  boundary-only outcomes, and canonical winding outcomes across replay, benign
  ordering variation, and checkpoint consumption.
- Adversarial rejection test: any hostile scenario that attempts to turn
  boundary-only contact into area overlap, to let opposite-sense coincidence
  drift canonical winding, to collapse mixed boundary/area islands, or to
  bypass the ledger receipt with raw loop data must deny with typed
  localization.

**Engineering decisions**
- The summum bonum closeout test for `7.5` is
  `planar_boolean_overlap_region_metaboss_is_canonical_replayable_area_honest_and_unforgeable`.
- The metaboss bundle should contain named hostile subcases, not one opaque
  mega fixture.

**Open questions**
- None.

## Operator Inventory

Milestone `7.5` adds or closes these operator families at overlap-region scope:

- construction operators:
  - `ConsumePlanarBooleanLoopReconstructionLedger`
  - `DeclarePlanarBooleanOverlapRegionExtraction`
  - `AdmitPlanarBooleanOverlapRegionExtraction`
  - `BindOverlapExtractionToLoopLedgerReceipt`
  - `RecoverLoopOverlapParticipation`
  - `BindOverlapChainToLoopIslands`
  - `BuildOverlapRegionAdjacencyIndex`
  - `CanonicalizeOverlapAdjacencyOrder`
  - `BuildCoplanarOverlapArrangementGraph`
  - `BuildOverlapArrangementCellSet`
  - `BuildOverlapCellContainmentMap`
  - `BuildOverlapCellWindingField`
  - `ExtractOverlapIslands`
  - `PartitionBoundaryContactComponents`
  - `PartitionAreaOverlapComponents`
- admission and denial operators:
  - `ClassifySharedBoundaryContact`
  - `BuildPureBoundaryOnlyOutcome`
  - `AdmitSharedAreaOverlap`
  - `RejectBoundaryOnlyContactAsAreaRegion`
  - `RejectMixedBoundaryAreaWithoutCellClosure`
  - `NormalizeOppositeSenseOverlapWinding`
  - `PromoteOverlapRegionCandidate`
  - `RejectOverlapRegionCandidateBeforeIdentity`
- canonicalization operators:
  - `BuildOverlapRegionCanonicalWinding`
  - `CanonicalizeOverlapRegionBoundary`
- identity, naming, and lineage operators:
  - `MintBooleanOverlapRegionIdentity`
  - `PropagatePersistentNamesThroughOverlapRegionExtraction`
  - `RecordOverlapRegionEntityParentage`
  - `ForkOverlapRegionEntityLineage`
- ledger and evidence operators:
  - `RegisterOverlapRegionGraphInvariantPack`
  - `RecordOverlapRegionDecisionLog`
  - `LocalizeOverlapRegionExtractionFailure`
  - `BuildStructuredOverlapRegionFailureReport`
  - `AssemblePlanarBooleanOverlapRegionLedger`
  - `BuildOverlapRegionLedgerReceipt`
  - `RequireBooleanOverlapRegionEvidence`
  - `RegisterBooleanOverlapRegionStageRequirement`
  - `ReplayPlanarBooleanOverlapRegionExtraction`
  - `CompareOverlapRegionReplayParity`
  - `CompareOverlapRegionCheckpointParity`
- public-contract fences:
  - `RejectSyntheticOverlapRegionEntry`
  - `RejectUnindexedOverlapParticipation`
  - `FenceFragmentClassificationToOverlapRegionLedgerReceipt`
  - `ValidateOverlapRegionValidatorRuntimeRegistration`
  - `RejectSyntheticOverlapRegionLedgerConstruction`

No `7.5` operator may remain an unclassified helper name.
`7.5` should keep topology declaration growth minimal; face-assembly and final
topology truth remain later milestones.

## Validator Inventory

Milestone `7.5` closes these validator families at overlap-region scope:

- request and receipt validators:
  - `ValidatePlanarBooleanLoopLedgerConsumption`
  - `ValidateOverlapRegionReceiptEnvelopeConsistency`
  - `ValidateOverlapRegionLedgerReceiptChain`
  - `RejectOverlapRegionLedgerMissingDecisionLogReceipt`
  - `RejectOverlapRegionLedgerMissingPersistentNamingReceipt`
  - `RejectOverlapRegionLedgerForeignProductLineage`
- participation, adjacency, and arrangement validators:
  - `ValidateOverlapParticipationCoverage`
  - `ValidateOverlapChainLineageCoverage`
  - `ValidateLoopIslandOverlapBindingConsistency`
  - `ValidateNoDanglingOverlapParticipationReferences`
  - `ValidateCanonicalOverlapAdjacencyOrderingStable`
  - `ValidateNoNPlusOneOverlapNeighborhoodDiscovery`
  - `ValidateCoplanarOverlapArrangementConstruction`
  - `ValidateOverlapArrangementCellCoverage`
- containment, contact, and area validators:
  - `ValidateOverlapCellContainmentConsistency`
  - `ValidateOverlapCellWindingFieldConsistency`
  - `ValidateOverlapIslandExtractionConsistency`
  - `ValidateBoundaryContactComponentPartition`
  - `ValidateAreaOverlapComponentPartition`
  - `ValidateSharedBoundaryContactClassificationConsistency`
  - `ValidateSharedAreaAdmissionConsistency`
  - `ValidateBoundaryOnlyIsNotAdmittedAreaRegion`
  - `ValidateMixedBoundaryAreaLocalization`
- normalization and product validators:
  - `ValidateOppositeSenseOverlapWindingNormalization`
  - `ValidateOverlapRegionCanonicalWindingConsistency`
  - `ValidateOverlapRegionPromotionBoundary`
  - `ValidateBoundaryOnlyOverlapLocalization`
  - `ValidateCoplanarOverlapRegionExtraction`
- identity, naming, and diagnostics validators:
  - `ValidateOverlapRegionIdentityCanonicality`
  - `ValidatePersistentNameUniqueness`
  - `ValidateNameSurvivalThroughOverlapRegionExtraction`
  - `ValidateNoDanglingNameReferences`
  - `ValidateOverlapRegionDecisionLogCoverage`
  - `ValidateOverlapRegionFailureLocalizationConsistency`
- determinism and replay validators:
  - `ValidateCanonicalOrderingStable`
  - `ValidateHashStabilityAcrossRuns`
  - `ValidateTieBreakerCoverage`
  - `ValidatePlanarBooleanOverlapRegionReplayParity`
  - `ValidatePlanarBooleanOverlapRegionCheckpointParity`
- Query/runtime registration validators:
  - `ValidateOverlapRegionValidatorRuntimeRegistration`
  - `ValidateOverlapRegionGraphInvariantPackRegistration`
  - `ValidatePreparedSpatialOverlapProductsCannotMutateTopologyTruth`

## Anti-Theatre Standard

For `7.5`, the following count as theatre and are not admitted as closeout
proof:

- constructing `PlanarBooleanOverlapRegionLedger` directly in tests
- bypassing `PlanarBooleanLoopReconstructionLedgerReceipt`
- using raw loop arrays as overlap extractor input
- asserting overlap regions without arrangement/cell evidence
- using fixture-only region IDs not derived from canonical identity inputs
- replaying from serialized region rows without proving the receipt chain

## Must Ship

- one canonical overlap-region extraction request boundary from
  `PlanarBooleanLoopReconstructionLedgerReceipt`
- first-class overlap participation, adjacency, arrangement, cell, containment,
  island, component, boundary-only, shared-area, canonical winding, identity,
  and ledger products
- typed area-honesty and winding-normalization denials
- overlap-region workload evidence and stage requirement closure
- replay, checkpoint, determinism, and anti-theatre proof
- one canonical `PlanarBooleanOverlapRegionLedgerReceipt` that `7.6+` must
  consume

## Acceptance Evidence

- focused positive and hostile tests for every named phase
- validator registration proof showing overlap graph/topology legality
  validators deny through typed runtime lanes
- operator classification matrix proving no `7.5` operator is an unclassified
  helper
- public-contract and compile-fail proof that synthetic overlap-region entry,
  raw loop bypass, and synthetic ledger construction are rejected
- workload evidence and stage-requirement proof for overlap extraction
- replay and checkpoint parity proof for canonical overlap-region identity,
  area honesty, and winding stability
- summum bonum test:
  `planar_boolean_overlap_region_metaboss_is_canonical_replayable_area_honest_and_unforgeable`
  - named hostile subcases:
    - `boundary_only_coincident_edges_do_not_admit_area`
    - `opposite_sense_same_area_overlap_has_stable_winding`
    - `nested_overlap_islands_preserve_region_identity`
    - `mixed_boundary_and_area_contact_does_not_collapse`
    - `benign_loop_order_variation_preserves_ledger_digest`
    - `synthetic_overlap_ledger_is_rejected`
    - `checkpoint_replay_preserves_region_identity_and_names`
    - `overlap_storm_uses_index_not_pairwise_rediscovery`

## Non-Goals

- keep/discard fragment classification
- face assembly, hole assembly, or shell/body result construction
- post-split degeneracy cleanup
- final topology legality certification
- EMBER or non-planar widening
- curved-edge, seam, periodic-surface, or trim-network overlap extraction

## Sequencing Notes

- Do not start `7.6` fragment classification until `7.5` closes with an
  overlap-region ledger receipt that classification can consume.
- `7.6+` must consume `PlanarBooleanOverlapRegionLedgerReceipt` as the
  exclusive overlap-truth boundary and must not reopen raw loops, raw islands,
  raw arrangement cells, or raw overlap scans.
- Do not put keep/discard fragment classification or face assembly into `7.5`.
- Do not widen into EMBER here.
- If a Query-owned retained artifact, support, inspection, outcome, or evidence
  boundary is missing, extend the Query-shaped path or mark the overlap surface
  blocked rather than inventing a local runtime lane.
- If additional hostile recipes are needed, add them through the workload
  catalog. Do not write overlap-only synthetic region fixtures and call them
  proof.

## Required Self-Check

- Does the milestone solve a real structural problem or just package work
  cosmetically? Yes: it freezes the overlap-region authority that fragment
  classification and face assembly depend on.
- Is the adversarial constraint precise and load-bearing? Yes: it requires one
  canonical overlap ledger or a typed localized denial for the same admitted
  loop ledger across replay, ordering pressure, opposite-sense coincidence, and
  area-honesty pressure.
- Does the roadmap justify this milestone now? Yes: `7.5` is the first
  overlap-region consumer of the `7.4` loop ledger and the roadmap places
  classification and assembly after it.
- Does the spec preserve crate authority boundaries? Yes: Query owns runtime
  entry and progression, `worth-kernel` owns workload evidence, `worth-spatial`
  owns overlap extraction semantics, and `worth-topo` remains topology truth.
- Are the phases carrying most of the real design information? Yes.
- Is each phase centered on one conceptual detail or boundary? Yes.
- Does each phase contain at least 2 adversarial tests by default? Yes.
- Could a competent engineer map this spec into honest types, modules, and
  tests? Yes.
- Does the milestone belong in this roadmap sequence, or is it out of order?
  It belongs here because overlap truth must freeze before fragment
  classification and face assembly can close honestly.
