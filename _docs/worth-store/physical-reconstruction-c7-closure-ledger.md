# WORTH Store C.7 Living Requirement-And-Evidence Ledger

This is the single living C.7 closure ledger. It follows the current source,
not historical test results. Corrections retain finding history and reopen
every causally affected row.

Status vocabulary is `OPEN`, `PROVED`, `DEFECT`, `BLOCKED`, or justified
`N/A`. Phase 1 establishes the complete causal model; later phases replace
`pending` evidence only after their production paths exist.

| ID | Phase | Guarantee | Evidence required | Current evidence | Source identity | Status |
| --- | --- | --- | --- | --- | --- | --- |
| C7-AUTHORITY-01 | 1 | Every durability mechanism and current consumer has one bidirectional trace and final disposition. | Fresh source discovery equals authority trace and removal ledger. | Fresh source discovery reconciles 1,901 live consumers with 133 absent paths; all fourteen ordered authority lanes resolve to their current source owners, including delegated active-tail inspection and exact-successor admission. [current-accounting live-consumers=1901 absent-paths=133 authority-lanes=14] | P1 closure 2e7fca4dc0c6 | PROVED |
| C7-AUTHORITY-02 | 2 | Only a sealed C4-derived basis can admit the exact nonoptional Store durability owner carried through request binding serving construction failure shutdown and read-only observation. | Compile attacks basis-substitution and zero-effect rebind journeys plus source lifecycle mutants. | Five compile attacks reject incomplete raw duplicated omitted and reconstructible authority; foreign-policy rebind leaves target media counters unchanged; policy identity limits and basis are observable; four lifecycle mutants fail. | P2 authority 82d05073012c | PROVED |
| C7-API-01 | 1 | Every current public durability-facing surface has one locked destination and cutover phase. | Public export inventory resolves through its public facade to current source anchors. | Exact public export reconciliation proves 312 locked surfaces, including 46 Phase 10 additions, each with one current facade reachability and disposition. [current-accounting locked-surfaces=312 phase-ten-surfaces=46] | P1 closure 2e7fca4dc0c6 | PROVED |
| C7-API-02 | 8 | Ordinary mutation callers observe idempotency cancellation and typed physical fate without a stronger-looking compatibility wrapper. | Downstream compile journey and old-facade absence gate. | A positive downstream specimen compiles all six admission branches four retained-success cases and three terminal fates through prepare/start/wait; three UI attacks reject ordinary phase driving noncompletion acknowledgment and evidence readmission; exact API inventory locks 24 Phase 8 surfaces and deletes eleven phase-driving entries. | P8 mutation 686c4f426d46 | PROVED |
| C7-EVIDENCE-01 | 8 | Executed-boundary evidence is a one-way descriptive projection from exact physical acknowledgment while proven-no-effect and indeterminate fates expose only separately named diagnostic evidence. | Forward projection journeys reverse-authority compile attacks and constructor dependency gate. | Completion projects exact executed-boundary and performance evidence while both noncompletion fates project separately named diagnostics; the reverse-authority UI attack fails and the mutation-settlement source gate kills public constructor and missing-attack mutants. | P8 mutation 686c4f426d46 | PROVED |
| C7-DESTINATION-01 | 1 | The authority ledger and future topology name one Store cross-domain owner without empty production placeholders. | Specification topology anchors and phase-aware populated-destination gate. | Destination ownership resolves all Phase 2 through Phase 6 proof types and the populated tree contains admission runtime-binding mutation progression grouping WAL barrier and stable facade owners. | P1 closure 2e7fca4dc0c6 | PROVED |
| C7-GRAPH-01 | 1 | Current Cargo and feature edges expose every durability island without importing a circular owner into Store. | Exact cargo metadata reconciliation and ordinary graph assertions. | Exact metadata reconciles the authority packages; ordinary Store reaches WAL meaning scheduler and backend owners while recovery and certification probes remain outside its graph. | P1 closure 2e7fca4dc0c6 | PROVED |
| C7-GRAPH-02 | 9 | The final ordinary Store graph contains no phase-numbered feature authority and activates no certification or replay authority. | Full workspace metadata inspection ordinary Store feature-tree inspection and controlled declaration activation and authority mutants. | Full workspace metadata contains no phase-numbered declaration or activation; the ordinary Store tree excludes six certification and replay features; declaration dependency activation and ordinary-authority mutants fail. | P9 graph bf08adb1c463 | PROVED |
| C7-LEDGER-01 | 1 | The living ledger covers the complete causal claim and rejects omitted families duplicate guarantees and stale proof. | Ledger parser mutants and full normative-family reconciliation. | The parser and authoritative-fact-driven drift controls prove 54 unique guarantee rows cover all 24 required semantic families and reject independent drift in every machine-readable accounting fact. [current-accounting guarantee-rows=54 required-families=24] | P1 closure 2e7fca4dc0c6 | PROVED |
| C7-LEDGER-02 | 10 | Final closure binds every resolved guarantee to the current complete local-package source closure and preserves every discovered finding as audit history. | Complete-source identity validator all-row resolution gate reverse-ledger controls and retained finding history. | All 54 guarantees are proved against the current Phase 10 local-package closure. The append-only corpus contains 92 exact cases 42 through 133 with zero predicate mismatches; canonical plus sixteen source-derived lane reports bind the same current runtime source and all 92 kills. The exact eighteen-file independent audit reports zero issues, and finding history retains the verifier, planner-denial, mutation-causality, invocation, timeout, and local-concurrency corrections. | P10 closure e79bbd6a6fc7 | PROVED |
| C7-MECHANISM-01 | 1 | Every inherited backend barrier WAL root and ordinary publication mechanism has a real terminal effect and one current ownership or explicit disconnected disposition. | Focused owner tests plus manual source trace of each terminal effect. | Focused mechanism probes remain real; the Phase 3 ownership gate proves one ordinary C4 WAL effect and one Store settlement while the displaced lower WAL lifecycle probe is certification-only. | P1 closure 2e7fca4dc0c6 | PROVED |
| C7-VOCABULARY-01 | 1 | Production names preserve physical durability semantics and cannot introduce milestone or compatibility identities. | Production source scan and locked semantic family inventory. | Physical runtime source rejects C7-named and compatibility durability identities while broad vocabulary counts remain reconciled. | P1 closure 2e7fca4dc0c6 | PROVED |
| C7-IDEMPOTENCY-01 | 2 | Canonical request equivalence binds the exact Store policy scope payload platform request operation family and security bases while excluding attempt deadline allocation WAL group schedule and cancellation facts. | Independent versioned golden reconstruction every-field deltas and allocation policy ordering effect and role mutants. | Independent v1 reconstruction matches the golden digest; every effect field and canonical security order changes it; allocation cannot feed back; five source mutants reject omitted policy effect ordering allocation and native-field-role drift. | P2 mutation 4ba82fd4f971 | PROVED |
| C7-IDEMPOTENCY-02 | 2 | A Store-issued durable-generation lease denies silent expired or foreign key reuse while unresolved bindings remain authoritative under the admitted pending bound. | Lease issuance expiry conflict unresolved-retention foreign-identity and pending-bound tests. | Registry tests prove exact fresh duplicate conflict expired unseen and pending-bound outcomes; real preparation preserves one unresolved identity across same-fingerprint retry and rejects foreign Store runtime and policy identities before effect. | P2 mutation 4ba82fd4f971 | PROVED |
| C7-WAL-01 | 3 | Ordinary WAL append uses one Store Signal scheduler executor and C4 media route. | Real append journey independent scan and direct-executor absence gate. | A real two-record mutation appends one independently verified frame through the installed WAL Signal family scheduler lane Store executor and scheduled C4 artifact effect; source ownership proves one effect owner one completion-bound Store settlement and one appended transition while default facades reject displaced executors. | P3 WAL 20f75e398eb4 | PROVED |
| C7-WAL-02 | 3 | Persisted attempt binding joins exact mutation lease member range and redo without changing request equivalence. | Independent attempt-binding inspector and substitution attacks. | A test-owned bounded decoder proves exact key lease fingerprint Store runtime operation member LSN range redo digest and independently encoded ordered redo with full consumption; every field mutation length attack reordering truncation trailing byte redo substitution and completion work range or digest substitution fails. | P3 WAL 20f75e398eb4 | PROVED |
| C7-WAL-03 | 3 | Every WAL denial before possible media effect returns the exact still-owned prepared mutation and releases every range command and capacity reservation made by that attempt. | Sealed-owner denial denied-media retry concurrent-append and one-command health-fence journeys plus linear outcome rollback and cleanup source inspection. | Reservation lifecycle submission Signal scheduler command and media pre-effect failures retain exact actionable causes and return the same prepared identity; repeated fenced admissions reuse released one-command capacity with no residual work, proven-no-effect retry succeeds, and a strict torn prefix remains indeterminate and seals allocation. | P3 WAL 20f75e398eb4 | PROVED |
| C7-WAL-04 | 3 | WAL range allocation consumes only a prepared mutation bound to the exact current Store runtime and Signal profile. | Foreign-Store and stale-runtime append attacks with zero target WAL effect and preserved continuation. | The private preparation-admission owner rejects foreign Store stale runtime mismatched Signal profile and released publication authority before allocation; target WAL observation and bytes remain unchanged and the preserved preparation remains usable only by its rightful live owner. | P3 WAL 20f75e398eb4 | PROVED |
| C7-BARRIER-01 | 4 | Only the matching C4 file-synchronization completion through Signal scheduler executor and exact Store settlement can promote an appended member to WAL durable. | Real scheduled barrier journeys completion-binding substitutions route mutants raw-settlement compile attack and exact constructor-location gate. | The ordinary route binds member append work digest policy admission profile barrier and exact scope before Signal and scheduler admission; wrong work digest artifact scheduler outcome or physical effect cannot produce the private completion witness and production contains one WAL-durable promotion at the barrier owner. | P4 data 1279058a3eae | PROVED |
| C7-DATA-01 | 4 | Data dispatch is constructible only from the matching durable WAL member proof for the current Store runtime and Signal profile. | Compile failures foreign and stale authority matrix and WAL-before-data inversion mutant. | Both public and semantic-owner operations consume only WalDurablePhysicalMutation; foreign Store stale runtime and mismatched Signal profile are denied before candidate residency; raw appended and direct progression construction attacks fail. | P4 data 1279058a3eae | PROVED |
| C7-DATA-02 | 4 | Each physical frame carries one certified prior-page basis plus only its canonical newly applied nonempty redo delta. | PageLSN-ahead lifetime-redo empty-delta and prior-basis substitution mutants. | Prepared frames require the exact prior identity pageLSN digest and nonempty bounded ordinals; binding derives a strictly advancing delta solely from the member range and rejects ahead stale empty lifetime-history and substituted-prior cases. | P4 data 1279058a3eae | PROVED |
| C7-DATA-03 | 4 | Every dispatched frame has one exact agreement among target encoded pageLSN encoded bytes payload digest and WAL redo target claim. | Independent encoded-frame oracle target digest and pageLSN substitutions plus controlled derivation mutants. | PageWalBasis decodes pageLSN and hashes the exact encoded frame internally; WAL target claims consume that derived digest; independent journeys reject target bytes digest pageLSN and redo-coverage substitutions. | P4 data 1279058a3eae | PROVED |
| C7-DATA-04 | 4 | Data-settled authority requires every declared new-artifact and existing-artifact writeback effect to have exact identity source target bytes completed fate and no premature clean transition. | Real mixed multi-frame join exact-effect substitutions missing and duplicate effects early-clean mutant and exact constructor-location gate. | New-artifact and existing-artifact writeback sources remain distinct; the completion-bound join rejects empty missing duplicate substituted incomplete or inspection-required effects; cleaning requires its exact completed writeback settlement and production contains one dispatched and one settled promotion at their named owners. | P4 data 1279058a3eae | PROVED |
| C7-GROUP-01 | 5 | Group commit shares only physical cost while preserving unique mutation range fate cancellation and acknowledgment. | Four-request three-sealed-member identity-blender courtroom. | One request becomes an exact retained no-effect fact before WAL; same-fingerprint replay returns that fact while conflict and every older prepared value are denied before reservation. Three distinct members seal with exact identities ranges ordinals and one shared barrier; cardinality-preserving derivation yields three distinct durable proofs. Append and barrier observations report exact current-stage amplification and only a planned shared root. Four seal cancellation validation and amplification mutants fail. | P5 group e9f4500b3473 | PROVED |
| C7-WAL-05 | 6 | The admitted WAL segment-byte limit drives whole-group fit and rotation while preserving exact segment generation range and artifact identity across reopen. | Multi-segment group-fit journeys two-rotation siege independent inventory oracle and size identity and partial-rotation mutants. | Syntax-aware ownership proof parses real Rust functions in evaluation order, excludes deferred and statically dead syntax from positive proof, keeps all syntax and macro identifiers visible to prohibitions, binds segment parsing to a top-level collected mapping, and ignores exact `cfg(test)` owners. It requires reservation continuation release sealing bounded reopen delegated active-tail inspection and exact-successor admission without pinning local phrasing. WAL issues separate proofs for a verified-prefix interruption and a strict incomplete first frame; only Store topology can promote the latter into cleanup authority for the active exact-next same-generation successor of a verified segment. Lone internal noncontiguous wrong-generation malformed and digest-damaged artifacts remain denials. The ordinary WAL inventory reopen trace has exactly seventeen semantic steps. [current-accounting wal-reopen-steps=17] Controlled cases 121 through 124, 127, and 129 through 132 plus focused dead-branch, nested-item, and unreachable-arm controls cover the proof gate and cleanup regression. | P6 WAL f05217961d9d | PROVED |
| C7-CHECKPOINT-01 | 6 | Fuzzy capture fixes one exact root WAL and dirty-generation source while streaming within admitted checkpoint allocation and I/O bounds under continuing foreground mutation. | 32-times-resident pressure siege exact allocation and I/O counters foreground-progress assertion and whole-Store materialization and source-substitution mutants. | Four real dirty frames freeze one root WAL range and dirty frontier while 128 later foreground WAL mutations complete with checkpoint media sealed. Live maintenance allocation equals the admitted slice metadata stays within policy and returns to baseline; current and peak use remain observable on the managed handle. Independent artifact decoding and total media counters prove the exact header dirty records footer recovery obligations synchronization replacement and namespace publication. The buffer-pool 32-times interleaving proof and eight bounded-capture mutants reject unbounded policy false or leaked accounting source substitution whole-source accumulation deferred slice emission and weakened pressure. | P6 checkpoint 3563c3333733 | PROVED |
| C7-CHECKPOINT-02 | 6 | Every admitted checkpoint is a Store-managed idempotent lifecycle with observable progress safe pre-publication cancellation typed terminal fate finalization disposal enumeration and close-time drain. | Public-handle journey duplicate start cancellation seams drop abandonment close drain and fire-and-forget and lifecycle mutants. | One retained current attempt and owned join handle serialize checkpoint admission. Same-key callers join while distinct keys defer; Store terminal counters and publication truth finalize before waiters wake. Nine real lifecycle journeys prove safe cancellation and exact cleanup, inspection-required uncertainty, pending disposal as observation-only abandonment, exact terminal poll and disposal, refusal of no-effect cancellation after publication cutover, caller-drop survival, close admission stop and drain, and retained publication authority. Eight lifecycle mutants reject worker detachment forgotten enumeration omitted close cancellation or join observer-first finalization publication-crossing cancellation cancellation-on-drop and missing disposal evidence. | P6 checkpoint 3563c3333733 | PROVED |
| C7-CHECKPOINT-03 | 6 | Every checkpoint effect consumes the exact checkpoint-capture aspect basis Signal family background scheduler lease and matching Foundational policy receipt before the sole Store executor reaches C4 media. | Production-route trace policy-receipt and background-pressure observations plus foreground-lane raw-Signal direct-media receipt-substitution and missing-independent-evidence mutants. | Dedicated mutation semantics bind the CheckpointCapture Signal family; every action traverses Signal, BackgroundCheckpoint admission, the exact Foundational AuthoritativeMutation RebuildCapable ColdPath receipt, one Store executor, and scheduled C4 create append file-sync replace and namespace-sync effects. The ordinary serving journey independently decodes the published artifact; a sealed-authority compile attack and eleven route mutants reject basis family lane pressure policy receipt operation media and evidence bypasses. | P6 checkpoint 3563c3333733 | PROVED |
| C7-RETENTION-01 | 6 | Every completed checkpoint owns a nonempty canonical tail of original WAL artifacts from its exact boundary through the cutover durable frontier with exact owner order generation adjacency no gap or overlap and admitted physical-byte bound; copied ranges and unfenced publication open no authority. | Focused admission proofs independent multi-rotation filesystem reconstruction foreground publication-cutover race and empty reorder identity generation gap overlap boundary truncation copied-range byte-bound detached-outcome early-fence and unguarded-cutover mutants. | `ContiguousRetainedWalTail` consumes only the WAL owner's immutable nonempty physical inventory under a typed cutover guard and validates canonical artifact order exact generation and successor identity LSN adjacency checkpoint boundary durable frontier and checked physical-byte sum. The guard spans candidate replacement and namespace sync; the completed checkpoint retains the exact authority and exposes no constructor or deletion effect. Four focused admission tests two real filesystem journeys and fourteen controlled mutants prove zero-new-record and multi-rotation tails independent byte and range facts foreground exclusion at cutover and rejection of copied topology detached authority or weakened bounds. | P6 checkpoint 3563c3333733 | PROVED |
| C7-IDEMPOTENCY-03 | 6 | A namespace-durable checkpoint publishes the exact live binding compaction advances the durable generation exactly once and rebuilds the bounded derived registry from compaction plus retained WAL. | Fresh-process rebuild unresolved and expired-terminal worlds repeated-publication generation checks and omitted-fate premature-expiry staged and rename-only advancement mutants. | Four real-media reopen journeys independently prove checkpoint bytes read dirty bytes skipped binding records retained WAL members peak WAL buffer bytes unresolved compaction-plus-tail reconstruction exact terminal replay terminal retention through generations one through three omission only at generation four expired denial and independent pending versus total-live bounds. Nine owner tests reject malformed noncanonical foreign duplicate discontinuous incomplete-group and limit-overflow worlds. Three source-contract tests reject twelve bypass buffering authority cutover and fate mutants; the existing repeated-publication journey proves same-key observation does not advance generation while one later publication advances exactly once. | P6 idempotency 533d38e845bc | PROVED |
| C7-RETENTION-02 | 6 | WAL deletion consumes namespace-durable checkpoint contiguous-tail and binding-compaction authority and cannot remove the last persisted copy of any live attempt binding. | Premature deletion denial eligible reclamation unresolved-binding preservation and last-copy missing-compaction and checkpoint-existence mutants. | Exact retained-suffix and compaction-cutoff admission mint a private per-segment no-last-copy proof; the dedicated Signal/Foundational background route schedules canonical C4 removal; only an exact completed receipt consumes the oldest inventory entry; fail-before preserves retry truth; a deterministic two-segment journey proves completed-prefix accounting before second-delete denial; possible effect seals; fresh-process reopen proves the deleted prefix stays absent. | P6 reclamation 907bfeafc498 | PROVED |
| C7-ROOT-01 | 7 | One canonical root progression distinguishes candidate durability atomic replacement and parent-namespace durability and advances current-root authority only after the exact final barrier. | Exact C.4 receipt joins crash-seam matrix missing-directory-sync mutant and independent fresh-process root observation. | Candidate artifact writes and per-artifact synchronization precede typed atomic replacement; replacement precedes typed namespace synchronization; only `PhysicalCurrentRootOwner::advance` consumes namespace-durable authority. Root-carriage fault journeys and fresh-process reopen prove generation-two truth only after the final barrier. | P7 root d112448ef3db | PROVED |
| C7-ROOT-02 | 7 | Root preparation consumes one exact old root successor generation durability policy checkpoint or bootstrap-reopen basis and every WAL-durable data-settled member of one sealed group. | Group-order and cardinality journeys plus foreign Store runtime generation policy checkpoint WAL data member and digest substitution mutants. | `RootPublicationPhysicalMutationMember` carries each exact settled mutation and completion projection; planning binds current-root generation Store runtime policy group and ordered membership before transition admission. One-member multi-member foreign stale and substituted-member journeys reject any incomplete or mismatched basis. | P7 root d112448ef3db | PROVED |
| C7-ROOT-03 | 7 | Compiler-visible root-prepared root-replaced and root-namespace-durable types expose only the next legal transition and have no public or lower-crate constructor. | Constructor-location gate compile attacks and skipped reordered or duplicated-stage mutants. | Distinct planning candidate written prepared replaced namespace-durable and completed types are consumed by one next-stage operation; their constructors remain crate-owned. The Phase 7 compile attack rejects plan cloning and the source contract rejects projection duplication or a missing consuming continuation. | P7 root d112448ef3db | PROVED |
| C7-ROOT-04 | 7 | One sealed durability group publishes exactly one successor root while preserving every member identity and its shared publication binding. | One-member and multi-member publication journeys incomplete duplicate reordered and split-root mutants. | Root planning consumes one sealed group basis and nonempty ordered member set into one candidate identity and successor generation; every later stage carries the same group and members. Root-carriage journeys prove exact WAL barrier data-effect record and completion evidence through one shared cutover. | P7 root d112448ef3db | PROVED |
| C7-ROOT-05 | 7 | The exact root-publication aspect basis uses only the `RootPublication` Signal family and each root effect consumes its matching filesystem-admitted scheduler capability and Foundational policy receipt. | Production-route trace and family partition action capability policy lane and receipt-substitution mutants. | The canonical root work port binds `RootPublication` scope action identity Signal route scheduler demand and Foundational receipt for candidate sync replacement and namespace sync. The root Signal pause journey proves no replacement while Signal is blocked; exact scheduler exhaustion denies before dispatch preserves identity and retries without media drift. | P7 root d112448ef3db | PROVED |
| C7-ROOT-06 | 7 | Namespace-durable cutover emits the immediately previous root and required supporting-artifact inventory as retained truth without granting deletion authority. | Exact artifact manifest offline inspection retained-old-root journey premature-retention and early-deletion compile attacks. | Current-root advancement derives `RetainedPhysicalRoot` only from the immediately displaced manifest after namespace durability and exposes it as retention truth without a delete operation. Fresh-process artifact and manifest courtrooms keep the old root inventory present while selecting exactly the new bootstrap-catalog root. | P7 root d112448ef3db | PROVED |
| C7-ROOT-07 | 7 | Fail-before root effects preserve the exact prior linear authority while any possible replacement or namespace effect remains indeterminate inspection-required and unacknowledgeable. | Candidate-sync replacement namespace-sync fault matrix and after-replace-before-directory-sync crash mutant. | Exact first-write proven-no-effect restores the complete candidate plan and retries without poisoning health; partial candidate writes failed synchronization possible replacement and possible namespace effects retain typed indeterminate residue revoke health and cannot construct current-root advancement authority. Controlled fault and retry journeys cover every posture. | P7 root d112448ef3db | PROVED |
| C7-ROOT-08 | 7 | No parallel current-root runtime log writer parser authoritative cache or direct catalog-replacement route remains after cutover. | Public export source Cargo fixture filesystem-name and direct-effect absence gates plus removal-ledger reconciliation. | `PhysicalCurrentRootOwner` is the sole current-root and free-space owner; serving reads checkpoint capture planning and observation use read-only snapshots. Direct append preparation and batch facades catalog-replacement progression duplicate publication state parallel root runtime log parser fixtures and legacy Signal dependency lane are deleted with no alias fallback or double write. | P7 root d112448ef3db | PROVED |
| C7-FAILURE-01 | 4 | Denial before effect preserves the exact linear authority while any possible or partial barrier or data effect remains indeterminate and unacknowledgeable. | Barrier and data boundary fault matrices candidate failure-posture mutants and typed progression compile restrictions. | Barrier pre-effect outcomes return the appended mutation; data pre-effect outcomes return WAL-durable authority; missing unsettled possible and partial effects retain indeterminate values and cannot construct WAL-durable data-settled or later acknowledgment authority. | P4 data 1279058a3eae | PROVED |
| C7-LIFECYCLE-01 | 8 | Store owns started mutation settlement through close even after caller handle drop. | Drop abandonment close drain and cancellation mutants. | Six deterministic boundaries prove handle drop never cancels and close drains all dropped-handle work; runtime-closing cancellation is distinct and counted; source mutants reject worker detachment missing close marking early notification and drop-triggered cancellation. | P8 mutation 686c4f426d46 | PROVED |
| C7-OBSERVATION-01 | 8 | Store terminal state and idempotency fate finalize before waiter notification while lost caller observation becomes counted completed-but-unobserved truth without changing physical fate. | Acknowledgment-delivery loss duplicate-observer and finalize-before-notify journeys plus ordering mutants. | Every drop boundary reaches completed with one completed-unobserved event; duplicate live handles join one attempt; shutdown retains all terminal cancellation panic and abandoned-observation counters; an early-notification mutant dies because persistence installation and counters must precede waiter publication. | P8 mutation 686c4f426d46 | PROVED |
| C7-SETTLEMENT-01 | 8 | Only the exact completed root-publication member can construct one completed mutation whose consuming projection yields physical acknowledgment with the same completed breadth. | Completion carriage journey acknowledgment compile attacks member-substitution mutants and constructor-location gate. | Managed completion derives its fact from the exact completed root member and preserves record data-effect WAL and root-generation breadth through consuming acknowledgment; completed fate compaction/reopen restores that basis; noncompleted acknowledgment and public-mint mutants fail. | P8 mutation 686c4f426d46 | PROVED |
| C7-SETTLEMENT-02 | 8 | Proven no effect is constructible only before WAL group seal while every possible post-seal effect becomes exact indeterminate C8 basis with no acknowledgment or automatic retry surface. | Pre-effect cancellation and deadline journeys post-seal fault matrix no-retry compile attacks and fate-substitution mutants. | Pre-effect cancellation and Signal deadline produce distinct exact no-effect causes; five post-seal cancellation boundaries remain effectful and a post-seal elapsed deadline cannot rewrite completion; completed no-effect and indeterminate fates persist distinctly while reopened indeterminate blocks ordinary retry; noncompletion acknowledgment fails to compile. | P8 mutation 686c4f426d46 | PROVED |
| C7-SIGNAL-01 | 2 | Store installs the exact policy basis and five operation-specific Signal families for WAL append barrier checkpoint capture root publication and WAL reclamation without letting Signal become durability authority. | Exact-family work reconciliation source role mutant and authority compile failure. | The real reconciliation trace requires twelve exact installed bindings: four reads inherited exact writeback inherited publication one C7 policy basis and five C7 operational contracts. Every observed C7 operation selects only its matching family and stable Store partition; the policy basis is installed but cannot stand in for operation work or physical effect. | P2 mutation 4ba82fd4f971 | PROVED |
| C7-SIGNAL-02 | 8 | Caller mutation deadlines and cancellation enter the derived monotonic Signal lifecycle before effect without changing request equivalence or idempotency lease retention. | Clock deadline cancellation and lease-independence journeys plus substitution mutants. | Certification advances the installed Signal monotonic clock through its real binding; elapsed pre-effect deadlines prove no effect while elapsed post-seal deadlines preserve completion; the canonical fingerprint and generation lease remain independent of attempt deadline and cancellation state under existing field-delta and retention mutants. | P8 mutation 686c4f426d46 | PROVED |
| C7-FOUNDATIONAL-01 | 2 | Foundational consumes six exact Store aspect-native C7 work contracts and one-way evidence while neither layer can mint platform authority or completed physical effect. | Canonical family source role lane domain construction tests exact work reconciliation plus reverse-import and substitution gates. | Aspect-native certifies the policy-binding contract and five operational contracts with their exact posture masks Signal families and partitions; the reconciliation oracle rejects family role and partition drift while structural gates reject reverse authority and any use of policy or evidence as completed work. | P2 mutation 4ba82fd4f971 | PROVED |
| C7-PERFORMANCE-01 | 10 | Every governed C7 performance claim is counter-backed and bound to its exact policy receipt workload scale environment cold or warm posture arrival and burst model utilization repetitions and percentile report. | Counter-backed siege receipts exact structural oracles scale sensitivity qualification dimensions and controlled counter defects. | The current controlled campaign records 82 ordinary cases with a 34,793 ms maximum and ten nested-executable cold cases with a 199,397 ms maximum, all below their exact class budgets. Canonical and sixteen lane reports retain the current-host structural qualification, counter-backed receipts, all eighteen campaign timings, and seven stages per crash case; the maximum crash-case total is 129,749 ms under the 180,000 ms hang guard. | P10 closure e79bbd6a6fc7 | PROVED |
| C7-CLEANUP-01 | 1 | Every preserved narrowed moved replaced or deleted responsibility names its last consumer phase and mechanical absence proof. | Fresh removal discovery and schema mutants. | Exact source-metadata reconciliation proves 2,034 removal rows: 1,901 live consumers, 133 absent paths, 132 completed deletions, and one completed move; unclassified, stale-open, rediscovered-deleted, and family-mismatch sets are empty. [current-accounting removal-rows=2034 live-consumers=1901 absent-paths=133 deleted-paths=132 completed-moves=1] | P1 closure 2e7fca4dc0c6 | PROVED |
| C7-CLEANUP-02 | 9 | Direct runtime recovery executor parallel root log compatibility aliases and stale features are absent from the ordinary graph. | Source metadata feature and public-export absence gates. | The generic backend durability runtime recovery-owned WAL executor and physical-isolation root log are absent; the misleading durable-publication declaration topology is replaced by a descriptive publication-declaration owner; exact API and removal inventories reconcile; cutover and aspect-contract gates reject old paths names unpartitioned contracts missing contracts and the generic barrier key. | P9 closeout 443a06635f7b | PROVED |
| C7-CLEANUP-03 | 10 | Phase 10 leaves no temporary production hook report input copied runtime truth or alternate certification route while retaining only sealed C4 yieldpoints and shared schedule infrastructure with continuing ownership. | Source and feature absence gates artifact-policy oracle removal-ledger reconciliation and final dirty-tree inspection. | Source and feature absence gates, Cargo and removal inventories, and the final dirty-tree inspection reconcile only the planner denial, semantic verifier, evidence-integrity mutations, and their locked documentation. The Store scan covers 5,324 Rust files with zero files above 400 lines. The evidence directory contains exactly one controlled report, one canonical report, and sixteen lane reports with no extra artifact; no scratch harness or retained courtroom world remains. | P10 closure e79bbd6a6fc7 | PROVED |
| C7-DOCUMENTATION-01 | 9 | Public durability and owner documentation explain real API lifecycle cost fate recovery and unsupported profiles. | Documentation review against final source and executable examples. | The public durability and checkpoint guide names the exact facade lifecycle identity fate recovery profile cost Foundational Worth Proof and Signal boundaries; its two Rust examples are byte-bound to an external compile specimen; four owner READMEs the physical-reality audit aspect-native gate documentation public API ledger and reconstruction roadmap describe the same current authority and bounded C8 handoff. | P9 closeout 443a06635f7b | PROVED |
| C7-COURTROOM-01 | 10 | Independent fresh-root processes cover all eight explicit termination seams and sixteen source-closure-derived lawful schedule lanes with exact per-case replay identity artifacts timing and independent physical observation. | Eight-seam canonical release sixteen-lane CI exact rerun source-stability case-manifest digest timing and independent-oracle controls. | The accepted canonical report covers all eight explicit termination seams, and sixteen accepted isolated lane reports exactly match its source-derived seed and trace manifest. Every report binds runtime source `5f4ad808d5d3`, controlled source `3fa132c678b3`, all 92 killed cases, exact replay and process records, eighteen campaign timings, seven stages per crash case, independent fresh reopen, and zero findings. The independent eighteen-file audit reports zero issues. | P10 closure e79bbd6a6fc7 | PROVED |
| C7-HANDOFF-01 | 10 | C8 receives one linear sealed branch-agnostic handoff containing exact source/profile root checkpoint WAL-tail operation-fate backend allocation and residue truth without live runtime replay scheduler Signal pool or semantic authority. | Exact closeout journey nine-step C8 handoff authority trace forty-six-surface Phase 10 API inventory and constructor linearity report-conversion and partial-fact compiler denials. | The exact closeout journey proves the linear nine-step C8 handoff, 46 locked Phase 10 surfaces, constructor linearity, and report-conversion and partial-fact compiler denials. [current-accounting c8-handoff-steps=9] Canonical plus sixteen lanes bind the handoff composition to the corrected runtime source while excluding live replay scheduler Signal pool and semantic authority from the sealed facts. | P10 closure e79bbd6a6fc7 | PROVED |

## Phase 1 finding history

| Finding | Severity | Affected guarantees | Defect | Root correction | Closure evidence |
| --- | --- | --- | --- | --- | --- |
| C7-P1-F01 | High | C7-AUTHORITY-01 C7-API-01 C7-CLEANUP-01 | Selected-symbol path membership could miss renamed semantic surfaces and additional calls inside an already classified file. | Broadened discovery to durability WAL barrier root acknowledgment checkpoint pageLSN and ordinary mutation semantic families and recorded exact per-family occurrence counts. | Same-file count mutant fails and all 1485 current paths reconcile. |
| C7-P1-F02 | High | C7-AUTHORITY-01 C7-CLEANUP-01 | Broad discovery initially allowed generic owner and responsibility labels that were complete syntactically but semantically useless. | Classified preserved rows to exact crate owners and C7 cutover rows to path-sensitive named destinations; generic labels are mechanically rejected. | Ownership gate passes over every removal row. |
| C7-P1-F03 | Medium | C7-AUTHORITY-01 | Oversized patch transport corrupted an initial generated ledger row while preserving an apparently plausible partial file. | Deleted the partial artifact and rebuilt every ordered row through bounded patches before exact-set reconciliation. | Parser and exact reconciliation pass; final ledger has 1486 lines. |
| C7-P1-F04 | Low | C7-MECHANISM-01 | First ordinary publication probe omitted its Cargo-required certification feature and therefore never executed. | Reran with the target-declared `certification-test-authority` feature and retained the rejected command as history. | Thirteen ordinary publication tests pass under the declared feature authority. |
| C7-P1-F05 | Medium | C7-AUTHORITY-01 C7-MECHANISM-01 | The inherited runner still scanned the ledger-deleted `c6_handoff` tree and matched candidate receipt adjacency through indentation. | Removed only the C6-ledger-proven deleted scan root and replaced formatting-sensitive matching with structural concrete-field and field-order validation. | C6 runtime ownership and C5 writer gate families pass including an indentation mutant. |
| C7-P1-F06 | High | C7-API-01 C7-CLEANUP-01 | The first public API ledger asserted only eight representative names and therefore allowed unlisted durability entry points to survive without a disposition. | Replaced subset membership with exact equality over the twenty-four current C7 boundary surfaces including ordinary and reconstructive record entry points and the certification-only direct WAL executor. | Missing extra and duplicate surfaces now fail before later cutover work can rely on the inventory. |
| C7-P1-F07 | High | C7-LEDGER-01 | Proved rows required a non-pending source identity but did not prove that the recorded digest described the current Phase 1 source closure. | Defined the canonical Phase 1 source closure in the gate and recompute its SHA-256 identity for every proved Phase 1 row. | A wrong-digest mutant fails with the exact current identity required for closure. |

## Phase 2 finding history

| Finding | Severity | Affected guarantees | Defect | Root correction | Closure evidence |
| --- | --- | --- | --- | --- | --- |
| C7-P2-F01 | High | C7-AUTHORITY-02 | Initial durability files crossed the code-size constitution and mixed platform-basis derivation with unrelated media and profile responsibilities. | Extracted admission basis identity derivation and its focused tests into semantically named modules. | Every dirty Rust file is at or below 400 lines and the real-media identity journey passes. |
| C7-P2-F02 | High | C7-AUTHORITY-01 C7-DESTINATION-01 C7-CLEANUP-01 C7-AUTHORITY-02 | Phase 1 destination assumptions removal discovery and mutation seams became stale when Phase 2 populated the destination and changed admission signatures. | Made the destination gate phase-aware reconciled every new path in the removal ledger and rebound mutations to the real admission seam. | The full boundary runner passes with 1527 exact path dispositions and all controlled mutants rejected. |
| C7-P2-F03 | Critical | C7-AUTHORITY-02 | Construction failure discarded the joined durability owner and serving exposed no authority-free policy observation. | Carried the exact owner through construction failure and added a copy-only observation containing identity basis profile and admitted bounds. | Failure-owner and missing-observation source mutants fail; runtime observation matches the admitted policy exactly. |
| C7-P2-F04 | Critical | C7-AUTHORITY-02 | Normal shutdown stored the mandatory durability owner in an `Option`, allowing lifecycle correctness to depend on developer conscientiousness. | Made the shutdown owner structurally mandatory and consume it exactly once before media close. | The optional-owner source mutant fails and open cannot omit admitted durability at compile time. |
| C7-P2-F05 | Critical | C7-IDEMPOTENCY-01 C7-FOUNDATIONAL-01 | The aspect-native mutation field role was declared but the canonical construction path never certified it, so the registry looked complete without enforcing the role. | Certified `NativePhysicalMutationRequest` in the real construction path and split the source gate across record declaration ownership construction and domain authorities. | The real gate passes and a field-role substitution mutant fails alongside allocation policy ordering and effect mutants. |
| C7-P2-F06 | High | C7-IDEMPOTENCY-02 C7-CHECKPOINT-01 C7-LEDGER-01 | The Phase 2 idempotency row demanded Phase 6 binding compaction and could never close honestly in phase order. | Narrowed Phase 2 to lease expiry unresolved authority and pending bounds; retained binding compaction solely under the Phase 6 checkpoint guarantee. | The Phase 2 row now matches the specification phase contract and the source-closure validator tracks it independently. |
| C7-P2-F07 | High | C7-IDEMPOTENCY-01 C7-FAILURE-01 | The golden fingerprint test reused production byte assembly and source-failure proof covered only early EOF so plausible derivation and streaming faults could survive green tests. | Added an independent fingerprint reconstruction every-field deltas exact security ordering and real producer excess-length invalid-count and early-end journeys with no-effect and retry observations. | Five registry tests and seven preparation journeys pass with exact failure kinds and unchanged media counters. |
| C7-P2-F08 | High | C7-DESTINATION-01 C7-IDEMPOTENCY-01 C7-SIGNAL-01 | The public submission facade owned a 112-line preparation pipeline and decomposed admitted policy into scalar plumbing creating a second representation of canonical inputs. | Moved preparation orchestration into its semantic director owner carried `PhysicalIdempotencyPolicy` intact and derived fingerprint bytes from the same canonical field record used by aspect-native construction. | Facade delegation and canonical-owner source gates pass; all dirty files remain within the line cap and scrutinized advisories retain one responsibility. |
| C7-P2-F09 | Medium | C7-AUTHORITY-02 | A panic-cleanup test asserted an exact delta on a process-global admission counter and failed nondeterministically when the full suite admitted unrelated roots concurrently. | Replaced the global exact-delta oracle with monotonic panic observation plus immediate readmission of the exact controlled root. | The isolated test and the full eighty-six-test library target pass while proving exact-root cleanup. |
| C7-P2-F10 | High | C7-AUTHORITY-01 C7-CLEANUP-01 C7-LEDGER-01 | The Phase 2 refactor added twenty tracked sources and changed ten semantic-family counts after the earlier removal-ledger snapshot. | Re-ran source discovery classified every new path by concrete semantic responsibility and retained transitional narrow or replace dispositions instead of blanket preservation. | All 1547 current path rows reconcile and the six inventory schema classification drift and omission tests pass. |
| C7-P2-F11 | High | C7-IDEMPOTENCY-01 C7-LEDGER-01 | The idempotency guarantee described only excluded allocation facts so an implementation omitting effect-relevant Store policy scope payload operation-family or security inputs could satisfy the row's wording. | Rewrote the guarantee as a two-sided equivalence contract and required independent every-field plus canonical-order evidence. | The strengthened row is bound to the Phase 2 mutation source closure and the omitted-policy effect ordering and allocation mutants all fail. |
| C7-P2-F12 | High | C7-SIGNAL-01 C7-LEDGER-01 | The exact Phase 2 source closure omitted the physical Signal-family journey even though the ledger cited that journey as proof. | Extracted the durability binding assertion into its own semantic module and added that exact module to the mutation-and-Signal source closure. | Any change to the four-family dependency binding proof now invalidates the recorded Phase 2 source identity. |
| C7-P2-F13 | High | C7-API-01 C7-LEDGER-01 | The exact public API inventory remained frozen at the twenty-four inherited surfaces and omitted every new Phase 2 durability mutation and preparation surface. | Added all forty Phase 2 additions with real semantics destination owners dispositions phases and declaration anchors and made the gate require the exact inherited-plus-Phase-2 union. | All sixty-four rows resolve to live source anchors; omitted extra duplicate and renamed surfaces fail the exact-set or anchor gate. |
| C7-P2-F14 | High | C7-API-01 C7-LEDGER-01 | Declaration anchors could remain green after a Phase 2 type was removed from the public facade because the API gate did not prove re-export reachability. | Added exact durability-root export membership and the complete physical-runtime to record-serving to publication preparation re-export chain. | Hiding any Phase 2 durability or preparation surface behind a private module now fails at its first public facade. |

## Phase 3 finding history

| Finding | Severity | Affected guarantees | Defect | Root correction | Closure evidence |
| --- | --- | --- | --- | --- | --- |
| C7-P3-F01 | High | C7-WAL-03 C7-FAILURE-01 C7-LEDGER-01 | WAL reservation denial consumed the only `PreparedPhysicalMutation` even though no effect had occurred, so inspection sealing, append contention, LSN exhaustion, or frame-planning rejection could strand an admitted mutation without a typed continuation. The Phase 3 source closure also omitted the preparation and attempt-binding files that governed this round trip. | Made every reservation denial return the exact linear prepared value without cloning authority; reversible planning rejection releases only the unperformed WAL allocation and reconstructs the same preparation including Signal profile and resource facts. Added the governing preparation, batch, admission, and attempt-binding sources to the Phase 3 closure. | Sealed-owner and executor-paused concurrent-append journeys consume the denial outcome, prove the exact preparation survives, and successfully retry after the blocking append settles. |
| C7-P3-F02 | Critical | C7-WAL-02 C7-WAL-04 C7-AUTHORITY-02 | The ordinary append facade accepted any public `PreparedPhysicalMutation`, but the WAL owner did not validate its Store identity, runtime identity, or prepared Signal profile against the target runtime before range allocation. A mutation prepared in Store A could therefore be appended into Store B's WAL while persisting Store A's identity. | Added a mandatory Store/runtime/profile preparation-admission owner at WAL construction and changed range reservation to accept only its private admitted wrapper. Foreign Store, stale runtime, and mismatched profile causes remain distinct and return the exact linear preparation before allocation. | Independent two-Store and close/reopen attacks prove exact denials, unchanged target WAL observation and zero artifact bytes; the rightful Store subsequently appends the preserved foreign-denied preparation. |
| C7-P3-F03 | High | C7-WAL-03 C7-FAILURE-01 | A failure after range reservation but still proven no-effect returned `WalRangeReservedPhysicalMutation`, yet the public facade had no operation that could consume that type. The owner cleared `in_flight` while stranding the caller's only continuation, and backend denial was mislabeled as submission rejection. | Added a lossless reserved-to-prepared rollback that is callable only after Store classifies proven no effect, carries the prepared Signal profile and resource shape through reservation, and returns the exact backend `ArtifactTreeFailure` cause. Indeterminate outcomes retain the reserved range and cannot enter rollback. | A one-shot real positioned-write denial returns the same mutation identity and fingerprint with an untouched frontier, then the returned preparation appends successfully through the canonical route. |
| C7-P3-F04 | High | C7-WAL-01 C7-WAL-02 | The Phase 3 plan required completed-receipt substitution attacks, but the implementation compared work range and digest only inline and no test independently varied any member of that exact settlement triple. | Added one named `matches_completion_binding` predicate owned by `PhysicalWalAppendSettlement`, made promotion consume it, and attacked wrong work, wrong range, and wrong digest independently in the real consecutive-append journey. | The exact binding passes while each isolated substitution fails before `WalAppendedPhysicalMutation` promotion. |
| C7-P3-F05 | High | C7-SIGNAL-01 C7-SIGNAL-02 C7-LEDGER-01 | The Phase 2 Signal row combined installed family bindings with Phase 8 caller deadline and cancellation semantics, allowing a proved early-phase row to overclaim behavior whose request values are intentionally only carried during Phase 3. | Narrowed Phase 2 to the exact installed dependency profile and created a separate Phase 8 guarantee for caller deadline, cancellation, monotonic clock, request-equivalence exclusion, and lease independence. | The existing Phase 2 profile journey still proves its narrowed row; the unimplemented caller-lifecycle claim is visibly OPEN in its owning phase. |
| C7-P3-F06 | High | C7-WAL-02 C7-WAL-04 | `PhysicalWalMemberIdentity` wrapped only the per-runtime operation ordinal, so independent runtimes could both mint member `1`; the nominal member identity was therefore not stable across the Store/runtime authority dimensions carried by its mutation. | Replaced the scalar ordinal with a domain-separated SHA-256 identity over exact Store, runtime, and operation identity and persisted that full member digest in the attempt binding and frame declaration. | The two-Store journey proves both runtimes begin at the same local mutation ordinal while their WAL member identities remain distinct; all fifteen durability-admission journeys pass. |
| C7-P3-F07 | High | C7-WAL-03 C7-FAILURE-01 | Physical work admission could deny a declared command before consuming it, including after the serving-health fence, while leaving the command and its bounded capacity reserved until shutdown. WAL correctly returned the prepared mutation but leaked attempt-local physical work authority and could exhaust admission under repeated denials. | Made the generic physical admission owner cancel the exact declared identity on every admission error; cancellation atomically removes only pre-dispatch work, returns scope and semantic capacity, records terminal accounting, and cannot reclassify dispatched work. | An indeterminate real write revokes serving health under a one-command profile; two consecutive WAL-shaped physical submissions are both denied, both exact identities appear as cancelled before dispatch, the second submission proves capacity reuse, and shutdown has no residual command. |
| C7-P3-F08 | High | C7-WAL-03 C7-API-01 C7-DOCUMENTATION-01 | The public WAL guide still matched the removed tuple-shaped reservation denial and discarded the linear continuation in both safe-denial branches. The example therefore did not compile against the shipped facade and taught callers to lose the exact prepared mutation needed for cause-specific continuation handling. | Rewrote the guide around the current four-way outcome with separate retained reservation and pre-effect values plus an inspection-only reserved value; explicitly separated proven no effect from retry eligibility; bound every Rust block byte-semantically to one external-consumer compile specimen and included both proof owners in the Phase 3 source closure. | The exact documentation-drift journey passes and the complete authority UI target compiles the specimen through the public facade while all authority attacks remain rejected. |
| C7-P3-F09 | High | C7-WAL-03 C7-API-01 | A `PhysicalRecordSubmission` whose publication owner had been released returned `InspectionRequired` from `append_prepared_wal`, even though no WAL inspection seal or possible effect existed. The API collapsed lifecycle loss into media suspicion and directed callers toward the wrong recovery action. | Added the exact `PublicationAuthorityReleased` reservation denial at the weak-facade boundary and retained `InspectionRequired` solely for a live WAL owner sealed after indeterminate evidence or nonempty unreconstructed media. | A mutation prepared before close remains byte-for-byte identifiable after close; append through the released facade returns the new lifecycle cause, preserves the preparation, and exposes no live WAL observation. |
| C7-P3-F10 | High | C7-WAL-03 C7-API-01 C7-FAILURE-01 | WAL append compressed work-request denial, bounded-capacity deferral, lifecycle staleness, identity exhaustion, and scheduler reservation denial into two generic labels. The public cause could not tell a caller whether to correct input, wait for capacity, move to a live runtime, inspect exhaustion, or reduce scheduler pressure. | Replaced `SubmissionRejected` with exact denied deferred stale and failed variants carrying the existing public work outcomes; made scheduler reservation denial carry its exact physical-instance admission cause. No new parallel error vocabulary was introduced. | A real one-command profile holds an unrelated declared read, returns `SubmissionDeferred` with exact `Commands` dimension and limit from the canonical WAL route, preserves mutation identity and fingerprint with zero appended frames, then appends the same preparation after exact capacity cancellation. |
| C7-P3-F11 | High | C7-WAL-02 C7-LEDGER-01 | The real WAL journey independently verified frame framing and coverage but treated Store attempt-binding persistence as a handful of substring searches. Coincidental bytes, reordered equal-width fields, missing fields, trailing garbage, or a substituted redo payload could satisfy those checks without proving the exact persisted join required by the ledger. | Added a test-owned bounded decoder that shares no production binding encoder or parser, requires exact frame and member-payload consumption, validates the domain and every field boundary in order, and compares the complete binding plus redo payload to independently held public expectations. | One Store-written frame decodes exact key lease fingerprint Store runtime operation member LSN range redo digest and canonical redo. Isolated mutation of every field, key/fingerprint order swap, truncation, trailing bytes, and redo-payload substitution are all rejected. |
| C7-P3-F12 | High | C7-WAL-03 C7-API-01 C7-FAILURE-01 | WAL append retained a generic `DependencyBlocked` cause after Signal had already classified the exact admission posture and blocking condition. Callers could not distinguish dependency readiness, partition mismatch, temporal conditions, or reference drift and therefore lacked a truthful correction target. | Made the public cause carry Signal's canonical admission class and optional condition directly; Store projects existing Signal truth and introduces no competing classification vocabulary. | The public guide names both retained fields, its exact external compile specimen remains facade-bound, and the complete Store journey and authority suites pass with the exhaustive cause shape. |
| C7-P3-F13 | High | C7-WAL-02 C7-LEDGER-01 | The independent attempt-binding inspector derived its expected redo payload and redo digest from production `CanonicalRedoRecords`, so a production ordering or encoding defect could agree with its own oracle and survive every field substitution attack. | Added a test-owned canonical redo encoder driven only by the original fixture records with independently declared domain count ordinal length and payload rules; the expected digest now derives only from those independent bytes. | The real Store frame matches the independent encoding and digest exactly, reversing the source record order changes the oracle, and payload substitution remains rejected by the independent member inspector. |
| C7-P3-F14 | High | C7-WAL-01 C7-CLEANUP-01 C7-LEDGER-01 | The cleanup source test proved that certification guards existed around the displaced backend append input and path-bound WAL planner, but it would still pass if an unguarded duplicate export were added elsewhere in the same facade. | Changed the gate to remove the one exact allowed guarded occurrence and require zero residual symbol occurrences in the default facade source. | Controlled backend and WAL mutants that append an unguarded duplicate export both fail, while the real guarded-only facades pass. |
| C7-P3-F15 | Critical | C7-WAL-01 C7-WAL-02 C7-FAILURE-01 | `WalAppendedPhysicalMutation::new` accepted a raw `PhysicalWalAppendSettlement`; exact work range and digest comparison was an adjacent boolean convention rather than an input type required by the appended transition. Another in-crate caller could therefore bypass the comparison while still constructing the stronger phase. | Added a private `CompletionBoundPhysicalWalAppendSettlement` constructible only by exact triple comparison and changed appended-phase construction to require that sealed witness. The public raw settlement remains observation only. | Production promotion cannot compile without consuming the exact comparison result; the real consecutive append passes while isolated work range and digest substitutions all fail to produce the required witness. |

## Phase 4 finding history

| Finding | Severity | Affected guarantees | Defect | Root correction | Closure evidence |
| --- | --- | --- | --- | --- | --- |
| C7-P4-F01 | Critical | C7-DATA-02 C7-DATA-03 | `PageWalBasis` accepted a caller-supplied resulting payload digest beside encoded bytes, allowing two representations of the same persisted frame to disagree. | Removed the digest input and derive both encoded pageLSN and SHA-256 payload identity from the exact encoded frame inside the basis constructor. | Encoded-frame journeys and a controlled caller-digest mutant reject pageLSN or byte substitution before WAL target claims can form. |
| C7-P4-F02 | Critical | C7-BARRIER-01 C7-DATA-01 C7-DATA-04 | WAL-durable and data-settled promotion depended on adjacent boolean validation while stronger progression constructors accepted raw settlements. | Introduced private completion-bound witnesses produced only by exact barrier and data joins and required those witnesses at the stronger constructors. | Raw-settlement compile attacks fail and controlled work artifact digest scheduler source target and effect substitutions cannot produce either witness. |
| C7-P4-F03 | High | C7-FAILURE-01 C7-DATA-04 | Candidate-frame failures erased whether an effect was proven absent unsettled before effect or possibly performed, permitting uncertain failures to resemble safe retry. | Preserved the three-way candidate failure posture through both new-artifact and C6 paths and classify any prior or possible effect as indeterminate. | The real partial-effect matrix and posture mutants keep uncertain authority unacknowledgeable while exact pre-effect failures retain their linear continuation. |
| C7-P4-F04 | High | C7-API-01 C7-AUTHORITY-01 C7-LEDGER-01 | The public API inventory authority trace and living ledger remained frozen at Phase 3 even after Phase 4 production types and routes existed. | Added all twenty-six Phase 4 public surfaces two complete ordinary authority lanes a distinct barrier family and four independently removable data guarantees. | Exact API and trace equality pass; removing `C7-DATA-03` fails even while the broader DATA family remains represented. |
| C7-P4-F05 | High | C7-CLEANUP-01 C7-AUTHORITY-01 | Thirty Phase 4 consumers had no preserve narrow move or delete accounting and forty-four existing paths had stale semantic-family counts. | Recomputed only declared-family counts in memory and assigned every new path a reviewed semantic responsibility destination disposition last consumer phase and absence gate. | All 1619 live consumers reconcile exactly with two retained Phase 3 deletion records; omission count drift stale-row and generic-classification mutants fail. |
| C7-P4-F06 | High | C7-BARRIER-01 C7-DATA-03 C7-DATA-04 C7-FAILURE-01 | Existing tests exercised expected cases but no single contract gate attacked route bypass encoded-frame derivation exact C6 source settlement and premature cleaning together. | Added semantically split WAL-barrier and data-settlement gates with adversarial scheduler completion witness inversion digest basis source and early-clean mutants. | Every honest source inspection and every controlled false implementation now runs under the same Phase 4 source identity as the real journeys and compile attacks. |
| C7-P4-F07 | High | C7-BARRIER-01 C7-DATA-01 C7-DATA-04 C7-LEDGER-01 | The semantic gate proved the expected constructor calls but inspected only their known owner files. A second in-crate stronger-state constructor elsewhere could survive every mutant after a mechanical source-hash refresh. | Added a narrowly scoped production-runtime scan that requires each WAL-durable data-dispatched data-settled and completion-witness construction token exactly once at its semantic owner. | Honest production has the exact five-location set; controlled extra missing and relocated authority each fail independently. |
| C7-P4-F08 | High | C7-BARRIER-01 C7-DATA-01 C7-DATA-04 | The WAL-durable Phase 4 compile attack was registered inside the broad durability-policy UI session while the focused Phase 4 session exercised only data progression. A focused Phase 4 run therefore omitted its first transition and paid for unrelated policy cases when expanded. | Moved the existing barrier attack into the single focused Phase 4 compiler session beside the valid data specimen and sealed data progression attack without duplicating a compiler case. | The focused test now compiles the valid Phase 4 API and rejects both raw barrier promotion and predecessor-skipping data progression in one session; the full UI target retains the same total cases. |
| C7-P4-F09 | Medium | C7-LEDGER-01 | One 84-line ledger mutant test mixed family omission duplicate and stale-evidence shape checks with four independent phase source-identity closures. Its name and failure body no longer predicted one responsibility. | Split ledger-shape mutants from source-identity mutants and centralized the repeated exact-identity mutation in one named assertion step. | Dirty-function scrutiny reports no long-function or signature candidates and each test failure now identifies either ledger shape or source closure. |

## Phase 5 finding history

| Finding | Severity | Affected guarantees | Defect | Root correction | Closure evidence |
| --- | --- | --- | --- | --- | --- |
| C7-P5-F01 | Critical | C7-GROUP-01 C7-BARRIER-01 C7-API-01 | Scalar append and scalar barrier surfaces survived beside the group path so ordinary callers could bypass exact membership and shared-barrier semantics. | Deleted the scalar barrier module and scalar append adapter and migrated every ordinary caller to the nonempty group API with consuming group continuations. | Public API and removal gates prove the scalar surfaces absent while all thirty-four durability-admission journeys use the group path. |
| C7-P5-F02 | Critical | C7-GROUP-01 C7-IDEMPOTENCY-02 C7-FAILURE-01 | Pre-seal cancellation had no retained terminal registry state so an older prepared value could still reach WAL after a same-binding cancellation. | Added explicit Unsealed GroupSealed and ProvenNoEffect states with atomic all-member sealing and exact terminal replay; group append fences terminal bindings before reservation. | The identity blender cancels through a duplicate value then proves the older preparation is rejected with zero WAL frames; same fingerprint replays the fact and conflicting fingerprint is denied. |
| C7-P5-F03 | Critical | C7-GROUP-01 C7-BARRIER-01 | Per-member durable derivation used an iterator shape that could be narrowed to one member while retaining a valid-looking group settlement. | Added cardinality-preserving `NonEmpty::map` and derive every member proof through that typestate-preserving operation. | One shared barrier yields three distinct member proofs and the member-collapse mutant fails. |
| C7-P5-F04 | High | C7-GROUP-01 C7-PERFORMANCE-01 | Group cost sharing had no stage-honest observation and a generic counter bag could misreport future data root or acknowledgment work as completed zeroes. | Added append-stage and barrier-stage amplification types carrying only proved current facts and an explicitly planned root count. | The three-member journey reports one group three frames nonzero WAL bytes one planned shared root one barrier and three durable members; the shared-root collapse mutant fails. |
| C7-P5-F05 | High | C7-API-01 C7-AUTHORITY-01 C7-CLEANUP-01 C7-LEDGER-01 | Cancellation amplification and idempotency-seal surfaces were reachable but absent from the locked API inventory authority trace and removal accounting. | Locked ten additional public surfaces added preparation cancellation and atomic-seal authority steps and classified every new semantic owner and test. | All 138 API rows resolve; ten authority lanes are exact and ordered; 1631 live plus six deleted removal rows reconcile. |
| C7-P5-F06 | High | C7-GROUP-01 C7-LEDGER-01 | Existing source gates depended on rustfmt line breaks and pre-Phase-5 registry field names so honest topology changes failed while some no-op source mutants could pass. | Made the gates whitespace-insensitive updated them to the terminal-aware registry and added a dedicated group-commit gate with four causal mutants. | All contract gates and mutant companions pass; seal bypass partial validation cancellation-after-seal and root-amplification collapse are rejected. |
| C7-P5-F07 | High | C7-DATA-03 C7-MECHANISM-01 C7-CLEANUP-01 | The schema-2 pageLSN frame header shipped while the independent offline verifier and broad integration oracles still decoded schema 1, used a 40-byte header, and retained stale payload capacities and exact byte totals. Focused Phase 4 and Phase 5 tests therefore passed while a harness-correct holistic run failed thirty-seven journeys. | Upgraded the cert-only verifier to schema 2 with independent checksum and non-data pageLSN validation, corrected every header-sensitive payload and manifest capacity, and added one semantic durable-frame test oracle so exact counters derive from current physical geometry instead of scattered C5 literals. | All 53 offline-verifier unit and doc tests pass; the real observer prerequisite builds; all 285 physical-record journeys pass, including independent manifest walks, hostile corruption, exact extent streaming, chunk views, scans, and fresh-process courtrooms. |

## Phase 6 finding history

| Finding | Severity | Affected guarantees | Defect | Root correction | Closure evidence |
| --- | --- | --- | --- | --- | --- |
| C7-P6-F01 | Critical | C7-WAL-05 C7-FAILURE-01 | Rotation derived a group's first LSN from the newly selected empty segment frontier, allowing the global WAL sequence to rebase when a whole group moved to a fresh artifact. | Captured the exact pre-rotation global group LSN start and made every rotated member plan consume that carried value while segment-local byte placement remains independent. | The two-rotation journey proves five frames span global LSNs 1 through 6 without overlap or reset and the controlled rotated-LSN-rebase mutant fails. |
| C7-P6-F02 | High | C7-WAL-05 | The hostile generation-reopen fixture rewrote only the first frame in a multi-frame segment, so a generation substitution could fail for incidental mixed-frame metadata instead of the intended complete-artifact identity rule. | Rewrote every relevant frame header consistently and recomputed each frame checksum before reopening the hostile artifact. | The internally consistent wrong-generation artifact reaches the production inventory identity comparison and returns the exact typed generation denial. |
| C7-P6-F03 | Critical | C7-WAL-05 C7-GROUP-01 C7-FAILURE-01 | Rotation and reopen journeys did not prove the dangerous partial-effect case where one member of a newly created segment lands and a later member fails; reservation reuse or competitor admission could silently fork the group. | Added a real-media partial rotated-group journey that retains the exact reserved suffix and in-flight owner across failure then resumes the same segment generation artifact and contiguous LSN range. | The first member is independently observed on media the continuation reports one appended and one remaining member a competing group is denied and exact continuation completes before the competitor can proceed. |
| C7-P6-F04 | High | C7-API-01 C7-AUTHORITY-01 C7-CLEANUP-01 C7-LEDGER-01 | Phase 6 added five public WAL lifecycle surfaces two authority routes and fifteen tracked files while the API inventory authority trace and removal accounting remained frozen; thirty-five existing semantic-family counts also drifted. | Locked every reachable surface added distinct create and bounded-reopen lanes recomputed only declared-family counts and assigned each new path an exact semantic owner and disposition. | All 146 public surfaces twelve ordered lanes and 1649 live plus eight deletion rows reconcile through exact omission extra drift and schema gates. |
| C7-P6-F05 | High | C7-WAL-05 C7-LEDGER-01 | Runtime journeys covered expected segment worlds but no single source contract attacked whole-group placement exact partial continuation and bounded hostile reopen together, so a false implementation could survive by changing an unobserved branch. | Added one WAL-segment-lifecycle contract gate over the exact production owners and seven independently controlled mutations. | Honest source inspection passes while per-member creation rotated LSN rebasing reserved-suffix replanning premature owner release unbounded listing byte-limit bypass and unsealed reopen each fail. |
| C7-P6-F06 | High | C7-WAL-05 C7-DOCUMENTATION-01 | The public WAL guide explained append fate but omitted segment-byte versus inventory policy whole-group rotation create-versus-append disposition partial continuation bounded reopen and the explicit absence of Phase 6 deletion authority. | Added a segment placement and reopen contract section using the current public types and exact lifecycle and cost boundaries without introducing a second example implementation. | Documentation drift remains bound to the external compile specimen and focused documentation verification passes against the same source closure as the runtime proof. |
| C7-P6-F07 | Medium | C7-DATA-04 C7-VOCABULARY-01 C7-CLEANUP-01 | Current production and proof code still named existing-artifact writeback truth as `C6Writeback`, preserving a milestone label after its semantics became an ordinary C7 input and tempting legacy accounting for an unreleased product. | Renamed the closed source and failure variants tests and contract vocabulary to `ExistingArtifactWriteback` and added no compatibility alias or legacy ledger row. | The data-durability and WAL-before-data families pass and scoped source search finds no remaining phase-coded writeback identity. |
| C7-P6-F08 | High | C7-WAL-03 C7-WAL-05 C7-FAILURE-01 | Three WAL denial journeys retained the pre-rotation assumption that initialization eagerly created an empty first segment. One standalone retry journey tried to identify the WAL effect through a shared media-operation ordinal even though recovery-journal persistence now precedes segment creation, and two demanded a zero-byte artifact after pre-allocation authority denial. | Deleted the redundant ordinal-coupled retry journey instead of preserving false harness precision, made foreign and stale-runtime oracles require complete artifact absence, and retained the stronger partial-group continuation journey as the runtime proof of exact no-effect continuation. Production continues to classify a write failure after successful creation as indeterminate. | The focused authority journeys, partial rotated-group continuation, backend effect-classification tests, and complete durability-admission family distinguish no effect from created-artifact uncertainty without a duplicate counter-shaped oracle. |
| C7-P6-F09 | High | C7-LEDGER-01 C7-GROUP-01 C7-DATA-04 C7-FAILURE-01 | Refactoring left four proof mechanisms anchored to dead source shape: the group-commit gate named the removed `append_group_members` step, one mutation targeted the deleted collapsed settlement module, one exact-receipt mutation omitted the new-artifact range check, and the physical-writer gate still required a range-write receipt for candidate creation. | Retargeted each proof to its exact live semantic seam: `reserve_and_append_group`, the dispatched-settlement owner, the complete new-artifact receipt conjunction, and `CompletedArtifactNewWrite`. No compatibility source or alias was restored. | Honest checks and every adjacent adversarial mutant pass: two group-commit gates, five mutation-binding audits, five mutation-corpus invariants, and all twelve physical-writer gates. |
| C7-P6-F10 | High | C7-AUTHORITY-01 C7-CLEANUP-01 C7-LEDGER-01 | Deleting the redundant WAL retry journey converted one live removal-ledger row into a deletion record, but narrative evidence still claimed 1650 live and seven deleted rows. The parser reconciled path fate while allowing the stale prose to remain green. | Recomputed the disposition totals directly from the CSV and updated every current narrative occurrence to 1657 total, 1649 live, and eight deleted. | Direct CSV accounting, all six removal-inventory tests, and all eleven closure-ledger validators agree on the same current world. |
| C7-P6-F11 | High | C7-API-01 C7-AUTHORITY-01 C7-CLEANUP-01 C7-LEDGER-01 | The bounded dirty-generation capture cutover added nine durability-facing surfaces and two tracked capture files without API or removal classification; concurrent live-WAL inventory work also left two paths unclassified and three WAL-family counts stale. | Locked the capture types and entry methods through the buffer-pool facade added exact semantic owners for all four source paths and updated only the three mechanically reported WAL counts. | All 155 public surfaces resolve; 1653 live plus eight deleted removal rows reconcile with no omission stale row or family-count drift; the checkpoint guarantees remain OPEN pending artifact lifecycle and retention proof. |
| C7-P6-F12 | High | C7-WAL-05 C7-CLEANUP-01 C7-LEDGER-01 | The WAL lifecycle contract still required `complete_member` to be visible across the entire physical runtime after its live owner had correctly narrowed it to the WAL module. Honest topology failed while restoring broader visibility would have weakened the boundary. | Retargeted the contract to require the narrower `pub(super)` member-completion seam while retaining distinct terminal group release and inspection sealing assertions. | The honest lifecycle contract and all seven rotation continuation and reopen mutants pass without widening the production API. |
| C7-P6-F13 | Medium | C7-CHECKPOINT-01 C7-API-01 C7-CLEANUP-01 | The checkpoint format exposed a public one-variant `CheckpointCapturePosture` even though C.7 admits only concurrent capture, creating a dead extension point and making a fixed format invariant look caller-selectable. | Deleted the enum, field, getter, and facade export; retained one private format tag that the encoder always writes and the decoder rejects when substituted. | Warnings-denied Clippy, four focused hostile and round-trip tests, 139 package unit tests, nine integration tests, and twenty compile-fail proofs pass with no dead-code suppression in the active slice. |
| C7-P6-F14 | High | C7-CHECKPOINT-01 C7-MECHANISM-01 C7-CLEANUP-01 | Premature checkpoint candidate and published variants polluted `RecordArtifactFile`, the ordinary record-serving artifact universe, and forced unrelated serving, recovery, scheduling, and data-identity code to understand checkpoint lifecycle files. | Deleted both variants, their filename helpers, the duplicate naming test, and their dirty-basis tags. Checkpoint identity remains path-independent; the later checkpoint backend owner will define lifecycle topology without widening ordinary record semantics. | The full Store library compiles with all five exhaustive ordinary-artifact matches unchanged, the Phase 6 public API gate passes, and the checkpoint format rejects the removed tags as unknown artifacts. |
| C7-P6-F15 | Critical | C7-CHECKPOINT-03 C7-FAILURE-01 | Checkpoint completion entered execution settlement without a distinct causal-lowering fate or explicit courtroom source closure, allowing it to disappear into another physical family. | Added the exact CheckpointCompleted fate and explicit checkpoint cases throughout causal lowering and source closure. | Checkpoint settlement and courtroom source manifests accept only the dedicated checkpoint family and the complete physical-record journey suite passes. |
| C7-P6-F16 | Critical | C7-CHECKPOINT-03 C7-FOUNDATIONAL-01 C7-PERFORMANCE-01 | Checkpoint work initially used a raw file-sync pressure shape and was classified as ValidationPlanning, so authoritative publication could enter the wrong policy and interference lane. | Routed candidate synchronization through the filesystem-admitted checkpoint pressure class and required Foundational AuthoritativeMutation RebuildCapable ColdPath evidence. | The checkpoint route gate rejects foreground raw-Signal wrong-pressure and planning-policy mutants while preserving the admitted foreground WAL barrier lane. |
| C7-P6-F17 | High | C7-CHECKPOINT-03 C7-MECHANISM-01 | The C4 artifact path owner rejected a root-level admitted checkpoint name because it required at least one child component after the admitted root. | Reused the already admitted root handle when the artifact has zero directory components while preserving normal child traversal. | Direct backend proofs and the ordinary checkpoint journey create and publish the root-level checkpoint only through scheduled C4 operations. |
| C7-P6-F18 | High | C7-CHECKPOINT-03 C7-CLEANUP-01 | Checkpoint failures retained unused private payloads and a dead public posture extension, while checkpoint capture and settlement files crossed the repository line cap. | Removed decorative payloads and the unused posture surface, extracted checkpoint failure vocabulary and durability settlement classification into semantic modules, and added no warning suppression. | Default and all-feature all-target Store checks are warning-free; 270 dirty or untracked Rust files have zero line-cap violations and the checkpoint slice contains no dead-code or unused allowances. |
| C7-P6-F19 | High | C7-CHECKPOINT-03 C7-SIGNAL-01 C7-LEDGER-01 | The holistic journey suite encoded stale topology: a generic append text oracle conflated WAL and checkpoint owners, native Signal cardinality remained nine, and the courtroom source manifest named the pre-checkpoint digest. | Replaced the generic append oracle with exact semantic owner assertions, advanced the Signal binding contract to ten, and regenerated the exact courtroom source identity. | All 290 physical-record journeys and all three authority UI groups pass, including independent checkpoint decoding and sealed checkpoint authority. |
| C7-P6-F20 | High | C7-API-01 C7-CLEANUP-01 C7-LEDGER-01 | The ledger generator omitted ten Phase 6 durability surfaces and twenty-one tracked checkpoint paths, retained one stale row, and allowed twenty-five semantic-family counts to drift. | Expanded the locked public set and facade reachability, removed the false stale row, classified every new path under a concrete semantic owner, and reconciled only the reported family counts. | All 174 public surfaces resolve; all 1687 removal rows reconcile; omission stale-row and count-drift mutants fail. |
| C7-P6-F21 | Critical | C7-CHECKPOINT-03 C7-LEDGER-01 | No single adversarial contract proved the complete checkpoint route, so individually green tests could miss a basis scheduler receipt operation or direct-media bypass. | Added one exact route contract from checkpoint mutation semantics through Signal background scheduling Foundational policy Store execution and all five scheduled C4 publication effects, plus a separate checkpoint source identity. | The honest route passes and eleven controlled mutants reject projection basis wrong Signal foreground routing raw Signal wrong pressure planning policy receipt substitution wrong operation direct media unscheduled publication and missing independent journey evidence. |
| C7-P6-F22 | Critical | C7-RETENTION-01 C7-RETENTION-02 C7-AUTHORITY-01 C7-CLEANUP-01 | Recovery physics exported synthetic delete recycle and exclude requests whose eligibility required only a covering checkpoint and contiguous range. Those types could appear to authorize WAL destruction without live-binding compaction or last-copy proof. | Deleted the entire action request candidate eligibility and admitted-action family. Moved the one real recovery fact into source precedence as `ContiguousWalTailProof`; it proves only exact boundary adjacency and opens no retention effect authority. | All 16 recovery-physics unit tests and 44 compile-fail doc tests pass; the real certification boundary test rejects a shifted tail; all 174 public surfaces and 1688 removal rows reconcile with 1679 live and nine deleted paths. |
| C7-P6-F23 | High | C7-CHECKPOINT-03 C7-LEDGER-01 | The compiler-total checkpoint completion fate reached production and courtroom evidence but the feature-gated runner label projection omitted it, making all-feature evidence builds non-exhaustive. | Added the exact `checkpoint-completed` projection and a focused distinction from ordinary publication completion; no wildcard or fallback label was introduced. | Both feature-gated label tests pass and warnings-denied all-target all-feature Clippy passes for recovery physics certification and the Store test runner. |
| C7-P6-F24 | Medium | C7-CHECKPOINT-03 C7-CLEANUP-01 | Managed checkpoint admission moved active-attempt contention into `PhysicalCheckpointStartDeferred`, but the public capture-failure vocabulary still retained an unreachable `CaptureAlreadyActive` variant from the superseded synchronous path. | Deleted the duplicate failure variant; active capture contention now has one semantic owner and one caller-visible classification at start admission. | Narrow constructor search finds no missing capture-failure producer; all six checkpoint lifecycle tests, the exact sealed-authority compile attack, formatting, and warnings-denied all-target all-feature Clippy pass. |
| C7-P6-F25 | Medium | C7-CHECKPOINT-03 C7-LEDGER-01 | The checkpoint route gate searched the ordinary journey for one whitespace-sensitive start expression, so `rustfmt` alone erased independent-publication evidence and made an honest route fail. | Normalized the journey before semantic matching and added a controlled mutant that removes the managed checkpoint facade from the evidence path. | The honest route and all eleven route mutants pass after repository formatting, including the new missing-independent-evidence attack. |
| C7-P6-F26 | High | C7-API-01 C7-CLEANUP-01 C7-LEDGER-01 | Adding the managed checkpoint facade pushed the shared API-reachability gate to 418 lines, mixing Phase 6 checkpoint and WAL reachability with the earlier phase registry in one oversized file. | Extracted the complete Phase 6 reachability contract into its own semantic submodule and added that owner to the checkpoint source closure without creating a generic helper bag. | The public-API and removal-inventory tests each execute and pass; the parent gate is 245 lines, the Phase 6 owner is 176 lines, and the checkpoint source identity tracks both files. |
| C7-P6-F27 | High | C7-CHECKPOINT-01 C7-LEDGER-01 | The first whole-Store materialization gate rejected only `collect` into `Vec` or `Box`, so a helper-hidden `Vec::new` plus `push`, another collection family, or deferred slice emission could accumulate the complete dirty source while preserving every positive route assertion. | Expanded the prohibition across both capture-owner files, rejected heap accumulation primitives, and required exactly one immediate append and allocation scope for each unfinished and completed slice. | Honest capture and the 32-times Store siege pass; eight controlled mutants reject unbounded policy false accounting leaked accounting substituted frontier direct collection helper-hidden accumulation missing slice emission and weakened pressure. |
| C7-P6-F28 | High | C7-AUTHORITY-01 C7-CLEANUP-01 C7-LEDGER-01 | Current proved ledger narratives still claimed 1688 total 1679 live and nine deleted removal rows after checkpoint pressure evidence raised the inventory to 1700 total and the actual disposition split was 1692 live plus eight deleted. | Recomputed totals directly from the reconciled CSV and corrected both current-world guarantee narratives without rewriting historical finding evidence. | Direct CSV accounting reports 1700 rows and eight delete dispositions; the removal inventory and all exact source-identity validators pass against 1692 live consumers. |
| C7-P6-F29 | High | C7-CHECKPOINT-02 C7-LEDGER-01 | The checkpoint worker notified handle waiters of terminal fate before recording Store-owned counters publication retention current-attempt terminal posture and worker panic truth, so caller-visible finalization could briefly race ahead of the lifecycle owner. | Reordered terminal settlement to record the exact result in the Store owner before completing and notifying the attempt; added an observer-first ordering mutant. | Nine lifecycle journeys pass and the managed-lifecycle contract requires owner finalization before handle completion while the reversed-order mutant fails. |
| C7-P6-F30 | High | C7-CHECKPOINT-02 C7-API-01 C7-CLEANUP-01 C7-LEDGER-01 | Existing lifecycle tests covered start cancellation uncertainty and close drain but did not execute pending or terminal disposal post-publication cancellation refusal or adversarially forbid detached worker forgotten enumeration omitted close join and cancellation-on-drop. | Added three nonredundant lifecycle observation journeys locked all handle methods through the public facade and added one Store-ownership contract with eight causal mutants; removed a duplicate terminal-rejoin journey already proved by the pressure siege and start contract. | All nine lifecycle journeys both lifecycle contract tests public reachability and exact removal inventory pass; the new observation module is independently classified and source-bound. |
| C7-P6-F31 | High | C7-RETENTION-01 C7-CHECKPOINT-02 C7-FAILURE-01 | The first retained-tail join released the WAL inventory lock before checkpoint replacement and namespace synchronization, so a foreground durable append could cross publication while the completed checkpoint still claimed the earlier tail frontier. | Added `PhysicalWalCheckpointCutover`, a typed guard that owns the WAL runtime lock from exact inventory projection through replacement and namespace sync, then releases it before ordinary foreground work resumes. | A deterministic journey pauses replacement and namespace sync, proves foreground WAL completion cannot cross the fence, then proves the completed tail ends at the pre-foreground boundary; early-release and unguarded-cutover mutants fail. |
| C7-P6-F32 | High | C7-RETENTION-01 C7-LEDGER-01 | Retained-tail source mutants used formatting-sensitive replacement and could silently apply zero edits after `rustfmt`, allowing a false implementation to remain green under a no-op adversarial attack. | Canonicalized source before mutation and required every controlled anchor to match exactly once through `mutate_once`; strengthened the detached-outcome attack to target the stored authority field rather than a duplicate parameter. | The honest contract and all fourteen topology authority fence bound and independent-oracle mutants pass only after each named defect is proven to have been injected exactly once. |
| C7-P6-F33 | High | C7-RETENTION-01 C7-CLEANUP-01 C7-LEDGER-01 | Joining retention admission replacement namespace synchronization and cleanup into checkpoint capture pushed `capture/execution.rs` to 428 lines and collapsed publication cutover into the streaming-capture owner. | Extracted `capture/publication_cutover.rs` as the semantic owner of fenced tail admission publication and failure cleanup; `execution.rs` returned to bounded capture orchestration. | Store-scoped line-cap accounting reports zero violations; warnings-denied Clippy and retained-tail unit journey and adversarial contract families pass across the extracted boundary. |
| C7-P6-F34 | High | C7-CHECKPOINT-01 C7-CLEANUP-01 C7-LEDGER-01 | After publication cutover extraction `PhysicalCheckpointCaptureOwner::execute` remained a 138-line mixed-level body that directly owned candidate creation bounded allocation streaming emission cancellation source validation finalization synchronization and publication handoff. | Added `capture/streaming.rs` as the bounded dirty-source owner and reduced execution to named creation streaming source-validation finalization synchronization and publication phases while retaining cleanup authority in the lifecycle owner. | Function scrutiny no longer reports `execute`; the focused bounded-capture contract and checkpoint journey family pass with no new public or compatibility surface. |
| C7-P6-F35 | High | C7-CHECKPOINT-01 C7-LEDGER-01 | The bounded-capture contract required the loop and two duplicated append calls to remain inside `execute`, so the test enforced the collapsed implementation shape and would reject a semantically stronger decomposition. | Retargeted the contract to the streaming owner and separately requires bounded next-slice allocation exactly one immediate emission path progress accounting no accumulation and complete-or-more continuation; all controlled edits now attack semantic obligations rather than source duplication. | Honest decomposed capture passes and all eight bounded-capture mutants still reject unbounded policy false or leaked accounting substituted frontier direct or helper-hidden accumulation deferred emission and weakened pressure. |
| C7-P6-F36 | Critical | C7-IDEMPOTENCY-03 C7-PERFORMANCE-01 C7-CLEANUP-01 | The first binding-compaction design retained encoded records in a second memory-shaped collection and had no bounded fresh-process stream contract, making memory proportional to live history and inviting all-history reopen. | Made pending compaction retain only authority and generation, encode one record per consumer call, and reopen only fixed headers, the footer, and one bounded binding frame while skipping the dirty body. | Independent reopen counters match real media exactly; source mutants that add `Vec<Box<[u8]>>` to either publication or reopen fail. |
| C7-P6-F37 | Critical | C7-IDEMPOTENCY-03 C7-AUTHORITY-01 C7-FAILURE-01 | Instance construction could create durability and WAL owners without one compiler-visible checkpoint-plus-retained-WAL reconstruction boundary, allowing ordinary authority before idempotency truth was rebuilt. | Added one `PhysicalDurabilityReopenBasis`, split pre-reopen and post-reopen durability-owner types, and moved record-serving installation after checkpoint reopen WAL inventory idempotency rebuild and owner installation. | The sixteen-step authority lane resolves exactly and raw-owner authority, duplicate-inventory, missing-member-transfer, and construction-bypass mutants fail. |
| C7-P6-F38 | Critical | C7-IDEMPOTENCY-03 C7-GROUP-01 C7-FAILURE-01 | Compaction bytes and retained WAL payloads had no canonical rebuild owner, and terminal fate remained coupled to registry storage, so malformed order foreign facts incomplete groups or omitted fate could reconstruct dishonest authority. | Added canonical persisted-binding and compaction decoders, one bounded idempotency rebuilder, `RebuiltUnresolved` state, borrowed verified WAL payloads, and the stable `idempotency/fate/` seam. | Owner tests reject order duplicate foreign lease tail group and canonical-roundtrip violations; fresh-process journeys prove unresolved and terminal worlds; omitted-fate and weaker-WAL-inspection mutants fail. |
| C7-P6-F39 | High | C7-IDEMPOTENCY-02 C7-IDEMPOTENCY-03 C7-PERFORMANCE-01 | The policy bounded pending unresolved work but not the complete authoritative registry, so terminal fates retained by lease could grow without an independent live-binding limit. | Added `LiveIdempotencyBindingLimit` as a nonzero admitted policy input and enforce it independently during ordinary admission and fresh-process rebuild. | A real terminal binding exhausts the total-live bound after pending count returns to zero, while unit rebuild tests distinguish pending and live overflow. |
| C7-P6-F40 | High | C7-IDEMPOTENCY-03 C7-LEDGER-01 | Initial reopen proof covered only one unresolved compaction-plus-tail world and did not independently prove exact reopen cost terminal retention expiry repeated generation cutover or the corresponding false implementations. | Added independent checkpoint and WAL media accounting, separate legal fresh-process terminal and uninterrupted expiry worlds, and controlled cutover fate buffering inspection and authority mutants with exact non-no-op anchors. | Four real-media journeys nine owner tests and twelve controlled source mutants pass, including same-key generation stability and subsequent single advancement from the existing publication journey. |
| C7-P6-F41 | High | C7-API-01 C7-AUTHORITY-01 C7-CLEANUP-01 C7-LEDGER-01 | The new reopen implementation left eleven public surfaces twenty-two tracked files twenty-seven family counts and the WAL-reopen authority lane outside the living accounting world. | Locked all 205 public surfaces, classified every new file, updated only discovered counts, and replaced the stale nine-step WAL-only lane with the exact sixteen-step durability reopen basis. | API inventory removal reconciliation and authority trace gates all pass; 1731 rows reconcile with 1722 live and nine absent paths. |
| C7-P6-F42 | Medium | C7-CLEANUP-01 C7-LEDGER-01 | The removal-ledger reconciler reported unclassified paths without their discovered family counts, forcing reviewers to reconstruct the generator's evidence manually. | Made the generic mismatch diagnostic retain each unclassified path's exact discovered family-count map. | The controlled omission and drift tests still fail causally, and the real inventory now closes directly from the diagnostic evidence it emits. |
| C7-P6-F43 | High | C7-WAL-05 C7-CHECKPOINT-02 C7-LEDGER-01 | The holistic physical suite retained the pre-idempotency WAL frame size in three rotation fixtures and inferred checkpoint publication from a fixed backend-arrival ordinal. The new binding payload made valid two-member groups exceed the stale limit, while additional bounded compaction writes left cutover tests paused in capture. | Advanced the exact fixture segment limit to the current admitted two-member group size and made cutover synchronization drive from `PhysicalCheckpointProgressPhase::PublicationReplacement`, carrying the observed arrival identity into replacement and namespace release. | All seven exposed WAL rotation continuation hostile-reopen and checkpoint-cutover failures pass, followed by all 306 physical-record journeys. |
| C7-P6-F44 | Medium | C7-WAL-05 C7-API-01 C7-CLEANUP-01 | `PhysicalSignalConstructionFailure` retained the unreachable standalone `WalArtifactInitializationRejected` variant after WAL reopen moved under the unified durability-state basis, and the hostile reopen oracle was the only remaining consumer. | Deleted the dead variant and made hostile worlds require `DurabilityStateReopenRejected(PhysicalDurabilityStateReopenFailure::Wal(...))` with no compatibility match. | Scoped source search finds no retired variant; both hostile WAL reopen journeys and the complete physical-record suite pass through the single current authority boundary. |
| C7-P6-F45 | Critical | C7-AUTHORITY-01 C7-IDEMPOTENCY-03 C7-FAILURE-01 C7-CLEANUP-01 | The decomposed instance constructor omitted the raw durability owner from `PhysicalStoreInstanceConstructionFailure`, so work-preparation or durability-reopen failure could drop that authority before residency shutdown and media release. | Made every construction-failure path retain the exact raw owner and made abort explicitly drop it after residency and termination teardown but before media close; successful construction still installs only the distinct reopened owner typestate. | The lifecycle contract distinguishes raw pre-reopen and installed post-reopen owners, rejects optional or omitted-owner mutants, and all twenty-nine contract tests pass. |
| C7-P6-F46 | High | C7-LEDGER-01 C7-CHECKPOINT-02 C7-GROUP-01 C7-IDEMPOTENCY-02 C7-WAL-05 | The complete boundary gate exposed seven structural oracles frozen to pre-Phase-6 function names state variants decoder stages and incidental inline layout. Focused checks had not exercised those honest paths, and several controlled edits were no-ops masked by the already-failing baseline. | Retargeted every oracle to the live semantic owner: phase-driven checkpoint publication, binding-compaction-aware cutover and decoder completion, verified borrowed WAL inspection, sealing bindings, decomposed fresh admission and replay classification, and compiler-distinct raw versus reopened durability ownership. | All twenty-nine honest and controlled-mutant contract tests pass together; removal reconciliation reports no omitted stale or rediscovered path and only the exact reviewed family-count changes. |
| C7-P6-F47 | High | C7-API-01 C7-CLEANUP-01 C7-LEDGER-01 | Adding the total-live idempotency dimension pushed the durability policy owner to 401 lines, and the workspace cap was obscured by unrelated repository-wide debt. | Extracted the versioned policy identity encoder into `admission/policy/identity.rs`, a semantic growth point for future policy dimensions, rather than creating a generic helper or compressing the policy owner to game the cap. | The policy owner is 379 lines, the identity owner is 32 lines, Store unit tests pass, and removal reconciliation accounts for all 1732 rows with 1723 live and nine absent paths. |

## Phase 7 finding history

| Finding | Severity | Affected guarantees | Defect | Root correction | Closure evidence |
| --- | --- | --- | --- | --- | --- |
| C7-P7-F01 | Critical | C7-ROOT-02 C7-ROOT-04 C7-ROOT-07 | Root preparation projected each data-settled member into record output and then discarded the exact settled mutation proof, so later root stages could preserve member identities while losing the WAL append, shared barrier, data-effect, and completion evidence those identities named. | Added one nonconstructible `RootPublicationPhysicalMutationMember` that owns the exact settled mutation basis and completion projection; every prepared, replaced, namespace-durable, and completed root now carries the nonempty exact member set, and retryable planning failures retain the same linear evidence. | Focused one-member and multi-member carriage journeys prove exact records, append settlement, barrier settlement, and data-effect count survive every root transition; the active-transition denial returns the planning owner and succeeds on retry; warnings-denied all-target all-feature Clippy passes. |
| C7-P7-F02 | Critical | C7-ROOT-02 C7-ROOT-04 C7-ROOT-07 | Candidate preparation cloned opaque prepared record projections before successor-root planning, so the type system did not prove one consumed preparation became one candidate and one exact failure residue. | Added a consumed `RootPublicationCandidatePlan` and distinct written-candidate state owning the exact group members, assembled publication plan, successor allocation truth, active transition, and stage-appropriate failure residue; pre-effect denials return a consumable candidate continuation while possible-effect failures retain the exact candidate evidence; removed `Clone` from `PreparedPhysicalRootProjection`. | The Phase 7 compile-fail authority case rejects `Clone` for both planning and candidate plans; the ownership contract rejects a projection `Clone` derive or duplication method and requires the consuming continuation API; focused root-carriage journeys, exact formatting, compilation, and warnings-denied all-target all-feature Clippy pass. |
| C7-P7-F03 | High | C7-API-01 C7-ROOT-08 C7-LEDGER-01 | The locked public API still required deleted `PhysicalRootPublicationRuntime` from the displaced isolation crate and omitted the complete canonical Phase 7 progression failure and submission boundary. | Removed the stale parallel-runtime surface, locked thirty-five canonical Phase 7 surfaces, and added a Phase 7 facade-reachability owner covering durability exports linear planning and candidate types and all six root submission transitions. | All 243 exact public surfaces resolve to current declarations and destinations; the Phase 7 facade gate rejects hidden durability or record-serving exports and missing submission operations. |
| C7-P7-F04 | High | C7-CLEANUP-01 C7-ROOT-08 C7-LEDGER-01 | The removal ledger omitted thirty-four Phase 7 semantic owners, retained absent parallel publication layout migration and operational paths as open, and carried stale family counts across root work and scheduler owners. | Classified every new path by its exact root responsibility, marked twenty-nine actually absent paths deleted in Phase 7, removed two present files that no longer match a tracked family, and updated only discovery-reported family counts. | All 1785 rows reconcile exactly: 1747 live tracked consumers and thirty-eight absent records comprising thirty-seven deletions and one completed move; schema classification omission stale-row rediscovery and count-drift tests all pass. |
| C7-P7-F05 | Critical | C7-ROOT-04 C7-ROOT-07 C7-FAILURE-01 | Root candidate execution marked the transition effect-started before the first artifact write, consumed the failed frame bytes, and classified every candidate-write failure as inspection-required. An exact first-write proven-no-effect result therefore poisoned Store health and returned a nominal candidate continuation whose publication plan no longer contained the failed artifact bytes. | Added a narrow recoverable candidate-frame write that retains the exact owned frame; root execution now derives `RetryableNoEffect` only from zero completed artifacts plus exact proven-no-effect evidence, restores the complete candidate plan, and releases the transition without revoking health. Partial, unsettled, and possible-effect results remain typed inspection-required residue and revoke runtime health. | The first-write denial journey retries the returned complete plan through prepared-root publication; seven residency units and five root-carriage journeys pass. The source contract rejects four partial-effect, discarded-frame, retry-health-poison, and missing-inspection-health mutants; the full five-test authority suite and warnings-denied all-target all-feature Clippy pass. |
| C7-P7-F06 | High | C7-ROOT-03 C7-DESTINATION-01 C7-LEDGER-01 | The dirty-worktree structural scan found `physical_runtime/work/scheduler_demand.rs` at 437 lines with queue-locality identity derivation embedded beside demand construction, lane admission, queue lowering, and scheduler admission. The file exceeded the constitutional hard cap and hid a stable semantic boundary inside a broad owner. | Extracted physical queue-locality identity and range derivation into `work/scheduler_demand/locality.rs`; the parent retains scheduler-demand construction and admission, with exact root-publication action and artifact binding and no alias or duplicate authority. | The parent is 288 lines and the semantic locality owner is 155; all 518 dirty Rust files are within the 400-line cap. Root carriage, WAL barrier, locality, locked API, and six-test removal reconciliation pass, and the 1786-row inventory accounts for the new owner. |
| C7-P7-F07 | High | C7-SIGNAL-01 C7-ROOT-03 C7-LEDGER-01 | The broader physical-locality family still expected eleven installed native record and durability Signal bindings after Phase 7 added the distinct root-publication binding. The stale count made honest one-graph topology fail while focused root tests stayed green. | Updated the shared exact native-binding expectation to twelve and corrected the locality oracle narrative; retained the same one-graph and one-route-per-binding assertions. | All five locality, ten profile, and eleven readiness tests pass; locked public API and six-test removal reconciliation remain green. |
| C7-P7-F08 | Critical | C7-ROOT-01 C7-ROOT-08 C7-FAILURE-01 | A canonical namespace-durable root publication advanced `PhysicalCurrentRootOwner` to generation two, but `ServingPhysicalRuntime::records()` still sourced generation one from the duplicate `RecordPublicationState`; the newly durable record was therefore invisible to the serving reader. The same duplicate state remained writable through the direct `append_batch` catalog-replacement lane. | Migrated or deleted every direct-publication consumer, removed direct catalog-replacement progression, routed planning readers checkpoint capture observation and serving through `PhysicalCurrentRootOwner` snapshots, and deleted current-root and free-space authority from general director state. Deleted the old facade rather than adding double writes generation selection aliases or a compatibility adapter. | Canonical publication now advances generation two and immediately serves the new record; reconstructive capacity transition reopens at the requested capacity. Five legacy public surfaces and the entire direct writer are absent, the canonical eleven-step authority lane resolves, all 1799 removal rows reconcile, warning-denied all-target compilation passes, and all 289 physical-record journeys pass. |
| C7-P7-F09 | Critical | C7-ROOT-02 C7-ROOT-04 C7-API-01 | Canonical durable-data materialization reads and frames streaming sources after `PreparedPhysicalRootProjection` captures `PublicationObservation`, but it never updates or finalizes that observation. Namespace-durable completion therefore preserves record identities while reporting zero completed bytes transfers copies and scratch for a real extent publication, violating the inherited C5 operation-result contract. | Durable materialization now records each member's exact source copies, copied bytes, scratch peak, and transfer-width peak; exact data settlement finalizes completed bytes and transfer count from that member's settled data effects before a completion projection can be created. Shared group and root amplification remains separate instead of being copied from ambient media counters into every member. | The canonical mixed inline-plus-1-MiB extent courtroom executes and proves 1,048,619 requested and completed bytes, exactly `chunks + 1` settled member data transfers, a 16-KiB peak transfer width, exact source-copy evidence, and fresh-process read parity. The settlement-bound source contract rejects omitted settlement, constant transfer count, invisible extent-copy, and incomplete-byte mutants; all six authority tests, all five root-carriage tests, exact removal reconciliation, and warnings-denied all-target all-feature Clippy pass. |
| C7-P7-F10 | Critical | C7-IDEMPOTENCY-01 C7-ROOT-08 C7-FAILURE-01 | Canonical durable preparation could express only preserve-current manifest capacity, while the required reconstructive capacity change remained reachable only through the direct publication lane. Treating reconstruction as an execution-local choice would let the same idempotency identity request different root effects and let retry or group merge silently hardcode the ordinary posture. | Added one typed `PhysicalManifestCapacityTransition` selected before admission, encoded it after placement in canonical request scope, carried it through prepared mutation WAL planning root projection proven-no-effect reconstruction and root rebase, and reject mixed settled-group transitions before publication planning. The ordinary facade explicitly selects preserve-current; the explicit facade carries reconstruction without a compatibility alias. | The canonical manifest-scale courtroom passes at 1, 9, and 65 records and proves no-effect denial plus successful capacity changes 4 to 8 to 2 with 35 records and fresh reopen parity. A dedicated contract rejects omitted identity, facade hardcoding, retry-state loss, mixed-group admission, and root-rebase hardcoding mutants. |
| C7-P7-F11 | High | C7-API-01 C7-AUTHORITY-01 C7-CLEANUP-01 C7-LEDGER-01 | The closed-world API lock omitted the new transition type and explicit preparation method, the authority trace still required the fully deleted four-step physical-isolation root island, and cleanup accounting retained legacy append-family claims after canonical scale migration. | Locked both public surfaces through the existing facade chain, removed the absent parallel-root lane rather than inventing replacement files, classified the transition owner, and reconciled only the five mechanically reported family-count drifts. | All 245 public surfaces resolve; both authority-order and authority-mutant tests pass; all six removal tests reconcile 1,790 rows comprising 1,752 live and 38 proved absent records; all 17 source-closure validators pass. |
| C7-P7-F12 | Critical | C7-ROOT-01 C7-ROOT-02 C7-ROOT-03 C7-ROOT-04 C7-ROOT-05 C7-ROOT-06 C7-ROOT-07 C7-ROOT-08 C7-LEDGER-01 | Production and focused proofs had advanced through Phase 7 while all eight Phase 7 guarantee rows remained `OPEN` with pending evidence, and the ledger validator enforced exact source identity only through Phase 6. The ledger could therefore report Phase 7 work without proving or reopening it. | Completed every Phase 7 evidence row from current production and independent proofs, defined one bounded Phase 7 root source closure, and added a phase-order gate requiring all eight rows to be `PROVED` against that exact identity before Phase 8 can begin. | The honest Phase 7 closure validator passes; controlled unresolved-status stale-source-identity omitted-guarantee and phase-reassignment mutations fail. The complete boundary gate binds the living ledger to production proofs API accounting authority trace and compile attacks. |
| C7-P7-F13 | High | C7-AUTHORITY-01 C7-API-01 C7-CLEANUP-01 C7-LEDGER-01 | Direct-writer deletion left five removed surfaces in the public lock, an authority lane targeting deleted progression files, two dead semantic families in live discovery, twenty-nine deleted paths marked open, fifteen new paths unclassified, fifty-one family-count drifts, and three source contracts anchored to pre-extraction formatting or files. | Deleted the legacy API locks and live discovery families, replaced the old lane with the canonical root progression, retained retired family names only as deletion history, classified every new owner, reconciled exact current counts, and retargeted contracts to semantic owners instead of restoring old source shape. | Public API authority trace removal reconciliation mutation-preparation and all eight WAL-before-data validators pass with their adversarial mutants; 241 current public surfaces and 1732 live tracked consumers resolve without a compatibility row. |
| C7-P7-F14 | High | C7-SIGNAL-01 C7-ROOT-05 C7-ROOT-08 C7-CLEANUP-01 | After the direct publication path was removed, unreachable scheduler reservation helpers a transient Signal publication-dependency graph and mailbox API catalog-cutover residency hooks reusable-byte storage and trace-only settlement conveniences remained as dead alternate mechanics. | Deleted the entire unreachable Signal dependency subsystem and its certification exports, removed scheduler catalog-cutover and reusable-byte remnants, and let the compiler expose and remove every causal import helper and declaration method. | Warning-denied all-target compilation is clean; canonical root Signal gating exact scheduler-capacity retry and all 289 physical-record journeys pass. No compatibility command observation or dormant graph node remains. |

## Phase 8 finding history

| Finding | Severity | Affected guarantees | Defect | Root correction | Closure evidence |
| --- | --- | --- | --- | --- | --- |
| C7-P8-F01 | Critical | C7-LEDGER-01 C7-API-02 C7-LIFECYCLE-01 C7-OBSERVATION-01 C7-SETTLEMENT-01 C7-SETTLEMENT-02 C7-EVIDENCE-01 | The initial Phase 8 ledger described the ordinary facade, Store ownership, and Signal cancellation/deadline input in only three rows. It did not separately require completion-only acknowledgment authority, proven-no-effect versus indeterminate separation, completed-but-unobserved finalization, or one-way evidence projection, so an implementation could satisfy every listed row while remaining causally incomplete. | Added four semantic guarantee rows and an exact Phase 8 validator; implemented positive and adversarial API proof lifecycle boundary matrices terminal persistence/reopen proof one-way projections and source mutants; reconciled current API authority and removal inventories. | Closed by the current Phase 8 source identity and independent evidence named in every affected guarantee. |

## Phase 9 finding history

| Finding | Severity | Affected guarantees | Defect | Root correction | Closure evidence |
| --- | --- | --- | --- | --- | --- |
| C7-P9-F01 | Critical | C7-CLEANUP-02 C7-DOCUMENTATION-01 C7-API-01 | `worth-store-wal::DurablePublicationDeclaration` and its `durable_publication/` directory described a declaration and scope as if durability had already completed, despite granting no execution barrier or acknowledgment authority. | Built the semantically named `publication_declaration/` destination in parallel, renamed the declaration and three scopes, migrated exact consumers, deleted the old directory, and locked the cutover with old-path old-name incomplete-destination and reintroduction mutants. | The affected crate graph compiles; the declaration-topology gate and locked public API pass; source search finds no old type names; removal reconciliation proves all five old paths absent and all five new owners classified. |
| C7-P9-F02 | High | C7-SIGNAL-01 C7-FOUNDATIONAL-01 C7-CLEANUP-02 C7-DOCUMENTATION-01 | The aspect-native documentation still claimed five generic durability contracts while production had added WAL reclamation and left the five mutation contracts without the exact Store partition needed for scoped invalidation. | Defined six exact contracts: five stable Store-partitioned mutation bases and one policy-identity-partitioned Foundational projection; retained runtime incarnation and artifact-specific identity in typed Store commands and graph ownership instead of duplicating physical truth into Signal or Foundational state; added unpartitioned ephemeral-runtime missing-contract and generic-barrier-key mutants. | Six focused work-semantics tests and both aspect-contract gate tests pass; the engineering spec and aspect-native gate document the exact keys roles families masks partitions profile lifetime and non-authority boundaries. |
| C7-P9-F03 | High | C7-AUTHORITY-01 C7-API-01 C7-CLEANUP-02 C7-DOCUMENTATION-01 C7-LEDGER-01 | The current public API and S9 inventories retained the deleted declaration name; seven physical-reality rows described pre-C7 authority; the removal ledger omitted Phase 9 files and carried migration-wide token-count drift; the roadmap did not link the current contract or bounded C8 handoff. | Reconciled every item from current source: locked the renamed API; classified implemented deleted and model-only audit lanes; updated exact removal counts and path fates; linked the guide ledgers audit and successor handoff; and added an exact Phase 9 cleanup/documentation source-closure validator. | The public API gate passes; all seven removal-ledger tests pass; the audit parses as forty 22-column rows with all seven target rows complete; guide examples compile externally and match documentation byte-for-byte; the validator rejects omission wrong phase stale proof unresolved closeout and premature Phase 10 evidence. |
| C7-P9-F04 | Critical | C7-SIGNAL-01 C7-CLEANUP-02 C7-DOCUMENTATION-01 C7-LEDGER-01 | The first partition correction included ephemeral runtime identity in the installed Signal profile, so the same Store and policy produced a different semantic profile after lawful reopen. The holistic suite also found a cleanup oracle aimed at the deleted recovery file and an empty `wal_durability/` directory left behind. | Partitioned mutation contracts by stable Store identity, kept runtime-incarnation separation in the runtime-owned graph and typed Store commands, added an ephemeral-runtime partition mutant, retargeted the cleanup proof to descriptive `wal_recovery_basis/`, and deleted the proven-empty obsolete directory. | Both independent reopen-stability journeys pass, the displaced-executor absence journey passes, and the aspect gate rejects unpartitioned ephemeral-runtime missing-contract and wrong-key mutants. |
| C7-P9-F05 | High | C7-GRAPH-02 C7-API-01 C7-CLEANUP-02 C7-DOCUMENTATION-01 | `worth-store-test-support` depended directly on `worth-signal` only to manufacture mutation deadlines, violating the rule that the Store composition owner is the sole Store package allowed to own the private physical Signal runtime and forcing fixtures to understand Signal units. | Added Store-owned nonzero millisecond constructors for mutation and checkpoint deadlines, migrated the public guide and compile specimen plus both physical-residency fixtures, removed the test-support feature and dependency edge, and locked both new methods in the exact public API inventory. | Zero-duration unit attacks fail, positive durations preserve their exact millisecond value, the guide parity and external compile proofs pass, test support compiles with physical-residency fixtures, and the repository boundary check reports valid Road 1 Cargo topology. |

## Phase 10 finding history

| Finding | Severity | Affected guarantees | Defect | Root correction | Closure evidence |
| --- | --- | --- | --- | --- | --- |
| C7-P10-F01 | Critical | C7-COURTROOM-01 C7-LEDGER-02 | The bounded closeout corpus stopped at its historical C6 endpoint, so C7-specific source, scheduling, reopen, timing, and cleanup regressions could escape a report still described as complete. | Extended the append-only C7 catalog through every discovered C7 defect, added exact contiguous-range and scope-membership gates, and made the bounded report derive its required set from that catalog. | Catalog topology tests prove the complete ordered C7 range through case 128. Final source-bound schema v5 contains that exact contiguous 87-case set, and canonical plus all sixteen lane reports embed the same complete manifest. |
| C7-P10-F02 | Critical | C7-PERFORMANCE-01 C7-COURTROOM-01 | Causal work evidence allocated a workload-sized collection eagerly, so a proof intended to expose bounded operation counts could itself scale with the workload. | Replaced eager collection construction with sparse carried causal evidence and preserved exact identity and count reconciliation at the consumer boundary. | Focused allocation and work-reconciliation tests reject whole-workload materialization. Final canonical and sixteen-lane evidence reconcile the sparse carried counters with exact observed work at all eight reliability points. |
| C7-P10-F03 | Critical | C7-WAL-05 C7-HANDOFF-01 C7-COURTROOM-01 | Fresh reopen treated an empty logical cutoff as inspection-required even when the retained inventory still proved the canonical WAL origin, confusing an intact empty prefix with lost physical history. | Made reopen classification consume the retained canonical-origin fact and require inspection only when both the logical cutoff and canonical origin are absent. | The two-rotation reopen journey and the canonical-origin controlled case reject the old classification. |
| C7-P10-F04 | High | C7-PERFORMANCE-01 C7-COURTROOM-01 | The performance oracle accepted a workload whose seed checkpoint never started, allowing a nominally bounded checkpoint claim to contain no checkpoint work. | Required exactly one started seed checkpoint and added a controlled case that changes the condition back to a vacuous upper-bound check. | Focused performance-oracle tests reject zero started checkpoints and accept the exact seeded workload. |
| C7-P10-F05 | Critical | C7-PERFORMANCE-01 C7-COURTROOM-01 | The closeout configuration did not carry the admitted checkpoint-memory bound through every fresh process, and the prior report schema could not prove that the selected bound reached the serving runtime. | Added the bounded checkpoint-memory field to configuration schema v3, required independent decoding, and propagated the admitted nonzero bound into producer, serving, reopen, and evidence. | Configuration propagation and bounded-memory controlled cases pass. Final canonical and sixteen-lane multi-process evidence independently decode and retain the exact admitted nonzero bound through producer serving observer and reopen roles. |
| C7-P10-F06 | Critical | C7-COURTROOM-01 | Two WAL termination points selected media operation roles that did not match the real positioned-write and file-state synchronization operations, so they could stop at an incidental operation with the right ordinal. | Bound each named point to its exact media role, relative occurrence, and pause directive. | Focused point-selection tests and role-substitution controlled cases reach the intended operations and reject mismatches. |
| C7-P10-F07 | High | C7-COURTROOM-01 | The data-write termination point selected an absolute positioned-write occurrence even though earlier lawful writes vary by scenario, making the point unstable under legitimate prefix changes. | Defined the selection relative to the sealed checkpoint marker and carried that relative match into child configuration and verification. | The exact second-write test and its controlled relative-selection case fail at the named predicate and restore source byte-identically. |
| C7-P10-F08 | Critical | C7-COURTROOM-01 C7-LEDGER-02 | Checkpoint-marker verification trusted the marker label while omitting its media role or selected relative occurrence, so a stale or substituted marker could certify the wrong physical boundary. | Extracted one checkpoint verifier that requires exact label, role, relative match, and ordering and rejects surplus or substituted detail. | Both marker-substitution controlled cases and focused negative tests fail at their exact predicates. |
| C7-P10-F09 | High | C7-COURTROOM-01 C7-CLEANUP-03 | The independent offline observer still admitted bounded-residency configuration schema v2 after the writer had moved to v3, allowing stale process input to masquerade as current evidence. | Updated the independent consumer to require schema v3 and its complete current field set without sharing the writer decoder. | Independent configuration tests accept the current profile and the stale-schema controlled case is rejected. |
| C7-P10-F10 | Critical | C7-PERFORMANCE-01 C7-COURTROOM-01 | Fresh reopen discarded the configuration-derived bounded durability policy and silently reconstructed the standard policy, invalidating cross-process memory and scheduling claims. | Carried the admitted bounded policy through the reopen configuration path and required the reopened runtime to retain it exactly. | The reopen-policy journey and controlled substitution case reject standard-policy fallback. |
| C7-P10-F11 | High | C7-COURTROOM-01 | The offline protocol encoded an empty byte prefix as an empty token, making absence indistinguishable from a missing field in line-oriented evidence. | Assigned the explicit `-` token to an empty prefix and kept nonempty prefixes hexadecimal. | The independent observer unit and controlled ambiguity case prove the exact protocol distinction. |
| C7-P10-F12 | Critical | C7-IDEMPOTENCY-03 C7-HANDOFF-01 C7-COURTROOM-01 | Reopen rejected an exactly repeated WAL binding even though it is the one lawful overlap needed to reconstruct an already-bound attempt idempotently. | Accepted only a new binding or the exact typed already-bound outcome and retained rejection for every conflicting overlap. | Bootstrap tests and the exact-overlap controlled case distinguish lawful repetition from substitution. |
| C7-P10-F13 | Critical | C7-PERFORMANCE-01 C7-COURTROOM-01 | The seed checkpoint completion counter was advanced outside the serving process, so the report could claim serving-owned checkpoint work that the serving runtime never completed. | Moved seed-checkpoint completion into the serving owner and made evidence reconcile the serving process's exact counter. | Focused process-owner and controlled omission tests prove the serving process performs and reports the checkpoint. |
| C7-P10-F14 | Critical | C7-HANDOFF-01 C7-COURTROOM-01 | Post-interruption recovery residue was treated uniformly, even though only points before physical settlement may retain one unresolved obligation and later points must retain none. | Derived exact expected residue from each point's unsettled-media posture and required the fresh observer and reopener to agree. | Separate unsettled and settled point tests reject zero-or-one shortcuts. Final canonical and sixteen-lane evidence carry the exact point-dependent residue truth, with fresh observer and reopener agreement at every point. |
| C7-P10-F15 | High | C7-COURTROOM-01 | Process roles were unique only within one case, so evidence from different termination points could reuse the same role identity and satisfy local checks while cross-case provenance collided. | Included the termination-point identity in every case role and bound all forty runtime records through dedicated campaign accounting that requires five exact roles per case and unique qualified roles across the campaign. | The topology and role-collapse controls reject label collision; cases 115 and 116 reject omission of actual runtime evidence. Final canonical schema v15 contains exactly 40 records five per point with globally unique qualified roles, while all sixteen lanes contain 80 corresponding records. |
| C7-P10-F16 | High | C7-PERFORMANCE-01 C7-COURTROOM-01 | Source binding measured only wall time and omitted exact source-file and byte workload, while its aggregate wall gate could be satisfied by a different source closure or hidden workload shrink. | Bound source timing to exact file and byte counters, retained a secondary cold wall guard, and added scale and headroom controls. | Focused source-binding timing tests and the cold-headroom controlled case pass. Final canonical and all sixteen lane reports bind the same exact source digest file count byte count and timing posture without workload shrink. |
| C7-P10-F17 | High | C7-CLEANUP-03 C7-COURTROOM-01 | Dirty-close truth was independently executed and asserted by two courtroom owners, creating duplicate proof lanes and inconsistent future insertion points. | Kept the semantic oracle owner, removed the duplicate execution path, and routed the one canonical evidence projection through it. | Focused oracle and reporting tests pass, and scoped source inspection finds one execution owner. |
| C7-P10-F18 | Critical | C7-PERFORMANCE-01 C7-COURTROOM-01 | The courtroom reported one aggregate elapsed duration and applied a flat per-case multiplier, so a single over-budget process stage could hide inside faster cases and the report could not localize cost. | Added a closed seven-stage per-case timing type covering world construction, seed producer, baseline observer, serving writer, post-interruption observer, fresh reopener, and evidence binding; each case binds its own total and the named 180-second secondary hang guard. | Timing schema v15, stage-sum, omission, substitution, headroom, and localization tests pass. Final canonical evidence binds all seven stages at all eight points with a 60,901 ms maximum; all sixteen schedule lanes pass with a 59,688 ms maximum. |
| C7-P10-F19 | Critical | C7-PERFORMANCE-01 C7-COURTROOM-01 C7-LEDGER-02 | One 180-second execution ceiling mixed ordinary tests with cases that compile and execute a fresh release binary, causing valid nested work to time out without reporting its actual cost. | Added an exhaustive target-to-cost-class mapping, distinct 180-second ordinary and 300-second nested-executable cold limits, and required class, elapsed milliseconds, and exact class-derived budget in report schema v5. | Warning-denied campaign compilation and cost-class controls pass. Final schema v5 contains all 87 exact cases classified as 77 ordinary and 10 nested-executable-cold, completes in 2,244,658 ms, and records a 200,949 ms maximum with exact class-derived budgets and no breach. |
| C7-P10-F20 | Medium | C7-CLEANUP-03 C7-LEDGER-02 | Two cutover scans used loops that returned on their first match, obscuring their first-match responsibility and failing warning-denied composition review. | Replaced both loops with explicit first-match selection while preserving the exact forbidden-path diagnostics. | Focused cutover tests and warning-denied Store runner Clippy pass. |
| C7-P10-F21 | High | C7-PERFORMANCE-01 C7-CLEANUP-03 C7-COURTROOM-01 C7-LEDGER-02 | Dirty-function scrutiny found fifty-five advisory candidates, including positional cost and shutdown construction, mixed argument parsing, an eight-input oracle, seven-input campaign entry, and broad reconciliation routines. Treating the scan as informational would leave field-order and responsibility mistakes dependent on reviewer attention. | Added named campaign, rerun, oracle, media-signature, cost, and shutdown construction; split token collection from typed process-mode construction; separated dirty and work-reconciliation checks by exact responsibility; divided the campaign into prepared and executed states; split the compound parser test; and separated special-product dispatch from ordinary planned-product execution. Retained only reviewed linear scenarios, exact schema projections, and typed authority joins whose visible sequence is their one responsibility. | Final dirty-file scrutiny covers 32 Rust files, reviews all six advisory candidates, and reports zero scan errors. Store runner and affected-package warning-denied Clippy pass; the Store-scoped cap covers all 5,320 Rust files with zero overages. |
| C7-P10-F22 | High | C7-PERFORMANCE-01 C7-COURTROOM-01 C7-LEDGER-02 | Source applicability was checked only immediately before each controlled case, so a stale late-catalog seam could invalidate more than thirty minutes of preceding execution. The complete run reached case 102 before discovering two checkpoint field renames. | Added an explicit read-only `--preflight` mode and made every executing campaign validate its complete selected set before report-session creation or any source write. Preflight and execution share one replacement-preparation function. Corrected cases 102 and 103 to the current typed checkpoint fields. | Five focused preflight tests and warning-denied runner compilation pass; all 87 current bindings validate before execution; corrected cases 102 and 103 reach their exact intended observations and restore identical source bytes; the final 87-case source-bound report completes without a late applicability failure. |
| C7-P10-F23 | High | C7-CLEANUP-03 C7-LEDGER-02 | The final removal inventory had unclassified Phase 10 files and stale tracked-family counts after semantic splits, so it no longer accounted exactly for the current Store topology. | Added semantic rows for reliability-seed checkpoint completion, checkpoint-signature verification, reliability-case tests, campaign process accounting, interrupted WAL-segment cleanup, exact-successor first-frame cleanup, and their controlled-case catalog owners; rebound all changed family counts; and narrowed the producer row from checkpoint publication to seed-workload ownership after checkpoint completion moved to its own file. | All seven removal-ledger discovery, semantic-classification, path-fate, and negative-control tests pass against the current tree. |
| C7-P10-F24 | Critical | C7-COURTROOM-01 C7-LEDGER-02 | The eight-point campaign constructed forty runtime process records but never bound them to campaign accounting and never serialized them. A test over forty distinct static labels therefore allowed actual process provenance to disappear while the ledger claimed global uniqueness. | Added a dedicated campaign accounting constructor that requires exact per-case cardinality, exact point-qualified roles, unique identities within each case, and unique roles across the campaign; added the same records to the point report under schema v15. | Four focused accounting tests and the projection control pass; cases 115 and 116 reach their exact omission predicates. Final canonical schema v15 serializes exactly 40 point-process records and every schedule lane serializes its five exact records. |
| C7-P10-F25 | Critical | C7-COURTROOM-01 C7-LEDGER-02 | The first process-accounting correction joined eight sequential cases into an authority designed for one simultaneous process set. A lawful operating-system reuse of a process identifier across non-overlapping cases then denied the canonical run after all eight points had executed. | Kept the ordinary simultaneous run under `PhysicalWorkExecutionContext` and introduced lifetime-aware campaign accounting: process identifiers must be unique within a case, qualified roles must be unique across the campaign, and identifier reuse between sequential cases is accepted. | The focused constructor proofs establish exact cardinality role shape within-case identity uniqueness and lawful cross-case identifier reuse. Final canonical schema v15 passes with 40 records and all sixteen sequential schedule lanes pass with 80 total records. |
| C7-P10-F26 | Critical | C7-COURTROOM-01 C7-LEDGER-02 | The first accounting correction also left an extension call that copied all forty sequential-case records into the ordinary four-process observation set. The ordinary oracle checked only its first four positions, so unique extra identities could be accepted and lawful identifier reuse was what finally exposed the scope collapse. | Deleted the observation-extension API and call, kept campaign records solely under the dedicated campaign authority, and made the ordinary oracle require exactly four records before checking identity uniqueness and order. Added controlled case 117 for this exact scope extension. | The exact-cardinality regression and case 117 pass; all 87 source bindings preflight; canonical and sixteen-lane reports retain campaign records only under dedicated campaign accounting while ordinary observation remains exact-cardinality. |
| C7-P10-F27 | Critical | C7-WAL-05 C7-CLEANUP-03 C7-COURTROOM-01 | CI lane 8 paused the first positioned write after the filesystem had created the next WAL artifact but before any byte effect. The resulting canonical zero-byte trailing segment was rejected as hostile on fresh reopen, even though the same point passed under a different schedule. | Classified only one exact next-segment, same-generation, trailing empty artifact as interrupted create-before-write residue; validated the complete retained nonempty tail and checkpoint cutoff first, then removed that artifact durably before runtime admission. Lone, internal, wrong-generation, and noncontiguous empty segments remain failures and are never normalized. Added controlled case 118. | Positive and negative cleanup journeys pass; case 118 reaches its exact predicate in 19,242 ms; all 87 bindings pass. Final lane 8 with seed 2312892726080252336 passes with a 58,366 ms case total, and the complete canonical plus sixteen-lane evidence set validates exactly. |
| C7-P10-F28 | Critical | C7-WAL-05 C7-CLEANUP-03 C7-COURTROOM-01 | CI lane 1 stopped during the prefix of a new WAL frame. Fresh reopen classified the incomplete final frame as hostile even though every preceding frame was complete and the uncommitted suffix was confined to the active final segment. | Added WAL-issued `VerifiedWalActiveTail` and `InterruptedWalTail` proof types; only the active final segment may retain an incomplete suffix after at least one fully verified frame. Store validates retained topology and checkpoint bounds before applying backend-owned durable truncation. Internal damage partial first frames malformed complete headers and complete digest failures remain rejections without cleanup. Added controlled case 119. | All twelve WAL tests and seven WAL-reopen journeys pass; case 119 reaches its exact predicate in 19,190 ms and all 87 bindings pass. Final lane 1 with seed 14382456006366398454 passes with a 60,555 ms case total, and the complete canonical plus sixteen-lane evidence set validates exactly. |
| C7-P10-F29 | High | C7-CLEANUP-03 C7-LEDGER-02 | Holistic line-cap QA found the dirty bounded-residency oracle at 403 lines because evidence storage and accessors remained mixed into verification after Phase 10 growth. | Extracted `oracle/evidence.rs` as the semantic evidence owner with enclosing-module-only visibility while leaving truth verification in `oracle.rs`; also split active-tail frame classification from segment accumulation so both new functions remain within the function advisory. | All 53 focused oracle tests pass; `oracle.rs` is 326 lines and `oracle/evidence.rs` is 121; active-tail inspection and per-frame classification remain within advisories; the Store scan reports 5,320 Rust files and zero files above 400 lines; warning-denied affected-package Clippy passes. |
| C7-P10-F30 | High | C7-AUTHORITY-01 C7-API-01 C7-CLEANUP-01 C7-LEDGER-01 C7-LEDGER-02 | Late Phase 10 additions changed the API and removal inventories and expanded the guarantee table, but the current evidence retained earlier prose counts while every row remained `PROVED`. Source identity and all-row status checks could therefore certify numerically stale current evidence. | Exposed typed read-only accounting from the existing API removal and authority-trace owners; added stable machine-readable clauses to the four affected rows; and made living plus final ledger closure compare those clauses to freshly derived owner facts without duplicating any CSV parser. Added controlled case 120. | The honest accounting projection and fourteen independent fact-drift controls pass; case 120 reaches the exact stale-accounting predicate; the append-only corpus preflight resolves all 87 current bindings. |
| C7-P10-F31 | Critical | C7-WAL-05 C7-LEDGER-01 C7-LEDGER-02 | The load-bearing WAL lifecycle gate compacted raw source text and brace-counted function bodies, so comments could counterfeit required statements and delimiters inside literals could corrupt ownership extraction. The committed gate was also already red because it still required the deleted direct segment verifier after reopen delegated active-tail inspection. | Replaced raw-text compaction with a test-only Rust AST projection that ignores comments and literals, resolves real functions, and checks semantic calls assignments variants and order without pinning local-variable phrasing. Retargeted reopen evidence to `interrupted_active_tail::inspect`; canonical-origin internals remain proved by their production unit test rather than duplicated source spelling. Added controlled cases 121 and 122. | Four focused lifecycle controls and all 18 WAL-scoped contract checks pass; comments cannot supply bounded listing, delimiter literals cannot move function ownership, the stale reopen owner is rejected, and cases 121 and 122 reach their exact predicates. WAL inventory, seven reopen journeys, rotation, continuation, and the full non-ledger runner suite pass; the regenerated 18-report source-bound set validates with zero issues. |
| C7-P10-F32 | Critical | C7-WAL-05 C7-LEDGER-02 | The first AST projection recorded an outer method before evaluating its receiver, so ordered lifecycle checks encoded syntactic preorder rather than Rust evaluation order. Phase 6 source identity also bound only the parent lifecycle file, omitting the split proof modules and their AST owner. | Traversal now visits receivers and arguments before recording their call, with a direct nested-call order control. The Phase 6 WAL closure now binds `wal_source_syntax.rs` and the complete `wal_segment_lifecycle` proof tree. Added controlled cases 123 and 124. | All 18 WAL-scoped contract and 27 non-Phase-10 closure checks pass; cases 123 and 124 reach the exact inverted-order and omitted-proof-source predicates; the complete bounded corpus preflight resolves 87 exact bindings. |
| C7-P10-F33 | High | C7-PERFORMANCE-01 C7-COURTROOM-01 C7-LEDGER-02 | Canonical execution exceeded the 120-second secondary wall threshold twice at 122,167 ms and 123,641 ms after all semantic preflights passed. The implementation had promoted a narrow environment guard into a portable acceptance threshold, and its denial discarded the seven stage timings required to localize the cost. | Exact per-stage and total timing remains report evidence; the secondary threshold is now an explicitly named 180-second hang guard aligned with the existing ordinary execution ceiling. Any breach reports the slowest stage and all seven stage durations. Updated case 113 and added controlled cases 125 and 126. | Six feature-gated timing tests pass; cases 113, 125, and 126 reach the exact gross-headroom, observed-headroom, and missing-localization predicates in the final 87-case corpus. Canonical maximum case time is 79,874 ms and the sixteen-lane maximum is 114,254 ms; every report retains all seven stages and the complete 18-report set validates exactly. |
| C7-P10-F34 | Critical | C7-WAL-05 C7-CLEANUP-03 C7-COURTROOM-01 C7-LEDGER-02 | Schedule lane 1 with seed 10679339076921371489 and checkpoint-before-target stopped after a strict prefix of the first frame in a newly rotated active successor. Fresh reopen reproducibly returned `SegmentInspection(InvalidFrame)`. The earlier interrupted-tail repair required one complete frame in the same segment, so Store never joined the verified predecessor topology that made this final successor residue classifiable. | Kept zero-complete-frame bytes outside `VerifiedWalSegment`. WAL now issues the distinct `InterruptedWalSegmentStart` proof only for a nonempty strict incomplete first frame. Store holds it as a non-removing candidate and grants removal authority only after `admit_after` proves active exact-next same-generation succession from a fully verified retained segment. Added controlled case 127. | All twelve WAL crate tests and seven WAL-reopen journeys pass, including exact successor cleanup and no-predecessor preservation. Lone internal noncontiguous wrong-generation malformed-header and complete-digest damage remain denials. Case 127 reaches its exact predicate in 16,830 ms; final lane 1 with seed 14382456006366398454 passes with a 60,555 ms case total; all 87 bindings and the complete 18-report set validate exactly. |
| C7-P10-F35 | Critical | C7-GROUP-01 C7-LEDGER-02 C7-CLEANUP-03 | The pushed C7 source consumes `NonEmpty::map`, but that Worth Proof operation exists only in an unrelated unstaged workspace change. The dirty tree compiles while a clean checkout of the published commit cannot, so the prior source-bound closure was not a reproducible publication proof. | Published the narrowly owned `NonEmpty::map` operation with its foundation test in commit `d20c3bcc6`, then regenerated C7 certification only after the committed source and all cheap preflights were stable. | The committed Rust tree compiles with `NonEmpty::map` present in Worth Proof; warning-denied runner compilation, the complete non-final runner suite, and all 18 regenerated reports bind the same published source. |
| C7-P10-F36 | High | C7-WAL-05 C7-HANDOFF-01 C7-LEDGER-02 | Ledger accounting checked only the total authority-lane count and described the C8 handoff as seventeen steps, silently substituting the separate seventeen-step WAL inventory reopen lane for the actual nine-step handoff. | Derived typed handoff and WAL-reopen counts from their named authoritative step definitions, bound each fact to its own guarantee, corrected the handoff language, and added controlled case 128 for direct count substitution. | Focused accounting tests distinguish nine C8 handoff steps from seventeen WAL-reopen steps; all fourteen independent accounting-fact drifts fail; case 128 reaches `phase-ten-handoff-step-accounting-substitution-accepted` in 5,560 ms; all 87 bindings and the complete 18-report set validate exactly. |
| C7-P10-F37 | Critical | C7-WAL-05 C7-LEDGER-01 C7-LEDGER-02 | The AST drift alarm ignored calls hidden in macro token streams and allowed required steps in never-invoked closures or statically dead branches. A live forbidden call hidden by a macro plus dead required syntax therefore passed the exact gate whose evidence claimed counterfeit resistance. | Split positive proof from prohibition observation: deferred, nested, and statically unreachable syntax cannot supply positive proof, while every ordinary AST step and every identifier in recursively nested macro tokens remains visible to prohibitions. Bound the honest parser closure specifically to a top-level `map(...).collect()` initializer instead of globally trusting closure bodies. Split traversal into its own semantic-projection owner and bound the complete projection tree into the Phase 6 source closure. Added cases 129 through 131. | Eight focused projection controls and five lifecycle controls pass, including the combined attack, deferred-prohibition visibility, nested items, and a trivially unreachable match arm. Cases 129 through 131 die at their exact macro, closure, and dead-branch predicates; all 92 catalog bindings preflight. |
| C7-P10-F38 | Medium | C7-WAL-05 C7-LEDGER-02 | Function ownership discovery treated an exact `#[cfg(test)]` helper as a competing production owner, so a harmless same-named test helper could break the gate while production ownership remained singular. | Excluded exact test-only functions, methods, impls, and inline modules from production-owner discovery without ignoring mixed feature configurations. Added controlled case 132. | The focused owner-collision control passes; case 132 reintroduces the collision and dies at `wal-source-cfg-test-owner-collision`; the honest production lifecycle gate remains green. |
| C7-P10-F39 | High | C7-API-01 C7-WAL-03 C7-FAILURE-01 C7-LEDGER-02 | `WalAppendPlanner` mapped a poisoned in-memory mutex to `WalArtifactStoreDenial::Io`, falsely telling callers that media failed and giving the wrong recovery direction. | Introduced the planner-scoped `WalAppendPlannerDenial` with distinct `Artifact` and `StatePoisoned` variants; artifact causes remain exact, while poisoned state directs callers to discard and reopen the planner. The broader artifact-store APIs do not advertise the impossible planner-only failure. Added controlled case 133 and locked the new public surface. | All four planner tests pass, including exact checksum propagation and the real poisoned-lock control. Case 133 dies at `wal-planner-poison-misclassified`; the public API inventory reconciles 312 surfaces and the Cargo/removal inventories reconcile exactly. |
| C7-P10-F40 | Medium | C7-COURTROOM-01 C7-LEDGER-02 | The first case 132 control failed for the intended owner collision but used an unmarked `expect` panic, so the mutation runner correctly refused to credit the failure as causal evidence. | Replaced the generic panic with the exact registered mutation predicate and retained the runner's strict single-predicate requirement. | The original campaign attempt is preserved in audit history as rejected evidence; rerun case 132 emits exactly `wal-source-cfg-test-owner-collision`, and cases 129 through 133 all localize to one exact predicate. |
| C7-P10-F41 | High | C7-AUTHORITY-01 C7-AUTHORITY-02 C7-CLEANUP-01 C7-IDEMPOTENCY-01 C7-IDEMPOTENCY-02 C7-SIGNAL-01 C7-FOUNDATIONAL-01 C7-WAL-01 C7-WAL-02 C7-WAL-03 C7-WAL-04 C7-BARRIER-01 C7-DATA-01 C7-DATA-02 C7-DATA-03 C7-DATA-04 C7-FAILURE-01 C7-GROUP-01 C7-WAL-05 C7-CHECKPOINT-01 C7-CHECKPOINT-02 C7-CHECKPOINT-03 C7-RETENTION-01 C7-IDEMPOTENCY-03 C7-RETENTION-02 C7-ROOT-01 C7-ROOT-02 C7-ROOT-03 C7-ROOT-04 C7-ROOT-05 C7-ROOT-06 C7-ROOT-07 C7-ROOT-08 C7-API-02 C7-EVIDENCE-01 C7-LIFECYCLE-01 C7-OBSERVATION-01 C7-SETTLEMENT-01 C7-SETTLEMENT-02 C7-SIGNAL-02 C7-GRAPH-02 C7-CLEANUP-02 C7-DOCUMENTATION-01 C7-PERFORMANCE-01 C7-CLEANUP-03 C7-COURTROOM-01 C7-HANDOFF-01 C7-LEDGER-01 C7-LEDGER-02 | C8 Phase 3 extended Store WAL format backend and runtime sources inside completed C7 causal closures while the C7 ledger identities and tracked-consumer inventory remained frozen; Windows newline-sensitive controlled mutants and substring constructor counting also made the wider runner fail for the wrong reasons. | Reconciled every new C8 durability consumer with a specific C7 disposition and owner normalized controlled source mutations before replacement distinguished ordinary scheduler construction from recovery-only construction and rebound every affected C7 phase closure only after its semantic gate remained green. | The complete warnings-denied store-test-runner lane proves current C7 and C8 ledgers inventories mutation controls exact scheduler owners and all source identities against one dirty snapshot. |
| C7-P10-F42 | High | C7-AUTHORITY-01 C7-CLEANUP-03 C7-LEDGER-01 C7-LEDGER-02 | The runtime-owner uniqueness gate counted raw constructor substrings inside only the Store physical runtime, inferred test posture from filenames, and ignored generated or nested includes, cfg-selected and inline-module paths, Cargo target-root identity, path-overridden module bases, and procedural-macro expansion, so legal tokenization, aliases, macros, projections, tests-named production modules, generated Rust, literal expression includes, inline or cfg-path decoys, custom-root or nested-path module decoys, split-string procedural macros, and construction from another dependent crate could hide parallel authorities. | Replaced text counting with a Cargo-target and module-declaration reachability graph plus parsed call-expression evidence; carry distinct conventional-module, direct-path, and include bases through root, external, inline, and included sources; preserve Cargo target identity; remove the sole generated Rust include; traverse literal includes at every syntax position; fail closed on computed includes, cfg-dependent module paths, and every non-lockfile-reviewed procedural-macro package; reject governed aliases, indirect or unresolved qualified calls, macro-carried owners, and macros inside governed impls; exclude only structurally test-only syntax; and scan every reachable Worth Store product source. | Focused warnings-denied controls admit each exact owner and reject ordinary, comment-separated, raw, imported-alias, out-of-runtime, macro, projection, unguarded tests-named module, generated-include, nested literal-include, cfg-path-decoy, custom lib/bin target-root-decoy, nested path-base-decoy, mod-rs/non-mod-rs/include-within-inline decoys, and a real split-string local proc-macro attack while exact cfg-test twins and six exact lockfile-bound reviewed proc macros stay admitted; the lockfile, syntax, reachability, and causal test owners are bound into C7 source identity. |
| C7-P10-F43 | High | C7-AUTHORITY-01 C7-CLEANUP-03 C7-LEDGER-01 C7-LEDGER-02 | The first syntax-aware source graph still inherited module bases across subdirectory includes, inspected only the first `cfg_attr` layer, skipped external module items nested in blocks, and admitted local dependency-owned or build-generated declarative and aliased macros capable of emitting module declarations or Rust includes, so actual-vs-decoy files could keep a sixth runtime owner outside the uniqueness scan. | Included fragments now establish conventional-module and direct-path bases from their own source directory; `cfg_attr` path detection recurses through nested `cfg_attr`; one context-aware visitor discovers external modules at every reachable item position; qualified include paths resolve by their exact terminal macro identity; include import aliases fail closed; and production macros carrying a module or include expansion fail closed before expansion. Cargo metadata additionally scans ordinary dependency macro sources with a visited path graph, recursively resolves every literal include relative to its containing source regardless of extension or package root, rejects computed or generated external includes and unreviewed source expansion, and admits only thirteen exact lockfile-pinned declarative macro packages plus six reviewed procedural macro packages. Macro provenance and compiled causal fixtures have separate bounded owners. | Nine focused graph controls include real Cargo-compiled root, external, inline, function-local, macro-generated module, macro-generated include, qualified include, local aliased include, external path-dependency aliased-include, build-script-generated alias, arbitrary-extension literal include, and outside-root literal include fixtures; actual include sources are discovered while same-named decoys are excluded, computed and nested path redirection plus unproved dependency expansions are rejected, cfg-test controls remain excluded, and the real production workspace retains exactly one approved constructor per governed authority. |
| C7-P10-F44 | High | C7-AUTHORITY-01 C7-CLEANUP-03 C7-LEDGER-01 C7-LEDGER-02 | Constructor alias inspection discarded `UseTree::Path` prefixes, so a legal grouped self alias such as `PhysicalResidencyPoolOwner::{self as PoolOwner}` hid the governed owner and allowed `PoolOwner::open` to escape both rejection and constructor counting. | The alias walker now carries the complete nested use prefix; terminal `self as Alias` resolves to the containing prefix leaf and fails closed when that canonical leaf is a governed constructor owner. | Direct grouped, crate-qualified, and nested-group hostile twins all die at the owner-alias predicate, while an unrelated module self alias remains admitted and the real workspace retains one exact constructor site. |
| C7-P10-F45 | High | C7-AUTHORITY-01 C7-CLEANUP-03 C7-LEDGER-01 C7-LEDGER-02 | Rust raw identifiers preserve an `r#` spelling in syntax tokens while resolving to the same semantic symbol, so raw constructor owners, methods, aliases, type paths, macro tokens, and include provenance escaped exact string comparisons. | Added one semantic identifier projection that strips only the syntactic raw prefix and applied it across constructor calls, use-tree aliases, type paths, macro tokens, module names, attributes, include recognition, and external declarative-macro provenance. | Compiled raw-owner, raw-method, raw-literal-include, and raw-include-alias worlds remain visible; raw macro-carried owner, type-alias, and imported-alias attacks fail closed; unrelated raw identifiers remain admitted. |
| C7-P10-F46 | Medium | C7-AUTHORITY-01 C7-CLEANUP-03 C7-LEDGER-01 C7-LEDGER-02 | The first raw-identifier correction also normalized grammar-keyword recognition, so the ordinary identifier `r#mod` inside a macro was misclassified as a generated module and a raw-named macro could be confused with the `macro_rules` definition form. | Restricted semantic unraw projection to Rust symbols; source-expansion keyword recognition now preserves exact token spelling for `mod` and exact path spelling for `macro_rules`. | A real Cargo package compiles and passes source discovery with `r#mod` data plus an invoked raw-named macro; the external declarative-macro scanner admits the same two controls while continuing to reject semantic raw include aliases. |
| C7-P10-F47 | High | C7-PERFORMANCE-01 C7-CLEANUP-03 C7-COURTROOM-01 C7-HANDOFF-01 C7-LEDGER-02 | A corrected C8 capacity-proof mutant changed a governed file inside the complete Phase 10 crates closure while all five shared C7 rows retained the prior identity, leaving the constitutional C7 boundary gate red at 105 of 106. | Reopened the complete-source guarantee set, retained the independent reviewer finding, and rebound all five rows only after the current semantic boundary remained otherwise green. | The exact warnings-denied C7 boundary passes 106 of 106 against the current complete Phase 10 source identity, while its stale-identity control still rejects the prior digest. |
| C7-P10-F48 | High | C7-AUTHORITY-01 C7-AUTHORITY-02 C7-API-01 C7-API-02 C7-EVIDENCE-01 C7-DESTINATION-01 C7-GRAPH-01 C7-GRAPH-02 C7-LEDGER-01 C7-LEDGER-02 C7-MECHANISM-01 C7-VOCABULARY-01 C7-IDEMPOTENCY-01 C7-IDEMPOTENCY-02 C7-WAL-01 C7-WAL-02 C7-WAL-03 C7-WAL-04 C7-BARRIER-01 C7-DATA-01 C7-DATA-02 C7-DATA-03 C7-DATA-04 C7-GROUP-01 C7-WAL-05 C7-CHECKPOINT-01 C7-CHECKPOINT-02 C7-CHECKPOINT-03 C7-RETENTION-01 C7-IDEMPOTENCY-03 C7-RETENTION-02 C7-ROOT-01 C7-ROOT-02 C7-ROOT-03 C7-ROOT-04 C7-ROOT-05 C7-ROOT-06 C7-ROOT-07 C7-ROOT-08 C7-FAILURE-01 C7-LIFECYCLE-01 C7-OBSERVATION-01 C7-SETTLEMENT-01 C7-SETTLEMENT-02 C7-SIGNAL-01 C7-SIGNAL-02 C7-FOUNDATIONAL-01 C7-PERFORMANCE-01 C7-CLEANUP-01 C7-CLEANUP-02 C7-CLEANUP-03 C7-DOCUMENTATION-01 C7-COURTROOM-01 C7-HANDOFF-01 | C8 Phase 7 added checkpoint-covered WAL classification and Store-governed cleanup planning freshness removal effects and fresh-process proofs inside the completed C7 source closures while the C7 consumer inventory and all inherited source identities remained frozen. | Classified every new C8 cleanup consumer under one specific C7 responsibility updated only the exact changed family counts retained the expanded current accounting and rebound every affected C7 phase closure after its semantic inventory stayed green. | The warnings-denied C7 inventory current-accounting and complete 106-test boundary pass against the same source graph as the C8 Phase 7 ledger and facade gates. |
