# WORTH Store C.7 Living Requirement-And-Evidence Ledger

This is the single living C.7 closure ledger. It follows the current source,
not historical test results. Corrections retain finding history and reopen
every causally affected row.

Status vocabulary is `OPEN`, `PROVED`, `DEFECT`, `BLOCKED`, or justified
`N/A`. Phase 1 establishes the complete causal model; later phases replace
`pending` evidence only after their production paths exist.

| ID | Phase | Guarantee | Evidence required | Current evidence | Source identity | Status |
| --- | --- | --- | --- | --- | --- | --- |
| C7-AUTHORITY-01 | 1 | Every durability mechanism and current consumer has one bidirectional trace and final disposition. | Fresh source discovery equals authority trace and removal ledger. | 1589 live consumers with exact semantic-family counts reconcile; two Phase 3 deletions remain as historical absence records and six ordered authority lanes resolve. | P1 closure 3c317abf12c5 | PROVED |
| C7-AUTHORITY-02 | 2 | Only a sealed C4-derived basis can admit the exact nonoptional Store durability owner carried through request binding serving construction failure shutdown and read-only observation. | Compile attacks basis-substitution and zero-effect rebind journeys plus source lifecycle mutants. | Five compile attacks reject incomplete raw duplicated omitted and reconstructible authority; foreign-policy rebind leaves target media counters unchanged; policy identity limits and basis are observable; four lifecycle mutants fail. | P2 authority 566a78bfbc70 | PROVED |
| C7-API-01 | 1 | Every current public durability-facing surface has one locked destination and cutover phase. | Public export inventory resolves through its public facade to current source anchors. | Eighty locked surfaces cover the inherited boundary plus every Phase 2 and Phase 3 addition; exact-set declaration-anchor and re-export-chain checks reject omissions extras drift and hidden declarations. | P1 closure 3c317abf12c5 | PROVED |
| C7-API-02 | 8 | Ordinary mutation callers observe idempotency cancellation and typed physical fate without a stronger-looking compatibility wrapper. | Downstream compile journey and old-facade absence gate. | pending | pending | OPEN |
| C7-DESTINATION-01 | 1 | The authority ledger and future topology name one Store cross-domain owner without empty production placeholders. | Specification topology anchors and phase-aware populated-destination gate. | Destination ownership resolves all Phase 2 through Phase 4 proof types and the populated tree contains admission runtime-binding mutation progression WAL and stable facade owners. | P1 closure 3c317abf12c5 | PROVED |
| C7-GRAPH-01 | 1 | Current Cargo and feature edges expose every durability island without importing a circular owner into Store. | Exact cargo metadata reconciliation and ordinary graph assertions. | Exact metadata reconciles the authority packages; ordinary Store reaches WAL meaning scheduler and backend owners while recovery and certification probes remain outside its graph. | P1 closure 3c317abf12c5 | PROVED |
| C7-LEDGER-01 | 1 | The living ledger covers the complete causal claim and rejects omitted families duplicate guarantees and stale proof. | Ledger parser mutants and full normative-family reconciliation. | Thirty-one guarantee rows cover twenty-two required semantic families and controlled ledger mutants fail. | P1 closure 3c317abf12c5 | PROVED |
| C7-MECHANISM-01 | 1 | Every inherited backend barrier WAL root and ordinary publication mechanism has a real terminal effect and one current ownership or explicit disconnected disposition. | Focused owner tests plus manual source trace of each terminal effect. | Focused mechanism probes remain real; the Phase 3 ownership gate proves one ordinary C4 WAL effect and one Store settlement while the displaced lower WAL lifecycle probe is certification-only. | P1 closure 3c317abf12c5 | PROVED |
| C7-VOCABULARY-01 | 1 | Production names preserve physical durability semantics and cannot introduce milestone or compatibility identities. | Production source scan and locked semantic family inventory. | Physical runtime source rejects C7-named and compatibility durability identities while broad vocabulary counts remain reconciled. | P1 closure 3c317abf12c5 | PROVED |
| C7-IDEMPOTENCY-01 | 2 | Canonical request equivalence binds the exact Store policy scope payload platform request operation family and security bases while excluding attempt deadline allocation WAL group schedule and cancellation facts. | Independent versioned golden reconstruction every-field deltas and allocation policy ordering effect and role mutants. | Independent v1 reconstruction matches the golden digest; every effect field and canonical security order changes it; allocation cannot feed back; five source mutants reject omitted policy effect ordering allocation and native-field-role drift. | P2 mutation f9fad2a723b9 | PROVED |
| C7-IDEMPOTENCY-02 | 2 | A Store-issued durable-generation lease denies silent expired or foreign key reuse while unresolved bindings remain authoritative under the admitted pending bound. | Lease issuance expiry conflict unresolved-retention foreign-identity and pending-bound tests. | Registry tests prove exact fresh duplicate conflict expired unseen and pending-bound outcomes; real preparation preserves one unresolved identity across same-fingerprint retry and rejects foreign Store runtime and policy identities before effect. | P2 mutation f9fad2a723b9 | PROVED |
| C7-WAL-01 | 3 | Ordinary WAL append uses one Store Signal scheduler executor and C4 media route. | Real append journey independent scan and direct-executor absence gate. | A real two-record mutation appends one independently verified frame through the installed WAL Signal family scheduler lane Store executor and scheduled C4 artifact effect; source ownership proves one effect owner one completion-bound Store settlement and one appended transition while default facades reject displaced executors. | P3 WAL a44d5fd1d363 | PROVED |
| C7-WAL-02 | 3 | Persisted attempt binding joins exact mutation lease member range and redo without changing request equivalence. | Independent attempt-binding inspector and substitution attacks. | A test-owned bounded decoder proves exact key lease fingerprint Store runtime operation member LSN range redo digest and independently encoded ordered redo with full consumption; every field mutation length attack reordering truncation trailing byte redo substitution and completion work range or digest substitution fails. | P3 WAL a44d5fd1d363 | PROVED |
| C7-WAL-03 | 3 | Every WAL denial before possible media effect returns the exact still-owned prepared mutation and releases every range command and capacity reservation made by that attempt. | Sealed-owner denial denied-media retry concurrent-append and one-command health-fence journeys plus linear outcome rollback and cleanup source inspection. | Reservation lifecycle submission Signal scheduler command and media pre-effect failures retain exact actionable causes and return the same prepared identity; repeated fenced admissions reuse released one-command capacity with no residual work, proven-no-effect retry succeeds, and a strict torn prefix remains indeterminate and seals allocation. | P3 WAL a44d5fd1d363 | PROVED |
| C7-WAL-04 | 3 | WAL range allocation consumes only a prepared mutation bound to the exact current Store runtime and Signal profile. | Foreign-Store and stale-runtime append attacks with zero target WAL effect and preserved continuation. | The private preparation-admission owner rejects foreign Store stale runtime mismatched Signal profile and released publication authority before allocation; target WAL observation and bytes remain unchanged and the preserved preparation remains usable only by its rightful live owner. | P3 WAL a44d5fd1d363 | PROVED |
| C7-DATA-01 | 4 | Data dispatch is constructible only from the matching durable WAL member proof. | Compile failures foreign-receipt matrix and inversion mutant. | pending | pending | OPEN |
| C7-DATA-02 | 4 | Each page carries one certified prior-page basis plus only the newly applied nonempty redo delta. | PageLSN-ahead lifetime-redo and prior-basis substitution mutants. | pending | pending | OPEN |
| C7-GROUP-01 | 5 | Group commit shares only physical cost while preserving unique mutation range fate cancellation and acknowledgment. | Four-request three-sealed-member identity-blender courtroom. | pending | pending | OPEN |
| C7-CHECKPOINT-01 | 6 | Bounded fuzzy checkpoint capture and WAL retention preserve every last authority under pressure. | Memory-tail siege fresh reopen and last-authority mutants. | pending | pending | OPEN |
| C7-ROOT-01 | 7 | One canonical root replacement reaches parent-namespace durability before acknowledgment. | Crash-seam matrix independent root observation and parallel-log absence. | pending | pending | OPEN |
| C7-FAILURE-01 | 4 | Denial before effect proves no effect while possible effect remains indeterminate and unacknowledgeable. | Boundary fault matrix and typed fate compile restrictions. | pending | pending | OPEN |
| C7-LIFECYCLE-01 | 8 | Store owns started mutation settlement through close even after caller handle drop. | Drop abandonment close drain and cancellation mutants. | pending | pending | OPEN |
| C7-SIGNAL-01 | 2 | Store installs one exact Signal dependency profile for WAL append barrier checkpoint capture and root publication without letting Signal become durability authority. | Exact-family binding journey source role mutant and authority compile failure. | The prepared mutation carries the installed four-family Signal profile; all four bind as Dependency; an Output-role mutant fails; preparation performs no WAL backend data root or acknowledgment effect. | P2 mutation f9fad2a723b9 | PROVED |
| C7-SIGNAL-02 | 8 | Caller mutation deadlines and cancellation enter the derived monotonic Signal lifecycle before effect without changing request equivalence or idempotency lease retention. | Clock deadline cancellation and lease-independence journeys plus substitution mutants. | pending | pending | OPEN |
| C7-FOUNDATIONAL-01 | 2 | Foundational consumes exact Store aspect-native durability work bases and one-way evidence while neither layer can mint platform authority or completed physical effect. | Canonical family source role lane domain construction tests plus reverse-import and substitution gates. | Aspect-native certifies the exact native request fingerprint and four durability work families; canonical source role lane domain substitutions fail; structural gates reject reverse authority and the sealed Store preparation compile attack. | P2 mutation f9fad2a723b9 | PROVED |
| C7-PERFORMANCE-01 | 10 | Group checkpoint page-basis retry and observation state remain explicitly bounded under admitted policy. | Counter-backed siege allocation evidence and scale mutants. | pending | pending | OPEN |
| C7-CLEANUP-01 | 1 | Every preserved narrowed moved replaced or deleted responsibility names its last consumer phase and mechanical absence proof. | Fresh removal discovery and schema mutants. | 1591 rows name exact counts responsibilities concrete owners dispositions last consumers phases and absence gates; 1589 are live and two are Phase 3 deletion records. | P1 closure 3c317abf12c5 | PROVED |
| C7-CLEANUP-02 | 9 | Direct runtime recovery executor parallel root log compatibility aliases and stale features are absent from the ordinary graph. | Source metadata feature and public-export absence gates. | pending | pending | OPEN |
| C7-DOCUMENTATION-01 | 9 | Public durability and owner documentation explain real API lifecycle cost fate recovery and unsupported profiles. | Documentation review against final source and executable examples. | pending | pending | OPEN |
| C7-COURTROOM-01 | 10 | Independent fresh-root processes and schedule perturbation expose unanticipated interleavings with exact replay. | Source-closure-derived seeds N-seed CI and independent artifact oracle. | pending | pending | OPEN |
| C7-HANDOFF-01 | 10 | C8 receives exact indeterminate persisted-effect and bounded WAL-tail authority without importing ordinary replay. | Successor compile boundary and handoff documentation. | pending | pending | OPEN |

## Phase 1 finding history

| Finding | Severity | Affected guarantees | Defect | Root correction | Closure evidence |
| --- | --- | --- | --- | --- | --- |
| C7-P1-F01 | High | C7-AUTHORITY-01 C7-API-01 C7-CLEANUP-01 | Selected-symbol path membership could miss renamed semantic surfaces and additional calls inside an already classified file. | Broadened discovery to durability WAL barrier root acknowledgment checkpoint pageLSN and ordinary mutation semantic families and recorded exact per-family occurrence counts. | Same-file count mutant fails and all 1485 current paths reconcile. |
| C7-P1-F02 | High | C7-AUTHORITY-01 C7-CLEANUP-01 | Broad discovery initially allowed generic owner and responsibility labels that were complete syntactically but semantically useless. | Classified preserved rows to exact crate owners and C7 cutover rows to path-sensitive named destinations; generic labels are mechanically rejected. | Ownership gate passes over every removal row. |
| C7-P1-F03 | Medium | C7-AUTHORITY-01 | Oversized patch transport corrupted an initial generated ledger row while preserving an apparently plausible partial file. | Deleted the partial artifact and rebuilt every ordered row through bounded patches before exact-set reconciliation. | Parser and exact reconciliation pass; final ledger has 1486 lines. |
| C7-P1-F04 | Low | C7-MECHANISM-01 | First ordinary publication probe omitted its Cargo-required certification feature and therefore never executed. | Reran with the target-declared `certification-test-authority` feature and retained the rejected command as history. | Thirteen ordinary publication tests pass under the declared feature authority. |
| C7-P1-F05 | Medium | C7-AUTHORITY-01 C7-MECHANISM-01 | The inherited runner still scanned the ledger-deleted `c6_handoff` tree and matched candidate receipt adjacency through indentation. | Removed only the C6-ledger-proven deleted scan root and replaced formatting-sensitive matching with structural concrete-field and field-order validation. | C6 runtime ownership and C5 writer gate families pass including an indentation mutant. |
| C7-P1-F06 | High | C7-API-01 C7-CLEANUP-01 | The first public API ledger asserted only eight representative names and therefore allowed unlisted durability entry points to survive without a disposition. | Replaced subset membership with exact equality over the twenty-four current C7 boundary surfaces including ordinary and reconstructive record entry points and the certification-only direct WAL executor. | Missing extra and duplicate surfaces now fail before later cutover work can rely on the inventory. |
| C7-P1-F07 | High | C7-LEDGER-01 | Proved rows required a non-pending source identity but did not prove that the recorded digest described the current Phase 1 source closure. | Defined the canonical Phase 1 source closure in the gate and recompute its SHA-256 identity for every proved Phase 1 row. | A wrong-digest mutant fails with the exact current identity required for closure. |

## Phase 2 finding history

| Finding | Severity | Affected guarantees | Defect | Root correction | Closure evidence |
| --- | --- | --- | --- | --- | --- |
| C7-P2-F01 | High | C7-AUTHORITY-02 | Initial durability files crossed the code-size constitution and mixed platform-basis derivation with unrelated media and profile responsibilities. | Extracted admission basis identity derivation and its focused tests into semantically named modules. | Every dirty Rust file is at or below 400 lines and the real-media identity journey passes. |
| C7-P2-F02 | High | C7-AUTHORITY-01 C7-DESTINATION-01 C7-CLEANUP-01 C7-AUTHORITY-02 | Phase 1 destination assumptions removal discovery and mutation seams became stale when Phase 2 populated the destination and changed admission signatures. | Made the destination gate phase-aware reconciled every new path in the removal ledger and rebound mutations to the real admission seam. | The full boundary runner passes with 1527 exact path dispositions and all controlled mutants rejected. |
| C7-P2-F03 | Critical | C7-AUTHORITY-02 | Construction failure discarded the joined durability owner and serving exposed no authority-free policy observation. | Carried the exact owner through construction failure and added a copy-only observation containing identity basis profile and admitted bounds. | Failure-owner and missing-observation source mutants fail; runtime observation matches the admitted policy exactly. |
| C7-P2-F04 | Critical | C7-AUTHORITY-02 | Normal shutdown stored the mandatory durability owner in an `Option`, allowing lifecycle correctness to depend on developer conscientiousness. | Made the shutdown owner structurally mandatory and consume it exactly once before media close. | The optional-owner source mutant fails and open cannot omit admitted durability at compile time. |
| C7-P2-F05 | Critical | C7-IDEMPOTENCY-01 C7-FOUNDATIONAL-01 | The aspect-native mutation field role was declared but the canonical construction path never certified it, so the registry looked complete without enforcing the role. | Certified `NativePhysicalMutationRequest` in the real construction path and split the source gate across record declaration ownership construction and domain authorities. | The real gate passes and a field-role substitution mutant fails alongside allocation policy ordering and effect mutants. |
| C7-P2-F06 | High | C7-IDEMPOTENCY-02 C7-CHECKPOINT-01 C7-LEDGER-01 | The Phase 2 idempotency row demanded Phase 6 binding compaction and could never close honestly in phase order. | Narrowed Phase 2 to lease expiry unresolved authority and pending bounds; retained binding compaction solely under the Phase 6 checkpoint guarantee. | The Phase 2 row now matches the specification phase contract and the source-closure validator tracks it independently. |
| C7-P2-F07 | High | C7-IDEMPOTENCY-01 C7-FAILURE-01 | The golden fingerprint test reused production byte assembly and source-failure proof covered only early EOF so plausible derivation and streaming faults could survive green tests. | Added an independent fingerprint reconstruction every-field deltas exact security ordering and real producer excess-length invalid-count and early-end journeys with no-effect and retry observations. | Five registry tests and seven preparation journeys pass with exact failure kinds and unchanged media counters. |
| C7-P2-F08 | High | C7-DESTINATION-01 C7-IDEMPOTENCY-01 C7-SIGNAL-01 | The public submission facade owned a 112-line preparation pipeline and decomposed admitted policy into scalar plumbing creating a second representation of canonical inputs. | Moved preparation orchestration into its semantic director owner carried `PhysicalIdempotencyPolicy` intact and derived fingerprint bytes from the same canonical field record used by aspect-native construction. | Facade delegation and canonical-owner source gates pass; all dirty files remain within the line cap and scrutinized advisories retain one responsibility. |
| C7-P2-F09 | Medium | C7-AUTHORITY-02 | A panic-cleanup test asserted an exact delta on a process-global admission counter and failed nondeterministically when the full suite admitted unrelated roots concurrently. | Replaced the global exact-delta oracle with monotonic panic observation plus immediate readmission of the exact controlled root. | The isolated test and the full eighty-six-test library target pass while proving exact-root cleanup. |
| C7-P2-F10 | High | C7-AUTHORITY-01 C7-CLEANUP-01 C7-LEDGER-01 | The Phase 2 refactor added twenty tracked sources and changed ten semantic-family counts after the earlier removal-ledger snapshot. | Re-ran source discovery classified every new path by concrete semantic responsibility and retained transitional narrow or replace dispositions instead of blanket preservation. | All 1547 current path rows reconcile and the six inventory schema classification drift and omission tests pass. |
| C7-P2-F11 | High | C7-IDEMPOTENCY-01 C7-LEDGER-01 | The idempotency guarantee described only excluded allocation facts so an implementation omitting effect-relevant Store policy scope payload operation-family or security inputs could satisfy the row's wording. | Rewrote the guarantee as a two-sided equivalence contract and required independent every-field plus canonical-order evidence. | The strengthened row is bound to the Phase 2 mutation source closure and the omitted-policy effect ordering and allocation mutants all fail. |
| C7-P2-F12 | High | C7-SIGNAL-01 C7-LEDGER-01 | The exact Phase 2 source closure omitted the physical Signal-family journey even though the ledger cited that journey as proof. | Extracted the durability binding assertion into its own semantic module and added that exact module to the mutation-and-Signal source closure. | Any change to the four-family dependency binding proof now invalidates the recorded Phase 2 source identity. |
| C7-P2-F13 | High | C7-API-01 C7-LEDGER-01 | The exact public API inventory remained frozen at the twenty-four inherited surfaces and omitted every new Phase 2 durability mutation and preparation surface. | Added all forty Phase 2 additions with real semantics destination owners dispositions phases and declaration anchors and made the gate require the exact inherited-plus-Phase-2 union. | All sixty-four rows resolve to live source anchors; omitted extra duplicate and renamed surfaces fail the exact-set or anchor gate. |
| C7-P2-F14 | High | C7-API-01 C7-LEDGER-01 | Declaration anchors could remain green after a Phase 2 type was removed from the public facade because the API gate did not prove re-export reachability. | Added exact durability-root export membership and the complete physical-runtime to record-serving to publication preparation re-export chain. | Hiding any Phase 2 durability or preparation surface behind a private module now fails at its first public facade. |

## Phase 3 finding history

| Finding | Severity | Affected guarantees | Defect | Root correction | Closure evidence |
| --- | --- | --- | --- | --- | --- |
| C7-P3-F01 | High | C7-WAL-03 C7-FAILURE-01 C7-LEDGER-01 | WAL reservation denial consumed the only `PreparedPhysicalMutation` even though no effect had occurred, so inspection sealing, append contention, LSN exhaustion, or frame-planning rejection could strand an admitted mutation without a typed continuation. The Phase 3 source closure also omitted the preparation and attempt-binding files that governed this round trip. | Made every reservation denial return the exact linear prepared value without cloning authority; reversible planning rejection releases only the unperformed WAL allocation and reconstructs the same preparation including Signal profile and resource facts. Added the governing preparation, batch, admission, and attempt-binding sources to the Phase 3 closure. | Sealed-owner and executor-paused concurrent-append journeys consume the denial outcome, prove the exact preparation survives, and successfully retry after the blocking append settles. |
| C7-P3-F02 | Critical | C7-WAL-02 C7-WAL-04 C7-AUTHORITY-02 | The ordinary append facade accepted any public `PreparedPhysicalMutation`, but the WAL owner did not validate its Store identity, runtime identity, or prepared Signal profile against the target runtime before range allocation. A mutation prepared in Store A could therefore be appended into Store B's WAL while persisting Store A's identity. | Added a mandatory Store/runtime/profile preparation-admission owner at WAL construction and changed range reservation to accept only its private admitted wrapper. Foreign Store, stale runtime, and mismatched profile causes remain distinct and return the exact linear preparation before allocation. | Independent two-Store and close/reopen attacks prove exact denials, unchanged target WAL observation and zero artifact bytes; the rightful Store subsequently appends the preserved foreign-denied preparation. |
| C7-P3-F03 | High | C7-WAL-03 C7-FAILURE-01 | A failure after range reservation but still proven no-effect returned `WalRangeReservedPhysicalMutation`, yet the public facade had no operation that could consume that type. The owner cleared `in_flight` while stranding the caller's only continuation, and backend denial was mislabeled as submission rejection. | Added a lossless reserved-to-prepared rollback that is callable only after Store classifies proven no effect, carries the prepared Signal profile and resource shape through reservation, and returns the exact backend `ArtifactTreeFailure` cause. Indeterminate outcomes retain the reserved range and cannot enter rollback. | A one-shot real positioned-write denial returns the same mutation identity and fingerprint with an untouched frontier, then the returned preparation appends successfully through the canonical route. |
| C7-P3-F04 | High | C7-WAL-01 C7-WAL-02 | The Phase 3 plan required completed-receipt substitution attacks, but the implementation compared work range and digest only inline and no test independently varied any member of that exact settlement triple. | Added one named `matches_completion_binding` predicate owned by `PhysicalWalAppendSettlement`, made promotion consume it, and attacked wrong work, wrong range, and wrong digest independently in the real consecutive-append journey. | The exact binding passes while each isolated substitution fails before `WalAppendedPhysicalMutation` promotion. |
| C7-P3-F05 | High | C7-SIGNAL-01 C7-SIGNAL-02 C7-LEDGER-01 | The Phase 2 Signal row combined installed family bindings with Phase 8 caller deadline and cancellation semantics, allowing a proved early-phase row to overclaim behavior whose request values are intentionally only carried during Phase 3. | Narrowed Phase 2 to the exact installed dependency profile and created a separate Phase 8 guarantee for caller deadline, cancellation, monotonic clock, request-equivalence exclusion, and lease independence. | The existing Phase 2 profile journey still proves its narrowed row; the unimplemented caller-lifecycle claim is visibly OPEN in its owning phase. |
| C7-P3-F06 | High | C7-WAL-02 C7-WAL-04 | `PhysicalWalMemberIdentity` wrapped only the per-runtime operation ordinal, so independent runtimes could both mint member `1`; the nominal member identity was therefore not stable across the Store/runtime authority dimensions carried by its mutation. | Replaced the scalar ordinal with a domain-separated SHA-256 identity over exact Store, runtime, and operation identity and persisted that full member digest in the attempt binding and frame declaration. | The two-Store journey proves both runtimes begin at the same local mutation ordinal while their WAL member identities remain distinct; all fifteen durability-admission journeys pass. |
| C7-P3-F07 | High | C7-WAL-03 C7-FAILURE-01 | Physical work admission could deny a declared command before consuming it, including after the serving-health fence, while leaving the command and its bounded capacity reserved until shutdown. WAL correctly returned the prepared mutation but leaked attempt-local physical work authority and could exhaust admission under repeated denials. | Made the generic physical admission owner cancel the exact declared identity on every admission error; cancellation atomically removes only pre-dispatch work, returns scope and semantic capacity, records terminal accounting, and cannot reclassify dispatched work. | An indeterminate real write revokes serving health under a one-command profile; two consecutive WAL-shaped physical submissions are both denied, both exact identities appear as cancelled before dispatch, the second submission proves capacity reuse, and shutdown has no residual command. |
| C7-P3-F08 | High | C7-WAL-03 C7-API-01 C7-DOCUMENTATION-01 | The public WAL guide still matched the removed tuple-shaped reservation denial and discarded the linear continuation in both safe-denial branches. The example therefore did not compile against the shipped facade and taught callers to lose the exact prepared mutation needed for cause-specific continuation handling. | Rewrote the guide around the current four-way outcome with separate retained reservation and pre-effect values plus an inspection-only reserved value; explicitly separated proven no effect from retry eligibility; bound every Rust block byte-semantically to one external-consumer compile specimen and included both proof owners in the Phase 3 source closure. | The exact documentation-drift journey passes and the complete authority UI target compiles the specimen through the public facade while all authority attacks remain rejected. |
| C7-P3-F09 | High | C7-WAL-03 C7-API-01 | A `PhysicalRecordSubmission` whose publication owner had been released returned `InspectionRequired` from `append_prepared_wal`, even though no WAL inspection seal or possible effect existed. The API collapsed lifecycle loss into media suspicion and directed callers toward the wrong recovery action. | Added the exact `PublicationAuthorityReleased` reservation denial at the weak-facade boundary and retained `InspectionRequired` solely for a live WAL owner sealed after indeterminate evidence or nonempty unreconstructed media. | A mutation prepared before close remains byte-for-byte identifiable after close; append through the released facade returns the new lifecycle cause, preserves the preparation, and exposes no live WAL observation. |
| C7-P3-F10 | High | C7-WAL-03 C7-API-01 C7-FAILURE-01 | WAL append compressed work-request denial, bounded-capacity deferral, lifecycle staleness, identity exhaustion, and scheduler reservation denial into two generic labels. The public cause could not tell a caller whether to correct input, wait for capacity, move to a live runtime, inspect exhaustion, or reduce scheduler pressure. | Replaced `SubmissionRejected` with exact denied deferred stale and failed variants carrying the existing public work outcomes; made scheduler reservation denial carry its exact physical-instance admission cause. No new parallel error vocabulary was introduced. | A real one-command profile holds an unrelated declared read, returns `SubmissionDeferred` with exact `Commands` dimension and limit from the canonical WAL route, preserves mutation identity and fingerprint with zero appended frames, then appends the same preparation after exact capacity cancellation. |
| C7-P3-F11 | High | C7-WAL-02 C7-LEDGER-01 | The real WAL journey independently verified frame framing and coverage but treated Store attempt-binding persistence as a handful of substring searches. Coincidental bytes, reordered equal-width fields, missing fields, trailing garbage, or a substituted redo payload could satisfy those checks without proving the exact persisted join required by the ledger. | Added a test-owned bounded decoder that shares no production binding encoder or parser, requires exact frame and member-payload consumption, validates the domain and every field boundary in order, and compares the complete binding plus redo payload to independently held public expectations. | One Store-written frame decodes exact key lease fingerprint Store runtime operation member LSN range redo digest and canonical redo. Isolated mutation of every field, key/fingerprint order swap, truncation, trailing bytes, and redo-payload substitution are all rejected. |
| C7-P3-F12 | High | C7-WAL-03 C7-API-01 C7-FAILURE-01 | WAL append retained a generic `DependencyBlocked` cause after Signal had already classified the exact admission posture and blocking condition. Callers could not distinguish dependency readiness, partition mismatch, temporal conditions, or reference drift and therefore lacked a truthful correction target. | Made the public cause carry Signal's canonical admission class and optional condition directly; Store projects existing Signal truth and introduces no competing classification vocabulary. | The public guide names both retained fields, its exact external compile specimen remains facade-bound, and the complete Store journey and authority suites pass with the exhaustive cause shape. |
| C7-P3-F13 | High | C7-WAL-02 C7-LEDGER-01 | The independent attempt-binding inspector derived its expected redo payload and redo digest from production `CanonicalRedoRecords`, so a production ordering or encoding defect could agree with its own oracle and survive every field substitution attack. | Added a test-owned canonical redo encoder driven only by the original fixture records with independently declared domain count ordinal length and payload rules; the expected digest now derives only from those independent bytes. | The real Store frame matches the independent encoding and digest exactly, reversing the source record order changes the oracle, and payload substitution remains rejected by the independent member inspector. |
| C7-P3-F14 | High | C7-WAL-01 C7-CLEANUP-01 C7-LEDGER-01 | The cleanup source test proved that certification guards existed around the displaced backend append input and path-bound WAL planner, but it would still pass if an unguarded duplicate export were added elsewhere in the same facade. | Changed the gate to remove the one exact allowed guarded occurrence and require zero residual symbol occurrences in the default facade source. | Controlled backend and WAL mutants that append an unguarded duplicate export both fail, while the real guarded-only facades pass. |
| C7-P3-F15 | Critical | C7-WAL-01 C7-WAL-02 C7-FAILURE-01 | `WalAppendedPhysicalMutation::new` accepted a raw `PhysicalWalAppendSettlement`; exact work range and digest comparison was an adjacent boolean convention rather than an input type required by the appended transition. Another in-crate caller could therefore bypass the comparison while still constructing the stronger phase. | Added a private `CompletionBoundPhysicalWalAppendSettlement` constructible only by exact triple comparison and changed appended-phase construction to require that sealed witness. The public raw settlement remains observation only. | Production promotion cannot compile without consuming the exact comparison result; the real consecutive append passes while isolated work range and digest substitutions all fail to produce the required witness. |
