# Milestone 9.16.1 Canonical Graph Progression Closure Ledger

**Owner:** Milestone 9.16.1
**Canonical specification:** `milestone-9.16.1.md`
**Status:** Closed — Phases 1-5 are implemented and every named authority,
parity, facade, documentation, handoff, and residue row below is proved or
closed. Every dirty unallowlisted Rust file is at or below 400 lines;
unrelated repository-wide line-cap debt is recorded as out-of-scope baseline,
not attributed to or made a blocker for this milestone. Unrelated monolith
behavior remains preserved and no whole-crate deletion is authorized.
**Historical policy:** Milestones 9.9, 9.10, 9.11, and 9.15 and completed
Milestone 9.16 rows retain their recorded status. This ledger adds stronger
guarantees; it does not reopen, revoke, or rewrite historical status or
closure.

A requirement is `PROVED` only when its production owner, public consumer
evidence, adversarial evidence, lifecycle and performance posture, and residue
posture agree. A finding is `CLOSED` only when the root cause and every current
dependent guarantee have been checked through the destination path.

## Requirement Ledger

| ID | Phase | Guarantee | Status | Required closure evidence |
|---|---:|---|---|---|
| C1 | 1 | Every installed query and operation owns one sealed graph-obligation set with exhaustive kind, owner, selection, graph-read requirement, resource, effect, and terminal-evidence meaning. | **PROVED** | Installation owns the sealed set and fixed selector index; query/operation twins cover every kind and exact lower owner, unsupported combinations deny, runtime/generation substitutions deny, public construction fails, entry/byte budgets hold, and warm lookup performs zero digest-text materialization. |
| C2-L | 1 | Relational owns graph truth and exact mechanics; Runtime Bridge remains Query-independent and owns installed neutral correspondence, crossing, and Signal-facing lowering; Signal owns policy evidence. | **PROVED** | Relational branch-qualified snapshot and execution-basis evidence, Runtime Bridge owner tests, foreign-evidence hostility, and a normal dependency tree containing no `worth-query` crate. |
| C2-Q | 1-2 | Query owns Query-to-neutral translation, portable comparison, authority continuity, legal composition, and the compiler-visible installed-selection-plan-session progression without recreating lower-owner decisions. | **PROVED** | The destination packages own the typed progression; shared query and operation seams consume genuine Relational, Bridge, and Signal evidence after session start, and full Query, Bank, and Worth UI parity is green. Later semantic-lane cutovers remain C6-C12 work. |
| C3 | 2 | At the covered shared application seams there is one installed selection path and selection carries no execution authority. | **PROVED** | Sealed destination selection is consumed from the installed query/operation set; public construction fails and selection cannot mint terminal evidence. Legacy monolith selectors remain explicitly inventoried for later per-surface cutover. |
| C4 | 2 | Every read or mutation entering the covered shared application seams uses one requirement, inventory, structural-cost, budget, capacity-reservation, and sealed-plan admission chain without losing predecessor feature behavior. | **PROVED** | Application query, ordinary operation, and capability-operation admission traverse the same typed chain; saturation and drop tests prove exact capacity behavior; real Bank reads/mutations and complete Query/Worth UI workspaces remain green. Lane execution migration remains Phase 3/4 work. |
| C5 | 2 | One managed graph-work session begins before every decision-bearing graph read at the covered shared seams and binds runtime, generation, query/operation, principal/context, typed branch, branch-qualified basis, provider, managed run, and reservation. | **PROVED** | Query, ordinary-operation, and capability admission open the session first; all retained facts carry its fixed-width identity; a separate fixed-width managed-run identity is retained; cross-branch equal-version evidence denies; retries mint distinct session/run identities; and drop returns exact resource baselines. |
| C6 | 3 | Current, continuation, historical, preview, and live application-query lanes consume one canonical graph-read planning authority. | **PROVED** | Every lane consumes the Phase 2 admitted graph-work plan and session-owned read port. Public Bank transcripts cover current, continuation, historical, preview, live delivery, and live close; lane twins prove equal canonical read-graph meaning while distinct attempts retain distinct plan, session, and basis identities; compile-fail cases deny raw planning/review imports; and application-query result execution has one shared authorized-read entry with no direct graph handle. |
| C7 | 3 | Read-only execution terminates from session-owned read products and cannot construct proposed-state, invariant, or commit authority. | **PROVED** | The private session port mints an unforgeable session/plan/basis-bound read proof; only its exact consumption can mint the public read completion. The terminal carries actual dependency counters plus exact basis and graph-capacity release receipts, and compile-fail cases deny both construction and mutation authority. |
| C8 | 4 | Capability and ordinary authorization retain exact Relational, Bridge, and Signal evidence inside the same provider session and complete decision read-set. | **PROVED** | Ordinary and capability authorization retain session-bound facts and revalidate through the session's exact typed branch. Principal, purpose, request, trusted time, exact grant/path, negative fact, equivalent-grant, policy, branch, session, and commit-basis substitutions deny through the public mutation path. |
| C9 | 4 | Mutation continues from the common session through proposed state, actual installed invariant execution, and provider compare-and-commit. | **PROVED** | The operation session binds the lower mutation worker; the primary provider retains one sealed Relational candidate produced by all three real invariant phases and consumes it exactly once. Relevant drift goes stale, unrelated drift re-compares and commits, owner-skip and real invariant-violation probes deny without state change, and response-loss retry returns the authoritative result. |
| C10-2 | 2 | Admission denial and abandonment of an admitted Phase 2 plan release graph-work reservations and session resources exactly once. | **PROVED** | Query and operation capacity twins saturate at 64, deny the sixty-fifth admission, admit exactly one replacement after one plan is dropped, and return the provider-session resource counter to zero after cancellation-by-abandonment. |
| C10-3 | 3 | Successful, denied, cancelled, failed, overflowed, closed, and abandoned read outcomes release sessions, bases, result buffers, live queues, graph reservations, and retained continuation resources exactly once. | **PROVED** | One-shot and continuation tests prove basis and result-buffer release; graph-capacity saturation proves one exact reservation returns; live close returns the read terminal; and cancellation, deadline, source close, overflow, and drop return Relational, Bridge, Signal, queue, and provider-session baselines. |
| C10-4 | 4 | Success, conflict, explicit cancellation, failure, and indeterminate mutation terminals release sessions, bases, buffers, proposals, and continuations exactly once. | **PROVED** | The public lifecycle matrix covers commit, stale conflict, cancellation, plan denial, pretransaction abort, invariant-owner failure, response loss, and idempotent retry. Every path returns active snapshots and provider-session resources to exact baselines, and terminal completion verifies the exact worker/run cleanup plus Relational, Bridge, provider-buffer, and capacity-reservation release evidence. |
| C11 | 5 | Obligation outcomes, receipts, envelopes, and inspection views are derived only from actual terminal evidence and grant no execution authority. | **PROVED** | Read and commit publication consume sealed execution terminals; fresh and recovered commits retain distinct provenance; public construction and escalation fail; installed-obligation adoption consumes installation inspection only. The synthetic monolith executor, outcome/envelope families, and dispatch receipts are absent under the permanent residue test. |
| C12 | 5 | Destination packages own each covered authority after parity-gated atomic cutover; no destination package imports the monolith, no retired predecessor remains authoritative, and unrelated monolith behavior remains intact. | **PROVED** | Installation, admission, execution, publication, and Host own the covered progression without a monolith dependency. Boundary checks, compile-fail visibility, exact source/path residue, hostile Host adoption, and full Query, Bank, and Worth UI parity prove the cutover while generic monolith read and graph-composition behavior remains green. |
| C12-P1/2 | 1-2 | Installed meaning and shared admission/authorization transfer atomically to destination owners while comparison artifacts remain non-executable and unrelated predecessors remain intact. | **PROVED** | The four Phase 1/2 migration rows below are integrated at their existing constructors/shared seams, public proof construction fails, full consumer parity is green, and no unrelated monolith surface was removed. |
| C12-P3 | 3 | Application-query graph-read authority transfers atomically to admission/execution owners while result shaping remains in the semantic kernel and unrelated generic monolith reads remain intact. | **PROVED** | All five lane families switch through the same session-owned port; raw application-query Relational execution and its parallel admitted-plan field are gone; ordinary public planner/review routes fail to compile; and full Query, Bank, and Worth UI suites preserve feature parity. |
| C12-P4 | 4 | Application-operation mutation progression continues from the Phase 2 session through proposal, installed invariant execution, and compare-and-commit without introducing another authority lane. | **PROVED** | The real ordinary and capability operation consumers use the session-bound primary provider path. The lower Milestone 9.15 managed run is retained only as the exact bound mutation worker, while proposal validation, all three invariant phases, serialization, commit, and cleanup are owned by the destination session/provider progression. Unrelated generic monolith execution remains preserved and open under its own rows. |
| C12-P5 | 5 | Before each remaining publication, facade, documentation, or residue cutover, the predecessor is the sole executable authority; cutover atomically transfers every covered consumer and disables only that exact predecessor authority. | **PROVED** | The migration table names each predecessor and preserved contract. Publication consumers switched to terminal-derived receipts; Installation owns read-only obligation adoption and Host only re-exports it; the monolith obligation tree, synthetic executor, callback paths, specialized Consumer Kit, and authority aliases are absent; unrelated generic runtime remains. |
| C13-1/2 | 1-2 | Phase 1 installation retains cold canonical identity and ordinary Phase 2 warm selection/admission performs zero canonicalization, SHA, or digest-text materialization. | **PROVED** | Fixed-width identities are carried; installed lookup and Bank capability admissions report exact-zero warm canonical work; structural counters are bounded independently from encoded-byte budgets. |
| C13-3 | 3 | Ordinary warm read work scales only with selected obligations, actual traversal, projected result shape, and delivered live work, with zero canonicalization, SHA, or digest-text materialization. | **PROVED** | Bank scale twins vary unrelated grants, graph/candidate population, result rows, and 32 live consumers while preserving exact warm canonical-work posture and exposing only the declared traversal, projection, authorization, and delivery counters. |
| C13-4 | 4 | Ordinary warm mutation work scales only with admitted semantic delta. | **PROVED** | Production mutation-work evidence counts decision facts, proposed facts, invariant-state facts, invariant work, and actual Relational invariant executions/results. A scale twin adds 128 unrelated records while preserving the exact baseline counts, and retry consumes the retained authoritative completion rather than repeating mutation work. |
| C13-5 | 5 | Ordinary warm publication work scales only with admitted semantic delta. | **PROVED** | Separate real Bank query and mutation twins vary 128 unrelated grants or graph records while retaining equal terminal semantic work. Publication and inspection perform no registry scan and report exact-zero canonical entries, SHA compression blocks, and digest-text materializations. |
| C14 | 5 | Public documentation and examples describe and compile against the single real facade, distinguish one-way integration from competing authority, and name every parity-gated migration. | **PROVED** | The AI README and canonical progression document agree with the real declaration/Host facades; the required focused documents were revised, the authority-capable Consumer Kit document was removed, the migration map names inherited guarantees and exact retirement, the external Host example compiles and runs, doctests pass, and all 19 changed Markdown files have valid relative links. |
| C15 | 5 | Milestone 9.16 resumes at Runtime Phase 7.3 by consuming the existing Phase 6 feature contracts and the new session authority without a parallel selector, planner, authorization lane, or receipt model. | **PROVED** | The explicit handoff below reconciles 9.9, 9.10, 9.11, 9.15, and 9.16 Phase 6/7.1/7.2 guarantees and names every migrated surface. Phase 7.3 inherits the same typed branch, installed query, plan, session, access decision, result shaping, and publication path. |
| C.L | all | The ledger covers every authority, lifecycle, owner, public boundary, semantic-surface migration, exact retirement target, downstream parity, and performance claim without a Cartesian test matrix. | **PROVED** | Separate phase rows, semantic-surface cutovers, causal hostile cases, mutation probes, lifecycle matrices, independent warm-work twins, exact residue, full consumers, and an exact dirty-file audit prove the covered milestone without attributing unrelated repository debt to it. |

### Historical Phase 1 Evidence Snapshot

This section records what the original implementation pass reported. It is not
current closure evidence after the 2026-08-02 audit and does not override the
requirement statuses above.

- `worth-query-installation` owns the sealed installed vocabulary, typed set and
  row identities, fixed kind index, query/operation bindings, exact owner
  progressions, and installation canonical-work evidence. Public consumers can
  inspect but cannot construct or mutate the set.
- Installation evidence covers all five semantic kinds; the distinct
  principal, ability, and capability authorization routes; complete selection,
  resource, effect, and terminal row semantics; read-only absence; unsupported
  invariant ownership; entry and encoded-byte overflow; and exact query and
  operation runtime/generation affinity.
- `cargo test -p worth-query-installation --quiet` passes 155 unit tests and 4
  compile-fail documentation tests. `cargo clippy -p
  worth-query-installation --all-targets --no-deps -- -D warnings` passes.
- `cargo test -p worth-query-execution --lib --quiet -- --test-threads=8`
  passes 404 tests. The governed-live fixture uses its own exactly installed
  operation, while the hostile `TouchAccount` fixture retains no undeclared
  label authority.
- `cargo test --manifest-path workspaces/worth-query-bank-world/Cargo.toml -p
  bank-domain --test estate_capability_installation --quiet` passes 11 real
  consumer tests without granting execution authority from installation alone.
- The warm-path source guard proves no query/operation obligation
  reconstruction helper and no direct SHA implementation in the installed
  obligation family. Kind lookup reports one index probe and exact-zero
  canonical work.
- `scripts/quality/scrutinize_rust_functions.py --dirty .` scans 86 dirty Rust
  files with zero scan errors; every dirty Rust file is at or below 400 lines.
  Advisory candidates were inspected against semantic responsibility. The
  wide application-query admission boundary remains Phase 2 work rather than a
  Phase 1 helper split.
- `boundary-check --root .` reports valid Road 1 topology, and `agent-context
  check` passes. Query's local warning-denied Clippy gates pass. A broad
  dependency-lint invocation remains blocked by unrelated dirty
  `worth-relational` Clippy findings from the paused convergence surface; this
  is not counted as Phase 1 evidence and is not silently reported green.

### Historical Phase 2 Evidence Snapshot

This section likewise preserves the reported proof snapshot without treating
it as current integrated closure.

- Installation-owned obligation selection progresses linearly through one
  required set, executable review, live resource reservation, sealed admitted
  plan, and managed graph-work session. Selection and review remain
  non-authoritative; public consumers cannot construct a proof-bearing
  transition.
- Query and operation admission open the managed provider session before
  principal, ability, capability, disclosure, or other decision-bearing graph
  observation. Retained decision facts and terminal receipts carry the exact
  session identity and typed branch supplied by the installed runtime basis.
- Every privileged mutation revalidation requires the concrete managed
  mutation session. It derives the current Relational execution basis from
  that session's typed branch, rejects facts or commit bases from another
  session or branch, and releases the temporary basis exactly once. No
  idempotency or commit path opens an implicit/default snapshot.
- Real Bank reads and mutations traverse the public host facade and retain the
  selected obligation, admitted plan, provider session, typed branch, and
  exact release evidence. Unsupported owners and forged construction fail
  before effects, and cancellation/denial paths return reservations and bases
  exactly once.
- `cargo test --manifest-path workspaces/worth-query/Cargo.toml -p
  worth-query-admission -q -- --test-threads=1` passes 101 unit tests and 8
  documentation tests. Strict all-target Clippy passes for the admission
  package.
- `cargo test --manifest-path workspaces/worth-query/Cargo.toml -p
  worth-query-execution -q -- --test-threads=1` passes 423 unit tests and 13
  documentation tests. The five-test commit authorization lifecycle module
  includes ordinary and capability revalidation on non-default branches plus
  a hostile cross-session/cross-branch substitution denial.
- `cargo test --manifest-path workspaces/worth-query-bank-world/Cargo.toml -q
  -- --test-threads=1` passes the full Bank workspace suite with one explicitly
  ignored high-operation probe. Its frozen schema inventory includes the
  intentionally installed, capability-governed `estate_customer_identity`
  query.
- Strict all-target Clippy passes for `worth-query-execution` and the entire
  Bank workspace. Root, Query, and Bank formatting checks pass. `boundary-check
  --root .` and `agent-context check` both pass.
- Residue scans report zero implicit authorization-revalidation snapshots,
  zero obsolete capability-refresh calls, and zero literal `"main"` authority
  defaults in the canonical production roots. All 270 dirty Rust files satisfy
  the 400-line rule after session affinity was rehomed to its own semantic
  module.
- The repository-wide line-cap guard remains independently red on 111 tracked,
  unallowlisted baseline files outside this slice. No exemption was added and
  the failure is not reported as green; Milestone 9.16.1 closure remains open
  while the wider repository debt exists.

### 2026-08-02 Reopen Audit

- The Query and Worth UI workspaces compile, so the package graph and public
  type graph remain connected.
- Runtime Bridge passes 939 tests and its dependency tree contains no
  `worth-query` crate. C2 remains proved.
- `worth-query-admission` passes 101 unit tests and 8 documentation tests;
  `worth-query-execution` passes 423 unit tests and 13 documentation tests.
  These are retained candidate evidence for C3-C5, not integrated closure.
- `worth-query-installation` passes 154 of 155 unit tests. Its warm-path
  residue guard detects `.render_hex()` in graph-work session identity
  construction, reopening C1 and C13.
- The graph-obligation hostile journey passes 14 of 15 tests. The owner matrix
  contains at least one obligation kind with no real supported lane,
  reopening the exhaustive-owner claim.
- `worth-query --lib` passes 2,450 tests and fails 262. The installed operating
  world passes 167 and fails 153; public declarative journeys pass 25 and fail
  12. Most failures panic where legacy string identities are assumed to be
  canonical SHA-256 rather than being migrated at their owning cold seam.
- Worth UI Query binding passes 38 tests and fails 40 at the same Query identity
  boundary. This is downstream parity failure, not forty independent UI
  migration rows.
- The Bank workspace is functionally green except for one canonical-work scale
  twin whose encoded and SHA input byte counts differ by one. The performance
  proof remains open.
- `boundary-check` and `agent-context check` pass. Mechanical topology cannot
  substitute for the red functional and performance evidence above.

### 2026-08-03 Safe Pre-Implementation Checkpoint

- The rejected Query and Bank graph-obligation/session implementation from
  `f7536ab14` is reversed. Its valid Relational branch-qualified snapshot and
  execution-basis work is retained. The corrected roadmap, Milestones 9.13.2,
  9.16, 9.16.1, and 9.17, and both Query closure ledgers are retained. This
  checkpoint contains no 9.16.1 graph-obligation authority migration and makes
  no Phase 1 or 2 closure claim.
- Relational now keeps branch identity on published snapshot handles and
  execution-basis admission without turning publication into physical
  retention authority. Published handles establish branch-qualified
  eligibility and the logical history reconstruction fence; cache protection,
  retained-state admission, and record retention remain separate authorities.
  Internal historical reads reconstruct transiently, while a physically
  pruned publication cannot mint an execution-basis lease. The full Relational
  library suite is green with 989 passed and 25 intentionally ignored tests.
- Runtime Bridge remains Query-independent. Its dependency tree contains no
  `worth-query` crate, its suite is green, and the lower-runtime inventory
  explicitly classifies the Query-side async live declaration as a one-way
  boundary adapter rather than Bridge-owned Query behavior.
- The disconnected Phase 6 graph-read handoff now carries typed canonical
  digest identities from cold construction through access requirements,
  operation resolution, selectivity, and plan review. Warm parsing and
  per-row dotted-path reconstruction were removed from the restored path.
- The graph-read budget distinguishes the ordinary intermediate-set ceiling
  from the broad-traversal semantic threshold. Mutation authority-ceiling and
  decision-fact tests now exercise the intentionally widened Phase 7
  `TouchAccountOperation` contract without weakening the compile-wide versus
  install-narrow hostility proof.
- `cargo test --workspace` in `workspaces/worth-query` is green, including
  2,712 monolith tests, 404 execution tests, public journeys, compile-fail
  certification, and documentation tests. The full Bank workspace and the
  Worth UI Query-binding package are green; one Bank high-operation probe
  remains explicitly ignored by its existing contract.
- `boundary-check --root .`, `agent-context check`, formatting, and dirty-file
  composition scrutiny are green. The repository-wide line-cap guard remains
  red on 112 unrelated tracked baseline files, with zero failures among dirty
  files. The touched monolith `runtime.rs` remains covered by its pre-existing
  explicit allowlist entry and changes only by one extracted-module
  declaration; no exemption was added. This is not counted as milestone
  closure evidence.
- The broader Worth UI certification workspace still has 24 topology-inventory
  failures inherited from current `master`. The real Query-binding consumer
  lane is green, so this is recorded as a separate master-integration barrier,
  not hidden as Query parity success or broadened into 9.16.1 work.

### 2026-08-03 Phase 1-2 Pre-Implementation QA Audit

This snapshot supersedes current-status conclusions in the historical Phase 1
and Phase 2 evidence sections without rewriting those audit records.

- The current source is the clean safe checkpoint at `831295285`. The rejected
  implementation remains available only as negative audit evidence; none of
  its destination graph-obligation or managed-session types exist in the
  production tree.
- C2 is split. Lower-owner dependency direction is proved: Runtime Bridge has
  no normal dependency on Query, and Relational retains branch-qualified
  snapshot and execution-basis identities. Query composition is defective:
  there is no installed-selection-plan-session progression at the checkpoint.
- C3-C5 are defects rather than unknown work. Application-query planning still
  exposes proof-like constructors and executable review, application query and
  operation authorization observe Relational/Bridge evidence before the later
  provider session, and the monolith can report selection-backed invariant or
  validator execution without calling the semantic owner.
- The rejected owner-completion implementation was synthetic: one evidence
  object caused Query to mark every step in an owner route complete. Query
  admission and execution phase labels were also encoded as if they were lower
  semantic owners. Neither pattern is admissible evidence for Phase 1 or 2.
- Destination comparison artifacts are explicitly non-executable before an
  exact cutover. A Phase 1 or 2 row may close only when its covered production
  consumers switch in one coherent slice and the exact predecessor authority
  becomes insufficient in that same slice.
- Query, Bank, Runtime Bridge, Relational, and the complete Worth UI workspace
  are green at this audit boundary. The earlier Worth UI topology failures
  remain historical evidence of why package compilation is not a parity gate;
  they are not current failures.
- CQ10, CQ15, CQ16, and CQ17 remain closed. Their corrections are prerequisites
  for the new implementation and are not reopened merely because Phase 1-2
  work resumes.

### 2026-08-03 Phase 1-2 Implementation And QA Closure

This is the current evidence snapshot. It closes only the Phase 1 installed
meaning and Phase 2 shared admission/session foundation. It does not claim the
Phase 3-5 lane, mutation-terminal, publication, or monolith-authority cutovers.

- `worth-query-installation` now derives one sealed obligation set at each
  installed application-query and application-operation constructor. The set
  retains exact kind, lower owner, selection basis, graph-read requirement,
  resource, effect, terminal-evidence, runtime, schema, and generation meaning.
  Its fixed selector index performs one probe and no warm canonical, SHA, or
  digest-text work.
- The Phase 2 destination path is installed set -> sealed selection -> existing
  graph-read/resource review -> live capacity reservation -> sealed admitted
  plan -> managed graph-work session. Selection and inspection cannot execute,
  and the ordinary host facade exposes no constructor for the sealed phases.
- Shared application-query admission and ordinary/capability operation
  authorization now open the session before principal currentness, ability,
  grant/path, Bridge, Signal, or disclosure observations. Retained facts carry
  the exact fixed-width session identity; the session also retains a distinct
  fixed-width managed-run identity, typed branch/truth-branch affinity,
  branch-qualified basis, principal/context, provider, and reservation.
- The governed-query path does not relabel a capability decision from its
  earlier capability-access session. It opens the query session and reobserves
  the genuine lower-owner capability evidence into that session. Equal-version
  evidence from another typed branch denies, and retries mint fresh session and
  managed-run identities.
- The Bank QA pass exposed an invalid assumption that every capability target
  is an executable operation. `ViewRestrictedEstateOperation` is a
  programless capability authorization target, so installation now grants it a
  narrow graph-authorization authority with exactly one capability obligation
  without minting executable operation authority. The complete Bank workspace
  is green, including its 18 capability-admission tests; one inherited
  high-operation probe remains intentionally ignored.
- Capacity twins independently saturate query and operation graph work at 64,
  deny the sixty-fifth admission, admit exactly one replacement after one
  admitted plan is abandoned, and return the resource baseline to zero after
  all plans are dropped. This proves Phase 2 admission denial and cancellation-
  by-abandonment, not the later explicit read/mutation terminal matrix.
- The complete Query workspace is green, including 2,712 monolith tests and
  the installation, admission, execution, public-journey, compile-fail, and
  documentation suites. The complete Worth UI workspace is green after its
  stale topology fixture was brought forward to the current declaration
  protocol and its certification tests were corrected to preserve the
  intentional standalone QueryBinding/Intent routing introduced on `master`.
- Runtime Bridge remains Query-independent: its normal dependency tree has no
  Query package. Strict warning-denied Clippy passes for the Phase 1/2 Query
  destination packages. All 68 dirty Rust files are at or below 400 lines;
  composition scrutiny reports no scan errors. The repository-wide line-cap
  guard remains red only on inherited, unallowlisted baseline files outside
  this slice and is not represented as green closure evidence.
- QA deliberately leaves CQ1-CQ6, CQ11, CQ13, C6-C9, C10-3/4, C11-C12,
  C12-P3/5, C13-3/5, and C14-C15 open. Those rows cover the fake monolith
  executor, legacy public proof-like planning surfaces, exact query-lane and
  mutation-terminal cutovers, real invariant execution, terminal receipts,
  publication, docs, and final residue. Treating the Phase 2 foundation as
  proof of those later migrations would recreate the overbroad closure error
  this ledger is designed to prevent.

### 2026-08-03 Phase 3 Implementation And QA Closure

This snapshot supersedes only the Phase 3 status conclusions above. It does
not close or partially implement the Phase 4 mutation terminal, Phase 5
publication/documentation/residue, or unrelated generic monolith rows.

- The Phase 2 application-query plan now moves its admitted graph-read plan and
  exact graph-capacity reservation into the managed provider session. The
  session owns the only port that can lend the raw Relational runtime and
  primary-graph layout to the private result-shaping kernel.
- The kernel returns actual predicate/negative-space, ordering, membership,
  traversal, projection, candidate, record, field, relation, and ordering-key
  work together with an unforgeable session/plan/basis proof. It cannot mint a
  successful public receipt directly.
- Completing that proof through the same session validates exact branch,
  schema, plan, session, and basis affinity; consumes the exact Relational
  basis release receipt; returns one graph-work reservation; and mints the
  read-only terminal embedded in the application-query receipt. The terminal
  exposes no proposed-state, invariant, mutation, commit, retry, continuation,
  or execution authority.
- Current one-shot, continuation, historical, preview, live opening, and live
  delivery all use the same authorized-read/session-port entry. Equal query
  meaning is proved by the canonical read-graph digest; distinct attempts
  correctly retain distinct plan, session, and basis identities. The earlier
  ledger phrase `plan-identity equality` was defective and has been removed.
- The ordinary admission facade no longer exports raw planning input,
  caller-driven review, or graph-plan construction. The monolith's admitted
  graph-read plan is no longer exported through its ordinary facade. A hidden
  one-way integration seam remains for destination execution and the preserved
  generic workspace-read engine; it cannot create a second public authority
  lane.
- The unrelated generic workspace-read engine remains present and green. Phase
  3 retires only the application-query competing planning/execution authority;
  it neither authorizes nor claims deletion of unrelated monolith features.
- Compile-fail certification proves that an ordinary-facade consumer cannot
  import the raw planning input or review transition, import the monolith
  admitted graph plan, construct a read terminal, or obtain mutation authority
  from one. The doc-hidden cross-crate integration seam remains governed by
  dependency and facade enforcement and is not an ordinary consumer contract.
  Runtime hostility covers foreign runtime, cross-basis, cancellation, revoked
  authorization, continuation, live lifecycle, and exact cleanup.
- The complete Query workspace passes, including 2,712 monolith tests and the
  normal compile-fail suite. The complete Bank workspace passes with one
  existing ignored speed probe; focused public read suites pass 9 historical,
  8 live, and 19 ordinary tests with one ignored speed probe. The complete
  Worth UI workspace passes with one existing ignored test.
- Strict warning-denied Clippy passes for admission and execution. Formatting
  passes in Query, Bank, and Worth UI; boundary-check and generated-context
  checks pass after explicitly refreshing the narrowed-facade and
  certification-dev-dependency snapshots. All 142 dirty Rust files are at or
  below 400 lines, and composition scrutiny reports zero scan errors. The
  repository-wide line-cap guard remains red on inherited, unrelated files and
  is not represented as green milestone closure evidence.
- Bank result/graph/candidate/policy scale twins and 32 live consumers preserve
  exact-zero warm canonical work. Query receipts expose the actual structural
  dependency counters, so output shape cannot masquerade as graph-read proof.
- C8-C9, C10-4, C11-C12, C12-P4/5, C13-4/5, C14-C15, CQ1-CQ4, CQ11, and the
  Phase 4/5 portions of CQ13 remain open. Phase 3 does not close authorization
  completeness, proposed state, installed invariant execution, commit,
  mutation retry, publication, documentation, or final milestone residue.

### 2026-08-03 Phase 4 Implementation And QA Closure

This snapshot supersedes only the Phase 4 status conclusions above. It does
not close Phase 5 publication, facade, documentation, or residue, and it does
not authorize deletion of unrelated monolith behavior.

- The Phase 2 application-operation session now binds the exact lower
  Milestone 9.15 mutation managed run as its worker. The worker cannot be
  substituted across operation, graph-work session, graph-work run, runtime,
  generation, branch, or provider identities, and terminal completion checks
  the exact worker cleanup receipt.
- Ordinary and capability authorization facts remain in that same session.
  Commit-basis comparison, reauthorization, capability refresh, and
  idempotency inspection open the exact typed branch snapshot rather than an
  implicit current/default snapshot. The earlier Historical Phase 2 Evidence
  Snapshot overstated this guarantee before it existed; this Phase 4 evidence
  is the first closure evidence for it.
- The final residue audit caught two remaining implicit-current paths after
  the first green pass: capability-operation refresh still called its obsolete
  non-session method, and provider idempotency lookup still selected the
  runtime's latest snapshot and index generation. The obsolete method is gone;
  graph-work acquisition, capability refresh, idempotency inspection, index
  recovery, response-loss recovery, and retry now require an explicit branch.
  A branch-hostile test proves that a main-branch idempotency record is absent
  from an independently advanced feature branch.
- The primary provider no longer reports invariant success after checking
  only a locator. It opens a Relational transaction on the session branch,
  prepares the exact mutation batch, executes commit-boundary,
  mutation-sensitive, and snapshot-publication invariants, and retains the
  sealed owner-minted validated candidate. Commit can consume only that exact
  candidate and rechecks its branch head.
- Provider serialization spans decision read-set comparison, proposed-state
  construction, real invariant validation, and commit. Relevant concurrent
  drift therefore returns stale while unrelated drift re-compares at the new
  head and commits instead of failing because it validated outside the
  serialized progression.
- Mutation probes independently deny a forged invariant-success verdict with
  no owner candidate and an actual Relational invariant violation injected
  into the exact batch. Both leave authoritative state unchanged. Branch,
  session, proposal, plan, decision-fact, and commit-basis substitutions are
  denied separately.
- The mutation lifecycle matrix covers successful commit, stale conflict,
  explicit cancellation, plan denial, pretransaction abort, invariant-owner
  failure, response loss, and idempotent retry. Every path returns active
  snapshots and provider-session resources to exact baselines; terminal
  completion also requires explicit Relational, Bridge, provider-buffer, and
  graph-capacity release evidence.
- Production-owned mutation-work evidence records decision facts, proposed
  facts, invariant-state facts, invariant work, and real Relational invariant
  executions/results. Adding 128 unrelated records preserves every baseline
  count, while the non-empty mutation proves the oracle is not vacuous.
- Focused application-attempt, authorization, lifecycle, invariant-mutation,
  branch-snapshot, scale, ordinary Bank mutation, and capability Bank mutation
  suites are green. The complete Query workspace is green, including the
  2,712-test monolith suite, destination crates, compiler-boundary suites, and
  documentation tests. The complete Bank and Worth UI workspaces are green;
  Bank retains its one explicitly scheduled ignored speed probe.
- The full Relational owner library is green with 990 passed and 25 explicitly
  ignored tests. Warning-denied production Clippy is green for
  `worth-query-execution`. Relational warning-denied Clippy remains red on 20
  inherited production findings and 141 all-target findings outside the
  Phase 4 touched files; this is recorded as repository debt rather than
  claimed as Phase 4 evidence.
- Road 1 boundary and generated-context checks pass. Formatting and diff
  hygiene pass. Composition scrutiny covers 173 dirty Rust files with zero
  scan errors; every dirty Rust file is at or below 400 lines after the
  touched Relational facade was reduced to exactly 400. The repository-wide
  line-cap guard remains red on 112 tracked, unallowlisted baseline files and
  is not represented as green milestone closure evidence.
- QA leaves CQ1-CQ4, CQ11, CQ13, C11-C12, C12-P5, C13-5, and C14-C15 open.
  Those rows cover the generic monolith fake executor, manual invariant packs,
  remaining public authority-like surfaces, publication, documentation, and
  final residue. Phase 4 neither deletes them nor misreports them as migrated.

### Phase 5 Application-Query Publication Evidence Snapshot

- `worth-query-publication` now owns the application-query publication receipt
  and borrowed inspection view. The only production constructor consumes the
  execution-owned admitted disclosed result and copies its completed terminal;
  the publication product is accepted by no execution transition.
- The inspection view exposes the actual session, managed run, admitted plan,
  typed terminal basis, result/work counters, and release posture without
  canonicalizing, hashing, rendering digest text, or scanning another registry.
  Existing read-only receipt inspection remains source-compatible through the
  publication receipt and does not become execution authority.
- The real Bank governed-disclosure journey publishes through
  `worth-query-host`, inspects the exact terminal identities and releases, and
  preserves the existing disclosed/omitted product behavior. A compile-fail
  doctest rejects direct publication-receipt construction.
- The Bank publication scale twin varies unrelated installed principals and
  grants from zero to 128 while result and ordinary work remain equal and both
  publication phases report zero canonical entries, SHA compression blocks,
  and digest-text materializations.
- `cargo test -p worth-query-publication` passes all three compile-fail doctests.
  The focused real Bank publication and publication-scale tests pass. C11 and
  the complete Phase 5 surface inventory remain open at this snapshot because
  mutation commit/retry publication and the monolith outcome families had not
  yet been implemented or proved.

### Phase 5 Application-Commit Publication Evidence Snapshot

- A fresh execution commit receipt cannot exist until the exact graph-work
  session, managed run, admitted plan, installed binding/obligation, typed
  Relational and truth branch, snapshot, Relational basis, Bridge reservation,
  provider memory, and execution capacity all converge into one cleanup-backed
  mutation completion.
- Fresh and recovered commits are now distinct terminal postures. A fresh
  terminal retains the actual decision, proposal, invariant-state, invariant-
  work, and Relational invariant execution/result counts. An idempotent recovery
  names the branch-qualified authoritative commit without claiming that the
  retry executed it; if the retry reached a managed inspection run, that
  separately cleaned-up session is recorded as retry inspection evidence.
- `worth-query-publication` consumes only the sealed commit receipt and derives
  a non-authoritative publication receipt and inspection view. The real Bank
  commit facade now stores that destination publication receipt while preserving
  its existing commit/count/precondition/canonical-work API.
- The public Bank mutation-family journey proves executed and recovered
  publication postures, actual installed invariant work, and exact cleanup. A
  separate twin adds 128 unrelated graph records and preserves mutation work,
  changed/effect counts, and exact-zero publication canonical/SHA work.
- QA corrected seven execution/Bank tests that previously asserted a fresh
  receipt and a recovered receipt were structurally identical. They now assert
  authoritative commit equivalence independently from terminal provenance.
  Query execution is green at 414/414, all Bank server test binaries are green,
  execution/publication strict Clippy is green, and execution/publication
  compile-fail doctests are green.
- C13-5 is proved for both read and mutation publication. C11 remains open only
  because the generic monolith obligation outcome/envelope families still sit
  behind the synthetic predecessor executor and must migrate with their exact
  owner rows before their publication claims can be trusted.

### 2026-08-03 Phase 5 Authority Retirement, Facade, And Documentation Closure

This snapshot supersedes the remaining-open conclusions in the two publication
snapshots above and closes the covered Phase 5 implementation.

- Installation owns the sealed graph-obligation set and the read-only adoption
  projection over that evidence. The Host audience contains no implementation:
  its facade is a flat re-export of the Installation-owned inspection function
  and types. The boundary checker rejected the first local Host wrapper during
  QA; the implementation was moved to its correct owner before closure.
- The external Host consumer declares and installs a real query, observes one
  `GraphRead` obligation owned by Relational with a `GraphReadProduct` terminal,
  and proves that inspection and executable admission bind the same canonical
  read-graph digest. Host has no dependency on the monolith.
- The monolith graph-obligation authority tree, synthetic selection-backed
  executor, dispatch outcomes/envelopes, manual invariant-pack callbacks,
  no-op composition authority, application-level orchestration alias, and
  specialized Consumer Kit registration/execution proof workspace are absent.
  A permanent certification test checks their paths and representative symbols.
- Generic monolith workspace reads and graph composition remain present and
  green. They accept no manual invariant callback and are explicitly documented
  as unrelated non-application features rather than alternate 9.16.1 authority.
- The canonical graph-progression document owns the cross-surface mental model
  and migration table. AI README, authoring, provider-session, invariant,
  support, workspace, execution, and runtime-surface documents agree with that
  topology. The obsolete authority-capable Consumer Kit document is removed;
  19 changed Markdown files have zero broken relative links.
- Full monolith tests pass with 2,561 unit, 320 installed-world, 37 bridge, and
  22 public-bridge tests. Installation passes 151 tests, admission 94,
  execution 414, and publication/Host/certification suites and doctests pass.
  The complete Bank workspace and all 78 Worth UI Query-binding tests are green.
- Strict warning-denied Clippy passes for the monolith plus Installation,
  Admission, Execution, Publication, Host, and Certification. Road 1 boundary,
  generated-context, formatting, diff hygiene, and dirty-file composition
  checks pass. QA split an inherited touched 415-line preview mutation module
  into 304-line caller operations and a 116-line symbolic-target owner; all 347
  dirty Rust files are now at or below 400 lines unless explicitly allowlisted.
- The repository-wide line-cap command still fails on 105 tracked,
  unallowlisted baseline files outside this change set. An exact path
  intersection reports zero dirty failures, so that repository debt is not a
  Phase 5 closure condition. Neither an allowlist expansion nor unrelated
  feature deletion is used to conceal it. The unchanged debt is distributed
  across the Query monolith (53), Runtime Bridge (16), Signal (15), Server (3),
  Store (3), Geometry (3), Signal WASM (3), Query destination packages (2),
  Worth UI (2), Foundational (2), Harness (1), Math (1), and the Bank workspace
  (1).

### Milestone 9.16 Runtime Phase 7.3 Handoff Record

Phase 7.3 inherits these earlier guarantees unchanged:

- **9.9:** the five installed obligation kinds, exact lower-owner routes,
  selection basis, resource/effect posture, terminal requirements, typed
  denials, and bounded lookup;
- **9.10:** graph-read requirement derivation, inventory matching, structural
  cost, budget, typed denial, and read-only inspection;
- **9.11:** downstream adoption through an honest public facade with exact
  source/path residue and no consumer-local authority construction;
- **9.15:** provider lifecycle, complete decision read-sets, proposed state,
  installed invariant execution, compare-and-commit, and exact cleanup; and
- **9.16 Phase 6 and Phases 7.1-7.2:** installed query identity, parameter and
  basis binding, ordering, projection, continuation, history, preview, live,
  result shaping, capability meaning, purpose/request/trusted-time binding,
  exact grant/path evidence, policy evidence, and revalidation.

Milestone 9.16.1 migrated exactly these authority surfaces:

1. query and operation obligation installation and sealed selection;
2. shared query admission and operation authorization into one bounded plan and
   managed session;
3. every application-query lane onto the session-owned graph-read terminal;
4. application-operation proposal, real installed invariant execution, sealed
   Relational validation, compare-and-commit, cleanup, and retry provenance;
5. read and commit publication onto terminal-derived, non-authoritative
   receipts; and
6. downstream obligation adoption onto Installation-owned read-only inspection
   through the re-export-only Host facade.

Phase 7.3 may consume the existing typed application-query front door and the
same branch-qualified plan, session, access decision, result, continuation,
live, and publication contracts. It may not add a disclosure-specific selector,
planner, raw graph executor, authorization lane, receipt, compatibility alias,
or branch default. Unrelated repository line-cap debt does not authorize Phase
7.3 to rebuild or bypass this progression.

## Finding Ledger

| ID | Impact | Finding | Status | Root correction |
|---|---|---|---|---|
| CQ1 | Critical | The monolith selection-backed executor can report blocking invariants or validators as executed without invoking their semantic owner. | **CLOSED** | The synthetic executor and its result envelope are absent. Execution completion now requires the exact owner terminal; mutation probes independently fail on skipped Relational validation and on a real invariant violation without state change. |
| CQ2 | Critical | Public execution-input, dispatch, planning-input, requirement-evidence, inventory, and review constructors allow callers to assemble authority-like products. | **CLOSED** | Authority transitions are private or sealed. Consolidated compile-fail cases deny raw planning/review, terminal construction, mutation escalation, and monolith plan import; public surfaces expose declarations and read-only inspection only. |
| CQ3 | Critical | Manual invariant-pack entry points and a no-op composition path permit invariant execution outside installed provider progression. | **CLOSED** | Manual graph/read invariant callbacks and no-op defaults are absent from production source. Installed operation sessions execute all real invariant phases and retain the Relational-minted validated candidate before commit. |
| CQ4 | Critical | Actual graph-obligation authority remains in the monolith rather than destination packages. | **CLOSED** | Installation, Admission, Execution, and Publication own the covered progression. The exact monolith obligation tree and application orchestration alias are absent; generic workspace read/composition features remain preserved and non-authoritative for application obligations. |
| CQ5 | Critical | The graph-read access facade publicly exported construction and review surfaces that belonged to installation/admission transitions. | **CLOSED** | Ordinary admission exposes declarations and read-only requirement/cost/review inspection only. Raw planning input and the review transition remain on the doc-hidden, dependency-governed one-way integration seam and cannot themselves mint execution authority. |
| CQ6 | Critical | Application-query access to the monolith admitted graph-read plan and its own direct Relational executor formed parallel execution products beside the destination plan. | **CLOSED** | All application-query lanes consume the destination admitted plan through the session-owned graph port; the application-query parallel plan field and direct graph handle are gone, and the ordinary monolith facade no longer exports its admitted plan. The unrelated generic workspace-read engine remains preserved under CQ4 rather than being falsely deleted here. |
| CQ7 | Critical | Application-query admission did not consume one canonical installed obligation selection before graph-read planning. | **CLOSED** | The shared application-query seam consumes the sealed installed selection through requirement, review, capacity reservation, admitted plan, session-owned read execution, and terminal completion before publishing any lane result. |
| CQ8 | Critical | Application-query and authorization observations could occur before the managed graph-work session that consumes their evidence. | **CLOSED** | Query, ordinary-operation, and capability admission now open the admitted session before principal currentness, grant/path, Bridge, Signal, or disclosure observation; every retained fact is session-bound and abandonment returns the reservation baseline. |
| CQ9 | High | Application operations could synthesize graph-read, touch, or invariant scope beside the installed obligation grammar. | **CLOSED** | Installation lowers the typed operation contracts into one sealed obligation set, and operation authorization consumes that set rather than reconstructing scope. Actual invariant execution and commit remain Phase 4 authority work under C9/CQ1/CQ3. |
| CQ10 | High | Earlier convergence documentation attempted to reopen closed milestones and ledger rows. | **CLOSED** | The uncommitted reopen/revoke edits were removed. Milestone 9.16.1 records the stronger guarantee append-only while historical statuses remain unchanged. |
| CQ11 | High | Historical consumer-kit/reference-consumer proof can demonstrate local authority construction rather than real public-facade adoption. | **CLOSED** | The specialized Consumer Kit authority workspace is absent. Installation owns a read-only adoption projection, Host only re-exports it, and a real external Host consumer installs a declarative query and proves one obligation/planning basis without monolith access. |
| CQ12 | High | Existing Phase 7.2 lower-owner evidence was composed outside the new canonical obligation/session progression. | **CLOSED** | Phase 2 preserves the exact Relational, Bridge, and Signal evidence while observing and retaining it inside the managed session. Phase 4 completeness and mutation revalidation remain governed separately by C8/C9. |
| CQ13 | High | A broad green suite could conceal two paths, synthetic execution, or warm hashing if residue and structural counters are not independently enforced. | **CLOSED** | Permanent source/path residue counts exactly one Host inspection re-export and no retired monolith path; compile-fail cases deny parallel planning; mutation probes require owner execution; separate read, mutation, publication, unrelated-population, and fan-out twins retain exact-zero warm canonical/SHA work. |
| CQ14 | Critical | Plan/session/fact affinity could infer `"main"` or bind only runtime/snapshot/version, allowing equal-version evidence from another branch to substitute during Phase 2 admission. | **CLOSED** | The admitted plan, session, and decision facts consume the installed runtime's typed branch-qualified execution basis; equal-version foreign-branch evidence denies and production roots contain no default-branch authority. Mutation commit, retry, receipt, and publication affinity remain later C9/C11 work. |
| CQ15 | Critical | The Phase 6-to-admission bridge accepts legacy graph identities as strings and then panics while parsing them as canonical SHA-256, disconnecting monolith, public, and Worth UI feature paths. | **CLOSED** | Typed canonical digest identity is established at cold construction and carried through requirements, operation resolution, selectivity, and review without warm parsing. Query, Bank, and Worth UI Query-binding suites are restored before any 9.16.1 cutover. |
| CQ16 | Critical | Milestone 9.13.2's whole-monolith deletion requirement leaked into the earlier 9.16.1 specification and conflated removal of competing authority with deletion of unrelated feature surfaces. | **CLOSED** | Milestones 9.13.2, 9.16, and 9.16.1, the roadmap, and this ledger now define parity-gated semantic-surface migration, atomic authority cutover, exact predecessor retirement, and explicit scope amendment for broader discoveries. |
| CQ17 | High | Worth UI compiled while half of its Query-binding tests failed, allowing package-local proof to conceal downstream feature loss. | **CLOSED** | The real Worth UI Query-binding package is green at the safe checkpoint and remains a required parity gate for every affected read, continuation, live, projection, and receipt migration row. |
| CQ18 | High | Graph-work session identity construction materialized canonical digest text on the warm path. | **CLOSED** | Session and managed-run identities are distinct fixed-width values carried without canonical rendering; retry twins prove fresh identities and warm canonical-work evidence remains exact zero. |
| CQ19 | Critical | A successful application-query receipt could describe graph work without proving that the session-owned read ran or that its exact basis and graph reservation were released. | **CLOSED** | The private owner port returns an unforgeable session/plan/basis proof, and only the matching session can consume it into a public read completion carrying actual dependency evidence plus exact Relational-basis and graph-capacity release receipts. |
| CQ20 | Critical | The graph-work session carried a distinct managed-run identity without actually binding the lower mutation managed run that performed application-operation work. | **CLOSED** | The application-operation authority binds the exact lower run through the graph-work session; provider plans and terminal cleanup must match the operation, graph-work session, graph-work run, worker run, runtime, generation, branch, and provider identities. |
| CQ21 | Critical | Primary-provider invariant execution checked only locator closure and then reported success while the real Relational invariant engine ran later as an incidental commit side effect. | **CLOSED** | Relational owns a sealed validated-mutation candidate minted only after all three real invariant phases. Query retains that candidate at invariant execution and provider commit cannot proceed without consuming it; both owner-skip and real invariant-violation mutation probes deny without state change. |
| CQ22 | Critical | Mutation revalidation and idempotency inspection could open an implicit current/default snapshot, and provider index publication rebuilt the literal `main` branch despite a typed session branch. | **CLOSED** | Relational exposes exact branch snapshots; every authorization, capability, idempotency, invariant, before/after snapshot, commit, and index-publication step uses the session's typed branch. Cross-branch equal-version evidence and missing branches deny. |
| CQ23 | High | Relational validation occurred outside the provider compare-and-commit serialization window, so two unrelated mutations could validate at the same head and the second would abort instead of re-comparing and committing. | **CLOSED** | The provider serialization guard now covers comparison through sealed validation and commit. Relevant same-fact races remain stale, while unrelated mutations re-compare after the first commit and both succeed. |
| CQ24 | High | Combined Phase 4/5 ledger rows concealed whether mutation progression was complete independently of publication and residue work. | **CLOSED** | C12-P4/C12-P5 and C13-4/C13-5 are separate rows with phase-specific owners and evidence. Phase 4 closed independently first; the later Phase 5 snapshot closes publication and residue without rewriting that history. |
| CQ25 | Critical | The first Phase 4 green pass still allowed capability-operation refresh and provider idempotency lookup/index recovery to select implicit current truth instead of the managed session's exact branch. | **CLOSED** | Capability progression calls only the session-bound refresh; the obsolete implicit refresh is removed. Graph-work acquisition names the application branch, and every idempotency inspection/recovery call supplies an explicit branch whose exact snapshot and index head are used. A branch-hostile test proves another branch's idempotency record cannot substitute. |
| CQ26 | Critical | Worth UI installed a legacy `BlockingInvariant` named `measurement-allocation-integrity` without declaring invariant semantics or supplying an executor, so downstream adoption appeared complete only because the monolith converted selection into synthetic success. | **CLOSED** | The exact legacy registration is removed. Worth UI's real installed query/operation contracts remain the semantic source and all 78 Query-binding tests pass; production-source residue finds no downstream `WorthQueryDomainGraphObligationDefinition` construction. Phase 5 subsequently retires the broader synthetic executor and specialized Consumer Kit authority under CQ1-CQ4/CQ11. |

## Semantic-Surface Migration And Retirement Ledger

One row is one authority surface. Before its cutover, the predecessor is the
sole executable authority. After cutover, the destination is the sole
authority and only the exact predecessor capability becomes residue.

| Surface | Inherited contract and parity gate | Cutover and retirement | Status |
|---|---|---|---|
| Installed application-query obligation meaning | Preserve the existing installed query identity, read graph, authorization, basis, lane, continuation, live, resource, and cold-work contracts while deriving one sealed obligation set from those typed owners. | Integrated at the existing installed-query constructor; the distributed predecessor contracts remain semantic inputs but no parallel caller-authored obligation grammar exists. | **PROVED** |
| Installed application-operation obligation meaning | Preserve the existing installed operation authorization, graph-read, touch, effect, invariant, decision-fact, resource, and precondition contracts while deriving one sealed obligation set. | Integrated at the existing installed-operation constructor; covered authorization-time scope synthesis now consumes the installed set. Later owner execution retires per Phase 4 row. | **PROVED** |
| Shared application-query admission seam | Preserve every current, continuation, historical, preview, and live admission denial and lifecycle while adding one installed selection, support, budget, capacity, sealed plan, and managed session before decision observation. | The shared admission seam is cut over atomically; lane-specific semantic execution remains a declared Phase 3 predecessor until its own parity row closes. | **PROVED** |
| Shared application-operation authorization seam | Preserve Phase 7.1-7.2 principal, ability, capability, precondition, purpose, request, time, exact-path, and revalidation meaning while binding it to one selected plan and managed session. | Authorization admission is cut over atomically and carries the session into the existing mutation handoff; proposal, invariant, and commit authority remain Phase 4 work. | **PROVED** |
| Application-operation proposal, installed invariant, and compare-and-commit progression | Preserve ordinary and capability authorization, proposal, invariant violation, relevant/unrelated conflict, cancellation, failure, response-loss, retry, cleanup, branch, and warm-work behavior through the real consumers. | The Phase 2 session binds the lower managed run as its exact worker, retains the complete decision read-set, and owns provider progression through a sealed Relational validated candidate and serialized commit. The predecessor worker remains a one-way semantic executor rather than a competing session, proposal, invariant, or commit authority. | **PROVED** |
| Application-query terminal publication | Preserve the governed disclosed result, omission, query identity, basis, lane, result, work, and terminal-release inspection behavior while making publication a non-authoritative destination product. | The publication package consumes the one sealed admitted disclosed result, derives one private-constructor publication receipt and inspection view from its actual read terminal, and retains no executor. The real Bank host journey, hostile constructor doctest, and unrelated-authority scale twin prove the cutover. | **PROVED** |
| Application-operation commit publication | Preserve commit identity, precondition, changed/effect count, response-loss, idempotent retry, branch, invariant-work, cleanup, and warm-cost behavior without equating a recovered commit with fresh execution. | Execution seals fresh commits only after exact session cleanup and distinguishes recovered prior commits from current retry inspection. Publication consumes that terminal into a non-authoritative receipt; the Bank facade, full mutation families, recovery hostility, constructor doctest, and unrelated-population scale twin prove the cutover. | **PROVED** |
| Worth UI legacy domain-package obligation registration | Preserve every real Worth UI installed query/operation, graph-read, projection, live, refresh, workflow, and resource contract while removing the unowned `measurement-allocation-integrity` claim. | Worth UI no longer registers the legacy `BlockingInvariant`: the declaration had no invariant contract or executor and could only reach the selection-backed monolith executor. Its real installed operation contracts continue to derive destination-owned graph work, and all 78 Query-binding tests pass after the exact removal. | **PROVED** |
| `worth-query/src/runtime/mutation/graph_composition/obligation/` | Preserve every covered 9.9 obligation kind, selector, owner route, denial, effect, receipt, preview/branch posture, and cost guarantee through real mutation consumers. | All covered kinds are installed, selected, planned, and completed by destination owners. The exact tree and empty directory residue are absent; generic graph composition remains present. | **PROVED** |
| Selection-backed obligation executors | Every executable kind reaches its real semantic owner and mutation probes fail when owner execution is skipped. | Owner terminal evidence replaced synthetic completion atomically; the fake executor, dispatch, outcome, and envelope symbols are absent and owner-skip probes deny. | **PROVED** |
| Manual invariant-pack callbacks and no-op default composition | Every supported invariant family produces the same lawful success/violation and consequential state through installed provider-session execution. | Installed provider-session execution proves lawful and violating paths; graph/read callback entry points and no-op defaults are absent. | **PROVED** |
| Application-query proof-like graph-read planning constructors and executable review | Preserve 9.10 requirement, inventory, cost, budget, denial, and inspection meaning without caller-mintable authority. | Raw planning and review transitions are hidden behind the one-way integration seam; the ordinary admission facade retains only non-executable requirement, cost, and review inspection. Compile-fail consumers prove the retired imports stay unavailable. | **PROVED** |
| Application-query-facing monolith admitted graph-read plan export | Current, continuation, historical, preview, and live lanes retain exact result, lifecycle, receipt, and warm-cost behavior without giving public consumers another executable plan. | The ordinary monolith facade no longer exports its admitted graph-read plan. Destination execution and the preserved generic workspace-read engine may lower internally through the hidden integration seam, which mints no public authority. | **PROVED** |
| Application-query graph-read plan authority and raw Relational execution | Preserve Phase 6 identity, parameter, basis, ordering, projection, continuation, history, preview, live, and result contracts. | Result shaping remains in application query, while all lane families consume the destination plan and can reach raw graph mechanics only through the session-owned port. The application-query parallel plan product and direct graph handle are absent. | **PROVED** |
| Generic monolith workspace-read engine | Preserve generic workspace-read declarations, planning, execution, receipts, tests, and internal adapters that are not application-query authority. | No Phase 3 cutover or retirement is authorized. Its ordinary admitted-plan export is narrowed, but the engine remains present unless a later named semantic-surface migration proves parity. | **N/A** — preserve |
| Pre-session authorization observation | Preserve Phase 7.1-7.2 principal, purpose, trusted-time, exact-grant/path, negative-fact, policy, and revalidation outcomes. | Each shared query/operation observation family is session-bound and its pre-session entry is gone; Bank and hostile twins pass. Later mutation revalidation is a Phase 4 terminal concern. | **PROVED** |
| Consumer Kit graph-obligation surfaces | Preserve 9.9/9.11 public adoption and inspection value while forbidding local authority construction. | Installation owns read-only installed-obligation adoption; Host re-exports it without behavior. The specialized Consumer Kit registration, execution proof, support pin, local ceremony, and residue manifest are absent. | **PROVED** |
| Facade adapters and aliases | Preserve the ordinary caller workflow and source behavior; an adapter must lower one way into the destination and mint no authority. | Declaration/Host facades retain the ordinary workflow as one-way re-exports. Boundary enforcement rejected and eliminated a local Host wrapper; residue and dependency checks find no alias that mints covered authority. | **PROVED** |
| Unrelated `worth-query` monolith responsibilities | Full Query, public declarative, Worth UI, and Bank suites prove no unsupported feature deletion or semantic drift. | No cutover or retirement is authorized by 9.16.1; broader findings require an explicit phase amendment or successor milestone. | **N/A** — preserve |

## Evidence Selection Policy

Evidence is selected from the causal ledger rather than multiplied across every
category and input combination:

1. one positive public transcript for each materially distinct terminal;
2. one one-axis negative for each independent authority or affinity dimension;
3. one interaction case for each composition rule that combines dimensions;
4. one mutation probe for each mechanism whose bypass could create false
   success;
5. one independent scale twin for each input axis that could widen warm work;
6. one lifecycle matrix shared across terminal outcomes where the ownership
   contract is identical; and
7. one migration inventory that names every predecessor, destination, covered
   consumer, parity proof, cutover, and exact authority residue.

Security, architecture, correctness, privacy, lifecycle, performance, DX, and
other risk categories are considered when they can invalidate a ledger row.
They do not create a fixed Cartesian test matrix.

## Closure Rule

Milestone 9.16.1 closes only when C1-C15 and C.L are `PROVED`, every high or
critical finding is `CLOSED`, every covered migration row has parity and an
atomic cutover or a justified integration result, every retired predecessor is
absent, and unrelated Query, public, Worth UI, and Bank behavior is green. The
hostile external consumer must demonstrate exactly one obligation and
graph-read planning authority path, while the public docs and permanent
enforcement agree with production topology. Closure adds a handoff to
Milestone 9.16 Runtime Phase 7.3; it changes no historical milestone status.
