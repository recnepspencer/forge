# Runtime Phase 8 Finish Plan

**Status:** **OPEN — hostile QA reopened F1-F6A on 2026-08-08.**
The earlier F6A closure is historical evidence, not current authority. The
current checkout still contains compile-time contract collapse, facade escape,
copied publication truth, non-exact outbox observation, hidden ordinary-lane
work, string identity authority, and proof fixtures that cannot support the
claims made for them. Undo and redo remain compiled but provisional; this plan
neither accepts nor redesigns their product semantics.

**Opened:** 2026-08-07 after the correction-slice implementation was reviewed
against the current tree.

**Supersedes:** the deleted `milestone-9.16-runtime-phase-8-correction-plan.md`.
That document is not an implementation authority and must not be restored.

**Related evidence:** the
[Phase 8 closure ledger](./milestone-9.16-runtime-phase-8-closure-ledger.md) is
an evidence archive. Its historical `CLOSED` and `PROVED` labels do not override
this plan's current-state classification or exit conditions.

## 0. Current Correction Authority

This section governs the reopened implementation. Later sections preserve the
original destination, requirements, and audit history, but every later
`CLOSED` or `PROVED` statement is historical until the corresponding correction
row below closes from a source-bound final tree.

### 0.1 Adversarial constraint

Phase 8 must remain correct when a caller searches every accepted compatibility
facade, two operations share field names across different entity/aspect axes, a
persisted outbox row changes after commit, two provider runtimes reuse equal
local ordinals, a protected fact influences an internal decision, and ordinary
operations scale mutation breadth without requesting aftermath work. Invalid
states that are statically knowable must be unrepresentable; runtime checks are
reserved for owner truth unavailable before execution and for corrupt or
reconstructed artifacts.

### 0.2 Authority lock

- Relational remains the sole canonical lineage, history, head, commit, and
  prior-truth authority. Query may carry Relational-issued identities but may
  not infer or reconstruct their meaning.
- Runtime Bridge receives no lineage authority. It transports existing typed
  references and performs no lineage interpretation.
- Generic wire identity, protocol version, and compatibility-window vocabulary
  belongs in `worth-foundational` as descriptive cross-runtime meaning. Query
  owns only an application's typed association to that meaning; the external
  owner independently declares what it accepts.
- Publication owns copied, closed descriptions. Retaining an execution receipt,
  execution enum, raw session identity, pre-image, outbox bytes, or authority
  binding behind a private field is not publication containment.
- Undo and redo remain reachable only through `provisional_aftermath`. They
  create no accepted Phase 8 closure evidence and no second history authority.

### 0.3 Dependency-ordered correction batches

| Batch | Required production correction | Cutover and deletion | Closure evidence |
|---|---|---|---|
| **C0** | Reset governing status and install this correction ledger. | Old closure labels become historical; no historical green run is reused. | Every hostile finding maps to one open row, owner, proof, and later batch. |
| **C1** | Replace the execution-root wildcard facade with accepted semantic-owner exports. | Provisional aftermath, public assertion helpers, and always-deny readmission functions disappear from accepted paths. | Positive provisional import; negative `primary_graph` and `installed::domain_computation` imports; opaque wire identity cannot substitute for a receipt. |
| **C2** | Author each operation as one typed semantic definition whose external-effect and aftermath choices transition from undecided to explicit none/declared. | Flat per-operation external-effect/aftermath member insertion ceases to be the ordinary authoring path; runtime cardinality remains defensive owner validation, not the ordinary public guarantee. | Compile-pass zero/one twins, omission and duplicate compile failures, direct raw-member resolver denials, and no test-only authority constructors in accepted C2 evidence. No public reconstruction door exists, so this batch must not fabricate an initial-install/reinstall corruption journey. |
| **C3** | Add Foundational protocol identity, exact produced version, consumer compatibility window, and typed unsupported posture. | Query-owned generic wire vocabulary and rail `String`/`&str` identity disappear. | Frozen v1 corpus, legitimate v2 coexistence, downgrade/retirement denial, separate-process hostile rail, module-move journey through declaration/install/outbox. |
| **C4** | Observe the outbox at the exact Relational commit and bind causal identities to runtime, record, commit, correlation, and predecessor. | Current-head partial validation and stale handle-record redispatch disappear. | Every persisted field corruption, cross-runtime equal-ordinal twin, exact record proof, real initial-vs-retry attempt comparison, structural owner-read counters. |
| **C5** | Carry exact entity/aspect/field pre-image locators through declaration, installation, and retention; check demand before constructing a mutation footprint. | Bare field-name contracts, caller-authored footprint substitutes, and eager ordinary-lane footprint construction disappear. | Same-name cross-axis denials, real footprint composition matrix, exact work counters, breadth twins proving ordinary zero work. |
| **C6** | Store only publication-owned closed projections and exhaustively map disclosure-admitted owner truth. | Execution receipt wrapping, execution-type inspection, raw identities, hardcoded recovery truth, unsafe derived `Debug`, and incomplete boundary evidence disappear. | Complete-surface noninterference, decision-relevant protected-fact twins, privacy compile proof with all fields supplied, exhaustive recovery mapping. |
| **C7** | Replace raw session strings with an owner-minted application-attempt affinity and make every phase field private to its semantic transition owner. | Reconstructible `pub(super)` bags and string-key lookup authority disappear. | Cross-session substitution, wrong-phase progression, cleanup fault matrix, production initial/retry routing, composition review. |
| **C8** | Rebuild the courtroom and closure record from the final source state. | Theatre tests, vacuous residue scans, stale counts/symbols, and prose-only verification claims disappear. | Deterministic source fingerprint, exact commands/results, mutation recipes, full constitutional and standing verification. |

Only one production batch advances at a time. Each batch runs an implementation
boundary review, root-cause implementation, requirement/evidence QA loop, test
honesty audit, and dirty-set composition/line-cap audit before the next batch is
admitted.

#### C2 batch result (2026-08-08)

C2 is proved for its batch boundary; C8 final-source rebinding remains open.
Operation authoring now seals explicit external-effect and aftermath choices in
typestate before schema registration. Query authoring advances through named
stages, and principal/closure roles use typed named requirements rather than
positional strings and booleans. Executable-operation installation and
capability-only graph-authority installation have separate typed routes. At
runtime, admitted capability access consumes the capability-only graph session
and opens a separately capacity-reserved executable-operation session carrying
the exact governed entity and installed-capability affinity before current
authorization facts are rebound.

The 49-case compile court rejects omission, duplicate slot selection, missing
query lanes, status-as-identity substitution, and reversed relation endpoints.
Direct installation-owner tests reject malformed raw member ambiguity; there is
no public reconstruction API, so no fake reinstall journey was added. A Bank
causal twin makes the former executable-install catch-and-fallback selection
fail. The full 609-test execution suite and complete Bank domain suite pass.
Mutation probes independently reopened every new compile attack, both raw
ambiguity oracles, and the graph-authority source twin before restoration.

#### C3 batch result (2026-08-08)

C3 is proved for its batch boundary; C8 final-source rebinding remains open.
Foundational owns the version-free boundary protocol family, exact positive
version, consumer compatibility window, and typed predates/exceeds/retired
outcome. Query declaration, installation, canonical identity, committed outbox,
fresh owner restoration, and transport carry family/version unchanged and own
no support policy. Runtime Bridge is not involved.

Bank owns independent v1 and v2 producer projections with fully consumed frozen
corpora. The separate rail owns its `Current`, `V2Only`, and `V1Retired` startup
profiles and distinct decoders. Separate-process evidence proves v1/v2
coexistence, crossed-label and corrupt-prefix denial, exact retirement-floor
admission, future/predating/retired posture, raw invalid identity/version
deserialization, and unchanged remote ledger state. A normal Bank v1 operation
against a `V2Only` rail preserves `{ produced: 1, PredatesWindow }` through the
adapter, Query classification, and publication. The real
declaration-to-installation-to-outbox module-move journey proves Rust diagnostic
identity cannot substitute for portable protocol meaning. Foundational-owned
compile-fail doctests keep invalid static declarations portable across
worktrees; the Query compiler court retains private request-construction and
typestate attacks.

### 0.4 Destination additions

```text
workspaces/worth-query/crates/worth-query-declaration/src/application_schema/
  operation_definition/
    builder.rs               # typestate selection of every static slot
    contract_slots.rs        # private undecided/none/declared states
    definition.rs            # one sealed operation semantic unit
    registration.rs          # schema membership of the sealed unit

crates/worth-foundational/src/boundary_protocol/
  identity.rs                # canonical descriptive protocol name
  version.rs                 # exact artifact version
  compatibility_window.rs    # consumer-supported interval and retirement
  unsupported_version.rs     # typed boundary outcome

workspaces/worth-query/crates/worth-query-execution/src/
  facade.rs                  # accepted exports enumerated by semantic owner
  domain_computation/primary_graph/provider/
    committed_dispatch_outbox.rs  # exact-commit owner observation
  domain_computation/primary_graph/application_attempt/provider_execution/
    application_attempt_affinity.rs # owner-minted lookup identity
    phase/                         # private consuming progression values

workspaces/worth-query/crates/worth-query-publication/src/application_aftermath/
  boundary_evidence.rs       # completed portable description only
```

### 0.5 Batch gate

A correction row moves to `PROVED` only when the production route derives the
claim, the displaced route is gone, the negative and positive twins fail/pass
for the intended causes, a named mutation would turn the proof red, affected
downstream rows have been reopened, and verification is bound to the current
source fingerprint. Broad suite success cannot override a missing focused
proof.

### 0.6 Dirty-set composition blockers discovered by the C1 gate

The C1 quality gate inspected every advisory candidate in the scoped dirty Rust
set rather than treating the facade diff as the whole product. The following
defects are current Phase 8 work, not advisory debt that may be ignored. They
remain open under the batch that owns their root cause; C1's facade result does
not close them.

| Owning batch | Dirty-set defect that must close | Required correction and proof |
|---|---|---|
| **C2** | Installation chooses the capability-only graph-authority route by catching `MissingProgram` and budget denials from full executable installation. | Resolve shared declarations explicitly, then compile executable contracts and capability graph authority through separate typed doors. Prove identical capability authority with and without unrelated executable members and prove executable corruption cannot select the graph lane. |
| **C2** | Query authoring repeats nine-to-ten positional arguments and principal-binding closure decomposes named meaning into strings and booleans. | Add staged query-definition authoring plus named principal/field requirement values and posture enums. Compile attacks must reject omitted axes and positional role/posture substitution. |
| **C5** | Aggregate projection is a 197-line mixed-phase coordinator spanning validation, cache protocol, bounded scan, work accounting, arithmetic, scope recording, and publication. | Introduce sealed validated-plan, cache-result, and completed-scan values while preserving one work-accounting owner. Cache-miss, overflow, and budget mutations must fail focused proofs. |
| **C7** | Provider registration, entity resolution, capability/delegation observation, admission, revalidation, and delegation activation pass reconstructible bags or independent authority tuples. | Replace them with owner-minted affinity/context values and private consuming transitions. Cross-runtime, cross-snapshot, cross-plan, cross-session, cross-branch, and wrong-phase compiler attacks must fail. |
| **C7** | Capability and provider-effect lowering maintain parallel mutable semantic collections whose indices may drift independently. | Use private accumulators that atomically add a rule with its paths and an effect with its step, intent, and emission. Mutate each enum arm and insertion order independently. |
| **C8** | The `worth-proof` public-producer certification uses file-wide substring heuristics, and sealed-minting evidence duplicates its fixture inventory across independently editable lists. | Use item-scoped parsing or an AST, one typed semantic case catalog, independent fixture-directory completeness, and negative fixtures with unrelated impls and wrong-type `Self` producers. |
| **C8** | `tests/support/milestone2` and `transactions/logic` are ticket/bucket topology rather than stable semantic ownership. | Rehome proof evidence by sealed-minting responsibility and Relational transaction code by planning, admission, staging, validation, and footprint ownership; rerun boundary and composition enforcement. |
| **C8** | Recovery inventory converts a named row back into eight positional arguments; a production-named comparison admission module contains only item-level test code; the compensated fixture ignores its posture argument; a Bank test combines unrelated contract families. | Restore named row construction, either create a real production admission route or move the whole module to test support, remove or implement the compensation variant, and split Bank scenarios by independently failing contract. Mutation of each advertised variant must affect its owning proof. |

## 1. Purpose

This is the one implementation reference for cleaning up and honestly finishing
Runtime Phase 8. It answers four questions without requiring another
architectural excavation:

1. What the current implementation genuinely establishes and must preserve.
2. What exists in code but is not accepted as settled product design.
3. What defects had to close before Phase 8 could honestly finish.
4. In what order those defects must be corrected and proved.

The old correction plan became unsuitable because it mixed audit history,
implementation instructions, and completion claims. In particular, it treated
the implemented undo and redo lane as settled architecture even though the
product decision is to reconsider that lane in a separate milestone.

This plan does **not** require a hard deletion of the undo and redo code. It
requires honest containment: that code may remain and must continue to compile,
but it may not close this plan, expand the public contract, introduce a second
history authority, or constrain the later milestone's design.

## 2. Source Of Truth And Precedence

For this cleanup, evidence is read in this order:

1. the engineering constitution and boundary enforcement;
2. the owning crate's current production code and manifests;
3. this finish plan;
4. the Phase 8 specification, as amended by this plan's scope lock;
5. focused tests and the historical closure ledger;
6. agent transcripts and implementation reports.

Claude's handoff is useful evidence of intent and work performed. It is not
proof that the current checkout passes, that a test proves the claim named for
it, or that a design decision is accepted. Every completion claim must be
re-established from the current tree.

## 3. Product Decision Lock

### 3.1 Phase 8's accepted finish

Phase 8 finishes the following product foundation:

- an application operation owns one compiled aftermath contract;
- one schema-declared typed external emission is bound to that contract;
- the exact bounded wire projection is co-committed with the ordinary mutation;
- a real external owner receives and independently decodes that projection;
- dispatch posture is derived from distinct, predecessor-linked causal events;
- recovery handles carry owner-derived binding and fresh runtime authority;
- exact pre-mutation truth demanded by an installed contract is retained or the
  commit fails closed;
- Relational remains the only canonical commit-history and entity-lineage
  authority;
- publication exposes only sealed runtime results and cannot manufacture a
  recovery, dispatch, or history decision;
- ordinary operations with no aftermath work retain their zero-work posture.

### 3.2 Undo and redo are provisional, not deleted

The current undo and redo implementation is classified **PROVISIONAL**.

That means:

- its files, tests, and types may remain in the tree;
- compatibility edits needed to keep the workspace compiling are allowed;
- defects that let it bypass Relational, runtime ownership, or supported-feature
  authority must still be fixed;
- it is not acceptance evidence for this plan;
- no new public promise, facade dependency, feature document, or roadmap claim
  may present it as settled;
- no current cleanup slice may deepen its semantics merely because the current
  implementation already chose a shape;
- a future, separately governed **Query Undo/Redo Semantics milestone** decides
  whether to keep, revise, or remove it.

The later milestone must re-decide at least: eligible operations, inverse versus
compensation semantics, exact occurrence-level causal meaning, current-head
policy, divergence behavior, redo invalidation, idempotency, retained-input
requirements, authorization re-entry, public DX, and interaction with the
cross-runtime branching roadmap.

Two pieces remain useful outside that unsettled product decision:

- `ExpectedBranchHead` is a general Relational compare-and-commit precondition
  when it is owned and checked by Relational; its use by current redo policy is
  provisional.
- commit-time pre-image retention is a general aftermath foundation. Capturing
  exact prior truth may close here; claiming that undo restores it correctly
  may not.

### 3.3 Branch and tree behavior stays elsewhere

Branch selection, branch-local inversion, branch-shaped redo, merge interaction,
rebase, history navigation, and durable cross-runtime recovery remain owned by
the [cross-runtime merging and branching roadmap](../cross-runtime/merging-and-branching-roadmap.md).
Phase 8 creates no placeholder, dormant authority, or Query-local substitute.

## 4. Current-State Ledger

This section is the governing status summary. The historical ledger may provide
supporting detail, but it may not contradict these rows.

### 4.1 Accepted foundation to preserve

| Area | Current result | Finish posture |
|---|---|---|
| Compiled aftermath | Aftermath is compiled into the installed operation contract rather than installed through a free host-authored lane. | **ACCEPTED** |
| External declaration | External-effect posture is derived from the operation's typed emission lane; the duplicate aftermath declaration was removed. | **ACCEPTED** |
| External payload carriage | Query carries the declared version-free protocol family, exact positive produced version, effect identity, bound, exact bytes, and correlation into a co-committed outbox row and owner-sealed dispatch request. | **ACCEPTED** |
| External topology | Bank has a real separate TCP rail with an independent decoder and owner ledger; healthy and hostile semantic journeys pass. | **ACCEPTED** |
| Retention derivation | Pre-image demand is derived from the installed contract and retention failure aborts before commit. | **ACCEPTED** |
| Retention field affinity | Relational supplies an opaque validated mutation footprint; retention requires the exact demanded field on the exact record and aspect. | **ACCEPTED** |
| Recovery substrate | Linear-resource, freshness, boundary-readmission, exact-handle authority, and runtime-owned lifecycle carriers replace the hand-rolled lanes. | **ACCEPTED** |
| History ownership | The Query-local mutable lineage chain was removed; Relational supplies commit identity, parents, head, ancestry, and publication. | **ACCEPTED AND NON-NEGOTIABLE** |
| Runtime Bridge | No ordinary Phase 8 legality, currentness, or history authority was added to Bridge. | **ACCEPTED AND NON-NEGOTIABLE** |
| Publication | Publication owns closed aftermath, external-effect, and recovery-support descriptions sourced only from sealed execution outcomes. | **ACCEPTED** |
| Composition and line caps | Named commit/publication responsibilities satisfy the scoped function advisories and the complete dirty Rust set satisfies the 400-line guard. | **ACCEPTED** |

### 4.2 Provisional residue

| Area | What exists | Rule until the successor milestone |
|---|---|---|
| Undo admission and progression | Move-only admissions, recorded-inverse and compensation paths, proved-undo evidence. | Keep compiling; do not claim product acceptance or add semantics. |
| Redo admission and progression | A retained-input continuation, redo intent, fresh admission, and current-head checks. | Keep compiling; do not treat the current policy as final. |
| Undo/redo causal facts | Query prepares operation-semantic facts that are co-committed through Relational. | May not become a Query history API; occurrence semantics remain unsettled. |
| Undo/redo Bank facades and tests | Bank-specific journeys exercise the current design. | Regression evidence only, not specification authority. |
| `Inverts<Original, Authority>` | Relates action kinds through a proof gate. | Does not prove which concrete committed occurrence another occurrence inverted. |

### 4.3 Finish-blocker status

The table preserves each entry defect while naming the correction that now owns
its closure. Detailed attacks and mutation evidence live in the closure ledger.

| ID | Entry defect | Closure | Result |
|---|---|---|---|
| **F8.1** | Supported `test-support` builds exposed registry authority. | F1 removed the feature and every public registry/slot/terminal lane. | **CLOSED** |
| **F8.2** | Per-operation external-effect and aftermath cardinality was ambiguous. | F1 enforces zero-or-one before identity and rejects malformed internal ambiguity defensively at installation. | **CLOSED** |
| **F8.3** | Copyable axes-only `BindingMatch` overclaimed dynamic exactness. | F1 removed the artifact; exact comparison is inseparable from its owner continuation. | **CLOSED** |
| **F8.4** | Application binding generation was mislabeled as a branch version. | F1 renamed the axis and its denial without inventing Query branch vocabulary. | **CLOSED** |
| **F8.5** | Rust `type_name` was used as wire identity. | C3 installs and transports an application-declared protocol family plus exact version while keeping Rust identity diagnostic-only. | **CLOSED AFTER C3** |
| **F8.6** | Outbox evidence stopped before committed persistence. | F3 reads the committed row through a fresh provider-sealed owner observation. | **CLOSED** |
| **F8.7** | Pre-image retention lacked exact changed-field affinity. | F4 consumes Relational's opaque exact mutation footprint and fails closed on every mismatch. | **CLOSED** |
| **F8.8** | The external rail lacked hostile semantic cases. | C3 rejects wrong effect, protocol family, version/window, bound, and bytes before owner admission, with healthy v1/v2 twins. | **CLOSED AFTER C3** |
| **F8.9** | Provider commit, emission, dispatch, and observation reused causal identity. | F3 gives every stage a distinct typed identity and exact predecessor. | **CLOSED** |
| **F8.10** | The required publication owner was absent. | F5 makes publication the sole owner of closed aftermath and recovery descriptions. | **CLOSED** |
| **F8.11** | Commit orchestration and facade composition were dishonest. | F4/F5 established the authority topology; F6A decomposed the outer commit and provider progression into private move-only phases and re-ran fatal composition evidence over the complete corrective scope. | **CLOSED AFTER F6A** |
| **F8.12** | Documentation, fixtures, and standing verification overstated closure. | F6A repaired false facade reachability, the wrong-reason doctest, and the unsupported safe-retry identity claim, then re-ran the exact standing set from the corrected tree. | **CLOSED AFTER F6A** |

## 5. Authority Model

### 5.1 Placement decision tree

For every new or moved type, ask in order; the first `yes` wins:

1. Does it decide whether an operation is legal? Put the generic law in
   `worth-proof`, with a concrete Query owner facade where the operation occurs.
2. Would it mean the same thing crossing a boundary into another runtime? Put
   only that portable description in `worth-foundational`.
3. Does it need a clock, counter, live table, provider transaction, or `Drop`?
   Put it in the owning runtime.

Before concluding that vocabulary is absent, verify the prospective owner's
manifest can see the crate that already owns it.

### 5.2 Locked owners

| Meaning | Sole authority owner | What other layers may do |
|---|---|---|
| Declared operation, effect, payload wire identity, and aftermath meaning | Query declaration | Installation validates and compiles it. |
| Installed legal next-action contract | Query installation | Execution consumes it; hosts cannot rebuild it. |
| Generic proof, linearity, freshness-state, and boundary-readmission laws | `worth-proof` | Query wraps them in concrete owner types and supplies runtime evidence. |
| Live recovery registry, clock sampling, cleanup, and force termination | Query execution runtime | Tests observe through owner-controlled fixture APIs, not public registry capability. |
| Authoritative commit identity, ordered parents, branch head, ancestry, serialization, and publication | Relational | Query may co-commit admitted application-semantic facts but may not mirror history. |
| Authoritative entity lineage and truth correspondence | Relational | Query consumes typed owner evidence; it does not create another lineage vocabulary. |
| External effect result | External owner/rail | Query classifies only the returned observation and cannot infer completion from silence. |
| Portable description of an already-completed event or continuity fact | Foundational | It carries no current legality, head, or live owner authority. |
| Cross-runtime projection, mapping, and transport | Runtime Bridge | It cannot decide legality, currentness, parentage, or history. |
| Consumer-facing aftermath posture and disclosed next actions | Query publication | It projects sealed owner results; it mints no execution authority. |

### 5.3 Lineage and causality lock

The word "lineage" must not blur four different meanings:

- **canonical commit history** belongs to Relational;
- **canonical entity lineage** belongs to Relational;
- **operation-semantic admission** may belong to Query when a separately
  accepted product says one operation lawfully relates to another;
- **portable boundary description** may belong to Foundational only after an
  owning runtime has established the completed fact.

Runtime Bridge is never the missing authority. It may transport an admitted
projection when a real cross-runtime consumer exists. It may not create the
relation, choose a parent, decide the current head, or publish canonical
history.

The current provisional `undo-of` and `redo-of` facts are therefore neither a
Query lineage store nor Foundational history. Their product meaning remains for
the successor milestone to accept or revise.

## 6. Enforcement Standard

Every guarantee names the strongest honest enforcement rung:

| Rung | Enforcement | Use in this plan |
|---|---|---|
| **1** | Invalid state or call is unrepresentable. | Delete redundant parameters; move authority by value; separate phase types. |
| **2** | Value is unconstructable outside its owner. | Private fields/minting; sealed owner continuations. |
| **3** | Dynamic owner truth is checked at runtime. | Wire bytes, live clock, exact record/field mutation, external response. |
| **4** | Documentation only. | Never sufficient for a legality or authority claim. |

Dynamic values cannot all become distinct Rust types. Compile-time strength
means the compiler requires the owner-minted evidence or continuation that
contains those values; it does not mean replacing a necessary dynamic owner
comparison with `PhantomData` and calling it exact.

Specific rules:

- A supported feature must not make an authority-bearing item public.
- A test hook may inject data or observe an owner result; it may not grant a
  production capability that normal code lacks.
- A stable protocol family and its exact produced version are explicit declared
  protocol data, not `type_name::<T>()`; consumer support windows remain owned
  by the external consumer.
- A proof of a dynamic comparison may not be reusable for unrelated values of
  the same Rust type.
- If a later operation requires the result of an earlier one, progression must
  consume a private phase-specific value, not a bag of repeated fields.
- A runtime check is accepted only when the owner truth is necessarily dynamic
  and the checked values cannot be supplied through a second door.

## 7. Adversarial Courtroom

The courtroom is defined before the implementation slices. No slice closes by
adding only a happy-path unit test.

### Case A: supported-feature authority

Build a real external consumer with every supported feature enabled. Give it
two live handles A and B. It may observe fixture-safe posture for A, but it
cannot obtain the registry, name B's slot through A, force-terminate either
handle, choose a terminal kind, or enumerate live resources. The compile
boundary must hold under default and every supported feature set. The legacy
`test-support` authority feature must not exist or be reintroduced.

### Case B: ambiguous declaration

Author one operation with two different external-effect contracts, and another
with two different aftermath contracts. Both declarations fail before
canonical identity and installation. The positive twins with exactly one
member install and preserve deterministic identity.

### Case C: exact dynamic binding

Compare binding pair A successfully, then try to use the resulting artifact to
open pair B of the same axes type. The public surface makes that substitution
impossible or the owner continuation rejects it without accepting another
target parameter. A drift twin identifies the exact semantic axis.

### Case D: real committed outbox

Execute the Bank death-notification operation through the ordinary public
front door. After commit, obtain a fresh provider/Relational owner view and read
the outbox row. Independently assert effect identifier, version-free protocol
family, exact produced version, declared maximum, exact bytes, correlation,
outcome identity, and commit affinity. Mutating or omitting any one field must
fail the oracle.

### Case E: real external meaning

Send the committed outbox payload through the separate rail process. The rail's
independent decoder must ledger the exact estate, notice, and subject. Then run
hostile twins for wrong effect, wrong protocol family, predating/retired/future
version, crossed-version or malformed bytes, and dishonest bound. Each is
rejected before ledger admission; healthy v1 and v2 twins complete.

### Case F: causal predecessor ladder

For one dispatch, assert four distinct identities:

```text
Relational provider commit
    -> admitted/co-committed application emission
        -> dispatch attempt
            -> external-owner observation
```

Every edge names its exact predecessor. Lost response, acknowledgement without
completion, duplicate acknowledgement, timeout followed by completion, and
safe retry must not let an earlier identity construct or impersonate a later
posture.

### Case G: exact retained field

Read two fields on one record, mutate only field B, and demand prior truth for
field A. The commit denies. The positive twin mutates the exact demanded field
and retains its pre-mutation value. Repeat with two records sharing the same
slot to preserve the existing wrong-record protection.

### Case H: publication ownership and noninterference

Create publication only from a sealed committed/runtime outcome. An external
consumer cannot construct it from raw receipt fields, a wire identity, a
recovery slot, or a copied posture enum. A protected fact may influence an
internal aftermath decision without leaking through explanation, next-action
availability, causal shape, or identifiers. Runtime Bridge is absent from the
legality path.

### Case I: provisional-lane containment

Mechanical residue and dependency checks prove the provisional undo/redo lane
owns no chain, mutable head, parent selection, canonical publication, Bridge
authority, or Foundational upgrade. Public docs call it provisional. Its tests
remain regression tests, not exit evidence.

### Case J: ordinary-lane noninterference

With a live transport installed, commit an operation declaring neither external
effect nor recovery work. It creates no outbox row, performs no dispatch, mints
no handle, and records zero work in every Phase 8-specific counter.

## 8. Destination Topology

The following topology is the implementation destination. Names may change only
when the replacement preserves the same single responsibility and this plan is
updated first.

```text
workspaces/worth-query/crates/worth-query-declaration/src/
  application_schema/
    operation_contract_cardinality.rs   [create]
      # exactly-zero-or-one aftermath and external-effect member per operation
    effect_payload.rs                    [modify]
      # explicit stable external wire identity and bounded byte projection
    external_effect_authoring.rs         [modify]
      # binds declared effect, payload, wire identity, correlation, and bound
    aftermath_authoring.rs               [preserve]
      # one operation aftermath declaration; no external posture duplicate

workspaces/worth-query/crates/worth-query-installation/src/
  application_operation/
    contract_resolution.rs               [split if needed]
    contract_resolution/
      external_effect.rs                 [destination]
      aftermath.rs                       [destination]
      # resolution assumes declaration cardinality; no first-match semantics
  application_aftermath/                 [preserve]
      # installed operation-owned correction and external posture

workspaces/worth-query/crates/worth-query-execution/src/domain_computation/
  application_aftermath/
    recovery_handle/handle.rs            [modify]
      # owner facade; no supported-feature registry escape
    recovery_progression/binding_axis.rs [modify]
      # exact application-binding-generation name and denial
    external_effect/
      outbox.rs                           [modify]
        # committed effect/wire metadata and exact bytes
      identity.rs                         [modify]
        # distinct event identity types/derivations
      dispatch.rs                         [decompose]
        # orchestration only
      observation.rs                      [create]
        # external-owner result classification from attempt identity
      transport.rs                        [preserve]
        # carried record projection; no replacement payload parameter
  managed_run/recovery_registry.rs       [modify]
      # runtime-private lifecycle controls; fixture observer separated
  primary_graph/provider/
    session_lifecycle.rs                 [decompose]
    session_commit/
      preimage_retention.rs              [create/move]
      outbox_commit.rs                    [create/move]
      aftermath_causality.rs              [create/move]
      relational_commit.rs               [create/move]
      receipt.rs                          [create/move]

workspaces/worth-query/crates/worth-query-publication/src/
  application_aftermath/                 [create]
    mod.rs
    outcome.rs                            # accepted installed/committed posture
    recovery.rs                           # process-local recovery/support posture
    external_effect.rs                    # commit/attempt/observation projection
    access_and_disclosure.rs              # noninterference and disclosure
    boundary_evidence.rs                  # completed portable description only

workspaces/worth-query-bank-world/crates/bank-external-rail/src/
  protocol/                               [modify]
    payload.rs                            # explicit protocol identifiers
    notice.rs                             # independent semantic decoder
  server/ledger.rs                        [preserve]
    # external owner's decoded meaning and status
```

The provisional undo/redo files stay where they are for now. This plan creates
no destination topology for their final form; that is intentionally owned by
the successor milestone.

## 9. Ordered Finish Slices

| Slice | Scope | Closes | Depends on |
|---|---|---|---|
| **F0** | Documentation and status reset represented by this plan. | False governing plan and false closure posture. | None |
| **F1** | Authority surface and declaration determinism. | F8.1-F8.4 | F0 |
| **F2** | Stable external protocol contract and real rail closure. | F8.5, F8.8 | F1 |
| **F3** | Committed outbox proof and exact causal ladder. | F8.6, F8.9 | F2 |
| **F4** | Exact-field pre-image retention and commit composition. | F8.7, relevant F8.11 | F1 |
| **F5** | Publication owner, facade cutover, and noninterference. | F8.10, remaining F8.11 | F2-F4 |
| **F6** | Courtroom, documentation, and closure audit. | F8.12 and all reopened dependent claims | F1-F5 |

Only one slice is implemented and reviewed at a time. If a slice cannot be
reviewed as one coherent vertical change, split it before implementation and
update this table.

### F1 - Authority surface and declaration determinism

**Historical status (2026-08-07): CLOSED; CURRENT STATUS: REOPENED.** Courtroom A-C passed under default and all
supported features. The controlling evidence and mutation attacks are recorded
in the Phase 8 closure ledger's F1 section. F8.5's four deterministic real-rail
failures remain the explicit F2 entry condition.

1. Remove supported-feature public access to the registry, slot-addressed
   termination, and terminal selection. Replace tests with crate-private fixture
   helpers or observation-only fixtures.
2. Add semantic cardinality validation for `OperationExternalEffect` and
   `OperationAftermath` before canonical schema identity is minted.
3. Remove installation `find_map` as an ambiguity policy. Resolution may assume
   the declaration proved cardinality, while defensive failure remains explicit.
4. Rename `branch_version_ordinal` to application binding generation across
   binding, denial, tests, diagnostics, and docs.
5. Correct `BindingMatch` claims. Phase 8 must not depend on an axes-only token
   as exact dynamic authority. If the substrate is changed, prove public
   non-reuse; otherwise keep Query's comparison inseparable from its owner
   continuation and record the broader substrate work separately.

**Exit:** Courtroom A-C pass under default and all supported features. No public
facade exports test-only identity or registry controls.

### F2 - Stable external protocol and real rail

**Historical status (2026-08-08): CLOSED; superseded by current correction C3.**
The earlier F2 implementation fused protocol family and version into one wire
identifier. That destination is non-governing. C3 replaces it with Foundational
`BoundaryProtocolIdentity`, exact `BoundaryProtocolVersion`, consumer-owned
compatibility windows, and typed unsupported-version posture.

1. The payload declares a version-free canonical protocol family and a separate
   exact positive version; both participate in schema/installed identity and
   persist as distinct outbox fields.
2. Rust `TypeId`/`type_name` remain in-process checks or diagnostics only.
3. Query carries family and version unchanged; it neither negotiates nor owns a
   compatibility window.
4. The independent rail owns its accepted interval and retirement policy,
   selects the exact decoder by version, and rejects family/version/bound/bytes
   mismatches before ledger admission.
5. Frozen v1 and v2 producer corpora, crossed-label attacks, predating/future/
   retirement postures, malformed raw frames, and unchanged-ledger assertions
   are load-bearing evidence.

**Exit:** Courtroom E passes against separately spawned rail profiles. Moving the
Rust module path without changing the declared family/version does not change
wire behavior or canonical protocol meaning.

### F3 - Committed outbox and causal ladder

**Historical status (2026-08-08): CLOSED; CURRENT STATUS: REOPENED.** Dispatch and safe re-dispatch consumed a
fresh, provider-sealed observation of the committed Relational row. The seal is
private to the provider owner module, so production dispatch cannot rebuild it
from the receipt copy. Provider commit, co-committed emission, dispatch attempt,
and external-owner observation are distinct stage-typed events with exact
predecessors; public posture and the outer completion wrapper are opaque.

1. Replace the in-memory "persistence" proof with a real commit followed by a
   fresh owner read of the committed outbox row.
2. Give provider commit, admitted/co-committed emission, dispatch attempt, and
   external observation distinct event identities.
3. Seal each successor from its exact predecessor. A posture constructor may
   not accept a free identity bag.
4. Preserve indeterminate outcomes: missing/lost responses do not guess external
   completion. Safe retry remains rail-idempotent and owner-observed.
5. Split identity derivation, transport orchestration, and observation
   classification into their destination responsibilities.

**Exit:** Courtrooms D and F pass. The final QA loop caught and corrected an
initial crate-wide observation sealer: a receipt-copy substitution first kept
the behavioral courtrooms green, but after moving the seal into the provider
owner the same mutation fails to compile with `E0624`. Commit-id, version,
branch, runtime, later-head, ambiguity, malformed-field, predecessor, retry
ordinal, completion-opacity, and canonical-work attacks are pinned by the F3
closure ledger. F4 is now admissible.

### F4 - Retention fidelity and commit composition

**Historical status (2026-08-08): CLOSED; CURRENT STATUS: REOPENED.** Relational derives an opaque validated
mutation footprint from its invariant-validated merged plan. Query can ask only
whether that concrete owner value mutates an exact record/aspect/field; it
cannot author or reinterpret the footprint. Retention consumes admitted facts,
fails closed before commit, and its production-path wrong-field denial preserves
the Relational head. Provider handoff is an affine mutation-work/pre-image bundle
keyed by exact Relational commit identity rather than singleton completed slots.
The commit path is decomposed into named responsibilities and clears fatal
composition, line-cap, all-feature, boundary, and adversarial mutation gates.

1. Bind a demanded pre-image to the exact changed field on the exact mutated
   record, not merely a same-named observed field on a mutated record. Consume
   a read-only footprint derived by Relational from its invariant-validated
   merged plan; Query must not reinterpret the earlier worker batch as owner
   truth.
2. Preserve fail-closed behavior for missing, ambiguous, over-bound, and
   multi-segment demands.
3. Move retention, outbox preparation, aftermath-causality preparation,
   Relational commit, and receipt assembly out of the mixed commit orchestrator
   into named responsibilities.
4. Keep the orchestrator linear and small: prepare owner-derived components,
   perform one Relational commit, then seal the receipt from that result.

**Exit:** Courtrooms G and J pass. Exact-field, other-aspect, other-record,
endpoint-only, whole-mutation, fail-closed, no-write, wrong-commit, interleaving,
lost-response, and stale-bundle attacks are recorded in the F4 closure ledger.
These prove exact retained prior truth and ordinary-lane noninterference; undo
and redo remain provisional and this slice does not prove their product
semantics. F5 is admissible.

### F5 - Publication owner and facade cutover

**Historical status (2026-08-08): CLOSED; CURRENT STATUS: REOPENED.** Publication was described as owning the closed committed
aftermath, external-effect, and recovery-support descriptions. Its only sources
are one sealed commit receipt or one disclosure-admitted recovery inspection
view. The generic receipt no longer dereferences to or returns its execution
terminal, execution contains no publication helper, and Bank exposes the
publication-owned result while retaining its execution receipt privately for
recovery. Undo and redo remain compiled but are explicitly provisional and
outside the accepted publication/`primary_graph` contract.

1. Create `worth-query-publication/src/application_aftermath/` at the destination
   topology.
2. Accept only sealed installed/committed/runtime outputs. Publication never
   accepts a raw recovery slot, raw identity, caller-selected next action, or
   uncommitted causality.
3. Publish accepted outcome, recovery/support, and external-effect posture with
   inherited branch affinity and access/disclosure constraints.
4. Do not publish provisional undo/redo as a settled public contract. Existing
   compatibility exports may remain only when clearly provisional and not
   required by an accepted consumer facade.
5. Update audience facades so ordinary consumers do not import installation,
   execution, or publication internals.

**Exit:** Courtroom H, the real-rail fault matrix, protected-fact
noninterference, 13 compile-fail contracts, both mutation probes, the explicit
21-file composition audit, dirty line cap, all-feature check, clippy, boundary
check, and agent-context check pass. F6 is admissible.

### F6 - Closure audit

**Historical status (2026-08-08): CLOSED AFTER F6A; CURRENT STATUS: REOPENED.** The complete Query and Bank workspaces
pass
formatting, all-target/all-feature clippy, and all-feature tests. The root
`worth-proof` suite, boundary check, generated-context check, dirty line cap,
focused composition audit, facade/residue probes, and documentation-link audit
also pass. The closure audit found and corrected three evidence defects: one
Bank test-only needless double borrow exposed only by all-target clippy, five
provisional undo/redo compiler fixtures that failed at the removed accepted
facade instead of their intended authority boundary, and one stale public
authoring comment that described a removed duplicate external-effect axis.
The closure ledger records the exact final evidence and the initial
wrong-reason compiler-fixture failure.

The later requirement-level audit found that this closure was not admissible:
two mixed-level commit coordinators remained 148 and 205 lines long, a Bank
residue test confused occurrence anywhere in a facade file with export from the
accepted facade, a Bank redo doctest failed at a stale import rather than its
claimed substitution boundary, and no production-path proof compared an
original external dispatch attempt identity with its safe-retry successor.

### F6A - Commit-composition and evidence-honesty corrective

**Historical status: CLOSED; CURRENT STATUS: REOPENED.** F6A is a corrective continuation of F6, not a new accepted
feature slice. It preserves the F1-F5 authority destination. Closure was
admitted only after all of the following became true:

1. Private, move-only phase values separate commit preparation, managed-run
   admission, provider progression, and terminal completion. `entry.rs` and
   `progression.rs` remain same-level, small coordinators rather than bags of
   inline lifecycle work.
2. Provider progression separately owns session admission, attempt
   registration, proposed-state/invariant progression, and authorized
   idempotency/commit resolution. No raw identifiers or caller-authored proof
   fields replace the existing owner continuations.
3. Accepted-facade exclusion and provisional-facade reachability are proved by
   compiler evidence. Source residue remains supplemental and may not stand in
   for Rust reachability.
4. The Bank redo substitution doctest imports the provisional types from their
   real facade and fails on the intended call contract.
5. Initial dispatch and safe redispatch share one runtime-owned fresh-attempt
   operation. Owner evidence compares equal emission/correlation with unequal
   attempt identities; forcing ordinal reuse makes that evidence fail. The real
   Bank rail remains the consequential exactly-once twin.
6. The closure ledger records the reopening, the initial defects, every
   mutation probe, and the exact final command results. No historical green run
   is reused as proof of the corrected final tree.

**Exit:** focused owner and compiler evidence, real Bank rail journeys,
composition advisories over the complete F6A Rust scope, dirty line caps, both
full workspaces, root `worth-proof`, boundary enforcement, and generated-context
checks pass. A final completeness attack finds no unsupported `PROVED` or
`CLOSED` row before F6 and Phase 8 are reclosed.

1. Run the full courtroom from the current tree.
2. Reclassify every affected `Q8.*`/`R8.*` ledger row as accepted, provisional,
   open, or proved without collapsing those meanings.
3. Rename evidence whose boundary is narrower than its current name.
4. Update feature docs, crate docs, milestone status, and roadmap handoffs.
5. Prove provisional undo/redo containment mechanically.
6. Run the complete standing verification set and record exact commands, scopes,
   and results.

**Exit:** every F8 blocker is closed, all accepted requirements have independent
evidence, provisional claims remain visibly provisional, and no document treats
historical closure as current authority.

## 10. Must Preserve

- Query-agnostic schema crates and audience-facade dependency direction.
- Cert-only replay and reconstruction.
- `worth-*` never depending on `worthy-*`.
- Relational's sole canonical history and lineage ownership.
- Runtime Bridge as mapping/transport rather than legality or history.
- The real separate-process external rail and independent decoder.
- Typed fault classification that never guesses completion.
- Co-commit of ordinary mutation and dispatch outbox data.
- Fresh current authority before a recovery transition.
- Runtime-owned clock, registry, cleanup, and `Drop` lifecycle.
- Exact ordinary-lane zero-work posture.
- The 400-line default file cap and composition laws.
- The ability to replace the provisional undo/redo design without undoing the
  accepted Phase 8 foundation.

## 11. Documentation Deliverables

Phase 8 cannot close on code alone. F6 must leave:

- this plan updated to closed with an exact closure date;
- the closure ledger explicitly marked as evidence history plus current closure
  rows, not a competing plan;
- the Phase 8 specification banner pointing here and marking undo/redo sections
  provisional;
- the Milestone 9.16 spec and Query roadmap reflecting the same status;
- the cross-runtime roadmap retaining exclusive branch-aware ownership;
- the worth-proof hardening plan corrected so a former Query-local chain is not
  routed into Foundational as canonical history;
- feature documentation for the accepted external-effect, recovery, retention,
  and publication surfaces;
- a named successor handoff for the unaccepted undo/redo product decision.

Documentation may describe a capability as supported only when its public
consumer proof, facade, authority boundary, and tests agree.

## 12. Standing Verification

Every slice runs focused owner tests and the causally affected integration
proofs. Before F6 closure, run at minimum:

```text
cargo fmt --all --check
cargo clippy --workspace --all-targets --all-features -- -D warnings
cargo test --workspace --all-features
```

at both:

```text
workspaces/worth-query
workspaces/worth-query-bank-world
```

Also run from the repository root:

```text
cargo test --manifest-path crates/worth-proof/Cargo.toml
cargo run --manifest-path tools/boundary-check/Cargo.toml -- --root .
cargo run --manifest-path tools/agent-context/Cargo.toml -- check
scripts/ci/check_workspace_rust_line_caps.sh dirty
```

On Windows, invoke the line-cap script through the repository's available Bash
environment. Record any platform-specific substitution explicitly.

Required focused evidence includes:

- declaration cardinality unit and public authoring tests;
- recovery all-features compile boundary and cross-handle hostility;
- aftermath/external-effect compile-fail certification;
- real committed outbox readback;
- real Bank rail semantic and hostile-payload journeys;
- causal predecessor fault matrix;
- exact-record/exact-field pre-image tests;
- publication public-consumer and noninterference tests;
- ordinary no-aftermath counter proof;
- residue and dependency checks for Query history, Bridge authority, replay, and
  provisional-lane containment.

A broad green suite does not replace a missing boundary proof. A failing focused
test blocks any broader green claim.

## 13. Exit Condition And Successor Handoff

Runtime Phase 8 cleanup is finished only when:

1. F8.1-F8.12 are closed by the named courtroom evidence;
2. F1-F6 have been independently reviewed against the complete scoped diff;
3. publication exists in its owning crate and audience facades are honest;
4. the real Bank rail proves domain meaning and hostile rejection;
5. no supported feature widens authority;
6. Relational remains the sole canonical history and lineage owner;
7. all standing verification is green from the current tree;
8. accepted, provisional, and deferred claims are consistent across every
   governing document.

The successor **Query Undo/Redo Semantics milestone** begins from the explicit
provisional inventory in section 4.2. It may reuse code only after independently
accepting its authority model and courtroom. This plan hands it evidence, not a
pre-decided architecture.
