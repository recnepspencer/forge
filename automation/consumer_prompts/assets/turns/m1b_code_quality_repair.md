Fix the concrete structural findings for phase {phase.id}: {phase.title}.
Keep the repair limited to the named ownership, topology, file-shape, or public
surface defect and run focused verification.

Finish with:
`RUNNER_EVENT: {"event_type":"code_quality_repair_completed","payload":{"notes":{"done":["..."],"verification":["..."]}}}`

{contract}
