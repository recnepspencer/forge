Fix the admitted test findings for phase {phase.id}: {phase.title}. Prefer real
production paths, keep assertions meaningful, and run focused verification.

Finish with:
`RUNNER_EVENT: {"event_type":"test_repair_completed","payload":{"next_turn":"test_review","notes":{"done":["..."],"verification":["..."]}}}`

{contract}
