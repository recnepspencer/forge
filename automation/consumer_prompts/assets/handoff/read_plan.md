## Cross-provider handoff (oversight -> build)

Your plan was authored by the planner on a different provider session, so it is
NOT in your chat history. Before editing, READ the plan artifact and treat it as
the working plan:

- Path: `.runner-handoff/phase-{phase.id}/plan.md`
- If it is missing or clearly stale versus the current code, rebuild the missing
  part inline in chat first, then proceed.
