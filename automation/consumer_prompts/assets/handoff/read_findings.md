## Cross-provider handoff (oversight -> build)

The review findings were written by the reviewer on a different provider
session, so they are NOT in your chat history. The projection summary is only a
pointer. READ the findings artifact as the artifact of record:

- Path: `.runner-handoff/phase-{phase.id}/findings.md`
- If it is missing, reconstruct the blocking set from the projection summary and
  the current code state before repairing.
