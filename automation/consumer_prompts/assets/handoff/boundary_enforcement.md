## Constitution enforcement (mandatory)

Before completing this boundary review, run the machine enforcement and treat
any diagnostic as a blocking finding:

- `cargo run --manifest-path tools/boundary-check/Cargo.toml -- --root .`
- `cargo run --manifest-path tools/agent-context/Cargo.toml -- check`

A red constitution blocks the phase. Cite the exact diagnostic codes in your
boundary brief. Do not advance on convention — the machine contract at
`tools/boundary-check/config/road1.toml` is authoritative.
